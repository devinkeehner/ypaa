import { C as require_react, O as __commonJSMin, j as __toESM$1, n as require_jsx_runtime, t as require_react_dom } from "../index.js";
import { t as v4$1 } from "./v4-DiJ-vc2V.js";
import { t as createLucideIcon$1 } from "./createLucideIcon-hDPz0_4O.js";
import { n as CalendarDays } from "./program-types-CCKzS0sz.js";
import { t as ChevronRight$1 } from "./chevron-right-iKKN5M3j.js";
import { t as Check } from "./check-BxL9AktT.js";
import { i as MapPin, t as ProgramExplorer } from "./ProgramExplorer-gXc6Yfz3.js";
import * as m from "crypto";
//#region node_modules/@puckeditor/core/dist/chunk-M6W7YEVX.mjs
var import_react = /* @__PURE__ */ __toESM$1(require_react(), 1);
var __create$6 = Object.create;
var __defProp$24 = Object.defineProperty;
var __defProps$6 = Object.defineProperties;
var __getOwnPropDesc$6 = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs$6 = Object.getOwnPropertyDescriptors;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropSymbols$7 = Object.getOwnPropertySymbols;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp$7 = Object.prototype.hasOwnProperty;
var __propIsEnum$7 = Object.prototype.propertyIsEnumerable;
var __defNormalProp$7 = (obj, key, value) => key in obj ? __defProp$24(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues$7 = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp$7.call(b, prop)) __defNormalProp$7(a, prop, b[prop]);
	if (__getOwnPropSymbols$7) {
		for (var prop of __getOwnPropSymbols$7(b)) if (__propIsEnum$7.call(b, prop)) __defNormalProp$7(a, prop, b[prop]);
	}
	return a;
};
var __spreadProps$6 = (a, b) => __defProps$6(a, __getOwnPropDescs$6(b));
var __objRest$4 = (source, exclude) => {
	var target = {};
	for (var prop in source) if (__hasOwnProp$7.call(source, prop) && exclude.indexOf(prop) < 0) target[prop] = source[prop];
	if (source != null && __getOwnPropSymbols$7) {
		for (var prop of __getOwnPropSymbols$7(source)) if (exclude.indexOf(prop) < 0 && __propIsEnum$7.call(source, prop)) target[prop] = source[prop];
	}
	return target;
};
var __esm = (fn, res) => function __init() {
	return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require() {
	return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") {
		for (let key of __getOwnPropNames(from)) if (!__hasOwnProp$7.call(to, key) && key !== except) __defProp$24(to, key, {
			get: () => from[key],
			enumerable: !(desc = __getOwnPropDesc$6(from, key)) || desc.enumerable
		});
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create$6(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp$24(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));
var __async = (__this, __arguments, generator) => {
	return new Promise((resolve, reject) => {
		var fulfilled = (value) => {
			try {
				step(generator.next(value));
			} catch (e) {
				reject(e);
			}
		};
		var rejected = (value) => {
			try {
				step(generator.throw(value));
			} catch (e) {
				reject(e);
			}
		};
		var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
		step((generator = generator.apply(__this, __arguments)).next());
	});
};
var init_react_import = __esm({ "../tsup-config/react-import.js"() {
	"use strict";
} });
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-AOEDIUVK.mjs
init_react_import();
var styles_module_default$3 = {
	"RichTextEditor": "_RichTextEditor_z25h4_1",
	"RichTextEditor--editor": "_RichTextEditor--editor_z25h4_50",
	"RichTextEditor--disabled": "_RichTextEditor--disabled_z25h4_107",
	"RichTextEditor--isActive": "_RichTextEditor--isActive_z25h4_111",
	"RichTextEditor-menu": "_RichTextEditor-menu_z25h4_117"
};
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-SHVCBQDY.mjs
var import_flat = /* @__PURE__ */ __toESM$1((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = flatten;
	flatten.flatten = flatten;
	flatten.unflatten = unflatten;
	function isBuffer(obj) {
		return obj && obj.constructor && typeof obj.constructor.isBuffer === "function" && obj.constructor.isBuffer(obj);
	}
	function keyIdentity(key) {
		return key;
	}
	function flatten(target, opts) {
		opts = opts || {};
		const delimiter = opts.delimiter || ".";
		const maxDepth = opts.maxDepth;
		const transformKey = opts.transformKey || keyIdentity;
		const output = {};
		function step(object, prev, currentDepth) {
			currentDepth = currentDepth || 1;
			Object.keys(object).forEach(function(key) {
				const value = object[key];
				const isarray = opts.safe && Array.isArray(value);
				const type = Object.prototype.toString.call(value);
				const isbuffer = isBuffer(value);
				const isobject = type === "[object Object]" || type === "[object Array]";
				const newKey = prev ? prev + delimiter + transformKey(key) : transformKey(key);
				if (!isarray && !isbuffer && isobject && Object.keys(value).length && (!opts.maxDepth || currentDepth < maxDepth)) return step(value, newKey, currentDepth + 1);
				output[newKey] = value;
			});
		}
		step(target);
		return output;
	}
	function unflatten(target, opts) {
		opts = opts || {};
		const delimiter = opts.delimiter || ".";
		const overwrite = opts.overwrite || false;
		const transformKey = opts.transformKey || keyIdentity;
		const result = {};
		if (isBuffer(target) || Object.prototype.toString.call(target) !== "[object Object]") return target;
		function getkey(key) {
			const parsedKey = Number(key);
			return isNaN(parsedKey) || key.indexOf(".") !== -1 || opts.object ? key : parsedKey;
		}
		function addKeys(keyPrefix, recipient, target) {
			return Object.keys(target).reduce(function(result, key) {
				result[keyPrefix + delimiter + key] = target[key];
				return result;
			}, recipient);
		}
		function isEmpty(val) {
			const type = Object.prototype.toString.call(val);
			const isArray = type === "[object Array]";
			const isObject = type === "[object Object]";
			if (!val) return true;
			else if (isArray) return !val.length;
			else if (isObject) return !Object.keys(val).length;
		}
		target = Object.keys(target).reduce(function(result, key) {
			const type = Object.prototype.toString.call(target[key]);
			if (!(type === "[object Object]" || type === "[object Array]") || isEmpty(target[key])) {
				result[key] = target[key];
				return result;
			} else return addKeys(key, result, flatten(target[key], opts));
		}, {});
		Object.keys(target).forEach(function(key) {
			const split = key.split(delimiter).map(transformKey);
			let key1 = getkey(split.shift());
			let key2 = getkey(split[0]);
			let recipient = result;
			while (key2 !== void 0) {
				if (key1 === "__proto__") return;
				const type = Object.prototype.toString.call(recipient[key1]);
				const isobject = type === "[object Object]" || type === "[object Array]";
				if (!overwrite && !isobject && typeof recipient[key1] !== "undefined") return;
				if (overwrite && !isobject || !overwrite && recipient[key1] == null) recipient[key1] = typeof key2 === "number" && !opts.object ? [] : {};
				recipient = recipient[key1];
				if (split.length > 0) {
					key1 = getkey(split.shift());
					key2 = getkey(split[0]);
				}
			}
			recipient[key1] = unflatten(target[key], opts);
		});
		return result;
	}
})))(), 1);
init_react_import();
init_react_import();
init_react_import();
var defaultSlots = (value, fields) => Object.keys(fields).reduce((acc, fieldName) => fields[fieldName].type === "slot" ? __spreadValues$7({ [fieldName]: [] }, acc) : acc, value);
var isPromise = (v) => !!v && typeof v.then === "function";
var flatten = (values) => values.reduce((acc, item) => __spreadValues$7(__spreadValues$7({}, acc), item), {});
var containsPromise = (arr) => arr.some(isPromise);
var walkField = ({ value, fields, mappers, propKey = "", propPath = "", id = "", config, recurseSlots = false }) => {
	var _a, _b, _c;
	const fieldType = (_a = fields[propKey]) == null ? void 0 : _a.type;
	const map = mappers[fieldType];
	if (map && fieldType === "slot") {
		const content = value || [];
		const mappedContent = recurseSlots ? content.map((el) => {
			if (!config.components[el.type]) throw new Error(`Could not find component config for ${el.type}`);
			return mapFields(el, mappers, config, recurseSlots);
		}) : content;
		if (containsPromise(mappedContent)) return Promise.all(mappedContent);
		return map({
			value: mappedContent,
			parentId: id,
			propName: propPath,
			field: fields[propKey],
			propPath
		});
	} else if (map && fields[propKey]) return map({
		value,
		parentId: id,
		propName: propKey,
		field: fields[propKey],
		propPath
	});
	if (value && typeof value === "object") if (Array.isArray(value)) {
		const arrayFields = ((_b = fields[propKey]) == null ? void 0 : _b.type) === "array" ? fields[propKey].arrayFields : null;
		if (!arrayFields) return value;
		const newValue = value.map((el, idx) => walkField({
			value: el,
			fields: arrayFields,
			mappers,
			propKey,
			propPath: `${propPath}[${idx}]`,
			id,
			config,
			recurseSlots
		}));
		if (containsPromise(newValue)) return Promise.all(newValue);
		return newValue;
	} else if ("$$typeof" in value) return value;
	else return walkObject({
		value,
		fields: ((_c = fields[propKey]) == null ? void 0 : _c.type) === "object" ? fields[propKey].objectFields : fields,
		mappers,
		id,
		getPropPath: (k) => `${propPath}.${k}`,
		config,
		recurseSlots
	});
	return value;
};
var walkObject = ({ value, fields, mappers, id, getPropPath, config, recurseSlots }) => {
	const newProps = Object.entries(value).map(([k, v]) => {
		const newValue = walkField({
			value: v,
			fields,
			mappers,
			propKey: k,
			propPath: getPropPath(k),
			id,
			config,
			recurseSlots
		});
		if (isPromise(newValue)) return newValue.then((resolvedValue) => ({ [k]: resolvedValue }));
		return { [k]: newValue };
	}, {});
	if (containsPromise(newProps)) return Promise.all(newProps).then(flatten);
	return flatten(newProps);
};
function mapFields(item, mappers, config, recurseSlots = false, shouldDefaultSlots = true) {
	var _a, _b, _c, _d, _e;
	const itemType = "type" in item ? item.type : "root";
	const componentConfig = itemType === "root" ? config.root : (_a = config.components) == null ? void 0 : _a[itemType];
	const newProps = walkObject({
		value: shouldDefaultSlots ? defaultSlots((_b = item.props) != null ? _b : {}, (_c = componentConfig == null ? void 0 : componentConfig.fields) != null ? _c : {}) : item.props,
		fields: (_d = componentConfig == null ? void 0 : componentConfig.fields) != null ? _d : {},
		mappers,
		id: item.props ? (_e = item.props.id) != null ? _e : "root" : "root",
		getPropPath: (k) => k,
		config,
		recurseSlots
	});
	if (isPromise(newProps)) return newProps.then((resolvedProps) => __spreadProps$6(__spreadValues$7({}, item), { props: resolvedProps }));
	return __spreadProps$6(__spreadValues$7({}, item), { props: newProps });
}
function walkTree(data, config, callbackFn) {
	var _a, _b;
	const walkItem = (item) => {
		return mapFields(item, { slot: ({ value, parentId, propName }) => {
			var _a2;
			const content = value;
			return (_a2 = callbackFn(content, {
				parentId,
				propName
			})) != null ? _a2 : content;
		} }, config, true);
	};
	if ("props" in data) return walkItem(data);
	const _data = data;
	const zones = (_a = _data.zones) != null ? _a : {};
	const mappedContent = _data.content.map(walkItem);
	return {
		root: walkItem(_data.root),
		content: (_b = callbackFn(mappedContent, {
			parentId: "root",
			propName: "default-zone"
		})) != null ? _b : mappedContent,
		zones: Object.keys(zones).reduce((acc, zoneCompound) => __spreadProps$6(__spreadValues$7({}, acc), { [zoneCompound]: zones[zoneCompound].map(walkItem) }), {})
	};
}
init_react_import();
var rootAreaId = "root";
var rootZone = "default-zone";
var rootDroppableId = `${rootAreaId}:${rootZone}`;
init_react_import();
init_react_import();
var stripSlots = (data, config) => {
	return mapFields(data, { slot: () => null }, config);
};
var { flatten: flatten2, unflatten } = import_flat.default;
var isPureObject = (val) => val != null && Object.prototype.toString.call(val) === "[object Object]";
var emptyArrayStr = "__puck_[]";
var emptyObjectStr = "__puck_{}";
function encodeEmptyObjects(props = {}) {
	const result = {};
	for (const key in props) {
		if (!Object.prototype.hasOwnProperty.call(props, key)) continue;
		const val = props[key];
		if (Array.isArray(val) && val.length === 0) result[key] = emptyArrayStr;
		else if (isPureObject(val) && Object.keys(val).length === 0) result[key] = emptyObjectStr;
		else result[key] = val;
	}
	return result;
}
function decodeEmptyObjects(props = {}) {
	const result = {};
	for (const key in props) {
		if (!Object.prototype.hasOwnProperty.call(props, key)) continue;
		const val = props[key];
		if (val === emptyArrayStr) result[key] = [];
		else if (val === emptyObjectStr) result[key] = {};
		else result[key] = val;
	}
	return result;
}
var flattenNode = (node, config) => {
	return __spreadProps$6(__spreadValues$7({}, node), { props: encodeEmptyObjects(flatten2(stripSlots(node, config).props)) });
};
var expandNode = (node) => {
	const props = unflatten(decodeEmptyObjects(node.props));
	return __spreadProps$6(__spreadValues$7({}, node), { props });
};
init_react_import();
var toComponent = (item) => {
	return "type" in item ? item : __spreadProps$6(__spreadValues$7({}, item), {
		props: __spreadProps$6(__spreadValues$7({}, item.props), { id: "root" }),
		type: "root"
	});
};
init_react_import();
init_react_import();
init_react_import();
var getZoneId$1 = (zoneCompound) => {
	if (!zoneCompound) return [];
	if (zoneCompound && zoneCompound.indexOf(":") > -1) return zoneCompound.split(":");
	return [rootDroppableId, zoneCompound];
};
function forRelatedZones(item, data, cb, path = []) {
	Object.entries(data.zones || {}).forEach(([zoneCompound, content]) => {
		const [parentId] = getZoneId$1(zoneCompound);
		if (parentId === item.props.id) cb(path, zoneCompound, content);
	});
}
function walkAppState(state, config, mapContent = (content) => content, mapNodeOrSkip = (item) => item) {
	var _a;
	let newZones = {};
	const newZoneIndex = {};
	const newNodeIndex = {};
	const processContent = (path, zoneCompound, content, zoneType, newId) => {
		var _a2;
		const [parentId] = zoneCompound.split(":");
		const mappedContent = ((_a2 = mapContent(content, zoneCompound, zoneType)) != null ? _a2 : content) || [];
		const [_2, zone] = zoneCompound.split(":");
		const newZoneCompound = `${newId || parentId}:${zone}`;
		const newContent2 = mappedContent.map((zoneChild, index) => processItem(zoneChild, [...path, newZoneCompound], index));
		newZoneIndex[newZoneCompound] = {
			contentIds: newContent2.map((item) => item.props.id),
			type: zoneType
		};
		return [newZoneCompound, newContent2];
	};
	const processRelatedZones = (item, newId, initialPath) => {
		forRelatedZones(item, state.data, (relatedPath, relatedZoneCompound, relatedContent) => {
			const [zoneCompound, newContent2] = processContent(relatedPath, relatedZoneCompound, relatedContent, "dropzone", newId);
			newZones[zoneCompound] = newContent2;
		}, initialPath);
	};
	const processItem = (item, path, index) => {
		const mappedItem = mapNodeOrSkip(item, path, index);
		if (!mappedItem) return item;
		const id = mappedItem.props.id;
		const newProps = __spreadProps$6(__spreadValues$7({}, mapFields(mappedItem, { slot: ({ value, parentId: parentId2, propPath }) => {
			const content = value;
			const [_2, newContent2] = processContent(path, `${parentId2}:${propPath}`, content, "slot", parentId2);
			return newContent2;
		} }, config).props), { id });
		processRelatedZones(item, id, path);
		const newItem = __spreadProps$6(__spreadValues$7({}, mappedItem), { props: newProps });
		const thisZoneCompound = path[path.length - 1];
		const [parentId, zone] = thisZoneCompound ? thisZoneCompound.split(":") : [null, ""];
		newNodeIndex[id] = {
			data: newItem,
			flatData: flattenNode(newItem, config),
			path,
			parentId,
			zone
		};
		const finalData = __spreadProps$6(__spreadValues$7({}, newItem), { props: __spreadValues$7({}, newItem.props) });
		if (newProps.id === "root") {
			delete finalData["type"];
			delete finalData.props["id"];
		}
		return finalData;
	};
	const zones = state.data.zones || {};
	const [_, newContent] = processContent([], rootDroppableId, state.data.content, "root");
	const processedContent = newContent;
	const zonesAlreadyProcessed = Object.keys(newZones);
	Object.keys(zones || {}).forEach((zoneCompound) => {
		const [parentId] = zoneCompound.split(":");
		if (zonesAlreadyProcessed.includes(zoneCompound)) return;
		const [_2, newContent2] = processContent([rootDroppableId], zoneCompound, zones[zoneCompound], "dropzone", parentId);
		newZones[zoneCompound] = newContent2;
	}, newZones);
	let rootAsComponent = toComponent({ props: __spreadValues$7({}, (_a = state.data.root.props) != null ? _a : state.data.root) });
	if (state.data.root.readOnly) rootAsComponent.readOnly = state.data.root.readOnly;
	const processedRoot = processItem(rootAsComponent, [], -1);
	const root = __spreadValues$7(__spreadValues$7({}, state.data.root), processedRoot);
	return __spreadProps$6(__spreadValues$7({}, state), {
		data: {
			root,
			content: processedContent,
			zones: __spreadValues$7(__spreadValues$7({}, state.data.zones), newZones)
		},
		indexes: {
			nodes: __spreadValues$7(__spreadValues$7({}, state.indexes.nodes), newNodeIndex),
			zones: __spreadValues$7(__spreadValues$7({}, state.indexes.zones), newZoneIndex)
		}
	});
}
init_react_import();
var setupZone = (data, zoneKey) => {
	if (zoneKey === rootDroppableId) return data;
	const newData = __spreadProps$6(__spreadValues$7({}, data), { zones: data.zones ? __spreadValues$7({}, data.zones) : {} });
	newData.zones[zoneKey] = newData.zones[zoneKey] || [];
	return newData;
};
//#endregion
//#region node_modules/@puckeditor/core/node_modules/uuid/dist/esm-browser/rng.js
var getRandomValues;
var rnds8 = new Uint8Array(16);
function rng() {
	if (!getRandomValues) {
		getRandomValues = typeof crypto !== "undefined" && crypto.getRandomValues && crypto.getRandomValues.bind(crypto);
		if (!getRandomValues) throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
	}
	return getRandomValues(rnds8);
}
//#endregion
//#region node_modules/@puckeditor/core/node_modules/uuid/dist/esm-browser/stringify.js
/**
* Convert array of 16 byte values to UUID string format of the form:
* XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX
*/
var byteToHex = [];
for (let i = 0; i < 256; ++i) byteToHex.push((i + 256).toString(16).slice(1));
function unsafeStringify(arr, offset = 0) {
	return byteToHex[arr[offset + 0]] + byteToHex[arr[offset + 1]] + byteToHex[arr[offset + 2]] + byteToHex[arr[offset + 3]] + "-" + byteToHex[arr[offset + 4]] + byteToHex[arr[offset + 5]] + "-" + byteToHex[arr[offset + 6]] + byteToHex[arr[offset + 7]] + "-" + byteToHex[arr[offset + 8]] + byteToHex[arr[offset + 9]] + "-" + byteToHex[arr[offset + 10]] + byteToHex[arr[offset + 11]] + byteToHex[arr[offset + 12]] + byteToHex[arr[offset + 13]] + byteToHex[arr[offset + 14]] + byteToHex[arr[offset + 15]];
}
var native_default = { randomUUID: typeof crypto !== "undefined" && crypto.randomUUID && crypto.randomUUID.bind(crypto) };
//#endregion
//#region node_modules/@puckeditor/core/node_modules/uuid/dist/esm-browser/v4.js
function v4(options, buf, offset) {
	if (native_default.randomUUID && !buf && !options) return native_default.randomUUID();
	options = options || {};
	const rnds = options.random || (options.rng || rng)();
	rnds[6] = rnds[6] & 15 | 64;
	rnds[8] = rnds[8] & 63 | 128;
	if (buf) {
		offset = offset || 0;
		for (let i = 0; i < 16; ++i) buf[offset + i] = rnds[i];
		return buf;
	}
	return unsafeStringify(rnds);
}
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-6XCJ3Y7O.mjs
init_react_import();
init_react_import();
var setAction = (state, action, appStore) => {
	if (typeof action.state === "object") {
		const newState = __spreadValues$7(__spreadValues$7({}, state), action.state);
		if (action.state.indexes) return newState;
		console.warn("`set` is expensive and may cause unnecessary re-renders. Consider using a more atomic action instead.");
		return walkAppState(newState, appStore.config);
	}
	return __spreadValues$7(__spreadValues$7({}, state), action.state(state));
};
init_react_import();
init_react_import();
var insert = (list, index, item) => {
	const result = Array.from(list || []);
	result.splice(index, 0, item);
	return result;
};
init_react_import();
var generateId = (type) => type ? `${type}-${v4()}` : v4();
init_react_import();
var getIdsForParent = (zoneCompound, state) => {
	const [parentId] = zoneCompound.split(":");
	const node = state.indexes.nodes[parentId];
	return ((node == null ? void 0 : node.path) || []).map((p) => p.split(":")[0]);
};
init_react_import();
var populateIds = (data, config, override = false) => {
	const id = generateId(data.type);
	return walkTree(__spreadProps$6(__spreadValues$7({}, data), { props: override ? __spreadProps$6(__spreadValues$7({}, data.props), { id }) : __spreadValues$7({}, data.props) }), config, (contents) => contents.map((item) => {
		const id2 = generateId(item.type);
		return __spreadProps$6(__spreadValues$7({}, item), { props: override ? __spreadProps$6(__spreadValues$7({}, item.props), { id: id2 }) : __spreadValues$7({ id: id2 }, item.props) });
	}));
};
function insertAction(state, action, appStore) {
	const id = action.id || generateId(action.componentType);
	const emptyComponentData = populateIds({
		type: action.componentType,
		props: __spreadProps$6(__spreadValues$7({}, appStore.config.components[action.componentType].defaultProps || {}), { id })
	}, appStore.config);
	const [parentId] = action.destinationZone.split(":");
	const idsInPath = getIdsForParent(action.destinationZone, state);
	return walkAppState(state, appStore.config, (content, zoneCompound) => {
		if (zoneCompound === action.destinationZone) return insert(content || [], action.destinationIndex, emptyComponentData);
		return content;
	}, (childItem, path) => {
		if (childItem.props.id === id || childItem.props.id === parentId) return childItem;
		else if (idsInPath.includes(childItem.props.id)) return childItem;
		else if (path.includes(action.destinationZone)) return childItem;
		return null;
	});
}
init_react_import();
var replaceAction = (state, action, appStore) => {
	const [parentId] = action.destinationZone.split(":");
	const idsInPath = getIdsForParent(action.destinationZone, state);
	const originalId = state.indexes.zones[action.destinationZone].contentIds[action.destinationIndex];
	if (originalId !== action.data.props.id) throw new Error(`Can't change the id during a replace action. Please us "remove" and "insert" to define a new node.`);
	const newSlotIds = [];
	const data = walkTree(action.data, appStore.config, (contents, opts) => {
		newSlotIds.push(`${opts.parentId}:${opts.propName}`);
		return contents.map((item) => {
			const id = generateId(item.type);
			return __spreadProps$6(__spreadValues$7({}, item), { props: __spreadValues$7({ id }, item.props) });
		});
	});
	const stateWithDeepSlotsRemoved = __spreadProps$6(__spreadValues$7({}, state), { ui: __spreadValues$7(__spreadValues$7({}, state.ui), action.ui) });
	Object.keys(state.indexes.zones).forEach((zoneCompound) => {
		if (zoneCompound.split(":")[0] === originalId) {
			if (!newSlotIds.includes(zoneCompound)) delete stateWithDeepSlotsRemoved.indexes.zones[zoneCompound];
		}
	});
	return walkAppState(stateWithDeepSlotsRemoved, appStore.config, (content, zoneCompound) => {
		const newContent = [...content];
		if (zoneCompound === action.destinationZone) newContent[action.destinationIndex] = data;
		return newContent;
	}, (childItem, path) => {
		const pathIds = path.map((p) => p.split(":")[0]);
		if (childItem.props.id === data.props.id) return data;
		else if (childItem.props.id === parentId) return childItem;
		else if (idsInPath.indexOf(childItem.props.id) > -1) return childItem;
		else if (pathIds.indexOf(data.props.id) > -1) return childItem;
		return null;
	});
};
init_react_import();
var replaceRootAction = (state, action, appStore) => {
	return walkAppState(state, appStore.config, (content) => content, (childItem) => {
		if (childItem.props.id === "root") return __spreadProps$6(__spreadValues$7({}, childItem), {
			props: __spreadValues$7(__spreadValues$7({}, childItem.props), action.root.props),
			readOnly: action.root.readOnly
		});
		return childItem;
	});
};
init_react_import();
init_react_import();
function getItem(selector, state) {
	var _a, _b;
	const zone = (_a = state.indexes.zones) == null ? void 0 : _a[selector.zone || rootDroppableId];
	return zone ? (_b = state.indexes.nodes[zone.contentIds[selector.index]]) == null ? void 0 : _b.data : void 0;
}
function duplicateAction(state, action, appStore) {
	const item = getItem({
		index: action.sourceIndex,
		zone: action.sourceZone
	}, state);
	const idsInPath = getIdsForParent(action.sourceZone, state);
	const newItem = __spreadProps$6(__spreadValues$7({}, item), { props: __spreadProps$6(__spreadValues$7({}, item.props), { id: generateId(item.type) }) });
	const modified = walkAppState(state, appStore.config, (content, zoneCompound) => {
		if (zoneCompound === action.sourceZone) return insert(content, action.sourceIndex + 1, item);
		return content;
	}, (childItem, path, index) => {
		const zoneCompound = path[path.length - 1];
		if (path.map((p) => p.split(":")[0]).indexOf(newItem.props.id) > -1) return __spreadProps$6(__spreadValues$7({}, childItem), { props: __spreadProps$6(__spreadValues$7({}, childItem.props), { id: generateId(childItem.type) }) });
		if (zoneCompound === action.sourceZone && index === action.sourceIndex + 1) return newItem;
		const [sourceZoneParent] = action.sourceZone.split(":");
		if (sourceZoneParent === childItem.props.id || idsInPath.indexOf(childItem.props.id) > -1) return childItem;
		return null;
	});
	return __spreadProps$6(__spreadValues$7({}, modified), { ui: __spreadProps$6(__spreadValues$7({}, modified.ui), { itemSelector: {
		index: action.sourceIndex + 1,
		zone: action.sourceZone
	} }) });
}
init_react_import();
init_react_import();
init_react_import();
var remove = (list, index) => {
	const result = Array.from(list);
	result.splice(index, 1);
	return result;
};
var moveAction = (state, action, appStore) => {
	if (action.sourceZone === action.destinationZone && action.sourceIndex === action.destinationIndex) return state;
	const item = getItem({
		zone: action.sourceZone,
		index: action.sourceIndex
	}, state);
	if (!item) return state;
	const idsInSourcePath = getIdsForParent(action.sourceZone, state);
	const idsInDestinationPath = getIdsForParent(action.destinationZone, state);
	return walkAppState(state, appStore.config, (content, zoneCompound) => {
		if (zoneCompound === action.sourceZone && zoneCompound === action.destinationZone) return insert(remove(content, action.sourceIndex), action.destinationIndex, item);
		else if (zoneCompound === action.sourceZone) return remove(content, action.sourceIndex);
		else if (zoneCompound === action.destinationZone) return insert(content, action.destinationIndex, item);
		return content;
	}, (childItem, path) => {
		const [sourceZoneParent] = action.sourceZone.split(":");
		const [destinationZoneParent] = action.destinationZone.split(":");
		const childId = childItem.props.id;
		if (sourceZoneParent === childId || destinationZoneParent === childId || item.props.id === childId || idsInSourcePath.indexOf(childId) > -1 || idsInDestinationPath.indexOf(childId) > -1 || path.includes(action.destinationZone)) return childItem;
		return null;
	});
};
var reorderAction = (state, action, appStore) => {
	return moveAction(state, {
		type: "move",
		sourceIndex: action.sourceIndex,
		sourceZone: action.destinationZone,
		destinationIndex: action.destinationIndex,
		destinationZone: action.destinationZone
	}, appStore);
};
init_react_import();
var removeAction = (state, action, appStore) => {
	const item = getItem({
		index: action.index,
		zone: action.zone
	}, state);
	const nodesToDelete = Object.entries(state.indexes.nodes).reduce((acc, [nodeId, nodeData]) => {
		if (nodeData.path.map((p) => p.split(":")[0]).includes(item.props.id)) return [...acc, nodeId];
		return acc;
	}, [item.props.id]);
	const newState = walkAppState(state, appStore.config, (content, zoneCompound) => {
		if (zoneCompound === action.zone) return remove(content, action.index);
		return content;
	});
	Object.keys(newState.data.zones || {}).forEach((zoneCompound) => {
		const parentId = zoneCompound.split(":")[0];
		if (nodesToDelete.includes(parentId) && newState.data.zones) delete newState.data.zones[zoneCompound];
	});
	Object.keys(newState.indexes.zones).forEach((zoneCompound) => {
		const parentId = zoneCompound.split(":")[0];
		if (nodesToDelete.includes(parentId)) delete newState.indexes.zones[zoneCompound];
	});
	nodesToDelete.forEach((id) => {
		delete newState.indexes.nodes[id];
	});
	return newState;
};
init_react_import();
var zoneCache = {};
function registerZoneAction(state, action) {
	if (zoneCache[action.zone]) return __spreadProps$6(__spreadValues$7({}, state), {
		data: __spreadProps$6(__spreadValues$7({}, state.data), { zones: __spreadProps$6(__spreadValues$7({}, state.data.zones), { [action.zone]: zoneCache[action.zone] }) }),
		indexes: __spreadProps$6(__spreadValues$7({}, state.indexes), { zones: __spreadProps$6(__spreadValues$7({}, state.indexes.zones), { [action.zone]: __spreadProps$6(__spreadValues$7({}, state.indexes.zones[action.zone]), {
			contentIds: zoneCache[action.zone].map((item) => item.props.id),
			type: "dropzone"
		}) }) })
	});
	return __spreadProps$6(__spreadValues$7({}, state), { data: setupZone(state.data, action.zone) });
}
function unregisterZoneAction(state, action) {
	const _zones = __spreadValues$7({}, state.data.zones || {});
	const zoneIndex = __spreadValues$7({}, state.indexes.zones || {});
	if (_zones[action.zone]) {
		zoneCache[action.zone] = _zones[action.zone];
		delete _zones[action.zone];
	}
	delete zoneIndex[action.zone];
	return __spreadProps$6(__spreadValues$7({}, state), {
		data: __spreadProps$6(__spreadValues$7({}, state.data), { zones: _zones }),
		indexes: __spreadProps$6(__spreadValues$7({}, state.indexes), { zones: zoneIndex })
	});
}
init_react_import();
var setDataAction = (state, action, appStore) => {
	if (typeof action.data === "object") {
		console.warn("`setData` is expensive and may cause unnecessary re-renders. Consider using a more atomic action instead.");
		return walkAppState(__spreadProps$6(__spreadValues$7({}, state), { data: __spreadValues$7(__spreadValues$7({}, state.data), action.data) }), appStore.config);
	}
	return walkAppState(__spreadProps$6(__spreadValues$7({}, state), { data: __spreadValues$7(__spreadValues$7({}, state.data), action.data(state.data)) }), appStore.config);
};
init_react_import();
var setUiAction = (state, action) => {
	if (typeof action.ui === "object") return __spreadProps$6(__spreadValues$7({}, state), { ui: __spreadValues$7(__spreadValues$7({}, state.ui), action.ui) });
	return __spreadProps$6(__spreadValues$7({}, state), { ui: __spreadValues$7(__spreadValues$7({}, state.ui), action.ui(state.ui)) });
};
init_react_import();
var makeStatePublic = (state) => {
	const { data, ui } = state;
	return {
		data,
		ui
	};
};
init_react_import();
function storeInterceptor(reducer, record, onAction) {
	return (state, action) => {
		const newAppState = reducer(state, action);
		const isValidType = ![
			"registerZone",
			"unregisterZone",
			"setData",
			"setUi",
			"set"
		].includes(action.type);
		if (typeof action.recordHistory !== "undefined" ? action.recordHistory : isValidType) {
			if (record) record(newAppState);
		}
		onAction?.(action, makeStatePublic(newAppState), makeStatePublic(state));
		return newAppState;
	};
}
function createReducer({ record, onAction, appStore }) {
	return storeInterceptor((state, action) => {
		if (action.type === "set") return setAction(state, action, appStore);
		if (action.type === "insert") return insertAction(state, action, appStore);
		if (action.type === "replace") return replaceAction(state, action, appStore);
		if (action.type === "replaceRoot") return replaceRootAction(state, action, appStore);
		if (action.type === "duplicate") return duplicateAction(state, action, appStore);
		if (action.type === "reorder") return reorderAction(state, action, appStore);
		if (action.type === "move") return moveAction(state, action, appStore);
		if (action.type === "remove") return removeAction(state, action, appStore);
		if (action.type === "registerZone") return registerZoneAction(state, action);
		if (action.type === "unregisterZone") return unregisterZoneAction(state, action);
		if (action.type === "setData") return setDataAction(state, action, appStore);
		if (action.type === "setUi") return setUiAction(state, action);
		return state;
	}, record, onAction);
}
//#endregion
//#region node_modules/fast-equals/dist/esm/index.mjs
var getOwnPropertyNames = Object.getOwnPropertyNames, getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
/**
* Combine two comparators into a single comparators.
*/
function combineComparators(comparatorA, comparatorB) {
	return function isEqual(a, b, state) {
		return comparatorA(a, b, state) && comparatorB(a, b, state);
	};
}
/**
* Wrap the provided `areItemsEqual` method to manage the circular state, allowing
* for circular references to be safely included in the comparison without creating
* stack overflows.
*/
function createIsCircular(areItemsEqual) {
	return function isCircular(a, b, state) {
		if (!a || !b || typeof a !== "object" || typeof b !== "object") return areItemsEqual(a, b, state);
		var cache = state.cache;
		var cachedA = cache.get(a);
		var cachedB = cache.get(b);
		if (cachedA && cachedB) return cachedA === b && cachedB === a;
		cache.set(a, b);
		cache.set(b, a);
		var result = areItemsEqual(a, b, state);
		cache.delete(a);
		cache.delete(b);
		return result;
	};
}
/**
* Get the properties to strictly examine, which include both own properties that are
* not enumerable and symbol properties.
*/
function getStrictProperties(object) {
	return getOwnPropertyNames(object).concat(getOwnPropertySymbols(object));
}
/**
* Whether the object contains the property passed as an own property.
*/
var hasOwn = Object.hasOwn || (function(object, property) {
	return hasOwnProperty.call(object, property);
});
/**
* Whether the values passed are strictly equal or both NaN.
*/
function sameValueZeroEqual(a, b) {
	return a === b || !a && !b && a !== a && b !== b;
}
var PREACT_VNODE = "__v";
var PREACT_OWNER = "__o";
var REACT_OWNER = "_owner";
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor, keys = Object.keys;
/**
* Whether the arrays are equal in value.
*/
function areArraysEqual(a, b, state) {
	var index = a.length;
	if (b.length !== index) return false;
	while (index-- > 0) if (!state.equals(a[index], b[index], index, index, a, b, state)) return false;
	return true;
}
/**
* Whether the dates passed are equal in value.
*/
function areDatesEqual(a, b) {
	return sameValueZeroEqual(a.getTime(), b.getTime());
}
/**
* Whether the errors passed are equal in value.
*/
function areErrorsEqual(a, b) {
	return a.name === b.name && a.message === b.message && a.cause === b.cause && a.stack === b.stack;
}
/**
* Whether the functions passed are equal in value.
*/
function areFunctionsEqual(a, b) {
	return a === b;
}
/**
* Whether the `Map`s are equal in value.
*/
function areMapsEqual(a, b, state) {
	var size = a.size;
	if (size !== b.size) return false;
	if (!size) return true;
	var matchedIndices = new Array(size);
	var aIterable = a.entries();
	var aResult;
	var bResult;
	var index = 0;
	while (aResult = aIterable.next()) {
		if (aResult.done) break;
		var bIterable = b.entries();
		var hasMatch = false;
		var matchIndex = 0;
		while (bResult = bIterable.next()) {
			if (bResult.done) break;
			if (matchedIndices[matchIndex]) {
				matchIndex++;
				continue;
			}
			var aEntry = aResult.value;
			var bEntry = bResult.value;
			if (state.equals(aEntry[0], bEntry[0], index, matchIndex, a, b, state) && state.equals(aEntry[1], bEntry[1], aEntry[0], bEntry[0], a, b, state)) {
				hasMatch = matchedIndices[matchIndex] = true;
				break;
			}
			matchIndex++;
		}
		if (!hasMatch) return false;
		index++;
	}
	return true;
}
/**
* Whether the numbers are equal in value.
*/
var areNumbersEqual = sameValueZeroEqual;
/**
* Whether the objects are equal in value.
*/
function areObjectsEqual(a, b, state) {
	var properties = keys(a);
	var index = properties.length;
	if (keys(b).length !== index) return false;
	while (index-- > 0) if (!isPropertyEqual(a, b, state, properties[index])) return false;
	return true;
}
/**
* Whether the objects are equal in value with strict property checking.
*/
function areObjectsEqualStrict(a, b, state) {
	var properties = getStrictProperties(a);
	var index = properties.length;
	if (getStrictProperties(b).length !== index) return false;
	var property;
	var descriptorA;
	var descriptorB;
	while (index-- > 0) {
		property = properties[index];
		if (!isPropertyEqual(a, b, state, property)) return false;
		descriptorA = getOwnPropertyDescriptor(a, property);
		descriptorB = getOwnPropertyDescriptor(b, property);
		if ((descriptorA || descriptorB) && (!descriptorA || !descriptorB || descriptorA.configurable !== descriptorB.configurable || descriptorA.enumerable !== descriptorB.enumerable || descriptorA.writable !== descriptorB.writable)) return false;
	}
	return true;
}
/**
* Whether the primitive wrappers passed are equal in value.
*/
function arePrimitiveWrappersEqual(a, b) {
	return sameValueZeroEqual(a.valueOf(), b.valueOf());
}
/**
* Whether the regexps passed are equal in value.
*/
function areRegExpsEqual(a, b) {
	return a.source === b.source && a.flags === b.flags;
}
/**
* Whether the `Set`s are equal in value.
*/
function areSetsEqual(a, b, state) {
	var size = a.size;
	if (size !== b.size) return false;
	if (!size) return true;
	var matchedIndices = new Array(size);
	var aIterable = a.values();
	var aResult;
	var bResult;
	while (aResult = aIterable.next()) {
		if (aResult.done) break;
		var bIterable = b.values();
		var hasMatch = false;
		var matchIndex = 0;
		while (bResult = bIterable.next()) {
			if (bResult.done) break;
			if (!matchedIndices[matchIndex] && state.equals(aResult.value, bResult.value, aResult.value, bResult.value, a, b, state)) {
				hasMatch = matchedIndices[matchIndex] = true;
				break;
			}
			matchIndex++;
		}
		if (!hasMatch) return false;
	}
	return true;
}
/**
* Whether the TypedArray instances are equal in value.
*/
function areTypedArraysEqual(a, b) {
	var index = a.length;
	if (b.length !== index) return false;
	while (index-- > 0) if (a[index] !== b[index]) return false;
	return true;
}
/**
* Whether the URL instances are equal in value.
*/
function areUrlsEqual(a, b) {
	return a.hostname === b.hostname && a.pathname === b.pathname && a.protocol === b.protocol && a.port === b.port && a.hash === b.hash && a.username === b.username && a.password === b.password;
}
function isPropertyEqual(a, b, state, property) {
	if ((property === REACT_OWNER || property === PREACT_OWNER || property === PREACT_VNODE) && (a.$$typeof || b.$$typeof)) return true;
	return hasOwn(b, property) && state.equals(a[property], b[property], property, property, a, b, state);
}
var ARGUMENTS_TAG = "[object Arguments]";
var BOOLEAN_TAG = "[object Boolean]";
var DATE_TAG = "[object Date]";
var ERROR_TAG = "[object Error]";
var MAP_TAG = "[object Map]";
var NUMBER_TAG = "[object Number]";
var OBJECT_TAG = "[object Object]";
var REG_EXP_TAG = "[object RegExp]";
var SET_TAG = "[object Set]";
var STRING_TAG = "[object String]";
var URL_TAG = "[object URL]";
var isArray = Array.isArray;
var isTypedArray = typeof ArrayBuffer === "function" && ArrayBuffer.isView ? ArrayBuffer.isView : null;
var assign = Object.assign;
var getTag = Object.prototype.toString.call.bind(Object.prototype.toString);
/**
* Create a comparator method based on the type-specific equality comparators passed.
*/
function createEqualityComparator(_a) {
	var areArraysEqual = _a.areArraysEqual, areDatesEqual = _a.areDatesEqual, areErrorsEqual = _a.areErrorsEqual, areFunctionsEqual = _a.areFunctionsEqual, areMapsEqual = _a.areMapsEqual, areNumbersEqual = _a.areNumbersEqual, areObjectsEqual = _a.areObjectsEqual, arePrimitiveWrappersEqual = _a.arePrimitiveWrappersEqual, areRegExpsEqual = _a.areRegExpsEqual, areSetsEqual = _a.areSetsEqual, areTypedArraysEqual = _a.areTypedArraysEqual, areUrlsEqual = _a.areUrlsEqual;
	/**
	* compare the value of the two objects and return true if they are equivalent in values
	*/
	return function comparator(a, b, state) {
		if (a === b) return true;
		if (a == null || b == null) return false;
		var type = typeof a;
		if (type !== typeof b) return false;
		if (type !== "object") {
			if (type === "number") return areNumbersEqual(a, b, state);
			if (type === "function") return areFunctionsEqual(a, b, state);
			return false;
		}
		var constructor = a.constructor;
		if (constructor !== b.constructor) return false;
		if (constructor === Object) return areObjectsEqual(a, b, state);
		if (isArray(a)) return areArraysEqual(a, b, state);
		if (isTypedArray != null && isTypedArray(a)) return areTypedArraysEqual(a, b, state);
		if (constructor === Date) return areDatesEqual(a, b, state);
		if (constructor === RegExp) return areRegExpsEqual(a, b, state);
		if (constructor === Map) return areMapsEqual(a, b, state);
		if (constructor === Set) return areSetsEqual(a, b, state);
		var tag = getTag(a);
		if (tag === DATE_TAG) return areDatesEqual(a, b, state);
		if (tag === REG_EXP_TAG) return areRegExpsEqual(a, b, state);
		if (tag === MAP_TAG) return areMapsEqual(a, b, state);
		if (tag === SET_TAG) return areSetsEqual(a, b, state);
		if (tag === OBJECT_TAG) return typeof a.then !== "function" && typeof b.then !== "function" && areObjectsEqual(a, b, state);
		if (tag === URL_TAG) return areUrlsEqual(a, b, state);
		if (tag === ERROR_TAG) return areErrorsEqual(a, b, state);
		if (tag === ARGUMENTS_TAG) return areObjectsEqual(a, b, state);
		if (tag === BOOLEAN_TAG || tag === NUMBER_TAG || tag === STRING_TAG) return arePrimitiveWrappersEqual(a, b, state);
		return false;
	};
}
/**
* Create the configuration object used for building comparators.
*/
function createEqualityComparatorConfig(_a) {
	var circular = _a.circular, createCustomConfig = _a.createCustomConfig, strict = _a.strict;
	var config = {
		areArraysEqual: strict ? areObjectsEqualStrict : areArraysEqual,
		areDatesEqual,
		areErrorsEqual,
		areFunctionsEqual,
		areMapsEqual: strict ? combineComparators(areMapsEqual, areObjectsEqualStrict) : areMapsEqual,
		areNumbersEqual,
		areObjectsEqual: strict ? areObjectsEqualStrict : areObjectsEqual,
		arePrimitiveWrappersEqual,
		areRegExpsEqual,
		areSetsEqual: strict ? combineComparators(areSetsEqual, areObjectsEqualStrict) : areSetsEqual,
		areTypedArraysEqual: strict ? areObjectsEqualStrict : areTypedArraysEqual,
		areUrlsEqual
	};
	if (createCustomConfig) config = assign({}, config, createCustomConfig(config));
	if (circular) {
		var areArraysEqual$1 = createIsCircular(config.areArraysEqual);
		var areMapsEqual$1 = createIsCircular(config.areMapsEqual);
		var areObjectsEqual$1 = createIsCircular(config.areObjectsEqual);
		var areSetsEqual$1 = createIsCircular(config.areSetsEqual);
		config = assign({}, config, {
			areArraysEqual: areArraysEqual$1,
			areMapsEqual: areMapsEqual$1,
			areObjectsEqual: areObjectsEqual$1,
			areSetsEqual: areSetsEqual$1
		});
	}
	return config;
}
/**
* Default equality comparator pass-through, used as the standard `isEqual` creator for
* use inside the built comparator.
*/
function createInternalEqualityComparator(compare) {
	return function(a, b, _indexOrKeyA, _indexOrKeyB, _parentA, _parentB, state) {
		return compare(a, b, state);
	};
}
/**
* Create the `isEqual` function used by the consuming application.
*/
function createIsEqual(_a) {
	var circular = _a.circular, comparator = _a.comparator, createState = _a.createState, equals = _a.equals, strict = _a.strict;
	if (createState) return function isEqual(a, b) {
		var _a = createState(), _b = _a.cache, cache = _b === void 0 ? circular ? /* @__PURE__ */ new WeakMap() : void 0 : _b, meta = _a.meta;
		return comparator(a, b, {
			cache,
			equals,
			meta,
			strict
		});
	};
	if (circular) return function isEqual(a, b) {
		return comparator(a, b, {
			cache: /* @__PURE__ */ new WeakMap(),
			equals,
			meta: void 0,
			strict
		});
	};
	var state = {
		cache: void 0,
		equals,
		meta: void 0,
		strict
	};
	return function isEqual(a, b) {
		return comparator(a, b, state);
	};
}
/**
* Whether the items passed are deeply-equal in value.
*/
var deepEqual$2 = createCustomEqual();
createCustomEqual({ strict: true });
createCustomEqual({ circular: true });
createCustomEqual({
	circular: true,
	strict: true
});
createCustomEqual({ createInternalComparator: function() {
	return sameValueZeroEqual;
} });
createCustomEqual({
	strict: true,
	createInternalComparator: function() {
		return sameValueZeroEqual;
	}
});
createCustomEqual({
	circular: true,
	createInternalComparator: function() {
		return sameValueZeroEqual;
	}
});
createCustomEqual({
	circular: true,
	createInternalComparator: function() {
		return sameValueZeroEqual;
	},
	strict: true
});
/**
* Create a custom equality comparison method.
*
* This can be done to create very targeted comparisons in extreme hot-path scenarios
* where the standard methods are not performant enough, but can also be used to provide
* support for legacy environments that do not support expected features like
* `RegExp.prototype.flags` out of the box.
*/
function createCustomEqual(options) {
	if (options === void 0) options = {};
	var _a = options.circular, circular = _a === void 0 ? false : _a, createCustomInternalComparator = options.createInternalComparator, createState = options.createState, _b = options.strict, strict = _b === void 0 ? false : _b;
	var comparator = createEqualityComparator(createEqualityComparatorConfig(options));
	return createIsEqual({
		circular,
		comparator,
		createState,
		equals: createCustomInternalComparator ? createCustomInternalComparator(comparator) : createInternalEqualityComparator(comparator),
		strict
	});
}
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-4HOGLOEA.mjs
init_react_import();
var defaultViewports = [
	{
		width: 360,
		height: "auto",
		icon: "Smartphone",
		label: "Small"
	},
	{
		width: 768,
		height: "auto",
		icon: "Tablet",
		label: "Medium"
	},
	{
		width: 1280,
		height: "auto",
		icon: "Monitor",
		label: "Large"
	},
	{
		width: "100%",
		height: "auto",
		icon: "FullWidth",
		label: "Full-width"
	}
];
init_react_import();
init_react_import();
var getChanged = (newItem, oldItem) => {
	return newItem ? Object.keys(newItem.props || {}).reduce((acc, item) => {
		const newItemProps = (newItem == null ? void 0 : newItem.props) || {};
		const oldItemProps = (oldItem == null ? void 0 : oldItem.props) || {};
		return __spreadProps$6(__spreadValues$7({}, acc), { [item]: !deepEqual$2(oldItemProps[item], newItemProps[item]) });
	}, {}) : {};
};
var cache = { lastChange: {} };
var resolveComponentData = (_0, _1, ..._2) => __async(null, [
	_0,
	_1,
	..._2
], function* (item, config, metadata = {}, onResolveStart, onResolveEnd, trigger = "replace", parent = null) {
	const configForItem = "type" in item && item.type !== "root" ? config.components[item.type] : config.root;
	const resolvedItem = __spreadValues$7({}, item);
	const shouldRunResolver = (configForItem == null ? void 0 : configForItem.resolveData) && item.props;
	const id = "id" in item.props ? item.props.id : "root";
	if (shouldRunResolver) {
		const { item: oldItem = null, resolved = {}, parentId: oldParentId = null } = cache.lastChange[id] || {};
		const parentChanged = !(oldParentId === null) && (parent == null ? void 0 : parent.props.id) !== oldParentId;
		const dataChanged = item && !deepEqual$2(item, oldItem);
		if (trigger === "move" && !parentChanged || trigger !== "move" && trigger !== "force" && !dataChanged) return {
			node: resolved,
			didChange: false
		};
		const changed = getChanged(item, oldItem);
		if (onResolveStart) onResolveStart(item);
		const { props: resolvedProps, readOnly = {} } = yield configForItem.resolveData(item, {
			changed,
			lastData: oldItem,
			metadata: __spreadValues$7(__spreadValues$7({}, metadata), configForItem.metadata),
			trigger,
			parent
		});
		resolvedItem.props = __spreadValues$7(__spreadValues$7({}, item.props), resolvedProps);
		if (Object.keys(readOnly).length) resolvedItem.readOnly = readOnly;
	}
	const itemAsComponentData = toComponent(resolvedItem);
	let itemWithResolvedChildren = yield mapFields(resolvedItem, { slot: (_02) => __async(null, [_02], function* ({ value }) {
		const content = value;
		return yield Promise.all(content.map((childItem) => __async(null, null, function* () {
			return (yield resolveComponentData(childItem, config, metadata, onResolveStart, onResolveEnd, trigger, itemAsComponentData)).node;
		})));
	}) }, config);
	if (shouldRunResolver && onResolveEnd) onResolveEnd(resolvedItem);
	cache.lastChange[id] = {
		item,
		resolved: itemWithResolvedChildren,
		parentId: parent == null ? void 0 : parent.props.id
	};
	return {
		node: itemWithResolvedChildren,
		didChange: !deepEqual$2(item, itemWithResolvedChildren)
	};
});
init_react_import();
var defaultAppState = {
	data: {
		content: [],
		root: {},
		zones: {}
	},
	ui: {
		leftSideBarVisible: true,
		rightSideBarVisible: true,
		arrayState: {},
		itemSelector: null,
		componentList: {},
		isDragging: false,
		previewMode: "edit",
		viewports: {
			current: {
				width: defaultViewports[0].width,
				height: defaultViewports[0].height || "auto"
			},
			options: [],
			controlsVisible: true
		},
		field: { focus: null },
		plugin: { current: null }
	},
	indexes: {
		nodes: {},
		zones: {}
	}
};
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-Y2EFNT5P.mjs
var require_classnames = __commonJS({ "../../node_modules/classnames/index.js"(exports, module) {
	"use strict";
	init_react_import();
	(function() {
		"use strict";
		var hasOwn = {}.hasOwnProperty;
		function classNames() {
			var classes = "";
			for (var i = 0; i < arguments.length; i++) {
				var arg = arguments[i];
				if (arg) classes = appendClass(classes, parseValue(arg));
			}
			return classes;
		}
		function parseValue(arg) {
			if (typeof arg === "string" || typeof arg === "number") return arg;
			if (typeof arg !== "object") return "";
			if (Array.isArray(arg)) return classNames.apply(null, arg);
			if (arg.toString !== Object.prototype.toString && !arg.toString.toString().includes("[native code]")) return arg.toString();
			var classes = "";
			for (var key in arg) if (hasOwn.call(arg, key) && arg[key]) classes = appendClass(classes, key);
			return classes;
		}
		function appendClass(value, newClass) {
			if (!newClass) return value;
			if (value) return value + " " + newClass;
			return value + newClass;
		}
		if (typeof module !== "undefined" && module.exports) {
			classNames.default = classNames;
			module.exports = classNames;
		} else if (typeof define === "function" && typeof define.amd === "object" && define.amd) define("classnames", [], function() {
			return classNames;
		});
		else window.classNames = classNames;
	})();
} });
init_react_import();
var import_classnames = __toESM(require_classnames());
var getClassNameFactory = (rootClass, styles, config = { baseClass: "" }) => (options = {}) => {
	if (typeof options === "string") {
		const descendant = options;
		if (styles[`${rootClass}-${descendant}`]) return config.baseClass + styles[`${rootClass}-${descendant}`] || "";
		return "";
	} else if (typeof options === "object") {
		const modifiers = options;
		const prefixedModifiers = {};
		for (let modifier in modifiers) prefixedModifiers[styles[`${rootClass}--${modifier}`]] = modifiers[modifier];
		const c = styles[rootClass];
		return config.baseClass + (0, import_classnames.default)(__spreadValues$7({ [c]: !!c }, prefixedModifiers));
	} else return config.baseClass + styles[rootClass] || "";
};
var get_class_name_factory_default = getClassNameFactory;
/*! Bundled license information:

classnames/index.js:
(*!
Copyright (c) 2018 Jed Watson.
Licensed under the MIT License (MIT), see
http://jedwatson.github.io/classnames
*)
*/
//#endregion
//#region node_modules/zustand/esm/vanilla.mjs
var import_jsx_runtime = require_jsx_runtime();
var import_react_dom = /* @__PURE__ */ __toESM$1(require_react_dom(), 1);
var createStoreImpl = (createState) => {
	let state;
	const listeners = /* @__PURE__ */ new Set();
	const setState = (partial, replace) => {
		const nextState = typeof partial === "function" ? partial(state) : partial;
		if (!Object.is(nextState, state)) {
			const previousState = state;
			state = (replace != null ? replace : typeof nextState !== "object" || nextState === null) ? nextState : Object.assign({}, state, nextState);
			listeners.forEach((listener) => listener(state, previousState));
		}
	};
	const getState = () => state;
	const getInitialState = () => initialState;
	const subscribe = (listener) => {
		listeners.add(listener);
		return () => listeners.delete(listener);
	};
	const api = {
		setState,
		getState,
		getInitialState,
		subscribe
	};
	const initialState = state = createState(setState, getState, api);
	return api;
};
var createStore = ((createState) => createState ? createStoreImpl(createState) : createStoreImpl);
//#endregion
//#region node_modules/zustand/esm/react.mjs
var identity = (arg) => arg;
function useStore(api, selector = identity) {
	const slice = import_react.useSyncExternalStore(api.subscribe, import_react.useCallback(() => selector(api.getState()), [api, selector]), import_react.useCallback(() => selector(api.getInitialState()), [api, selector]));
	import_react.useDebugValue(slice);
	return slice;
}
var createImpl = (createState) => {
	const api = createStore(createState);
	const useBoundStore = (selector) => useStore(api, selector);
	Object.assign(useBoundStore, api);
	return useBoundStore;
};
var create = ((createState) => createState ? createImpl(createState) : createImpl);
//#endregion
//#region node_modules/zustand/esm/middleware.mjs
var subscribeWithSelectorImpl = (fn) => (set, get, api) => {
	const origSubscribe = api.subscribe;
	api.subscribe = ((selector, optListener, options) => {
		let listener = selector;
		if (optListener) {
			const equalityFn = (options == null ? void 0 : options.equalityFn) || Object.is;
			let currentSlice = selector(api.getState());
			listener = (state) => {
				const nextSlice = selector(state);
				if (!equalityFn(currentSlice, nextSlice)) {
					const previousSlice = currentSlice;
					optListener(currentSlice = nextSlice, previousSlice);
				}
			};
			if (options == null ? void 0 : options.fireImmediately) optListener(currentSlice, currentSlice);
		}
		return origSubscribe(listener);
	});
	return fn(set, get, api);
};
var subscribeWithSelector = subscribeWithSelectorImpl;
//#endregion
//#region node_modules/@radix-ui/primitive/dist/index.mjs
var __defProp$23 = Object.defineProperty;
var __name$20 = (target, value) => __defProp$23(target, "name", {
	value,
	configurable: true
});
var canUseDOM$1 = !!(typeof window !== "undefined" && window.document && window.document.createElement);
function composeEventHandlers(originalEventHandler, ourEventHandler, { checkForDefaultPrevented = true } = {}) {
	return /* @__PURE__ */ __name$20(function handleEvent(event) {
		originalEventHandler?.(event);
		if (checkForDefaultPrevented === false || !event || !event.defaultPrevented) return ourEventHandler?.(event);
	}, "handleEvent");
}
__name$20(composeEventHandlers, "composeEventHandlers");
function getOwnerWindow(element) {
	if (!canUseDOM$1) throw new Error("Cannot access window outside of the DOM");
	return element?.ownerDocument?.defaultView ?? window;
}
__name$20(getOwnerWindow, "getOwnerWindow");
function getOwnerDocument(element) {
	if (!canUseDOM$1) throw new Error("Cannot access document outside of the DOM");
	return element?.ownerDocument ?? document;
}
__name$20(getOwnerDocument, "getOwnerDocument");
function getActiveElement(node, activeDescendant = false) {
	const { activeElement } = getOwnerDocument(node);
	if (!activeElement?.nodeName) return null;
	if (isFrame(activeElement) && activeElement.contentDocument) return getActiveElement(activeElement.contentDocument.body, activeDescendant);
	if (activeDescendant) {
		const id = activeElement.getAttribute("aria-activedescendant");
		if (id) {
			const element = getOwnerDocument(activeElement).getElementById(id);
			if (element) return element;
		}
	}
	return activeElement;
}
__name$20(getActiveElement, "getActiveElement");
function isFrame(element) {
	return element.tagName === "IFRAME";
}
__name$20(isFrame, "isFrame");
//#endregion
//#region node_modules/@radix-ui/react-compose-refs/dist/index.mjs
var __defProp$22 = Object.defineProperty;
var __name$19 = (target, value) => __defProp$22(target, "name", {
	value,
	configurable: true
});
function setRef$1(ref, value) {
	if (typeof ref === "function") return ref(value);
	else if (ref !== null && ref !== void 0) ref.current = value;
}
__name$19(setRef$1, "setRef");
function composeRefs(...refs) {
	return (node) => {
		let hasCleanup = false;
		const cleanups = refs.map((ref) => {
			const cleanup = setRef$1(ref, node);
			if (!hasCleanup && typeof cleanup == "function") hasCleanup = true;
			return cleanup;
		});
		if (hasCleanup) return () => {
			for (let i = 0; i < cleanups.length; i++) {
				const cleanup = cleanups[i];
				if (typeof cleanup == "function") cleanup();
				else setRef$1(refs[i], null);
			}
		};
	};
}
__name$19(composeRefs, "composeRefs");
function useComposedRefs(...refs) {
	return import_react.useCallback(composeRefs(...refs), refs);
}
__name$19(useComposedRefs, "useComposedRefs");
//#endregion
//#region node_modules/@radix-ui/react-context/dist/index.mjs
var __defProp$21 = Object.defineProperty;
var __name$18 = (target, value) => __defProp$21(target, "name", {
	value,
	configurable: true
});
/* @__NO_SIDE_EFFECTS__ */
function createContext2$2(rootComponentName, defaultContext) {
	const Context = import_react.createContext(defaultContext);
	Context.displayName = rootComponentName + "Context";
	const Provider = /* @__PURE__ */ __name$18((props) => {
		const { children, ...context } = props;
		const value = import_react.useMemo(() => context, Object.values(context));
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Context.Provider, {
			value,
			children
		});
	}, "Provider");
	Provider.displayName = rootComponentName + "Provider";
	function useContext2(consumerName, options = {}) {
		const { optional = false } = options;
		const context = import_react.useContext(Context);
		if (context) return context;
		if (defaultContext !== void 0) return defaultContext;
		if (optional) return void 0;
		throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
	}
	__name$18(useContext2, "useContext");
	return [Provider, useContext2];
}
__name$18(createContext2$2, "createContext");
/* @__NO_SIDE_EFFECTS__ */
function createContextScope(scopeName, createContextScopeDeps = []) {
	let defaultContexts = [];
	function createContext3(rootComponentName, defaultContext) {
		const BaseContext = import_react.createContext(defaultContext);
		BaseContext.displayName = rootComponentName + "Context";
		const index = defaultContexts.length;
		defaultContexts = [...defaultContexts, defaultContext];
		const Provider = /* @__PURE__ */ __name$18((props) => {
			const { scope, children, ...context } = props;
			const Context = scope?.[scopeName]?.[index] || BaseContext;
			const value = import_react.useMemo(() => context, Object.values(context));
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Context.Provider, {
				value,
				children
			});
		}, "Provider");
		Provider.displayName = rootComponentName + "Provider";
		function useContext2(consumerName, scope, options = {}) {
			const { optional = false } = options;
			const Context = scope?.[scopeName]?.[index] || BaseContext;
			const context = import_react.useContext(Context);
			if (context) return context;
			if (defaultContext !== void 0) return defaultContext;
			if (optional) return void 0;
			throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
		}
		__name$18(useContext2, "useContext");
		return [Provider, useContext2];
	}
	__name$18(createContext3, "createContext");
	const createScope = /* @__PURE__ */ __name$18(() => {
		const scopeContexts = defaultContexts.map((defaultContext) => {
			return import_react.createContext(defaultContext);
		});
		return /* @__PURE__ */ __name$18(function useScope(scope) {
			const contexts = scope?.[scopeName] || scopeContexts;
			return import_react.useMemo(() => ({ [`__scope${scopeName}`]: {
				...scope,
				[scopeName]: contexts
			} }), [scope, contexts]);
		}, "useScope");
	}, "createScope");
	createScope.scopeName = scopeName;
	return [createContext3, composeContextScopes(createScope, ...createContextScopeDeps)];
}
__name$18(createContextScope, "createContextScope");
function composeContextScopes(...scopes) {
	const baseScope = scopes[0];
	if (scopes.length === 1) return baseScope;
	const createScope = /* @__PURE__ */ __name$18(() => {
		const scopeHooks = scopes.map((createScope2) => ({
			useScope: createScope2(),
			scopeName: createScope2.scopeName
		}));
		return /* @__PURE__ */ __name$18(function useComposedScopes(overrideScopes) {
			const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
				const currentScope = useScope(overrideScopes)[`__scope${scopeName}`];
				return {
					...nextScopes2,
					...currentScope
				};
			}, {});
			return import_react.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
		}, "useComposedScopes");
	}, "createScope");
	createScope.scopeName = baseScope.scopeName;
	return createScope;
}
__name$18(composeContextScopes, "composeContextScopes");
//#endregion
//#region node_modules/@radix-ui/react-slot/dist/index.mjs
var __defProp$20 = Object.defineProperty;
var __name$17 = (target, value) => __defProp$20(target, "name", {
	value,
	configurable: true
});
/* @__NO_SIDE_EFFECTS__ */
function createSlot(ownerName) {
	const Slot2 = import_react.forwardRef((props, forwardedRef) => {
		let { children, ...slotProps } = props;
		let slottableElement = null;
		let hasSlottable = false;
		const newChildren = [];
		if (isLazyComponent(children) && typeof use === "function") children = use(children._payload);
		import_react.Children.forEach(children, (maybeSlottable) => {
			if (isSlottable(maybeSlottable)) {
				hasSlottable = true;
				const slottable = maybeSlottable;
				let child = "child" in slottable.props ? slottable.props.child : slottable.props.children;
				if (isLazyComponent(child) && typeof use === "function") child = use(child._payload);
				slottableElement = getSlottableElementFromSlottable(slottable, child);
				newChildren.push(slottableElement?.props?.children);
			} else newChildren.push(maybeSlottable);
		});
		if (slottableElement) slottableElement = import_react.cloneElement(slottableElement, void 0, newChildren);
		else if (!hasSlottable && import_react.Children.count(children) === 1 && import_react.isValidElement(children)) slottableElement = children;
		const slottableElementRef = slottableElement ? getElementRef$1(slottableElement) : void 0;
		const composedRef = useComposedRefs(forwardedRef, slottableElementRef);
		if (!slottableElement) {
			if (children || children === 0) throw new Error(hasSlottable ? createSlottableError(ownerName) : createSlotError(ownerName));
			return children;
		}
		const mergedProps = mergeProps(slotProps, slottableElement.props ?? {});
		if (slottableElement.type !== import_react.Fragment) mergedProps.ref = forwardedRef ? composedRef : slottableElementRef;
		return import_react.cloneElement(slottableElement, mergedProps);
	});
	Slot2.displayName = `${ownerName}.Slot`;
	return Slot2;
}
__name$17(createSlot, "createSlot");
var SLOTTABLE_IDENTIFIER = Symbol.for("radix.slottable");
/* @__NO_SIDE_EFFECTS__ */
function createSlottable(ownerName) {
	const Slottable2 = /* @__PURE__ */ __name$17((props) => "child" in props ? props.children(props.child) : props.children, "Slottable");
	Slottable2.displayName = `${ownerName}.Slottable`;
	Slottable2.__radixId = SLOTTABLE_IDENTIFIER;
	return Slottable2;
}
__name$17(createSlottable, "createSlottable");
var getSlottableElementFromSlottable = /* @__PURE__ */ __name$17((slottable, child) => {
	if ("child" in slottable.props) {
		const child2 = slottable.props.child;
		if (!import_react.isValidElement(child2)) return null;
		return import_react.cloneElement(child2, void 0, slottable.props.children(child2.props.children));
	}
	return import_react.isValidElement(child) ? child : null;
}, "getSlottableElementFromSlottable");
function mergeProps(slotProps, childProps) {
	const overrideProps = { ...childProps };
	for (const propName in childProps) {
		const slotPropValue = slotProps[propName];
		const childPropValue = childProps[propName];
		if (/^on[A-Z]/.test(propName)) {
			if (slotPropValue && childPropValue) overrideProps[propName] = (...args) => {
				const result = childPropValue(...args);
				slotPropValue(...args);
				return result;
			};
			else if (slotPropValue) overrideProps[propName] = slotPropValue;
		} else if (propName === "style") overrideProps[propName] = {
			...slotPropValue,
			...childPropValue
		};
		else if (propName === "className") overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
	}
	return {
		...slotProps,
		...overrideProps
	};
}
__name$17(mergeProps, "mergeProps");
function getElementRef$1(element) {
	let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
	let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
	if (mayWarn) return element.ref;
	getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
	mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
	if (mayWarn) return element.props.ref;
	return element.props.ref || element.ref;
}
__name$17(getElementRef$1, "getElementRef");
function isSlottable(child) {
	return import_react.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER;
}
__name$17(isSlottable, "isSlottable");
var REACT_LAZY_TYPE = Symbol.for("react.lazy");
function isLazyComponent(element) {
	return element != null && typeof element === "object" && "$$typeof" in element && element.$$typeof === REACT_LAZY_TYPE && "_payload" in element && isPromiseLike(element._payload);
}
__name$17(isLazyComponent, "isLazyComponent");
function isPromiseLike(value) {
	return typeof value === "object" && value !== null && "then" in value;
}
__name$17(isPromiseLike, "isPromiseLike");
var createSlotError = /* @__PURE__ */ __name$17((ownerName) => {
	return `${ownerName} failed to slot onto its children. Expected a single React element child or \`Slottable\`.`;
}, "createSlotError");
var createSlottableError = /* @__PURE__ */ __name$17((ownerName) => {
	return `${ownerName} failed to slot onto its \`Slottable\`. Expected \`Slottable\` to receive a single React element child.`;
}, "createSlottableError");
var use = import_react[" use ".trim().toString()];
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@radix-ui/react-popover/node_modules/@radix-ui/react-primitive/dist/index.mjs
var __defProp$19 = Object.defineProperty;
var __name$16 = (target, value) => __defProp$19(target, "name", {
	value,
	configurable: true
});
var Primitive = [
	"a",
	"button",
	"div",
	"form",
	"h2",
	"h3",
	"img",
	"input",
	"label",
	"li",
	"nav",
	"ol",
	"p",
	"select",
	"span",
	"svg",
	"ul"
].reduce((primitive, node) => {
	const Slot = /* @__PURE__ */ createSlot(`Primitive.${node}`);
	const Node = import_react.forwardRef((props, forwardedRef) => {
		const { asChild, ...primitiveProps } = props;
		const Comp = asChild ? Slot : node;
		if (typeof window !== "undefined") window[Symbol.for("radix-ui")] = true;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Comp, {
			...primitiveProps,
			ref: forwardedRef
		});
	});
	Node.displayName = `Primitive.${node}`;
	return {
		...primitive,
		[node]: Node
	};
}, {});
function dispatchDiscreteCustomEvent(target, event) {
	if (target) import_react_dom.flushSync(() => target.dispatchEvent(event));
}
__name$16(dispatchDiscreteCustomEvent, "dispatchDiscreteCustomEvent");
//#endregion
//#region node_modules/@radix-ui/react-use-callback-ref/dist/index.mjs
var __defProp$18 = Object.defineProperty;
var __name$15 = (target, value) => __defProp$18(target, "name", {
	value,
	configurable: true
});
function useCallbackRef$1(callback) {
	const callbackRef = import_react.useRef(callback);
	import_react.useEffect(() => {
		callbackRef.current = callback;
	});
	return import_react.useMemo(() => ((...args) => callbackRef.current?.(...args)), []);
}
__name$15(useCallbackRef$1, "useCallbackRef");
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@radix-ui/react-popover/node_modules/@radix-ui/react-dismissable-layer/dist/index.mjs
var __defProp$17 = Object.defineProperty;
var __name$14 = (target, value) => __defProp$17(target, "name", {
	value,
	configurable: true
});
var CONTEXT_UPDATE = "dismissableLayer.update";
var POINTER_DOWN_OUTSIDE = "dismissableLayer.pointerDownOutside";
var FOCUS_OUTSIDE = "dismissableLayer.focusOutside";
var originalBodyPointerEvents;
var DismissableLayerContext = import_react.createContext({
	layers: /* @__PURE__ */ new Set(),
	layersWithOutsidePointerEventsDisabled: /* @__PURE__ */ new Set(),
	branches: /* @__PURE__ */ new Set(),
	dismissableSurfaces: /* @__PURE__ */ new Set()
});
var DismissableLayer = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$14(function DismissableLayer2(props, forwardedRef) {
	const { disableOutsidePointerEvents = false, deferPointerDownOutside = false, onEscapeKeyDown, onPointerDownOutside, onFocusOutside, onInteractOutside, onDismiss, ...layerProps } = props;
	const context = import_react.useContext(DismissableLayerContext);
	const [node, setNode] = import_react.useState(null);
	const ownerDocument = node?.ownerDocument ?? globalThis?.document;
	const [, force] = import_react.useState({});
	const composedRefs = useComposedRefs(forwardedRef, setNode);
	const layers = Array.from(context.layers);
	const [highestLayerWithOutsidePointerEventsDisabled] = [...context.layersWithOutsidePointerEventsDisabled].slice(-1);
	const highestLayerWithOutsidePointerEventsDisabledIndex = highestLayerWithOutsidePointerEventsDisabled ? layers.indexOf(highestLayerWithOutsidePointerEventsDisabled) : -1;
	const index = node ? layers.indexOf(node) : -1;
	const isBodyPointerEventsDisabled = context.layersWithOutsidePointerEventsDisabled.size > 0;
	const isPointerEventsEnabled = index >= highestLayerWithOutsidePointerEventsDisabledIndex;
	const isDeferredPointerDownOutsideRef = import_react.useRef(false);
	const pointerDownOutside = usePointerDownOutside((event) => {
		onPointerDownOutside?.(event);
		onInteractOutside?.(event);
		if (!event.defaultPrevented) onDismiss?.();
	}, {
		ownerDocument,
		deferPointerDownOutside,
		isDeferredPointerDownOutsideRef,
		dismissableSurfaces: context.dismissableSurfaces,
		shouldHandlePointerDownOutside: import_react.useCallback((target) => {
			if (!(target instanceof Node)) return false;
			const isPointerDownOnBranch = [...context.branches].some((branch) => branch.contains(target));
			return isPointerEventsEnabled && !isPointerDownOnBranch;
		}, [context.branches, isPointerEventsEnabled])
	});
	const focusOutside = useFocusOutside((event) => {
		if (deferPointerDownOutside && isDeferredPointerDownOutsideRef.current) return;
		const target = event.target;
		if ([...context.branches].some((branch) => branch.contains(target))) return;
		onFocusOutside?.(event);
		onInteractOutside?.(event);
		if (!event.defaultPrevented) onDismiss?.();
	}, ownerDocument);
	const isHighestLayer = node ? index === layers.length - 1 : false;
	const handleKeyDown = useCallbackRef$1((event) => {
		if (event.key !== "Escape") return;
		onEscapeKeyDown?.(event);
		if (!event.defaultPrevented && onDismiss) {
			event.preventDefault();
			onDismiss();
		}
	});
	import_react.useEffect(() => {
		if (!isHighestLayer) return;
		ownerDocument.addEventListener("keydown", handleKeyDown, { capture: true });
		return () => ownerDocument.removeEventListener("keydown", handleKeyDown, { capture: true });
	}, [
		ownerDocument,
		isHighestLayer,
		handleKeyDown
	]);
	import_react.useEffect(() => {
		if (!node) return;
		if (disableOutsidePointerEvents) {
			if (context.layersWithOutsidePointerEventsDisabled.size === 0) {
				originalBodyPointerEvents = ownerDocument.body.style.pointerEvents;
				ownerDocument.body.style.pointerEvents = "none";
			}
			context.layersWithOutsidePointerEventsDisabled.add(node);
		}
		context.layers.add(node);
		dispatchUpdate();
		return () => {
			if (disableOutsidePointerEvents) {
				context.layersWithOutsidePointerEventsDisabled.delete(node);
				if (context.layersWithOutsidePointerEventsDisabled.size === 0) ownerDocument.body.style.pointerEvents = originalBodyPointerEvents;
			}
		};
	}, [
		node,
		ownerDocument,
		disableOutsidePointerEvents,
		context
	]);
	import_react.useEffect(() => {
		return () => {
			if (!node) return;
			context.layers.delete(node);
			context.layersWithOutsidePointerEventsDisabled.delete(node);
			dispatchUpdate();
		};
	}, [node, context]);
	import_react.useEffect(() => {
		const handleUpdate = /* @__PURE__ */ __name$14(() => force({}), "handleUpdate");
		document.addEventListener(CONTEXT_UPDATE, handleUpdate);
		return () => document.removeEventListener(CONTEXT_UPDATE, handleUpdate);
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Primitive.div, {
		...layerProps,
		ref: composedRefs,
		style: {
			pointerEvents: isBodyPointerEventsDisabled ? isPointerEventsEnabled ? "auto" : "none" : void 0,
			...props.style
		},
		onFocusCapture: composeEventHandlers(props.onFocusCapture, focusOutside.onFocusCapture),
		onBlurCapture: composeEventHandlers(props.onBlurCapture, focusOutside.onBlurCapture),
		onPointerDownCapture: composeEventHandlers(props.onPointerDownCapture, pointerDownOutside.onPointerDownCapture)
	});
}, "DismissableLayer"));
function useDismissableLayerSurface() {
	const context = import_react.useContext(DismissableLayerContext);
	const [node, setNode] = import_react.useState(null);
	import_react.useEffect(() => {
		if (!node) return;
		context.dismissableSurfaces.add(node);
		return () => {
			context.dismissableSurfaces.delete(node);
		};
	}, [node, context.dismissableSurfaces]);
	return setNode;
}
__name$14(useDismissableLayerSurface, "useDismissableLayerSurface");
var IS_TRUE = /* @__PURE__ */ __name$14(() => true, "IS_TRUE");
function usePointerDownOutside(onPointerDownOutside, args) {
	const { ownerDocument = globalThis?.document, deferPointerDownOutside = false, isDeferredPointerDownOutsideRef, dismissableSurfaces, shouldHandlePointerDownOutside = IS_TRUE } = args;
	const handlePointerDownOutside = useCallbackRef$1(onPointerDownOutside);
	const isPointerInsideReactTreeRef = import_react.useRef(false);
	const isPointerDownOutsideRef = import_react.useRef(false);
	const interceptedOutsideInteractionEventsRef = import_react.useRef(/* @__PURE__ */ new Map());
	const handleClickRef = import_react.useRef(() => {});
	import_react.useEffect(() => {
		function resetOutsideInteraction() {
			isPointerDownOutsideRef.current = false;
			isDeferredPointerDownOutsideRef.current = false;
			interceptedOutsideInteractionEventsRef.current.clear();
		}
		__name$14(resetOutsideInteraction, "resetOutsideInteraction");
		function isOutsideInteractionIntercepted() {
			return Array.from(interceptedOutsideInteractionEventsRef.current.values()).some(Boolean);
		}
		__name$14(isOutsideInteractionIntercepted, "isOutsideInteractionIntercepted");
		function handleInteractionCapture(event) {
			if (!isPointerDownOutsideRef.current) return;
			const target = event.target;
			if (!(target instanceof Node && [...dismissableSurfaces].some((surface) => surface.contains(target)))) interceptedOutsideInteractionEventsRef.current.set(event.type, true);
			if (event.type === "click") window.setTimeout(() => {
				if (isPointerDownOutsideRef.current) handleClickRef.current();
			}, 0);
		}
		__name$14(handleInteractionCapture, "handleInteractionCapture");
		function handleInteractionBubble(event) {
			if (isPointerDownOutsideRef.current) interceptedOutsideInteractionEventsRef.current.set(event.type, false);
		}
		__name$14(handleInteractionBubble, "handleInteractionBubble");
		const handlePointerDown = /* @__PURE__ */ __name$14((event) => {
			if (event.target && !isPointerInsideReactTreeRef.current) {
				let handleAndDispatchPointerDownOutsideEvent2 = function() {
					ownerDocument.removeEventListener("click", handleClickRef.current);
					const wasOutsideInteractionIntercepted = isOutsideInteractionIntercepted();
					resetOutsideInteraction();
					if (!wasOutsideInteractionIntercepted) handleAndDispatchCustomEvent(POINTER_DOWN_OUTSIDE, handlePointerDownOutside, eventDetail, { discrete: true });
				};
				__name$14(handleAndDispatchPointerDownOutsideEvent2, "handleAndDispatchPointerDownOutsideEvent");
				if (!shouldHandlePointerDownOutside(event.target)) {
					ownerDocument.removeEventListener("click", handleClickRef.current);
					resetOutsideInteraction();
					isPointerInsideReactTreeRef.current = false;
					return;
				}
				const eventDetail = { originalEvent: event };
				isPointerDownOutsideRef.current = true;
				isDeferredPointerDownOutsideRef.current = deferPointerDownOutside && event.button === 0;
				interceptedOutsideInteractionEventsRef.current.clear();
				if (!deferPointerDownOutside || event.button !== 0) handleAndDispatchPointerDownOutsideEvent2();
				else {
					ownerDocument.removeEventListener("click", handleClickRef.current);
					handleClickRef.current = handleAndDispatchPointerDownOutsideEvent2;
					ownerDocument.addEventListener("click", handleClickRef.current, { once: true });
				}
			} else {
				ownerDocument.removeEventListener("click", handleClickRef.current);
				resetOutsideInteraction();
			}
			isPointerInsideReactTreeRef.current = false;
		}, "handlePointerDown");
		const outsideInteractionEvents = [
			"pointerup",
			"mousedown",
			"mouseup",
			"touchstart",
			"touchend",
			"click"
		];
		for (const eventName of outsideInteractionEvents) {
			ownerDocument.addEventListener(eventName, handleInteractionCapture, true);
			ownerDocument.addEventListener(eventName, handleInteractionBubble);
		}
		const timerId = window.setTimeout(() => {
			ownerDocument.addEventListener("pointerdown", handlePointerDown);
		}, 0);
		return () => {
			window.clearTimeout(timerId);
			ownerDocument.removeEventListener("pointerdown", handlePointerDown);
			ownerDocument.removeEventListener("click", handleClickRef.current);
			for (const eventName of outsideInteractionEvents) {
				ownerDocument.removeEventListener(eventName, handleInteractionCapture, true);
				ownerDocument.removeEventListener(eventName, handleInteractionBubble);
			}
		};
	}, [
		ownerDocument,
		handlePointerDownOutside,
		deferPointerDownOutside,
		isDeferredPointerDownOutsideRef,
		dismissableSurfaces,
		shouldHandlePointerDownOutside
	]);
	return { onPointerDownCapture: /* @__PURE__ */ __name$14(() => isPointerInsideReactTreeRef.current = true, "onPointerDownCapture") };
}
__name$14(usePointerDownOutside, "usePointerDownOutside");
function useFocusOutside(onFocusOutside, ownerDocument = globalThis?.document) {
	const handleFocusOutside = useCallbackRef$1(onFocusOutside);
	const isFocusInsideReactTreeRef = import_react.useRef(false);
	import_react.useEffect(() => {
		const handleFocus = /* @__PURE__ */ __name$14((event) => {
			if (event.target && !isFocusInsideReactTreeRef.current) handleAndDispatchCustomEvent(FOCUS_OUTSIDE, handleFocusOutside, { originalEvent: event }, { discrete: false });
		}, "handleFocus");
		ownerDocument.addEventListener("focusin", handleFocus);
		return () => ownerDocument.removeEventListener("focusin", handleFocus);
	}, [ownerDocument, handleFocusOutside]);
	return {
		onFocusCapture: /* @__PURE__ */ __name$14(() => isFocusInsideReactTreeRef.current = true, "onFocusCapture"),
		onBlurCapture: /* @__PURE__ */ __name$14(() => isFocusInsideReactTreeRef.current = false, "onBlurCapture")
	};
}
__name$14(useFocusOutside, "useFocusOutside");
function dispatchUpdate() {
	const event = new CustomEvent(CONTEXT_UPDATE);
	document.dispatchEvent(event);
}
__name$14(dispatchUpdate, "dispatchUpdate");
function handleAndDispatchCustomEvent(name, handler, detail, { discrete }) {
	const target = detail.originalEvent.target;
	const event = new CustomEvent(name, {
		bubbles: false,
		cancelable: true,
		detail
	});
	if (handler) target.addEventListener(name, handler, { once: true });
	if (discrete) dispatchDiscreteCustomEvent(target, event);
	else target.dispatchEvent(event);
}
__name$14(handleAndDispatchCustomEvent, "handleAndDispatchCustomEvent");
//#endregion
//#region node_modules/@radix-ui/react-focus-guards/dist/index.mjs
var __defProp$16 = Object.defineProperty;
var __name$13 = (target, value) => __defProp$16(target, "name", {
	value,
	configurable: true
});
var count$1 = 0;
var guards = null;
function FocusGuards(props) {
	useFocusGuards();
	return props.children;
}
__name$13(FocusGuards, "FocusGuards");
function useFocusGuards() {
	import_react.useEffect(() => {
		if (!guards) guards = {
			start: createFocusGuard(),
			end: createFocusGuard()
		};
		const { start, end } = guards;
		if (document.body.firstElementChild !== start) document.body.insertAdjacentElement("afterbegin", start);
		if (document.body.lastElementChild !== end) document.body.insertAdjacentElement("beforeend", end);
		count$1++;
		return () => {
			if (count$1 === 1) {
				guards?.start.remove();
				guards?.end.remove();
				guards = null;
			}
			count$1 = Math.max(0, count$1 - 1);
		};
	}, []);
}
__name$13(useFocusGuards, "useFocusGuards");
function createFocusGuard() {
	const element = document.createElement("span");
	element.setAttribute("data-radix-focus-guard", "");
	element.tabIndex = 0;
	element.style.outline = "none";
	element.style.opacity = "0";
	element.style.position = "fixed";
	element.style.pointerEvents = "none";
	return element;
}
__name$13(createFocusGuard, "createFocusGuard");
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@radix-ui/react-popover/node_modules/@radix-ui/react-focus-scope/dist/index.mjs
var __defProp$15 = Object.defineProperty;
var __name$12 = (target, value) => __defProp$15(target, "name", {
	value,
	configurable: true
});
var AUTOFOCUS_ON_MOUNT = "focusScope.autoFocusOnMount";
var AUTOFOCUS_ON_UNMOUNT = "focusScope.autoFocusOnUnmount";
var EVENT_OPTIONS = {
	bubbles: false,
	cancelable: true
};
var FocusScope = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$12(function FocusScope2(props, forwardedRef) {
	const { loop = false, trapped = false, onMountAutoFocus: onMountAutoFocusProp, onUnmountAutoFocus: onUnmountAutoFocusProp, ...scopeProps } = props;
	const [container, setContainer] = import_react.useState(null);
	const onMountAutoFocus = useCallbackRef$1(onMountAutoFocusProp);
	const onUnmountAutoFocus = useCallbackRef$1(onUnmountAutoFocusProp);
	const lastFocusedElementRef = import_react.useRef(null);
	const composedRefs = useComposedRefs(forwardedRef, setContainer);
	const focusScope = import_react.useRef({
		paused: false,
		pause() {
			this.paused = true;
		},
		resume() {
			this.paused = false;
		}
	}).current;
	import_react.useEffect(() => {
		if (trapped) {
			let handleFocusIn2 = function(event) {
				if (focusScope.paused || !container) return;
				const target = event.target;
				if (container.contains(target)) lastFocusedElementRef.current = target;
				else focus(lastFocusedElementRef.current, { select: true });
			}, handleFocusOut2 = function(event) {
				if (focusScope.paused || !container) return;
				const relatedTarget = event.relatedTarget;
				if (relatedTarget === null) return;
				if (!container.contains(relatedTarget)) focus(lastFocusedElementRef.current, { select: true });
			}, handleMutations2 = function(mutations) {
				if (document.activeElement !== document.body) return;
				for (const mutation of mutations) if (mutation.removedNodes.length > 0) focus(container);
			};
			__name$12(handleFocusIn2, "handleFocusIn");
			__name$12(handleFocusOut2, "handleFocusOut");
			__name$12(handleMutations2, "handleMutations");
			document.addEventListener("focusin", handleFocusIn2);
			document.addEventListener("focusout", handleFocusOut2);
			const mutationObserver = new MutationObserver(handleMutations2);
			if (container) mutationObserver.observe(container, {
				childList: true,
				subtree: true
			});
			return () => {
				document.removeEventListener("focusin", handleFocusIn2);
				document.removeEventListener("focusout", handleFocusOut2);
				mutationObserver.disconnect();
			};
		}
	}, [
		trapped,
		container,
		focusScope.paused
	]);
	import_react.useEffect(() => {
		if (container) {
			focusScopesStack.add(focusScope);
			const previouslyFocusedElement = document.activeElement;
			if (!container.contains(previouslyFocusedElement)) {
				const mountEvent = new CustomEvent(AUTOFOCUS_ON_MOUNT, EVENT_OPTIONS);
				container.addEventListener(AUTOFOCUS_ON_MOUNT, onMountAutoFocus);
				container.dispatchEvent(mountEvent);
				if (!mountEvent.defaultPrevented) {
					focusFirst(removeLinks(getTabbableCandidates(container)), { select: true });
					if (document.activeElement === previouslyFocusedElement) focus(container);
				}
			}
			return () => {
				container.removeEventListener(AUTOFOCUS_ON_MOUNT, onMountAutoFocus);
				setTimeout(() => {
					const unmountEvent = new CustomEvent(AUTOFOCUS_ON_UNMOUNT, EVENT_OPTIONS);
					container.addEventListener(AUTOFOCUS_ON_UNMOUNT, onUnmountAutoFocus);
					container.dispatchEvent(unmountEvent);
					if (!unmountEvent.defaultPrevented) focus(previouslyFocusedElement ?? document.body, { select: true });
					container.removeEventListener(AUTOFOCUS_ON_UNMOUNT, onUnmountAutoFocus);
					focusScopesStack.remove(focusScope);
				}, 0);
			};
		}
	}, [
		container,
		onMountAutoFocus,
		onUnmountAutoFocus,
		focusScope
	]);
	const handleKeyDown = import_react.useCallback((event) => {
		if (!loop && !trapped) return;
		if (focusScope.paused) return;
		const isTabKey = event.key === "Tab" && !event.altKey && !event.ctrlKey && !event.metaKey;
		const focusedElement = document.activeElement;
		if (isTabKey && focusedElement) {
			const container2 = event.currentTarget;
			const [first, last] = getTabbableEdges(container2);
			if (!(first && last)) {
				if (focusedElement === container2) event.preventDefault();
			} else if (!event.shiftKey && focusedElement === last) {
				event.preventDefault();
				if (loop) focus(first, { select: true });
			} else if (event.shiftKey && focusedElement === first) {
				event.preventDefault();
				if (loop) focus(last, { select: true });
			}
		}
	}, [
		loop,
		trapped,
		focusScope.paused
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Primitive.div, {
		tabIndex: -1,
		...scopeProps,
		ref: composedRefs,
		onKeyDown: handleKeyDown
	});
}, "FocusScope"));
function focusFirst(candidates, { select = false } = {}) {
	const previouslyFocusedElement = document.activeElement;
	for (const candidate of candidates) {
		focus(candidate, { select });
		if (document.activeElement !== previouslyFocusedElement) return;
	}
}
__name$12(focusFirst, "focusFirst");
function getTabbableEdges(container) {
	const candidates = getTabbableCandidates(container);
	return [findVisible(candidates, container), findVisible(candidates.reverse(), container)];
}
__name$12(getTabbableEdges, "getTabbableEdges");
function getTabbableCandidates(container) {
	const nodes = [];
	const walker = document.createTreeWalker(container, NodeFilter.SHOW_ELEMENT, { acceptNode: /* @__PURE__ */ __name$12((node) => {
		const isHiddenInput = node.tagName === "INPUT" && node.type === "hidden";
		if (node.disabled || node.hidden || isHiddenInput) return NodeFilter.FILTER_SKIP;
		return node.tabIndex >= 0 ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
	}, "acceptNode") });
	while (walker.nextNode()) nodes.push(walker.currentNode);
	return nodes;
}
__name$12(getTabbableCandidates, "getTabbableCandidates");
function findVisible(elements, container) {
	const canUseCheckVisibility = typeof container.checkVisibility === "function" && container.checkVisibility({ checkVisibilityCSS: true });
	for (const element of elements) if (!(canUseCheckVisibility ? !element.checkVisibility({ checkVisibilityCSS: true }) : isHidden(element, { upTo: container }))) return element;
}
__name$12(findVisible, "findVisible");
function isHidden(node, { upTo }) {
	if (getComputedStyle(node).visibility === "hidden") return true;
	while (node) {
		if (upTo !== void 0 && node === upTo) return false;
		if (getComputedStyle(node).display === "none") return true;
		node = node.parentElement;
	}
	return false;
}
__name$12(isHidden, "isHidden");
function isSelectableInput(element) {
	return element instanceof HTMLInputElement && "select" in element;
}
__name$12(isSelectableInput, "isSelectableInput");
function focus(element, { select = false } = {}) {
	if (element && element.focus) {
		const previouslyFocusedElement = document.activeElement;
		element.focus({ preventScroll: true });
		if (element !== previouslyFocusedElement && isSelectableInput(element) && select) element.select();
	}
}
__name$12(focus, "focus");
var focusScopesStack = createFocusScopesStack();
function createFocusScopesStack() {
	let stack = [];
	return {
		add(focusScope) {
			const activeFocusScope = stack[0];
			if (focusScope !== activeFocusScope) activeFocusScope?.pause();
			stack = arrayRemove(stack, focusScope);
			stack.unshift(focusScope);
		},
		remove(focusScope) {
			stack = arrayRemove(stack, focusScope);
			stack[0]?.resume();
		}
	};
}
__name$12(createFocusScopesStack, "createFocusScopesStack");
function arrayRemove(array, item) {
	const updatedArray = [...array];
	const index = updatedArray.indexOf(item);
	if (index !== -1) updatedArray.splice(index, 1);
	return updatedArray;
}
__name$12(arrayRemove, "arrayRemove");
function removeLinks(items) {
	return items.filter((item) => item.tagName !== "A");
}
__name$12(removeLinks, "removeLinks");
//#endregion
//#region node_modules/@radix-ui/react-use-layout-effect/dist/index.mjs
var useLayoutEffect2 = globalThis?.document ? import_react.useLayoutEffect : () => {};
//#endregion
//#region node_modules/@radix-ui/react-id/dist/index.mjs
var __defProp$14 = Object.defineProperty;
var __name$11 = (target, value) => __defProp$14(target, "name", {
	value,
	configurable: true
});
var useReactId = import_react[" useId ".trim().toString()] || (() => void 0);
var count = 0;
function useId(deterministicId) {
	const [id, setId] = import_react.useState(useReactId());
	useLayoutEffect2(() => {
		if (!deterministicId) setId((reactId) => reactId ?? String(count++));
	}, [deterministicId]);
	return deterministicId || (id ? `radix-${id}` : "");
}
__name$11(useId, "useId");
//#endregion
//#region node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
/**
* Custom positioning reference element.
* @see https://floating-ui.com/docs/virtual-elements
*/
var sides = [
	"top",
	"right",
	"bottom",
	"left"
];
var min = Math.min;
var max = Math.max;
var round = Math.round;
var floor = Math.floor;
var createCoords = (v) => ({
	x: v,
	y: v
});
var oppositeSideMap = {
	left: "right",
	right: "left",
	bottom: "top",
	top: "bottom"
};
function clamp(start, value, end) {
	return max(start, min(value, end));
}
function evaluate(value, param) {
	return typeof value === "function" ? value(param) : value;
}
function getSide(placement) {
	return placement.split("-")[0];
}
function getAlignment(placement) {
	return placement.split("-")[1];
}
function getOppositeAxis(axis) {
	return axis === "x" ? "y" : "x";
}
function getAxisLength(axis) {
	return axis === "y" ? "height" : "width";
}
function getSideAxis(placement) {
	const firstChar = placement[0];
	return firstChar === "t" || firstChar === "b" ? "y" : "x";
}
function getAlignmentAxis(placement) {
	return getOppositeAxis(getSideAxis(placement));
}
function getAlignmentSides(placement, rects, rtl) {
	if (rtl === void 0) rtl = false;
	const alignment = getAlignment(placement);
	const alignmentAxis = getAlignmentAxis(placement);
	const length = getAxisLength(alignmentAxis);
	let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
	if (rects.reference[length] > rects.floating[length]) mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
	return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
}
function getExpandedPlacements(placement) {
	const oppositePlacement = getOppositePlacement(placement);
	return [
		getOppositeAlignmentPlacement(placement),
		oppositePlacement,
		getOppositeAlignmentPlacement(oppositePlacement)
	];
}
function getOppositeAlignmentPlacement(placement) {
	return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
}
var lrPlacement = ["left", "right"];
var rlPlacement = ["right", "left"];
var tbPlacement = ["top", "bottom"];
var btPlacement = ["bottom", "top"];
function getSideList(side, isStart, rtl) {
	switch (side) {
		case "top":
		case "bottom":
			if (rtl) return isStart ? rlPlacement : lrPlacement;
			return isStart ? lrPlacement : rlPlacement;
		case "left":
		case "right": return isStart ? tbPlacement : btPlacement;
		default: return [];
	}
}
function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
	const alignment = getAlignment(placement);
	let list = getSideList(getSide(placement), direction === "start", rtl);
	if (alignment) {
		list = list.map((side) => side + "-" + alignment);
		if (flipAlignment) list = list.concat(list.map(getOppositeAlignmentPlacement));
	}
	return list;
}
function getOppositePlacement(placement) {
	const side = getSide(placement);
	return oppositeSideMap[side] + placement.slice(side.length);
}
function expandPaddingObject(padding) {
	var _padding$top, _padding$right, _padding$bottom, _padding$left;
	return {
		top: (_padding$top = padding.top) != null ? _padding$top : 0,
		right: (_padding$right = padding.right) != null ? _padding$right : 0,
		bottom: (_padding$bottom = padding.bottom) != null ? _padding$bottom : 0,
		left: (_padding$left = padding.left) != null ? _padding$left : 0
	};
}
function getPaddingObject(padding) {
	return typeof padding !== "number" ? expandPaddingObject(padding) : {
		top: padding,
		right: padding,
		bottom: padding,
		left: padding
	};
}
function rectToClientRect(rect) {
	const { x, y, width, height } = rect;
	return {
		width,
		height,
		top: y,
		left: x,
		right: x + width,
		bottom: y + height,
		x,
		y
	};
}
//#endregion
//#region node_modules/@floating-ui/core/dist/floating-ui.core.mjs
function computeCoordsFromPlacement(_ref, placement, rtl) {
	let { reference, floating } = _ref;
	const sideAxis = getSideAxis(placement);
	const alignmentAxis = getAlignmentAxis(placement);
	const alignLength = getAxisLength(alignmentAxis);
	const side = getSide(placement);
	const isVertical = sideAxis === "y";
	const commonX = reference.x + reference.width / 2 - floating.width / 2;
	const commonY = reference.y + reference.height / 2 - floating.height / 2;
	const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
	let coords;
	switch (side) {
		case "top":
			coords = {
				x: commonX,
				y: reference.y - floating.height
			};
			break;
		case "bottom":
			coords = {
				x: commonX,
				y: reference.y + reference.height
			};
			break;
		case "right":
			coords = {
				x: reference.x + reference.width,
				y: commonY
			};
			break;
		case "left":
			coords = {
				x: reference.x - floating.width,
				y: commonY
			};
			break;
		default: coords = {
			x: reference.x,
			y: reference.y
		};
	}
	const alignment = getAlignment(placement);
	if (alignment) coords[alignmentAxis] += commonAlign * (alignment === "end" ? 1 : -1) * (rtl && isVertical ? -1 : 1);
	return coords;
}
/**
* Resolves with an object of overflow side offsets that determine how much the
* element is overflowing a given clipping boundary on each side.
* - positive = overflowing the boundary by that number of pixels
* - negative = how many pixels left before it will overflow
* - 0 = lies flush with the boundary
* @see https://floating-ui.com/docs/detectOverflow
*/
async function detectOverflow(state, options) {
	var _await$platform$isEle;
	if (options === void 0) options = {};
	const { x, y, platform, rects, elements, strategy } = state;
	const { boundary = "clippingAncestors", rootBoundary = "viewport", elementContext = "floating", altBoundary = false, padding = 0 } = evaluate(options, state);
	const paddingObject = getPaddingObject(padding);
	const element = elements[altBoundary ? elementContext === "floating" ? "reference" : "floating" : elementContext];
	const clippingClientRect = rectToClientRect(await platform.getClippingRect({
		element: ((_await$platform$isEle = await (platform.isElement == null ? void 0 : platform.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform.getDocumentElement == null ? void 0 : platform.getDocumentElement(elements.floating)),
		boundary,
		rootBoundary,
		strategy
	}));
	const rect = elementContext === "floating" ? {
		x,
		y,
		width: rects.floating.width,
		height: rects.floating.height
	} : rects.reference;
	const offsetParent = await (platform.getOffsetParent == null ? void 0 : platform.getOffsetParent(elements.floating));
	const offsetScale = await (platform.isElement == null ? void 0 : platform.isElement(offsetParent)) && await (platform.getScale == null ? void 0 : platform.getScale(offsetParent)) || {
		x: 1,
		y: 1
	};
	const elementClientRect = rectToClientRect(platform.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform.convertOffsetParentRelativeRectToViewportRelativeRect({
		elements,
		rect,
		offsetParent,
		strategy
	}) : rect);
	return {
		top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
		bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
		left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
		right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
	};
}
var MAX_RESET_COUNT = 50;
/**
* Computes the `x` and `y` coordinates that will place the floating element
* next to a given reference element.
*
* This export does not have any `platform` interface logic. You will need to
* write one for the platform you are using Floating UI with.
*/
var computePosition$1 = async (reference, floating, config) => {
	const { placement = "bottom", strategy = "absolute", middleware = [], platform } = config;
	const platformWithDetectOverflow = platform.detectOverflow ? platform : {
		...platform,
		detectOverflow
	};
	const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(floating));
	let rects = await platform.getElementRects({
		reference,
		floating,
		strategy
	});
	let { x, y } = computeCoordsFromPlacement(rects, placement, rtl);
	let statefulPlacement = placement;
	let resetCount = 0;
	const middlewareData = {};
	for (let i = 0; i < middleware.length; i++) {
		const currentMiddleware = middleware[i];
		if (!currentMiddleware) continue;
		const { name, fn } = currentMiddleware;
		const { x: nextX, y: nextY, data, reset } = await fn({
			x,
			y,
			initialPlacement: placement,
			placement: statefulPlacement,
			strategy,
			middlewareData,
			rects,
			platform: platformWithDetectOverflow,
			elements: {
				reference,
				floating
			}
		});
		x = nextX != null ? nextX : x;
		y = nextY != null ? nextY : y;
		middlewareData[name] = {
			...middlewareData[name],
			...data
		};
		if (reset && resetCount < MAX_RESET_COUNT) {
			resetCount++;
			if (typeof reset === "object") {
				if (reset.placement) statefulPlacement = reset.placement;
				if (reset.rects) rects = reset.rects === true ? await platform.getElementRects({
					reference,
					floating,
					strategy
				}) : reset.rects;
				({x, y} = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
			}
			i = -1;
		}
	}
	return {
		x,
		y,
		placement: statefulPlacement,
		strategy,
		middlewareData
	};
};
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$3 = (options) => ({
	name: "arrow",
	options,
	async fn(state) {
		const { x, y, placement, rects, platform, elements, middlewareData } = state;
		const { element, padding = 0 } = evaluate(options, state) || {};
		if (element == null) return {};
		const paddingObject = getPaddingObject(padding);
		const coords = {
			x,
			y
		};
		const axis = getAlignmentAxis(placement);
		const length = getAxisLength(axis);
		const arrowDimensions = await platform.getDimensions(element);
		const isYAxis = axis === "y";
		const minProp = isYAxis ? "top" : "left";
		const maxProp = isYAxis ? "bottom" : "right";
		const clientProp = isYAxis ? "clientHeight" : "clientWidth";
		const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
		const startDiff = coords[axis] - rects.reference[axis];
		const arrowOffsetParent = await (platform.getOffsetParent == null ? void 0 : platform.getOffsetParent(element));
		let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
		if (!clientSize || !await (platform.isElement == null ? void 0 : platform.isElement(arrowOffsetParent))) clientSize = elements.floating[clientProp] || rects.floating[length];
		const centerToReference = endDiff / 2 - startDiff / 2;
		const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
		const minPadding = min(paddingObject[minProp], largestPossiblePadding);
		const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
		const max = clientSize - arrowDimensions[length] - maxPadding;
		const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
		const offset = clamp(minPadding, center, max);
		const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset && rects.reference[length] / 2 - (center < minPadding ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
		const alignmentOffset = shouldAddOffset ? center < minPadding ? center - minPadding : center - max : 0;
		return {
			[axis]: coords[axis] + alignmentOffset,
			data: {
				[axis]: offset,
				centerOffset: center - offset - alignmentOffset,
				...shouldAddOffset && { alignmentOffset }
			},
			reset: shouldAddOffset
		};
	}
});
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "flip",
		options,
		async fn(state) {
			var _middlewareData$arrow, _middlewareData$flip;
			const { placement, middlewareData, rects, initialPlacement, platform, elements } = state;
			const { mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = true, fallbackPlacements: specifiedFallbackPlacements, fallbackStrategy = "bestFit", fallbackAxisSideDirection = "none", flipAlignment = true, ...detectOverflowOptions } = evaluate(options, state);
			if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) return {};
			const side = getSide(placement);
			const initialSideAxis = getSideAxis(initialPlacement);
			const isBasePlacement = getSide(initialPlacement) === initialPlacement;
			const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating));
			const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
			const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
			if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
			const placements = [initialPlacement, ...fallbackPlacements];
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const overflows = [];
			let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
			if (checkMainAxis) overflows.push(overflow[side]);
			if (checkCrossAxis) {
				const sides = getAlignmentSides(placement, rects, rtl);
				overflows.push(overflow[sides[0]], overflow[sides[1]]);
			}
			overflowsData = [...overflowsData, {
				placement,
				overflows
			}];
			if (!overflows.every((side) => side <= 0)) {
				var _middlewareData$flip2, _overflowsData$filter;
				const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
				const nextPlacement = placements[nextIndex];
				if (nextPlacement) {
					if (!(checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false) || overflowsData.every((d) => getSideAxis(d.placement) === initialSideAxis ? d.overflows[0] > 0 : true)) return {
						data: {
							index: nextIndex,
							overflows: overflowsData
						},
						reset: { placement: nextPlacement }
					};
				}
				let resetPlacement = (_overflowsData$filter = overflowsData.filter((d) => d.overflows[0] <= 0).sort((a, b) => a.overflows[1] - b.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
				if (!resetPlacement) switch (fallbackStrategy) {
					case "bestFit": {
						var _overflowsData$filter2;
						const placement = (_overflowsData$filter2 = overflowsData.filter((d) => {
							if (hasFallbackAxisSideDirection) {
								const currentSideAxis = getSideAxis(d.placement);
								return currentSideAxis === initialSideAxis || currentSideAxis === "y";
							}
							return true;
						}).map((d) => [d.placement, d.overflows.filter((overflow) => overflow > 0).reduce((acc, overflow) => acc + overflow, 0)]).sort((a, b) => a[1] - b[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
						if (placement) resetPlacement = placement;
						break;
					}
					case "initialPlacement":
						resetPlacement = initialPlacement;
						break;
				}
				if (placement !== resetPlacement) return { reset: { placement: resetPlacement } };
			}
			return {};
		}
	};
};
function getSideOffsets(overflow, rect) {
	return {
		top: overflow.top - rect.height,
		right: overflow.right - rect.width,
		bottom: overflow.bottom - rect.height,
		left: overflow.left - rect.width
	};
}
function isAnySideFullyClipped(overflow) {
	return sides.some((side) => overflow[side] >= 0);
}
/**
* Provides data to hide the floating element in applicable situations, such as
* when it is not in the same clipping context as the reference element.
* @see https://floating-ui.com/docs/hide
*/
var hide$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "hide",
		options,
		async fn(state) {
			const { rects, platform } = state;
			const { strategy = "referenceHidden", ...detectOverflowOptions } = evaluate(options, state);
			switch (strategy) {
				case "referenceHidden": {
					const offsets = getSideOffsets(await platform.detectOverflow(state, {
						...detectOverflowOptions,
						elementContext: "reference"
					}), rects.reference);
					return { data: {
						referenceHiddenOffsets: offsets,
						referenceHidden: isAnySideFullyClipped(offsets)
					} };
				}
				case "escaped": {
					const offsets = getSideOffsets(await platform.detectOverflow(state, {
						...detectOverflowOptions,
						altBoundary: true
					}), rects.floating);
					return { data: {
						escapedOffsets: offsets,
						escaped: isAnySideFullyClipped(offsets)
					} };
				}
				default: return {};
			}
		}
	};
};
var originSides = /* @__PURE__ */ new Set(["left", "top"]);
async function convertValueToCoords(state, options) {
	const { placement, platform, elements } = state;
	const rtl = await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating));
	const side = getSide(placement);
	const alignment = getAlignment(placement);
	const isVertical = getSideAxis(placement) === "y";
	const mainAxisMulti = originSides.has(side) ? -1 : 1;
	const crossAxisMulti = rtl && isVertical ? -1 : 1;
	const rawValue = evaluate(options, state);
	let { mainAxis, crossAxis, alignmentAxis } = typeof rawValue === "number" ? {
		mainAxis: rawValue,
		crossAxis: 0,
		alignmentAxis: null
	} : {
		mainAxis: rawValue.mainAxis || 0,
		crossAxis: rawValue.crossAxis || 0,
		alignmentAxis: rawValue.alignmentAxis
	};
	if (alignment && typeof alignmentAxis === "number") crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
	return isVertical ? {
		x: crossAxis * crossAxisMulti,
		y: mainAxis * mainAxisMulti
	} : {
		x: mainAxis * mainAxisMulti,
		y: crossAxis * crossAxisMulti
	};
}
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$2 = function(options) {
	if (options === void 0) options = 0;
	return {
		name: "offset",
		options,
		async fn(state) {
			var _middlewareData$offse, _middlewareData$arrow;
			const { x, y, placement, middlewareData } = state;
			const diffCoords = await convertValueToCoords(state, options);
			if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) return {};
			return {
				x: x + diffCoords.x,
				y: y + diffCoords.y,
				data: {
					...diffCoords,
					placement
				}
			};
		}
	};
};
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "shift",
		options,
		async fn(state) {
			const { x, y, placement, platform } = state;
			const { mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = false, limiter = { fn: (_ref) => {
				let { x, y } = _ref;
				return {
					x,
					y
				};
			} }, ...detectOverflowOptions } = evaluate(options, state);
			const coords = {
				x,
				y
			};
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const crossAxis = getSideAxis(placement);
			const mainAxis = getOppositeAxis(crossAxis);
			let mainAxisCoord = coords[mainAxis];
			let crossAxisCoord = coords[crossAxis];
			const clampCoord = (axis, coord) => clamp(coord + overflow[axis === "y" ? "top" : "left"], coord, coord - overflow[axis === "y" ? "bottom" : "right"]);
			if (checkMainAxis) mainAxisCoord = clampCoord(mainAxis, mainAxisCoord);
			if (checkCrossAxis) crossAxisCoord = clampCoord(crossAxis, crossAxisCoord);
			const limitedCoords = limiter.fn({
				...state,
				[mainAxis]: mainAxisCoord,
				[crossAxis]: crossAxisCoord
			});
			return {
				...limitedCoords,
				data: {
					x: limitedCoords.x - x,
					y: limitedCoords.y - y,
					enabled: {
						[mainAxis]: checkMainAxis,
						[crossAxis]: checkCrossAxis
					}
				}
			};
		}
	};
};
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift$2 = function(options) {
	if (options === void 0) options = {};
	return {
		options,
		fn(state) {
			var _rawOffset$mainAxis, _rawOffset$crossAxis;
			const { x, y, placement, rects, middlewareData } = state;
			const { offset = 0, mainAxis: checkMainAxis = true, crossAxis: checkCrossAxis = true } = evaluate(options, state);
			const coords = {
				x,
				y
			};
			const crossAxis = getSideAxis(placement);
			const mainAxis = getOppositeAxis(crossAxis);
			let mainAxisCoord = coords[mainAxis];
			let crossAxisCoord = coords[crossAxis];
			const rawOffset = evaluate(offset, state);
			const computedOffset = typeof rawOffset === "number" ? {
				mainAxis: rawOffset,
				crossAxis: 0
			} : {
				mainAxis: (_rawOffset$mainAxis = rawOffset.mainAxis) != null ? _rawOffset$mainAxis : 0,
				crossAxis: (_rawOffset$crossAxis = rawOffset.crossAxis) != null ? _rawOffset$crossAxis : 0
			};
			if (checkMainAxis) {
				const len = mainAxis === "y" ? "height" : "width";
				const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
				const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
				if (mainAxisCoord < limitMin) mainAxisCoord = limitMin;
				else if (mainAxisCoord > limitMax) mainAxisCoord = limitMax;
			}
			if (checkCrossAxis) {
				var _middlewareData$offse, _middlewareData$offse2;
				const len = mainAxis === "y" ? "width" : "height";
				const isOriginSide = originSides.has(getSide(placement));
				const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
				const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
				if (crossAxisCoord < limitMin) crossAxisCoord = limitMin;
				else if (crossAxisCoord > limitMax) crossAxisCoord = limitMax;
			}
			return {
				[mainAxis]: mainAxisCoord,
				[crossAxis]: crossAxisCoord
			};
		}
	};
};
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size$2 = function(options) {
	if (options === void 0) options = {};
	return {
		name: "size",
		options,
		async fn(state) {
			const { placement, rects, platform, elements } = state;
			const { apply = () => {}, ...detectOverflowOptions } = evaluate(options, state);
			const overflow = await platform.detectOverflow(state, detectOverflowOptions);
			const side = getSide(placement);
			const alignment = getAlignment(placement);
			const isYAxis = getSideAxis(placement) === "y";
			const { width, height } = rects.floating;
			let heightSide;
			let widthSide;
			if (side === "top" || side === "bottom") {
				heightSide = side;
				widthSide = alignment === (await (platform.isRTL == null ? void 0 : platform.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
			} else {
				widthSide = side;
				heightSide = alignment === "end" ? "top" : "bottom";
			}
			const maximumClippingHeight = height - overflow.top - overflow.bottom;
			const maximumClippingWidth = width - overflow.left - overflow.right;
			const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
			const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
			const shiftData = state.middlewareData.shift;
			const noShift = !shiftData;
			let availableHeight = overflowAvailableHeight;
			let availableWidth = overflowAvailableWidth;
			if (shiftData != null && shiftData.enabled.x) availableWidth = maximumClippingWidth;
			if (shiftData != null && shiftData.enabled.y) availableHeight = maximumClippingHeight;
			if (noShift && !alignment) if (isYAxis) availableWidth = width - 2 * max(overflow.left, overflow.right);
			else availableHeight = height - 2 * max(overflow.top, overflow.bottom);
			await apply({
				...state,
				availableWidth,
				availableHeight
			});
			const nextDimensions = await platform.getDimensions(elements.floating);
			if (width !== nextDimensions.width || height !== nextDimensions.height) return { reset: { rects: true } };
			return {};
		}
	};
};
//#endregion
//#region node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
function hasWindow() {
	return typeof window !== "undefined";
}
function getNodeName(node) {
	if (isNode$1(node)) return (node.nodeName || "").toLowerCase();
	return "#document";
}
function getWindow$1(node) {
	var _node$ownerDocument;
	return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
}
function getDocumentElement(node) {
	var _ref;
	return (_ref = (isNode$1(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
}
function isNode$1(value) {
	if (!hasWindow()) return false;
	return value instanceof Node || value instanceof getWindow$1(value).Node;
}
function isElement$1(value) {
	if (!hasWindow()) return false;
	return value instanceof Element || value instanceof getWindow$1(value).Element;
}
function isHTMLElement$1(value) {
	if (!hasWindow()) return false;
	return value instanceof HTMLElement || value instanceof getWindow$1(value).HTMLElement;
}
function isShadowRoot(value) {
	if (!hasWindow() || typeof ShadowRoot === "undefined") return false;
	return value instanceof ShadowRoot || value instanceof getWindow$1(value).ShadowRoot;
}
function isOverflowElement(element) {
	const { overflow, overflowX, overflowY, display } = getComputedStyle$1(element);
	return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
}
function isTableElement(element) {
	return /^(table|td|th)$/.test(getNodeName(element));
}
function isTopLayer(element) {
	try {
		if (element.matches(":popover-open")) return true;
	} catch (_e) {}
	try {
		return element.matches(":modal");
	} catch (_e) {
		return false;
	}
}
var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
var containRe = /paint|layout|strict|content/;
var isNotNone = (value) => !!value && value !== "none";
var isWebKitValue;
function isContainingBlock(elementOrCss) {
	const css = isElement$1(elementOrCss) ? getComputedStyle$1(elementOrCss) : elementOrCss;
	return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
}
function getContainingBlock(element) {
	let currentNode = getParentNode(element);
	while (isHTMLElement$1(currentNode) && !isLastTraversableNode(currentNode)) {
		if (isContainingBlock(currentNode)) return currentNode;
		else if (isTopLayer(currentNode)) return null;
		currentNode = getParentNode(currentNode);
	}
	return null;
}
function isWebKit() {
	if (isWebKitValue == null) isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
	return isWebKitValue;
}
function isLastTraversableNode(node) {
	return /^(html|body|#document)$/.test(getNodeName(node));
}
function getComputedStyle$1(element) {
	return getWindow$1(element).getComputedStyle(element);
}
function getNodeScroll(element) {
	if (isElement$1(element)) return {
		scrollLeft: element.scrollLeft,
		scrollTop: element.scrollTop
	};
	return {
		scrollLeft: element.scrollX,
		scrollTop: element.scrollY
	};
}
function getParentNode(node) {
	if (getNodeName(node) === "html") return node;
	const result = node.assignedSlot || node.parentNode || isShadowRoot(node) && node.host || getDocumentElement(node);
	return isShadowRoot(result) ? result.host : result;
}
function getNearestOverflowAncestor(node) {
	const parentNode = getParentNode(node);
	if (isLastTraversableNode(parentNode)) return (node.ownerDocument || node).body;
	if (isHTMLElement$1(parentNode) && isOverflowElement(parentNode)) return parentNode;
	return getNearestOverflowAncestor(parentNode);
}
function getOverflowAncestors(node, list, traverseIframes) {
	var _node$ownerDocument2;
	if (list === void 0) list = [];
	if (traverseIframes === void 0) traverseIframes = true;
	const scrollableAncestor = getNearestOverflowAncestor(node);
	const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
	const win = getWindow$1(scrollableAncestor);
	if (isBody) {
		const frameElement = getFrameElement$1(win);
		return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
	} else return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
}
function getFrameElement$1(win) {
	return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
}
//#endregion
//#region node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
function getCssDimensions(element) {
	const css = getComputedStyle$1(element);
	let width = parseFloat(css.width) || 0;
	let height = parseFloat(css.height) || 0;
	const hasOffset = isHTMLElement$1(element);
	const offsetWidth = hasOffset ? element.offsetWidth : width;
	const offsetHeight = hasOffset ? element.offsetHeight : height;
	const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
	if (shouldFallback) {
		width = offsetWidth;
		height = offsetHeight;
	}
	return {
		width,
		height,
		$: shouldFallback
	};
}
function unwrapElement(element) {
	return !isElement$1(element) ? element.contextElement : element;
}
function getScale$1(element) {
	const domElement = unwrapElement(element);
	if (!isHTMLElement$1(domElement)) return createCoords(1);
	const rect = domElement.getBoundingClientRect();
	const { width, height, $ } = getCssDimensions(domElement);
	let x = ($ ? round(rect.width) : rect.width) / width;
	let y = ($ ? round(rect.height) : rect.height) / height;
	if (!x || !Number.isFinite(x)) x = 1;
	if (!y || !Number.isFinite(y)) y = 1;
	return {
		x,
		y
	};
}
var noOffsets = /* @__PURE__ */ createCoords(0);
function getVisualOffsets(element) {
	const win = getWindow$1(element);
	if (!isWebKit() || !win.visualViewport) return noOffsets;
	return {
		x: win.visualViewport.offsetLeft,
		y: win.visualViewport.offsetTop
	};
}
function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
	if (isFixed === void 0) isFixed = false;
	return !!floatingOffsetParent && isFixed && floatingOffsetParent === getWindow$1(element);
}
function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
	if (includeScale === void 0) includeScale = false;
	if (isFixedStrategy === void 0) isFixedStrategy = false;
	const clientRect = element.getBoundingClientRect();
	const domElement = unwrapElement(element);
	let scale = createCoords(1);
	if (includeScale) if (offsetParent) {
		if (isElement$1(offsetParent)) scale = getScale$1(offsetParent);
	} else scale = getScale$1(element);
	const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
	let x = (clientRect.left + visualOffsets.x) / scale.x;
	let y = (clientRect.top + visualOffsets.y) / scale.y;
	let width = clientRect.width / scale.x;
	let height = clientRect.height / scale.y;
	if (domElement && offsetParent) {
		const win = getWindow$1(domElement);
		const offsetWin = isElement$1(offsetParent) ? getWindow$1(offsetParent) : offsetParent;
		let currentWin = win;
		let currentIFrame = getFrameElement$1(currentWin);
		while (currentIFrame && offsetWin !== currentWin) {
			const iframeScale = getScale$1(currentIFrame);
			const iframeRect = currentIFrame.getBoundingClientRect();
			const css = getComputedStyle$1(currentIFrame);
			const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
			const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
			x *= iframeScale.x;
			y *= iframeScale.y;
			width *= iframeScale.x;
			height *= iframeScale.y;
			x += left;
			y += top;
			currentWin = getWindow$1(currentIFrame);
			currentIFrame = getFrameElement$1(currentWin);
		}
	}
	return rectToClientRect({
		width,
		height,
		x,
		y
	});
}
function getWindowScrollBarX(element, rect) {
	const leftScroll = getNodeScroll(element).scrollLeft;
	if (!rect) return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
	return rect.left + leftScroll;
}
function getHTMLOffset(documentElement, scroll) {
	const htmlRect = documentElement.getBoundingClientRect();
	return {
		x: htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect),
		y: htmlRect.top + scroll.scrollTop
	};
}
function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
	let { elements, rect, offsetParent, strategy } = _ref;
	const isFixed = strategy === "fixed";
	const documentElement = getDocumentElement(offsetParent);
	const topLayer = elements ? isTopLayer(elements.floating) : false;
	if (offsetParent === documentElement || topLayer && isFixed) return rect;
	let scroll = {
		scrollLeft: 0,
		scrollTop: 0
	};
	let scale = createCoords(1);
	const offsets = createCoords(0);
	const isOffsetParentAnElement = isHTMLElement$1(offsetParent);
	if (isOffsetParentAnElement || !isFixed) {
		if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) scroll = getNodeScroll(offsetParent);
		if (isOffsetParentAnElement) {
			const offsetRect = getBoundingClientRect(offsetParent);
			scale = getScale$1(offsetParent);
			offsets.x = offsetRect.x + offsetParent.clientLeft;
			offsets.y = offsetRect.y + offsetParent.clientTop;
		}
	}
	const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
	return {
		width: rect.width * scale.x,
		height: rect.height * scale.y,
		x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
		y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
	};
}
function getClientRects(element) {
	return element.getClientRects ? Array.from(element.getClientRects()) : [];
}
function getDocumentRect(html) {
	const scroll = getNodeScroll(html);
	const body = html.ownerDocument.body;
	const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
	const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
	let x = -scroll.scrollLeft + getWindowScrollBarX(html);
	const y = -scroll.scrollTop;
	if (getComputedStyle$1(body).direction === "rtl") x += max(html.clientWidth, body.clientWidth) - width;
	return {
		width,
		height,
		x,
		y
	};
}
var SCROLLBAR_MAX = 25;
function getViewportRect(element, strategy, rootBoundary) {
	if (rootBoundary === void 0) rootBoundary = "viewport";
	const isLayoutViewport = rootBoundary === "layoutViewport";
	const win = getWindow$1(element);
	const html = getDocumentElement(element);
	const visualViewport = win.visualViewport;
	let width = html.clientWidth;
	let height = html.clientHeight;
	let x = 0;
	let y = 0;
	if (visualViewport) {
		const layoutRelativeClientCoords = !isWebKit() || strategy === "fixed";
		if (isLayoutViewport) {
			if (!layoutRelativeClientCoords) {
				x = -visualViewport.offsetLeft;
				y = -visualViewport.offsetTop;
			}
		} else {
			width = visualViewport.width;
			height = visualViewport.height;
			if (layoutRelativeClientCoords) {
				x = visualViewport.offsetLeft;
				y = visualViewport.offsetTop;
			}
		}
	}
	if (getWindowScrollBarX(html) <= 0) {
		const doc = html.ownerDocument;
		const body = doc.body;
		const bodyStyles = getComputedStyle(body);
		const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
		const reservedWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
		const gutter = getComputedStyle(html).scrollbarGutter === "stable both-edges" ? reservedWidth / 2 : reservedWidth;
		if (gutter <= SCROLLBAR_MAX) width -= gutter;
	}
	return {
		width,
		height,
		x,
		y
	};
}
function getInnerBoundingClientRect(element, strategy) {
	const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
	const top = clientRect.top + element.clientTop;
	const left = clientRect.left + element.clientLeft;
	const scale = getScale$1(element);
	return {
		width: element.clientWidth * scale.x,
		height: element.clientHeight * scale.y,
		x: left * scale.x,
		y: top * scale.y
	};
}
function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
	let rect;
	if (clippingAncestor === "viewport" || clippingAncestor === "layoutViewport") rect = getViewportRect(element, strategy, clippingAncestor);
	else if (clippingAncestor === "document") rect = getDocumentRect(getDocumentElement(element));
	else if (isElement$1(clippingAncestor)) rect = getInnerBoundingClientRect(clippingAncestor, strategy);
	else {
		const visualOffsets = getVisualOffsets(element);
		rect = {
			x: clippingAncestor.x - visualOffsets.x,
			y: clippingAncestor.y - visualOffsets.y,
			width: clippingAncestor.width,
			height: clippingAncestor.height
		};
	}
	return rectToClientRect(rect);
}
function getClippingElementAncestors(element, cache) {
	const cachedResult = cache.get(element);
	if (cachedResult) return cachedResult;
	let result = getOverflowAncestors(element, [], false).filter((el) => isElement$1(el) && getNodeName(el) !== "body");
	let lastKeptComputedStyle = null;
	const elementIsFixed = getComputedStyle$1(element).position === "fixed";
	let currentNode = elementIsFixed ? getParentNode(element) : element;
	while (isElement$1(currentNode) && !isLastTraversableNode(currentNode)) {
		const computedStyle = getComputedStyle$1(currentNode);
		const currentNodeIsContaining = isContainingBlock(currentNode);
		const lastPosition = lastKeptComputedStyle ? lastKeptComputedStyle.position : elementIsFixed ? "fixed" : "";
		if (!currentNodeIsContaining && (lastPosition === "fixed" || lastPosition === "absolute" && computedStyle.position === "static")) result = result.filter((ancestor) => ancestor !== currentNode);
		else lastKeptComputedStyle = computedStyle;
		currentNode = getParentNode(currentNode);
	}
	cache.set(element, result);
	return result;
}
function getClippingRect(_ref) {
	let { element, boundary, rootBoundary, strategy } = _ref;
	const clippingAncestors = [...boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary), rootBoundary];
	const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
	let top = firstRect.top;
	let right = firstRect.right;
	let bottom = firstRect.bottom;
	let left = firstRect.left;
	for (let i = 1; i < clippingAncestors.length; i++) {
		const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i], strategy);
		top = max(rect.top, top);
		right = min(rect.right, right);
		bottom = min(rect.bottom, bottom);
		left = max(rect.left, left);
	}
	return {
		width: right - left,
		height: bottom - top,
		x: left,
		y: top
	};
}
function getDimensions(element) {
	const { width, height } = getCssDimensions(element);
	return {
		width,
		height
	};
}
function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
	const isOffsetParentAnElement = isHTMLElement$1(offsetParent);
	const documentElement = getDocumentElement(offsetParent);
	const isFixed = strategy === "fixed";
	const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
	let scroll = {
		scrollLeft: 0,
		scrollTop: 0
	};
	const offsets = createCoords(0);
	if (isOffsetParentAnElement || !isFixed) {
		if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) scroll = getNodeScroll(offsetParent);
		if (isOffsetParentAnElement) {
			const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
			offsets.x = offsetRect.x + offsetParent.clientLeft;
			offsets.y = offsetRect.y + offsetParent.clientTop;
		}
	}
	if (!isOffsetParentAnElement && documentElement) offsets.x = getWindowScrollBarX(documentElement);
	const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
	return {
		x: rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x,
		y: rect.top + scroll.scrollTop - offsets.y - htmlOffset.y,
		width: rect.width,
		height: rect.height
	};
}
function isStaticPositioned(element) {
	return getComputedStyle$1(element).position === "static";
}
function getTrueOffsetParent(element, polyfill) {
	if (!isHTMLElement$1(element) || getComputedStyle$1(element).position === "fixed") return null;
	if (polyfill) return polyfill(element);
	let rawOffsetParent = element.offsetParent;
	if (getDocumentElement(element) === rawOffsetParent) rawOffsetParent = rawOffsetParent.ownerDocument.body;
	return rawOffsetParent;
}
function getOffsetParent(element, polyfill) {
	const win = getWindow$1(element);
	if (isTopLayer(element)) return win;
	if (!isHTMLElement$1(element)) {
		let svgOffsetParent = getParentNode(element);
		while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
			if (isElement$1(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) return svgOffsetParent;
			svgOffsetParent = getParentNode(svgOffsetParent);
		}
		return win;
	}
	let offsetParent = getTrueOffsetParent(element, polyfill);
	while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) offsetParent = getTrueOffsetParent(offsetParent, polyfill);
	if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) return win;
	return offsetParent || getContainingBlock(element) || win;
}
var getElementRects = async function(data) {
	const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
	const getDimensionsFn = this.getDimensions;
	const floatingDimensions = await getDimensionsFn(data.floating);
	return {
		reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
		floating: {
			x: 0,
			y: 0,
			width: floatingDimensions.width,
			height: floatingDimensions.height
		}
	};
};
function isRTL(element) {
	return getComputedStyle$1(element).direction === "rtl";
}
var platform = {
	convertOffsetParentRelativeRectToViewportRelativeRect,
	getDocumentElement,
	getClippingRect,
	getOffsetParent,
	getElementRects,
	getClientRects,
	getDimensions,
	getScale: getScale$1,
	isElement: isElement$1,
	isRTL
};
function rectsAreEqual(a, b) {
	return a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
}
function observeMove(element, onMove, ancestorResize) {
	let io = null;
	let timeoutId;
	const root = getDocumentElement(element);
	function cleanup() {
		var _io;
		clearTimeout(timeoutId);
		(_io = io) == null || _io.disconnect();
		io = null;
	}
	function refresh(skip, threshold) {
		if (skip === void 0) skip = false;
		if (threshold === void 0) threshold = 1;
		cleanup();
		const elementRectForRootMargin = element.getBoundingClientRect();
		const { left, top, width, height } = elementRectForRootMargin;
		if (!skip) onMove();
		if (!width || !height) return;
		const insetTop = floor(top);
		const insetRight = floor(root.clientWidth - (left + width));
		const insetBottom = floor(root.clientHeight - (top + height));
		const insetLeft = floor(left);
		const options = {
			rootMargin: -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px",
			threshold: max(0, min(1, threshold)) || 1
		};
		let isFirstUpdate = true;
		function handleObserve(entries) {
			const ratio = entries[0].intersectionRatio;
			if (!rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) return refresh();
			if (ratio !== threshold) {
				if (!isFirstUpdate) return refresh();
				if (!ratio) timeoutId = setTimeout(() => {
					refresh(false, 1e-7);
				}, 1e3);
				else refresh(false, ratio);
			}
			isFirstUpdate = false;
		}
		try {
			io = new IntersectionObserver(handleObserve, {
				...options,
				root: root.ownerDocument
			});
		} catch (_e) {
			io = new IntersectionObserver(handleObserve, options);
		}
		io.observe(element);
	}
	const win = getWindow$1(element);
	const handleResize = () => refresh(ancestorResize);
	win.addEventListener("resize", handleResize);
	refresh(true);
	return () => {
		win.removeEventListener("resize", handleResize);
		cleanup();
	};
}
/**
* Automatically updates the position of the floating element when necessary.
* Should only be called when the floating element is mounted on the DOM or
* visible on the screen.
* @returns cleanup function that should be invoked when the floating element is
* removed from the DOM or hidden from the screen.
* @see https://floating-ui.com/docs/autoUpdate
*/
function autoUpdate(reference, floating, update, options) {
	if (options === void 0) options = {};
	const { ancestorScroll = true, ancestorResize = true, elementResize = typeof ResizeObserver === "function", layoutShift = typeof IntersectionObserver === "function", animationFrame = false } = options;
	const referenceEl = unwrapElement(reference);
	const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
	ancestors.forEach((ancestor) => {
		ancestorScroll && ancestor.addEventListener("scroll", update);
		ancestorResize && ancestor.addEventListener("resize", update);
	});
	const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update, ancestorResize) : null;
	let reobserveFrame = -1;
	let resizeObserver = null;
	if (elementResize) {
		resizeObserver = new ResizeObserver((_ref) => {
			let [firstEntry] = _ref;
			if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
				resizeObserver.unobserve(floating);
				cancelAnimationFrame(reobserveFrame);
				reobserveFrame = requestAnimationFrame(() => {
					var _resizeObserver;
					(_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
				});
			}
			update();
		});
		if (referenceEl && !animationFrame) resizeObserver.observe(referenceEl);
		if (floating) resizeObserver.observe(floating);
	}
	let frameId;
	let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
	if (animationFrame) frameLoop();
	function frameLoop() {
		const nextRefRect = getBoundingClientRect(reference);
		if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) update();
		prevRefRect = nextRefRect;
		frameId = requestAnimationFrame(frameLoop);
	}
	update();
	return () => {
		var _resizeObserver2;
		ancestors.forEach((ancestor) => {
			ancestorScroll && ancestor.removeEventListener("scroll", update);
			ancestorResize && ancestor.removeEventListener("resize", update);
		});
		cleanupIo?.();
		(_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
		resizeObserver = null;
		if (animationFrame) cancelAnimationFrame(frameId);
	};
}
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset$1 = offset$2;
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift$1 = shift$2;
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip$1 = flip$2;
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size$1 = size$2;
/**
* Provides data to hide the floating element in applicable situations, such as
* when it is not in the same clipping context as the reference element.
* @see https://floating-ui.com/docs/hide
*/
var hide$1 = hide$2;
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$2 = arrow$3;
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift$1 = limitShift$2;
/**
* Computes the `x` and `y` coordinates that will place the floating element
* next to a given reference element.
*/
var computePosition = (reference, floating, options) => {
	const cache = /* @__PURE__ */ new Map();
	const mergedOptions = options != null ? options : {};
	const platformWithCache = {
		...platform,
		...mergedOptions.platform,
		_c: cache
	};
	return computePosition$1(reference, floating, {
		...mergedOptions,
		platform: platformWithCache
	});
};
//#endregion
//#region node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
var index = typeof document !== "undefined" ? import_react.useLayoutEffect : function noop() {};
function deepEqual$1(a, b) {
	if (a === b) return true;
	if (typeof a !== typeof b) return false;
	if (typeof a === "function" && a.toString() === b.toString()) return true;
	let length;
	let i;
	let keys;
	if (a && b && typeof a === "object") {
		if (Array.isArray(a)) {
			length = a.length;
			if (length !== b.length) return false;
			for (i = length; i-- !== 0;) if (!deepEqual$1(a[i], b[i])) return false;
			return true;
		}
		keys = Object.keys(a);
		length = keys.length;
		if (length !== Object.keys(b).length) return false;
		for (i = length; i-- !== 0;) if (!{}.hasOwnProperty.call(b, keys[i])) return false;
		for (i = length; i-- !== 0;) {
			const key = keys[i];
			if (key === "_owner" && a.$$typeof) continue;
			if (!deepEqual$1(a[key], b[key])) return false;
		}
		return true;
	}
	return a !== a && b !== b;
}
function getDPR(element) {
	if (typeof window === "undefined") return 1;
	return (element.ownerDocument.defaultView || window).devicePixelRatio || 1;
}
function roundByDPR(element, value) {
	const dpr = getDPR(element);
	return Math.round(value * dpr) / dpr;
}
function useLatestRef(value) {
	const ref = import_react.useRef(value);
	index(() => {
		ref.current = value;
	});
	return ref;
}
/**
* Provides data to position a floating element.
* @see https://floating-ui.com/docs/useFloating
*/
function useFloating(options) {
	if (options === void 0) options = {};
	const { placement = "bottom", strategy = "absolute", middleware = [], platform, elements: { reference: externalReference, floating: externalFloating } = {}, transform = true, whileElementsMounted, open } = options;
	const [data, setData] = import_react.useState({
		x: 0,
		y: 0,
		strategy,
		placement,
		middlewareData: {},
		isPositioned: false
	});
	const [latestMiddleware, setLatestMiddleware] = import_react.useState(middleware);
	if (!deepEqual$1(latestMiddleware, middleware)) setLatestMiddleware(middleware);
	const [_reference, _setReference] = import_react.useState(null);
	const [_floating, _setFloating] = import_react.useState(null);
	const setReference = import_react.useCallback((node) => {
		if (node !== referenceRef.current) {
			referenceRef.current = node;
			_setReference(node);
		}
	}, []);
	const setFloating = import_react.useCallback((node) => {
		if (node !== floatingRef.current) {
			floatingRef.current = node;
			_setFloating(node);
		}
	}, []);
	const referenceEl = externalReference || _reference;
	const floatingEl = externalFloating || _floating;
	const referenceRef = import_react.useRef(null);
	const floatingRef = import_react.useRef(null);
	const dataRef = import_react.useRef(data);
	const hasWhileElementsMounted = whileElementsMounted != null;
	const whileElementsMountedRef = useLatestRef(whileElementsMounted);
	const platformRef = useLatestRef(platform);
	const openRef = useLatestRef(open);
	const update = import_react.useCallback(() => {
		if (!referenceRef.current || !floatingRef.current) return;
		const config = {
			placement,
			strategy,
			middleware: latestMiddleware
		};
		if (platformRef.current) config.platform = platformRef.current;
		computePosition(referenceRef.current, floatingRef.current, config).then((data) => {
			const fullData = {
				...data,
				isPositioned: openRef.current !== false
			};
			if (isMountedRef.current && !deepEqual$1(dataRef.current, fullData)) {
				dataRef.current = fullData;
				import_react_dom.flushSync(() => {
					setData(fullData);
				});
			}
		});
	}, [
		latestMiddleware,
		placement,
		strategy,
		platformRef,
		openRef
	]);
	index(() => {
		if (open === false && dataRef.current.isPositioned) {
			dataRef.current.isPositioned = false;
			setData((data) => ({
				...data,
				isPositioned: false
			}));
		}
	}, [open]);
	const isMountedRef = import_react.useRef(false);
	index(() => {
		isMountedRef.current = true;
		return () => {
			isMountedRef.current = false;
		};
	}, []);
	index(() => {
		if (referenceEl) referenceRef.current = referenceEl;
		if (floatingEl) floatingRef.current = floatingEl;
		if (referenceEl && floatingEl) {
			if (whileElementsMountedRef.current) return whileElementsMountedRef.current(referenceEl, floatingEl, update);
			update();
		}
	}, [
		referenceEl,
		floatingEl,
		update,
		whileElementsMountedRef,
		hasWhileElementsMounted
	]);
	const refs = import_react.useMemo(() => ({
		reference: referenceRef,
		floating: floatingRef,
		setReference,
		setFloating
	}), [setReference, setFloating]);
	const elements = import_react.useMemo(() => ({
		reference: referenceEl,
		floating: floatingEl
	}), [referenceEl, floatingEl]);
	const floatingStyles = import_react.useMemo(() => {
		const initialStyles = {
			position: strategy,
			left: 0,
			top: 0
		};
		if (!elements.floating) return initialStyles;
		const x = roundByDPR(elements.floating, data.x);
		const y = roundByDPR(elements.floating, data.y);
		if (transform) return {
			...initialStyles,
			transform: "translate(" + x + "px, " + y + "px)",
			...getDPR(elements.floating) >= 1.5 && { willChange: "transform" }
		};
		return {
			position: strategy,
			left: x,
			top: y
		};
	}, [
		strategy,
		transform,
		elements.floating,
		data.x,
		data.y
	]);
	return import_react.useMemo(() => ({
		...data,
		update,
		refs,
		elements,
		floatingStyles
	}), [
		data,
		update,
		refs,
		elements,
		floatingStyles
	]);
}
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* This wraps the core `arrow` middleware to allow React refs as the element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow$1 = (options) => {
	function isRef(value) {
		return {}.hasOwnProperty.call(value, "current");
	}
	return {
		name: "arrow",
		options,
		fn(state) {
			const { element, padding } = typeof options === "function" ? options(state) : options;
			if (element && isRef(element)) {
				if (element.current != null) return arrow$2({
					element: element.current,
					padding
				}).fn(state);
				return {};
			}
			if (element) return arrow$2({
				element,
				padding
			}).fn(state);
			return {};
		}
	};
};
/**
* Modifies the placement by translating the floating element along the
* specified axes.
* A number (shorthand for `mainAxis` or distance), or an axes configuration
* object may be passed.
* @see https://floating-ui.com/docs/offset
*/
var offset = (options, deps) => {
	const result = offset$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Optimizes the visibility of the floating element by shifting it in order to
* keep it in view when it will overflow the clipping boundary.
* @see https://floating-ui.com/docs/shift
*/
var shift = (options, deps) => {
	const result = shift$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Built-in `limiter` that will stop `shift()` at a certain point.
*/
var limitShift = (options, deps) => {
	return {
		fn: limitShift$1(options).fn,
		options: [options, deps]
	};
};
/**
* Optimizes the visibility of the floating element by flipping the `placement`
* in order to keep it in view when the preferred placement(s) will overflow the
* clipping boundary. Alternative to `autoPlacement`.
* @see https://floating-ui.com/docs/flip
*/
var flip = (options, deps) => {
	const result = flip$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Provides data that allows you to change the size of the floating element —
* for instance, prevent it from overflowing the clipping boundary or match the
* width of the reference element.
* @see https://floating-ui.com/docs/size
*/
var size = (options, deps) => {
	const result = size$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Provides data to hide the floating element in applicable situations, such as
* when it is not in the same clipping context as the reference element.
* @see https://floating-ui.com/docs/hide
*/
var hide = (options, deps) => {
	const result = hide$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
/**
* Provides data to position an inner element of the floating element so that it
* appears centered to the reference element.
* This wraps the core `arrow` middleware to allow React refs as the element.
* @see https://floating-ui.com/docs/arrow
*/
var arrow = (options, deps) => {
	const result = arrow$1(options);
	return {
		name: result.name,
		fn: result.fn,
		options: [options, deps]
	};
};
//#endregion
//#region node_modules/@radix-ui/react-use-size/dist/index.mjs
var __defProp$13 = Object.defineProperty;
var __name$10 = (target, value) => __defProp$13(target, "name", {
	value,
	configurable: true
});
function useSize(element) {
	const [size, setSize] = import_react.useState(void 0);
	useLayoutEffect2(() => {
		if (element) {
			setSize({
				width: element.offsetWidth,
				height: element.offsetHeight
			});
			const resizeObserver = new ResizeObserver((entries) => {
				if (!Array.isArray(entries)) return;
				if (!entries.length) return;
				const entry = entries[0];
				let width;
				let height;
				if ("borderBoxSize" in entry) {
					const borderSizeEntry = entry["borderBoxSize"];
					const borderSize = Array.isArray(borderSizeEntry) ? borderSizeEntry[0] : borderSizeEntry;
					width = borderSize["inlineSize"];
					height = borderSize["blockSize"];
				} else {
					width = element.offsetWidth;
					height = element.offsetHeight;
				}
				setSize({
					width,
					height
				});
			});
			resizeObserver.observe(element, { box: "border-box" });
			return () => resizeObserver.unobserve(element);
		} else setSize(void 0);
	}, [element]);
	return size;
}
__name$10(useSize, "useSize");
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@radix-ui/react-popover/node_modules/@radix-ui/react-popper/dist/index.mjs
var __defProp$12 = Object.defineProperty;
var __name$9 = (target, value) => __defProp$12(target, "name", {
	value,
	configurable: true
});
var POPPER_NAME = "Popper";
var [createPopperContext, createPopperScope] = /* @__PURE__ */ createContextScope(POPPER_NAME);
var [PopperProvider, usePopperContext] = createPopperContext(POPPER_NAME);
var Popper = /* @__PURE__ */ __name$9((props) => {
	const { __scopePopper, children } = props;
	const [anchor, setAnchor] = import_react.useState(null);
	const [placementState, setPlacementState] = import_react.useState(void 0);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopperProvider, {
		scope: __scopePopper,
		anchor,
		onAnchorChange: setAnchor,
		placementState,
		setPlacementState,
		children
	});
}, "Popper");
var ANCHOR_NAME = "PopperAnchor";
var PopperAnchor = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$9(function PopperAnchor2(props, forwardedRef) {
	const { __scopePopper, virtualRef, ...anchorProps } = props;
	const context = usePopperContext(ANCHOR_NAME, __scopePopper);
	const ref = import_react.useRef(null);
	const onAnchorChange = context.onAnchorChange;
	const composedRefs = useComposedRefs(forwardedRef, import_react.useCallback((node) => {
		ref.current = node;
		if (node) onAnchorChange(node);
	}, [onAnchorChange]));
	const anchorRef = import_react.useRef(null);
	import_react.useEffect(() => {
		if (!virtualRef) return;
		const previousAnchor = anchorRef.current;
		anchorRef.current = virtualRef.current;
		if (previousAnchor !== anchorRef.current) onAnchorChange(anchorRef.current);
	});
	const sideAndAlign = context.placementState && getSideAndAlignFromPlacement(context.placementState);
	const placedSide = sideAndAlign?.[0];
	const placedAlign = sideAndAlign?.[1];
	return virtualRef ? null : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Primitive.div, {
		"data-radix-popper-side": placedSide,
		"data-radix-popper-align": placedAlign,
		...anchorProps,
		ref: composedRefs
	});
}, "PopperAnchor"));
var CONTENT_NAME$1 = "PopperContent";
var [PopperContentProvider, useContentContext] = createPopperContext(CONTENT_NAME$1);
var PopperContent = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$9(function PopperContent2(props, forwardedRef) {
	const { __scopePopper, side = "bottom", sideOffset = 0, align = "center", alignOffset = 0, arrowPadding = 0, avoidCollisions = true, collisionBoundary = [], collisionPadding: collisionPaddingProp = 0, sticky = "partial", hideWhenDetached = false, updatePositionStrategy = "optimized", onPlaced, ...contentProps } = props;
	const context = usePopperContext(CONTENT_NAME$1, __scopePopper);
	const [content, setContent] = import_react.useState(null);
	const composedRefs = useComposedRefs(forwardedRef, setContent);
	const [arrow$4, setArrow] = import_react.useState(null);
	const arrowSize = useSize(arrow$4);
	const arrowWidth = arrowSize?.width ?? 0;
	const arrowHeight = arrowSize?.height ?? 0;
	const desiredPlacement = side + (align !== "center" ? "-" + align : "");
	const collisionPadding = typeof collisionPaddingProp === "number" ? collisionPaddingProp : {
		top: 0,
		right: 0,
		bottom: 0,
		left: 0,
		...collisionPaddingProp
	};
	const boundary = Array.isArray(collisionBoundary) ? collisionBoundary : [collisionBoundary];
	const hasExplicitBoundaries = boundary.length > 0;
	const detectOverflowOptions = {
		padding: collisionPadding,
		boundary: boundary.filter(isNotNull),
		altBoundary: hasExplicitBoundaries
	};
	const { refs, floatingStyles, placement, isPositioned, middlewareData } = useFloating({
		strategy: "fixed",
		placement: desiredPlacement,
		whileElementsMounted: /* @__PURE__ */ __name$9((...args) => {
			return autoUpdate(...args, { animationFrame: updatePositionStrategy === "always" });
		}, "whileElementsMounted"),
		elements: { reference: context.anchor },
		middleware: [
			offset({
				mainAxis: sideOffset + arrowHeight,
				alignmentAxis: alignOffset
			}),
			avoidCollisions && shift({
				mainAxis: true,
				crossAxis: false,
				limiter: sticky === "partial" ? limitShift() : void 0,
				...detectOverflowOptions
			}),
			avoidCollisions && flip({ ...detectOverflowOptions }),
			size({
				...detectOverflowOptions,
				apply: /* @__PURE__ */ __name$9(({ elements, rects, availableWidth, availableHeight }) => {
					const { width: anchorWidth, height: anchorHeight } = rects.reference;
					const contentStyle = elements.floating.style;
					contentStyle.setProperty("--radix-popper-available-width", `${availableWidth}px`);
					contentStyle.setProperty("--radix-popper-available-height", `${availableHeight}px`);
					contentStyle.setProperty("--radix-popper-anchor-width", `${anchorWidth}px`);
					contentStyle.setProperty("--radix-popper-anchor-height", `${anchorHeight}px`);
				}, "apply")
			}),
			arrow$4 && arrow({
				element: arrow$4,
				padding: arrowPadding
			}),
			transformOrigin({
				arrowWidth,
				arrowHeight
			}),
			hideWhenDetached && hide({
				strategy: "referenceHidden",
				...detectOverflowOptions,
				boundary: hasExplicitBoundaries ? detectOverflowOptions.boundary : void 0
			})
		]
	});
	const setPlacementState = context.setPlacementState;
	useLayoutEffect2(() => {
		setPlacementState(placement);
		return () => {
			setPlacementState(void 0);
		};
	}, [placement, setPlacementState]);
	const [placedSide, placedAlign] = getSideAndAlignFromPlacement(placement);
	const handlePlaced = useCallbackRef$1(onPlaced);
	useLayoutEffect2(() => {
		if (isPositioned) handlePlaced?.();
	}, [isPositioned, handlePlaced]);
	const arrowX = middlewareData.arrow?.x;
	const arrowY = middlewareData.arrow?.y;
	const cannotCenterArrow = middlewareData.arrow?.centerOffset !== 0;
	const [contentZIndex, setContentZIndex] = import_react.useState();
	useLayoutEffect2(() => {
		if (content) setContentZIndex(window.getComputedStyle(content).zIndex);
	}, [content]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref: refs.setFloating,
		"data-radix-popper-content-wrapper": "",
		style: {
			...floatingStyles,
			transform: isPositioned ? floatingStyles.transform : "translate(0, -200%)",
			minWidth: "max-content",
			zIndex: contentZIndex,
			"--radix-popper-transform-origin": [middlewareData.transformOrigin?.x, middlewareData.transformOrigin?.y].join(" "),
			...middlewareData.hide?.referenceHidden && {
				visibility: "hidden",
				pointerEvents: "none"
			}
		},
		dir: props.dir,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopperContentProvider, {
			scope: __scopePopper,
			placedSide,
			placedAlign,
			onArrowChange: setArrow,
			arrowX,
			arrowY,
			shouldHideArrow: cannotCenterArrow,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Primitive.div, {
				"data-side": placedSide,
				"data-align": placedAlign,
				...contentProps,
				ref: composedRefs,
				style: {
					...contentProps.style,
					animation: !isPositioned ? "none" : contentProps.style?.animation
				}
			})
		})
	});
}, "PopperContent"));
function isNotNull(value) {
	return value !== null;
}
__name$9(isNotNull, "isNotNull");
var transformOrigin = /* @__PURE__ */ __name$9((options) => ({
	name: "transformOrigin",
	options,
	fn(data) {
		const { placement, rects, middlewareData } = data;
		const isArrowHidden = middlewareData.arrow?.centerOffset !== 0;
		const arrowWidth = isArrowHidden ? 0 : options.arrowWidth;
		const arrowHeight = isArrowHidden ? 0 : options.arrowHeight;
		const [placedSide, placedAlign] = getSideAndAlignFromPlacement(placement);
		const noArrowAlign = {
			start: "0%",
			center: "50%",
			end: "100%"
		}[placedAlign];
		const arrowXCenter = (middlewareData.arrow?.x ?? 0) + arrowWidth / 2;
		const arrowYCenter = (middlewareData.arrow?.y ?? 0) + arrowHeight / 2;
		let x = "";
		let y = "";
		if (placedSide === "bottom") {
			x = isArrowHidden ? noArrowAlign : `${arrowXCenter}px`;
			y = `${-arrowHeight}px`;
		} else if (placedSide === "top") {
			x = isArrowHidden ? noArrowAlign : `${arrowXCenter}px`;
			y = `${rects.floating.height + arrowHeight}px`;
		} else if (placedSide === "right") {
			x = `${-arrowHeight}px`;
			y = isArrowHidden ? noArrowAlign : `${arrowYCenter}px`;
		} else if (placedSide === "left") {
			x = `${rects.floating.width + arrowHeight}px`;
			y = isArrowHidden ? noArrowAlign : `${arrowYCenter}px`;
		}
		return { data: {
			x,
			y
		} };
	}
}), "transformOrigin");
function getSideAndAlignFromPlacement(placement) {
	const [side, align = "center"] = placement.split("-");
	return [side, align];
}
__name$9(getSideAndAlignFromPlacement, "getSideAndAlignFromPlacement");
var Root2 = Popper;
var Anchor = PopperAnchor;
var Content = PopperContent;
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@radix-ui/react-popover/node_modules/@radix-ui/react-portal/dist/index.mjs
var __defProp$11 = Object.defineProperty;
var __name$8 = (target, value) => __defProp$11(target, "name", {
	value,
	configurable: true
});
var Portal = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$8(function Portal2(props, forwardedRef) {
	const { container: containerProp, ...portalProps } = props;
	const [mounted, setMounted] = import_react.useState(false);
	useLayoutEffect2(() => setMounted(true), []);
	const container = containerProp || mounted && globalThis?.document?.body;
	return container ? import_react_dom.createPortal(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Primitive.div, {
		...portalProps,
		ref: forwardedRef
	}), container) : null;
}, "Portal"));
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@radix-ui/react-popover/node_modules/@radix-ui/react-presence/dist/index.mjs
var __defProp$10 = Object.defineProperty;
var __name$7 = (target, value) => __defProp$10(target, "name", {
	value,
	configurable: true
});
function useStateMachine(initialState, machine) {
	return import_react.useReducer((state, event) => {
		return machine[state][event] ?? state;
	}, initialState);
}
__name$7(useStateMachine, "useStateMachine");
var Presence = /* @__PURE__ */ __name$7((props) => {
	const { present, children } = props;
	const presence = usePresence(present);
	const child = typeof children === "function" ? children({ present: presence.isPresent }) : import_react.Children.only(children);
	const ref = useStableComposedRefs(presence.ref, getElementRef(child));
	return typeof children === "function" || presence.isPresent ? import_react.cloneElement(child, { ref }) : null;
}, "Presence");
function usePresence(present) {
	const [node, setNode] = import_react.useState();
	const stylesRef = import_react.useRef(null);
	const prevPresentRef = import_react.useRef(present);
	const prevAnimationNameRef = import_react.useRef("none");
	const mountAnimationNameRef = import_react.useRef(void 0);
	const [state, send] = useStateMachine(present ? "mounted" : "unmounted", {
		mounted: {
			UNMOUNT: "unmounted",
			ANIMATION_OUT: "unmountSuspended"
		},
		unmountSuspended: {
			MOUNT: "mounted",
			ANIMATION_END: "unmounted"
		},
		unmounted: { MOUNT: "mounted" }
	});
	import_react.useEffect(() => {
		if (state === "mounted") {
			prevAnimationNameRef.current = mountAnimationNameRef.current ?? getAnimationName(stylesRef.current);
			mountAnimationNameRef.current = void 0;
		} else prevAnimationNameRef.current = "none";
	}, [state]);
	useLayoutEffect2(() => {
		const styles = stylesRef.current;
		const wasPresent = prevPresentRef.current;
		if (wasPresent !== present) {
			const prevAnimationName = prevAnimationNameRef.current;
			const currentAnimationName = getAnimationName(styles);
			if (present) {
				mountAnimationNameRef.current = currentAnimationName;
				send("MOUNT");
			} else if (currentAnimationName === "none" || styles?.display === "none") send("UNMOUNT");
			else if (wasPresent && prevAnimationName !== currentAnimationName) send("ANIMATION_OUT");
			else send("UNMOUNT");
			prevPresentRef.current = present;
		}
	}, [present, send]);
	useLayoutEffect2(() => {
		if (node) {
			let timeoutId;
			const ownerWindow = node.ownerDocument.defaultView ?? window;
			const handleAnimationEnd = /* @__PURE__ */ __name$7((event) => {
				const isCurrentAnimation = getAnimationName(stylesRef.current).includes(CSS.escape(event.animationName));
				if (event.target === node && isCurrentAnimation) {
					send("ANIMATION_END");
					if (!prevPresentRef.current) {
						const currentFillMode = node.style.animationFillMode;
						node.style.animationFillMode = "forwards";
						timeoutId = ownerWindow.setTimeout(() => {
							if (node.style.animationFillMode === "forwards") node.style.animationFillMode = currentFillMode;
						});
					}
				}
			}, "handleAnimationEnd");
			const handleAnimationStart = /* @__PURE__ */ __name$7((event) => {
				if (event.target === node) prevAnimationNameRef.current = getAnimationName(stylesRef.current);
			}, "handleAnimationStart");
			node.addEventListener("animationstart", handleAnimationStart);
			node.addEventListener("animationcancel", handleAnimationEnd);
			node.addEventListener("animationend", handleAnimationEnd);
			return () => {
				ownerWindow.clearTimeout(timeoutId);
				node.removeEventListener("animationstart", handleAnimationStart);
				node.removeEventListener("animationcancel", handleAnimationEnd);
				node.removeEventListener("animationend", handleAnimationEnd);
			};
		} else send("ANIMATION_END");
	}, [node, send]);
	return {
		isPresent: ["mounted", "unmountSuspended"].includes(state),
		ref: import_react.useCallback((node2) => {
			if (node2) {
				const styles = getComputedStyle(node2);
				stylesRef.current = styles;
				mountAnimationNameRef.current = getAnimationName(styles);
			} else stylesRef.current = null;
			setNode(node2);
		}, [])
	};
}
__name$7(usePresence, "usePresence");
function setRef(ref, value) {
	if (typeof ref === "function") return ref(value);
	else if (ref !== null && ref !== void 0) ref.current = value;
}
__name$7(setRef, "setRef");
function useStableComposedRefs(...refs) {
	const refsRef = import_react.useRef(refs);
	refsRef.current = refs;
	return import_react.useCallback((node) => {
		const currentRefs = refsRef.current;
		let hasCleanup = false;
		const cleanups = currentRefs.map((ref) => {
			const cleanup = setRef(ref, node);
			if (!hasCleanup && typeof cleanup === "function") hasCleanup = true;
			return cleanup;
		});
		if (hasCleanup) return () => {
			for (let i = 0; i < cleanups.length; i++) {
				const cleanup = cleanups[i];
				if (typeof cleanup === "function") cleanup();
				else setRef(currentRefs[i], null);
			}
		};
	}, []);
}
__name$7(useStableComposedRefs, "useStableComposedRefs");
function getAnimationName(styles) {
	return styles?.animationName || "none";
}
__name$7(getAnimationName, "getAnimationName");
function getElementRef(element) {
	let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
	let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
	if (mayWarn) return element.ref;
	getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
	mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
	if (mayWarn) return element.props.ref;
	return element.props.ref || element.ref;
}
__name$7(getElementRef, "getElementRef");
//#endregion
//#region node_modules/@radix-ui/react-use-effect-event/dist/index.mjs
var __defProp$9 = Object.defineProperty;
var __name$6 = (target, value) => __defProp$9(target, "name", {
	value,
	configurable: true
});
var useReactEffectEvent = import_react[" useEffectEvent ".trim().toString()];
var useReactInsertionEffect = import_react[" useInsertionEffect ".trim().toString()];
function useEffectEvent(callback) {
	if (typeof useReactEffectEvent === "function") return useReactEffectEvent(callback);
	const ref = import_react.useRef(() => {
		throw new Error("Cannot call an event handler while rendering.");
	});
	if (typeof useReactInsertionEffect === "function") useReactInsertionEffect(() => {
		ref.current = callback;
	});
	else useLayoutEffect2(() => {
		ref.current = callback;
	});
	return import_react.useMemo(() => ((...args) => ref.current?.(...args)), []);
}
__name$6(useEffectEvent, "useEffectEvent");
//#endregion
//#region node_modules/@radix-ui/react-use-controllable-state/dist/index.mjs
var __defProp$8 = Object.defineProperty;
var __name$5 = (target, value) => __defProp$8(target, "name", {
	value,
	configurable: true
});
var useInsertionEffect = import_react[" useInsertionEffect ".trim().toString()] || useLayoutEffect2;
function useControllableState({ prop, defaultProp, onChange = /* @__PURE__ */ __name$5(() => {}, "onChange"), caller }) {
	const [uncontrolledProp, setUncontrolledProp, onChangeRef] = useUncontrolledState({
		defaultProp,
		onChange
	});
	const isControlled = prop !== void 0;
	return [isControlled ? prop : uncontrolledProp, import_react.useCallback((nextValue) => {
		if (isControlled) {
			const value2 = isFunction(nextValue) ? nextValue(prop) : nextValue;
			if (value2 !== prop) onChangeRef.current?.(value2);
		} else setUncontrolledProp(nextValue);
	}, [
		isControlled,
		prop,
		setUncontrolledProp,
		onChangeRef
	])];
}
__name$5(useControllableState, "useControllableState");
function useUncontrolledState({ defaultProp, onChange }) {
	const [value, setValue] = import_react.useState(defaultProp);
	const prevValueRef = import_react.useRef(value);
	const onChangeRef = import_react.useRef(onChange);
	useInsertionEffect(() => {
		onChangeRef.current = onChange;
	}, [onChange]);
	import_react.useEffect(() => {
		if (prevValueRef.current !== value) {
			onChangeRef.current?.(value);
			prevValueRef.current = value;
		}
	}, [value, prevValueRef]);
	return [
		value,
		setValue,
		onChangeRef
	];
}
__name$5(useUncontrolledState, "useUncontrolledState");
function isFunction(value) {
	return typeof value === "function";
}
__name$5(isFunction, "isFunction");
var SYNC_STATE = Symbol("RADIX:SYNC_STATE");
function useControllableStateReducer(reducer, userArgs, initialArg, init) {
	const { prop: controlledState, defaultProp, onChange: onChangeProp, caller } = userArgs;
	const isControlled = controlledState !== void 0;
	const onChange = useEffectEvent(onChangeProp);
	const args = [{
		...initialArg,
		state: defaultProp
	}];
	if (init) args.push(init);
	const [internalState, dispatch] = import_react.useReducer((state2, action) => {
		if (action.type === SYNC_STATE) return {
			...state2,
			state: action.state
		};
		const next = reducer(state2, action);
		if (isControlled && !Object.is(next.state, state2.state)) onChange(next.state);
		return next;
	}, ...args);
	const uncontrolledState = internalState.state;
	const prevValueRef = import_react.useRef(uncontrolledState);
	import_react.useEffect(() => {
		if (prevValueRef.current !== uncontrolledState) {
			prevValueRef.current = uncontrolledState;
			if (!isControlled) onChange(uncontrolledState);
		}
	}, [
		uncontrolledState,
		prevValueRef,
		isControlled
	]);
	const state = import_react.useMemo(() => {
		if (controlledState !== void 0) return {
			...internalState,
			state: controlledState
		};
		return internalState;
	}, [internalState, controlledState]);
	import_react.useEffect(() => {
		if (isControlled && !Object.is(controlledState, internalState.state)) dispatch({
			type: SYNC_STATE,
			state: controlledState
		});
	}, [
		controlledState,
		internalState.state,
		isControlled
	]);
	return [state, dispatch];
}
__name$5(useControllableStateReducer, "useControllableStateReducer");
//#endregion
//#region node_modules/aria-hidden/dist/es2015/index.js
var getDefaultParent = function(originalTarget) {
	if (typeof document === "undefined") return null;
	return (Array.isArray(originalTarget) ? originalTarget[0] : originalTarget).ownerDocument.body;
};
var counterMap = /* @__PURE__ */ new WeakMap();
var uncontrolledNodes = /* @__PURE__ */ new WeakMap();
var markerMap = {};
var lockCount = 0;
var unwrapHost = function(node) {
	return node && (node.host || unwrapHost(node.parentNode));
};
var correctTargets = function(parent, targets) {
	return targets.map(function(target) {
		if (parent.contains(target)) return target;
		var correctedTarget = unwrapHost(target);
		if (correctedTarget && parent.contains(correctedTarget)) return correctedTarget;
		console.error("aria-hidden", target, "in not contained inside", parent, ". Doing nothing");
		return null;
	}).filter(function(x) {
		return Boolean(x);
	});
};
/**
* Marks everything except given node(or nodes) as aria-hidden
* @param {Element | Element[]} originalTarget - elements to keep on the page
* @param [parentNode] - top element, defaults to document.body
* @param {String} [markerName] - a special attribute to mark every node
* @param {String} [controlAttribute] - html Attribute to control
* @return {Undo} undo command
*/
var applyAttributeToOthers = function(originalTarget, parentNode, markerName, controlAttribute) {
	var targets = correctTargets(parentNode, Array.isArray(originalTarget) ? originalTarget : [originalTarget]);
	if (!markerMap[markerName]) markerMap[markerName] = /* @__PURE__ */ new WeakMap();
	var markerCounter = markerMap[markerName];
	var hiddenNodes = [];
	var elementsToKeep = /* @__PURE__ */ new Set();
	var elementsToStop = new Set(targets);
	var keep = function(el) {
		if (!el || elementsToKeep.has(el)) return;
		elementsToKeep.add(el);
		keep(el.parentNode);
	};
	targets.forEach(keep);
	var deep = function(parent) {
		if (!parent || elementsToStop.has(parent)) return;
		Array.prototype.forEach.call(parent.children, function(node) {
			if (elementsToKeep.has(node)) deep(node);
			else try {
				var attr = node.getAttribute(controlAttribute);
				var alreadyHidden = attr !== null && attr !== "false";
				var counterValue = (counterMap.get(node) || 0) + 1;
				var markerValue = (markerCounter.get(node) || 0) + 1;
				counterMap.set(node, counterValue);
				markerCounter.set(node, markerValue);
				hiddenNodes.push(node);
				if (counterValue === 1 && alreadyHidden) uncontrolledNodes.set(node, true);
				if (markerValue === 1) node.setAttribute(markerName, "true");
				if (!alreadyHidden) node.setAttribute(controlAttribute, "true");
			} catch (e) {
				console.error("aria-hidden: cannot operate on ", node, e);
			}
		});
	};
	deep(parentNode);
	elementsToKeep.clear();
	lockCount++;
	return function() {
		hiddenNodes.forEach(function(node) {
			var counterValue = counterMap.get(node) - 1;
			var markerValue = markerCounter.get(node) - 1;
			counterMap.set(node, counterValue);
			markerCounter.set(node, markerValue);
			if (!counterValue) {
				if (!uncontrolledNodes.has(node)) node.removeAttribute(controlAttribute);
				uncontrolledNodes.delete(node);
			}
			if (!markerValue) node.removeAttribute(markerName);
		});
		lockCount--;
		if (!lockCount) {
			counterMap = /* @__PURE__ */ new WeakMap();
			counterMap = /* @__PURE__ */ new WeakMap();
			uncontrolledNodes = /* @__PURE__ */ new WeakMap();
			markerMap = {};
		}
	};
};
/**
* Marks everything except given node(or nodes) as aria-hidden
* @param {Element | Element[]} originalTarget - elements to keep on the page
* @param [parentNode] - top element, defaults to document.body
* @param {String} [markerName] - a special attribute to mark every node
* @return {Undo} undo command
*/
var hideOthers = function(originalTarget, parentNode, markerName) {
	if (markerName === void 0) markerName = "data-aria-hidden";
	var targets = Array.from(Array.isArray(originalTarget) ? originalTarget : [originalTarget]);
	var activeParentNode = parentNode || getDefaultParent(originalTarget);
	if (!activeParentNode) return function() {
		return null;
	};
	targets.push.apply(targets, Array.from(activeParentNode.querySelectorAll("[aria-live], script")));
	return applyAttributeToOthers(targets, activeParentNode, markerName, "aria-hidden");
};
//#endregion
//#region node_modules/tslib/tslib.es6.mjs
var __assign = function() {
	__assign = Object.assign || function __assign(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign.apply(this, arguments);
};
function __rest(s, e) {
	var t = {};
	for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
	if (s != null && typeof Object.getOwnPropertySymbols === "function") {
		for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
	}
	return t;
}
function __spreadArray(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
}
//#endregion
//#region node_modules/react-remove-scroll-bar/dist/es2015/constants.js
var zeroRightClassName = "right-scroll-bar-position";
var fullWidthClassName = "width-before-scroll-bar";
var noScrollbarsClassName = "with-scroll-bars-hidden";
/**
* Name of a CSS variable containing the amount of "hidden" scrollbar
* ! might be undefined ! use will fallback!
*/
var removedBarSizeVariable = "--removed-body-scroll-bar-size";
//#endregion
//#region node_modules/use-callback-ref/dist/es2015/assignRef.js
/**
* Assigns a value for a given ref, no matter of the ref format
* @param {RefObject} ref - a callback function or ref object
* @param value - a new value
*
* @see https://github.com/theKashey/use-callback-ref#assignref
* @example
* const refObject = useRef();
* const refFn = (ref) => {....}
*
* assignRef(refObject, "refValue");
* assignRef(refFn, "refValue");
*/
function assignRef$1(ref, value) {
	if (typeof ref === "function") ref(value);
	else if (ref) ref.current = value;
	return ref;
}
//#endregion
//#region node_modules/use-callback-ref/dist/es2015/useRef.js
/**
* creates a MutableRef with ref change callback
* @param initialValue - initial ref value
* @param {Function} callback - a callback to run when value changes
*
* @example
* const ref = useCallbackRef(0, (newValue, oldValue) => console.log(oldValue, '->', newValue);
* ref.current = 1;
* // prints 0 -> 1
*
* @see https://reactjs.org/docs/hooks-reference.html#useref
* @see https://github.com/theKashey/use-callback-ref#usecallbackref---to-replace-reactuseref
* @returns {MutableRefObject}
*/
function useCallbackRef(initialValue, callback) {
	var ref = (0, import_react.useState)(function() {
		return {
			value: initialValue,
			callback,
			facade: {
				get current() {
					return ref.value;
				},
				set current(value) {
					var last = ref.value;
					if (last !== value) {
						ref.value = value;
						ref.callback(value, last);
					}
				}
			}
		};
	})[0];
	ref.callback = callback;
	return ref.facade;
}
//#endregion
//#region node_modules/use-callback-ref/dist/es2015/useMergeRef.js
var useIsomorphicLayoutEffect$2 = typeof window !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
var currentValues = /* @__PURE__ */ new WeakMap();
/**
* Merges two or more refs together providing a single interface to set their value
* @param {RefObject|Ref} refs
* @returns {MutableRefObject} - a new ref, which translates all changes to {refs}
*
* @see {@link mergeRefs} a version without buit-in memoization
* @see https://github.com/theKashey/use-callback-ref#usemergerefs
* @example
* const Component = React.forwardRef((props, ref) => {
*   const ownRef = useRef();
*   const domRef = useMergeRefs([ref, ownRef]); // 👈 merge together
*   return <div ref={domRef}>...</div>
* }
*/
function useMergeRefs(refs, defaultValue) {
	var callbackRef = useCallbackRef(defaultValue || null, function(newValue) {
		return refs.forEach(function(ref) {
			return assignRef$1(ref, newValue);
		});
	});
	useIsomorphicLayoutEffect$2(function() {
		var oldValue = currentValues.get(callbackRef);
		if (oldValue) {
			var prevRefs_1 = new Set(oldValue);
			var nextRefs_1 = new Set(refs);
			var current_1 = callbackRef.current;
			prevRefs_1.forEach(function(ref) {
				if (!nextRefs_1.has(ref)) assignRef$1(ref, null);
			});
			nextRefs_1.forEach(function(ref) {
				if (!prevRefs_1.has(ref)) assignRef$1(ref, current_1);
			});
		}
		currentValues.set(callbackRef, refs);
	}, [refs]);
	return callbackRef;
}
//#endregion
//#region node_modules/use-sidecar/dist/es2015/medium.js
function ItoI(a) {
	return a;
}
function innerCreateMedium(defaults, middleware) {
	if (middleware === void 0) middleware = ItoI;
	var buffer = [];
	var assigned = false;
	return {
		read: function() {
			if (assigned) throw new Error("Sidecar: could not `read` from an `assigned` medium. `read` could be used only with `useMedium`.");
			if (buffer.length) return buffer[buffer.length - 1];
			return defaults;
		},
		useMedium: function(data) {
			var item = middleware(data, assigned);
			buffer.push(item);
			return function() {
				buffer = buffer.filter(function(x) {
					return x !== item;
				});
			};
		},
		assignSyncMedium: function(cb) {
			assigned = true;
			while (buffer.length) {
				var cbs = buffer;
				buffer = [];
				cbs.forEach(cb);
			}
			buffer = {
				push: function(x) {
					return cb(x);
				},
				filter: function() {
					return buffer;
				}
			};
		},
		assignMedium: function(cb) {
			assigned = true;
			var pendingQueue = [];
			if (buffer.length) {
				var cbs = buffer;
				buffer = [];
				cbs.forEach(cb);
				pendingQueue = buffer;
			}
			var executeQueue = function() {
				var cbs = pendingQueue;
				pendingQueue = [];
				cbs.forEach(cb);
			};
			var cycle = function() {
				return Promise.resolve().then(executeQueue);
			};
			cycle();
			buffer = {
				push: function(x) {
					pendingQueue.push(x);
					cycle();
				},
				filter: function(filter) {
					pendingQueue = pendingQueue.filter(filter);
					return buffer;
				}
			};
		}
	};
}
function createSidecarMedium(options) {
	if (options === void 0) options = {};
	var medium = innerCreateMedium(null);
	medium.options = __assign({
		async: true,
		ssr: false
	}, options);
	return medium;
}
//#endregion
//#region node_modules/use-sidecar/dist/es2015/exports.js
var SideCar = function(_a) {
	var sideCar = _a.sideCar, rest = __rest(_a, ["sideCar"]);
	if (!sideCar) throw new Error("Sidecar: please provide `sideCar` property to import the right car");
	var Target = sideCar.read();
	if (!Target) throw new Error("Sidecar medium not found");
	return import_react.createElement(Target, __assign({}, rest));
};
SideCar.isSideCarExport = true;
function exportSidecar(medium, exported) {
	medium.useMedium(exported);
	return SideCar;
}
//#endregion
//#region node_modules/react-remove-scroll/dist/es2015/medium.js
var effectCar = createSidecarMedium();
//#endregion
//#region node_modules/react-remove-scroll/dist/es2015/UI.js
var nothing = function() {};
/**
* Removes scrollbar from the page and contain the scroll within the Lock
*/
var RemoveScroll = import_react.forwardRef(function(props, parentRef) {
	var ref = import_react.useRef(null);
	var _a = import_react.useState({
		onScrollCapture: nothing,
		onWheelCapture: nothing,
		onTouchMoveCapture: nothing
	}), callbacks = _a[0], setCallbacks = _a[1];
	var forwardProps = props.forwardProps, children = props.children, className = props.className, removeScrollBar = props.removeScrollBar, enabled = props.enabled, shards = props.shards, sideCar = props.sideCar, noRelative = props.noRelative, noIsolation = props.noIsolation, inert = props.inert, allowPinchZoom = props.allowPinchZoom, _b = props.as, Container = _b === void 0 ? "div" : _b, gapMode = props.gapMode, rest = __rest(props, [
		"forwardProps",
		"children",
		"className",
		"removeScrollBar",
		"enabled",
		"shards",
		"sideCar",
		"noRelative",
		"noIsolation",
		"inert",
		"allowPinchZoom",
		"as",
		"gapMode"
	]);
	var SideCar = sideCar;
	var containerRef = useMergeRefs([ref, parentRef]);
	var containerProps = __assign(__assign({}, rest), callbacks);
	return import_react.createElement(import_react.Fragment, null, enabled && import_react.createElement(SideCar, {
		sideCar: effectCar,
		removeScrollBar,
		shards,
		noRelative,
		noIsolation,
		inert,
		setCallbacks,
		allowPinchZoom: !!allowPinchZoom,
		lockRef: ref,
		gapMode
	}), forwardProps ? import_react.cloneElement(import_react.Children.only(children), __assign(__assign({}, containerProps), { ref: containerRef })) : import_react.createElement(Container, __assign({}, containerProps, {
		className,
		ref: containerRef
	}), children));
});
RemoveScroll.defaultProps = {
	enabled: true,
	removeScrollBar: true,
	inert: false
};
RemoveScroll.classNames = {
	fullWidth: fullWidthClassName,
	zeroRight: zeroRightClassName
};
//#endregion
//#region node_modules/get-nonce/dist/es2015/index.js
var currentNonce;
var getNonce = function() {
	if (currentNonce) return currentNonce;
	if (typeof __webpack_nonce__ !== "undefined") return __webpack_nonce__;
};
//#endregion
//#region node_modules/react-style-singleton/dist/es2015/singleton.js
function makeStyleTag() {
	if (!document) return null;
	var tag = document.createElement("style");
	tag.type = "text/css";
	var nonce = getNonce();
	if (nonce) tag.setAttribute("nonce", nonce);
	return tag;
}
function injectStyles(tag, css) {
	if (tag.styleSheet) tag.styleSheet.cssText = css;
	else tag.appendChild(document.createTextNode(css));
}
function insertStyleTag(tag) {
	(document.head || document.getElementsByTagName("head")[0]).appendChild(tag);
}
var stylesheetSingleton = function() {
	var counter = 0;
	var stylesheet = null;
	return {
		add: function(style) {
			if (counter == 0) {
				if (stylesheet = makeStyleTag()) {
					injectStyles(stylesheet, style);
					insertStyleTag(stylesheet);
				}
			}
			counter++;
		},
		remove: function() {
			counter--;
			if (!counter && stylesheet) {
				stylesheet.parentNode && stylesheet.parentNode.removeChild(stylesheet);
				stylesheet = null;
			}
		}
	};
};
//#endregion
//#region node_modules/react-style-singleton/dist/es2015/hook.js
/**
* creates a hook to control style singleton
* @see {@link styleSingleton} for a safer component version
* @example
* ```tsx
* const useStyle = styleHookSingleton();
* ///
* useStyle('body { overflow: hidden}');
*/
var styleHookSingleton = function() {
	var sheet = stylesheetSingleton();
	return function(styles, isDynamic) {
		import_react.useEffect(function() {
			sheet.add(styles);
			return function() {
				sheet.remove();
			};
		}, [styles && isDynamic]);
	};
};
//#endregion
//#region node_modules/react-style-singleton/dist/es2015/component.js
/**
* create a Component to add styles on demand
* - styles are added when first instance is mounted
* - styles are removed when the last instance is unmounted
* - changing styles in runtime does nothing unless dynamic is set. But with multiple components that can lead to the undefined behavior
*/
var styleSingleton = function() {
	var useStyle = styleHookSingleton();
	var Sheet = function(_a) {
		var styles = _a.styles, dynamic = _a.dynamic;
		useStyle(styles, dynamic);
		return null;
	};
	return Sheet;
};
//#endregion
//#region node_modules/react-remove-scroll-bar/dist/es2015/utils.js
var zeroGap = {
	left: 0,
	top: 0,
	right: 0,
	gap: 0
};
var parse$1 = function(x) {
	return parseInt(x || "", 10) || 0;
};
var getOffset = function(gapMode) {
	var cs = window.getComputedStyle(document.body);
	var left = cs[gapMode === "padding" ? "paddingLeft" : "marginLeft"];
	var top = cs[gapMode === "padding" ? "paddingTop" : "marginTop"];
	var right = cs[gapMode === "padding" ? "paddingRight" : "marginRight"];
	return [
		parse$1(left),
		parse$1(top),
		parse$1(right)
	];
};
var getGapWidth = function(gapMode) {
	if (gapMode === void 0) gapMode = "margin";
	if (typeof window === "undefined") return zeroGap;
	var offsets = getOffset(gapMode);
	var documentWidth = document.documentElement.clientWidth;
	var windowWidth = window.innerWidth;
	return {
		left: offsets[0],
		top: offsets[1],
		right: offsets[2],
		gap: Math.max(0, windowWidth - documentWidth + offsets[2] - offsets[0])
	};
};
//#endregion
//#region node_modules/react-remove-scroll-bar/dist/es2015/component.js
var Style = styleSingleton();
var lockAttribute = "data-scroll-locked";
var getStyles$1 = function(_a, allowRelative, gapMode, important) {
	var left = _a.left, top = _a.top, right = _a.right, gap = _a.gap;
	if (gapMode === void 0) gapMode = "margin";
	return "\n  .".concat(noScrollbarsClassName, " {\n   overflow: hidden ").concat(important, ";\n   padding-right: ").concat(gap, "px ").concat(important, ";\n  }\n  body[").concat(lockAttribute, "] {\n    overflow: hidden ").concat(important, ";\n    overscroll-behavior: contain;\n    ").concat([
		allowRelative && "position: relative ".concat(important, ";"),
		gapMode === "margin" && "\n    padding-left: ".concat(left, "px;\n    padding-top: ").concat(top, "px;\n    padding-right: ").concat(right, "px;\n    margin-left:0;\n    margin-top:0;\n    margin-right: ").concat(gap, "px ").concat(important, ";\n    "),
		gapMode === "padding" && "padding-right: ".concat(gap, "px ").concat(important, ";")
	].filter(Boolean).join(""), "\n  }\n  \n  .").concat(zeroRightClassName, " {\n    right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(fullWidthClassName, " {\n    margin-right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(zeroRightClassName, " .").concat(zeroRightClassName, " {\n    right: 0 ").concat(important, ";\n  }\n  \n  .").concat(fullWidthClassName, " .").concat(fullWidthClassName, " {\n    margin-right: 0 ").concat(important, ";\n  }\n  \n  body[").concat(lockAttribute, "] {\n    ").concat(removedBarSizeVariable, ": ").concat(gap, "px;\n  }\n");
};
var getCurrentUseCounter = function() {
	var counter = parseInt(document.body.getAttribute("data-scroll-locked") || "0", 10);
	return isFinite(counter) ? counter : 0;
};
var useLockAttribute = function() {
	import_react.useEffect(function() {
		document.body.setAttribute(lockAttribute, (getCurrentUseCounter() + 1).toString());
		return function() {
			var newCounter = getCurrentUseCounter() - 1;
			if (newCounter <= 0) document.body.removeAttribute(lockAttribute);
			else document.body.setAttribute(lockAttribute, newCounter.toString());
		};
	}, []);
};
/**
* Removes page scrollbar and blocks page scroll when mounted
*/
var RemoveScrollBar = function(_a) {
	var noRelative = _a.noRelative, noImportant = _a.noImportant, _b = _a.gapMode, gapMode = _b === void 0 ? "margin" : _b;
	useLockAttribute();
	var gap = import_react.useMemo(function() {
		return getGapWidth(gapMode);
	}, [gapMode]);
	return import_react.createElement(Style, { styles: getStyles$1(gap, !noRelative, gapMode, !noImportant ? "!important" : "") });
};
//#endregion
//#region node_modules/react-remove-scroll/dist/es2015/aggresiveCapture.js
var passiveSupported = false;
if (typeof window !== "undefined") try {
	var options$1 = Object.defineProperty({}, "passive", { get: function() {
		passiveSupported = true;
		return true;
	} });
	window.addEventListener("test", options$1, options$1);
	window.removeEventListener("test", options$1, options$1);
} catch (err) {
	passiveSupported = false;
}
var nonPassive = passiveSupported ? { passive: false } : false;
//#endregion
//#region node_modules/react-remove-scroll/dist/es2015/handleScroll.js
var alwaysContainsScroll = function(node) {
	return node.tagName === "TEXTAREA";
};
var elementCanBeScrolled = function(node, overflow) {
	if (!(node instanceof Element)) return false;
	var styles = window.getComputedStyle(node);
	return styles[overflow] !== "hidden" && !(styles.overflowY === styles.overflowX && !alwaysContainsScroll(node) && styles[overflow] === "visible");
};
var elementCouldBeVScrolled = function(node) {
	return elementCanBeScrolled(node, "overflowY");
};
var elementCouldBeHScrolled = function(node) {
	return elementCanBeScrolled(node, "overflowX");
};
var locationCouldBeScrolled = function(axis, node) {
	var ownerDocument = node.ownerDocument;
	var current = node;
	do {
		if (typeof ShadowRoot !== "undefined" && current instanceof ShadowRoot) current = current.host;
		if (elementCouldBeScrolled(axis, current)) {
			var _a = getScrollVariables(axis, current);
			if (_a[1] > _a[2]) return true;
		}
		current = current.parentNode;
	} while (current && current !== ownerDocument.body);
	return false;
};
var getVScrollVariables = function(_a) {
	return [
		_a.scrollTop,
		_a.scrollHeight,
		_a.clientHeight
	];
};
var getHScrollVariables = function(_a) {
	return [
		_a.scrollLeft,
		_a.scrollWidth,
		_a.clientWidth
	];
};
var elementCouldBeScrolled = function(axis, node) {
	return axis === "v" ? elementCouldBeVScrolled(node) : elementCouldBeHScrolled(node);
};
var getScrollVariables = function(axis, node) {
	return axis === "v" ? getVScrollVariables(node) : getHScrollVariables(node);
};
var getDirectionFactor = function(axis, direction) {
	/**
	* If the element's direction is rtl (right-to-left), then scrollLeft is 0 when the scrollbar is at its rightmost position,
	* and then increasingly negative as you scroll towards the end of the content.
	* @see https://developer.mozilla.org/en-US/docs/Web/API/Element/scrollLeft
	*/
	return axis === "h" && direction === "rtl" ? -1 : 1;
};
var handleScroll = function(axis, endTarget, event, sourceDelta, noOverscroll) {
	var directionFactor = getDirectionFactor(axis, window.getComputedStyle(endTarget).direction);
	var delta = directionFactor * sourceDelta;
	var target = event.target;
	var targetInLock = endTarget.contains(target);
	var shouldCancelScroll = false;
	var isDeltaPositive = delta > 0;
	var availableScroll = 0;
	var availableScrollTop = 0;
	do {
		if (!target) break;
		var _a = getScrollVariables(axis, target), position = _a[0];
		var elementScroll = _a[1] - _a[2] - directionFactor * position;
		if (position || elementScroll) {
			if (elementCouldBeScrolled(axis, target)) {
				availableScroll += elementScroll;
				availableScrollTop += position;
			}
		}
		var parent_1 = target.parentNode;
		target = parent_1 && parent_1.nodeType === Node.DOCUMENT_FRAGMENT_NODE ? parent_1.host : parent_1;
	} while (!targetInLock && target !== document.body || targetInLock && (endTarget.contains(target) || endTarget === target));
	if (isDeltaPositive && (noOverscroll && Math.abs(availableScroll) < 1 || !noOverscroll && delta > availableScroll)) shouldCancelScroll = true;
	else if (!isDeltaPositive && (noOverscroll && Math.abs(availableScrollTop) < 1 || !noOverscroll && -delta > availableScrollTop)) shouldCancelScroll = true;
	return shouldCancelScroll;
};
//#endregion
//#region node_modules/react-remove-scroll/dist/es2015/SideEffect.js
var getTouchXY = function(event) {
	return "changedTouches" in event ? [event.changedTouches[0].clientX, event.changedTouches[0].clientY] : [0, 0];
};
var getDeltaXY = function(event) {
	return [event.deltaX, event.deltaY];
};
var extractRef = function(ref) {
	return ref && "current" in ref ? ref.current : ref;
};
var deltaCompare = function(x, y) {
	return x[0] === y[0] && x[1] === y[1];
};
var generateStyle = function(id) {
	return "\n  .block-interactivity-".concat(id, " {pointer-events: none;}\n  .allow-interactivity-").concat(id, " {pointer-events: all;}\n");
};
var idCounter = 0;
var lockStack = [];
function RemoveScrollSideCar(props) {
	var shouldPreventQueue = import_react.useRef([]);
	var touchStartRef = import_react.useRef([0, 0]);
	var activeAxis = import_react.useRef();
	var id = import_react.useState(idCounter++)[0];
	var Style = import_react.useState(styleSingleton)[0];
	var lastProps = import_react.useRef(props);
	import_react.useEffect(function() {
		lastProps.current = props;
	}, [props]);
	import_react.useEffect(function() {
		if (props.inert) {
			document.body.classList.add("block-interactivity-".concat(id));
			var allow_1 = __spreadArray([props.lockRef.current], (props.shards || []).map(extractRef), true).filter(Boolean);
			allow_1.forEach(function(el) {
				return el.classList.add("allow-interactivity-".concat(id));
			});
			return function() {
				document.body.classList.remove("block-interactivity-".concat(id));
				allow_1.forEach(function(el) {
					return el.classList.remove("allow-interactivity-".concat(id));
				});
			};
		}
	}, [
		props.inert,
		props.lockRef.current,
		props.shards
	]);
	var shouldCancelEvent = import_react.useCallback(function(event, parent) {
		if ("touches" in event && event.touches.length === 2 || event.type === "wheel" && event.ctrlKey) return !lastProps.current.allowPinchZoom;
		var touch = getTouchXY(event);
		var touchStart = touchStartRef.current;
		var deltaX = "deltaX" in event ? event.deltaX : touchStart[0] - touch[0];
		var deltaY = "deltaY" in event ? event.deltaY : touchStart[1] - touch[1];
		var currentAxis;
		var target = event.target;
		var moveDirection = Math.abs(deltaX) > Math.abs(deltaY) ? "h" : "v";
		if ("touches" in event && moveDirection === "h" && target.type === "range") return false;
		var selection = window.getSelection();
		var anchorNode = selection && selection.anchorNode;
		if (anchorNode ? anchorNode === target || anchorNode.contains(target) : false) return false;
		var canBeScrolledInMainDirection = locationCouldBeScrolled(moveDirection, target);
		if (!canBeScrolledInMainDirection) return true;
		if (canBeScrolledInMainDirection) currentAxis = moveDirection;
		else {
			currentAxis = moveDirection === "v" ? "h" : "v";
			canBeScrolledInMainDirection = locationCouldBeScrolled(moveDirection, target);
		}
		if (!canBeScrolledInMainDirection) return false;
		if (!activeAxis.current && "changedTouches" in event && (deltaX || deltaY)) activeAxis.current = currentAxis;
		if (!currentAxis) return true;
		var cancelingAxis = activeAxis.current || currentAxis;
		return handleScroll(cancelingAxis, parent, event, cancelingAxis === "h" ? deltaX : deltaY, true);
	}, []);
	var shouldPrevent = import_react.useCallback(function(_event) {
		var event = _event;
		if (!lockStack.length || lockStack[lockStack.length - 1] !== Style) return;
		var delta = "deltaY" in event ? getDeltaXY(event) : getTouchXY(event);
		var sourceEvent = shouldPreventQueue.current.filter(function(e) {
			return e.name === event.type && (e.target === event.target || event.target === e.shadowParent) && deltaCompare(e.delta, delta);
		})[0];
		if (sourceEvent && sourceEvent.should) {
			if (event.cancelable) event.preventDefault();
			return;
		}
		if (!sourceEvent) {
			var shardNodes = (lastProps.current.shards || []).map(extractRef).filter(Boolean).filter(function(node) {
				return node.contains(event.target);
			});
			if (shardNodes.length > 0 ? shouldCancelEvent(event, shardNodes[0]) : !lastProps.current.noIsolation) {
				if (event.cancelable) event.preventDefault();
			}
		}
	}, []);
	var shouldCancel = import_react.useCallback(function(name, delta, target, should) {
		var event = {
			name,
			delta,
			target,
			should,
			shadowParent: getOutermostShadowParent(target)
		};
		shouldPreventQueue.current.push(event);
		setTimeout(function() {
			shouldPreventQueue.current = shouldPreventQueue.current.filter(function(e) {
				return e !== event;
			});
		}, 1);
	}, []);
	var scrollTouchStart = import_react.useCallback(function(event) {
		touchStartRef.current = getTouchXY(event);
		activeAxis.current = void 0;
	}, []);
	var scrollWheel = import_react.useCallback(function(event) {
		shouldCancel(event.type, getDeltaXY(event), event.target, shouldCancelEvent(event, props.lockRef.current));
	}, []);
	var scrollTouchMove = import_react.useCallback(function(event) {
		shouldCancel(event.type, getTouchXY(event), event.target, shouldCancelEvent(event, props.lockRef.current));
	}, []);
	import_react.useEffect(function() {
		lockStack.push(Style);
		props.setCallbacks({
			onScrollCapture: scrollWheel,
			onWheelCapture: scrollWheel,
			onTouchMoveCapture: scrollTouchMove
		});
		document.addEventListener("wheel", shouldPrevent, nonPassive);
		document.addEventListener("touchmove", shouldPrevent, nonPassive);
		document.addEventListener("touchstart", scrollTouchStart, nonPassive);
		return function() {
			lockStack = lockStack.filter(function(inst) {
				return inst !== Style;
			});
			document.removeEventListener("wheel", shouldPrevent, nonPassive);
			document.removeEventListener("touchmove", shouldPrevent, nonPassive);
			document.removeEventListener("touchstart", scrollTouchStart, nonPassive);
		};
	}, []);
	var removeScrollBar = props.removeScrollBar, inert = props.inert;
	return import_react.createElement(import_react.Fragment, null, inert ? import_react.createElement(Style, { styles: generateStyle(id) }) : null, removeScrollBar ? import_react.createElement(RemoveScrollBar, {
		noRelative: props.noRelative,
		gapMode: props.gapMode
	}) : null);
}
function getOutermostShadowParent(node) {
	var shadowParent = null;
	while (node !== null) {
		if (node instanceof ShadowRoot) {
			shadowParent = node.host;
			node = node.host;
		}
		node = node.parentNode;
	}
	return shadowParent;
}
//#endregion
//#region node_modules/react-remove-scroll/dist/es2015/sidecar.js
var sidecar_default = exportSidecar(effectCar, RemoveScrollSideCar);
//#endregion
//#region node_modules/react-remove-scroll/dist/es2015/Combination.js
var ReactRemoveScroll = import_react.forwardRef(function(props, ref) {
	return import_react.createElement(RemoveScroll, __assign({}, props, {
		ref,
		sideCar: sidecar_default
	}));
});
ReactRemoveScroll.classNames = RemoveScroll.classNames;
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@radix-ui/react-popover/dist/index.mjs
var __defProp$7 = Object.defineProperty;
var __name$4 = (target, value) => __defProp$7(target, "name", {
	value,
	configurable: true
});
var POPOVER_NAME = "Popover";
var [createPopoverContext, createPopoverScope] = /* @__PURE__ */ createContextScope(POPOVER_NAME, [createPopperScope]);
var usePopperScope = createPopperScope();
var [PopoverProvider, usePopoverContext] = createPopoverContext(POPOVER_NAME);
var Popover = /* @__PURE__ */ __name$4((props) => {
	const { __scopePopover, children, open: openProp, defaultOpen, onOpenChange, modal = false } = props;
	const popperScope = usePopperScope(__scopePopover);
	const triggerRef = import_react.useRef(null);
	const [hasCustomAnchor, setHasCustomAnchor] = import_react.useState(false);
	const [open, setOpen] = useControllableState({
		prop: openProp,
		defaultProp: defaultOpen ?? false,
		onChange: onOpenChange,
		caller: POPOVER_NAME
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Root2, {
		...popperScope,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverProvider, {
			scope: __scopePopover,
			contentId: useId(),
			triggerRef,
			open,
			onOpenChange: setOpen,
			onOpenToggle: import_react.useCallback(() => setOpen((prevOpen) => !prevOpen), [setOpen]),
			hasCustomAnchor,
			onCustomAnchorAdd: import_react.useCallback(() => setHasCustomAnchor(true), []),
			onCustomAnchorRemove: import_react.useCallback(() => setHasCustomAnchor(false), []),
			modal,
			children
		})
	});
}, "Popover");
var TRIGGER_NAME = "PopoverTrigger";
var PopoverTrigger = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$4(function PopoverTrigger2(props, forwardedRef) {
	const { __scopePopover, ...triggerProps } = props;
	const context = usePopoverContext(TRIGGER_NAME, __scopePopover);
	const popperScope = usePopperScope(__scopePopover);
	const composedTriggerRef = useComposedRefs(forwardedRef, context.triggerRef);
	const trigger = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Primitive.button, {
		type: "button",
		"aria-haspopup": "dialog",
		"aria-expanded": context.open,
		"aria-controls": context.open ? context.contentId : void 0,
		"data-state": getState(context.open),
		...triggerProps,
		ref: composedTriggerRef,
		onClick: composeEventHandlers(props.onClick, context.onOpenToggle)
	});
	return context.hasCustomAnchor ? trigger : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Anchor, {
		asChild: true,
		...popperScope,
		children: trigger
	});
}, "PopoverTrigger"));
var PORTAL_NAME = "PopoverPortal";
var [PortalProvider, usePortalContext] = createPopoverContext(PORTAL_NAME, { forceMount: void 0 });
var PopoverPortal = /* @__PURE__ */ __name$4((props) => {
	const { __scopePopover, forceMount, children, container } = props;
	const context = usePopoverContext(PORTAL_NAME, __scopePopover);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PortalProvider, {
		scope: __scopePopover,
		forceMount,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Presence, {
			present: forceMount || context.open,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Portal, {
				asChild: true,
				container,
				children
			})
		})
	});
}, "PopoverPortal");
var CONTENT_NAME = "PopoverContent";
var PopoverContent = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$4(function PopoverContent2(props, forwardedRef) {
	const portalContext = usePortalContext(CONTENT_NAME, props.__scopePopover);
	const { forceMount = portalContext.forceMount, ...contentProps } = props;
	const context = usePopoverContext(CONTENT_NAME, props.__scopePopover);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Presence, {
		present: forceMount || context.open,
		children: context.modal ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContentModal, {
			...contentProps,
			ref: forwardedRef
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContentNonModal, {
			...contentProps,
			ref: forwardedRef
		})
	});
}, "PopoverContent"));
var Slot = /* @__PURE__ */ createSlot("PopoverContent.RemoveScroll");
var PopoverContentModal = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$4(function PopoverContentModal2(props, forwardedRef) {
	const context = usePopoverContext(CONTENT_NAME, props.__scopePopover);
	const contentRef = import_react.useRef(null);
	const composedRefs = useComposedRefs(forwardedRef, contentRef);
	const isRightClickOutsideRef = import_react.useRef(false);
	import_react.useEffect(() => {
		const content = contentRef.current;
		if (content) return hideOthers(content);
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ReactRemoveScroll, {
		as: Slot,
		allowPinchZoom: true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContentImpl, {
			...props,
			ref: composedRefs,
			trapFocus: context.open,
			disableOutsidePointerEvents: true,
			onCloseAutoFocus: composeEventHandlers(props.onCloseAutoFocus, (event) => {
				event.preventDefault();
				if (!isRightClickOutsideRef.current) context.triggerRef.current?.focus();
			}),
			onPointerDownOutside: composeEventHandlers(props.onPointerDownOutside, (event) => {
				const originalEvent = event.detail.originalEvent;
				const ctrlLeftClick = originalEvent.button === 0 && originalEvent.ctrlKey === true;
				isRightClickOutsideRef.current = originalEvent.button === 2 || ctrlLeftClick;
			}, { checkForDefaultPrevented: false }),
			onFocusOutside: composeEventHandlers(props.onFocusOutside, (event) => event.preventDefault(), { checkForDefaultPrevented: false })
		})
	});
}, "PopoverContentModal"));
var PopoverContentNonModal = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$4(function PopoverContentNonModal2(props, forwardedRef) {
	const context = usePopoverContext(CONTENT_NAME, props.__scopePopover);
	const hasInteractedOutsideRef = import_react.useRef(false);
	const hasPointerDownOutsideRef = import_react.useRef(false);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContentImpl, {
		...props,
		ref: forwardedRef,
		trapFocus: false,
		disableOutsidePointerEvents: false,
		onCloseAutoFocus: (event) => {
			props.onCloseAutoFocus?.(event);
			if (!event.defaultPrevented) {
				if (!hasInteractedOutsideRef.current) context.triggerRef.current?.focus();
				event.preventDefault();
			}
			hasInteractedOutsideRef.current = false;
			hasPointerDownOutsideRef.current = false;
		},
		onInteractOutside: (event) => {
			props.onInteractOutside?.(event);
			if (!event.defaultPrevented) {
				hasInteractedOutsideRef.current = true;
				if (event.detail.originalEvent.type === "pointerdown") hasPointerDownOutsideRef.current = true;
			}
			const target = event.target;
			if (context.triggerRef.current?.contains(target)) event.preventDefault();
			if (event.detail.originalEvent.type === "focusin" && hasPointerDownOutsideRef.current) event.preventDefault();
		}
	});
}, "PopoverContentNonModal"));
var PopoverContentImpl = /* @__PURE__ */ import_react.forwardRef(/* @__PURE__ */ __name$4(function PopoverContentImpl2(props, forwardedRef) {
	const { __scopePopover, trapFocus, onOpenAutoFocus, onCloseAutoFocus, disableOutsidePointerEvents, onEscapeKeyDown, onPointerDownOutside, onFocusOutside, onInteractOutside, ...contentProps } = props;
	const context = usePopoverContext(CONTENT_NAME, __scopePopover);
	const popperScope = usePopperScope(__scopePopover);
	useFocusGuards();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FocusScope, {
		asChild: true,
		loop: true,
		trapped: trapFocus,
		onMountAutoFocus: onOpenAutoFocus,
		onUnmountAutoFocus: onCloseAutoFocus,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DismissableLayer, {
			asChild: true,
			disableOutsidePointerEvents,
			onInteractOutside,
			onEscapeKeyDown,
			onPointerDownOutside,
			onFocusOutside,
			onDismiss: () => context.onOpenChange(false),
			deferPointerDownOutside: true,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Content, {
				"data-state": getState(context.open),
				role: "dialog",
				id: context.contentId,
				...popperScope,
				...contentProps,
				ref: forwardedRef,
				style: {
					...contentProps.style,
					"--radix-popover-content-transform-origin": "var(--radix-popper-transform-origin)",
					"--radix-popover-content-available-width": "var(--radix-popper-available-width)",
					"--radix-popover-content-available-height": "var(--radix-popper-available-height)",
					"--radix-popover-trigger-width": "var(--radix-popper-anchor-width)",
					"--radix-popover-trigger-height": "var(--radix-popper-anchor-height)"
				}
			})
		})
	});
}, "PopoverContentImpl"));
function getState(open) {
	return open ? "open" : "closed";
}
__name$4(getState, "getState");
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-O7FT3KAM.mjs
init_react_import();
init_react_import();
init_react_import();
var toKebabCase = (string) => string.replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase();
var mergeClasses = (...classes) => classes.filter((className, index, array) => {
	return Boolean(className) && className.trim() !== "" && array.indexOf(className) === index;
}).join(" ").trim();
init_react_import();
init_react_import();
var defaultAttributes$1 = {
	xmlns: "http://www.w3.org/2000/svg",
	width: 24,
	height: 24,
	viewBox: "0 0 24 24",
	fill: "none",
	stroke: "currentColor",
	strokeWidth: 2,
	strokeLinecap: "round",
	strokeLinejoin: "round"
};
var Icon = (0, import_react.forwardRef)((_a, ref) => {
	var _b = _a, { color = "currentColor", size = 24, strokeWidth = 2, absoluteStrokeWidth, className = "", children, iconNode } = _b, rest = __objRest$4(_b, [
		"color",
		"size",
		"strokeWidth",
		"absoluteStrokeWidth",
		"className",
		"children",
		"iconNode"
	]);
	return (0, import_react.createElement)("svg", __spreadValues$7(__spreadProps$6(__spreadValues$7({ ref }, defaultAttributes$1), {
		width: size,
		height: size,
		stroke: color,
		strokeWidth: absoluteStrokeWidth ? Number(strokeWidth) * 24 / Number(size) : strokeWidth,
		className: mergeClasses("lucide", className)
	}), rest), [...iconNode.map(([tag, attrs]) => (0, import_react.createElement)(tag, attrs)), ...Array.isArray(children) ? children : [children]]);
});
var createLucideIcon = (iconName, iconNode) => {
	const Component = (0, import_react.forwardRef)((_a, ref) => {
		var _b = _a, { className } = _b, props = __objRest$4(_b, ["className"]);
		return (0, import_react.createElement)(Icon, __spreadValues$7({
			ref,
			iconNode,
			className: mergeClasses(`lucide-${toKebabCase(iconName)}`, className)
		}, props));
	});
	Component.displayName = `${iconName}`;
	return Component;
};
var AlignLeft = createLucideIcon("AlignLeft", [
	["path", {
		d: "M15 12H3",
		key: "6jk70r"
	}],
	["path", {
		d: "M17 18H3",
		key: "1amg6g"
	}],
	["path", {
		d: "M21 6H3",
		key: "1jwq7v"
	}]
]);
init_react_import();
var Heading$1 = createLucideIcon("Heading", [
	["path", {
		d: "M6 12h12",
		key: "8npq4p"
	}],
	["path", {
		d: "M6 20V4",
		key: "1w1bmo"
	}],
	["path", {
		d: "M18 20V4",
		key: "o2hl4u"
	}]
]);
init_react_import();
var List = createLucideIcon("List", [
	["path", {
		d: "M3 12h.01",
		key: "nlz23k"
	}],
	["path", {
		d: "M3 18h.01",
		key: "1tta3j"
	}],
	["path", {
		d: "M3 6h.01",
		key: "1rqtza"
	}],
	["path", {
		d: "M8 12h13",
		key: "1za7za"
	}],
	["path", {
		d: "M8 18h13",
		key: "1lx6n3"
	}],
	["path", {
		d: "M8 6h13",
		key: "ik3vkj"
	}]
]);
init_react_import();
init_react_import();
var AlignCenter = createLucideIcon("AlignCenter", [
	["path", {
		d: "M17 12H7",
		key: "16if0g"
	}],
	["path", {
		d: "M19 18H5",
		key: "18s9l3"
	}],
	["path", {
		d: "M21 6H3",
		key: "1jwq7v"
	}]
]);
init_react_import();
var AlignJustify = createLucideIcon("AlignJustify", [
	["path", {
		d: "M3 12h18",
		key: "1i2n21"
	}],
	["path", {
		d: "M3 18h18",
		key: "1h113x"
	}],
	["path", {
		d: "M3 6h18",
		key: "d0wm0j"
	}]
]);
init_react_import();
var AlignRight = createLucideIcon("AlignRight", [
	["path", {
		d: "M21 12H9",
		key: "dn1m92"
	}],
	["path", {
		d: "M21 18H7",
		key: "1ygte8"
	}],
	["path", {
		d: "M21 6H3",
		key: "1jwq7v"
	}]
]);
init_react_import();
var Bold = createLucideIcon("Bold", [["path", {
	d: "M6 12h9a4 4 0 0 1 0 8H7a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1h7a4 4 0 0 1 0 8",
	key: "mg9rjx"
}]]);
init_react_import();
var ChevronDown$1 = createLucideIcon("ChevronDown", [["path", {
	d: "m6 9 6 6 6-6",
	key: "qrunsl"
}]]);
init_react_import();
var ChevronRight = createLucideIcon("ChevronRight", [["path", {
	d: "m9 18 6-6-6-6",
	key: "mthhwq"
}]]);
init_react_import();
var ChevronUp = createLucideIcon("ChevronUp", [["path", {
	d: "m18 15-6-6-6 6",
	key: "153udz"
}]]);
init_react_import();
var CircleCheckBig = createLucideIcon("CircleCheckBig", [["path", {
	d: "M21.801 10A10 10 0 1 1 17 3.335",
	key: "yps3ct"
}], ["path", {
	d: "m9 11 3 3L22 4",
	key: "1pflzl"
}]]);
init_react_import();
var Code = createLucideIcon("Code", [["polyline", {
	points: "16 18 22 12 16 6",
	key: "z7tu5w"
}], ["polyline", {
	points: "8 6 2 12 8 18",
	key: "1eg1df"
}]]);
init_react_import();
var Copy = createLucideIcon("Copy", [["rect", {
	width: "14",
	height: "14",
	x: "8",
	y: "8",
	rx: "2",
	ry: "2",
	key: "17jyea"
}], ["path", {
	d: "M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2",
	key: "zix9uf"
}]]);
init_react_import();
var CornerLeftUp = createLucideIcon("CornerLeftUp", [["polyline", {
	points: "14 9 9 4 4 9",
	key: "m9oyvo"
}], ["path", {
	d: "M20 20h-7a4 4 0 0 1-4-4V4",
	key: "1blwi3"
}]]);
init_react_import();
var EllipsisVertical = createLucideIcon("EllipsisVertical", [
	["circle", {
		cx: "12",
		cy: "12",
		r: "1",
		key: "41hilf"
	}],
	["circle", {
		cx: "12",
		cy: "5",
		r: "1",
		key: "gxeob9"
	}],
	["circle", {
		cx: "12",
		cy: "19",
		r: "1",
		key: "lyex9k"
	}]
]);
init_react_import();
var Expand = createLucideIcon("Expand", [
	["path", {
		d: "m21 21-6-6m6 6v-4.8m0 4.8h-4.8",
		key: "1c15vz"
	}],
	["path", {
		d: "M3 16.2V21m0 0h4.8M3 21l6-6",
		key: "1fsnz2"
	}],
	["path", {
		d: "M21 7.8V3m0 0h-4.8M21 3l-6 6",
		key: "hawz9i"
	}],
	["path", {
		d: "M3 7.8V3m0 0h4.8M3 3l6 6",
		key: "u9ee12"
	}]
]);
init_react_import();
var Globe = createLucideIcon("Globe", [
	["circle", {
		cx: "12",
		cy: "12",
		r: "10",
		key: "1mglay"
	}],
	["path", {
		d: "M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20",
		key: "13o1zl"
	}],
	["path", {
		d: "M2 12h20",
		key: "9i4pu4"
	}]
]);
init_react_import();
var Hammer = createLucideIcon("Hammer", [
	["path", {
		d: "m15 12-8.373 8.373a1 1 0 1 1-3-3L12 9",
		key: "eefl8a"
	}],
	["path", {
		d: "m18 15 4-4",
		key: "16gjal"
	}],
	["path", {
		d: "m21.5 11.5-1.914-1.914A2 2 0 0 1 19 8.172V7l-2.26-2.26a6 6 0 0 0-4.202-1.756L9 2.96l.92.82A6.18 6.18 0 0 1 12 8.4V10l2 2h1.172a2 2 0 0 1 1.414.586L18.5 14.5",
		key: "b7pghm"
	}]
]);
init_react_import();
var Hash = createLucideIcon("Hash", [
	["line", {
		x1: "4",
		x2: "20",
		y1: "9",
		y2: "9",
		key: "4lhtct"
	}],
	["line", {
		x1: "4",
		x2: "20",
		y1: "15",
		y2: "15",
		key: "vyu0kd"
	}],
	["line", {
		x1: "10",
		x2: "8",
		y1: "3",
		y2: "21",
		key: "1ggp8o"
	}],
	["line", {
		x1: "16",
		x2: "14",
		y1: "3",
		y2: "21",
		key: "weycgp"
	}]
]);
init_react_import();
var Heading1 = createLucideIcon("Heading1", [
	["path", {
		d: "M4 12h8",
		key: "17cfdx"
	}],
	["path", {
		d: "M4 18V6",
		key: "1rz3zl"
	}],
	["path", {
		d: "M12 18V6",
		key: "zqpxq5"
	}],
	["path", {
		d: "m17 12 3-2v8",
		key: "1hhhft"
	}]
]);
init_react_import();
var Heading2 = createLucideIcon("Heading2", [
	["path", {
		d: "M4 12h8",
		key: "17cfdx"
	}],
	["path", {
		d: "M4 18V6",
		key: "1rz3zl"
	}],
	["path", {
		d: "M12 18V6",
		key: "zqpxq5"
	}],
	["path", {
		d: "M21 18h-4c0-4 4-3 4-6 0-1.5-2-2.5-4-1",
		key: "9jr5yi"
	}]
]);
init_react_import();
var Heading3 = createLucideIcon("Heading3", [
	["path", {
		d: "M4 12h8",
		key: "17cfdx"
	}],
	["path", {
		d: "M4 18V6",
		key: "1rz3zl"
	}],
	["path", {
		d: "M12 18V6",
		key: "zqpxq5"
	}],
	["path", {
		d: "M17.5 10.5c1.7-1 3.5 0 3.5 1.5a2 2 0 0 1-2 2",
		key: "68ncm8"
	}],
	["path", {
		d: "M17 17.5c2 1.5 4 .3 4-1.5a2 2 0 0 0-2-2",
		key: "1ejuhz"
	}]
]);
init_react_import();
var Heading4 = createLucideIcon("Heading4", [
	["path", {
		d: "M12 18V6",
		key: "zqpxq5"
	}],
	["path", {
		d: "M17 10v3a1 1 0 0 0 1 1h3",
		key: "tj5zdr"
	}],
	["path", {
		d: "M21 10v8",
		key: "1kdml4"
	}],
	["path", {
		d: "M4 12h8",
		key: "17cfdx"
	}],
	["path", {
		d: "M4 18V6",
		key: "1rz3zl"
	}]
]);
init_react_import();
var Heading5 = createLucideIcon("Heading5", [
	["path", {
		d: "M4 12h8",
		key: "17cfdx"
	}],
	["path", {
		d: "M4 18V6",
		key: "1rz3zl"
	}],
	["path", {
		d: "M12 18V6",
		key: "zqpxq5"
	}],
	["path", {
		d: "M17 13v-3h4",
		key: "1nvgqp"
	}],
	["path", {
		d: "M17 17.7c.4.2.8.3 1.3.3 1.5 0 2.7-1.1 2.7-2.5S19.8 13 18.3 13H17",
		key: "2nebdn"
	}]
]);
init_react_import();
var Heading6 = createLucideIcon("Heading6", [
	["path", {
		d: "M4 12h8",
		key: "17cfdx"
	}],
	["path", {
		d: "M4 18V6",
		key: "1rz3zl"
	}],
	["path", {
		d: "M12 18V6",
		key: "zqpxq5"
	}],
	["circle", {
		cx: "19",
		cy: "16",
		r: "2",
		key: "15mx69"
	}],
	["path", {
		d: "M20 10c-2 2-3 3.5-3 6",
		key: "f35dl0"
	}]
]);
init_react_import();
var Italic = createLucideIcon("Italic", [
	["line", {
		x1: "19",
		x2: "10",
		y1: "4",
		y2: "4",
		key: "15jd3p"
	}],
	["line", {
		x1: "14",
		x2: "5",
		y1: "20",
		y2: "20",
		key: "bu0au3"
	}],
	["line", {
		x1: "15",
		x2: "9",
		y1: "4",
		y2: "20",
		key: "uljnxc"
	}]
]);
init_react_import();
var Layers = createLucideIcon("Layers", [
	["path", {
		d: "M12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83z",
		key: "zw3jo"
	}],
	["path", {
		d: "M2 12a1 1 0 0 0 .58.91l8.6 3.91a2 2 0 0 0 1.65 0l8.58-3.9A1 1 0 0 0 22 12",
		key: "1wduqc"
	}],
	["path", {
		d: "M2 17a1 1 0 0 0 .58.91l8.6 3.91a2 2 0 0 0 1.65 0l8.58-3.9A1 1 0 0 0 22 17",
		key: "kqbvx6"
	}]
]);
init_react_import();
var LayoutGrid = createLucideIcon("LayoutGrid", [
	["rect", {
		width: "7",
		height: "7",
		x: "3",
		y: "3",
		rx: "1",
		key: "1g98yp"
	}],
	["rect", {
		width: "7",
		height: "7",
		x: "14",
		y: "3",
		rx: "1",
		key: "6d4xhi"
	}],
	["rect", {
		width: "7",
		height: "7",
		x: "14",
		y: "14",
		rx: "1",
		key: "nxv5o0"
	}],
	["rect", {
		width: "7",
		height: "7",
		x: "3",
		y: "14",
		rx: "1",
		key: "1bb6yr"
	}]
]);
init_react_import();
var Link = createLucideIcon("Link", [["path", {
	d: "M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71",
	key: "1cjeqo"
}], ["path", {
	d: "M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71",
	key: "19qd67"
}]]);
init_react_import();
var ListOrdered = createLucideIcon("ListOrdered", [
	["path", {
		d: "M10 12h11",
		key: "6m4ad9"
	}],
	["path", {
		d: "M10 18h11",
		key: "11hvi2"
	}],
	["path", {
		d: "M10 6h11",
		key: "c7qv1k"
	}],
	["path", {
		d: "M4 10h2",
		key: "16xx2s"
	}],
	["path", {
		d: "M4 6h1v4",
		key: "cnovpq"
	}],
	["path", {
		d: "M6 18H4c0-1 2-2 2-3s-1-1.5-2-1",
		key: "m9a95d"
	}]
]);
init_react_import();
var LockOpen = createLucideIcon("LockOpen", [["rect", {
	width: "18",
	height: "11",
	x: "3",
	y: "11",
	rx: "2",
	ry: "2",
	key: "1w4ew1"
}], ["path", {
	d: "M7 11V7a5 5 0 0 1 9.9-1",
	key: "1mm8w8"
}]]);
init_react_import();
var Lock = createLucideIcon("Lock", [["rect", {
	width: "18",
	height: "11",
	x: "3",
	y: "11",
	rx: "2",
	ry: "2",
	key: "1w4ew1"
}], ["path", {
	d: "M7 11V7a5 5 0 0 1 10 0v4",
	key: "fwvmzm"
}]]);
init_react_import();
var Maximize2 = createLucideIcon("Maximize2", [
	["polyline", {
		points: "15 3 21 3 21 9",
		key: "mznyad"
	}],
	["polyline", {
		points: "9 21 3 21 3 15",
		key: "1avn1i"
	}],
	["line", {
		x1: "21",
		x2: "14",
		y1: "3",
		y2: "10",
		key: "ota7mn"
	}],
	["line", {
		x1: "3",
		x2: "10",
		y1: "21",
		y2: "14",
		key: "1atl0r"
	}]
]);
init_react_import();
var Minimize2 = createLucideIcon("Minimize2", [
	["polyline", {
		points: "4 14 10 14 10 20",
		key: "11kfnr"
	}],
	["polyline", {
		points: "20 10 14 10 14 4",
		key: "rlmsce"
	}],
	["line", {
		x1: "14",
		x2: "21",
		y1: "10",
		y2: "3",
		key: "o5lafz"
	}],
	["line", {
		x1: "3",
		x2: "10",
		y1: "21",
		y2: "14",
		key: "1atl0r"
	}]
]);
init_react_import();
var Minus = createLucideIcon("Minus", [["path", {
	d: "M5 12h14",
	key: "1ays0h"
}]]);
init_react_import();
var Monitor$1 = createLucideIcon("Monitor", [
	["rect", {
		width: "20",
		height: "14",
		x: "2",
		y: "3",
		rx: "2",
		key: "48i651"
	}],
	["line", {
		x1: "8",
		x2: "16",
		y1: "21",
		y2: "21",
		key: "1svkeh"
	}],
	["line", {
		x1: "12",
		x2: "12",
		y1: "17",
		y2: "21",
		key: "vw1qmm"
	}]
]);
init_react_import();
var PanelLeft = createLucideIcon("PanelLeft", [["rect", {
	width: "18",
	height: "18",
	x: "3",
	y: "3",
	rx: "2",
	key: "afitv7"
}], ["path", {
	d: "M9 3v18",
	key: "fh3hqa"
}]]);
init_react_import();
var PanelRight = createLucideIcon("PanelRight", [["rect", {
	width: "18",
	height: "18",
	x: "3",
	y: "3",
	rx: "2",
	key: "afitv7"
}], ["path", {
	d: "M15 3v18",
	key: "14nvp0"
}]]);
init_react_import();
var Plus = createLucideIcon("Plus", [["path", {
	d: "M5 12h14",
	key: "1ays0h"
}], ["path", {
	d: "M12 5v14",
	key: "s699le"
}]]);
init_react_import();
var Quote = createLucideIcon("Quote", [["path", {
	d: "M16 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2 1 1 0 0 1 1 1v1a2 2 0 0 1-2 2 1 1 0 0 0-1 1v2a1 1 0 0 0 1 1 6 6 0 0 0 6-6V5a2 2 0 0 0-2-2z",
	key: "rib7q0"
}], ["path", {
	d: "M5 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2 1 1 0 0 1 1 1v1a2 2 0 0 1-2 2 1 1 0 0 0-1 1v2a1 1 0 0 0 1 1 6 6 0 0 0 6-6V5a2 2 0 0 0-2-2z",
	key: "1ymkrd"
}]]);
init_react_import();
var RectangleEllipsis = createLucideIcon("RectangleEllipsis", [
	["rect", {
		width: "20",
		height: "12",
		x: "2",
		y: "6",
		rx: "2",
		key: "9lu3g6"
	}],
	["path", {
		d: "M12 12h.01",
		key: "1mp3jc"
	}],
	["path", {
		d: "M17 12h.01",
		key: "1m0b6t"
	}],
	["path", {
		d: "M7 12h.01",
		key: "eqddd0"
	}]
]);
init_react_import();
var Redo2 = createLucideIcon("Redo2", [["path", {
	d: "m15 14 5-5-5-5",
	key: "12vg1m"
}], ["path", {
	d: "M20 9H9.5A5.5 5.5 0 0 0 4 14.5A5.5 5.5 0 0 0 9.5 20H13",
	key: "6uklza"
}]]);
init_react_import();
var Search = createLucideIcon("Search", [["circle", {
	cx: "11",
	cy: "11",
	r: "8",
	key: "4ej97u"
}], ["path", {
	d: "m21 21-4.3-4.3",
	key: "1qie3q"
}]]);
init_react_import();
var SlidersHorizontal = createLucideIcon("SlidersHorizontal", [
	["line", {
		x1: "21",
		x2: "14",
		y1: "4",
		y2: "4",
		key: "obuewd"
	}],
	["line", {
		x1: "10",
		x2: "3",
		y1: "4",
		y2: "4",
		key: "1q6298"
	}],
	["line", {
		x1: "21",
		x2: "12",
		y1: "12",
		y2: "12",
		key: "1iu8h1"
	}],
	["line", {
		x1: "8",
		x2: "3",
		y1: "12",
		y2: "12",
		key: "ntss68"
	}],
	["line", {
		x1: "21",
		x2: "16",
		y1: "20",
		y2: "20",
		key: "14d8ph"
	}],
	["line", {
		x1: "12",
		x2: "3",
		y1: "20",
		y2: "20",
		key: "m0wm8r"
	}],
	["line", {
		x1: "14",
		x2: "14",
		y1: "2",
		y2: "6",
		key: "14e1ph"
	}],
	["line", {
		x1: "8",
		x2: "8",
		y1: "10",
		y2: "14",
		key: "1i6ji0"
	}],
	["line", {
		x1: "16",
		x2: "16",
		y1: "18",
		y2: "22",
		key: "1lctlv"
	}]
]);
init_react_import();
var Smartphone = createLucideIcon("Smartphone", [["rect", {
	width: "14",
	height: "20",
	x: "5",
	y: "2",
	rx: "2",
	ry: "2",
	key: "1yt0o3"
}], ["path", {
	d: "M12 18h.01",
	key: "mhygvu"
}]]);
init_react_import();
var SquareCode = createLucideIcon("SquareCode", [
	["path", {
		d: "M10 9.5 8 12l2 2.5",
		key: "3mjy60"
	}],
	["path", {
		d: "m14 9.5 2 2.5-2 2.5",
		key: "1bir2l"
	}],
	["rect", {
		width: "18",
		height: "18",
		x: "3",
		y: "3",
		rx: "2",
		key: "afitv7"
	}]
]);
init_react_import();
var Strikethrough = createLucideIcon("Strikethrough", [
	["path", {
		d: "M16 4H9a3 3 0 0 0-2.83 4",
		key: "43sutm"
	}],
	["path", {
		d: "M14 12a4 4 0 0 1 0 8H6",
		key: "nlfj13"
	}],
	["line", {
		x1: "4",
		x2: "20",
		y1: "12",
		y2: "12",
		key: "1e0a9i"
	}]
]);
init_react_import();
var Tablet = createLucideIcon("Tablet", [["rect", {
	width: "16",
	height: "20",
	x: "4",
	y: "2",
	rx: "2",
	ry: "2",
	key: "76otgf"
}], ["line", {
	x1: "12",
	x2: "12.01",
	y1: "18",
	y2: "18",
	key: "1dp563"
}]]);
init_react_import();
var ToyBrick = createLucideIcon("ToyBrick", [
	["rect", {
		width: "18",
		height: "12",
		x: "3",
		y: "8",
		rx: "1",
		key: "158fvp"
	}],
	["path", {
		d: "M10 8V5c0-.6-.4-1-1-1H6a1 1 0 0 0-1 1v3",
		key: "s0042v"
	}],
	["path", {
		d: "M19 8V5c0-.6-.4-1-1-1h-3a1 1 0 0 0-1 1v3",
		key: "9wmeh2"
	}]
]);
init_react_import();
var Trash = createLucideIcon("Trash", [
	["path", {
		d: "M3 6h18",
		key: "d0wm0j"
	}],
	["path", {
		d: "M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6",
		key: "4alrt4"
	}],
	["path", {
		d: "M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2",
		key: "v07s0e"
	}]
]);
init_react_import();
var Type = createLucideIcon("Type", [
	["polyline", {
		points: "4 7 4 4 20 4 20 7",
		key: "1nosan"
	}],
	["line", {
		x1: "9",
		x2: "15",
		y1: "20",
		y2: "20",
		key: "swin9y"
	}],
	["line", {
		x1: "12",
		x2: "12",
		y1: "4",
		y2: "20",
		key: "1tx1rr"
	}]
]);
init_react_import();
var Underline = createLucideIcon("Underline", [["path", {
	d: "M6 4v6a6 6 0 0 0 12 0V4",
	key: "9kb039"
}], ["line", {
	x1: "4",
	x2: "20",
	y1: "20",
	y2: "20",
	key: "nun2al"
}]]);
init_react_import();
var Undo2 = createLucideIcon("Undo2", [["path", {
	d: "M9 14 4 9l5-5",
	key: "102s5s"
}], ["path", {
	d: "M4 9h10.5a5.5 5.5 0 0 1 5.5 5.5a5.5 5.5 0 0 1-5.5 5.5H11",
	key: "f3b9sd"
}]]);
init_react_import();
var X = createLucideIcon("X", [["path", {
	d: "M18 6 6 18",
	key: "1bl5f8"
}], ["path", {
	d: "m6 6 12 12",
	key: "d8bk6v"
}]]);
init_react_import();
var ZoomIn = createLucideIcon("ZoomIn", [
	["circle", {
		cx: "11",
		cy: "11",
		r: "8",
		key: "4ej97u"
	}],
	["line", {
		x1: "21",
		x2: "16.65",
		y1: "21",
		y2: "16.65",
		key: "13gj7c"
	}],
	["line", {
		x1: "11",
		x2: "11",
		y1: "8",
		y2: "14",
		key: "1vmskp"
	}],
	["line", {
		x1: "8",
		x2: "14",
		y1: "11",
		y2: "11",
		key: "durymu"
	}]
]);
init_react_import();
var ZoomOut = createLucideIcon("ZoomOut", [
	["circle", {
		cx: "11",
		cy: "11",
		r: "8",
		key: "4ej97u"
	}],
	["line", {
		x1: "21",
		x2: "16.65",
		y1: "21",
		y2: "16.65",
		key: "13gj7c"
	}],
	["line", {
		x1: "8",
		x2: "14",
		y1: "11",
		y2: "11",
		key: "durymu"
	}]
]);
init_react_import();
init_react_import();
init_react_import();
var keyCodeMap = {
	ControlLeft: "ctrl",
	ControlRight: "ctrl",
	MetaLeft: "meta",
	MetaRight: "meta",
	ShiftLeft: "shift",
	ShiftRight: "shift",
	KeyA: "a",
	KeyB: "b",
	KeyC: "c",
	KeyD: "d",
	KeyE: "e",
	KeyF: "f",
	KeyG: "g",
	KeyH: "h",
	KeyI: "i",
	KeyJ: "j",
	KeyK: "k",
	KeyL: "l",
	KeyM: "m",
	KeyN: "n",
	KeyO: "o",
	KeyP: "p",
	KeyQ: "q",
	KeyR: "r",
	KeyS: "s",
	KeyT: "t",
	KeyU: "u",
	KeyV: "v",
	KeyW: "w",
	KeyX: "x",
	KeyY: "y",
	KeyZ: "z",
	Delete: "delete",
	Backspace: "backspace",
	AltRight: "altRight"
};
var useHotkeyStore = create()(subscribeWithSelector((set) => ({
	held: {},
	hold: (key) => set((s) => s.held[key] ? s : { held: __spreadProps$6(__spreadValues$7({}, s.held), { [key]: true }) }),
	release: (key) => set((s) => s.held[key] ? { held: __spreadProps$6(__spreadValues$7({}, s.held), { [key]: false }) } : s),
	reset: (held = {}) => set(() => ({ held })),
	triggers: {}
})));
var monitorHotkeys = (doc) => {
	const onKeyDown = (e) => {
		if (e.getModifierState("AltGraph")) {
			useHotkeyStore.getState().hold("altRight");
			return;
		}
		const key = keyCodeMap[e.code];
		if (key) {
			useHotkeyStore.getState().hold(key);
			const { held, triggers } = useHotkeyStore.getState();
			Object.values(triggers).forEach(({ combo, cb }) => {
				if (Object.entries(combo).every(([key2, value]) => value === !!held[key2]) && Object.entries(held).every(([key2, value]) => value === !!combo[key2])) {
					if (cb(e) !== false) e.preventDefault();
				}
			});
			if (key !== "meta" && key !== "ctrl" && key !== "shift") useHotkeyStore.getState().release(key);
		}
	};
	const onKeyUp = (e) => {
		if (!e.getModifierState("AltGraph") && e.code === "ControlRight") {
			useHotkeyStore.getState().release("altRight");
			return;
		}
		const key = keyCodeMap[e.code];
		if (key) if (key === "meta") useHotkeyStore.getState().reset();
		else useHotkeyStore.getState().release(key);
	};
	const onVisibilityChanged = (e) => {
		if (document.visibilityState === "hidden") useHotkeyStore.getState().reset();
	};
	const onBlur = () => {
		useHotkeyStore.getState().reset();
	};
	window.addEventListener("blur", onBlur);
	doc.addEventListener("keydown", onKeyDown);
	doc.addEventListener("keyup", onKeyUp);
	doc.addEventListener("visibilitychange", onVisibilityChanged);
	return () => {
		doc.removeEventListener("keydown", onKeyDown);
		doc.removeEventListener("keyup", onKeyUp);
		doc.removeEventListener("visibilitychange", onVisibilityChanged);
		window.removeEventListener("blur", onBlur);
	};
};
var useMonitorHotkeys = () => {
	(0, import_react.useEffect)(() => monitorHotkeys(document), []);
};
var useHotkey = (combo, cb) => {
	(0, import_react.useEffect)(() => useHotkeyStore.setState((s) => ({ triggers: __spreadProps$6(__spreadValues$7({}, s.triggers), { [`${Object.keys(combo).join("+")}`]: {
		combo,
		cb
	} }) })), []);
};
var EMPTY_HISTORY_INDEX = 0;
function debounce$2(func, timeout = 300) {
	let timer;
	return (...args) => {
		clearTimeout(timer);
		timer = setTimeout(() => {
			func(...args);
		}, timeout);
	};
}
var tidyState = (state) => {
	return __spreadProps$6(__spreadValues$7({}, state), { ui: __spreadProps$6(__spreadValues$7({}, state.ui), { field: __spreadProps$6(__spreadValues$7({}, state.ui.field), { focus: null }) }) });
};
var createHistorySlice = (set, get) => {
	return {
		initialAppState: {},
		index: EMPTY_HISTORY_INDEX,
		histories: [],
		hasPast: () => get().history.index > EMPTY_HISTORY_INDEX,
		hasFuture: () => get().history.index < get().history.histories.length - 1,
		prevHistory: () => {
			const { history } = get();
			return history.hasPast() ? history.histories[history.index - 1] : null;
		},
		nextHistory: () => {
			const s = get().history;
			return s.hasFuture() ? s.histories[s.index + 1] : null;
		},
		currentHistory: () => get().history.histories[get().history.index],
		back: () => {
			var _a;
			const { history, dispatch } = get();
			if (history.hasPast()) {
				dispatch({
					type: "set",
					state: tidyState(((_a = history.prevHistory()) == null ? void 0 : _a.state) || history.initialAppState)
				});
				set({ history: __spreadProps$6(__spreadValues$7({}, history), { index: history.index - 1 }) });
			}
		},
		forward: () => {
			var _a;
			const { history, dispatch } = get();
			if (history.hasFuture()) {
				const state = (_a = history.nextHistory()) == null ? void 0 : _a.state;
				dispatch({
					type: "set",
					state: state ? tidyState(state) : {}
				});
				set({ history: __spreadProps$6(__spreadValues$7({}, history), { index: history.index + 1 }) });
			}
		},
		setHistories: (histories) => {
			var _a;
			const { dispatch, history } = get();
			dispatch({
				type: "set",
				state: ((_a = histories[histories.length - 1]) == null ? void 0 : _a.state) || history.initialAppState
			});
			set({ history: __spreadProps$6(__spreadValues$7({}, history), {
				histories,
				index: histories.length - 1
			}) });
		},
		setHistoryIndex: (index) => {
			var _a;
			const { dispatch, history } = get();
			dispatch({
				type: "set",
				state: ((_a = history.histories[index]) == null ? void 0 : _a.state) || history.initialAppState
			});
			set({ history: __spreadProps$6(__spreadValues$7({}, history), { index }) });
		},
		record: debounce$2((state) => {
			const { histories, index } = get().history;
			const history = {
				state,
				id: generateId("history")
			};
			const newHistories = [...histories.slice(0, index + 1), history];
			set({ history: __spreadProps$6(__spreadValues$7({}, get().history), {
				histories: newHistories,
				index: newHistories.length - 1
			}) });
		}, 250)
	};
};
function useRegisterHistorySlice(appStore, { histories, index, initialAppState }) {
	(0, import_react.useEffect)(() => appStore.setState({ history: __spreadProps$6(__spreadValues$7({}, appStore.getState().history), {
		histories,
		index,
		initialAppState
	}) }), [
		histories,
		index,
		initialAppState
	]);
	const back = () => {
		appStore.getState().history.back();
	};
	const forward = () => {
		appStore.getState().history.forward();
	};
	useHotkey({
		altRight: false,
		meta: true,
		z: true
	}, back);
	useHotkey({
		altRight: false,
		meta: true,
		shift: true,
		z: true
	}, forward);
	useHotkey({
		altRight: false,
		meta: true,
		y: true
	}, forward);
	useHotkey({
		altRight: false,
		ctrl: true,
		z: true
	}, back);
	useHotkey({
		altRight: false,
		ctrl: true,
		shift: true,
		z: true
	}, forward);
	useHotkey({
		altRight: false,
		ctrl: true,
		y: true
	}, forward);
}
init_react_import();
var createNodesSlice = (_set, _get) => {
	const registry = /* @__PURE__ */ new Map();
	return {
		registerNode: (id, handle) => {
			registry.set(id, handle);
		},
		unregisterNode: (id) => {
			registry.delete(id);
		},
		syncNode: (id) => {
			var _a;
			if (!id) return;
			(_a = registry.get(id)) == null || _a.sync();
		},
		syncNodes: (ids) => {
			ids.forEach((id) => {
				var _a;
				if (!id) return;
				(_a = registry.get(id)) == null || _a.sync();
			});
		},
		setOverlayVisible: (id, visible) => {
			if (!id) return;
			const node = registry.get(id);
			if (!node) return;
			if (visible) {
				node.showOverlay();
				return;
			}
			node.hideOverlay();
		}
	};
};
init_react_import();
init_react_import();
var flattenData = (state, config) => {
	const data = [];
	walkAppState(state, config, (content) => content, (item) => {
		data.push(item);
		return item;
	});
	return data;
};
var createPermissionsSlice = (set, get) => {
	const resolvePermissions = (..._0) => __async(null, [..._0], function* (params = {}, force) {
		const { state, permissions, config } = get();
		const { cache, globalPermissions } = permissions;
		const resolvePermissionsForItem = (item2, force2 = false) => __async(null, null, function* () {
			var _a, _b;
			const { config: config2, state: appState, setComponentLoading } = get();
			const itemCache = cache[item2.props.id];
			const nodes = appState.indexes.nodes;
			const parentId = (_a = nodes[item2.props.id]) == null ? void 0 : _a.parentId;
			const parentNode = parentId ? nodes[parentId] : null;
			const parentData = (_b = parentNode == null ? void 0 : parentNode.data) != null ? _b : null;
			const componentConfig = item2.type === "root" ? config2.root : config2.components[item2.type];
			if (!componentConfig) return;
			const initialPermissions = __spreadValues$7(__spreadValues$7({}, globalPermissions), componentConfig.permissions);
			if (componentConfig.resolvePermissions) {
				const changed = getChanged(item2, itemCache == null ? void 0 : itemCache.lastData);
				const propsChanged = Object.values(changed).some((el) => el === true);
				const parentChanged = (itemCache == null ? void 0 : itemCache.lastParentId) !== parentId;
				if (propsChanged || parentChanged || force2) {
					const clearTimeout2 = setComponentLoading(item2.props.id, true, 50);
					const resolvedPermissions = yield componentConfig.resolvePermissions(item2, {
						changed,
						lastPermissions: (itemCache == null ? void 0 : itemCache.lastPermissions) || null,
						permissions: initialPermissions,
						appState: makeStatePublic(appState),
						lastData: (itemCache == null ? void 0 : itemCache.lastData) || null,
						parent: parentData
					});
					const latest = get().permissions;
					set({ permissions: __spreadProps$6(__spreadValues$7({}, latest), {
						cache: __spreadProps$6(__spreadValues$7({}, latest.cache), { [item2.props.id]: {
							lastParentId: parentId,
							lastData: item2,
							lastPermissions: resolvedPermissions
						} }),
						resolvedPermissions: __spreadProps$6(__spreadValues$7({}, latest.resolvedPermissions), { [item2.props.id]: resolvedPermissions })
					}) });
					clearTimeout2();
				}
			}
		});
		const resolvePermissionsForRoot = (force2 = false) => {
			const { state: appState } = get();
			resolvePermissionsForItem({
				type: "root",
				props: __spreadProps$6(__spreadValues$7({}, appState.data.root.props), { id: "root" })
			}, force2);
		};
		const { item, type, root } = params;
		if (item) yield resolvePermissionsForItem(item, force);
		else if (type) flattenData(state, config).filter((item2) => item2.type === type).map((item2) => __async(null, null, function* () {
			yield resolvePermissionsForItem(item2, force);
		}));
		else if (root) resolvePermissionsForRoot(force);
		else flattenData(state, config).map((item2) => __async(null, null, function* () {
			yield resolvePermissionsForItem(item2, force);
		}));
	});
	const refreshPermissions = (params) => resolvePermissions(params, true);
	return {
		cache: {},
		globalPermissions: {
			drag: true,
			edit: true,
			delete: true,
			duplicate: true,
			insert: true
		},
		resolvedPermissions: {},
		getPermissions: ({ item, type, root } = {}) => {
			const { config, permissions } = get();
			const { globalPermissions, resolvedPermissions } = permissions;
			if (item) {
				const componentConfig = config.components[item.type];
				const initialPermissions = __spreadValues$7(__spreadValues$7({}, globalPermissions), componentConfig == null ? void 0 : componentConfig.permissions);
				const resolvedForItem = resolvedPermissions[item.props.id];
				return resolvedForItem ? __spreadValues$7(__spreadValues$7({}, globalPermissions), resolvedForItem) : initialPermissions;
			} else if (type) {
				const componentConfig = config.components[type];
				return __spreadValues$7(__spreadValues$7({}, globalPermissions), componentConfig == null ? void 0 : componentConfig.permissions);
			} else if (root) {
				const rootConfig = config.root;
				const initialPermissions = __spreadValues$7(__spreadValues$7({}, globalPermissions), rootConfig == null ? void 0 : rootConfig.permissions);
				const resolvedForItem = resolvedPermissions["root"];
				return resolvedForItem ? __spreadValues$7(__spreadValues$7({}, globalPermissions), resolvedForItem) : initialPermissions;
			}
			return globalPermissions;
		},
		resolvePermissions,
		refreshPermissions
	};
};
var useRegisterPermissionsSlice = (appStore, globalPermissions) => {
	(0, import_react.useEffect)(() => {
		const { permissions } = appStore.getState();
		const { globalPermissions: existingGlobalPermissions } = permissions;
		appStore.setState({ permissions: __spreadProps$6(__spreadValues$7({}, permissions), { globalPermissions: __spreadValues$7(__spreadValues$7({}, existingGlobalPermissions), globalPermissions) }) });
		permissions.resolvePermissions();
	}, [globalPermissions]);
	(0, import_react.useEffect)(() => {
		return appStore.subscribe((s) => s.state.data, () => {
			appStore.getState().permissions.resolvePermissions();
		});
	}, []);
	(0, import_react.useEffect)(() => {
		return appStore.subscribe((s) => s.config, () => {
			appStore.getState().permissions.resolvePermissions();
		});
	}, []);
};
init_react_import();
var createFieldsSlice = (_set, _get) => {
	return {
		fields: {},
		loading: false,
		lastResolvedData: {},
		id: void 0
	};
};
var useRegisterFieldsSlice = (appStore, id) => {
	const resolveFields = (0, import_react.useCallback)((reset) => __async(null, null, function* () {
		var _a, _b;
		const { fields, lastResolvedData } = appStore.getState().fields;
		const metadata = appStore.getState().metadata;
		const nodes = appStore.getState().state.indexes.nodes;
		const node = nodes[id || "root"];
		const componentData = node == null ? void 0 : node.data;
		const parentNode = (node == null ? void 0 : node.parentId) ? nodes[node.parentId] : null;
		const parent = (parentNode == null ? void 0 : parentNode.data) || null;
		const { getComponentConfig, state } = appStore.getState();
		const componentConfig = getComponentConfig(componentData == null ? void 0 : componentData.type);
		if (!componentData || !componentConfig) return;
		const defaultFields = componentConfig.fields || {};
		const resolver = componentConfig.resolveFields;
		let lastFields = fields;
		if (reset) {
			appStore.setState((s) => ({ fields: __spreadProps$6(__spreadValues$7({}, s.fields), {
				fields: defaultFields,
				id
			}) }));
			lastFields = defaultFields;
		}
		if (resolver) {
			const timeout = setTimeout(() => {
				appStore.setState((s) => ({ fields: __spreadProps$6(__spreadValues$7({}, s.fields), { loading: true }) }));
			}, 50);
			const lastData = ((_a = lastResolvedData.props) == null ? void 0 : _a.id) === id ? lastResolvedData : null;
			const newFields = yield resolver(componentData, {
				changed: getChanged(componentData, lastData),
				fields: defaultFields,
				lastFields,
				metadata: __spreadValues$7(__spreadValues$7({}, metadata), componentConfig.metadata),
				lastData,
				appState: makeStatePublic(state),
				parent
			});
			clearTimeout(timeout);
			if (((_b = appStore.getState().selectedItem) == null ? void 0 : _b.props.id) !== id) return;
			appStore.setState({ fields: {
				fields: newFields,
				loading: false,
				lastResolvedData: componentData,
				id
			} });
		} else appStore.setState((s) => ({ fields: __spreadProps$6(__spreadValues$7({}, s.fields), {
			fields: defaultFields,
			id
		}) }));
	}), [id]);
	(0, import_react.useEffect)(() => {
		resolveFields(true);
		return appStore.subscribe((s) => s.state.indexes.nodes[id || "root"], () => resolveFields());
	}, [id]);
};
init_react_import();
var toRoot = (item) => {
	if ("type" in item && item.type !== "root") throw new Error("Converting non-root item to root.");
	const { readOnly } = item;
	if (item.props) {
		if ("id" in item.props) {
			const _a = item.props, { id } = _a;
			return {
				props: __objRest$4(_a, ["id"]),
				readOnly
			};
		}
		return {
			props: item.props,
			readOnly
		};
	}
	return {
		props: {},
		readOnly
	};
};
var defaultPageFields = { title: { type: "text" } };
var createAppStore = (initialAppStore) => create()(subscribeWithSelector((set, get) => {
	var _a, _b;
	return __spreadProps$6(__spreadValues$7({
		instanceId: generateId(),
		state: defaultAppState,
		config: { components: {} },
		componentState: {},
		plugins: [],
		overrides: {},
		viewports: defaultViewports,
		zoomConfig: {
			autoZoom: 1,
			rootHeight: 0,
			zoom: 1
		},
		status: "LOADING",
		iframe: {},
		_experimentalFullScreenCanvas: false,
		_experimentalVirtualization: false,
		metadata: {},
		fieldTransforms: {}
	}, initialAppStore), {
		fields: createFieldsSlice(set, get),
		history: createHistorySlice(set, get),
		nodes: createNodesSlice(set, get),
		permissions: createPermissionsSlice(set, get),
		getCurrentData: () => {
			var _a2;
			const s = get();
			return (_a2 = s.selectedItem) != null ? _a2 : s.state.data.root;
		},
		getComponentConfig: (type) => {
			var _a2;
			const { config, selectedItem } = get();
			const rootFields = ((_a2 = config.root) == null ? void 0 : _a2.fields) || defaultPageFields;
			return type && type !== "root" ? config.components[type] : selectedItem ? config.components[selectedItem.type] : __spreadProps$6(__spreadValues$7({}, config.root), { fields: rootFields });
		},
		selectedItem: ((_a = initialAppStore == null ? void 0 : initialAppStore.state) == null ? void 0 : _a.ui.itemSelector) ? getItem((_b = initialAppStore == null ? void 0 : initialAppStore.state) == null ? void 0 : _b.ui.itemSelector, initialAppStore.state) : null,
		dispatch: (action) => set((s) => {
			var _a2, _b2;
			const { record } = get().history;
			const state = createReducer({
				record,
				appStore: s
			})(s.state, action);
			const selectedItem = state.ui.itemSelector ? getItem(state.ui.itemSelector, state) : null;
			(_b2 = (_a2 = get()).onAction) == null || _b2.call(_a2, action, state, get().state);
			return __spreadProps$6(__spreadValues$7({}, s), {
				state,
				selectedItem
			});
		}),
		setZoomConfig: (zoomConfig) => set({ zoomConfig }),
		setStatus: (status) => set({ status }),
		setComponentState: (componentState) => set({ componentState }),
		pendingLoadTimeouts: {},
		setComponentLoading: (id, loading = true, defer = 0) => {
			const { setComponentState, pendingLoadTimeouts } = get();
			const loadId = generateId();
			const setLoading = () => {
				var _a2;
				const { componentState } = get();
				setComponentState(__spreadProps$6(__spreadValues$7({}, componentState), { [id]: __spreadProps$6(__spreadValues$7({}, componentState[id]), { loadingCount: (((_a2 = componentState[id]) == null ? void 0 : _a2.loadingCount) || 0) + 1 }) }));
			};
			const unsetLoading = () => {
				var _a2;
				const { componentState } = get();
				clearTimeout(timeout);
				delete pendingLoadTimeouts[loadId];
				set({ pendingLoadTimeouts });
				setComponentState(__spreadProps$6(__spreadValues$7({}, componentState), { [id]: __spreadProps$6(__spreadValues$7({}, componentState[id]), { loadingCount: Math.max((((_a2 = componentState[id]) == null ? void 0 : _a2.loadingCount) || 0) - 1, 0) }) }));
			};
			const timeout = setTimeout(() => {
				if (loading) setLoading();
				else unsetLoading();
				delete pendingLoadTimeouts[loadId];
				set({ pendingLoadTimeouts });
			}, defer);
			set({ pendingLoadTimeouts: __spreadProps$6(__spreadValues$7({}, pendingLoadTimeouts), { [id]: timeout }) });
			return unsetLoading;
		},
		unsetComponentLoading: (id) => {
			const { setComponentLoading } = get();
			setComponentLoading(id, false);
		},
		setUi: (ui, recordHistory) => set((s) => {
			const state = createReducer({
				record: () => {},
				appStore: s
			})(s.state, {
				type: "setUi",
				ui,
				recordHistory
			});
			const selectedItem = state.ui.itemSelector ? getItem(state.ui.itemSelector, state) : null;
			return __spreadProps$6(__spreadValues$7({}, s), {
				state,
				selectedItem
			});
		}),
		resolveComponentData: (componentData, trigger) => __async(null, null, function* () {
			var _a2, _b2;
			const { config, metadata, setComponentLoading, permissions, state } = get();
			const componentId = "id" in componentData.props ? componentData.props.id : "root";
			const parentId = (_a2 = state.indexes.nodes[componentId]) == null ? void 0 : _a2.parentId;
			const parentNode = parentId ? state.indexes.nodes[parentId] : null;
			const parentData = (_b2 = parentNode == null ? void 0 : parentNode.data) != null ? _b2 : null;
			const timeouts = {};
			return yield resolveComponentData(componentData, config, metadata, (item) => {
				const id = "id" in item.props ? item.props.id : "root";
				timeouts[id] = setComponentLoading(id, true, 50);
			}, (item) => __async(null, null, function* () {
				const id = "id" in item.props ? item.props.id : "root";
				if ("type" in item) yield permissions.refreshPermissions({ item });
				else yield permissions.refreshPermissions({ root: true });
				timeouts[id]();
			}), trigger, parentData);
		}),
		resolveAndCommitData: () => __async(null, null, function* () {
			const { config, state, dispatch, resolveComponentData: resolveComponentData2 } = get();
			walkAppState(state, config, (content) => content, (childItem, path) => {
				if (path.length > 1) return childItem;
				resolveComponentData2(childItem, "load").then((resolved) => {
					const { state: state2 } = get();
					const node = state2.indexes.nodes[resolved.node.props.id];
					if (node && resolved.didChange) if (resolved.node.props.id === "root") dispatch({
						type: "replaceRoot",
						root: toRoot(resolved.node)
					});
					else {
						const zoneCompound = `${node.parentId}:${node.zone}`;
						const index = state2.indexes.zones[zoneCompound].contentIds.indexOf(resolved.node.props.id);
						dispatch({
							type: "replace",
							data: resolved.node,
							destinationIndex: index,
							destinationZone: zoneCompound
						});
					}
				});
				return childItem;
			});
		})
	});
}));
var appStoreContext = (0, import_react.createContext)(createAppStore());
function useAppStore(selector) {
	return useStore((0, import_react.useContext)(appStoreContext), selector);
}
function useAppStoreApi() {
	return (0, import_react.useContext)(appStoreContext);
}
init_react_import();
var ControlContext = (0, import_react.createContext)({});
var useControlContext = () => {
	return (0, import_react.useContext)(ControlContext);
};
init_react_import();
init_react_import();
init_react_import();
var styles_module_default$2 = {
	"Select": "_Select_xjbef_1",
	"Select-button": "_Select-button_xjbef_6",
	"Select--hasOptions": "_Select--hasOptions_xjbef_19",
	"Select--disabled": "_Select--disabled_xjbef_23",
	"Select-buttonIcon": "_Select-buttonIcon_xjbef_27",
	"Select--standalone": "_Select--standalone_xjbef_33",
	"Select--actionBar": "_Select--actionBar_xjbef_38",
	"Select--hasValue": "_Select--hasValue_xjbef_44",
	"Select-items": "_Select-items_xjbef_61",
	"SelectItem": "_SelectItem_xjbef_72",
	"SelectItem--isSelected": "_SelectItem--isSelected_xjbef_87",
	"SelectItem-icon": "_SelectItem-icon_xjbef_93"
};
init_react_import();
init_react_import();
init_react_import();
var reorder$1 = (list, startIndex, endIndex) => {
	const result = Array.from(list);
	const [removed] = result.splice(startIndex, 1);
	result.splice(endIndex, 0, removed);
	return result;
};
init_react_import();
var replace = (list, index, newItem) => {
	const result = Array.from(list);
	result.splice(index, 1);
	result.splice(index, 0, newItem);
	return result;
};
init_react_import();
init_react_import();
init_react_import();
init_react_import();
var isProduction = true;
var prefix = "Invariant failed";
function invariant(condition, message) {
	if (condition) return;
	if (isProduction) throw new Error(prefix);
	var provided = typeof message === "function" ? message() : message;
	var value = provided ? "".concat(prefix, ": ").concat(provided) : prefix;
	throw new Error(value);
}
var getRect$1 = function getRect2(_ref) {
	var top = _ref.top, right = _ref.right, bottom = _ref.bottom, left = _ref.left;
	return {
		top,
		right,
		bottom,
		left,
		width: right - left,
		height: bottom - top,
		x: left,
		y: top,
		center: {
			x: (right + left) / 2,
			y: (bottom + top) / 2
		}
	};
};
var expand = function expand2(target, expandBy) {
	return {
		top: target.top - expandBy.top,
		left: target.left - expandBy.left,
		bottom: target.bottom + expandBy.bottom,
		right: target.right + expandBy.right
	};
};
var shrink = function shrink2(target, shrinkBy) {
	return {
		top: target.top + shrinkBy.top,
		left: target.left + shrinkBy.left,
		bottom: target.bottom - shrinkBy.bottom,
		right: target.right - shrinkBy.right
	};
};
var noSpacing = {
	top: 0,
	right: 0,
	bottom: 0,
	left: 0
};
var createBox = function createBox2(_ref2) {
	var borderBox = _ref2.borderBox, _ref2$margin = _ref2.margin, margin = _ref2$margin === void 0 ? noSpacing : _ref2$margin, _ref2$border = _ref2.border, border = _ref2$border === void 0 ? noSpacing : _ref2$border, _ref2$padding = _ref2.padding, padding = _ref2$padding === void 0 ? noSpacing : _ref2$padding;
	var marginBox = getRect$1(expand(borderBox, margin));
	var paddingBox = getRect$1(shrink(borderBox, border));
	var contentBox = getRect$1(shrink(paddingBox, padding));
	return {
		marginBox,
		borderBox: getRect$1(borderBox),
		paddingBox,
		contentBox,
		margin,
		border,
		padding
	};
};
var parse = function parse2(raw) {
	var value = raw.slice(0, -2);
	if (raw.slice(-2) !== "px") return 0;
	var result = Number(value);
	isNaN(result) && invariant(false);
	return result;
};
var calculateBox = function calculateBox2(borderBox, styles) {
	return createBox({
		borderBox,
		margin: {
			top: parse(styles.marginTop),
			right: parse(styles.marginRight),
			bottom: parse(styles.marginBottom),
			left: parse(styles.marginLeft)
		},
		padding: {
			top: parse(styles.paddingTop),
			right: parse(styles.paddingRight),
			bottom: parse(styles.paddingBottom),
			left: parse(styles.paddingLeft)
		},
		border: {
			top: parse(styles.borderTopWidth),
			right: parse(styles.borderRightWidth),
			bottom: parse(styles.borderBottomWidth),
			left: parse(styles.borderLeftWidth)
		}
	});
};
var getBox = function getBox2(el) {
	return calculateBox(el.getBoundingClientRect(), window.getComputedStyle(el));
};
var RESET_ZOOM_SMALLER_THAN_FRAME = true;
var getZoomConfig = (uiViewport, frame, zoom) => {
	const { width: frameWidth, height: frameHeight } = getBox(frame).contentBox;
	const viewportHeight = uiViewport.height === "auto" ? frameHeight : uiViewport.height;
	let rootHeight = 0;
	let autoZoom = 1;
	if (typeof uiViewport.width === "number" && (uiViewport.width > frameWidth || viewportHeight > frameHeight)) {
		const widthZoom = Math.min(frameWidth / uiViewport.width, 1);
		const heightZoom = Math.min(frameHeight / viewportHeight, 1);
		zoom = widthZoom;
		if (widthZoom < heightZoom) rootHeight = viewportHeight / zoom;
		else {
			rootHeight = viewportHeight;
			zoom = heightZoom;
		}
		autoZoom = zoom;
	} else if (RESET_ZOOM_SMALLER_THAN_FRAME) {
		autoZoom = 1;
		zoom = 1;
		rootHeight = viewportHeight;
	}
	return {
		autoZoom,
		rootHeight,
		zoom
	};
};
var useResetAutoZoom = (frameRef) => {
	const appStoreApi = useAppStoreApi();
	const resetAutoZoom = (options) => {
		const { state, zoomConfig, setZoomConfig } = appStoreApi.getState();
		const { viewports } = state.ui;
		const newViewports = (options == null ? void 0 : options.viewports) || viewports;
		if (frameRef.current) setZoomConfig(getZoomConfig(newViewports == null ? void 0 : newViewports.current, frameRef.current, zoomConfig.zoom));
	};
	return resetAutoZoom;
};
var getClassName$4 = get_class_name_factory_default("Select", styles_module_default$2);
var getItemClassName = get_class_name_factory_default("SelectItem", styles_module_default$2);
var Item$1 = ({ children, isSelected, onClick }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		className: getItemClassName({ isSelected }),
		onClick,
		children
	});
};
var Select = ({ children, options, onChange, value, defaultValue, mode, disabled = false }) => {
	const [open, setOpen] = (0, import_react.useState)(false);
	const hasOptions = options.length > 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName$4({
			hasValue: value !== defaultValue,
			hasOptions,
			actionBar: mode === "actionBar",
			standalone: mode === "standalone",
			disabled: disabled || !hasOptions
		}),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Popover, {
			open,
			onOpenChange: setOpen,
			children: [hasOptions ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverTrigger, {
				asChild: true,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
					className: getClassName$4("button"),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
						className: getClassName$4("buttonIcon"),
						children
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown$1, { size: 12 })]
				})
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: getClassName$4("button"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
					className: getClassName$4("buttonIcon"),
					children
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown$1, { size: 12 })]
			}) }), options.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverPortal, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PopoverContent, {
				align: "start",
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("ul", {
					className: getClassName$4("items"),
					"data-puck-rte-menu": true,
					children: options.map((option) => {
						const Icon2 = option.icon;
						return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Item$1, {
							isSelected: value === option.value,
							onClick: () => {
								onChange(option.value);
								setOpen(false);
							},
							children: [Icon2 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: getItemClassName("icon"),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon2, { size: 16 })
							}), option.label]
						}) }, option.value);
					})
				})
			}) })]
		})
	});
};
function SelectControl({ renderDefaultIcon, onChange, options, value, defaultValue }) {
	var _a, _b;
	const { inline, readOnly } = useControlContext();
	const optionsByValue = (0, import_react.useMemo)(() => options.reduce((acc, option) => __spreadProps$6(__spreadValues$7({}, acc), { [option.value]: option }), {}), [options]);
	const Node = (_b = value && ((_a = optionsByValue[value]) == null ? void 0 : _a.icon)) != null ? _b : renderDefaultIcon;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Select, {
		options,
		onChange,
		value,
		defaultValue,
		mode: inline ? "actionBar" : "standalone",
		disabled: readOnly,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Node, {})
	});
}
/*! Bundled license information:

lucide-react/dist/esm/shared/src/utils.js:
lucide-react/dist/esm/defaultAttributes.js:
lucide-react/dist/esm/Icon.js:
lucide-react/dist/esm/createLucideIcon.js:
lucide-react/dist/esm/icons/align-left.js:
lucide-react/dist/esm/icons/heading.js:
lucide-react/dist/esm/icons/list.js:
lucide-react/dist/esm/icons/align-center.js:
lucide-react/dist/esm/icons/align-justify.js:
lucide-react/dist/esm/icons/align-right.js:
lucide-react/dist/esm/icons/bold.js:
lucide-react/dist/esm/icons/chevron-down.js:
lucide-react/dist/esm/icons/chevron-right.js:
lucide-react/dist/esm/icons/chevron-up.js:
lucide-react/dist/esm/icons/circle-check-big.js:
lucide-react/dist/esm/icons/code.js:
lucide-react/dist/esm/icons/copy.js:
lucide-react/dist/esm/icons/corner-left-up.js:
lucide-react/dist/esm/icons/ellipsis-vertical.js:
lucide-react/dist/esm/icons/expand.js:
lucide-react/dist/esm/icons/globe.js:
lucide-react/dist/esm/icons/hammer.js:
lucide-react/dist/esm/icons/hash.js:
lucide-react/dist/esm/icons/heading-1.js:
lucide-react/dist/esm/icons/heading-2.js:
lucide-react/dist/esm/icons/heading-3.js:
lucide-react/dist/esm/icons/heading-4.js:
lucide-react/dist/esm/icons/heading-5.js:
lucide-react/dist/esm/icons/heading-6.js:
lucide-react/dist/esm/icons/italic.js:
lucide-react/dist/esm/icons/layers.js:
lucide-react/dist/esm/icons/layout-grid.js:
lucide-react/dist/esm/icons/link.js:
lucide-react/dist/esm/icons/list-ordered.js:
lucide-react/dist/esm/icons/lock-open.js:
lucide-react/dist/esm/icons/lock.js:
lucide-react/dist/esm/icons/maximize-2.js:
lucide-react/dist/esm/icons/minimize-2.js:
lucide-react/dist/esm/icons/minus.js:
lucide-react/dist/esm/icons/monitor.js:
lucide-react/dist/esm/icons/panel-left.js:
lucide-react/dist/esm/icons/panel-right.js:
lucide-react/dist/esm/icons/plus.js:
lucide-react/dist/esm/icons/quote.js:
lucide-react/dist/esm/icons/rectangle-ellipsis.js:
lucide-react/dist/esm/icons/redo-2.js:
lucide-react/dist/esm/icons/search.js:
lucide-react/dist/esm/icons/sliders-horizontal.js:
lucide-react/dist/esm/icons/smartphone.js:
lucide-react/dist/esm/icons/square-code.js:
lucide-react/dist/esm/icons/strikethrough.js:
lucide-react/dist/esm/icons/tablet.js:
lucide-react/dist/esm/icons/toy-brick.js:
lucide-react/dist/esm/icons/trash.js:
lucide-react/dist/esm/icons/type.js:
lucide-react/dist/esm/icons/underline.js:
lucide-react/dist/esm/icons/undo-2.js:
lucide-react/dist/esm/icons/x.js:
lucide-react/dist/esm/icons/zoom-in.js:
lucide-react/dist/esm/icons/zoom-out.js:
lucide-react/dist/esm/lucide-react.js:
(**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*)
*/
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-DPDJ67KA.mjs
init_react_import();
var optionNodes$2 = {
	left: {
		label: "Left",
		icon: AlignLeft
	},
	center: {
		label: "Center",
		icon: AlignCenter
	},
	right: {
		label: "Right",
		icon: AlignRight
	},
	justify: {
		label: "Justify",
		icon: AlignJustify
	}
};
var useAlignOptions = (fieldOptions) => {
	var _a;
	let blockOptions = [];
	if ((fieldOptions == null ? void 0 : fieldOptions.textAlign) !== false) if (!((_a = fieldOptions == null ? void 0 : fieldOptions.textAlign) == null ? void 0 : _a.alignments)) blockOptions = [
		"left",
		"center",
		"right",
		"justify"
	];
	else {
		if (fieldOptions == null ? void 0 : fieldOptions.textAlign.alignments.includes("left")) blockOptions.push("left");
		if (fieldOptions == null ? void 0 : fieldOptions.textAlign.alignments.includes("center")) blockOptions.push("center");
		if (fieldOptions == null ? void 0 : fieldOptions.textAlign.alignments.includes("right")) blockOptions.push("right");
		if (fieldOptions == null ? void 0 : fieldOptions.textAlign.alignments.includes("justify")) blockOptions.push("justify");
	}
	return (0, import_react.useMemo)(() => blockOptions.map((item) => ({
		value: item,
		label: optionNodes$2[item].label,
		icon: optionNodes$2[item].icon
	})), [blockOptions]);
};
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-ZMTK7SMX.mjs
init_react_import();
var optionNodes$1 = {
	h1: {
		label: "Heading 1",
		icon: Heading1
	},
	h2: {
		label: "Heading 2",
		icon: Heading2
	},
	h3: {
		label: "Heading 3",
		icon: Heading3
	},
	h4: {
		label: "Heading 4",
		icon: Heading4
	},
	h5: {
		label: "Heading 5",
		icon: Heading5
	},
	h6: {
		label: "Heading 6",
		icon: Heading6
	}
};
var useHeadingOptions = (fieldOptions) => {
	var _a;
	let blockOptions = [];
	if ((fieldOptions == null ? void 0 : fieldOptions.heading) !== false) if (!((_a = fieldOptions == null ? void 0 : fieldOptions.heading) == null ? void 0 : _a.levels)) blockOptions = [
		"h1",
		"h2",
		"h3",
		"h4",
		"h5",
		"h6"
	];
	else {
		if (fieldOptions == null ? void 0 : fieldOptions.heading.levels.includes(1)) blockOptions.push("h1");
		if (fieldOptions == null ? void 0 : fieldOptions.heading.levels.includes(2)) blockOptions.push("h2");
		if (fieldOptions == null ? void 0 : fieldOptions.heading.levels.includes(3)) blockOptions.push("h3");
		if (fieldOptions == null ? void 0 : fieldOptions.heading.levels.includes(4)) blockOptions.push("h4");
		if (fieldOptions == null ? void 0 : fieldOptions.heading.levels.includes(5)) blockOptions.push("h5");
		if (fieldOptions == null ? void 0 : fieldOptions.heading.levels.includes(6)) blockOptions.push("h6");
	}
	return (0, import_react.useMemo)(() => blockOptions.map((item) => ({
		value: item,
		label: optionNodes$1[item].label,
		icon: optionNodes$1[item].icon
	})), [blockOptions]);
};
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-LODTL5KW.mjs
init_react_import();
var optionNodes = {
	ul: {
		label: "Bullet list",
		icon: List
	},
	ol: {
		label: "Numbered list",
		icon: ListOrdered
	}
};
var useListOptions = (fieldOptions) => {
	let blockOptions = [];
	if ((fieldOptions == null ? void 0 : fieldOptions.listItem) !== false) blockOptions = ["ul", "ol"];
	return (0, import_react.useMemo)(() => blockOptions.map((item) => ({
		value: item,
		label: optionNodes[item].label,
		icon: optionNodes[item].icon
	})), [blockOptions]);
};
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-HGJWUFLT.mjs
init_react_import();
init_react_import();
var styles_module_default$1 = {
	"ActionBar": "_ActionBar_1nmyk_1",
	"ActionBar-label": "_ActionBar-label_1nmyk_18",
	"ActionBarAction": "_ActionBarAction_1nmyk_30",
	"ActionBar-group": "_ActionBar-group_1nmyk_38",
	"ActionBarAction--disabled": "_ActionBarAction--disabled_1nmyk_71",
	"ActionBarAction--active": "_ActionBarAction--active_1nmyk_93",
	"ActionBar-separator": "_ActionBar-separator_1nmyk_102"
};
var getClassName$3 = get_class_name_factory_default("ActionBar", styles_module_default$1);
var getActionClassName = get_class_name_factory_default("ActionBarAction", styles_module_default$1);
var ActionBar = ({ label, children }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
	className: getClassName$3(),
	onClick: (e) => {
		e.stopPropagation();
	},
	children: [label && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionBar.Group, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName$3("label"),
		children: label
	}) }), children]
});
var Action = ({ children, label, onClick, active = false, disabled }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
	type: "button",
	className: getActionClassName({
		active,
		disabled
	}),
	onClick,
	title: label,
	tabIndex: 0,
	disabled,
	children
});
var Group = ({ children }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	className: getClassName$3("group"),
	children
});
var Label = ({ label }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	className: getClassName$3("label"),
	children: label
});
var Separator = () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: getClassName$3("separator") });
ActionBar.Action = Action;
ActionBar.Label = Label;
ActionBar.Group = Group;
ActionBar.Separator = Separator;
init_react_import();
init_react_import();
var IconButton_module_default = {
	"IconButton": "_IconButton_1c68y_1",
	"IconButton--active": "_IconButton--active_1c68y_14",
	"IconButton--disabled": "_IconButton--disabled_1c68y_24"
};
init_react_import();
init_react_import();
var getClassName2$1 = get_class_name_factory_default("Loader", {
	"Loader": "_Loader_nacdm_13",
	"loader-animation": "_loader-animation_nacdm_1"
});
var Loader = (_a) => {
	var _b = _a, { color, size = 16 } = _b, props = __objRest$4(_b, ["color", "size"]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", __spreadValues$7({
		className: getClassName2$1(),
		style: {
			width: size,
			height: size,
			color
		},
		"aria-label": "loading"
	}, props));
};
var getClassName3$1 = get_class_name_factory_default("IconButton", IconButton_module_default);
var IconButton = ({ active = false, children, href, onClick, type, disabled, tabIndex, newTab, fullWidth, title, suppressHydrationWarning }) => {
	const [loading, setLoading] = (0, import_react.useState)(false);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(href ? "a" : "button", {
		className: getClassName3$1({
			active,
			disabled,
			fullWidth
		}),
		onClick: (e) => {
			if (!onClick) return;
			setLoading(true);
			Promise.resolve(onClick(e)).then(() => {
				setLoading(false);
			});
		},
		type,
		disabled: disabled || loading,
		tabIndex,
		target: newTab ? "_blank" : void 0,
		rel: newTab ? "noreferrer" : void 0,
		href,
		title,
		"aria-label": title,
		suppressHydrationWarning,
		children: [children, loading && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: ["\xA0\xA0", /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Loader, { size: 14 })] })]
	});
};
init_react_import();
init_react_import();
var styles_module_default3$1 = {
	"RichTextMenu": "_RichTextMenu_k97eh_1",
	"RichTextMenu--form": "_RichTextMenu--form_k97eh_7",
	"RichTextMenu-group": "_RichTextMenu-group_k97eh_17",
	"RichTextMenu--inline": "_RichTextMenu--inline_k97eh_35"
};
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
var getClassName4$1 = get_class_name_factory_default("Control", {
	"Control": "_Control_1aveu_1",
	"Control--inline": "_Control--inline_1aveu_6"
});
function Control({ icon, disabled, active, onClick, title }) {
	const { inline } = useControlContext();
	if (inline) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		className: getClassName4$1({ inline: true }),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Action, {
			onClick,
			disabled,
			active,
			label: title,
			children: icon
		})
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		className: getClassName4$1(),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
			onClick,
			disabled,
			active,
			title,
			children: icon
		})
	});
}
function AlignLeft2() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AlignLeft, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().setTextAlign("left").run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canAlignLeft),
		active: editorState == null ? void 0 : editorState.isAlignLeft,
		title: "Align left"
	});
}
init_react_import();
function AlignCenter2() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AlignCenter, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().setTextAlign("center").run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canAlignCenter),
		active: editorState == null ? void 0 : editorState.isAlignCenter,
		title: "Align center"
	});
}
init_react_import();
function AlignRight2() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AlignRight, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().setTextAlign("right").run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canAlignRight),
		active: editorState == null ? void 0 : editorState.isAlignRight,
		title: "Align right"
	});
}
init_react_import();
function AlignJustify2() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AlignJustify, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().setTextAlign("justify").run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canAlignJustify),
		active: editorState == null ? void 0 : editorState.isAlignJustify,
		title: "Justify"
	});
}
init_react_import();
init_react_import();
function AlignSelectFallback() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectControl, {
		options: useAlignOptions(useControlContext().options),
		onChange: () => {},
		value: "left",
		defaultValue: "left",
		renderDefaultIcon: AlignLeft
	});
}
var AlignSelectLoaded = (0, import_react.lazy)(() => import("./loaded-54625GTL-DuP8lhPN.js").then((m) => ({ default: m.AlignSelectLoaded })));
var AlignSelect = () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
	fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AlignSelectFallback, {}),
	children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AlignSelectLoaded, {})
});
init_react_import();
function Bold2() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bold, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleBold().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canBold),
		active: editorState == null ? void 0 : editorState.isBold,
		title: "Bold"
	});
}
init_react_import();
function Italic2() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Italic, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleItalic().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canItalic),
		active: editorState == null ? void 0 : editorState.isItalic,
		title: "Italic"
	});
}
init_react_import();
function Underline2() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Underline, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleUnderline().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canUnderline),
		active: editorState == null ? void 0 : editorState.isUnderline,
		title: "Underline"
	});
}
init_react_import();
function Strikethrough2() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Strikethrough, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleStrike().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canStrike),
		active: editorState == null ? void 0 : editorState.isStrike,
		title: "Strikethrough"
	});
}
init_react_import();
function InlineCode() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Code, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleCode().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canInlineCode),
		active: editorState == null ? void 0 : editorState.isInlineCode,
		title: "Inline code"
	});
}
init_react_import();
function BulletList() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(List, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleBulletList().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canBulletList),
		active: editorState == null ? void 0 : editorState.isBulletList,
		title: "Bullet list"
	});
}
init_react_import();
function OrderedList() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ListOrdered, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleOrderedList().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canOrderedList),
		active: editorState == null ? void 0 : editorState.isOrderedList,
		title: "Ordered list"
	});
}
init_react_import();
function CodeBlock() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SquareCode, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleCodeBlock().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canCodeBlock),
		active: editorState == null ? void 0 : editorState.isCodeBlock,
		title: "Code block"
	});
}
init_react_import();
function Blockquote() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Quote, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().toggleBlockquote().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canBlockquote),
		active: editorState == null ? void 0 : editorState.isBlockquote,
		title: "Blockquote"
	});
}
init_react_import();
function HorizontalRule() {
	const { editor, editorState } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Control, {
		icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Minus, {}),
		onClick: (e) => {
			e.stopPropagation();
			editor?.chain().focus().setHorizontalRule().run();
		},
		disabled: !(editorState == null ? void 0 : editorState.canHorizontalRule),
		title: "Horizontal rule"
	});
}
init_react_import();
init_react_import();
function HeadingSelectFallback() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectControl, {
		options: useHeadingOptions(useControlContext().options),
		onChange: () => {},
		value: "p",
		defaultValue: "p",
		renderDefaultIcon: Heading$1
	});
}
var HeadingSelectLoaded = (0, import_react.lazy)(() => import("./loaded-OMRIYZIQ-DsPB-PKI.js").then((m) => ({ default: m.HeadingSelectLoaded })));
var HeadingSelect = () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
	fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HeadingSelectFallback, {}),
	children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HeadingSelectLoaded, {})
});
init_react_import();
init_react_import();
function ListSelectFallback() {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectControl, {
		options: useListOptions(useControlContext().options),
		onChange: () => {},
		value: "p",
		defaultValue: "p",
		renderDefaultIcon: List
	});
}
var ListSelectLoaded = (0, import_react.lazy)(() => import("./loaded-Y4ZWZC55-B3y7wrid.js").then((m) => ({ default: m.ListSelectLoaded })));
var ListSelect = () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
	fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ListSelectFallback, {}),
	children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ListSelectLoaded, {})
});
var getClassName5$1 = get_class_name_factory_default("RichTextMenu", styles_module_default3$1);
var DefaultMenu = ({ children }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RichTextMenu, { children });
};
var RichTextMenu = ({ children }) => {
	const { inline } = useControlContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName5$1({
			inline,
			form: !inline
		}),
		"data-puck-rte-menu": true,
		children
	});
};
var Group2 = ({ children }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName5$1("group"),
		children
	});
};
RichTextMenu.Group = Group2;
RichTextMenu.Control = Control;
RichTextMenu.AlignCenter = AlignCenter2;
RichTextMenu.AlignJustify = AlignJustify2;
RichTextMenu.AlignLeft = AlignLeft2;
RichTextMenu.AlignRight = AlignRight2;
RichTextMenu.AlignSelect = AlignSelect;
RichTextMenu.Blockquote = Blockquote;
RichTextMenu.Bold = Bold2;
RichTextMenu.BulletList = BulletList;
RichTextMenu.CodeBlock = CodeBlock;
RichTextMenu.HeadingSelect = HeadingSelect;
RichTextMenu.HorizontalRule = HorizontalRule;
RichTextMenu.InlineCode = InlineCode;
RichTextMenu.Italic = Italic2;
RichTextMenu.ListSelect = ListSelect;
RichTextMenu.OrderedList = OrderedList;
RichTextMenu.Strikethrough = Strikethrough2;
RichTextMenu.Underline = Underline2;
var LoadedRichTextMenuInner = ({ editor = null, editorState = null, field, readOnly, inline }) => {
	const { renderMenu, renderInlineMenu } = field;
	const InlineMenu = (0, import_react.useMemo)(() => renderInlineMenu || DefaultMenu, [renderInlineMenu]);
	const Menu = (0, import_react.useMemo)(() => renderMenu || DefaultMenu, [renderMenu]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ControlContext.Provider, {
		value: {
			editor,
			editorState,
			inline,
			options: field.options,
			readOnly
		},
		children: inline ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineMenu, {
			editor,
			editorState,
			readOnly,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Group2, { children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bold2, {}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Italic2, {}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Underline2, {})
			] })
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Menu, {
			editor,
			editorState,
			readOnly,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Group2, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(HeadingSelect, {}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ListSelect, {})] }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Group2, { children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Bold2, {}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Italic2, {}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Underline2, {})
				] }),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Group2, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AlignSelect, {}) })
			]
		})
	});
};
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-TDQEUXB2.mjs
init_react_import();
var getClassName$2 = get_class_name_factory_default("RichTextEditor", styles_module_default$3);
var EditorInner = (0, import_react.memo)(({ children, menu, readOnly = false, field, inline = false, editor, id }) => {
	const { initialHeight } = field;
	const isActive = useAppStore((s) => {
		var _a;
		return ((_a = s.currentRichText) == null ? void 0 : _a.id) === id && inline === s.currentRichText.inline;
	});
	const appStoreApi = useAppStoreApi();
	const handleHotkeyCapture = (0, import_react.useCallback)((event) => {
		var _a, _b;
		if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "i") {
			event.stopPropagation();
			event.preventDefault();
			(_b = editor == null ? void 0 : (_a = editor.commands).toggleItalic) == null || _b.call(_a);
		}
		if (event.key.toLowerCase() === "backspace") event.stopPropagation();
	}, [editor]);
	const handleBlur = (0, import_react.useCallback)((e) => {
		var _a, _b;
		const targetInMenu = !!((_b = (_a = e.relatedTarget) == null ? void 0 : _a.closest) == null ? void 0 : _b.call(_a, "[data-puck-rte-menu]"));
		if (e.relatedTarget && !targetInMenu) appStoreApi.setState({ currentRichText: null });
		else e.stopPropagation();
	}, [appStoreApi]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName$2({
			editor: !inline,
			inline,
			isActive,
			disabled: readOnly
		}),
		style: inline ? {} : {
			height: initialHeight != null ? initialHeight : 192,
			overflowY: "auto"
		},
		onKeyDownCapture: handleHotkeyCapture,
		onBlur: handleBlur,
		children: [!inline && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName$2("menu"),
			children: menu
		}), children]
	});
});
EditorInner.displayName = "EditorInner";
init_react_import();
var LoadedRichTextMenuFull = (0, import_react.lazy)(() => import("./full-5QCBHXRC-Bddteg7e.js").then((m) => ({ default: m.LoadedRichTextMenuFull })));
var LoadedRichTextMenu = (props) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
		fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadedRichTextMenuInner, __spreadValues$7({}, props)),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadedRichTextMenuFull, __spreadValues$7({}, props))
	});
};
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-X2ZFWNCU.mjs
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
var getSlotTransform = (renderSlotEdit, renderSlotRender = renderSlotEdit) => ({ slot: ({ value: content, propName, field, isReadOnly }) => {
	const render = isReadOnly ? renderSlotRender : renderSlotEdit;
	const Slot = (dzProps) => render(__spreadProps$6(__spreadValues$7({
		allow: (field == null ? void 0 : field.type) === "slot" ? field.allow : [],
		disallow: (field == null ? void 0 : field.type) === "slot" ? field.disallow : []
	}, dzProps), {
		zone: propName,
		content
	}));
	return Slot;
} });
init_react_import();
init_react_import();
init_react_import();
function buildMappers(transforms, readOnly, forceReadOnly) {
	return Object.keys(transforms).reduce((acc, _fieldType) => {
		const fieldType = _fieldType;
		return __spreadProps$6(__spreadValues$7({}, acc), { [fieldType]: (_a) => {
			var _b = _a, { parentId } = _b, params = __objRest$4(_b, ["parentId"]);
			const wildcardPath = params.propPath.replace(/\[\d+\]/g, "[*]");
			const isReadOnly = (readOnly == null ? void 0 : readOnly[params.propPath]) || (readOnly == null ? void 0 : readOnly[wildcardPath]) || forceReadOnly || false;
			const fn = transforms[fieldType];
			return fn == null ? void 0 : fn(__spreadProps$6(__spreadValues$7({}, params), {
				isReadOnly,
				componentId: parentId
			}));
		} });
	}, {});
}
function useFieldTransforms(config, item, transforms, readOnly, forceReadOnly) {
	const mappers = (0, import_react.useMemo)(() => buildMappers(transforms, readOnly, forceReadOnly), [
		transforms,
		readOnly,
		forceReadOnly
	]);
	const transformedProps = (0, import_react.useMemo)(() => {
		return mapFields(item, mappers, config).props;
	}, [
		config,
		item,
		mappers
	]);
	return (0, import_react.useMemo)(() => __spreadValues$7(__spreadValues$7({}, item.props), transformedProps), [item.props, transformedProps]);
}
function useSlots(config, item, renderSlotEdit, renderSlotRender = renderSlotEdit, readOnly, forceReadOnly) {
	return useFieldTransforms(config, item, getSlotTransform(renderSlotEdit, renderSlotRender), readOnly, forceReadOnly);
}
init_react_import();
init_react_import();
var getClassName$1 = get_class_name_factory_default("RichTextEditor", styles_module_default$3);
function RichTextRenderFallback({ content }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName$1(),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "rich-text",
			dangerouslySetInnerHTML: { __html: content }
		})
	});
}
init_react_import();
var mapDeep = (source, path, render) => {
	if (!source) return null;
	if (path.length === 0) return render(source);
	const [key, ...rest] = path;
	if (Array.isArray(source)) return source.map((item) => mapDeep(item, path, render));
	return __spreadProps$6(__spreadValues$7({}, source), { [key]: mapDeep(source[key], rest, render) });
};
function useRichtextProps(fields, props) {
	const findAllRichtextKeys = (fields2, path = []) => {
		if (!fields2) return [];
		const result = [];
		for (const [key, field] of Object.entries(fields2)) {
			const currentPath = [...path, key];
			if (field.type === "richtext") result.push({
				path: currentPath,
				field
			});
			if (field.type === "array" && "arrayFields" in field) result.push(...findAllRichtextKeys(field.arrayFields, currentPath));
			if (field.type === "object" && "objectFields" in field) result.push(...findAllRichtextKeys(field.objectFields, currentPath));
		}
		return result;
	};
	const richtextKeys = (0, import_react.useMemo)(() => findAllRichtextKeys(fields), [fields]);
	return (0, import_react.useMemo)(() => {
		if (!(richtextKeys == null ? void 0 : richtextKeys.length)) return {};
		const RichTextRender = (0, import_react.lazy)(() => import("./Render-MJ563JES-BvL0nwRl.js").then((m) => ({ default: m.RichTextRender })));
		let result = __spreadValues$7({}, props);
		for (const { path, field } of richtextKeys) result = mapDeep(result, path, (content) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
			fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RichTextRenderFallback, { content }),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RichTextRender, {
				content,
				field
			})
		}, path.join(".")));
		return result;
	}, [
		richtextKeys,
		props,
		fields
	]);
}
init_react_import();
var SlotRenderPure = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotRender, __spreadValues$7({}, props));
var Item = ({ config, item, metadata }) => {
	const Component = config.components[item.type];
	const props = useSlots(config, item, (slotProps) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotRenderPure, __spreadProps$6(__spreadValues$7({}, slotProps), {
		config,
		metadata
	})));
	const richtextProps = useRichtextProps(Component.fields, props);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component.render, __spreadProps$6(__spreadValues$7(__spreadValues$7({}, props), richtextProps), { puck: __spreadProps$6(__spreadValues$7({}, props.puck), { metadata: metadata || {} }) }));
};
var SlotRender = (0, import_react.forwardRef)(function SlotRenderInternal({ className, style, content, config, metadata, as }, ref) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(as != null ? as : "div", {
		className,
		style,
		ref,
		children: content.map((item) => {
			if (!config.components[item.type]) return null;
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Item, {
				config,
				item,
				metadata
			}, item.props.id);
		})
	});
});
//#endregion
//#region node_modules/zustand/esm/vanilla/shallow.mjs
var isIterable = (obj) => Symbol.iterator in obj;
var hasIterableEntries = (value) => "entries" in value;
var compareEntries = (valueA, valueB) => {
	const mapA = valueA instanceof Map ? valueA : new Map(valueA.entries());
	const mapB = valueB instanceof Map ? valueB : new Map(valueB.entries());
	if (mapA.size !== mapB.size) return false;
	for (const [key, value] of mapA) if (!mapB.has(key) || !Object.is(value, mapB.get(key))) return false;
	return true;
};
var compareIterables = (valueA, valueB) => {
	const iteratorA = valueA[Symbol.iterator]();
	const iteratorB = valueB[Symbol.iterator]();
	let nextA = iteratorA.next();
	let nextB = iteratorB.next();
	while (!nextA.done && !nextB.done) {
		if (!Object.is(nextA.value, nextB.value)) return false;
		nextA = iteratorA.next();
		nextB = iteratorB.next();
	}
	return !!nextA.done && !!nextB.done;
};
function shallow(valueA, valueB) {
	if (Object.is(valueA, valueB)) return true;
	if (typeof valueA !== "object" || valueA === null || typeof valueB !== "object" || valueB === null) return false;
	if (Object.getPrototypeOf(valueA) !== Object.getPrototypeOf(valueB)) return false;
	if (isIterable(valueA) && isIterable(valueB)) {
		if (hasIterableEntries(valueA) && hasIterableEntries(valueB)) return compareEntries(valueA, valueB);
		return compareIterables(valueA, valueB);
	}
	return compareEntries({ entries: () => Object.entries(valueA) }, { entries: () => Object.entries(valueB) });
}
//#endregion
//#region node_modules/zustand/esm/react/shallow.mjs
function useShallow(selector) {
	const prev = import_react.useRef(void 0);
	return (state) => {
		const next = selector(state);
		return shallow(prev.current, next) ? prev.current : prev.current = next;
	};
}
//#endregion
//#region node_modules/@preact/signals-core/dist/signals-core.module.js
var i$1 = Symbol.for("preact-signals");
function t$1() {
	if (!(v > 1)) {
		var i, t = !1;
		(function() {
			var i = c$1;
			c$1 = void 0;
			while (void 0 !== i) {
				var t = i.S;
				if (t.v === i.v) {
					for (var n = t.t; void 0 !== n; n = n.x) if (n.i === i.i) n.i = t.i;
				}
				i = i.o;
			}
		})();
		while (void 0 !== h) {
			var n = h;
			h = void 0;
			s++;
			while (void 0 !== n) {
				var r = n.u;
				n.u = void 0;
				n.f &= -3;
				if (!(8 & n.f) && w(n)) try {
					n.c();
				} catch (n) {
					if (!t) {
						i = n;
						t = !0;
					}
				}
				n = r;
			}
		}
		s = 0;
		v--;
		if (t) throw i;
	} else v--;
}
function n$1(i) {
	if (v > 0) return i();
	e$1 = ++u$1;
	v++;
	try {
		return i();
	} finally {
		t$1();
	}
}
var r$1, o$1 = void 0;
function f(i) {
	var t = o$1, n = r$1;
	o$1 = void 0;
	r$1 = void 0;
	try {
		return i();
	} finally {
		o$1 = t;
		r$1 = n;
	}
}
var h = void 0, v = 0, s = 0, u$1 = 0, e$1 = 0, c$1 = void 0, d = 0;
function a$1(i) {
	if (void 0 !== o$1) {
		var t = i.n;
		if (void 0 === t || t.t !== o$1) {
			t = {
				i: 0,
				S: i,
				p: o$1.s,
				n: void 0,
				t: o$1,
				e: void 0,
				x: void 0,
				r: t
			};
			if (void 0 !== o$1.s) o$1.s.n = t;
			o$1.s = t;
			i.n = t;
			if (32 & o$1.f) i.S(t);
			return t;
		} else if (-1 === t.i) {
			t.i = 0;
			if (void 0 !== t.n) {
				t.n.p = t.p;
				if (void 0 !== t.p) t.p.n = t.n;
				t.p = o$1.s;
				t.n = void 0;
				o$1.s.n = t;
				o$1.s = t;
			}
			return t;
		}
	}
}
function l(i, t) {
	this.v = i;
	this.i = 0;
	this.n = void 0;
	this.t = void 0;
	this.l = 0;
	this.W = null == t ? void 0 : t.watched;
	this.Z = null == t ? void 0 : t.unwatched;
	this.name = null == t ? void 0 : t.name;
}
l.prototype.brand = i$1;
l.prototype.h = function() {
	return !0;
};
l.prototype.S = function(i) {
	var t = this, n = this.t;
	if (n !== i && void 0 === i.e) {
		i.x = n;
		this.t = i;
		if (void 0 !== n) n.e = i;
		else f(function() {
			var i;
			null == (i = t.W) || i.call(t);
		});
	}
};
l.prototype.U = function(i) {
	var t = this;
	if (void 0 !== this.t) {
		var n = i.e, r = i.x;
		if (void 0 !== n) {
			n.x = r;
			i.e = void 0;
		}
		if (void 0 !== r) {
			r.e = n;
			i.x = void 0;
		}
		if (i === this.t) {
			this.t = r;
			if (void 0 === r) f(function() {
				var i;
				null == (i = t.Z) || i.call(t);
			});
		}
	}
};
l.prototype.subscribe = function(i) {
	var t = this;
	return j(function() {
		var n = t.value;
		f(function() {
			return i(n);
		});
	}, { name: "sub" });
};
l.prototype.valueOf = function() {
	return this.value;
};
l.prototype.toString = function() {
	return this.value + "";
};
l.prototype.toJSON = function() {
	return this.value;
};
l.prototype.peek = function() {
	var i = this;
	return f(function() {
		return i.value;
	});
};
Object.defineProperty(l.prototype, "value", {
	get: function() {
		var i = a$1(this);
		if (void 0 !== i) i.i = this.i;
		return this.v;
	},
	set: function(i) {
		if (i !== this.v) {
			if (s > 100) throw new Error("Cycle detected");
			(function(i) {
				if (0 !== v && 0 === s) {
					if (i.l !== e$1) {
						i.l = e$1;
						c$1 = {
							S: i,
							v: i.v,
							i: i.i,
							o: c$1
						};
					}
				}
			})(this);
			this.v = i;
			this.i++;
			d++;
			v++;
			try {
				for (var n = this.t; void 0 !== n; n = n.x) n.t.N();
			} finally {
				t$1();
			}
		}
	}
});
function y(i, t) {
	return new l(i, t);
}
function w(i) {
	for (var t = i.s; void 0 !== t; t = t.n) if (t.S.i !== t.i || !t.S.h() || t.S.i !== t.i) return !0;
	return !1;
}
function _(i) {
	for (var t = i.s; void 0 !== t; t = t.n) {
		var n = t.S.n;
		if (void 0 !== n) t.r = n;
		t.S.n = t;
		t.i = -1;
		if (void 0 === t.n) {
			i.s = t;
			break;
		}
	}
}
function b(i) {
	var t = i.s, n = void 0;
	while (void 0 !== t) {
		var r = t.p;
		if (-1 === t.i) {
			t.S.U(t);
			if (void 0 !== r) r.n = t.n;
			if (void 0 !== t.n) t.n.p = r;
		} else n = t;
		t.S.n = t.r;
		if (void 0 !== t.r) t.r = void 0;
		t = r;
	}
	i.s = n;
}
function p(i, t) {
	l.call(this, void 0, t);
	this.x = i;
	this.s = void 0;
	this.g = d - 1;
	this.f = 4;
}
p.prototype = new l();
p.prototype.h = function() {
	this.f &= -3;
	if (1 & this.f) return !1;
	if (32 == (36 & this.f)) return !0;
	this.f &= -5;
	if (this.g === d) return !0;
	this.g = d;
	this.f |= 1;
	if (this.i > 0 && !w(this)) {
		this.f &= -2;
		return !0;
	}
	var i = o$1;
	try {
		_(this);
		o$1 = this;
		var t = this.x();
		if (16 & this.f || this.v !== t || 0 === this.i) {
			this.v = t;
			this.f &= -17;
			this.i++;
		}
	} catch (i) {
		this.v = i;
		this.f |= 16;
		this.i++;
	}
	o$1 = i;
	b(this);
	this.f &= -2;
	return !0;
};
p.prototype.S = function(i) {
	if (void 0 === this.t) {
		this.f |= 36;
		for (var t = this.s; void 0 !== t; t = t.n) t.S.S(t);
	}
	l.prototype.S.call(this, i);
};
p.prototype.U = function(i) {
	if (void 0 !== this.t) {
		l.prototype.U.call(this, i);
		if (void 0 === this.t) {
			this.f &= -33;
			for (var t = this.s; void 0 !== t; t = t.n) t.S.U(t);
		}
	}
};
p.prototype.N = function() {
	if (!(2 & this.f)) {
		this.f |= 6;
		for (var i = this.t; void 0 !== i; i = i.x) i.t.N();
	}
};
Object.defineProperty(p.prototype, "value", { get: function() {
	if (1 & this.f) throw new Error("Cycle detected");
	var i = a$1(this);
	this.h();
	if (void 0 !== i) i.i = this.i;
	if (16 & this.f) throw this.v;
	return this.v;
} });
function g(i, t) {
	return new p(i, t);
}
function S(i) {
	var n = i.m;
	i.m = void 0;
	if ("function" == typeof n) {
		v++;
		var r = o$1;
		o$1 = void 0;
		try {
			n();
		} catch (t) {
			i.f &= -2;
			i.f |= 8;
			m$1(i);
			throw t;
		} finally {
			o$1 = r;
			t$1();
		}
	}
}
function m$1(i) {
	for (var t = i.s; void 0 !== t; t = t.n) t.S.U(t);
	i.x = void 0;
	i.s = void 0;
	S(i);
}
function x(i) {
	if (o$1 !== this) throw new Error("Out-of-order effect");
	b(this);
	o$1 = i;
	this.f &= -2;
	if (8 & this.f) m$1(this);
	t$1();
}
function E(i, t) {
	this.x = i;
	this.m = void 0;
	this.s = void 0;
	this.u = void 0;
	this.f = 32;
	this.name = null == t ? void 0 : t.name;
	if (r$1) r$1.push(this);
}
E.prototype.c = function() {
	var i = this.S();
	try {
		if (8 & this.f) return;
		if (void 0 === this.x) return;
		var t = this.x();
		if ("function" == typeof t) this.m = t;
	} finally {
		i();
	}
};
E.prototype.S = function() {
	if (1 & this.f) throw new Error("Cycle detected");
	this.f |= 1;
	this.f &= -9;
	S(this);
	_(this);
	v++;
	var i = o$1;
	o$1 = this;
	return x.bind(this, i);
};
E.prototype.N = function() {
	if (!(2 & this.f)) {
		this.f |= 2;
		this.u = h;
		h = this;
	}
};
E.prototype.d = function() {
	this.f |= 8;
	if (!(1 & this.f)) m$1(this);
};
E.prototype.dispose = function() {
	this.d();
};
function j(i, t) {
	var n = new E(i, t);
	try {
		n.c();
	} catch (i) {
		n.d();
		throw i;
	}
	var r = n.d.bind(n);
	r[Symbol.dispose] = r;
	return r;
}
//#endregion
//#region node_modules/@dnd-kit/state/dist/index.mjs
var __create$5 = Object.create;
var __defProp$6 = Object.defineProperty;
var __defProps$5 = Object.defineProperties;
var __getOwnPropDesc$5 = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs$5 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols$6 = Object.getOwnPropertySymbols;
var __hasOwnProp$6 = Object.prototype.hasOwnProperty;
var __propIsEnum$6 = Object.prototype.propertyIsEnumerable;
var __knownSymbol$5 = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError$6 = (msg) => {
	throw TypeError(msg);
};
var __defNormalProp$6 = (obj, key, value) => key in obj ? __defProp$6(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues$6 = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp$6.call(b, prop)) __defNormalProp$6(a, prop, b[prop]);
	if (__getOwnPropSymbols$6) {
		for (var prop of __getOwnPropSymbols$6(b)) if (__propIsEnum$6.call(b, prop)) __defNormalProp$6(a, prop, b[prop]);
	}
	return a;
};
var __spreadProps$5 = (a, b) => __defProps$5(a, __getOwnPropDescs$5(b));
var __name$3 = (target, value) => __defProp$6(target, "name", {
	value,
	configurable: true
});
var __decoratorStart$5 = (base) => {
	var _a2;
	return [
		,
		,
		,
		__create$5((_a2 = base == null ? void 0 : base[__knownSymbol$5("metadata")]) != null ? _a2 : null)
	];
};
var __decoratorStrings$5 = [
	"class",
	"method",
	"getter",
	"setter",
	"accessor",
	"field",
	"value",
	"get",
	"set"
];
var __expectFn$5 = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError$6("Function expected") : fn;
var __decoratorContext$5 = (kind, name, done, metadata, fns) => ({
	kind: __decoratorStrings$5[kind],
	name,
	metadata,
	addInitializer: (fn) => done._ ? __typeError$6("Already initialized") : fns.push(__expectFn$5(fn || null))
});
var __decoratorMetadata$5 = (array, target) => __defNormalProp$6(target, __knownSymbol$5("metadata"), array[3]);
var __runInitializers$5 = (array, flags, self, value) => {
	for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
	return value;
};
var __decorateElement$5 = (array, flags, name, decorators, target, extra) => {
	var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
	var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings$5[k + 5];
	var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
	var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc$5(k < 4 ? target : {
		get [name]() {
			return __privateGet$6(this, extra);
		},
		set [name](x) {
			return __privateSet$6(this, extra, x);
		}
	}, name));
	k ? p && k < 4 && __name$3(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name$3(target, name);
	for (var i = decorators.length - 1; i >= 0; i--) {
		ctx = __decoratorContext$5(k, name, done = {}, array[3], extraInitializers);
		if (k) {
			ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn$3(target, x) : (x) => name in x };
			if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet$6 : __privateMethod$4)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
			if (k > 2) access.set = p ? (x, y) => __privateSet$6(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
		}
		it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : {
			get: desc.get,
			set: desc.set
		} : target, ctx), done._ = 1;
		if (k ^ 4 || it === void 0) __expectFn$5(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
		else if (typeof it !== "object" || it === null) __typeError$6("Object expected");
		else __expectFn$5(fn = it.get) && (desc.get = fn), __expectFn$5(fn = it.set) && (desc.set = fn), __expectFn$5(fn = it.init) && initializers.unshift(fn);
	}
	return k || __decoratorMetadata$5(array, target), desc && __defProp$6(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __accessCheck$6 = (obj, member, msg) => member.has(obj) || __typeError$6("Cannot " + msg);
var __privateIn$3 = (member, obj) => Object(obj) !== obj ? __typeError$6("Cannot use the \"in\" operator on this value") : member.has(obj);
var __privateGet$6 = (obj, member, getter) => (__accessCheck$6(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd$6 = (obj, member, value) => member.has(obj) ? __typeError$6("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet$6 = (obj, member, value, setter) => (__accessCheck$6(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod$4 = (obj, member, method) => (__accessCheck$6(obj, member, "access private method"), method);
function computed(compute, comparator) {
	if (comparator) {
		let previousValue;
		return g(() => {
			const value = compute();
			if (value && previousValue && comparator(previousValue, value)) return previousValue;
			previousValue = value;
			return value;
		});
	}
	return g(compute);
}
function deepEqual(a, b) {
	if (Object.is(a, b)) return true;
	if (a === null || b === null) return false;
	if (typeof a === "function" && typeof b === "function") return a === b;
	if (a instanceof Set && b instanceof Set) {
		if (a.size !== b.size) return false;
		for (const value of a) if (!b.has(value)) return false;
		return true;
	}
	if (Array.isArray(a)) {
		if (!Array.isArray(b) || a.length !== b.length) return false;
		return !a.some((value, index) => !deepEqual(value, b[index]));
	}
	if (typeof a === "object" && typeof b === "object") {
		const aKeys = Object.keys(a);
		const bKeys = Object.keys(b);
		if (aKeys.length !== bKeys.length) return false;
		return !aKeys.some((key) => !deepEqual(a[key], b[key]));
	}
	return false;
}
function reactive({ get }, _) {
	return {
		init(value) {
			return y(value);
		},
		get() {
			return get.call(this).value;
		},
		set(newValue) {
			const current = get.call(this);
			if (current.peek() === newValue) return;
			current.value = newValue;
		}
	};
}
function derived(target, _) {
	const map = /* @__PURE__ */ new WeakMap();
	return function() {
		let result = map.get(this);
		if (!result) {
			result = computed(target.bind(this));
			map.set(this, result);
		}
		return result.value;
	};
}
function enumerable(enumerable2 = true) {
	return function(_value, context) {
		context.addInitializer(function() {
			const host = context.kind === "field" ? this : context.static ? this : Object.getPrototypeOf(this);
			const descriptor = Object.getOwnPropertyDescriptor(host, context.name);
			if (descriptor) Object.defineProperty(host, context.name, __spreadProps$5(__spreadValues$6({}, descriptor), { enumerable: enumerable2 }));
		});
	};
}
function effects(...entries) {
	const effects2 = entries.map(j);
	return () => effects2.forEach((cleanup) => cleanup());
}
var _previous_dec, _initial_dec, _current_dec$1, _current_dec2, _previous_dec2, _initial_dec2 = [reactive], _init$5, _initial, _a$3, initial_get, initial_set, _ValueHistory_instances, _previous, _b$1, previous_get, previous_set, _current, _c$2, current_get, current_set;
_previous_dec2 = [reactive], _current_dec2 = [reactive], _current_dec$1 = [enumerable()], _initial_dec = [enumerable()], _previous_dec = [enumerable()];
var ValueHistory = class {
	constructor(defaultValue, equals = Object.is) {
		this.defaultValue = defaultValue;
		this.equals = equals;
		__runInitializers$5(_init$5, 5, this);
		__privateAdd$6(this, _ValueHistory_instances);
		__privateAdd$6(this, _initial, __runInitializers$5(_init$5, 8, this)), __runInitializers$5(_init$5, 11, this);
		__privateAdd$6(this, _previous, __runInitializers$5(_init$5, 12, this)), __runInitializers$5(_init$5, 15, this);
		__privateAdd$6(this, _current, __runInitializers$5(_init$5, 16, this)), __runInitializers$5(_init$5, 19, this);
		this.reset = this.reset.bind(this);
		this.reset();
	}
	get current() {
		return __privateGet$6(this, _ValueHistory_instances, current_get);
	}
	get initial() {
		return __privateGet$6(this, _ValueHistory_instances, initial_get);
	}
	get previous() {
		return __privateGet$6(this, _ValueHistory_instances, previous_get);
	}
	/** Set the current value */
	set current(value) {
		const current = f(() => __privateGet$6(this, _ValueHistory_instances, current_get));
		if (value && current && this.equals(current, value)) return;
		n$1(() => {
			if (!__privateGet$6(this, _ValueHistory_instances, initial_get)) __privateSet$6(this, _ValueHistory_instances, value, initial_set);
			__privateSet$6(this, _ValueHistory_instances, current, previous_set);
			__privateSet$6(this, _ValueHistory_instances, value, current_set);
		});
	}
	/** Reset the state to the initial value */
	reset(value = this.defaultValue) {
		n$1(() => {
			__privateSet$6(this, _ValueHistory_instances, void 0, previous_set);
			__privateSet$6(this, _ValueHistory_instances, value, initial_set);
			__privateSet$6(this, _ValueHistory_instances, value, current_set);
		});
	}
};
_init$5 = __decoratorStart$5(null);
_initial = /* @__PURE__ */ new WeakMap();
_ValueHistory_instances = /* @__PURE__ */ new WeakSet();
_previous = /* @__PURE__ */ new WeakMap();
_current = /* @__PURE__ */ new WeakMap();
_a$3 = __decorateElement$5(_init$5, 20, "#initial", _initial_dec2, _ValueHistory_instances, _initial), initial_get = _a$3.get, initial_set = _a$3.set;
_b$1 = __decorateElement$5(_init$5, 20, "#previous", _previous_dec2, _ValueHistory_instances, _previous), previous_get = _b$1.get, previous_set = _b$1.set;
_c$2 = __decorateElement$5(_init$5, 20, "#current", _current_dec2, _ValueHistory_instances, _current), current_get = _c$2.get, current_set = _c$2.set;
__decorateElement$5(_init$5, 2, "current", _current_dec$1, ValueHistory);
__decorateElement$5(_init$5, 2, "initial", _initial_dec, ValueHistory);
__decorateElement$5(_init$5, 2, "previous", _previous_dec, ValueHistory);
__decoratorMetadata$5(_init$5, ValueHistory);
function snapshot(value) {
	return f(() => {
		const output = {};
		for (const key in value) output[key] = value[key];
		return output;
	});
}
var _store;
var WeakStore = class {
	constructor() {
		__privateAdd$6(this, _store, /* @__PURE__ */ new WeakMap());
	}
	get(key, id) {
		var _a2;
		return key ? (_a2 = __privateGet$6(this, _store).get(key)) == null ? void 0 : _a2.get(id) : void 0;
	}
	set(key, id, value) {
		var _a2;
		if (!key) return;
		if (!__privateGet$6(this, _store).has(key)) __privateGet$6(this, _store).set(key, /* @__PURE__ */ new Map());
		return (_a2 = __privateGet$6(this, _store).get(key)) == null ? void 0 : _a2.set(id, value);
	}
	clear(key) {
		var _a2;
		return key ? (_a2 = __privateGet$6(this, _store).get(key)) == null ? void 0 : _a2.clear() : void 0;
	}
};
_store = /* @__PURE__ */ new WeakMap();
//#endregion
//#region node_modules/@dnd-kit/geometry/dist/index.mjs
var __create$4 = Object.create;
var __defProp$5 = Object.defineProperty;
var __getOwnPropDesc$4 = Object.getOwnPropertyDescriptor;
var __getOwnPropSymbols$5 = Object.getOwnPropertySymbols;
var __hasOwnProp$5 = Object.prototype.hasOwnProperty;
var __propIsEnum$5 = Object.prototype.propertyIsEnumerable;
var __knownSymbol$4 = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError$5 = (msg) => {
	throw TypeError(msg);
};
var __pow = Math.pow;
var __defNormalProp$5 = (obj, key, value) => key in obj ? __defProp$5(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues$5 = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp$5.call(b, prop)) __defNormalProp$5(a, prop, b[prop]);
	if (__getOwnPropSymbols$5) {
		for (var prop of __getOwnPropSymbols$5(b)) if (__propIsEnum$5.call(b, prop)) __defNormalProp$5(a, prop, b[prop]);
	}
	return a;
};
var __name$2 = (target, value) => __defProp$5(target, "name", {
	value,
	configurable: true
});
var __decoratorStart$4 = (base) => {
	var _a2;
	return [
		,
		,
		,
		__create$4((_a2 = base == null ? void 0 : base[__knownSymbol$4("metadata")]) != null ? _a2 : null)
	];
};
var __decoratorStrings$4 = [
	"class",
	"method",
	"getter",
	"setter",
	"accessor",
	"field",
	"value",
	"get",
	"set"
];
var __expectFn$4 = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError$5("Function expected") : fn;
var __decoratorContext$4 = (kind, name, done, metadata, fns) => ({
	kind: __decoratorStrings$4[kind],
	name,
	metadata,
	addInitializer: (fn) => done._ ? __typeError$5("Already initialized") : fns.push(__expectFn$4(fn || null))
});
var __decoratorMetadata$4 = (array, target) => __defNormalProp$5(target, __knownSymbol$4("metadata"), array[3]);
var __runInitializers$4 = (array, flags, self, value) => {
	for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
	return value;
};
var __decorateElement$4 = (array, flags, name, decorators, target, extra) => {
	var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
	var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings$4[k + 5];
	var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
	var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc$4(k < 4 ? target : {
		get [name]() {
			return __privateGet$5(this, extra);
		},
		set [name](x) {
			return __privateSet$5(this, extra, x);
		}
	}, name));
	k ? p && k < 4 && __name$2(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name$2(target, name);
	for (var i = decorators.length - 1; i >= 0; i--) {
		ctx = __decoratorContext$4(k, name, done = {}, array[3], extraInitializers);
		if (k) {
			ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn$2(target, x) : (x) => name in x };
			if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet$5 : __privateMethod$3)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
			if (k > 2) access.set = p ? (x, y) => __privateSet$5(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
		}
		it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : {
			get: desc.get,
			set: desc.set
		} : target, ctx), done._ = 1;
		if (k ^ 4 || it === void 0) __expectFn$4(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
		else if (typeof it !== "object" || it === null) __typeError$5("Object expected");
		else __expectFn$4(fn = it.get) && (desc.get = fn), __expectFn$4(fn = it.set) && (desc.set = fn), __expectFn$4(fn = it.init) && initializers.unshift(fn);
	}
	return k || __decoratorMetadata$4(array, target), desc && __defProp$5(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __accessCheck$5 = (obj, member, msg) => member.has(obj) || __typeError$5("Cannot " + msg);
var __privateIn$2 = (member, obj) => Object(obj) !== obj ? __typeError$5("Cannot use the \"in\" operator on this value") : member.has(obj);
var __privateGet$5 = (obj, member, getter) => (__accessCheck$5(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd$5 = (obj, member, value) => member.has(obj) ? __typeError$5("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet$5 = (obj, member, value, setter) => (__accessCheck$5(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod$3 = (obj, member, method) => (__accessCheck$5(obj, member, "access private method"), method);
var Point$1 = class _Point {
	/**
	* @param {number} Coordinate of the point on the horizontal axis
	* @param {number} Coordinate of the point on the vertical axis
	*/
	constructor(x, y) {
		this.x = x;
		this.y = y;
	}
	/**
	* Returns the delta between this point and another point.
	*
	* @param {Point} a - A point
	* @param {Point} b - Another point
	*/
	static delta(a, b) {
		return new _Point(a.x - b.x, a.y - b.y);
	}
	/**
	* Returns the distance (hypotenuse) between this point and another point.
	*
	* @param {Point} a - A point
	* @param {Point} b - Another point
	*/
	static distance(a, b) {
		return Math.hypot(a.x - b.x, a.y - b.y);
	}
	/**
	* Returns true if both points are equal.
	*
	* @param {Point} a - A point
	* @param {Point} b - Another point
	*/
	static equals(a, b) {
		return a.x === b.x && a.y === b.y;
	}
	static from({ x, y }) {
		return new _Point(x, y);
	}
};
var Rectangle = class _Rectangle {
	constructor(left, top, width, height) {
		this.left = left;
		this.top = top;
		this.width = width;
		this.height = height;
		this.scale = {
			x: 1,
			y: 1
		};
	}
	get inverseScale() {
		return {
			x: 1 / this.scale.x,
			y: 1 / this.scale.y
		};
	}
	translate(x, y) {
		const { top, left, width, height, scale } = this;
		const newShape = new _Rectangle(left + x, top + y, width, height);
		newShape.scale = __spreadValues$5({}, scale);
		return newShape;
	}
	get boundingRectangle() {
		const { width, height, left, top, right, bottom } = this;
		return {
			width,
			height,
			left,
			top,
			right,
			bottom
		};
	}
	get center() {
		const { left, top, right, bottom } = this;
		return new Point$1((left + right) / 2, (top + bottom) / 2);
	}
	get area() {
		const { width, height } = this;
		return width * height;
	}
	equals(shape) {
		if (!(shape instanceof _Rectangle)) return false;
		const { left, top, width, height } = this;
		return left === shape.left && top === shape.top && width === shape.width && height === shape.height;
	}
	containsPoint(point) {
		const { top, left, bottom, right } = this;
		return top <= point.y && point.y <= bottom && left <= point.x && point.x <= right;
	}
	intersectionArea(shape) {
		if (shape instanceof _Rectangle) return rectangleRectangleIntersection(this, shape);
		return 0;
	}
	intersectionRatio(shape) {
		const { area } = this;
		const intersectionArea = this.intersectionArea(shape);
		return intersectionArea / (shape.area + area - intersectionArea);
	}
	get bottom() {
		const { top, height } = this;
		return top + height;
	}
	get right() {
		const { left, width } = this;
		return left + width;
	}
	get aspectRatio() {
		const { width, height } = this;
		return width / height;
	}
	get corners() {
		return [
			{
				x: this.left,
				y: this.top
			},
			{
				x: this.right,
				y: this.top
			},
			{
				x: this.left,
				y: this.bottom
			},
			{
				x: this.right,
				y: this.bottom
			}
		];
	}
	static from({ top, left, width, height }) {
		return new _Rectangle(left, top, width, height);
	}
	static delta(a, b, alignment = {
		x: "center",
		y: "center"
	}) {
		const getCoordinate = (rect, axis) => {
			const align = alignment[axis];
			const start = axis === "x" ? rect.left : rect.top;
			const size = axis === "x" ? rect.width : rect.height;
			if (align == "start") return start;
			if (align == "end") return start + size;
			return start + size / 2;
		};
		return Point$1.delta({
			x: getCoordinate(a, "x"),
			y: getCoordinate(a, "y")
		}, {
			x: getCoordinate(b, "x"),
			y: getCoordinate(b, "y")
		});
	}
	static intersectionRatio(a, b) {
		return _Rectangle.from(a).intersectionRatio(_Rectangle.from(b));
	}
};
function rectangleRectangleIntersection(a, b) {
	const top = Math.max(b.top, a.top);
	const left = Math.max(b.left, a.left);
	const right = Math.min(b.left + b.width, a.left + a.width);
	const bottom = Math.min(b.top + b.height, a.top + a.height);
	const width = right - left;
	const height = bottom - top;
	if (left < right && top < bottom) return width * height;
	return 0;
}
var _direction_dec$1, _delta_dec$1, _a$2, _timestamp$1, _init$4;
var Position$1 = class extends (_a$2 = ValueHistory, _delta_dec$1 = [derived], _direction_dec$1 = [derived], _a$2) {
	constructor(initialValue) {
		const point = Point$1.from(initialValue);
		super(point, (a, b) => Point$1.equals(a, b));
		__runInitializers$4(_init$4, 5, this);
		__privateAdd$5(this, _timestamp$1, 0);
		this.velocity = {
			x: 0,
			y: 0
		};
	}
	get delta() {
		return Point$1.delta(this.current, this.initial);
	}
	get direction() {
		const { current, previous } = this;
		if (!previous) return null;
		const delta = {
			x: current.x - previous.x,
			y: current.y - previous.y
		};
		if (!delta.x && !delta.y) return null;
		if (Math.abs(delta.x) > Math.abs(delta.y)) return delta.x > 0 ? "right" : "left";
		return delta.y > 0 ? "down" : "up";
	}
	get current() {
		return super.current;
	}
	set current(coordinates) {
		const { current } = this;
		const point = Point$1.from(coordinates);
		const delta = {
			x: point.x - current.x,
			y: point.y - current.y
		};
		const timestamp = Date.now();
		const timeDelta = timestamp - __privateGet$5(this, _timestamp$1);
		const velocity = (delta2) => Math.round(delta2 / timeDelta * 100);
		n$1(() => {
			__privateSet$5(this, _timestamp$1, timestamp);
			this.velocity = {
				x: velocity(delta.x),
				y: velocity(delta.y)
			};
			super.current = point;
		});
	}
	reset(coordinates = this.defaultValue) {
		super.reset(Point$1.from(coordinates));
		this.velocity = {
			x: 0,
			y: 0
		};
	}
};
_init$4 = __decoratorStart$4(_a$2);
_timestamp$1 = /* @__PURE__ */ new WeakMap();
__decorateElement$4(_init$4, 2, "delta", _delta_dec$1, Position$1);
__decorateElement$4(_init$4, 2, "direction", _direction_dec$1, Position$1);
__decoratorMetadata$4(_init$4, Position$1);
function exceedsDistance({ x, y }, distance) {
	const dx = Math.abs(x);
	const dy = Math.abs(y);
	if (typeof distance === "number") return Math.sqrt(__pow(dx, 2) + __pow(dy, 2)) > distance;
	if ("x" in distance && "y" in distance) return dx > distance.x && dy > distance.y;
	if ("x" in distance) return dx > distance.x;
	if ("y" in distance) return dy > distance.y;
	return false;
}
var Axis$1 = /* @__PURE__ */ ((Axis2) => {
	Axis2["Horizontal"] = "x";
	Axis2["Vertical"] = "y";
	return Axis2;
})(Axis$1 || {});
var Axes = Object.values(Axis$1);
//#endregion
//#region node_modules/@dnd-kit/abstract/index.js
var __create$3 = Object.create;
var __defProp$4 = Object.defineProperty;
var __defProps$4 = Object.defineProperties;
var __getOwnPropDesc$3 = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs$4 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols$4 = Object.getOwnPropertySymbols;
var __hasOwnProp$4 = Object.prototype.hasOwnProperty;
var __propIsEnum$4 = Object.prototype.propertyIsEnumerable;
var __knownSymbol$3 = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError$4 = (msg) => {
	throw TypeError(msg);
};
var __defNormalProp$4 = (obj, key, value) => key in obj ? __defProp$4(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues$4 = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp$4.call(b, prop)) __defNormalProp$4(a, prop, b[prop]);
	if (__getOwnPropSymbols$4) {
		for (var prop of __getOwnPropSymbols$4(b)) if (__propIsEnum$4.call(b, prop)) __defNormalProp$4(a, prop, b[prop]);
	}
	return a;
};
var __spreadProps$4 = (a, b) => __defProps$4(a, __getOwnPropDescs$4(b));
var __name$1 = (target, value) => __defProp$4(target, "name", {
	value,
	configurable: true
});
var __objRest$3 = (source, exclude) => {
	var target = {};
	for (var prop in source) if (__hasOwnProp$4.call(source, prop) && exclude.indexOf(prop) < 0) target[prop] = source[prop];
	if (source != null && __getOwnPropSymbols$4) {
		for (var prop of __getOwnPropSymbols$4(source)) if (exclude.indexOf(prop) < 0 && __propIsEnum$4.call(source, prop)) target[prop] = source[prop];
	}
	return target;
};
var __decoratorStart$3 = (base) => {
	var _a;
	return [
		,
		,
		,
		__create$3((_a = base == null ? void 0 : base[__knownSymbol$3("metadata")]) != null ? _a : null)
	];
};
var __decoratorStrings$3 = [
	"class",
	"method",
	"getter",
	"setter",
	"accessor",
	"field",
	"value",
	"get",
	"set"
];
var __expectFn$3 = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError$4("Function expected") : fn;
var __decoratorContext$3 = (kind, name, done, metadata, fns) => ({
	kind: __decoratorStrings$3[kind],
	name,
	metadata,
	addInitializer: (fn) => done._ ? __typeError$4("Already initialized") : fns.push(__expectFn$3(fn || null))
});
var __decoratorMetadata$3 = (array, target) => __defNormalProp$4(target, __knownSymbol$3("metadata"), array[3]);
var __runInitializers$3 = (array, flags, self, value) => {
	for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
	return value;
};
var __decorateElement$3 = (array, flags, name, decorators, target, extra) => {
	var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
	var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings$3[k + 5];
	var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
	var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc$3(k < 4 ? target : {
		get [name]() {
			return __privateGet$4(this, extra);
		},
		set [name](x) {
			return __privateSet$4(this, extra, x);
		}
	}, name));
	k ? p && k < 4 && __name$1(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name$1(target, name);
	for (var i = decorators.length - 1; i >= 0; i--) {
		ctx = __decoratorContext$3(k, name, done = {}, array[3], extraInitializers);
		if (k) {
			ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn$1(target, x) : (x) => name in x };
			if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet$4 : __privateMethod$2)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
			if (k > 2) access.set = p ? (x, y) => __privateSet$4(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
		}
		it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : {
			get: desc.get,
			set: desc.set
		} : target, ctx), done._ = 1;
		if (k ^ 4 || it === void 0) __expectFn$3(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
		else if (typeof it !== "object" || it === null) __typeError$4("Object expected");
		else __expectFn$3(fn = it.get) && (desc.get = fn), __expectFn$3(fn = it.set) && (desc.set = fn), __expectFn$3(fn = it.init) && initializers.unshift(fn);
	}
	return k || __decoratorMetadata$3(array, target), desc && __defProp$4(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __accessCheck$4 = (obj, member, msg) => member.has(obj) || __typeError$4("Cannot " + msg);
var __privateIn$1 = (member, obj) => Object(obj) !== obj ? __typeError$4("Cannot use the \"in\" operator on this value") : member.has(obj);
var __privateGet$4 = (obj, member, getter) => (__accessCheck$4(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd$4 = (obj, member, value) => member.has(obj) ? __typeError$4("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet$4 = (obj, member, value, setter) => (__accessCheck$4(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod$2 = (obj, member, method) => (__accessCheck$4(obj, member, "access private method"), method);
function configure(plugin, options) {
	return {
		plugin,
		options
	};
}
function configurator(plugin) {
	return (options) => {
		return configure(plugin, options);
	};
}
function descriptor(plugin) {
	if (typeof plugin === "function") return {
		plugin,
		options: void 0
	};
	return plugin;
}
var _disabled_dec = [reactive], _init$3, _disabled, _cleanupFunctions$1;
var Plugin = class {
	/**
	* Creates a new plugin instance.
	*
	* @param manager - The drag and drop manager that owns this plugin
	* @param options - Optional configuration for the plugin
	*/
	constructor(manager, options) {
		this.manager = manager;
		this.options = options;
		__privateAdd$4(this, _disabled, __runInitializers$3(_init$3, 8, this, false)), __runInitializers$3(_init$3, 11, this);
		__privateAdd$4(this, _cleanupFunctions$1, /* @__PURE__ */ new Set());
	}
	/**
	* Enables a disabled plugin instance.
	*
	* @remarks
	* This method triggers effects when called.
	*/
	enable() {
		this.disabled = false;
	}
	/**
	* Disables an enabled plugin instance.
	*
	* @remarks
	* This method triggers effects when called.
	*/
	disable() {
		this.disabled = true;
	}
	/**
	* Checks if the plugin instance is disabled.
	*
	* @returns true if the plugin is disabled
	* @remarks
	* This method does not trigger effects when accessed.
	*/
	isDisabled() {
		return f(() => {
			return this.disabled;
		});
	}
	/**
	* Configures a plugin instance with new options.
	*
	* @param options - The new options to apply
	*/
	configure(options) {
		this.options = options;
	}
	/**
	* Registers an effect that will be cleaned up when the plugin is destroyed.
	*
	* @param callback - The effect callback to register
	* @returns A function to dispose of the effect
	*/
	registerEffect(callback) {
		const dispose = j(callback.bind(this));
		__privateGet$4(this, _cleanupFunctions$1).add(dispose);
		return dispose;
	}
	/**
	* Destroys a plugin instance and cleans up its resources.
	*
	* @remarks
	* This method:
	* - Calls all registered cleanup functions
	* - Should be overridden by subclasses to clean up additional resources
	*/
	destroy() {
		__privateGet$4(this, _cleanupFunctions$1).forEach((cleanup) => cleanup());
	}
	/**
	* Configures a plugin constructor with options.
	*
	* @param options - The options to configure the constructor with
	* @returns The configured plugin constructor
	*
	* @remarks
	* This method is used to configure the options that the
	* plugin constructor will use to create plugin instances.
	*/
	static configure(options) {
		return configure(this, options);
	}
};
_init$3 = __decoratorStart$3(null);
_disabled = /* @__PURE__ */ new WeakMap();
_cleanupFunctions$1 = /* @__PURE__ */ new WeakMap();
__decorateElement$3(_init$3, 4, "disabled", _disabled_dec, Plugin, _disabled);
__decoratorMetadata$3(_init$3, Plugin);
var CorePlugin = class extends Plugin {};
var _previousValues;
var PluginRegistry = class {
	/**
	* Creates a new plugin registry.
	*
	* @param manager - The drag and drop manager that owns this registry
	*/
	constructor(manager) {
		this.manager = manager;
		this.instances = /* @__PURE__ */ new Map();
		__privateAdd$4(this, _previousValues, []);
	}
	/**
	* Gets all registered plugin instances.
	*
	* @returns An array of all active plugin instances
	*/
	get values() {
		return Array.from(this.instances.values());
	}
	/**
	* Sets the list of plugins to be used by the registry.
	*
	* @param entries - Array of plugin constructors or descriptors
	* @remarks
	* This method:
	* - Filters out duplicate plugins
	* - Unregisters plugins that are no longer in use
	* - Registers new plugins with their options
	*/
	set values(entries) {
		const descriptors = entries.map(descriptor).reduceRight((acc, descriptor2) => {
			if (acc.some(({ plugin }) => plugin === descriptor2.plugin)) return acc;
			return [descriptor2, ...acc];
		}, []);
		const constructors = descriptors.map(({ plugin }) => plugin);
		for (const plugin of __privateGet$4(this, _previousValues)) if (!constructors.includes(plugin)) {
			if (plugin.prototype instanceof CorePlugin) continue;
			this.unregister(plugin);
		}
		for (const { plugin, options } of descriptors) this.register(plugin, options);
		__privateSet$4(this, _previousValues, constructors);
	}
	/**
	* Gets a plugin instance by its constructor.
	*
	* @param plugin - The plugin constructor to look up
	* @returns The plugin instance or undefined if not found
	*/
	get(plugin) {
		return this.instances.get(plugin);
	}
	/**
	* Registers a new plugin instance.
	*
	* @param plugin - The plugin constructor to register
	* @param options - Optional configuration for the plugin
	* @returns The registered plugin instance
	* @remarks
	* If the plugin is already registered, its options will be updated
	* and the existing instance will be returned.
	*/
	register(plugin, options) {
		const existingInstance = this.instances.get(plugin);
		if (existingInstance) {
			if (existingInstance.options !== options) existingInstance.options = options;
			return existingInstance;
		}
		const instance = new plugin(this.manager, options);
		this.instances.set(plugin, instance);
		return instance;
	}
	/**
	* Unregisters a plugin instance.
	*
	* @param plugin - The plugin constructor to unregister
	* @remarks
	* This method:
	* - Destroys the plugin instance
	* - Removes it from the registry
	*/
	unregister(plugin) {
		const instance = this.instances.get(plugin);
		if (instance) {
			instance.destroy();
			this.instances.delete(plugin);
		}
	}
	/**
	* Destroys all registered plugin instances.
	*
	* @remarks
	* This method:
	* - Calls destroy() on all plugin instances
	* - Clears the registry
	*/
	destroy() {
		for (const plugin of this.instances.values()) plugin.destroy();
		this.instances.clear();
	}
};
_previousValues = /* @__PURE__ */ new WeakMap();
function sortCollisions(a, b) {
	if (a.priority === b.priority) {
		if (a.type === b.type) return b.value - a.value;
		return b.type - a.type;
	}
	return b.priority - a.priority;
}
var DEFAULT_VALUE = [];
var _previousCoordinates, _collisions;
var CollisionObserver = class extends Plugin {
	/**
	* Creates a new CollisionObserver instance.
	*
	* @param manager - The drag drop manager instance
	*/
	constructor(manager) {
		super(manager);
		__privateAdd$4(this, _previousCoordinates);
		__privateAdd$4(this, _collisions);
		this.computeCollisions = this.computeCollisions.bind(this);
		__privateSet$4(this, _collisions, y(DEFAULT_VALUE));
		this.destroy = effects(() => {
			const collisions = this.computeCollisions();
			const coordinates = f(() => this.manager.dragOperation.position.current);
			if (collisions !== DEFAULT_VALUE) {
				const previousCoordinates = __privateGet$4(this, _previousCoordinates);
				__privateSet$4(this, _previousCoordinates, coordinates);
				if (previousCoordinates && coordinates.x == previousCoordinates.x && coordinates.y == previousCoordinates.y) return;
			} else __privateSet$4(this, _previousCoordinates, void 0);
			__privateGet$4(this, _collisions).value = collisions;
		}, () => {
			const { dragOperation } = this.manager;
			if (dragOperation.status.initialized) this.forceUpdate();
		});
	}
	/**
	* Forces an immediate update of collision detection.
	*
	* @param immediate - If true, updates collisions immediately. If false, resets previous coordinates.
	*/
	forceUpdate(immediate = true) {
		f(() => {
			if (immediate) __privateGet$4(this, _collisions).value = this.computeCollisions();
			else __privateSet$4(this, _previousCoordinates, void 0);
		});
	}
	/**
	* Computes collisions between draggable and droppable elements.
	*
	* @param entries - Optional array of droppable elements to check. If not provided, uses all registered droppables.
	* @param collisionDetector - Optional custom collision detector function
	* @returns Array of detected collisions, sorted by priority
	*/
	computeCollisions(entries, collisionDetector) {
		const { registry, dragOperation } = this.manager;
		const { source, shape, status } = dragOperation;
		if (!status.initialized || !shape) return DEFAULT_VALUE;
		const collisions = [];
		const potentialTargets = [];
		for (const entry of entries != null ? entries : registry.droppables) {
			if (entry.disabled) continue;
			if (source && !entry.accepts(source)) continue;
			const detectCollision = collisionDetector != null ? collisionDetector : entry.collisionDetector;
			if (!detectCollision) continue;
			potentialTargets.push(entry);
			entry.shape;
			const collision = f(() => detectCollision({
				droppable: entry,
				dragOperation
			}));
			if (collision) {
				if (entry.collisionPriority != null) collision.priority = entry.collisionPriority;
				collisions.push(collision);
			}
		}
		if (potentialTargets.length === 0) return DEFAULT_VALUE;
		collisions.sort(sortCollisions);
		return collisions;
	}
	/**
	* Gets the current collisions signal value.
	*/
	get collisions() {
		return __privateGet$4(this, _collisions).value;
	}
};
_previousCoordinates = /* @__PURE__ */ new WeakMap();
_collisions = /* @__PURE__ */ new WeakMap();
var Monitor = class {
	constructor() {
		this.registry = /* @__PURE__ */ new Map();
	}
	/**
	* Adds an event listener for the specified event type.
	*
	* @param name - The name of the event to listen for
	* @param handler - The function to call when the event occurs
	* @returns A function to remove the event listener
	*/
	addEventListener(name, handler) {
		const { registry } = this;
		const listeners = new Set(registry.get(name));
		listeners.add(handler);
		registry.set(name, listeners);
		return () => this.removeEventListener(name, handler);
	}
	/**
	* Removes an event listener for the specified event type.
	*
	* @param name - The name of the event
	* @param handler - The function to remove
	*/
	removeEventListener(name, handler) {
		const { registry } = this;
		const listeners = new Set(registry.get(name));
		listeners.delete(handler);
		registry.set(name, listeners);
	}
	/**
	* Dispatches an event to all registered listeners.
	*
	* @param name - The name of the event to dispatch
	* @param args - Arguments to pass to the event handlers
	*/
	dispatch(name, ...args) {
		const { registry } = this;
		const listeners = registry.get(name);
		if (!listeners) return;
		for (const listener of listeners) listener(...args);
	}
};
var DragDropMonitor = class extends Monitor {
	/**
	* Creates a new drag and drop monitor.
	*
	* @param manager - The drag and drop manager to monitor
	*/
	constructor(manager) {
		super();
		this.manager = manager;
	}
	/**
	* Dispatches a drag and drop event.
	*
	* @param type - The type of event to dispatch
	* @param event - The event data to dispatch
	*/
	dispatch(type, event) {
		const args = [event, this.manager];
		super.dispatch(type, ...args);
	}
};
function defaultPreventable(event, cancelable = true) {
	let defaultPrevented = false;
	return __spreadProps$4(__spreadValues$4({}, event), {
		cancelable,
		get defaultPrevented() {
			return defaultPrevented;
		},
		preventDefault() {
			if (!cancelable) return;
			defaultPrevented = true;
		}
	});
}
var CollisionNotifier = class extends CorePlugin {
	constructor(manager) {
		super(manager);
		const isEqual = (a, b) => a.map(({ id }) => id).join("") === b.map(({ id }) => id).join("");
		let previousCollisions = [];
		this.destroy = effects(() => {
			const { dragOperation, collisionObserver } = manager;
			if (dragOperation.status.initializing) {
				previousCollisions = [];
				collisionObserver.enable();
			}
		}, () => {
			const { collisionObserver, monitor } = manager;
			const { collisions } = collisionObserver;
			if (collisionObserver.isDisabled()) return;
			const event = defaultPreventable({ collisions });
			monitor.dispatch("collision", event);
			if (event.defaultPrevented) return;
			if (isEqual(collisions, previousCollisions)) return;
			else previousCollisions = collisions;
			const [firstCollision] = collisions;
			f(() => {
				var _a;
				if ((firstCollision == null ? void 0 : firstCollision.id) !== ((_a = manager.dragOperation.target) == null ? void 0 : _a.id)) {
					collisionObserver.disable();
					manager.actions.setDropTarget(firstCollision == null ? void 0 : firstCollision.id).then(() => {
						collisionObserver.enable();
					});
				}
			});
		});
	}
};
var CollisionPriority = /* @__PURE__ */ ((CollisionPriority2) => {
	CollisionPriority2[CollisionPriority2["Lowest"] = 0] = "Lowest";
	CollisionPriority2[CollisionPriority2["Low"] = 1] = "Low";
	CollisionPriority2[CollisionPriority2["Normal"] = 2] = "Normal";
	CollisionPriority2[CollisionPriority2["High"] = 3] = "High";
	CollisionPriority2[CollisionPriority2["Highest"] = 4] = "Highest";
	return CollisionPriority2;
})(CollisionPriority || {});
var CollisionType = /* @__PURE__ */ ((CollisionType2) => {
	CollisionType2[CollisionType2["Collision"] = 0] = "Collision";
	CollisionType2[CollisionType2["ShapeIntersection"] = 1] = "ShapeIntersection";
	CollisionType2[CollisionType2["PointerIntersection"] = 2] = "PointerIntersection";
	return CollisionType2;
})(CollisionType || {});
var _dropped_dec, _dragging_dec, _initialized_dec, _initializing_dec, _idle_dec, _current_dec, _value_dec = [reactive], _init2$1, _value;
_current_dec = [derived], _idle_dec = [derived], _initializing_dec = [derived], _initialized_dec = [derived], _dragging_dec = [derived], _dropped_dec = [derived];
var Status = class {
	constructor() {
		__runInitializers$3(_init2$1, 5, this);
		__privateAdd$4(this, _value, __runInitializers$3(_init2$1, 8, this, "idle")), __runInitializers$3(_init2$1, 11, this);
	}
	get current() {
		return this.value;
	}
	get idle() {
		return this.value === "idle";
	}
	get initializing() {
		return this.value === "initializing";
	}
	get initialized() {
		const { value } = this;
		return value !== "idle" && value !== "initialization-pending";
	}
	get dragging() {
		return this.value === "dragging";
	}
	get dropped() {
		return this.value === "dropped";
	}
	/**
	* Sets the current status value.
	*
	* @param value - The new status value
	*/
	set(value) {
		this.value = value;
	}
};
_init2$1 = __decoratorStart$3(null);
_value = /* @__PURE__ */ new WeakMap();
__decorateElement$3(_init2$1, 4, "value", _value_dec, Status, _value);
__decorateElement$3(_init2$1, 2, "current", _current_dec, Status);
__decorateElement$3(_init2$1, 2, "idle", _idle_dec, Status);
__decorateElement$3(_init2$1, 2, "initializing", _initializing_dec, Status);
__decorateElement$3(_init2$1, 2, "initialized", _initialized_dec, Status);
__decorateElement$3(_init2$1, 2, "dragging", _dragging_dec, Status);
__decorateElement$3(_init2$1, 2, "dropped", _dropped_dec, Status);
__decoratorMetadata$3(_init2$1, Status);
var DragActions = class {
	/**
	* Creates a new instance of drag actions.
	*
	* @param manager - The drag and drop manager instance
	*/
	constructor(manager) {
		this.manager = manager;
	}
	/**
	* Sets the source of the drag operation.
	*
	* @param source - The draggable entity or its unique identifier
	*/
	setDragSource(source) {
		const { dragOperation } = this.manager;
		dragOperation.sourceIdentifier = typeof source === "string" || typeof source === "number" ? source : source.id;
	}
	/**
	* Sets the target of the drop operation.
	*
	* @param identifier - The unique identifier of the droppable entity or null/undefined
	* @returns A promise that resolves to true if the drop was prevented
	*/
	setDropTarget(identifier) {
		return f(() => {
			const { dragOperation } = this.manager;
			const id = identifier != null ? identifier : null;
			if (dragOperation.targetIdentifier === id) return Promise.resolve(false);
			dragOperation.targetIdentifier = id;
			const event = defaultPreventable({ operation: dragOperation.snapshot() });
			if (dragOperation.status.dragging) this.manager.monitor.dispatch("dragover", event);
			return this.manager.renderer.rendering.then(() => event.defaultPrevented);
		});
	}
	/**
	* Starts a new drag operation.
	*
	* @param args - Configuration for the drag operation
	* @param args.event - The event that initiated the drag
	* @param args.source - The source draggable entity or its identifier
	* @param args.coordinates - The initial coordinates of the drag
	* @returns true if the drag operation started successfully
	* @throws {Error} If there is no drag source or another operation is active
	*/
	start(args) {
		return f(() => {
			const { dragOperation } = this.manager;
			if (args.source != null) this.setDragSource(args.source);
			if (!dragOperation.source) throw new Error("Cannot start a drag operation without a drag source");
			if (!dragOperation.status.idle) throw new Error("Cannot start a drag operation while another is active");
			const controller = new AbortController();
			const { event: nativeEvent, coordinates } = args;
			n$1(() => {
				dragOperation.status.set("initialization-pending");
				dragOperation.shape = null;
				dragOperation.canceled = false;
				dragOperation.activatorEvent = nativeEvent != null ? nativeEvent : null;
				dragOperation.position.reset(coordinates);
			});
			const beforeStartEvent = defaultPreventable({ operation: dragOperation.snapshot() });
			this.manager.monitor.dispatch("beforedragstart", beforeStartEvent);
			if (beforeStartEvent.defaultPrevented) {
				dragOperation.reset();
				controller.abort();
				return controller;
			}
			dragOperation.status.set("initializing");
			dragOperation.controller = controller;
			this.manager.renderer.rendering.then(() => {
				if (controller.signal.aborted) return;
				const { status } = dragOperation;
				if (status.current !== "initializing") return;
				dragOperation.status.set("dragging");
				this.manager.monitor.dispatch("dragstart", {
					nativeEvent,
					operation: dragOperation.snapshot(),
					cancelable: false
				});
			});
			return controller;
		});
	}
	/**
	* Moves the dragged entity to a new position.
	*
	* @param args - Configuration for the move operation
	* @param args.by - Relative coordinates to move by
	* @param args.to - Absolute coordinates to move to
	* @param args.event - The event that triggered the move
	* @param args.cancelable - Whether the move can be canceled
	* @param args.propagate - Whether to dispatch dragmove events
	*/
	move(args) {
		return f(() => {
			var _a, _b;
			const { dragOperation } = this.manager;
			const { status, controller } = dragOperation;
			if (!status.dragging || !controller || controller.signal.aborted) return;
			const event = defaultPreventable({
				nativeEvent: args.event,
				operation: dragOperation.snapshot(),
				by: args.by,
				to: args.to
			}, (_a = args.cancelable) != null ? _a : true);
			if ((_b = args.propagate) != null ? _b : true) this.manager.monitor.dispatch("dragmove", event);
			queueMicrotask(() => {
				var _a2, _b2, _c3, _d, _e;
				if (event.defaultPrevented) return;
				const coordinates = (_e = args.to) != null ? _e : {
					x: dragOperation.position.current.x + ((_b2 = (_a2 = args.by) == null ? void 0 : _a2.x) != null ? _b2 : 0),
					y: dragOperation.position.current.y + ((_d = (_c3 = args.by) == null ? void 0 : _c3.y) != null ? _d : 0)
				};
				dragOperation.position.current = coordinates;
			});
		});
	}
	/**
	* Stops the current drag operation.
	*
	* @param args - Configuration for stopping the operation
	* @param args.event - The event that triggered the stop
	* @param args.canceled - Whether the operation was canceled
	* @remarks
	* This method:
	* - Dispatches a dragend event
	* - Allows suspension of the operation
	* - Handles cleanup of the operation state
	*/
	stop(args = {}) {
		return f(() => {
			var _a, _b;
			const { dragOperation } = this.manager;
			const { controller } = dragOperation;
			if (!controller || controller.signal.aborted) return;
			let promise;
			const suspend = () => {
				const output = {
					resume: () => {},
					abort: () => {}
				};
				promise = new Promise((resolve, reject) => {
					output.resume = resolve;
					output.abort = reject;
				});
				return output;
			};
			controller.abort();
			const end = () => {
				this.manager.renderer.rendering.then(() => {
					dragOperation.status.set("dropped");
					const dropping = f(() => {
						var _a2;
						return ((_a2 = dragOperation.source) == null ? void 0 : _a2.status) === "dropping";
					});
					const cleanup = () => {
						if (dragOperation.controller === controller) dragOperation.controller = void 0;
						dragOperation.reset();
					};
					if (dropping) {
						const { source } = dragOperation;
						const dispose = j(() => {
							if ((source == null ? void 0 : source.status) === "idle") {
								dispose();
								cleanup();
							}
						});
					} else this.manager.renderer.rendering.then(cleanup);
				});
			};
			dragOperation.canceled = (_a = args.canceled) != null ? _a : false;
			this.manager.monitor.dispatch("dragend", {
				nativeEvent: args.event,
				operation: dragOperation.snapshot(),
				canceled: (_b = args.canceled) != null ? _b : false,
				suspend
			});
			if (promise) promise.then(end).catch(() => dragOperation.reset());
			else end();
		});
	}
};
var _disabled_dec2, _data_dec, _id_dec, _manager_dec = [reactive], _init3$1, _manager, _id, _data, _disabled2;
_id_dec = [reactive], _data_dec = [reactive], _disabled_dec2 = [reactive];
var Entity = class {
	/**
	* Creates a new instance of the `Entity` class.
	*
	* @param input - An object containing the initial properties of the entity.
	* @param manager - The manager that controls the drag and drop operations.
	*/
	constructor(input, manager) {
		__privateAdd$4(this, _manager, __runInitializers$3(_init3$1, 8, this)), __runInitializers$3(_init3$1, 11, this);
		__privateAdd$4(this, _id, __runInitializers$3(_init3$1, 12, this)), __runInitializers$3(_init3$1, 15, this);
		__privateAdd$4(this, _data, __runInitializers$3(_init3$1, 16, this)), __runInitializers$3(_init3$1, 19, this);
		__privateAdd$4(this, _disabled2, __runInitializers$3(_init3$1, 20, this)), __runInitializers$3(_init3$1, 23, this);
		const { effects: effects6, id, data = {}, disabled = false, register = true } = input;
		let previousId = id;
		this.manager = manager;
		this.id = id;
		this.data = data;
		this.disabled = disabled;
		this.effects = () => {
			var _a;
			return [() => {
				const { id: id2, manager: manager2 } = this;
				if (id2 === previousId) return;
				manager2?.registry.register(this);
				return () => manager2 == null ? void 0 : manager2.registry.unregister(this);
			}, ...(_a = effects6 == null ? void 0 : effects6()) != null ? _a : []];
		};
		this.register = this.register.bind(this);
		this.unregister = this.unregister.bind(this);
		this.destroy = this.destroy.bind(this);
		if (manager && register) queueMicrotask(this.register);
	}
	/**
	* A method that registers the entity with the manager.
	* @returns CleanupFunction | void
	*/
	register() {
		var _a;
		return (_a = this.manager) == null ? void 0 : _a.registry.register(this);
	}
	/**
	* A method that unregisters the entity from the manager.
	* @returns void
	*/
	unregister() {
		var _a;
		(_a = this.manager) == null || _a.registry.unregister(this);
	}
	/**
	* A method that cleans up the entity when it is no longer needed.
	* @returns void
	*/
	destroy() {
		var _a;
		(_a = this.manager) == null || _a.registry.unregister(this);
	}
};
_init3$1 = __decoratorStart$3(null);
_manager = /* @__PURE__ */ new WeakMap();
_id = /* @__PURE__ */ new WeakMap();
_data = /* @__PURE__ */ new WeakMap();
_disabled2 = /* @__PURE__ */ new WeakMap();
__decorateElement$3(_init3$1, 4, "manager", _manager_dec, Entity, _manager);
__decorateElement$3(_init3$1, 4, "id", _id_dec, Entity, _id);
__decorateElement$3(_init3$1, 4, "data", _data_dec, Entity, _data);
__decorateElement$3(_init3$1, 4, "disabled", _disabled_dec2, Entity, _disabled2);
__decoratorMetadata$3(_init3$1, Entity);
var EntityRegistry = class {
	constructor() {
		this.map = y(/* @__PURE__ */ new Map());
		this.cleanupFunctions = /* @__PURE__ */ new WeakMap();
		/**
		* Registers a entity in the registry.
		* @param key - The unique identifier of the entity.
		* @param value - The entity to register.
		* @returns A function that unregisters the entity.
		*/
		this.register = (key, value) => {
			const current = this.map.peek();
			const currentValue = current.get(key);
			const unregister = () => this.unregister(key, value);
			if (currentValue === value) return unregister;
			if (currentValue) {
				this.cleanupFunctions.get(currentValue)?.();
				this.cleanupFunctions.delete(currentValue);
			}
			const updatedMap = new Map(current);
			updatedMap.set(key, value);
			this.map.value = updatedMap;
			const cleanup = effects(...value.effects());
			this.cleanupFunctions.set(value, cleanup);
			return unregister;
		};
		/**
		* Unregisters an entity from the registry.
		* @param key - The unique identifier of the entity.
		* @param value - The entity instance to unregister.
		*/
		this.unregister = (key, value) => {
			const current = this.map.peek();
			if (current.get(key) !== value) return;
			this.cleanupFunctions.get(value)?.();
			this.cleanupFunctions.delete(value);
			const updatedMap = new Map(current);
			updatedMap.delete(key);
			this.map.value = updatedMap;
		};
	}
	/**
	* Iterator for the EntityRegistry class.
	* @returns An iterator for the values in the map.
	*/
	[Symbol.iterator]() {
		return this.map.peek().values();
	}
	get value() {
		return this.map.value.values();
	}
	/**
	* Checks if a entity with the given identifier exists in the registry.
	* @param identifier - The unique identifier of the entity.
	* @returns True if the entity exists, false otherwise.
	*/
	has(identifier) {
		return this.map.value.has(identifier);
	}
	/**
	* Retrieves a entity from the registry using its identifier.
	* @param identifier - The unique identifier of the entity.
	* @returns The entity if it exists, undefined otherwise.
	*/
	get(identifier) {
		return this.map.value.get(identifier);
	}
	/**
	* Destroys all entries in the registry and clears the registry.
	*/
	destroy() {
		for (const entry of this) {
			this.cleanupFunctions.get(entry)?.();
			entry.destroy();
		}
		this.map.value = /* @__PURE__ */ new Map();
	}
};
var _isDragSource_dec, _isDragging_dec, _isDropping_dec, _status_dec, _modifiers_dec, _type_dec, _c$1, _init4$1, _type, _modifiers, _status;
var Draggable$1 = class extends (_c$1 = Entity, _type_dec = [reactive], _modifiers_dec = [reactive], _status_dec = [reactive], _isDropping_dec = [derived], _isDragging_dec = [derived], _isDragSource_dec = [derived], _c$1) {
	constructor(_a, manager) {
		var _b = _a, { modifiers, type, sensors } = _b, input = __objRest$3(_b, [
			"modifiers",
			"type",
			"sensors"
		]);
		super(input, manager);
		__runInitializers$3(_init4$1, 5, this);
		__privateAdd$4(this, _type, __runInitializers$3(_init4$1, 8, this)), __runInitializers$3(_init4$1, 11, this);
		__privateAdd$4(this, _modifiers, __runInitializers$3(_init4$1, 12, this)), __runInitializers$3(_init4$1, 15, this);
		__privateAdd$4(this, _status, __runInitializers$3(_init4$1, 16, this, this.isDragSource ? "dragging" : "idle")), __runInitializers$3(_init4$1, 19, this);
		this.type = type;
		this.sensors = sensors;
		this.modifiers = modifiers;
		this.alignment = input.alignment;
	}
	get isDropping() {
		return this.status === "dropping" && this.isDragSource;
	}
	get isDragging() {
		return this.status === "dragging" && this.isDragSource;
	}
	get isDragSource() {
		var _a, _b;
		return ((_b = (_a = this.manager) == null ? void 0 : _a.dragOperation.source) == null ? void 0 : _b.id) === this.id;
	}
};
_init4$1 = __decoratorStart$3(_c$1);
_type = /* @__PURE__ */ new WeakMap();
_modifiers = /* @__PURE__ */ new WeakMap();
_status = /* @__PURE__ */ new WeakMap();
__decorateElement$3(_init4$1, 4, "type", _type_dec, Draggable$1, _type);
__decorateElement$3(_init4$1, 4, "modifiers", _modifiers_dec, Draggable$1, _modifiers);
__decorateElement$3(_init4$1, 4, "status", _status_dec, Draggable$1, _status);
__decorateElement$3(_init4$1, 2, "isDropping", _isDropping_dec, Draggable$1);
__decorateElement$3(_init4$1, 2, "isDragging", _isDragging_dec, Draggable$1);
__decorateElement$3(_init4$1, 2, "isDragSource", _isDragSource_dec, Draggable$1);
__decoratorMetadata$3(_init4$1, Draggable$1);
var _isDropTarget_dec, _shape_dec, _collisionPriority_dec, _collisionDetector_dec, _type_dec2, _accept_dec, _c2$1, _init5$1, _accept, _type2, _collisionDetector, _collisionPriority, _shape;
var Droppable$1 = class extends (_c2$1 = Entity, _accept_dec = [reactive], _type_dec2 = [reactive], _collisionDetector_dec = [reactive], _collisionPriority_dec = [reactive], _shape_dec = [reactive], _isDropTarget_dec = [derived], _c2$1) {
	constructor(_a, manager) {
		var _b = _a, { accept, collisionDetector, collisionPriority, type } = _b, input = __objRest$3(_b, [
			"accept",
			"collisionDetector",
			"collisionPriority",
			"type"
		]);
		super(input, manager);
		__runInitializers$3(_init5$1, 5, this);
		__privateAdd$4(this, _accept, __runInitializers$3(_init5$1, 8, this)), __runInitializers$3(_init5$1, 11, this);
		__privateAdd$4(this, _type2, __runInitializers$3(_init5$1, 12, this)), __runInitializers$3(_init5$1, 15, this);
		__privateAdd$4(this, _collisionDetector, __runInitializers$3(_init5$1, 16, this)), __runInitializers$3(_init5$1, 19, this);
		__privateAdd$4(this, _collisionPriority, __runInitializers$3(_init5$1, 20, this)), __runInitializers$3(_init5$1, 23, this);
		__privateAdd$4(this, _shape, __runInitializers$3(_init5$1, 24, this)), __runInitializers$3(_init5$1, 27, this);
		this.accept = accept;
		this.collisionDetector = collisionDetector;
		this.collisionPriority = collisionPriority;
		this.type = type;
	}
	/**
	* Checks whether or not the droppable accepts a given draggable.
	*
	* @param draggable - The draggable to check
	* @returns true if the draggable can be dropped here
	*/
	accepts(draggable) {
		const { accept } = this;
		if (!accept) return true;
		if (typeof accept === "function") return accept(draggable);
		if (!draggable.type) return false;
		if (Array.isArray(accept)) return accept.includes(draggable.type);
		return draggable.type === accept;
	}
	get isDropTarget() {
		var _a, _b;
		return ((_b = (_a = this.manager) == null ? void 0 : _a.dragOperation.target) == null ? void 0 : _b.id) === this.id;
	}
};
_init5$1 = __decoratorStart$3(_c2$1);
_accept = /* @__PURE__ */ new WeakMap();
_type2 = /* @__PURE__ */ new WeakMap();
_collisionDetector = /* @__PURE__ */ new WeakMap();
_collisionPriority = /* @__PURE__ */ new WeakMap();
_shape = /* @__PURE__ */ new WeakMap();
__decorateElement$3(_init5$1, 4, "accept", _accept_dec, Droppable$1, _accept);
__decorateElement$3(_init5$1, 4, "type", _type_dec2, Droppable$1, _type2);
__decorateElement$3(_init5$1, 4, "collisionDetector", _collisionDetector_dec, Droppable$1, _collisionDetector);
__decorateElement$3(_init5$1, 4, "collisionPriority", _collisionPriority_dec, Droppable$1, _collisionPriority);
__decorateElement$3(_init5$1, 4, "shape", _shape_dec, Droppable$1, _shape);
__decorateElement$3(_init5$1, 2, "isDropTarget", _isDropTarget_dec, Droppable$1);
__decoratorMetadata$3(_init5$1, Droppable$1);
var Sensor = class extends Plugin {
	/**
	* Creates a new sensor instance.
	*
	* @param manager - The drag drop manager instance
	* @param options - Optional sensor configuration
	*/
	constructor(manager, options) {
		super(manager, options);
		this.manager = manager;
		this.options = options;
	}
};
var Modifier = class extends Plugin {
	/**
	* Creates a new modifier instance.
	*
	* @param manager - The drag and drop manager that owns this modifier
	* @param options - Optional configuration for the modifier
	*/
	constructor(manager, options) {
		super(manager, options);
		this.manager = manager;
		this.options = options;
	}
	/**
	* Applies the modifier to the current drag operation.
	*
	* @param operation - The current state of the drag operation
	* @returns The transformed coordinates
	*
	* @remarks
	* Override this method to implement custom transformation logic.
	* The default implementation returns the original transform unchanged.
	*/
	apply(operation) {
		return operation.transform;
	}
};
var DragDropRegistry = class {
	/**
	* Creates a new registry instance.
	*
	* @param manager - The drag and drop manager that owns this registry
	*/
	constructor(manager) {
		/** Registry for draggable entities */
		this.draggables = new EntityRegistry();
		/** Registry for droppable entities */
		this.droppables = new EntityRegistry();
		this.plugins = new PluginRegistry(manager);
		this.sensors = new PluginRegistry(manager);
		this.modifiers = new PluginRegistry(manager);
	}
	register(input, options) {
		if (input instanceof Draggable$1) return this.draggables.register(input.id, input);
		if (input instanceof Droppable$1) return this.droppables.register(input.id, input);
		if (input.prototype instanceof Modifier) return this.modifiers.register(input, options);
		if (input.prototype instanceof Sensor) return this.sensors.register(input, options);
		if (input.prototype instanceof Plugin) return this.plugins.register(input, options);
		throw new Error("Invalid instance type");
	}
	unregister(input) {
		if (input instanceof Entity) {
			if (input instanceof Draggable$1) return this.draggables.unregister(input.id, input);
			if (input instanceof Droppable$1) return this.droppables.unregister(input.id, input);
			return () => {};
		}
		if (input.prototype instanceof Modifier) return this.modifiers.unregister(input);
		if (input.prototype instanceof Sensor) return this.sensors.unregister(input);
		if (input.prototype instanceof Plugin) return this.plugins.unregister(input);
		throw new Error("Invalid instance type");
	}
	/**
	* Destroys all registered entities and cleans up resources.
	*
	* @remarks
	* This method:
	* - Destroys all draggable and droppable entities
	* - Destroys all plugins, sensors, and modifiers
	* - Cleans up any associated resources
	*/
	destroy() {
		this.draggables.destroy();
		this.droppables.destroy();
		this.plugins.destroy();
		this.sensors.destroy();
		this.modifiers.destroy();
	}
};
var _transform_dec, _target_dec, _source_dec, _modifiers_dec2, _targetIdentifier_dec, _sourceIdentifier_dec, _activatorEvent_dec, _canceled_dec, _shape_dec2 = [derived], _manager2, _previousSource, _shape2, _init6, _canceled, _activatorEvent, _sourceIdentifier, _targetIdentifier, _modifiers2, _transform;
_canceled_dec = [reactive], _activatorEvent_dec = [reactive], _sourceIdentifier_dec = [reactive], _targetIdentifier_dec = [reactive], _modifiers_dec2 = [reactive], _source_dec = [derived], _target_dec = [derived], _transform_dec = [derived];
var DragOperation = class {
	/**
	* Creates a new drag operation instance.
	*
	* @param manager - The drag and drop manager that owns this operation
	*/
	constructor(manager) {
		__runInitializers$3(_init6, 5, this);
		__privateAdd$4(this, _manager2);
		__privateAdd$4(this, _previousSource);
		__privateAdd$4(this, _shape2, new ValueHistory(void 0, (a, b) => a && b ? a.equals(b) : a === b));
		/** Current status of the drag operation */
		this.status = new Status();
		__privateAdd$4(this, _canceled, __runInitializers$3(_init6, 8, this, false)), __runInitializers$3(_init6, 11, this);
		__privateAdd$4(this, _activatorEvent, __runInitializers$3(_init6, 12, this, null)), __runInitializers$3(_init6, 15, this);
		__privateAdd$4(this, _sourceIdentifier, __runInitializers$3(_init6, 16, this, null)), __runInitializers$3(_init6, 19, this);
		__privateAdd$4(this, _targetIdentifier, __runInitializers$3(_init6, 20, this, null)), __runInitializers$3(_init6, 23, this);
		__privateAdd$4(this, _modifiers2, __runInitializers$3(_init6, 24, this, [])), __runInitializers$3(_init6, 27, this);
		/** Current position of the dragged entity */
		this.position = new Position$1({
			x: 0,
			y: 0
		});
		__privateAdd$4(this, _transform, {
			x: 0,
			y: 0
		});
		__privateSet$4(this, _manager2, manager);
	}
	get shape() {
		const { current, initial, previous } = __privateGet$4(this, _shape2);
		if (!current || !initial) return null;
		return {
			current,
			initial,
			previous
		};
	}
	/**
	* Sets the shape of the dragged entity.
	*
	* @param value - The new shape or null to reset
	*/
	set shape(value) {
		if (!value) __privateGet$4(this, _shape2).reset();
		else __privateGet$4(this, _shape2).current = value;
	}
	get source() {
		var _a;
		const identifier = this.sourceIdentifier;
		if (identifier == null) return null;
		const value = __privateGet$4(this, _manager2).registry.draggables.get(identifier);
		if (value) __privateSet$4(this, _previousSource, value);
		return (_a = value != null ? value : __privateGet$4(this, _previousSource)) != null ? _a : null;
	}
	get target() {
		var _a;
		const identifier = this.targetIdentifier;
		return identifier != null ? (_a = __privateGet$4(this, _manager2).registry.droppables.get(identifier)) != null ? _a : null : null;
	}
	get transform() {
		const { x, y } = this.position.delta;
		let transform = {
			x,
			y
		};
		for (const modifier of this.modifiers) transform = modifier.apply(__spreadProps$4(__spreadValues$4({}, this.snapshot()), { transform }));
		__privateSet$4(this, _transform, transform);
		return transform;
	}
	/**
	* Creates a snapshot of the current drag operation state.
	*
	* @returns An immutable snapshot of the current operation state
	*/
	snapshot() {
		return f(() => ({
			source: this.source,
			target: this.target,
			activatorEvent: this.activatorEvent,
			transform: __privateGet$4(this, _transform),
			shape: this.shape ? snapshot(this.shape) : null,
			position: snapshot(this.position),
			status: snapshot(this.status),
			canceled: this.canceled
		}));
	}
	/**
	* Resets the drag operation to its initial state.
	*
	* @remarks
	* This method:
	* - Sets status to idle
	* - Clears source and target identifiers
	* - Resets shape history
	* - Resets position and transform
	* - Clears modifiers
	*/
	reset() {
		n$1(() => {
			this.status.set("idle");
			this.sourceIdentifier = null;
			this.targetIdentifier = null;
			__privateGet$4(this, _shape2).reset();
			this.position.reset({
				x: 0,
				y: 0
			});
			__privateSet$4(this, _transform, {
				x: 0,
				y: 0
			});
			this.modifiers = [];
		});
	}
};
_init6 = __decoratorStart$3(null);
_manager2 = /* @__PURE__ */ new WeakMap();
_previousSource = /* @__PURE__ */ new WeakMap();
_shape2 = /* @__PURE__ */ new WeakMap();
_canceled = /* @__PURE__ */ new WeakMap();
_activatorEvent = /* @__PURE__ */ new WeakMap();
_sourceIdentifier = /* @__PURE__ */ new WeakMap();
_targetIdentifier = /* @__PURE__ */ new WeakMap();
_modifiers2 = /* @__PURE__ */ new WeakMap();
_transform = /* @__PURE__ */ new WeakMap();
__decorateElement$3(_init6, 2, "shape", _shape_dec2, DragOperation);
__decorateElement$3(_init6, 4, "canceled", _canceled_dec, DragOperation, _canceled);
__decorateElement$3(_init6, 4, "activatorEvent", _activatorEvent_dec, DragOperation, _activatorEvent);
__decorateElement$3(_init6, 4, "sourceIdentifier", _sourceIdentifier_dec, DragOperation, _sourceIdentifier);
__decorateElement$3(_init6, 4, "targetIdentifier", _targetIdentifier_dec, DragOperation, _targetIdentifier);
__decorateElement$3(_init6, 4, "modifiers", _modifiers_dec2, DragOperation, _modifiers2);
__decorateElement$3(_init6, 2, "source", _source_dec, DragOperation);
__decorateElement$3(_init6, 2, "target", _target_dec, DragOperation);
__decorateElement$3(_init6, 2, "transform", _transform_dec, DragOperation);
__decoratorMetadata$3(_init6, DragOperation);
var defaultRenderer = { get rendering() {
	return Promise.resolve();
} };
var DragDropManager$1 = class {
	/**
	* Creates a new drag and drop manager instance.
	*
	* @param config - Optional configuration for plugins, sensors, modifiers, and renderer
	*/
	constructor(config) {
		/**
		* Cleans up resources and stops any active drag operations.
		*/
		this.destroy = () => {
			if (!this.dragOperation.status.idle) this.actions.stop({ canceled: true });
			this.dragOperation.modifiers.forEach((modifier) => modifier.destroy());
			this.registry.destroy();
			this.collisionObserver.destroy();
		};
		const { plugins = [], sensors = [], modifiers = [], renderer = defaultRenderer } = config != null ? config : {};
		const monitor = new DragDropMonitor(this);
		const registry = new DragDropRegistry(this);
		this.registry = registry;
		this.monitor = monitor;
		this.renderer = renderer;
		this.actions = new DragActions(this);
		this.dragOperation = new DragOperation(this);
		this.collisionObserver = new CollisionObserver(this);
		this.plugins = [CollisionNotifier, ...plugins];
		this.modifiers = modifiers;
		this.sensors = sensors;
		const { destroy } = this;
		const cleanup = effects(() => {
			var _a, _b, _c3;
			const currentModifiers = f(() => this.dragOperation.modifiers);
			const managerModifiers = this.modifiers;
			if (currentModifiers !== managerModifiers) currentModifiers.forEach((modifier) => modifier.destroy());
			this.dragOperation.modifiers = (_c3 = (_b = (_a = this.dragOperation.source) == null ? void 0 : _a.modifiers) == null ? void 0 : _b.map((modifier) => {
				const { plugin, options } = descriptor(modifier);
				return new plugin(this, options);
			})) != null ? _c3 : managerModifiers;
		});
		this.destroy = () => {
			cleanup();
			destroy();
		};
	}
	/**
	* Gets the list of active plugins.
	*
	* @returns Array of active plugin instances
	*/
	get plugins() {
		return this.registry.plugins.values;
	}
	/**
	* Sets the list of plugins to be used by the manager.
	*
	* @param plugins - Array of plugin constructors or instances
	*/
	set plugins(plugins) {
		this.registry.plugins.values = plugins;
	}
	/**
	* Gets the list of active modifiers.
	*
	* @returns Array of active modifier instances
	*/
	get modifiers() {
		return this.registry.modifiers.values;
	}
	/**
	* Sets the list of modifiers to be used by the manager.
	*
	* @param modifiers - Array of modifier constructors or instances
	*/
	set modifiers(modifiers) {
		this.registry.modifiers.values = modifiers;
	}
	/**
	* Gets the list of active sensors.
	*
	* @returns Array of active sensor instances
	*/
	get sensors() {
		return this.registry.sensors.values;
	}
	/**
	* Sets the list of sensors to be used by the manager.
	*
	* @param sensors - Array of sensor constructors or instances
	*/
	set sensors(sensors) {
		this.registry.sensors.values = sensors;
	}
};
//#endregion
//#region node_modules/@dnd-kit/dom/utilities.js
var __typeError$3 = (msg) => {
	throw TypeError(msg);
};
var __accessCheck$3 = (obj, member, msg) => member.has(obj) || __typeError$3("Cannot " + msg);
var __privateGet$3 = (obj, member, getter) => (__accessCheck$3(obj, member, "read from private field"), member.get(obj));
var __privateAdd$3 = (obj, member, value) => member.has(obj) ? __typeError$3("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet$3 = (obj, member, value, setter) => (__accessCheck$3(obj, member, "write to private field"), member.set(obj, value), value);
var __privateMethod$1 = (obj, member, method) => (__accessCheck$3(obj, member, "access private method"), method);
function isKeyframeEffect(effect) {
	if (!effect) return false;
	if (effect instanceof KeyframeEffect) return true;
	return "getKeyframes" in effect && typeof effect.getKeyframes === "function";
}
function getFinalKeyframe(element, match) {
	const animations2 = element.getAnimations();
	if (animations2.length > 0) for (const animation of animations2) {
		if (animation.playState !== "running") continue;
		const { effect } = animation;
		const matchedKeyframes = (isKeyframeEffect(effect) ? effect.getKeyframes() : []).filter(match);
		if (matchedKeyframes.length > 0) return [matchedKeyframes[matchedKeyframes.length - 1], animation];
	}
	return null;
}
function getBoundingRectangle(element) {
	const { width, height, top, left, bottom, right } = element.getBoundingClientRect();
	return {
		width,
		height,
		top,
		left,
		bottom,
		right
	};
}
var canUseDOM = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
function isWindow(element) {
	const elementString = Object.prototype.toString.call(element);
	return elementString === "[object Window]" || elementString === "[object global]";
}
function isNode(node) {
	return "nodeType" in node;
}
function getWindow(target) {
	var _a, _b, _c;
	if (!target) return window;
	if (isWindow(target)) return target;
	if (!isNode(target)) return window;
	if ("defaultView" in target) return (_a = target.defaultView) != null ? _a : window;
	return (_c = (_b = target.ownerDocument) == null ? void 0 : _b.defaultView) != null ? _c : window;
}
function isDocument(node) {
	const { Document } = getWindow(node);
	return node instanceof Document || "nodeType" in node && node.nodeType === Node.DOCUMENT_NODE;
}
function isHTMLElement(node) {
	if (!node || isWindow(node)) return false;
	return node instanceof getWindow(node).HTMLElement || "namespaceURI" in node && typeof node.namespaceURI === "string" && node.namespaceURI.endsWith("html");
}
function isSVGElement(node) {
	return node instanceof getWindow(node).SVGElement || "namespaceURI" in node && typeof node.namespaceURI === "string" && node.namespaceURI.endsWith("svg");
}
function getDocument(target) {
	if (!target) return document;
	if (isWindow(target)) return target.document;
	if (!isNode(target)) return document;
	if (isDocument(target)) return target;
	if (isHTMLElement(target) || isSVGElement(target)) return target.ownerDocument;
	return document;
}
function getViewportBoundingRectangle(element) {
	const { documentElement } = getDocument(element);
	const width = documentElement.clientWidth;
	const height = documentElement.clientHeight;
	return {
		top: 0,
		left: 0,
		right: width,
		bottom: height,
		width,
		height
	};
}
function isOverflowVisible(element, style) {
	if (isDetailsElement(element) && element.open === false) return false;
	const { overflow, overflowX, overflowY } = getComputedStyle(element);
	return overflow === "visible" && overflowX === "visible" && overflowY === "visible";
}
function isDetailsElement(element) {
	return element.tagName === "DETAILS";
}
function getVisibleBoundingRectangle(element, boundingClientRect = element.getBoundingClientRect(), margin = 0) {
	var _a;
	let rect = boundingClientRect;
	const { ownerDocument } = element;
	const ownerWindow = (_a = ownerDocument.defaultView) != null ? _a : window;
	let ancestor = element.parentElement;
	while (ancestor && ancestor !== ownerDocument.documentElement) {
		if (!isOverflowVisible(ancestor)) {
			const ancestorRect = ancestor.getBoundingClientRect();
			const marginTop = margin * (ancestorRect.bottom - ancestorRect.top);
			const marginRight = margin * (ancestorRect.right - ancestorRect.left);
			const marginBottom = margin * (ancestorRect.bottom - ancestorRect.top);
			const marginLeft = margin * (ancestorRect.right - ancestorRect.left);
			rect = {
				top: Math.max(rect.top, ancestorRect.top - marginTop),
				right: Math.min(rect.right, ancestorRect.right + marginRight),
				bottom: Math.min(rect.bottom, ancestorRect.bottom + marginBottom),
				left: Math.max(rect.left, ancestorRect.left - marginLeft),
				width: 0,
				height: 0
			};
			rect.width = rect.right - rect.left;
			rect.height = rect.bottom - rect.top;
		}
		ancestor = ancestor.parentElement;
	}
	const viewportWidth = ownerWindow.innerWidth;
	const viewportHeight = ownerWindow.innerHeight;
	const viewportMarginY = margin * viewportHeight;
	const viewportMarginX = margin * viewportWidth;
	rect = {
		top: Math.max(rect.top, 0 - viewportMarginY),
		right: Math.min(rect.right, viewportWidth + viewportMarginX),
		bottom: Math.min(rect.bottom, viewportHeight + viewportMarginY),
		left: Math.max(rect.left, 0 - viewportMarginX),
		width: 0,
		height: 0
	};
	rect.width = rect.right - rect.left;
	rect.height = rect.bottom - rect.top;
	if (rect.width < 0) rect.width = 0;
	if (rect.height < 0) rect.height = 0;
	return rect;
}
function isSafari() {
	return /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
}
function cloneElement(element) {
	const selector = "input, textarea, select, canvas, [contenteditable]";
	const clonedElement = element.cloneNode(true);
	const fields = Array.from(element.querySelectorAll(selector));
	Array.from(clonedElement.querySelectorAll(selector)).forEach((field, index) => {
		const originalField = fields[index];
		if (isField(field) && isField(originalField)) {
			if (field.type !== "file") field.value = originalField.value;
			if (field.type === "radio" && field.name) field.name = `Cloned__${field.name}`;
		}
		if (isCanvasElement(field) && isCanvasElement(originalField) && originalField.width > 0 && originalField.height > 0) field.getContext("2d")?.drawImage(originalField, 0, 0);
	});
	return clonedElement;
}
function isField(element) {
	return "value" in element;
}
function isCanvasElement(element) {
	return element.tagName === "CANVAS";
}
function getElementFromPoint(document2, { x, y }) {
	const element = document2.elementFromPoint(x, y);
	if (isIFrameElement(element)) {
		const { contentDocument } = element;
		if (contentDocument) {
			const { left, top } = element.getBoundingClientRect();
			return getElementFromPoint(contentDocument, {
				x: x - left,
				y: y - top
			});
		}
	}
	return element;
}
function isIFrameElement(element) {
	return (element == null ? void 0 : element.tagName) === "IFRAME";
}
var ProxiedElements = /* @__PURE__ */ new WeakMap();
var Listeners = class {
	constructor() {
		this.entries = /* @__PURE__ */ new Set();
		this.clear = () => {
			for (const entry of this.entries) {
				const [target, { type, listener, options }] = entry;
				target.removeEventListener(type, listener, options);
			}
			this.entries.clear();
		};
	}
	bind(target, input) {
		const listeners = Array.isArray(input) ? input : [input];
		const entries = [];
		for (const descriptor of listeners) {
			const { type, listener, options } = descriptor;
			const entry = [target, descriptor];
			target.addEventListener(type, listener, options);
			this.entries.add(entry);
			entries.push(entry);
		}
		return function cleanup() {
			for (const [target2, { type, listener, options }] of entries) target2.removeEventListener(type, listener, options);
		};
	}
};
function getFrameElement(el) {
	const refWindow = el == null ? void 0 : el.ownerDocument.defaultView;
	if (refWindow && refWindow.self !== refWindow.parent) return refWindow.frameElement;
}
function getFrameElements(el) {
	const frames = /* @__PURE__ */ new Set();
	let frame = getFrameElement(el);
	while (frame) {
		frames.add(frame);
		frame = getFrameElement(frame);
	}
	return frames;
}
function timeout$1(callback, duration) {
	const id = setTimeout(callback, duration);
	return () => clearTimeout(id);
}
function throttle$1(func, limit) {
	const time = () => performance.now();
	let cancel;
	let lastRan;
	return function(...args) {
		const context = this;
		if (!lastRan) {
			func.apply(context, args);
			lastRan = time();
		} else {
			cancel?.();
			cancel = timeout$1(() => {
				func.apply(context, args);
				lastRan = time();
			}, limit - (time() - lastRan));
		}
	};
}
function isRectEqual(a, b) {
	if (a === b) return true;
	if (!a || !b) return false;
	return a.top == b.top && a.left == b.left && a.right == b.right && a.bottom == b.bottom;
}
function isVisible(element, boundingClientRect = element.getBoundingClientRect()) {
	const { width, height } = getVisibleBoundingRectangle(element, boundingClientRect);
	return width > 0 && height > 0;
}
var Observer = canUseDOM ? ResizeObserver : class MockResizeObserver {
	observe() {}
	unobserve() {}
	disconnect() {}
};
var _initialized;
var ResizeNotifier = class extends Observer {
	constructor(callback) {
		super((entries) => {
			if (!__privateGet$3(this, _initialized)) {
				__privateSet$3(this, _initialized, true);
				return;
			}
			callback(entries, this);
		});
		__privateAdd$3(this, _initialized, false);
	}
};
_initialized = /* @__PURE__ */ new WeakMap();
var threshold = Array.from({ length: 100 }, (_, index) => index / 100);
var THROTTLE_INTERVAL = 75;
var _visible, _previousBoundingClientRect, _resizeObserver, _positionObserver, _visibilityObserver, _debug, _disconnected, _observePosition, _PositionObserver_instances, notify_fn, updateDebug_fn;
var PositionObserver = class {
	constructor(element, callback, options = {
		debug: false,
		skipInitial: false
	}) {
		this.element = element;
		this.callback = callback;
		__privateAdd$3(this, _PositionObserver_instances);
		this.disconnect = () => {
			var _a, _b, _c;
			__privateSet$3(this, _disconnected, true);
			(_a = __privateGet$3(this, _resizeObserver)) == null || _a.disconnect();
			(_b = __privateGet$3(this, _positionObserver)) == null || _b.disconnect();
			__privateGet$3(this, _visibilityObserver).disconnect();
			(_c = __privateGet$3(this, _debug)) == null || _c.remove();
		};
		__privateAdd$3(this, _visible, true);
		__privateAdd$3(this, _previousBoundingClientRect);
		__privateAdd$3(this, _resizeObserver);
		__privateAdd$3(this, _positionObserver);
		__privateAdd$3(this, _visibilityObserver);
		__privateAdd$3(this, _debug);
		__privateAdd$3(this, _disconnected, false);
		__privateAdd$3(this, _observePosition, throttle$1(() => {
			var _a, _b, _c;
			const { element } = this;
			(_a = __privateGet$3(this, _positionObserver)) == null || _a.disconnect();
			if (__privateGet$3(this, _disconnected) || !__privateGet$3(this, _visible) || !element.isConnected) return;
			const root = (_b = element.ownerDocument) != null ? _b : document;
			const { innerHeight, innerWidth } = (_c = root.defaultView) != null ? _c : window;
			const clientRect = element.getBoundingClientRect();
			const { top, left, bottom, right } = getVisibleBoundingRectangle(element, clientRect);
			const insetTop = -Math.floor(top);
			const insetLeft = -Math.floor(left);
			const rootMargin = `${insetTop}px ${-Math.floor(innerWidth - right)}px ${-Math.floor(innerHeight - bottom)}px ${insetLeft}px`;
			this.boundingClientRect = clientRect;
			__privateSet$3(this, _positionObserver, new IntersectionObserver((entries) => {
				const [entry] = entries;
				const { intersectionRect } = entry;
				if ((entry.intersectionRatio !== 1 ? entry.intersectionRatio : Rectangle.intersectionRatio(intersectionRect, getVisibleBoundingRectangle(element))) !== 1) __privateGet$3(this, _observePosition).call(this);
			}, {
				threshold,
				rootMargin,
				root
			}));
			__privateGet$3(this, _positionObserver).observe(element);
			__privateMethod$1(this, _PositionObserver_instances, notify_fn).call(this);
		}, THROTTLE_INTERVAL));
		this.boundingClientRect = element.getBoundingClientRect();
		__privateSet$3(this, _visible, isVisible(element, this.boundingClientRect));
		let initial = true;
		this.callback = (boundingClientRect) => {
			if (initial) {
				initial = false;
				if (options.skipInitial) return;
			}
			callback(boundingClientRect);
		};
		const root = element.ownerDocument;
		if (options == null ? void 0 : options.debug) {
			__privateSet$3(this, _debug, document.createElement("div"));
			__privateGet$3(this, _debug).style.background = "rgba(0,0,0,0.15)";
			__privateGet$3(this, _debug).style.position = "fixed";
			__privateGet$3(this, _debug).style.pointerEvents = "none";
			root.body.appendChild(__privateGet$3(this, _debug));
		}
		__privateSet$3(this, _visibilityObserver, new IntersectionObserver((entries) => {
			var _a, _b;
			const { boundingClientRect, isIntersecting: visible } = entries[entries.length - 1];
			const { width, height } = boundingClientRect;
			const previousVisible = __privateGet$3(this, _visible);
			__privateSet$3(this, _visible, visible);
			if (!width && !height) return;
			if (previousVisible && !visible) {
				(_a = __privateGet$3(this, _positionObserver)) == null || _a.disconnect();
				this.callback(null);
				(_b = __privateGet$3(this, _resizeObserver)) == null || _b.disconnect();
				__privateSet$3(this, _resizeObserver, void 0);
				if (__privateGet$3(this, _debug)) __privateGet$3(this, _debug).style.visibility = "hidden";
			} else __privateGet$3(this, _observePosition).call(this);
			if (visible && !__privateGet$3(this, _resizeObserver)) {
				__privateSet$3(this, _resizeObserver, new ResizeNotifier(__privateGet$3(this, _observePosition)));
				__privateGet$3(this, _resizeObserver).observe(element);
			}
		}, {
			threshold,
			root
		}));
		if (__privateGet$3(this, _visible) && !options.skipInitial) this.callback(this.boundingClientRect);
		__privateGet$3(this, _visibilityObserver).observe(element);
	}
};
_visible = /* @__PURE__ */ new WeakMap();
_previousBoundingClientRect = /* @__PURE__ */ new WeakMap();
_resizeObserver = /* @__PURE__ */ new WeakMap();
_positionObserver = /* @__PURE__ */ new WeakMap();
_visibilityObserver = /* @__PURE__ */ new WeakMap();
_debug = /* @__PURE__ */ new WeakMap();
_disconnected = /* @__PURE__ */ new WeakMap();
_observePosition = /* @__PURE__ */ new WeakMap();
_PositionObserver_instances = /* @__PURE__ */ new WeakSet();
notify_fn = function() {
	if (__privateGet$3(this, _disconnected)) return;
	__privateMethod$1(this, _PositionObserver_instances, updateDebug_fn).call(this);
	if (isRectEqual(this.boundingClientRect, __privateGet$3(this, _previousBoundingClientRect))) return;
	this.callback(this.boundingClientRect);
	__privateSet$3(this, _previousBoundingClientRect, this.boundingClientRect);
};
updateDebug_fn = function() {
	if (__privateGet$3(this, _debug)) {
		const { top, left, width, height } = getVisibleBoundingRectangle(this.element);
		__privateGet$3(this, _debug).style.overflow = "hidden";
		__privateGet$3(this, _debug).style.visibility = "visible";
		__privateGet$3(this, _debug).style.top = `${Math.floor(top)}px`;
		__privateGet$3(this, _debug).style.left = `${Math.floor(left)}px`;
		__privateGet$3(this, _debug).style.width = `${Math.floor(width)}px`;
		__privateGet$3(this, _debug).style.height = `${Math.floor(height)}px`;
	}
};
var framePositionObservers = /* @__PURE__ */ new WeakMap();
var scrollListeners = /* @__PURE__ */ new WeakMap();
function addFrameListener(frame, callback) {
	let cached = framePositionObservers.get(frame);
	if (!cached) cached = {
		disconnect: new PositionObserver(frame, (boundingClientRect) => {
			const cached2 = framePositionObservers.get(frame);
			if (!cached2) return;
			cached2.callbacks.forEach((callback2) => callback2(boundingClientRect));
		}, { skipInitial: true }).disconnect,
		callbacks: /* @__PURE__ */ new Set()
	};
	cached.callbacks.add(callback);
	framePositionObservers.set(frame, cached);
	return () => {
		cached.callbacks.delete(callback);
		if (cached.callbacks.size === 0) {
			framePositionObservers.delete(frame);
			cached.disconnect();
		}
	};
}
function observeParentFrames(frames, callback) {
	const cleanup = /* @__PURE__ */ new Set();
	for (const frame of frames) {
		const remove = addFrameListener(frame, callback);
		cleanup.add(remove);
	}
	return () => cleanup.forEach((remove) => remove());
}
function addScrollListener(element, callback) {
	var _a;
	const doc = element.ownerDocument;
	if (!scrollListeners.has(doc)) {
		const controller = new AbortController();
		const listeners2 = /* @__PURE__ */ new Set();
		document.addEventListener("scroll", (event) => listeners2.forEach((listener) => listener(event)), {
			capture: true,
			passive: true,
			signal: controller.signal
		});
		scrollListeners.set(doc, {
			disconnect: () => controller.abort(),
			listeners: listeners2
		});
	}
	const { listeners, disconnect } = (_a = scrollListeners.get(doc)) != null ? _a : {};
	if (!listeners || !disconnect) return () => {};
	listeners.add(callback);
	return () => {
		listeners.delete(callback);
		if (listeners.size === 0) {
			disconnect();
			scrollListeners.delete(doc);
		}
	};
}
var _elementObserver, _disconnected2, _frames, _handleScroll;
var FrameObserver = class {
	constructor(element, callback, options) {
		this.callback = callback;
		__privateAdd$3(this, _elementObserver);
		__privateAdd$3(this, _disconnected2, false);
		__privateAdd$3(this, _frames);
		__privateAdd$3(this, _handleScroll, throttle$1((event) => {
			if (__privateGet$3(this, _disconnected2)) return;
			if (!event.target) return;
			if ("contains" in event.target && typeof event.target.contains === "function") {
				for (const frame of __privateGet$3(this, _frames)) if (event.target.contains(frame)) {
					this.callback(__privateGet$3(this, _elementObserver).boundingClientRect);
					break;
				}
			}
		}, THROTTLE_INTERVAL));
		const frames = getFrameElements(element);
		const unobserveParentFrames = observeParentFrames(frames, callback);
		const removeScrollListener = addScrollListener(element, __privateGet$3(this, _handleScroll));
		__privateSet$3(this, _frames, frames);
		__privateSet$3(this, _elementObserver, new PositionObserver(element, callback, options));
		this.disconnect = () => {
			if (__privateGet$3(this, _disconnected2)) return;
			__privateSet$3(this, _disconnected2, true);
			unobserveParentFrames();
			removeScrollListener();
			__privateGet$3(this, _elementObserver).disconnect();
		};
	}
};
_elementObserver = /* @__PURE__ */ new WeakMap();
_disconnected2 = /* @__PURE__ */ new WeakMap();
_frames = /* @__PURE__ */ new WeakMap();
_handleScroll = /* @__PURE__ */ new WeakMap();
function supportsPopover(element) {
	return "showPopover" in element && "hidePopover" in element && typeof element.showPopover === "function" && typeof element.hidePopover === "function";
}
function showPopover(element) {
	try {
		if (supportsPopover(element) && element.isConnected && element.hasAttribute("popover") && !element.matches(":popover-open")) element.showPopover();
	} catch (error) {}
}
function isDocumentScrollingElement(element) {
	if (!canUseDOM || !element) return false;
	return element === getDocument(element).scrollingElement;
}
function getScrollPosition(scrollableElement) {
	const window2 = getWindow(scrollableElement);
	const rect = isDocumentScrollingElement(scrollableElement) ? getViewportBoundingRectangle(scrollableElement) : getBoundingRectangle(scrollableElement);
	const dimensions = isDocumentScrollingElement(scrollableElement) ? {
		height: window2.innerHeight,
		width: window2.innerWidth
	} : {
		height: scrollableElement.clientHeight,
		width: scrollableElement.clientWidth
	};
	const position = {
		current: {
			x: scrollableElement.scrollLeft,
			y: scrollableElement.scrollTop
		},
		max: {
			x: scrollableElement.scrollWidth - dimensions.width,
			y: scrollableElement.scrollHeight - dimensions.height
		}
	};
	return {
		rect,
		position,
		isTop: position.current.y <= 0,
		isLeft: position.current.x <= 0,
		isBottom: position.current.y >= position.max.y,
		isRight: position.current.x >= position.max.x
	};
}
function canScroll(scrollableElement, by) {
	const { isTop, isBottom, isLeft, isRight, position } = getScrollPosition(scrollableElement);
	const { x, y } = by != null ? by : {
		x: 0,
		y: 0
	};
	const top = !isTop && position.current.y + y > 0;
	const bottom = !isBottom && position.current.y + y < position.max.y;
	const left = !isLeft && position.current.x + x > 0;
	const right = !isRight && position.current.x + x < position.max.x;
	return {
		top,
		bottom,
		left,
		right,
		x: left || right,
		y: top || bottom
	};
}
var Scheduler$1 = class {
	constructor(scheduler4) {
		this.scheduler = scheduler4;
		this.pending = false;
		this.tasks = /* @__PURE__ */ new Set();
		this.resolvers = /* @__PURE__ */ new Set();
		this.flush = () => {
			const { tasks, resolvers } = this;
			this.pending = false;
			this.tasks = /* @__PURE__ */ new Set();
			this.resolvers = /* @__PURE__ */ new Set();
			for (const task of tasks) task();
			for (const resolve of resolvers) resolve();
		};
	}
	schedule(task) {
		this.tasks.add(task);
		if (!this.pending) {
			this.pending = true;
			this.scheduler(this.flush);
		}
		return new Promise((resolve) => this.resolvers.add(resolve));
	}
};
var scheduler = new Scheduler$1((callback) => {
	if (typeof requestAnimationFrame === "function") requestAnimationFrame(callback);
	else callback();
});
var scheduler2 = new Scheduler$1((callback) => setTimeout(callback, 50));
var cachedStyles = /* @__PURE__ */ new Map();
var clear = cachedStyles.clear.bind(cachedStyles);
function getComputedStyles(element, cached = false) {
	if (!cached) return computeStyles(element);
	let styles = cachedStyles.get(element);
	if (styles) return styles;
	styles = computeStyles(element);
	cachedStyles.set(element, styles);
	scheduler2.schedule(clear);
	return styles;
}
function computeStyles(element) {
	return getWindow(element).getComputedStyle(element);
}
function isFixed(node, computedStyle = getComputedStyles(node, true)) {
	return computedStyle.position === "fixed" || computedStyle.position === "sticky";
}
function isScrollable(element, computedStyle = getComputedStyles(element, true)) {
	const overflowRegex = /(auto|scroll|overlay)/;
	return [
		"overflow",
		"overflowX",
		"overflowY"
	].some((property) => {
		const value = computedStyle[property];
		return typeof value === "string" ? overflowRegex.test(value) : false;
	});
}
var defaultOptions = { excludeElement: true };
function getScrollableAncestors(element, options = defaultOptions) {
	const { limit, excludeElement } = options;
	const scrollParents = /* @__PURE__ */ new Set();
	function findScrollableAncestors(node) {
		if (limit != null && scrollParents.size >= limit) return scrollParents;
		if (!node) return scrollParents;
		if (isDocument(node) && node.scrollingElement != null && !scrollParents.has(node.scrollingElement)) {
			scrollParents.add(node.scrollingElement);
			return scrollParents;
		}
		if (!isHTMLElement(node)) {
			if (isSVGElement(node)) return findScrollableAncestors(node.parentElement);
			return scrollParents;
		}
		if (scrollParents.has(node)) return scrollParents;
		const computedStyle = getComputedStyles(node, true);
		if (excludeElement && node === element);
		else if (isScrollable(node, computedStyle)) scrollParents.add(node);
		if (isFixed(node, computedStyle)) {
			const { scrollingElement } = node.ownerDocument;
			if (scrollingElement) scrollParents.add(scrollingElement);
			return scrollParents;
		}
		return findScrollableAncestors(node.parentNode);
	}
	if (!element) return scrollParents;
	return findScrollableAncestors(element);
}
function getFirstScrollableAncestor(node) {
	const [firstScrollableAncestor] = getScrollableAncestors(node, { limit: 1 });
	return firstScrollableAncestor != null ? firstScrollableAncestor : null;
}
function getFrameTransform(el, boundary = window.frameElement) {
	const transform = {
		x: 0,
		y: 0,
		scaleX: 1,
		scaleY: 1
	};
	if (!el) return transform;
	let frame = getFrameElement(el);
	while (frame) {
		if (frame === boundary) return transform;
		const rect = getBoundingRectangle(frame);
		const { x: scaleX, y: scaleY } = getScale(frame, rect);
		transform.x = transform.x + rect.left;
		transform.y = transform.y + rect.top;
		transform.scaleX = transform.scaleX * scaleX;
		transform.scaleY = transform.scaleY * scaleY;
		frame = getFrameElement(frame);
	}
	return transform;
}
function getScale(element, boundingRectangle = getBoundingRectangle(element)) {
	const width = Math.round(boundingRectangle.width);
	const height = Math.round(boundingRectangle.height);
	if (isHTMLElement(element)) return {
		x: width / element.offsetWidth,
		y: height / element.offsetHeight
	};
	const styles = getComputedStyles(element, true);
	return {
		x: (parseFloat(styles.width) || width) / width,
		y: (parseFloat(styles.height) || height) / height
	};
}
function parseScale(scale) {
	if (scale === "none") return null;
	const values = scale.split(" ");
	const x = parseFloat(values[0]);
	const y = parseFloat(values[1]);
	if (isNaN(x) && isNaN(y)) return null;
	return {
		x: isNaN(x) ? y : x,
		y: isNaN(y) ? x : y
	};
}
function parseTranslate(translate) {
	if (translate === "none") return null;
	const [x, y, z = "0"] = translate.split(" ");
	const output = {
		x: parseFloat(x),
		y: parseFloat(y),
		z: parseInt(z, 10)
	};
	if (isNaN(output.x) && isNaN(output.y)) return null;
	return {
		x: isNaN(output.x) ? 0 : output.x,
		y: isNaN(output.y) ? 0 : output.y,
		z: isNaN(output.z) ? 0 : output.z
	};
}
function parseTransform(computedStyles) {
	var _a, _b, _c, _d, _e, _f, _g, _h, _i;
	const { scale, transform, translate } = computedStyles;
	const parsedScale = parseScale(scale);
	const parsedTranslate = parseTranslate(translate);
	const parsedMatrix = parseTransformMatrix(transform);
	if (!parsedMatrix && !parsedScale && !parsedTranslate) return null;
	const normalizedScale = {
		x: (_a = parsedScale == null ? void 0 : parsedScale.x) != null ? _a : 1,
		y: (_b = parsedScale == null ? void 0 : parsedScale.y) != null ? _b : 1
	};
	const normalizedTranslate = {
		x: (_c = parsedTranslate == null ? void 0 : parsedTranslate.x) != null ? _c : 0,
		y: (_d = parsedTranslate == null ? void 0 : parsedTranslate.y) != null ? _d : 0
	};
	const normalizedMatrix = {
		x: (_e = parsedMatrix == null ? void 0 : parsedMatrix.x) != null ? _e : 0,
		y: (_f = parsedMatrix == null ? void 0 : parsedMatrix.y) != null ? _f : 0,
		scaleX: (_g = parsedMatrix == null ? void 0 : parsedMatrix.scaleX) != null ? _g : 1,
		scaleY: (_h = parsedMatrix == null ? void 0 : parsedMatrix.scaleY) != null ? _h : 1
	};
	return {
		x: normalizedTranslate.x + normalizedMatrix.x,
		y: normalizedTranslate.y + normalizedMatrix.y,
		z: (_i = parsedTranslate == null ? void 0 : parsedTranslate.z) != null ? _i : 0,
		scaleX: normalizedScale.x * normalizedMatrix.scaleX,
		scaleY: normalizedScale.y * normalizedMatrix.scaleY
	};
}
function parseTransformMatrix(transform) {
	if (transform.startsWith("matrix3d(")) {
		const transformArray = transform.slice(9, -1).split(/, /);
		return {
			x: +transformArray[12],
			y: +transformArray[13],
			scaleX: +transformArray[0],
			scaleY: +transformArray[5]
		};
	} else if (transform.startsWith("matrix(")) {
		const transformArray = transform.slice(7, -1).split(/, /);
		return {
			x: +transformArray[4],
			y: +transformArray[5],
			scaleX: +transformArray[0],
			scaleY: +transformArray[3]
		};
	}
	return null;
}
var ScrollDirection = /* @__PURE__ */ ((ScrollDirection2) => {
	ScrollDirection2[ScrollDirection2["Idle"] = 0] = "Idle";
	ScrollDirection2[ScrollDirection2["Forward"] = 1] = "Forward";
	ScrollDirection2[ScrollDirection2["Reverse"] = -1] = "Reverse";
	return ScrollDirection2;
})(ScrollDirection || {});
var defaultThreshold = {
	x: .2,
	y: .2
};
var defaultTolerance = {
	x: 10,
	y: 10
};
function detectScrollIntent(scrollableElement, coordinates, intent, acceleration = 25, thresholdPercentage = defaultThreshold, tolerance = defaultTolerance) {
	const { x, y } = coordinates;
	const { rect, isTop, isBottom, isLeft, isRight } = getScrollPosition(scrollableElement);
	const frameTransform = getFrameTransform(scrollableElement);
	const parsedTransform = parseTransform(getComputedStyles(scrollableElement, true));
	const isXAxisInverted = parsedTransform !== null ? (parsedTransform == null ? void 0 : parsedTransform.scaleX) < 0 : false;
	const isYAxisInverted = parsedTransform !== null ? (parsedTransform == null ? void 0 : parsedTransform.scaleY) < 0 : false;
	const scrollContainerRect = new Rectangle(rect.left * frameTransform.scaleX + frameTransform.x, rect.top * frameTransform.scaleY + frameTransform.y, rect.width * frameTransform.scaleX, rect.height * frameTransform.scaleY);
	const direction = {
		x: 0,
		y: 0
	};
	const speed = {
		x: 0,
		y: 0
	};
	const threshold2 = {
		height: scrollContainerRect.height * thresholdPercentage.y,
		width: scrollContainerRect.width * thresholdPercentage.x
	};
	if ((!isTop || isYAxisInverted && !isBottom) && y <= scrollContainerRect.top + threshold2.height && (intent == null ? void 0 : intent.y) !== 1 && x >= scrollContainerRect.left - tolerance.x && x <= scrollContainerRect.right + tolerance.x) {
		direction.y = isYAxisInverted ? 1 : -1;
		speed.y = acceleration * Math.abs((scrollContainerRect.top + threshold2.height - y) / threshold2.height);
	} else if ((!isBottom || isYAxisInverted && !isTop) && y >= scrollContainerRect.bottom - threshold2.height && (intent == null ? void 0 : intent.y) !== -1 && x >= scrollContainerRect.left - tolerance.x && x <= scrollContainerRect.right + tolerance.x) {
		direction.y = isYAxisInverted ? -1 : 1;
		speed.y = acceleration * Math.abs((scrollContainerRect.bottom - threshold2.height - y) / threshold2.height);
	}
	if ((!isRight || isXAxisInverted && !isLeft) && x >= scrollContainerRect.right - threshold2.width && (intent == null ? void 0 : intent.x) !== -1 && y >= scrollContainerRect.top - tolerance.y && y <= scrollContainerRect.bottom + tolerance.y) {
		direction.x = isXAxisInverted ? -1 : 1;
		speed.x = acceleration * Math.abs((scrollContainerRect.right - threshold2.width - x) / threshold2.width);
	} else if ((!isLeft || isXAxisInverted && !isRight) && x <= scrollContainerRect.left + threshold2.width && (intent == null ? void 0 : intent.x) !== 1 && y >= scrollContainerRect.top - tolerance.y && y <= scrollContainerRect.bottom + tolerance.y) {
		direction.x = isXAxisInverted ? 1 : -1;
		speed.x = acceleration * Math.abs((scrollContainerRect.left + threshold2.width - x) / threshold2.width);
	}
	return {
		direction,
		speed
	};
}
function supportsScrollIntoViewIfNeeded(element) {
	return "scrollIntoViewIfNeeded" in element && typeof element.scrollIntoViewIfNeeded === "function";
}
function scrollIntoViewIfNeeded(el, centerIfNeeded = false) {
	if (supportsScrollIntoViewIfNeeded(el)) {
		el.scrollIntoViewIfNeeded(centerIfNeeded);
		return;
	}
	if (!isHTMLElement(el)) return el.scrollIntoView();
	var parent = getFirstScrollableAncestor(el);
	if (!isHTMLElement(parent)) return;
	const parentComputedStyle = getComputedStyles(parent, true), parentBorderTopWidth = parseInt(parentComputedStyle.getPropertyValue("border-top-width")), parentBorderLeftWidth = parseInt(parentComputedStyle.getPropertyValue("border-left-width")), overTop = el.offsetTop - parent.offsetTop < parent.scrollTop, overBottom = el.offsetTop - parent.offsetTop + el.clientHeight - parentBorderTopWidth > parent.scrollTop + parent.clientHeight, overLeft = el.offsetLeft - parent.offsetLeft < parent.scrollLeft, overRight = el.offsetLeft - parent.offsetLeft + el.clientWidth - parentBorderLeftWidth > parent.scrollLeft + parent.clientWidth, alignWithTop = overTop && !overBottom;
	if ((overTop || overBottom) && centerIfNeeded) parent.scrollTop = el.offsetTop - parent.offsetTop - parent.clientHeight / 2 - parentBorderTopWidth + el.clientHeight / 2;
	if ((overLeft || overRight) && centerIfNeeded) parent.scrollLeft = el.offsetLeft - parent.offsetLeft - parent.clientWidth / 2 - parentBorderLeftWidth + el.clientWidth / 2;
	if ((overTop || overBottom || overLeft || overRight) && !centerIfNeeded) el.scrollIntoView(alignWithTop);
}
function applyTransform(rect, parsedTransform, transformOrigin) {
	const { scaleX, scaleY, x: translateX, y: translateY } = parsedTransform;
	const x = rect.left + translateX + (1 - scaleX) * parseFloat(transformOrigin);
	const y = rect.top + translateY + (1 - scaleY) * parseFloat(transformOrigin.slice(transformOrigin.indexOf(" ") + 1));
	const w = scaleX ? rect.width * scaleX : rect.width;
	const h = scaleY ? rect.height * scaleY : rect.height;
	return {
		width: w,
		height: h,
		top: y,
		right: x + w,
		bottom: y + h,
		left: x
	};
}
function inverseTransform(rect, parsedTransform, transformOrigin) {
	const { scaleX, scaleY, x: translateX, y: translateY } = parsedTransform;
	const x = rect.left - translateX - (1 - scaleX) * parseFloat(transformOrigin);
	const y = rect.top - translateY - (1 - scaleY) * parseFloat(transformOrigin.slice(transformOrigin.indexOf(" ") + 1));
	const w = scaleX ? rect.width / scaleX : rect.width;
	const h = scaleY ? rect.height / scaleY : rect.height;
	return {
		width: w,
		height: h,
		top: y,
		right: x + w,
		bottom: y + h,
		left: x
	};
}
function animateTransform({ element, keyframes, options }) {
	return element.animate(keyframes, options).finished;
}
function computeTranslate(element, translate = getComputedStyles(element).translate, projected = true) {
	if (projected) {
		const keyframe = getFinalKeyframe(element, (keyframe2) => "translate" in keyframe2);
		if (keyframe) {
			const { translate: translate2 = "" } = keyframe[0];
			if (typeof translate2 === "string") {
				const finalTranslate = parseTranslate(translate2);
				if (finalTranslate) return finalTranslate;
			}
		}
	}
	if (translate) {
		const finalTranslate = parseTranslate(translate);
		if (finalTranslate) return finalTranslate;
	}
	return {
		x: 0,
		y: 0,
		z: 0
	};
}
var scheduler3$1 = new Scheduler$1((callback) => setTimeout(callback, 0));
var animations = /* @__PURE__ */ new Map();
var clear2 = animations.clear.bind(animations);
function getDocumentAnimations(element) {
	const document2 = element.ownerDocument;
	let documentAnimations = animations.get(document2);
	if (documentAnimations) return documentAnimations;
	documentAnimations = document2.getAnimations();
	animations.set(document2, documentAnimations);
	scheduler3$1.schedule(clear2);
	const elementAnimations = documentAnimations.filter((animation) => isKeyframeEffect(animation.effect) && animation.effect.target === element);
	animations.set(element, elementAnimations);
	return documentAnimations;
}
function forceFinishAnimations(element, options) {
	const animations2 = getDocumentAnimations(element).filter((animation) => {
		var _a, _b;
		if (isKeyframeEffect(animation.effect)) {
			const { target } = animation.effect;
			if ((_b = target && ((_a = options.isValidTarget) == null ? void 0 : _a.call(options, target))) != null ? _b : true) return animation.effect.getKeyframes().some((keyframe) => {
				for (const property of options.properties) if (keyframe[property]) return true;
			});
		}
	}).map((animation) => {
		const { effect, currentTime } = animation;
		const duration = effect == null ? void 0 : effect.getComputedTiming().duration;
		if (animation.pending || animation.playState === "finished") return;
		if (typeof duration == "number" && typeof currentTime == "number" && currentTime < duration) {
			animation.currentTime = duration;
			return () => {
				animation.currentTime = currentTime;
			};
		}
	});
	if (animations2.length > 0) return () => animations2.forEach((reset) => reset == null ? void 0 : reset());
}
var DOMRectangle = class extends Rectangle {
	constructor(element, options = {}) {
		var _a, _b, _c, _d;
		const { frameTransform = getFrameTransform(element), ignoreTransforms, getBoundingClientRect = getBoundingRectangle } = options;
		const resetAnimations = forceFinishAnimations(element, {
			properties: [
				"transform",
				"translate",
				"scale",
				"width",
				"height"
			],
			isValidTarget: (target) => (target !== element || isSafari()) && target.contains(element)
		});
		const boundingRectangle = getBoundingClientRect(element);
		let { top, left, width, height } = boundingRectangle;
		let updated;
		const computedStyles = getComputedStyles(element);
		const parsedTransform = parseTransform(computedStyles);
		const scale = {
			x: (_a = parsedTransform == null ? void 0 : parsedTransform.scaleX) != null ? _a : 1,
			y: (_b = parsedTransform == null ? void 0 : parsedTransform.scaleY) != null ? _b : 1
		};
		const projectedTransform = getProjectedTransform(element, computedStyles);
		resetAnimations?.();
		if (parsedTransform) {
			updated = inverseTransform(boundingRectangle, parsedTransform, computedStyles.transformOrigin);
			if (ignoreTransforms || projectedTransform) {
				top = updated.top;
				left = updated.left;
				width = updated.width;
				height = updated.height;
			}
		}
		const intrinsic = {
			width: (_c = updated == null ? void 0 : updated.width) != null ? _c : width,
			height: (_d = updated == null ? void 0 : updated.height) != null ? _d : height
		};
		if (projectedTransform && !ignoreTransforms && updated) {
			const projected = applyTransform(updated, projectedTransform, computedStyles.transformOrigin);
			top = projected.top;
			left = projected.left;
			width = projected.width;
			height = projected.height;
			scale.x = projectedTransform.scaleX;
			scale.y = projectedTransform.scaleY;
		}
		if (frameTransform) {
			if (!ignoreTransforms) {
				left *= frameTransform.scaleX;
				width *= frameTransform.scaleX;
				top *= frameTransform.scaleY;
				height *= frameTransform.scaleY;
			}
			left += frameTransform.x;
			top += frameTransform.y;
		}
		super(left, top, width, height);
		this.scale = scale;
		this.intrinsicWidth = intrinsic.width;
		this.intrinsicHeight = intrinsic.height;
	}
};
function getProjectedTransform(element, computedStyles) {
	var _a;
	const animations2 = element.getAnimations();
	let projectedTransform = null;
	if (!animations2.length) return null;
	for (const animation of animations2) {
		if (animation.playState !== "running") continue;
		const keyframes = isKeyframeEffect(animation.effect) ? animation.effect.getKeyframes() : [];
		const keyframe = keyframes[keyframes.length - 1];
		if (!keyframe) continue;
		const { transform, translate, scale } = keyframe;
		if (transform || translate || scale) {
			const parsedTransform = parseTransform({
				transform: typeof transform === "string" && transform ? transform : computedStyles.transform,
				translate: typeof translate === "string" && translate ? translate : computedStyles.translate,
				scale: typeof scale === "string" && scale ? scale : computedStyles.scale
			});
			if (parsedTransform) projectedTransform = projectedTransform ? {
				x: projectedTransform.x + parsedTransform.x,
				y: projectedTransform.y + parsedTransform.y,
				z: (_a = projectedTransform.z) != null ? _a : parsedTransform.z,
				scaleX: projectedTransform.scaleX * parsedTransform.scaleX,
				scaleY: projectedTransform.scaleY * parsedTransform.scaleY
			} : parsedTransform;
		}
	}
	return projectedTransform;
}
function supportsStyle(element) {
	return "style" in element && typeof element.style === "object" && element.style !== null && "setProperty" in element.style && "removeProperty" in element.style && typeof element.style.setProperty === "function" && typeof element.style.removeProperty === "function";
}
var Styles = class {
	constructor(element) {
		this.element = element;
		this.initial = /* @__PURE__ */ new Map();
	}
	set(properties, prefix = "") {
		const { element } = this;
		if (!supportsStyle(element)) return;
		for (const [key, value] of Object.entries(properties)) {
			const property = `${prefix}${key}`;
			if (!this.initial.has(property)) this.initial.set(property, element.style.getPropertyValue(property));
			element.style.setProperty(property, typeof value === "string" ? value : `${value}px`);
		}
	}
	remove(properties, prefix = "") {
		const { element } = this;
		if (!supportsStyle(element)) return;
		for (const key of properties) {
			const property = `${prefix}${key}`;
			element.style.removeProperty(property);
		}
	}
	reset() {
		const { element } = this;
		if (!supportsStyle(element)) return;
		for (const [key, value] of this.initial) element.style.setProperty(key, value);
		if (element.getAttribute("style") === "") element.removeAttribute("style");
	}
};
function isElement(target) {
	if (!target) return false;
	return target instanceof getWindow(target).Element || isNode(target) && target.nodeType === Node.ELEMENT_NODE;
}
function isKeyboardEvent(event) {
	if (!event) return false;
	const { KeyboardEvent } = getWindow(event.target);
	return event instanceof KeyboardEvent;
}
function isPointerEvent(event) {
	if (!event) return false;
	const { PointerEvent } = getWindow(event.target);
	return event instanceof PointerEvent;
}
function isTextInput(target) {
	if (!isElement(target)) return false;
	const { tagName } = target;
	return tagName === "INPUT" || tagName === "TEXTAREA" || isContentEditable(target);
}
function isContentEditable(element) {
	return element.hasAttribute("contenteditable") && element.getAttribute("contenteditable") !== "false";
}
var ids = {};
function generateUniqueId(prefix) {
	const id = ids[prefix] == null ? 0 : ids[prefix] + 1;
	ids[prefix] = id;
	return `${prefix}-${id}`;
}
//#endregion
//#region node_modules/@dnd-kit/collision/dist/index.js
var pointerIntersection$2 = ({ dragOperation, droppable }) => {
	const pointerCoordinates = dragOperation.position.current;
	if (!pointerCoordinates) return null;
	const { id } = droppable;
	if (!droppable.shape) return null;
	if (droppable.shape.containsPoint(pointerCoordinates)) return {
		id,
		value: 1 / Point$1.distance(droppable.shape.center, pointerCoordinates),
		type: CollisionType.PointerIntersection,
		priority: CollisionPriority.High
	};
	return null;
};
var shapeIntersection$1 = ({ dragOperation, droppable }) => {
	const { shape } = dragOperation;
	if (!droppable.shape || !(shape == null ? void 0 : shape.current)) return null;
	const intersectionArea = shape.current.intersectionArea(droppable.shape);
	if (intersectionArea) {
		const { position } = dragOperation;
		const distance = Point$1.distance(droppable.shape.center, position.current);
		const value = intersectionArea / (shape.current.area + droppable.shape.area - intersectionArea) / distance;
		return {
			id: droppable.id,
			value,
			type: CollisionType.ShapeIntersection,
			priority: CollisionPriority.Normal
		};
	}
	return null;
};
var defaultCollisionDetection$1 = (args) => {
	var _a;
	return (_a = pointerIntersection$2(args)) != null ? _a : shapeIntersection$1(args);
};
var closestCorners$1 = (input) => {
	const { dragOperation, droppable } = input;
	const { shape, position } = dragOperation;
	if (!droppable.shape) return null;
	const shapeCorners = shape ? Rectangle.from(shape.current.boundingRectangle).corners : void 0;
	const value = Rectangle.from(droppable.shape.boundingRectangle).corners.reduce((acc, corner, index) => {
		var _a;
		return acc + Point$1.distance(Point$1.from(corner), (_a = shapeCorners == null ? void 0 : shapeCorners[index]) != null ? _a : position.current);
	}, 0) / 4;
	return {
		id: droppable.id,
		value: 1 / value,
		type: CollisionType.Collision,
		priority: CollisionPriority.Normal
	};
};
//#endregion
//#region node_modules/@dnd-kit/dom/index.js
var __create$2 = Object.create;
var __defProp$3 = Object.defineProperty;
var __defProps$3 = Object.defineProperties;
var __getOwnPropDesc$2 = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs$3 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols$3 = Object.getOwnPropertySymbols;
var __hasOwnProp$3 = Object.prototype.hasOwnProperty;
var __propIsEnum$3 = Object.prototype.propertyIsEnumerable;
var __knownSymbol$2 = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError$2 = (msg) => {
	throw TypeError(msg);
};
var __defNormalProp$3 = (obj, key, value) => key in obj ? __defProp$3(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues$3 = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp$3.call(b, prop)) __defNormalProp$3(a, prop, b[prop]);
	if (__getOwnPropSymbols$3) {
		for (var prop of __getOwnPropSymbols$3(b)) if (__propIsEnum$3.call(b, prop)) __defNormalProp$3(a, prop, b[prop]);
	}
	return a;
};
var __spreadProps$3 = (a, b) => __defProps$3(a, __getOwnPropDescs$3(b));
var __name = (target, value) => __defProp$3(target, "name", {
	value,
	configurable: true
});
var __objRest$2 = (source, exclude) => {
	var target = {};
	for (var prop in source) if (__hasOwnProp$3.call(source, prop) && exclude.indexOf(prop) < 0) target[prop] = source[prop];
	if (source != null && __getOwnPropSymbols$3) {
		for (var prop of __getOwnPropSymbols$3(source)) if (exclude.indexOf(prop) < 0 && __propIsEnum$3.call(source, prop)) target[prop] = source[prop];
	}
	return target;
};
var __decoratorStart$2 = (base) => {
	var _a4;
	return [
		,
		,
		,
		__create$2((_a4 = base == null ? void 0 : base[__knownSymbol$2("metadata")]) != null ? _a4 : null)
	];
};
var __decoratorStrings$2 = [
	"class",
	"method",
	"getter",
	"setter",
	"accessor",
	"field",
	"value",
	"get",
	"set"
];
var __expectFn$2 = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError$2("Function expected") : fn;
var __decoratorContext$2 = (kind, name, done, metadata, fns) => ({
	kind: __decoratorStrings$2[kind],
	name,
	metadata,
	addInitializer: (fn) => done._ ? __typeError$2("Already initialized") : fns.push(__expectFn$2(fn || null))
});
var __decoratorMetadata$2 = (array, target) => __defNormalProp$3(target, __knownSymbol$2("metadata"), array[3]);
var __runInitializers$2 = (array, flags, self, value) => {
	for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
	return value;
};
var __decorateElement$2 = (array, flags, name, decorators, target, extra) => {
	var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
	var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings$2[k + 5];
	var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
	var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc$2(k < 4 ? target : {
		get [name]() {
			return __privateGet$2(this, extra);
		},
		set [name](x) {
			return __privateSet$2(this, extra, x);
		}
	}, name));
	k ? p && k < 4 && __name(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name(target, name);
	for (var i = decorators.length - 1; i >= 0; i--) {
		ctx = __decoratorContext$2(k, name, done = {}, array[3], extraInitializers);
		if (k) {
			ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn(target, x) : (x) => name in x };
			if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet$2 : __privateMethod)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
			if (k > 2) access.set = p ? (x, y) => __privateSet$2(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
		}
		it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : {
			get: desc.get,
			set: desc.set
		} : target, ctx), done._ = 1;
		if (k ^ 4 || it === void 0) __expectFn$2(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
		else if (typeof it !== "object" || it === null) __typeError$2("Object expected");
		else __expectFn$2(fn = it.get) && (desc.get = fn), __expectFn$2(fn = it.set) && (desc.set = fn), __expectFn$2(fn = it.init) && initializers.unshift(fn);
	}
	return k || __decoratorMetadata$2(array, target), desc && __defProp$3(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __accessCheck$2 = (obj, member, msg) => member.has(obj) || __typeError$2("Cannot " + msg);
var __privateIn = (member, obj) => Object(obj) !== obj ? __typeError$2("Cannot use the \"in\" operator on this value") : member.has(obj);
var __privateGet$2 = (obj, member, getter) => (__accessCheck$2(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd$2 = (obj, member, value) => member.has(obj) ? __typeError$2("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet$2 = (obj, member, value, setter) => (__accessCheck$2(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck$2(obj, member, "access private method"), method);
var defaultAttributes = {
	role: "button",
	roleDescription: "draggable"
};
var defaultDescriptionIdPrefix = `dnd-kit-description`;
var defaultAnnouncementIdPrefix = `dnd-kit-announcement`;
var defaultScreenReaderInstructions = { draggable: `To pick up a draggable item, press the space bar. While dragging, use the arrow keys to move the item in a given direction. Press space again to drop the item in its new position, or press escape to cancel.` };
var defaultAnnouncements = {
	dragstart({ operation: { source } }) {
		if (!source) return;
		return `Picked up draggable item ${source.id}.`;
	},
	dragover({ operation: { source, target } }) {
		if (!source || source.id === (target == null ? void 0 : target.id)) return;
		if (target) return `Draggable item ${source.id} was moved over droppable target ${target.id}.`;
		return `Draggable item ${source.id} is no longer over a droppable target.`;
	},
	dragend({ operation: { source, target }, canceled }) {
		if (!source) return;
		if (canceled) return `Dragging was cancelled. Draggable item ${source.id} was dropped.`;
		if (target) return `Draggable item ${source.id} was dropped over droppable target ${target.id}`;
		return `Draggable item ${source.id} was dropped.`;
	}
};
function isFocusable(element) {
	const tagName = element.tagName.toLowerCase();
	return [
		"input",
		"select",
		"textarea",
		"a",
		"button"
	].includes(tagName);
}
function createHiddenText(id, value) {
	const element = document.createElement("div");
	element.id = id;
	element.style.setProperty("display", "none");
	element.textContent = value;
	return element;
}
function createLiveRegion(id) {
	const element = document.createElement("div");
	element.id = id;
	element.setAttribute("role", "status");
	element.setAttribute("aria-live", "polite");
	element.setAttribute("aria-atomic", "true");
	element.style.setProperty("position", "fixed");
	element.style.setProperty("width", "1px");
	element.style.setProperty("height", "1px");
	element.style.setProperty("margin", "-1px");
	element.style.setProperty("border", "0");
	element.style.setProperty("padding", "0");
	element.style.setProperty("overflow", "hidden");
	element.style.setProperty("clip", "rect(0 0 0 0)");
	element.style.setProperty("clip-path", "inset(100%)");
	element.style.setProperty("white-space", "nowrap");
	return element;
}
var debouncedEvents = ["dragover", "dragmove"];
var Accessibility = class extends Plugin {
	constructor(manager, options) {
		super(manager);
		const { id, idPrefix: { description: descriptionPrefix = defaultDescriptionIdPrefix, announcement: announcementPrefix = defaultAnnouncementIdPrefix } = {}, announcements = defaultAnnouncements, screenReaderInstructions = defaultScreenReaderInstructions, debounce: debounceMs = 500 } = options != null ? options : {};
		const descriptionId = id ? `${descriptionPrefix}-${id}` : generateUniqueId(descriptionPrefix);
		const announcementId = id ? `${announcementPrefix}-${id}` : generateUniqueId(announcementPrefix);
		let hiddenTextElement;
		let liveRegionElement;
		let liveRegionTextNode;
		let latestAnnouncement;
		const updateAnnouncement = (value = latestAnnouncement) => {
			if (!liveRegionTextNode || !value) return;
			if ((liveRegionTextNode == null ? void 0 : liveRegionTextNode.nodeValue) !== value) liveRegionTextNode.nodeValue = value;
		};
		const scheduleUpdateAnnouncement = () => scheduler.schedule(updateAnnouncement);
		const debouncedUpdateAnnouncement = debounce$1(scheduleUpdateAnnouncement, debounceMs);
		const eventListeners = Object.entries(announcements).map(([eventName, getAnnouncement]) => {
			return this.manager.monitor.addEventListener(eventName, (event, manager2) => {
				const element = liveRegionTextNode;
				if (!element) return;
				const announcement = getAnnouncement == null ? void 0 : getAnnouncement(event, manager2);
				if (announcement && element.nodeValue !== announcement) {
					latestAnnouncement = announcement;
					if (debouncedEvents.includes(eventName)) debouncedUpdateAnnouncement();
					else {
						scheduleUpdateAnnouncement();
						debouncedUpdateAnnouncement.cancel();
					}
				}
			});
		});
		const initialize = () => {
			let elements = [];
			if (!(hiddenTextElement == null ? void 0 : hiddenTextElement.isConnected)) {
				hiddenTextElement = createHiddenText(descriptionId, screenReaderInstructions.draggable);
				elements.push(hiddenTextElement);
			}
			if (!(liveRegionElement == null ? void 0 : liveRegionElement.isConnected)) {
				liveRegionElement = createLiveRegion(announcementId);
				liveRegionTextNode = document.createTextNode("");
				liveRegionElement.appendChild(liveRegionTextNode);
				elements.push(liveRegionElement);
			}
			if (elements.length > 0) document.body.append(...elements);
		};
		const mutations = /* @__PURE__ */ new Set();
		function executeMutations() {
			for (const operation of mutations) operation();
		}
		this.registerEffect(() => {
			var _a4;
			mutations.clear();
			for (const draggable of this.manager.registry.draggables.value) {
				const activator = (_a4 = draggable.handle) != null ? _a4 : draggable.element;
				if (activator) {
					if (!hiddenTextElement || !liveRegionElement) mutations.add(initialize);
					if ((!isFocusable(activator) || isSafari()) && !activator.hasAttribute("tabindex")) mutations.add(() => activator.setAttribute("tabindex", "0"));
					if (!activator.hasAttribute("role") && !(activator.tagName.toLowerCase() === "button")) mutations.add(() => activator.setAttribute("role", defaultAttributes.role));
					if (!activator.hasAttribute("aria-roledescription")) mutations.add(() => activator.setAttribute("aria-roledescription", defaultAttributes.roleDescription));
					if (!activator.hasAttribute("aria-describedby")) mutations.add(() => activator.setAttribute("aria-describedby", descriptionId));
					for (const key of ["aria-pressed", "aria-grabbed"]) {
						const value = String(draggable.isDragging);
						if (activator.getAttribute(key) !== value) mutations.add(() => activator.setAttribute(key, value));
					}
					const disabled = String(draggable.disabled);
					if (activator.getAttribute("aria-disabled") !== disabled) mutations.add(() => activator.setAttribute("aria-disabled", disabled));
				}
			}
			if (mutations.size > 0) scheduler.schedule(executeMutations);
		});
		this.destroy = () => {
			super.destroy();
			hiddenTextElement?.remove();
			liveRegionElement?.remove();
			eventListeners.forEach((unsubscribe) => unsubscribe());
		};
	}
};
function debounce$1(fn, wait) {
	let timeout;
	const debounced = () => {
		clearTimeout(timeout);
		timeout = setTimeout(fn, wait);
	};
	debounced.cancel = () => clearTimeout(timeout);
	return debounced;
}
var Cursor = class extends Plugin {
	constructor(manager, options) {
		super(manager, options);
		this.manager = manager;
		const doc = computed(() => {
			var _a4;
			return getDocument((_a4 = this.manager.dragOperation.source) == null ? void 0 : _a4.element);
		});
		this.destroy = j(() => {
			var _a4;
			const { dragOperation } = this.manager;
			const { cursor = "grabbing", nonce } = (_a4 = this.options) != null ? _a4 : {};
			if (dragOperation.status.initialized) {
				const document2 = doc.value;
				const style = document2.createElement("style");
				if (nonce) style.setAttribute("nonce", nonce);
				style.textContent = `* { cursor: ${cursor} !important; }`;
				document2.head.appendChild(style);
				return () => style.remove();
			}
		});
	}
};
var ATTR_PREFIX = "data-dnd-";
var DROPPING_ATTRIBUTE = `${ATTR_PREFIX}dropping`;
var CSS_PREFIX = "--dnd-";
var ATTRIBUTE = `${ATTR_PREFIX}dragging`;
var PLACEHOLDER_ATTRIBUTE = `${ATTR_PREFIX}placeholder`;
var IGNORED_ATTRIBUTES = [
	ATTRIBUTE,
	PLACEHOLDER_ATTRIBUTE,
	"popover",
	"aria-pressed",
	"aria-grabbing"
];
var IGNORED_STYLES = ["view-transition-name"];
var CSS_RULES = `
  :root [${ATTRIBUTE}] {
    position: fixed !important;
    pointer-events: none !important;
    touch-action: none;
    z-index: calc(infinity);
    will-change: translate;
    top: var(${CSS_PREFIX}top, 0px) !important;
    left: var(${CSS_PREFIX}left, 0px) !important;
    right: unset !important;
    bottom: unset !important;
    width: var(${CSS_PREFIX}width, auto);
    max-width: var(${CSS_PREFIX}width, auto);
    height: var(${CSS_PREFIX}height, auto);
    max-height: var(${CSS_PREFIX}height, auto);
    transition: var(${CSS_PREFIX}transition) !important;
  }

  :root [${PLACEHOLDER_ATTRIBUTE}] {
    transition: none;
  }

  :root [${PLACEHOLDER_ATTRIBUTE}='hidden'] {
    visibility: hidden;
  }

  [${ATTRIBUTE}] * {
    pointer-events: none !important;
  }

  [${ATTRIBUTE}]:not([${DROPPING_ATTRIBUTE}]) {
    translate: var(${CSS_PREFIX}translate) !important;
  }

  [${ATTRIBUTE}][style*='${CSS_PREFIX}scale'] {
    scale: var(${CSS_PREFIX}scale) !important;
    transform-origin: var(${CSS_PREFIX}transform-origin) !important;
  }

  @layer {
    :where([${ATTRIBUTE}][popover]) {
      overflow: visible;
      background: unset;
      border: unset;
      margin: unset;
      padding: unset;
      color: inherit;

      &:is(input, button) {
        border: revert;
        background: revert;
      }
    }
  }
  [${ATTRIBUTE}]::backdrop, [${ATTR_PREFIX}overlay]:not([${ATTRIBUTE}]) {
    display: none;
    visibility: hidden;
  }
`.replace(/\n+/g, " ").replace(/\s+/g, " ").trim();
function createPlaceholder(source, type = "hidden") {
	return f(() => {
		const { element, manager } = source;
		if (!element || !manager) return;
		const containedDroppables = findContainedDroppables(element, manager.registry.droppables);
		const cleanup = [];
		const placeholder = cloneElement(element);
		const { remove } = placeholder;
		proxyDroppableElements(containedDroppables, placeholder, cleanup);
		configurePlaceholder(placeholder, type);
		placeholder.remove = () => {
			cleanup.forEach((fn) => fn());
			remove.call(placeholder);
		};
		return placeholder;
	});
}
function findContainedDroppables(element, droppables) {
	const containedDroppables = /* @__PURE__ */ new Map();
	for (const droppable of droppables) {
		if (!droppable.element) continue;
		if (element === droppable.element || element.contains(droppable.element)) {
			const identifierAttribute = `${ATTR_PREFIX}${generateUniqueId("dom-id")}`;
			droppable.element.setAttribute(identifierAttribute, "");
			containedDroppables.set(droppable, identifierAttribute);
		}
	}
	return containedDroppables;
}
function proxyDroppableElements(containedDroppables, placeholder, cleanup) {
	for (const [droppable, identifierAttribute] of containedDroppables) {
		if (!droppable.element) continue;
		const selector = `[${identifierAttribute}]`;
		const clonedElement = placeholder.matches(selector) ? placeholder : placeholder.querySelector(selector);
		droppable.element.removeAttribute(identifierAttribute);
		if (!clonedElement) continue;
		const originalElement = droppable.element;
		droppable.proxy = clonedElement;
		clonedElement.removeAttribute(identifierAttribute);
		ProxiedElements.set(originalElement, clonedElement);
		cleanup.push(() => {
			ProxiedElements.delete(originalElement);
			droppable.proxy = void 0;
		});
	}
}
function configurePlaceholder(placeholder, type = "hidden") {
	placeholder.setAttribute("inert", "true");
	placeholder.setAttribute("tab-index", "-1");
	placeholder.setAttribute("aria-hidden", "true");
	placeholder.setAttribute(PLACEHOLDER_ATTRIBUTE, type);
}
function isSameFrame(element, target) {
	if (element === target) return true;
	return getFrameElement(element) === getFrameElement(target);
}
function preventPopoverClose(event) {
	const { target } = event;
	if ("newState" in event && event.newState === "closed" && isElement(target) && target.hasAttribute("popover")) requestAnimationFrame(() => showPopover(target));
}
function isTableRow(element) {
	return element.tagName === "TR";
}
var styleSheetRegistry = /* @__PURE__ */ new Map();
var _overlay_dec, _a$1, _init$2, _overlay, _Feedback_instances, render_fn, injectStyles_fn;
var _Feedback = class _Feedback extends (_a$1 = Plugin, _overlay_dec = [reactive], _a$1) {
	constructor(manager, options) {
		super(manager, options);
		__privateAdd$2(this, _Feedback_instances);
		__privateAdd$2(this, _overlay, __runInitializers$2(_init$2, 8, this)), __runInitializers$2(_init$2, 11, this);
		this.state = {
			initial: {},
			current: {}
		};
		this.registerEffect(__privateMethod(this, _Feedback_instances, injectStyles_fn));
		this.registerEffect(__privateMethod(this, _Feedback_instances, render_fn));
	}
	destroy() {
		super.destroy();
		for (const [doc, registration] of styleSheetRegistry.entries()) if (registration.instances.has(this)) {
			registration.instances.delete(this);
			if (registration.instances.size === 0) {
				registration.cleanup();
				styleSheetRegistry.delete(doc);
			}
		}
	}
};
_init$2 = __decoratorStart$2(_a$1);
_overlay = /* @__PURE__ */ new WeakMap();
_Feedback_instances = /* @__PURE__ */ new WeakSet();
render_fn = function() {
	var _a4, _b2, _c3;
	const { state, manager, options } = this;
	const { dragOperation } = manager;
	const { position, source, status } = dragOperation;
	if (status.idle) {
		state.current = {};
		state.initial = {};
		return;
	}
	if (!source) return;
	const { element, feedback } = source;
	if (!element || feedback === "none" || !status.initialized || status.initializing) return;
	const { initial } = state;
	const feedbackElement = (_a4 = this.overlay) != null ? _a4 : element;
	const frameTransform = getFrameTransform(feedbackElement);
	const elementFrameTransform = getFrameTransform(element);
	const crossFrame = !isSameFrame(element, feedbackElement);
	const shape = new DOMRectangle(element, {
		frameTransform: crossFrame ? elementFrameTransform : null,
		ignoreTransforms: !crossFrame
	});
	const scaleDelta = {
		x: elementFrameTransform.scaleX / frameTransform.scaleX,
		y: elementFrameTransform.scaleY / frameTransform.scaleY
	};
	let { width, height, top, left } = shape;
	if (crossFrame) {
		width = width / scaleDelta.x;
		height = height / scaleDelta.y;
	}
	let elementMutationObserver;
	let documentMutationObserver;
	const styles = new Styles(feedbackElement);
	const { transition, translate, boxSizing, paddingBlockStart, paddingBlockEnd, paddingInlineStart, paddingInlineEnd, borderInlineStartWidth, borderInlineEndWidth, borderBlockStartWidth, borderBlockEndWidth } = getComputedStyles(element);
	const clone = feedback === "clone";
	const contentBox = boxSizing === "content-box";
	const widthOffset = contentBox ? parseInt(paddingInlineStart) + parseInt(paddingInlineEnd) + parseInt(borderInlineStartWidth) + parseInt(borderInlineEndWidth) : 0;
	const heightOffset = contentBox ? parseInt(paddingBlockStart) + parseInt(paddingBlockEnd) + parseInt(borderBlockStartWidth) + parseInt(borderBlockEndWidth) : 0;
	const placeholder = feedback !== "move" && !this.overlay ? createPlaceholder(source, clone ? "clone" : "hidden") : null;
	const isKeyboardOperation = f(() => isKeyboardEvent(manager.dragOperation.activatorEvent));
	if (translate !== "none") {
		const parsedTranslate = parseTranslate(translate);
		if (parsedTranslate && !initial.translate) initial.translate = parsedTranslate;
	}
	if (!initial.transformOrigin) {
		const current = f(() => position.current);
		initial.transformOrigin = {
			x: (current.x - left * frameTransform.scaleX - frameTransform.x) / (width * frameTransform.scaleX),
			y: (current.y - top * frameTransform.scaleY - frameTransform.y) / (height * frameTransform.scaleY)
		};
	}
	const { transformOrigin } = initial;
	const relativeTop = top * frameTransform.scaleY + frameTransform.y;
	const relativeLeft = left * frameTransform.scaleX + frameTransform.x;
	if (!initial.coordinates) {
		initial.coordinates = {
			x: relativeLeft,
			y: relativeTop
		};
		if (scaleDelta.x !== 1 || scaleDelta.y !== 1) {
			const { scaleX, scaleY } = elementFrameTransform;
			const { x: tX2, y: tY2 } = transformOrigin;
			initial.coordinates.x += (width * scaleX - width) * tX2;
			initial.coordinates.y += (height * scaleY - height) * tY2;
		}
	}
	if (!initial.dimensions) initial.dimensions = {
		width,
		height
	};
	if (!initial.frameTransform) initial.frameTransform = frameTransform;
	const coordinatesDelta = {
		x: initial.coordinates.x - relativeLeft,
		y: initial.coordinates.y - relativeTop
	};
	const sizeDelta = {
		width: (initial.dimensions.width * initial.frameTransform.scaleX - width * frameTransform.scaleX) * transformOrigin.x,
		height: (initial.dimensions.height * initial.frameTransform.scaleY - height * frameTransform.scaleY) * transformOrigin.y
	};
	const delta = {
		x: coordinatesDelta.x / frameTransform.scaleX + sizeDelta.width,
		y: coordinatesDelta.y / frameTransform.scaleY + sizeDelta.height
	};
	const projected = {
		left: left + delta.x,
		top: top + delta.y
	};
	feedbackElement.setAttribute(ATTRIBUTE, "true");
	const transform = f(() => dragOperation.transform);
	const initialTranslate = (_b2 = initial.translate) != null ? _b2 : {
		x: 0,
		y: 0
	};
	const translateString = `${transform.x * frameTransform.scaleX + initialTranslate.x}px ${transform.y * frameTransform.scaleY + initialTranslate.y}px 0`;
	const transitionString = transition ? `${transition}, translate 0ms linear` : "";
	styles.set({
		width: width - widthOffset,
		height: height - heightOffset,
		top: projected.top,
		left: projected.left,
		translate: translateString,
		transition: transitionString,
		scale: crossFrame ? `${scaleDelta.x} ${scaleDelta.y}` : "",
		"transform-origin": `${transformOrigin.x * 100}% ${transformOrigin.y * 100}%`
	}, CSS_PREFIX);
	if (placeholder) {
		element.insertAdjacentElement("afterend", placeholder);
		if (options == null ? void 0 : options.rootElement) (typeof options.rootElement === "function" ? options.rootElement(source) : options.rootElement).appendChild(element);
	}
	if (supportsPopover(feedbackElement)) {
		if (!feedbackElement.hasAttribute("popover")) feedbackElement.setAttribute("popover", "manual");
		showPopover(feedbackElement);
		feedbackElement.addEventListener("beforetoggle", preventPopoverClose);
	}
	const resizeObserver = new ResizeObserver(() => {
		if (!placeholder) return;
		const placeholderShape = new DOMRectangle(placeholder, {
			frameTransform,
			ignoreTransforms: true
		});
		const origin = transformOrigin != null ? transformOrigin : {
			x: 1,
			y: 1
		};
		const dX = (width - placeholderShape.width) * origin.x + delta.x;
		const dY = (height - placeholderShape.height) * origin.y + delta.y;
		styles.set({
			width: placeholderShape.width - widthOffset,
			height: placeholderShape.height - heightOffset,
			top: top + dY,
			left: left + dX
		}, CSS_PREFIX);
		elementMutationObserver?.takeRecords();
		if (isTableRow(element) && isTableRow(placeholder)) {
			const cells = Array.from(element.cells);
			const placeholderCells = Array.from(placeholder.cells);
			for (const [index, cell] of cells.entries()) {
				const placeholderCell = placeholderCells[index];
				cell.style.width = `${placeholderCell.offsetWidth}px`;
			}
		}
		dragOperation.shape = new DOMRectangle(feedbackElement);
	});
	const initialShape = new DOMRectangle(feedbackElement);
	f(() => dragOperation.shape = initialShape);
	const feedbackWindow = getWindow(feedbackElement);
	const handleWindowResize = (event) => {
		this.manager.actions.stop({ event });
	};
	if (isKeyboardOperation) feedbackWindow.addEventListener("resize", handleWindowResize);
	if (f(() => source.status) === "idle") requestAnimationFrame(() => source.status = "dragging");
	if (placeholder) {
		resizeObserver.observe(placeholder);
		elementMutationObserver = new MutationObserver((mutations) => {
			let hasChildrenMutations = false;
			for (const mutation of mutations) {
				if (mutation.target !== element) {
					hasChildrenMutations = true;
					continue;
				}
				if (mutation.type !== "attributes") continue;
				const attributeName = mutation.attributeName;
				if (attributeName.startsWith("aria-") || IGNORED_ATTRIBUTES.includes(attributeName)) continue;
				const attributeValue = element.getAttribute(attributeName);
				if (attributeName === "style") {
					if (supportsStyle(element) && supportsStyle(placeholder)) {
						const styles2 = element.style;
						for (const key of Array.from(placeholder.style)) if (styles2.getPropertyValue(key) === "") placeholder.style.removeProperty(key);
						for (const key of Array.from(styles2)) {
							if (IGNORED_STYLES.includes(key) || key.startsWith(CSS_PREFIX)) continue;
							const value = styles2.getPropertyValue(key);
							placeholder.style.setProperty(key, value);
						}
					}
				} else if (attributeValue !== null) placeholder.setAttribute(attributeName, attributeValue);
				else placeholder.removeAttribute(attributeName);
			}
			if (hasChildrenMutations && clone) placeholder.innerHTML = element.innerHTML;
		});
		elementMutationObserver.observe(element, {
			attributes: true,
			subtree: true,
			childList: true
		});
		documentMutationObserver = new MutationObserver((entries) => {
			for (const entry of entries) {
				if (entry.addedNodes.length === 0) continue;
				for (const node of Array.from(entry.addedNodes)) {
					if (node.contains(element) && element.nextElementSibling !== placeholder) {
						element.insertAdjacentElement("afterend", placeholder);
						showPopover(feedbackElement);
						return;
					}
					if (node.contains(placeholder) && placeholder.previousElementSibling !== element) {
						placeholder.insertAdjacentElement("beforebegin", element);
						showPopover(feedbackElement);
						return;
					}
				}
			}
		});
		documentMutationObserver.observe(element.ownerDocument.body, {
			childList: true,
			subtree: true
		});
	}
	const id = (_c3 = manager.dragOperation.source) == null ? void 0 : _c3.id;
	const restoreFocus = () => {
		var _a5;
		if (!isKeyboardOperation || id == null) return;
		const draggable = manager.registry.draggables.get(id);
		const element2 = (_a5 = draggable == null ? void 0 : draggable.handle) != null ? _a5 : draggable == null ? void 0 : draggable.element;
		if (isHTMLElement(element2)) element2.focus();
	};
	const cleanup = () => {
		elementMutationObserver?.disconnect();
		documentMutationObserver?.disconnect();
		resizeObserver.disconnect();
		feedbackWindow.removeEventListener("resize", handleWindowResize);
		if (supportsPopover(feedbackElement)) {
			feedbackElement.removeEventListener("beforetoggle", preventPopoverClose);
			feedbackElement.removeAttribute("popover");
		}
		feedbackElement.removeAttribute(ATTRIBUTE);
		styles.reset();
		source.status = "idle";
		const moved = state.current.translate != null;
		if (placeholder && (moved || placeholder.parentElement !== feedbackElement.parentElement) && feedbackElement.isConnected) placeholder.replaceWith(feedbackElement);
		placeholder?.remove();
	};
	const cleanupEffects = effects(() => {
		var _a5;
		const { transform: transform2, status: status2 } = dragOperation;
		if (!transform2.x && !transform2.y && !state.current.translate) return;
		if (status2.dragging) {
			const initialTranslate2 = (_a5 = initial.translate) != null ? _a5 : {
				x: 0,
				y: 0
			};
			const translate2 = {
				x: transform2.x / frameTransform.scaleX + initialTranslate2.x,
				y: transform2.y / frameTransform.scaleY + initialTranslate2.y
			};
			const previousTranslate = state.current.translate;
			const modifiers = f(() => dragOperation.modifiers);
			const currentShape = f(() => {
				var _a6;
				return (_a6 = dragOperation.shape) == null ? void 0 : _a6.current;
			});
			const translateTransition = isKeyboardOperation ? "250ms cubic-bezier(0.25, 1, 0.5, 1)" : "0ms linear";
			styles.set({
				transition: `${transition}, translate ${translateTransition}`,
				translate: `${translate2.x}px ${translate2.y}px 0`
			}, CSS_PREFIX);
			elementMutationObserver?.takeRecords();
			if (currentShape && currentShape !== initialShape && previousTranslate && !modifiers.length) {
				const delta2 = Point$1.delta(translate2, previousTranslate);
				dragOperation.shape = Rectangle.from(currentShape.boundingRectangle).translate(delta2.x * frameTransform.scaleX, delta2.y * frameTransform.scaleY);
			} else dragOperation.shape = new DOMRectangle(feedbackElement);
			state.current.translate = translate2;
		}
	}, function() {
		if (dragOperation.status.dropped) {
			this.dispose();
			source.status = "dropping";
			let translate2 = state.current.translate;
			const moved = translate2 != null;
			if (!translate2 && element !== feedbackElement) translate2 = {
				x: 0,
				y: 0
			};
			if (!translate2) {
				cleanup();
				return;
			}
			const dropAnimation = () => {
				var _a5, _b3;
				{
					showPopover(feedbackElement);
					const [, animation] = (_a5 = getFinalKeyframe(feedbackElement, (keyframe) => "translate" in keyframe)) != null ? _a5 : [];
					animation?.pause();
					const target = placeholder != null ? placeholder : element;
					const options2 = { frameTransform: isSameFrame(feedbackElement, target) ? null : void 0 };
					const current = new DOMRectangle(feedbackElement, options2);
					const currentTranslate = (_b3 = parseTranslate(getComputedStyles(feedbackElement).translate)) != null ? _b3 : translate2;
					const final = new DOMRectangle(target, options2);
					const delta2 = Rectangle.delta(current, final, source.alignment);
					const finalTranslate = {
						x: currentTranslate.x - delta2.x,
						y: currentTranslate.y - delta2.y
					};
					const heightKeyframes = Math.round(current.intrinsicHeight) !== Math.round(final.intrinsicHeight) ? {
						minHeight: [`${current.intrinsicHeight}px`, `${final.intrinsicHeight}px`],
						maxHeight: [`${current.intrinsicHeight}px`, `${final.intrinsicHeight}px`]
					} : {};
					const widthKeyframes = Math.round(current.intrinsicWidth) !== Math.round(final.intrinsicWidth) ? {
						minWidth: [`${current.intrinsicWidth}px`, `${final.intrinsicWidth}px`],
						maxWidth: [`${current.intrinsicWidth}px`, `${final.intrinsicWidth}px`]
					} : {};
					styles.set({ transition }, CSS_PREFIX);
					feedbackElement.setAttribute(DROPPING_ATTRIBUTE, "");
					elementMutationObserver?.takeRecords();
					animateTransform({
						element: feedbackElement,
						keyframes: __spreadProps$3(__spreadValues$3(__spreadValues$3({}, heightKeyframes), widthKeyframes), { translate: [`${currentTranslate.x}px ${currentTranslate.y}px 0`, `${finalTranslate.x}px ${finalTranslate.y}px 0`] }),
						options: {
							duration: moved || feedbackElement !== element ? 250 : 0,
							easing: "ease"
						}
					}).then(() => {
						feedbackElement.removeAttribute(DROPPING_ATTRIBUTE);
						animation?.finish();
						cleanup();
						requestAnimationFrame(restoreFocus);
					});
				}
			};
			manager.renderer.rendering.then(dropAnimation);
		}
	});
	return () => {
		cleanup();
		cleanupEffects();
	};
};
injectStyles_fn = function() {
	var _a4, _b2, _c3;
	const { status, source, target } = this.manager.dragOperation;
	const { nonce } = (_a4 = this.options) != null ? _a4 : {};
	if (status.initializing) {
		const sourceDocument = getDocument((_b2 = source == null ? void 0 : source.element) != null ? _b2 : null);
		const targetDocument = getDocument((_c3 = target == null ? void 0 : target.element) != null ? _c3 : null);
		const documents = /* @__PURE__ */ new Set([sourceDocument, targetDocument]);
		for (const doc of documents) {
			let registration = styleSheetRegistry.get(doc);
			if (!registration) {
				const style = document.createElement("style");
				style.textContent = CSS_RULES;
				if (nonce) style.setAttribute("nonce", nonce);
				doc.head.prepend(style);
				const mutationObserver = new MutationObserver((entries) => {
					for (const entry of entries) if (entry.type === "childList") {
						const removedNodes = Array.from(entry.removedNodes);
						if (removedNodes.length > 0 && removedNodes.includes(style)) doc.head.prepend(style);
					}
				});
				mutationObserver.observe(doc.head, { childList: true });
				registration = {
					cleanup: () => {
						mutationObserver.disconnect();
						style.remove();
					},
					instances: /* @__PURE__ */ new Set()
				};
				styleSheetRegistry.set(doc, registration);
			}
			registration.instances.add(this);
		}
	}
};
__decorateElement$2(_init$2, 4, "overlay", _overlay_dec, _Feedback, _overlay);
__decoratorMetadata$2(_init$2, _Feedback);
_Feedback.configure = configurator(_Feedback);
var Feedback = _Feedback;
var LOCKED = true;
var UNLOCKED = false;
var _dec, _a2, _dec2, _b = (_dec2 = [reactive], ScrollDirection.Forward), _init2, __b, __a;
_a2 = (_dec = [reactive], ScrollDirection.Reverse);
var ScrollLock = class {
	constructor() {
		__privateAdd$2(this, __b, __runInitializers$2(_init2, 8, this, LOCKED)), __runInitializers$2(_init2, 11, this);
		__privateAdd$2(this, __a, __runInitializers$2(_init2, 12, this, LOCKED)), __runInitializers$2(_init2, 15, this);
	}
	isLocked(direction) {
		if (direction === ScrollDirection.Idle) return false;
		if (direction == null) return this[ScrollDirection.Forward] === LOCKED && this[ScrollDirection.Reverse] === LOCKED;
		return this[direction] === LOCKED;
	}
	unlock(direction) {
		if (direction === ScrollDirection.Idle) return;
		this[direction] = UNLOCKED;
	}
};
_init2 = __decoratorStart$2(null);
__b = /* @__PURE__ */ new WeakMap();
__a = /* @__PURE__ */ new WeakMap();
__decorateElement$2(_init2, 4, _b, _dec2, ScrollLock, __b);
__decorateElement$2(_init2, 4, _a2, _dec, ScrollLock, __a);
__decoratorMetadata$2(_init2, ScrollLock);
var DIRECTIONS = [ScrollDirection.Forward, ScrollDirection.Reverse];
var ScrollIntent = class {
	constructor() {
		this.x = new ScrollLock();
		this.y = new ScrollLock();
	}
	isLocked() {
		return this.x.isLocked() && this.y.isLocked();
	}
};
var ScrollIntentTracker = class extends Plugin {
	constructor(manager) {
		super(manager);
		const scrollIntent = y(new ScrollIntent());
		let previousDelta = null;
		this.signal = scrollIntent;
		j(() => {
			const { status } = manager.dragOperation;
			if (!status.initialized) {
				previousDelta = null;
				scrollIntent.value = new ScrollIntent();
				return;
			}
			const { delta } = manager.dragOperation.position;
			if (previousDelta) {
				const directions = {
					x: getDirection$2(delta.x, previousDelta.x),
					y: getDirection$2(delta.y, previousDelta.y)
				};
				const intent = scrollIntent.peek();
				n$1(() => {
					for (const axis of Axes) for (const direction of DIRECTIONS) if (directions[axis] === direction) intent[axis].unlock(direction);
					scrollIntent.value = intent;
				});
			}
			previousDelta = delta;
		});
	}
	get current() {
		return this.signal.peek();
	}
};
function getDirection$2(a, b) {
	return Math.sign(a - b);
}
var _autoScrolling_dec, _a3, _init3, _autoScrolling, _meta, _scroll;
var Scroller = class extends (_a3 = CorePlugin, _autoScrolling_dec = [reactive], _a3) {
	constructor(manager) {
		super(manager);
		__privateAdd$2(this, _autoScrolling, __runInitializers$2(_init3, 8, this, false)), __runInitializers$2(_init3, 11, this);
		__privateAdd$2(this, _meta);
		__privateAdd$2(this, _scroll, () => {
			if (!__privateGet$2(this, _meta)) return;
			const { element, by } = __privateGet$2(this, _meta);
			if (by.y) element.scrollTop += by.y;
			if (by.x) element.scrollLeft += by.x;
		});
		this.scroll = (options) => {
			var _a4;
			if (this.disabled) return false;
			const elements = this.getScrollableElements();
			if (!elements) {
				__privateSet$2(this, _meta, void 0);
				return false;
			}
			const { position } = this.manager.dragOperation;
			const currentPosition = position == null ? void 0 : position.current;
			if (currentPosition) {
				const { by } = options != null ? options : {};
				const intent = by ? {
					x: getScrollIntent(by.x),
					y: getScrollIntent(by.y)
				} : void 0;
				const scrollIntent = intent ? void 0 : this.scrollIntentTracker.current;
				if (scrollIntent == null ? void 0 : scrollIntent.isLocked()) return false;
				for (const scrollableElement of elements) {
					const elementCanScroll = canScroll(scrollableElement, by);
					if (elementCanScroll.x || elementCanScroll.y) {
						const { speed, direction } = detectScrollIntent(scrollableElement, currentPosition, intent);
						if (scrollIntent) {
							for (const axis of Axes) if (scrollIntent[axis].isLocked(direction[axis])) {
								speed[axis] = 0;
								direction[axis] = 0;
							}
						}
						if (direction.x || direction.y) {
							const { x, y } = by != null ? by : direction;
							const scrollLeftBy = x * speed.x;
							const scrollTopBy = y * speed.y;
							if (scrollLeftBy || scrollTopBy) {
								const previousScrollBy = (_a4 = __privateGet$2(this, _meta)) == null ? void 0 : _a4.by;
								if (this.autoScrolling && previousScrollBy) {
									if (previousScrollBy.x && !scrollLeftBy || previousScrollBy.y && !scrollTopBy) continue;
								}
								__privateSet$2(this, _meta, {
									element: scrollableElement,
									by: {
										x: scrollLeftBy,
										y: scrollTopBy
									}
								});
								scheduler.schedule(__privateGet$2(this, _scroll));
								return true;
							}
						}
					}
				}
			}
			__privateSet$2(this, _meta, void 0);
			return false;
		};
		let previousElementFromPoint = null;
		let previousScrollableElements = null;
		const elementFromPoint = computed(() => {
			const { position, source } = manager.dragOperation;
			if (!position) return null;
			const element = getElementFromPoint(getDocument(source == null ? void 0 : source.element), position.current);
			if (element) previousElementFromPoint = element;
			return element != null ? element : previousElementFromPoint;
		});
		const scrollableElements = computed(() => {
			const element = elementFromPoint.value;
			const { documentElement } = getDocument(element);
			if (!element || element === documentElement) {
				const { target } = manager.dragOperation;
				const targetElement = target == null ? void 0 : target.element;
				if (targetElement) {
					const elements = getScrollableAncestors(targetElement, { excludeElement: false });
					previousScrollableElements = elements;
					return elements;
				}
			}
			if (element) {
				const elements = getScrollableAncestors(element, { excludeElement: false });
				if (this.autoScrolling && previousScrollableElements && elements.size < (previousScrollableElements == null ? void 0 : previousScrollableElements.size)) return previousScrollableElements;
				previousScrollableElements = elements;
				return elements;
			}
			previousScrollableElements = null;
			return null;
		}, deepEqual);
		this.getScrollableElements = () => {
			return scrollableElements.value;
		};
		this.scrollIntentTracker = new ScrollIntentTracker(manager);
		this.destroy = manager.monitor.addEventListener("dragmove", (event) => {
			if (this.disabled || event.defaultPrevented || !isKeyboardEvent(manager.dragOperation.activatorEvent) || !event.by) return;
			if (this.scroll({ by: event.by })) event.preventDefault();
		});
	}
};
_init3 = __decoratorStart$2(_a3);
_autoScrolling = /* @__PURE__ */ new WeakMap();
_meta = /* @__PURE__ */ new WeakMap();
_scroll = /* @__PURE__ */ new WeakMap();
__decorateElement$2(_init3, 4, "autoScrolling", _autoScrolling_dec, Scroller, _autoScrolling);
__decoratorMetadata$2(_init3, Scroller);
function getScrollIntent(value) {
	if (value > 0) return ScrollDirection.Forward;
	if (value < 0) return ScrollDirection.Reverse;
	return ScrollDirection.Idle;
}
var Scheduler = class {
	constructor(scheduler5) {
		this.scheduler = scheduler5;
		this.pending = false;
		this.tasks = /* @__PURE__ */ new Set();
		this.resolvers = /* @__PURE__ */ new Set();
		this.flush = () => {
			const { tasks, resolvers } = this;
			this.pending = false;
			this.tasks = /* @__PURE__ */ new Set();
			this.resolvers = /* @__PURE__ */ new Set();
			for (const task of tasks) task();
			for (const resolve of resolvers) resolve();
		};
	}
	schedule(task) {
		this.tasks.add(task);
		if (!this.pending) {
			this.pending = true;
			this.scheduler(this.flush);
		}
		return new Promise((resolve) => this.resolvers.add(resolve));
	}
};
var scheduler3 = new Scheduler((callback) => {
	if (typeof requestAnimationFrame === "function") requestAnimationFrame(callback);
	else callback();
});
var AUTOSCROLL_INTERVAL = 10;
var AutoScroller = class extends Plugin {
	constructor(manager, _options) {
		super(manager);
		const scroller = manager.registry.plugins.get(Scroller);
		if (!scroller) throw new Error("AutoScroller plugin depends on Scroller plugin");
		this.destroy = j(() => {
			if (this.disabled) return;
			const { position: _, status } = manager.dragOperation;
			if (status.dragging) if (scroller.scroll()) {
				scroller.autoScrolling = true;
				const interval = setInterval(() => scheduler3.schedule(scroller.scroll), AUTOSCROLL_INTERVAL);
				return () => {
					clearInterval(interval);
				};
			} else scroller.autoScrolling = false;
		});
	}
};
var listenerOptions = {
	capture: true,
	passive: true
};
var _timeout;
var ScrollListener = class extends CorePlugin {
	constructor(manager) {
		super(manager);
		__privateAdd$2(this, _timeout);
		this.handleScroll = () => {
			if (__privateGet$2(this, _timeout) == null) __privateSet$2(this, _timeout, setTimeout(() => {
				this.manager.collisionObserver.forceUpdate(false);
				__privateSet$2(this, _timeout, void 0);
			}, 50));
		};
		const { dragOperation } = this.manager;
		this.destroy = j(() => {
			var _a4, _b2, _c3;
			if (dragOperation.status.dragging) {
				const root = (_c3 = (_b2 = (_a4 = dragOperation.source) == null ? void 0 : _a4.element) == null ? void 0 : _b2.ownerDocument) != null ? _c3 : document;
				root.addEventListener("scroll", this.handleScroll, listenerOptions);
				return () => {
					root.removeEventListener("scroll", this.handleScroll, listenerOptions);
				};
			}
		});
	}
};
_timeout = /* @__PURE__ */ new WeakMap();
var PreventSelection = class extends Plugin {
	constructor(manager, options) {
		super(manager, options);
		this.manager = manager;
		this.destroy = j(() => {
			var _a4;
			const { dragOperation } = this.manager;
			const { nonce } = (_a4 = this.options) != null ? _a4 : {};
			if (dragOperation.status.initialized) {
				const style = document.createElement("style");
				if (nonce) style.setAttribute("nonce", nonce);
				style.textContent = `* { user-select: none !important; -webkit-user-select: none !important; }`;
				document.head.appendChild(style);
				removeSelection();
				document.addEventListener("selectionchange", removeSelection, { capture: true });
				return () => {
					document.removeEventListener("selectionchange", removeSelection, { capture: true });
					style.remove();
				};
			}
		});
	}
};
function removeSelection() {
	var _a4;
	(_a4 = document.getSelection()) == null || _a4.removeAllRanges();
}
var defaults = Object.freeze({
	offset: 10,
	keyboardCodes: {
		start: ["Space", "Enter"],
		cancel: ["Escape"],
		end: [
			"Space",
			"Enter",
			"Tab"
		],
		up: ["ArrowUp"],
		down: ["ArrowDown"],
		left: ["ArrowLeft"],
		right: ["ArrowRight"]
	},
	shouldActivate(args) {
		var _a4;
		const { event, source } = args;
		const target = (_a4 = source.handle) != null ? _a4 : source.element;
		return event.target === target;
	}
});
var _cleanupFunctions;
var _KeyboardSensor = class _KeyboardSensor extends Sensor {
	constructor(manager, options) {
		super(manager);
		this.manager = manager;
		this.options = options;
		__privateAdd$2(this, _cleanupFunctions, []);
		this.listeners = new Listeners();
		this.handleSourceKeyDown = (event, source, options) => {
			if (this.disabled || event.defaultPrevented) return;
			if (!isElement(event.target)) return;
			if (source.disabled) return;
			const { keyboardCodes = defaults.keyboardCodes, shouldActivate = defaults.shouldActivate } = options != null ? options : {};
			if (!keyboardCodes.start.includes(event.code)) return;
			if (!this.manager.dragOperation.status.idle) return;
			if (shouldActivate({
				event,
				source,
				manager: this.manager
			})) this.handleStart(event, source, options);
		};
	}
	bind(source, options = this.options) {
		return j(() => {
			var _a4;
			const target = (_a4 = source.handle) != null ? _a4 : source.element;
			const listener = (event) => {
				if (isKeyboardEvent(event)) this.handleSourceKeyDown(event, source, options);
			};
			if (target) {
				target.addEventListener("keydown", listener);
				return () => {
					target.removeEventListener("keydown", listener);
				};
			}
		});
	}
	handleStart(event, source, options) {
		const { element } = source;
		if (!element) throw new Error("Source draggable does not have an associated element");
		event.preventDefault();
		event.stopImmediatePropagation();
		scrollIntoViewIfNeeded(element);
		const { center } = new DOMRectangle(element);
		if (this.manager.actions.start({
			event,
			coordinates: {
				x: center.x,
				y: center.y
			},
			source
		}).signal.aborted) return this.cleanup();
		this.sideEffects();
		const sourceDocument = getDocument(element);
		const listeners = [this.listeners.bind(sourceDocument, [{
			type: "keydown",
			listener: (event2) => this.handleKeyDown(event2, source, options),
			options: { capture: true }
		}])];
		__privateGet$2(this, _cleanupFunctions).push(...listeners);
	}
	handleKeyDown(event, _source, options) {
		const { keyboardCodes = defaults.keyboardCodes } = options != null ? options : {};
		if (isKeycode(event, [...keyboardCodes.end, ...keyboardCodes.cancel])) {
			event.preventDefault();
			const canceled = isKeycode(event, keyboardCodes.cancel);
			this.handleEnd(event, canceled);
			return;
		}
		if (isKeycode(event, keyboardCodes.up)) this.handleMove("up", event);
		else if (isKeycode(event, keyboardCodes.down)) this.handleMove("down", event);
		if (isKeycode(event, keyboardCodes.left)) this.handleMove("left", event);
		else if (isKeycode(event, keyboardCodes.right)) this.handleMove("right", event);
	}
	handleEnd(event, canceled) {
		this.manager.actions.stop({
			event,
			canceled
		});
		this.cleanup();
	}
	handleMove(direction, event) {
		var _a4, _b2;
		const { shape } = this.manager.dragOperation;
		const factor = event.shiftKey ? 5 : 1;
		let by = {
			x: 0,
			y: 0
		};
		let offset = (_b2 = (_a4 = this.options) == null ? void 0 : _a4.offset) != null ? _b2 : defaults.offset;
		if (typeof offset === "number") offset = {
			x: offset,
			y: offset
		};
		if (!shape) return;
		switch (direction) {
			case "up":
				by = {
					x: 0,
					y: -offset.y * factor
				};
				break;
			case "down":
				by = {
					x: 0,
					y: offset.y * factor
				};
				break;
			case "left":
				by = {
					x: -offset.x * factor,
					y: 0
				};
				break;
			case "right":
				by = {
					x: offset.x * factor,
					y: 0
				};
				break;
		}
		if (by.x || by.y) {
			event.preventDefault();
			this.manager.actions.move({
				event,
				by
			});
		}
	}
	sideEffects() {
		const autoScroller = this.manager.registry.plugins.get(AutoScroller);
		if ((autoScroller == null ? void 0 : autoScroller.disabled) === false) {
			autoScroller.disable();
			__privateGet$2(this, _cleanupFunctions).push(() => {
				autoScroller.enable();
			});
		}
	}
	cleanup() {
		__privateGet$2(this, _cleanupFunctions).forEach((cleanup) => cleanup());
		__privateSet$2(this, _cleanupFunctions, []);
	}
	destroy() {
		this.cleanup();
		this.listeners.clear();
	}
};
_cleanupFunctions = /* @__PURE__ */ new WeakMap();
_KeyboardSensor.configure = configurator(_KeyboardSensor);
_KeyboardSensor.defaults = defaults;
var KeyboardSensor = _KeyboardSensor;
function isKeycode(event, codes) {
	return codes.includes(event.code);
}
var defaults2 = Object.freeze({ activationConstraints(event, source) {
	var _a4;
	const { pointerType, target } = event;
	if (pointerType === "mouse" && isElement(target) && (source.handle === target || ((_a4 = source.handle) == null ? void 0 : _a4.contains(target)))) return;
	if (pointerType === "touch") return { delay: {
		value: 250,
		tolerance: 5
	} };
	if (isTextInput(target) && !event.defaultPrevented) return { delay: {
		value: 200,
		tolerance: 0
	} };
	return {
		delay: {
			value: 200,
			tolerance: 10
		},
		distance: { value: 5 }
	};
} });
var _cleanup, _clearTimeout;
var _PointerSensor = class _PointerSensor extends Sensor {
	constructor(manager, options) {
		super(manager);
		this.manager = manager;
		this.options = options;
		__privateAdd$2(this, _cleanup, /* @__PURE__ */ new Set());
		__privateAdd$2(this, _clearTimeout);
		this.listeners = new Listeners();
		this.latest = {
			event: void 0,
			coordinates: void 0
		};
		this.handleMove = () => {
			const { event, coordinates: to } = this.latest;
			if (!event || !to) return;
			this.manager.actions.move({
				event,
				to
			});
		};
		this.handleCancel = this.handleCancel.bind(this);
		this.handlePointerUp = this.handlePointerUp.bind(this);
		this.handleKeyDown = this.handleKeyDown.bind(this);
	}
	activationConstraints(event, source) {
		var _a4;
		const { activationConstraints = defaults2.activationConstraints } = (_a4 = this.options) != null ? _a4 : {};
		return typeof activationConstraints === "function" ? activationConstraints(event, source) : activationConstraints;
	}
	bind(source, options = this.options) {
		return j(() => {
			var _a4;
			const controller = new AbortController();
			const { signal: signal3 } = controller;
			const listener = (event) => {
				if (isPointerEvent(event)) this.handlePointerDown(event, source, options);
			};
			let targets = [(_a4 = source.handle) != null ? _a4 : source.element];
			if (options == null ? void 0 : options.activatorElements) if (Array.isArray(options.activatorElements)) targets = options.activatorElements;
			else targets = options.activatorElements(source);
			for (const target of targets) {
				if (!target) continue;
				patchWindow(target.ownerDocument.defaultView);
				target.addEventListener("pointerdown", listener, { signal: signal3 });
			}
			return () => controller.abort();
		});
	}
	handlePointerDown(event, source, options = {}) {
		if (this.disabled || !event.isPrimary || event.button !== 0 || !isElement(event.target) || source.disabled || isCapturedBySensor(event) || !this.manager.dragOperation.status.idle) return;
		const { target } = event;
		const isNativeDraggable = isHTMLElement(target) && target.draggable && target.getAttribute("draggable") === "true";
		const offset = getFrameTransform(source.element);
		this.initialCoordinates = {
			x: event.clientX * offset.scaleX + offset.x,
			y: event.clientY * offset.scaleY + offset.y
		};
		const constraints = this.activationConstraints(event, source);
		event.sensor = this;
		if (!(constraints == null ? void 0 : constraints.delay) && !(constraints == null ? void 0 : constraints.distance)) this.handleStart(source, event);
		else {
			const { delay } = constraints;
			if (delay) {
				const timeout = setTimeout(() => this.handleStart(source, event), delay.value);
				__privateSet$2(this, _clearTimeout, () => {
					clearTimeout(timeout);
					__privateSet$2(this, _clearTimeout, void 0);
				});
			}
		}
		const ownerDocument = getDocument(event.target);
		const unbindListeners = this.listeners.bind(ownerDocument, [
			{
				type: "pointermove",
				listener: (event2) => this.handlePointerMove(event2, source)
			},
			{
				type: "pointerup",
				listener: this.handlePointerUp,
				options: { capture: true }
			},
			{
				type: "dragstart",
				listener: isNativeDraggable ? this.handleCancel : preventDefault,
				options: { capture: true }
			}
		]);
		const cleanup = () => {
			var _a4;
			unbindListeners();
			(_a4 = __privateGet$2(this, _clearTimeout)) == null || _a4.call(this);
			this.initialCoordinates = void 0;
		};
		__privateGet$2(this, _cleanup).add(cleanup);
	}
	handlePointerMove(event, source) {
		const coordinates = {
			x: event.clientX,
			y: event.clientY
		};
		const offset = getFrameTransform(source.element);
		coordinates.x = coordinates.x * offset.scaleX + offset.x;
		coordinates.y = coordinates.y * offset.scaleY + offset.y;
		if (this.manager.dragOperation.status.dragging) {
			event.preventDefault();
			event.stopPropagation();
			this.latest.event = event;
			this.latest.coordinates = coordinates;
			scheduler.schedule(this.handleMove);
			return;
		}
		if (!this.initialCoordinates) return;
		const delta = {
			x: coordinates.x - this.initialCoordinates.x,
			y: coordinates.y - this.initialCoordinates.y
		};
		const constraints = this.activationConstraints(event, source);
		const { distance, delay } = constraints != null ? constraints : {};
		if (distance) {
			if (distance.tolerance != null && exceedsDistance(delta, distance.tolerance)) return this.handleCancel(event);
			if (exceedsDistance(delta, distance.value)) return this.handleStart(source, event);
		}
		if (delay) {
			if (exceedsDistance(delta, delay.tolerance)) return this.handleCancel(event);
		}
	}
	handlePointerUp(event) {
		const { status } = this.manager.dragOperation;
		if (!status.idle) {
			event.preventDefault();
			event.stopPropagation();
			const canceled = !status.initialized;
			this.manager.actions.stop({
				event,
				canceled
			});
		}
		this.cleanup();
	}
	handleKeyDown(event) {
		if (event.key === "Escape") {
			event.preventDefault();
			this.handleCancel(event);
		}
	}
	handleStart(source, event) {
		var _a4;
		const { manager, initialCoordinates } = this;
		(_a4 = __privateGet$2(this, _clearTimeout)) == null || _a4.call(this);
		if (!initialCoordinates || !manager.dragOperation.status.idle) return;
		if (event.defaultPrevented) return;
		if (manager.actions.start({
			coordinates: initialCoordinates,
			event,
			source
		}).signal.aborted) return this.cleanup();
		event.preventDefault();
		const ownerDocument = getDocument(event.target);
		const pointerCaptureTarget = ownerDocument.body;
		pointerCaptureTarget.setPointerCapture(event.pointerId);
		const unbind = this.listeners.bind(ownerDocument, [
			{
				type: "touchmove",
				listener: preventDefault,
				options: { passive: false }
			},
			{
				type: "click",
				listener: preventDefault
			},
			{
				type: "contextmenu",
				listener: preventDefault
			},
			{
				type: "keydown",
				listener: this.handleKeyDown
			},
			{
				type: "lostpointercapture",
				listener: (event2) => {
					if (event2.target !== pointerCaptureTarget) return;
					this.handlePointerUp(event2);
				}
			}
		]);
		__privateGet$2(this, _cleanup).add(unbind);
	}
	handleCancel(event) {
		const { dragOperation } = this.manager;
		if (dragOperation.status.initialized) this.manager.actions.stop({
			event,
			canceled: true
		});
		this.cleanup();
	}
	cleanup() {
		this.latest = {
			event: void 0,
			coordinates: void 0
		};
		__privateGet$2(this, _cleanup).forEach((cleanup) => cleanup());
		__privateGet$2(this, _cleanup).clear();
	}
	destroy() {
		this.cleanup();
		this.listeners.clear();
	}
};
_cleanup = /* @__PURE__ */ new WeakMap();
_clearTimeout = /* @__PURE__ */ new WeakMap();
_PointerSensor.configure = configurator(_PointerSensor);
_PointerSensor.defaults = defaults2;
var PointerSensor = _PointerSensor;
function isCapturedBySensor(event) {
	return "sensor" in event;
}
function preventDefault(event) {
	event.preventDefault();
}
function noop() {}
var windows = /* @__PURE__ */ new WeakSet();
function patchWindow(window) {
	if (!window || windows.has(window)) return;
	window.addEventListener("touchmove", noop, {
		capture: false,
		passive: false
	});
	windows.add(window);
}
var defaultPreset = {
	modifiers: [],
	plugins: [
		Accessibility,
		AutoScroller,
		Cursor,
		Feedback,
		PreventSelection
	],
	sensors: [PointerSensor, KeyboardSensor]
};
var DragDropManager = class extends DragDropManager$1 {
	constructor(input = {}) {
		const { plugins = defaultPreset.plugins, sensors = defaultPreset.sensors, modifiers = [] } = input;
		super(__spreadProps$3(__spreadValues$3({}, input), {
			plugins: [
				ScrollListener,
				Scroller,
				...plugins
			],
			sensors,
			modifiers
		}));
	}
};
var _feedback_dec, _element_dec, _handle_dec, _c, _init4, _handle, _element$1, _feedback;
var Draggable = class extends (_c = Draggable$1, _handle_dec = [reactive], _element_dec = [reactive], _feedback_dec = [reactive], _c) {
	constructor(_a4, manager) {
		var _b2 = _a4, { element, effects: effects2 = () => [], handle, feedback = "default" } = _b2, input = __objRest$2(_b2, [
			"element",
			"effects",
			"handle",
			"feedback"
		]);
		super(__spreadValues$3({ effects: () => [...effects2(), () => {
			var _a5, _b3;
			const { manager: manager2 } = this;
			if (!manager2) return;
			const unbindFunctions = ((_b3 = (_a5 = this.sensors) == null ? void 0 : _a5.map(descriptor)) != null ? _b3 : [...manager2.sensors]).map((entry) => {
				const sensorInstance = entry instanceof Sensor ? entry : manager2.registry.register(entry.plugin);
				const options = entry instanceof Sensor ? void 0 : entry.options;
				return sensorInstance.bind(this, options);
			});
			return function cleanup() {
				unbindFunctions.forEach((unbind) => unbind());
			};
		}] }, input), manager);
		__privateAdd$2(this, _handle, __runInitializers$2(_init4, 8, this)), __runInitializers$2(_init4, 11, this);
		__privateAdd$2(this, _element$1, __runInitializers$2(_init4, 12, this)), __runInitializers$2(_init4, 15, this);
		__privateAdd$2(this, _feedback, __runInitializers$2(_init4, 16, this)), __runInitializers$2(_init4, 19, this);
		this.element = element;
		this.handle = handle;
		this.feedback = feedback;
	}
};
_init4 = __decoratorStart$2(_c);
_handle = /* @__PURE__ */ new WeakMap();
_element$1 = /* @__PURE__ */ new WeakMap();
_feedback = /* @__PURE__ */ new WeakMap();
__decorateElement$2(_init4, 4, "handle", _handle_dec, Draggable, _handle);
__decorateElement$2(_init4, 4, "element", _element_dec, Draggable, _element$1);
__decorateElement$2(_init4, 4, "feedback", _feedback_dec, Draggable, _feedback);
__decoratorMetadata$2(_init4, Draggable);
var _proxy_dec, _element_dec2, _c2, _init5, _element2, _d, element_get, element_set, _Droppable_instances, _proxy;
var Droppable = class extends (_c2 = Droppable$1, _element_dec2 = [reactive], _proxy_dec = [reactive], _c2) {
	constructor(_a4, manager) {
		var _b2 = _a4, { element, effects: effects2 = () => [] } = _b2, input = __objRest$2(_b2, ["element", "effects"]);
		const { collisionDetector = defaultCollisionDetection$1 } = input;
		const updateShape = (boundingClientRect) => {
			const { manager: manager2, element: element2 } = this;
			if (!element2 || boundingClientRect === null) {
				this.shape = void 0;
				return;
			}
			if (!manager2) return;
			const updatedShape = new DOMRectangle(element2);
			const shape = f(() => this.shape);
			if (updatedShape && (shape == null ? void 0 : shape.equals(updatedShape))) return shape;
			this.shape = updatedShape;
			return updatedShape;
		};
		const observePosition = y(false);
		super(__spreadProps$3(__spreadValues$3({}, input), {
			collisionDetector,
			effects: () => [
				...effects2(),
				() => {
					const { element: element2, manager: manager2 } = this;
					if (!manager2) return;
					const { dragOperation } = manager2;
					const { source } = dragOperation;
					observePosition.value = Boolean(source && dragOperation.status.initialized && element2 && !this.disabled && this.accepts(source));
				},
				() => {
					const { element: element2 } = this;
					if (observePosition.value && element2) {
						const positionObserver = new FrameObserver(element2, updateShape);
						return () => {
							positionObserver.disconnect();
							this.shape = void 0;
						};
					}
				},
				() => {
					var _a5;
					if ((_a5 = this.manager) == null ? void 0 : _a5.dragOperation.status.initialized) return () => {
						this.shape = void 0;
					};
				}
			]
		}), manager);
		__privateAdd$2(this, _Droppable_instances);
		__privateAdd$2(this, _element2, __runInitializers$2(_init5, 8, this)), __runInitializers$2(_init5, 11, this);
		__privateAdd$2(this, _proxy, __runInitializers$2(_init5, 12, this)), __runInitializers$2(_init5, 15, this);
		this.element = element;
		this.refreshShape = () => updateShape();
	}
	set element(element) {
		__privateSet$2(this, _Droppable_instances, element, element_set);
	}
	get element() {
		var _a4;
		return (_a4 = this.proxy) != null ? _a4 : __privateGet$2(this, _Droppable_instances, element_get);
	}
};
_init5 = __decoratorStart$2(_c2);
_element2 = /* @__PURE__ */ new WeakMap();
_Droppable_instances = /* @__PURE__ */ new WeakSet();
_proxy = /* @__PURE__ */ new WeakMap();
_d = __decorateElement$2(_init5, 20, "#element", _element_dec2, _Droppable_instances, _element2), element_get = _d.get, element_set = _d.set;
__decorateElement$2(_init5, 4, "proxy", _proxy_dec, Droppable, _proxy);
__decoratorMetadata$2(_init5, Droppable);
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@dnd-kit/react/utilities.js
function isRef(value) {
	return value != null && typeof value === "object" && "current" in value;
}
function currentValue(value) {
	var _a;
	if (value == null) return;
	if (isRef(value)) return (_a = value.current) != null ? _a : void 0;
	return value;
}
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@dnd-kit/react/hooks.js
var useIsomorphicLayoutEffect$1 = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
function useForceUpdate() {
	const setState = (0, import_react.useState)(0)[1];
	return (0, import_react.useCallback)(() => {
		setState((value) => value + 1);
	}, [setState]);
}
function useDeepSignal(target, synchronous) {
	const tracked = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	const forceUpdate = useForceUpdate();
	useIsomorphicLayoutEffect$1(() => {
		if (!target) {
			tracked.current.clear();
			return;
		}
		return j(() => {
			var _a;
			let stale = false;
			let sync = false;
			for (const entry of tracked.current) {
				const [key] = entry;
				const value = f(() => entry[1]);
				const latestValue = target[key];
				if (value !== latestValue) {
					stale = true;
					tracked.current.set(key, latestValue);
					sync = (_a = synchronous == null ? void 0 : synchronous(key, value, latestValue)) != null ? _a : false;
				}
			}
			if (stale) sync ? (0, import_react_dom.flushSync)(forceUpdate) : forceUpdate();
		});
	}, [target]);
	return (0, import_react.useMemo)(() => target ? new Proxy(target, { get(target2, key) {
		const value = target2[key];
		tracked.current.set(key, value);
		return value;
	} }) : target, [target]);
}
function useImmediateEffect(callback, _) {
	callback();
}
function useLatest(value) {
	const valueRef = (0, import_react.useRef)(value);
	useIsomorphicLayoutEffect$1(() => {
		valueRef.current = value;
	}, [value]);
	return valueRef;
}
function useOnValueChange(value, onChange, effect3 = import_react.useEffect, compare = Object.is) {
	const tracked = (0, import_react.useRef)(value);
	effect3(() => {
		const oldValue = tracked.current;
		if (!compare(value, oldValue)) {
			tracked.current = value;
			onChange(value, oldValue);
		}
	}, [onChange, value]);
}
function useOnElementChange(value, onChange) {
	const previous = (0, import_react.useRef)(currentValue(value));
	useIsomorphicLayoutEffect$1(() => {
		const current = currentValue(value);
		if (current !== previous.current) {
			previous.current = current;
			onChange(current);
		}
	});
}
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@dnd-kit/react/index.js
var __defProp$2 = Object.defineProperty;
var __defProps$2 = Object.defineProperties;
var __getOwnPropDescs$2 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols$2 = Object.getOwnPropertySymbols;
var __hasOwnProp$2 = Object.prototype.hasOwnProperty;
var __propIsEnum$2 = Object.prototype.propertyIsEnumerable;
var __defNormalProp$2 = (obj, key, value) => key in obj ? __defProp$2(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues$2 = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp$2.call(b, prop)) __defNormalProp$2(a, prop, b[prop]);
	if (__getOwnPropSymbols$2) {
		for (var prop of __getOwnPropSymbols$2(b)) if (__propIsEnum$2.call(b, prop)) __defNormalProp$2(a, prop, b[prop]);
	}
	return a;
};
var __spreadProps$2 = (a, b) => __defProps$2(a, __getOwnPropDescs$2(b));
var __objRest$1 = (source, exclude) => {
	var target = {};
	for (var prop in source) if (__hasOwnProp$2.call(source, prop) && exclude.indexOf(prop) < 0) target[prop] = source[prop];
	if (source != null && __getOwnPropSymbols$2) {
		for (var prop of __getOwnPropSymbols$2(source)) if (exclude.indexOf(prop) < 0 && __propIsEnum$2.call(source, prop)) target[prop] = source[prop];
	}
	return target;
};
var DragDropContext$1 = (0, import_react.createContext)(new DragDropManager());
var Renderer = (0, import_react.memo)((0, import_react.forwardRef)(({ children }, ref) => {
	const [transitionCount, setTransitionCount] = (0, import_react.useState)(0);
	const rendering = (0, import_react.useRef)(null);
	const resolver = (0, import_react.useRef)(null);
	const renderer = (0, import_react.useMemo)(() => ({
		renderer: { get rendering() {
			var _a2;
			return (_a2 = rendering.current) != null ? _a2 : Promise.resolve();
		} },
		trackRendering(callback) {
			if (!rendering.current) rendering.current = new Promise((resolve) => {
				resolver.current = resolve;
			});
			(0, import_react.startTransition)(() => {
				callback();
				setTransitionCount((count) => count + 1);
			});
		}
	}), []);
	useIsomorphicLayoutEffect$1(() => {
		var _a2;
		(_a2 = resolver.current) == null || _a2.call(resolver);
		rendering.current = null;
	}, [children, transitionCount]);
	(0, import_react.useImperativeHandle)(ref, () => renderer);
	return null;
}));
var options = [void 0, deepEqual];
function DragDropProvider(_a2) {
	var _b = _a2, { children, onCollision, onBeforeDragStart, onDragStart, onDragMove, onDragOver, onDragEnd } = _b, input = __objRest$1(_b, [
		"children",
		"onCollision",
		"onBeforeDragStart",
		"onDragStart",
		"onDragMove",
		"onDragOver",
		"onDragEnd"
	]);
	var _a3;
	const rendererRef = (0, import_react.useRef)(null);
	const [manager, setManager] = (0, import_react.useState)((_a3 = input.manager) != null ? _a3 : null);
	const { plugins, modifiers, sensors } = input;
	const handleBeforeDragStart = useLatest(onBeforeDragStart);
	const handleDragStart = useLatest(onDragStart);
	const handleDragOver = useLatest(onDragOver);
	const handleDragMove = useLatest(onDragMove);
	const handleDragEnd = useLatest(onDragEnd);
	const handleCollision = useLatest(onCollision);
	(0, import_react.useEffect)(() => {
		var _a4;
		if (!rendererRef.current) throw new Error("Renderer not found");
		const { renderer, trackRendering } = rendererRef.current;
		const manager2 = (_a4 = input.manager) != null ? _a4 : new DragDropManager(input);
		manager2.renderer = renderer;
		manager2.monitor.addEventListener("beforedragstart", (event) => {
			const callback = handleBeforeDragStart.current;
			if (callback) trackRendering(() => callback(event, manager2));
		});
		manager2.monitor.addEventListener("dragstart", (event) => {
			var _a5;
			return (_a5 = handleDragStart.current) == null ? void 0 : _a5.call(handleDragStart, event, manager2);
		});
		manager2.monitor.addEventListener("dragover", (event) => {
			const callback = handleDragOver.current;
			if (callback) trackRendering(() => callback(event, manager2));
		});
		manager2.monitor.addEventListener("dragmove", (event) => {
			const callback = handleDragMove.current;
			if (callback) trackRendering(() => callback(event, manager2));
		});
		manager2.monitor.addEventListener("dragend", (event) => {
			const callback = handleDragEnd.current;
			if (callback) trackRendering(() => callback(event, manager2));
		});
		manager2.monitor.addEventListener("collision", (event) => {
			var _a5;
			return (_a5 = handleCollision.current) == null ? void 0 : _a5.call(handleCollision, event, manager2);
		});
		(0, import_react.startTransition)(() => setManager(manager2));
		return manager2.destroy;
	}, [input.manager]);
	useOnValueChange(plugins, () => manager && (manager.plugins = plugins != null ? plugins : defaultPreset.plugins), ...options);
	useOnValueChange(sensors, () => manager && (manager.sensors = sensors != null ? sensors : defaultPreset.sensors), ...options);
	useOnValueChange(modifiers, () => manager && (manager.modifiers = modifiers != null ? modifiers : defaultPreset.modifiers), ...options);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DragDropContext$1.Provider, {
		value: manager,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Renderer, {
			ref: rendererRef,
			children
		}), children]
	});
}
function useDragDropManager() {
	return (0, import_react.useContext)(DragDropContext$1);
}
function useInstance(initializer) {
	var _a2;
	const manager = (_a2 = useDragDropManager()) != null ? _a2 : void 0;
	const [instance] = (0, import_react.useState)(() => initializer(manager));
	if (instance.manager !== manager) instance.manager = manager;
	useIsomorphicLayoutEffect$1(instance.register, [manager, instance]);
	return instance;
}
function useDraggable(input) {
	const { disabled, data, element, handle, id, modifiers, sensors } = input;
	const draggable = useInstance((manager) => new Draggable(__spreadProps$2(__spreadValues$2({}, input), {
		register: false,
		handle: currentValue(handle),
		element: currentValue(element)
	}), manager));
	const trackedDraggable = useDeepSignal(draggable, shouldUpdateSynchronously$1);
	useOnValueChange(id, () => draggable.id = id);
	useOnElementChange(handle, (handle2) => draggable.handle = handle2);
	useOnElementChange(element, (element2) => draggable.element = element2);
	useOnValueChange(data, () => data && (draggable.data = data));
	useOnValueChange(disabled, () => draggable.disabled = disabled === true);
	useOnValueChange(sensors, () => draggable.sensors = sensors);
	useOnValueChange(modifiers, () => draggable.modifiers = modifiers, void 0, deepEqual);
	useOnValueChange(input.feedback, () => {
		var _a2;
		return draggable.feedback = (_a2 = input.feedback) != null ? _a2 : "default";
	});
	useOnValueChange(input.alignment, () => draggable.alignment = input.alignment);
	return {
		draggable: trackedDraggable,
		get isDragging() {
			return trackedDraggable.isDragging;
		},
		get isDropping() {
			return trackedDraggable.isDropping;
		},
		get isDragSource() {
			return trackedDraggable.isDragSource;
		},
		handleRef: (0, import_react.useCallback)((element2) => {
			draggable.handle = element2 != null ? element2 : void 0;
		}, [draggable]),
		ref: (0, import_react.useCallback)((element2) => {
			var _a2, _b;
			if (!element2 && ((_a2 = draggable.element) == null ? void 0 : _a2.isConnected) && !((_b = draggable.manager) == null ? void 0 : _b.dragOperation.status.idle)) return;
			draggable.element = element2 != null ? element2 : void 0;
		}, [draggable])
	};
}
function shouldUpdateSynchronously$1(key, oldValue, newValue) {
	if (key === "isDragSource" && !newValue && oldValue) return true;
	return false;
}
var __create$1 = Object.create;
var __defProp2$1 = Object.defineProperty;
var __getOwnPropDesc$1 = Object.getOwnPropertyDescriptor;
var __knownSymbol$1 = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError$1 = (msg) => {
	throw TypeError(msg);
};
var __defNormalProp2$1 = (obj, key, value) => key in obj ? __defProp2$1(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __decoratorStart$1 = (base) => {
	var _a2;
	return [
		,
		,
		,
		__create$1((_a2 = base == null ? void 0 : base[__knownSymbol$1("metadata")]) != null ? _a2 : null)
	];
};
var __decoratorStrings$1 = [
	"class",
	"method",
	"getter",
	"setter",
	"accessor",
	"field",
	"value",
	"get",
	"set"
];
var __expectFn$1 = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError$1("Function expected") : fn;
var __decoratorContext$1 = (kind, name, done, metadata, fns) => ({
	kind: __decoratorStrings$1[kind],
	name,
	metadata,
	addInitializer: (fn) => done._ ? __typeError$1("Already initialized") : fns.push(__expectFn$1(fn || null))
});
var __decoratorMetadata$1 = (array, target) => __defNormalProp2$1(target, __knownSymbol$1("metadata"), array[3]);
var __runInitializers$1 = (array, flags, self, value) => {
	for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) fns[i].call(self);
	return value;
};
var __decorateElement$1 = (array, flags, name, decorators, target, extra) => {
	var it, done, ctx, access, k = flags & 7, s = false, p = false;
	var j = 2, key = __decoratorStrings$1[k + 5];
	var extraInitializers = array[j] || (array[j] = []);
	var desc = (target = target.prototype, __getOwnPropDesc$1(target, name));
	for (var i = decorators.length - 1; i >= 0; i--) {
		ctx = __decoratorContext$1(k, name, done = {}, array[3], extraInitializers);
		ctx.static = s, ctx.private = p, access = ctx.access = { has: (x) => name in x };
		access.get = (x) => x[name];
		it = (0, decorators[i])(desc[key], ctx), done._ = 1;
		__expectFn$1(it) && (desc[key] = it);
	}
	return desc && __defProp2$1(target, name, desc), target;
};
var __accessCheck$1 = (obj, member, msg) => member.has(obj) || __typeError$1("Cannot " + msg);
var __privateGet$1 = (obj, member, getter) => (__accessCheck$1(obj, member, "read from private field"), member.get(obj));
var __privateAdd$1 = (obj, member, value) => member.has(obj) ? __typeError$1("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet$1 = (obj, member, value, setter) => (__accessCheck$1(obj, member, "write to private field"), member.set(obj, value), value);
var Point = class _Point {
	/**
	* @param {number} Coordinate of the point on the horizontal axis
	* @param {number} Coordinate of the point on the vertical axis
	*/
	constructor(x, y) {
		this.x = x;
		this.y = y;
	}
	/**
	* Returns the delta between this point and another point.
	*
	* @param {Point} a - A point
	* @param {Point} b - Another point
	*/
	static delta(a, b) {
		return new _Point(a.x - b.x, a.y - b.y);
	}
	/**
	* Returns the distance (hypotenuse) between this point and another point.
	*
	* @param {Point} a - A point
	* @param {Point} b - Another point
	*/
	static distance(a, b) {
		return Math.hypot(a.x - b.x, a.y - b.y);
	}
	/**
	* Returns true if both points are equal.
	*
	* @param {Point} a - A point
	* @param {Point} b - Another point
	*/
	static equals(a, b) {
		return a.x === b.x && a.y === b.y;
	}
	static from({ x, y }) {
		return new _Point(x, y);
	}
};
var _direction_dec;
var _delta_dec;
var _a;
var _timestamp;
var _init$1;
var Position = class extends (_a = ValueHistory, _delta_dec = [derived], _direction_dec = [derived], _a) {
	constructor(initialValue) {
		const point = Point.from(initialValue);
		super(point, (a, b) => Point.equals(a, b));
		__runInitializers$1(_init$1, 5, this);
		__privateAdd$1(this, _timestamp, 0);
		this.velocity = {
			x: 0,
			y: 0
		};
	}
	get delta() {
		return Point.delta(this.current, this.initial);
	}
	get direction() {
		const { current, previous } = this;
		if (!previous) return null;
		const delta = {
			x: current.x - previous.x,
			y: current.y - previous.y
		};
		if (!delta.x && !delta.y) return null;
		if (Math.abs(delta.x) > Math.abs(delta.y)) return delta.x > 0 ? "right" : "left";
		return delta.y > 0 ? "down" : "up";
	}
	get current() {
		return super.current;
	}
	set current(coordinates) {
		const { current } = this;
		const point = Point.from(coordinates);
		const delta = {
			x: point.x - current.x,
			y: point.y - current.y
		};
		const timestamp = Date.now();
		const timeDelta = timestamp - __privateGet$1(this, _timestamp);
		const velocity = (delta2) => Math.round(delta2 / timeDelta * 100);
		n$1(() => {
			__privateSet$1(this, _timestamp, timestamp);
			this.velocity = {
				x: velocity(delta.x),
				y: velocity(delta.y)
			};
			super.current = point;
		});
	}
	reset(coordinates = this.defaultValue) {
		super.reset(Point.from(coordinates));
		this.velocity = {
			x: 0,
			y: 0
		};
	}
};
_init$1 = __decoratorStart$1(_a);
_timestamp = /* @__PURE__ */ new WeakMap();
__decorateElement$1(_init$1, 2, "delta", _delta_dec, Position);
__decorateElement$1(_init$1, 2, "direction", _direction_dec, Position);
__decoratorMetadata$1(_init$1, Position);
var Axis = /* @__PURE__ */ ((Axis2) => {
	Axis2["Horizontal"] = "x";
	Axis2["Vertical"] = "y";
	return Axis2;
})(Axis || {});
Object.values(Axis);
var pointerIntersection$1 = ({ dragOperation, droppable }) => {
	const pointerCoordinates = dragOperation.position.current;
	if (!pointerCoordinates) return null;
	const { id } = droppable;
	if (!droppable.shape) return null;
	if (droppable.shape.containsPoint(pointerCoordinates)) return {
		id,
		value: 1 / Point.distance(droppable.shape.center, pointerCoordinates),
		type: CollisionType.PointerIntersection,
		priority: CollisionPriority.High
	};
	return null;
};
var shapeIntersection = ({ dragOperation, droppable }) => {
	const { shape } = dragOperation;
	if (!droppable.shape || !(shape == null ? void 0 : shape.current)) return null;
	const intersectionArea = shape.current.intersectionArea(droppable.shape);
	if (intersectionArea) {
		const { position } = dragOperation;
		const distance = Point.distance(droppable.shape.center, position.current);
		const value = intersectionArea / (shape.current.area + droppable.shape.area - intersectionArea) / distance;
		return {
			id: droppable.id,
			value,
			type: CollisionType.ShapeIntersection,
			priority: CollisionPriority.Normal
		};
	}
	return null;
};
var defaultCollisionDetection = (args) => {
	var _a2;
	return (_a2 = pointerIntersection$1(args)) != null ? _a2 : shapeIntersection(args);
};
function useDroppable(input) {
	const { collisionDetector, data, disabled, element, id, accept, type } = input;
	const droppable = useInstance((manager) => new Droppable(__spreadProps$2(__spreadValues$2({}, input), {
		register: false,
		element: currentValue(element)
	}), manager));
	const trackedDroppalbe = useDeepSignal(droppable);
	useOnValueChange(id, () => droppable.id = id);
	useOnElementChange(element, (element2) => droppable.element = element2);
	useOnValueChange(accept, () => droppable.accept = accept, void 0, deepEqual);
	useOnValueChange(collisionDetector, () => droppable.collisionDetector = collisionDetector != null ? collisionDetector : defaultCollisionDetection);
	useOnValueChange(data, () => data && (droppable.data = data));
	useOnValueChange(disabled, () => droppable.disabled = disabled === true);
	useOnValueChange(type, () => droppable.type = type);
	return {
		droppable: trackedDroppalbe,
		get isDropTarget() {
			return trackedDroppalbe.isDropTarget;
		},
		ref: (0, import_react.useCallback)((element2) => {
			var _a2, _b;
			if (!element2 && ((_a2 = droppable.element) == null ? void 0 : _a2.isConnected) && !((_b = droppable.manager) == null ? void 0 : _b.dragOperation.status.idle)) return;
			droppable.element = element2 != null ? element2 : void 0;
		}, [droppable])
	};
}
//#endregion
//#region node_modules/@dnd-kit/dom/sortable.js
var __create = Object.create;
var __defProp$1 = Object.defineProperty;
var __defProps$1 = Object.defineProperties;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs$1 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols$1 = Object.getOwnPropertySymbols;
var __hasOwnProp$1 = Object.prototype.hasOwnProperty;
var __propIsEnum$1 = Object.prototype.propertyIsEnumerable;
var __knownSymbol = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError = (msg) => {
	throw TypeError(msg);
};
var __defNormalProp$1 = (obj, key, value) => key in obj ? __defProp$1(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues$1 = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp$1.call(b, prop)) __defNormalProp$1(a, prop, b[prop]);
	if (__getOwnPropSymbols$1) {
		for (var prop of __getOwnPropSymbols$1(b)) if (__propIsEnum$1.call(b, prop)) __defNormalProp$1(a, prop, b[prop]);
	}
	return a;
};
var __spreadProps$1 = (a, b) => __defProps$1(a, __getOwnPropDescs$1(b));
var __objRest = (source, exclude) => {
	var target = {};
	for (var prop in source) if (__hasOwnProp$1.call(source, prop) && exclude.indexOf(prop) < 0) target[prop] = source[prop];
	if (source != null && __getOwnPropSymbols$1) {
		for (var prop of __getOwnPropSymbols$1(source)) if (exclude.indexOf(prop) < 0 && __propIsEnum$1.call(source, prop)) target[prop] = source[prop];
	}
	return target;
};
var __decoratorStart = (base) => {
	var _a;
	return [
		,
		,
		,
		__create((_a = void 0) != null ? _a : null)
	];
};
var __decoratorStrings = [
	"class",
	"method",
	"getter",
	"setter",
	"accessor",
	"field",
	"value",
	"get",
	"set"
];
var __expectFn = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError("Function expected") : fn;
var __decoratorContext = (kind, name, done, metadata, fns) => ({
	kind: __decoratorStrings[kind],
	name,
	metadata,
	addInitializer: (fn) => done._ ? __typeError("Already initialized") : fns.push(__expectFn(fn || null))
});
var __decoratorMetadata = (array, target) => __defNormalProp$1(target, __knownSymbol("metadata"), array[3]);
var __runInitializers = (array, flags, self, value) => {
	for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
	return value;
};
var __decorateElement = (array, flags, name, decorators, target, extra) => {
	var fn, it, done, ctx, access, k = flags & 7, s = false, p = false;
	var j = array.length + 1, key = __decoratorStrings[k + 5];
	var initializers = array[j - 1] = [], extraInitializers = array[j] || (array[j] = []);
	var desc = (target = target.prototype, __getOwnPropDesc({
		get [name]() {
			return __privateGet(this, extra);
		},
		set [name](x) {
			return __privateSet(this, extra, x);
		}
	}, name));
	for (var i = decorators.length - 1; i >= 0; i--) {
		ctx = __decoratorContext(k, name, done = {}, array[3], extraInitializers);
		ctx.static = s, ctx.private = p, access = ctx.access = { has: (x) => name in x };
		access.get = (x) => x[name];
		access.set = (x, y) => x[name] = y;
		it = (0, decorators[i])({
			get: desc.get,
			set: desc.set
		}, ctx), done._ = 1;
		if (it === void 0) __expectFn(it) && (desc[key] = it);
		else if (typeof it !== "object" || it === null) __typeError("Object expected");
		else __expectFn(fn = it.get) && (desc.get = fn), __expectFn(fn = it.set) && (desc.set = fn), __expectFn(fn = it.init) && initializers.unshift(fn);
	}
	return desc && __defProp$1(target, name, desc), target;
};
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), member.set(obj, value), value);
function isSortable(element) {
	return element instanceof SortableDroppable || element instanceof SortableDraggable;
}
var TOLERANCE = 10;
var SortableKeyboardPlugin = class extends Plugin {
	constructor(manager) {
		super(manager);
		const cleanupEffect = j(() => {
			const { dragOperation } = manager;
			if (!isKeyboardEvent(dragOperation.activatorEvent)) return;
			if (!isSortable(dragOperation.source)) return;
			if (dragOperation.status.initialized) {
				const scroller = manager.registry.plugins.get(Scroller);
				if (scroller) {
					scroller.disable();
					return () => scroller.enable();
				}
			}
		});
		const unsubscribe = manager.monitor.addEventListener("dragmove", (event, manager2) => {
			queueMicrotask(() => {
				if (this.disabled || event.defaultPrevented || !event.nativeEvent) return;
				const { dragOperation } = manager2;
				if (!isKeyboardEvent(event.nativeEvent)) return;
				if (!isSortable(dragOperation.source)) return;
				if (!dragOperation.shape) return;
				const { actions, collisionObserver, registry } = manager2;
				const { by } = event;
				if (!by) return;
				const direction = getDirection$1(by);
				const { source, target } = dragOperation;
				const { center } = dragOperation.shape.current;
				const potentialTargets = [];
				const cleanup = [];
				n$1(() => {
					for (const droppable of registry.droppables) {
						const { id: id2 } = droppable;
						if (!droppable.accepts(source) || id2 === (target == null ? void 0 : target.id) && isSortable(droppable) || !droppable.element) continue;
						let previousShape = droppable.shape;
						const shape = new DOMRectangle(droppable.element, { getBoundingClientRect: (element) => getVisibleBoundingRectangle(element, void 0, .2) });
						if (!shape.height || !shape.width) continue;
						if (direction == "down" && center.y + TOLERANCE < shape.center.y || direction == "up" && center.y - TOLERANCE > shape.center.y || direction == "left" && center.x - TOLERANCE > shape.center.x || direction == "right" && center.x + TOLERANCE < shape.center.x) {
							potentialTargets.push(droppable);
							droppable.shape = shape;
							cleanup.push(() => droppable.shape = previousShape);
						}
					}
				});
				event.preventDefault();
				collisionObserver.disable();
				const collisions = collisionObserver.computeCollisions(potentialTargets, closestCorners$1);
				n$1(() => cleanup.forEach((clean) => clean()));
				const [firstCollision] = collisions;
				if (!firstCollision) return;
				const { id } = firstCollision;
				const { index, group } = source.sortable;
				actions.setDropTarget(id).then(() => {
					const { source: source2, target: target2, shape } = dragOperation;
					if (!source2 || !isSortable(source2) || !shape) return;
					const { index: newIndex, group: newGroup, target: targetElement } = source2.sortable;
					const updated = index !== newIndex || group !== newGroup;
					const element = updated ? targetElement : target2 == null ? void 0 : target2.element;
					if (!element) return;
					scrollIntoViewIfNeeded(element);
					const updatedShape = new DOMRectangle(element);
					if (!updatedShape) return;
					const delta = Rectangle.delta(updatedShape, Rectangle.from(shape.current.boundingRectangle), source2.alignment);
					actions.move({ by: delta });
					if (updated) actions.setDropTarget(source2.id).then(() => collisionObserver.enable());
					else collisionObserver.enable();
				});
			});
		});
		this.destroy = () => {
			unsubscribe();
			cleanupEffect();
		};
	}
};
function getDirection$1(delta) {
	const { x, y } = delta;
	if (x > 0) return "right";
	else if (x < 0) return "left";
	else if (y > 0) return "down";
	else if (y < 0) return "up";
}
var __defProp2 = Object.defineProperty;
var __defProps2 = Object.defineProperties;
var __getOwnPropDescs2 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols2 = Object.getOwnPropertySymbols;
var __hasOwnProp2 = Object.prototype.hasOwnProperty;
var __propIsEnum2 = Object.prototype.propertyIsEnumerable;
var __defNormalProp2 = (obj, key, value) => key in obj ? __defProp2(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues2 = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp2.call(b, prop)) __defNormalProp2(a, prop, b[prop]);
	if (__getOwnPropSymbols2) {
		for (var prop of __getOwnPropSymbols2(b)) if (__propIsEnum2.call(b, prop)) __defNormalProp2(a, prop, b[prop]);
	}
	return a;
};
var __spreadProps2 = (a, b) => __defProps2(a, __getOwnPropDescs2(b));
function arrayMove(array, from, to) {
	if (from === to) return array;
	const newArray = array.slice();
	newArray.splice(to, 0, newArray.splice(from, 1)[0]);
	return newArray;
}
function mutate(items, event, mutation) {
	var _a, _b;
	const { source, target, canceled } = event.operation;
	if (!source || !target || canceled) {
		if ("preventDefault" in event) event.preventDefault();
		return items;
	}
	const findIndex = (item, id) => item === id || typeof item === "object" && "id" in item && item.id === id;
	if (Array.isArray(items)) {
		const sourceIndex2 = items.findIndex((item) => findIndex(item, source.id));
		const targetIndex2 = items.findIndex((item) => findIndex(item, target.id));
		if (sourceIndex2 === -1 || targetIndex2 === -1) return items;
		if (!canceled && "index" in source && typeof source.index === "number") {
			const projectedSourceIndex = source.index;
			if (projectedSourceIndex !== sourceIndex2) return mutation(items, sourceIndex2, projectedSourceIndex);
		}
		return mutation(items, sourceIndex2, targetIndex2);
	}
	const entries = Object.entries(items);
	let sourceIndex = -1;
	let sourceParent;
	let targetIndex = -1;
	let targetParent;
	for (const [id, children] of entries) {
		if (sourceIndex === -1) {
			sourceIndex = children.findIndex((item) => findIndex(item, source.id));
			if (sourceIndex !== -1) sourceParent = id;
		}
		if (targetIndex === -1) {
			targetIndex = children.findIndex((item) => findIndex(item, target.id));
			if (targetIndex !== -1) targetParent = id;
		}
		if (sourceIndex !== -1 && targetIndex !== -1) break;
	}
	if (!source.manager) return items;
	const { dragOperation } = source.manager;
	const position = (_b = (_a = dragOperation.shape) == null ? void 0 : _a.current.center) != null ? _b : dragOperation.position.current;
	if (targetParent == null) {
		if (target.id in items) {
			const insertionIndex = target.shape && position.y > target.shape.center.y ? items[target.id].length : 0;
			targetParent = target.id;
			targetIndex = insertionIndex;
		}
	}
	if (sourceParent == null || targetParent == null || sourceParent === targetParent && sourceIndex === targetIndex) {
		if ("preventDefault" in event) event.preventDefault();
		return items;
	}
	if (sourceParent === targetParent) return __spreadProps2(__spreadValues2({}, items), { [sourceParent]: mutation(items[sourceParent], sourceIndex, targetIndex) });
	const modifier = target.shape && Math.round(position.y) > Math.round(target.shape.center.y) ? 1 : 0;
	const sourceItem = items[sourceParent][sourceIndex];
	return __spreadProps2(__spreadValues2({}, items), {
		[sourceParent]: [...items[sourceParent].slice(0, sourceIndex), ...items[sourceParent].slice(sourceIndex + 1)],
		[targetParent]: [
			...items[targetParent].slice(0, targetIndex + modifier),
			sourceItem,
			...items[targetParent].slice(targetIndex + modifier)
		]
	});
}
function move(items, event) {
	return mutate(items, event, arrayMove);
}
var defaultGroup = "__default__";
var OptimisticSortingPlugin = class extends Plugin {
	constructor(manager) {
		super(manager);
		const getSortableInstances = () => {
			const sortableInstances = /* @__PURE__ */ new Map();
			for (const droppable of manager.registry.droppables) if (droppable instanceof SortableDroppable) {
				const { sortable } = droppable;
				const { group } = sortable;
				let instances = sortableInstances.get(group);
				if (!instances) {
					instances = /* @__PURE__ */ new Set();
					sortableInstances.set(group, instances);
				}
				instances.add(sortable);
			}
			for (const [group, instances] of sortableInstances) sortableInstances.set(group, new Set(sort(instances)));
			return sortableInstances;
		};
		const unsubscribe = [manager.monitor.addEventListener("dragover", (event, manager2) => {
			if (this.disabled) return;
			const { dragOperation } = manager2;
			const { source, target } = dragOperation;
			if (!isSortable(source) || !isSortable(target)) return;
			if (source.sortable === target.sortable) return;
			const instances = getSortableInstances();
			const sameGroup = source.sortable.group === target.sortable.group;
			const sourceInstances = instances.get(source.sortable.group);
			const targetInstances = sameGroup ? sourceInstances : instances.get(target.sortable.group);
			if (!sourceInstances || !targetInstances) return;
			queueMicrotask(() => {
				if (event.defaultPrevented) return;
				manager2.renderer.rendering.then(() => {
					var _a, _b, _c;
					const newInstances = getSortableInstances();
					for (const [group, sortableInstances] of instances.entries()) {
						const entries = Array.from(sortableInstances).entries();
						for (const [index, sortable] of entries) if (sortable.index !== index || sortable.group !== group || !((_a = newInstances.get(group)) == null ? void 0 : _a.has(sortable))) return;
					}
					const sourceElement = source.sortable.element;
					const targetElement = target.sortable.element;
					if (!targetElement || !sourceElement) return;
					if (!sameGroup && target.id === source.sortable.group) return;
					const orderedSourceSortables = sort(sourceInstances);
					const orderedTargetSortables = sameGroup ? orderedSourceSortables : sort(targetInstances);
					const sourceGroup = (_b = source.sortable.group) != null ? _b : defaultGroup;
					const targetGroup = (_c = target.sortable.group) != null ? _c : defaultGroup;
					const state = {
						[sourceGroup]: orderedSourceSortables,
						[targetGroup]: orderedTargetSortables
					};
					const newState = move(state, event);
					if (state === newState) return;
					const sourceIndex = newState[targetGroup].indexOf(source.sortable);
					const targetIndex = newState[targetGroup].indexOf(target.sortable);
					manager2.collisionObserver.disable();
					reorder(sourceElement, sourceIndex, targetElement, targetIndex);
					n$1(() => {
						for (const [index, sortable] of newState[sourceGroup].entries()) sortable.index = index;
						if (!sameGroup) for (const [index, sortable] of newState[targetGroup].entries()) {
							sortable.group = target.sortable.group;
							sortable.index = index;
						}
					});
					manager2.actions.setDropTarget(source.id).then(() => manager2.collisionObserver.enable());
				});
			});
		}), manager.monitor.addEventListener("dragend", (event, manager2) => {
			if (!event.canceled) return;
			const { dragOperation } = manager2;
			const { source } = dragOperation;
			if (!isSortable(source)) return;
			if (source.sortable.initialIndex === source.sortable.index && source.sortable.initialGroup === source.sortable.group) return;
			queueMicrotask(() => {
				const instances = getSortableInstances();
				const initialGroupInstances = instances.get(source.sortable.initialGroup);
				if (!initialGroupInstances) return;
				manager2.renderer.rendering.then(() => {
					for (const [group, sortableInstances] of instances.entries()) {
						const entries = Array.from(sortableInstances).entries();
						for (const [index, sortable] of entries) if (sortable.index !== index || sortable.group !== group) return;
					}
					const initialGroup = sort(initialGroupInstances);
					const sourceElement = source.sortable.element;
					const target = initialGroup[source.sortable.initialIndex];
					const targetElement = target == null ? void 0 : target.element;
					if (!target || !targetElement || !sourceElement) return;
					reorder(sourceElement, target.index, targetElement, source.index);
					n$1(() => {
						for (const [_, sortableInstances] of instances.entries()) {
							const entries = Array.from(sortableInstances).values();
							for (const sortable of entries) {
								sortable.index = sortable.initialIndex;
								sortable.group = sortable.initialGroup;
							}
						}
					});
				});
			});
		})];
		this.destroy = () => {
			for (const unsubscribeListener of unsubscribe) unsubscribeListener();
		};
	}
};
function reorder(sourceElement, sourceIndex, targetElement, targetIndex) {
	const position = targetIndex < sourceIndex ? "afterend" : "beforebegin";
	targetElement.insertAdjacentElement(position, sourceElement);
}
function sortByIndex(a, b) {
	return a.index - b.index;
}
function sort(instances) {
	return Array.from(instances).sort(sortByIndex);
}
var defaultPlugins = [SortableKeyboardPlugin, OptimisticSortingPlugin];
var defaultSortableTransition = {
	duration: 250,
	easing: "cubic-bezier(0.25, 1, 0.5, 1)",
	idle: false
};
var store = new WeakStore();
var _group_dec, _index_dec = [reactive], _init, _index, _previousGroup, _previousIndex, _group, _element;
_group_dec = [reactive];
var Sortable2 = class {
	constructor(_a, manager) {
		__privateAdd(this, _index, __runInitializers(_init, 8, this)), __runInitializers(_init, 11, this);
		__privateAdd(this, _previousGroup);
		__privateAdd(this, _previousIndex);
		__privateAdd(this, _group, __runInitializers(_init, 12, this)), __runInitializers(_init, 15, this);
		__privateAdd(this, _element);
		this.register = () => {
			n$1(() => {
				var _a, _b;
				(_a = this.manager) == null || _a.registry.register(this.droppable);
				(_b = this.manager) == null || _b.registry.register(this.draggable);
			});
			return () => this.unregister();
		};
		this.unregister = () => {
			n$1(() => {
				var _a, _b;
				(_a = this.manager) == null || _a.registry.unregister(this.droppable);
				(_b = this.manager) == null || _b.registry.unregister(this.draggable);
			});
		};
		this.destroy = () => {
			n$1(() => {
				this.droppable.destroy();
				this.draggable.destroy();
			});
		};
		var _b = _a, { effects: inputEffects = () => [], group, index, sensors, type, transition = defaultSortableTransition, plugins = defaultPlugins } = _b, input = __objRest(_b, [
			"effects",
			"group",
			"index",
			"sensors",
			"type",
			"transition",
			"plugins"
		]);
		this.droppable = new SortableDroppable(input, manager, this);
		this.draggable = new SortableDraggable(__spreadProps$1(__spreadValues$1({}, input), {
			effects: () => [
				() => {
					var _a2, _b2, _c;
					const status = (_a2 = this.manager) == null ? void 0 : _a2.dragOperation.status;
					if ((status == null ? void 0 : status.initializing) && this.id === ((_c = (_b2 = this.manager) == null ? void 0 : _b2.dragOperation.source) == null ? void 0 : _c.id)) store.clear(this.manager);
					if (status == null ? void 0 : status.dragging) store.set(this.manager, this.id, f(() => ({
						initialIndex: this.index,
						initialGroup: this.group
					})));
				},
				() => {
					const { index: index2, group: group2, manager: _ } = this;
					const previousIndex = __privateGet(this, _previousIndex);
					const previousGroup = __privateGet(this, _previousGroup);
					if (index2 !== previousIndex || group2 !== previousGroup) {
						__privateSet(this, _previousIndex, index2);
						__privateSet(this, _previousGroup, group2);
						this.animate();
					}
				},
				() => {
					const { target } = this;
					const { feedback, isDragSource } = this.draggable;
					if (feedback == "move" && isDragSource) this.droppable.disabled = !target;
				},
				() => {
					const { manager: manager2 } = this;
					for (const plugin of plugins) manager2?.registry.register(plugin);
				},
				...inputEffects()
			],
			type,
			sensors
		}), manager, this);
		__privateSet(this, _element, input.element);
		this.manager = manager;
		this.index = index;
		__privateSet(this, _previousIndex, index);
		this.group = group;
		__privateSet(this, _previousGroup, group);
		this.type = type;
		this.transition = transition;
	}
	get initialIndex() {
		var _a, _b;
		return (_b = (_a = store.get(this.manager, this.id)) == null ? void 0 : _a.initialIndex) != null ? _b : this.index;
	}
	get initialGroup() {
		var _a, _b;
		return (_b = (_a = store.get(this.manager, this.id)) == null ? void 0 : _a.initialGroup) != null ? _b : this.group;
	}
	animate() {
		f(() => {
			const { manager, transition } = this;
			const { shape } = this.droppable;
			if (!manager) return;
			const { idle } = manager.dragOperation.status;
			if (!shape || !transition || idle && !transition.idle) return;
			manager.renderer.rendering.then(() => {
				const { element } = this;
				if (!element) return;
				const updatedShape = this.refreshShape();
				if (!updatedShape) return;
				const delta = {
					x: shape.boundingRectangle.left - updatedShape.boundingRectangle.left,
					y: shape.boundingRectangle.top - updatedShape.boundingRectangle.top
				};
				const { translate } = getComputedStyles(element);
				const currentTranslate = computeTranslate(element, translate, false);
				const finalTranslate = computeTranslate(element, translate);
				if (delta.x || delta.y) animateTransform({
					element,
					keyframes: { translate: [`${currentTranslate.x + delta.x}px ${currentTranslate.y + delta.y}px ${currentTranslate.z}`, `${finalTranslate.x}px ${finalTranslate.y}px ${finalTranslate.z}`] },
					options: transition
				}).then(() => {
					if (!manager.dragOperation.status.dragging) this.droppable.shape = void 0;
				});
			});
		});
	}
	get manager() {
		return this.draggable.manager;
	}
	set manager(manager) {
		n$1(() => {
			this.draggable.manager = manager;
			this.droppable.manager = manager;
		});
	}
	set element(element) {
		n$1(() => {
			const previousElement = __privateGet(this, _element);
			const droppableElement = this.droppable.element;
			const draggableElement = this.draggable.element;
			if (!droppableElement || droppableElement === previousElement) this.droppable.element = element;
			if (!draggableElement || draggableElement === previousElement) this.draggable.element = element;
			__privateSet(this, _element, element);
		});
	}
	get element() {
		var _a, _b;
		const element = __privateGet(this, _element);
		if (!element) return;
		return (_b = (_a = ProxiedElements.get(element)) != null ? _a : element) != null ? _b : this.droppable.element;
	}
	set target(target) {
		this.droppable.element = target;
	}
	get target() {
		return this.droppable.element;
	}
	set source(source) {
		this.draggable.element = source;
	}
	get source() {
		return this.draggable.element;
	}
	get disabled() {
		return this.draggable.disabled && this.droppable.disabled;
	}
	set feedback(value) {
		this.draggable.feedback = value;
	}
	set disabled(value) {
		n$1(() => {
			this.droppable.disabled = value;
			this.draggable.disabled = value;
		});
	}
	set data(data) {
		n$1(() => {
			this.droppable.data = data;
			this.draggable.data = data;
		});
	}
	set handle(handle) {
		this.draggable.handle = handle;
	}
	set id(id) {
		n$1(() => {
			this.droppable.id = id;
			this.draggable.id = id;
		});
	}
	get id() {
		return this.droppable.id;
	}
	set sensors(value) {
		this.draggable.sensors = value;
	}
	set modifiers(value) {
		this.draggable.modifiers = value;
	}
	set collisionPriority(value) {
		this.droppable.collisionPriority = value;
	}
	set collisionDetector(value) {
		this.droppable.collisionDetector = value != null ? value : defaultCollisionDetection$1;
	}
	set alignment(value) {
		this.draggable.alignment = value;
	}
	get alignment() {
		return this.draggable.alignment;
	}
	set type(type) {
		n$1(() => {
			this.droppable.type = type;
			this.draggable.type = type;
		});
	}
	get type() {
		return this.draggable.type;
	}
	set accept(value) {
		this.droppable.accept = value;
	}
	get accept() {
		return this.droppable.accept;
	}
	get isDropTarget() {
		return this.droppable.isDropTarget;
	}
	/**
	* A boolean indicating whether the sortable item is the source of a drag operation.
	*/
	get isDragSource() {
		return this.draggable.isDragSource;
	}
	/**
	* A boolean indicating whether the sortable item is being dragged.
	*/
	get isDragging() {
		return this.draggable.isDragging;
	}
	/**
	* A boolean indicating whether the sortable item is being dropped.
	*/
	get isDropping() {
		return this.draggable.isDropping;
	}
	get status() {
		return this.draggable.status;
	}
	refreshShape() {
		return this.droppable.refreshShape();
	}
	accepts(draggable) {
		return this.droppable.accepts(draggable);
	}
};
_init = __decoratorStart();
_index = /* @__PURE__ */ new WeakMap();
_previousGroup = /* @__PURE__ */ new WeakMap();
_previousIndex = /* @__PURE__ */ new WeakMap();
_group = /* @__PURE__ */ new WeakMap();
_element = /* @__PURE__ */ new WeakMap();
__decorateElement(_init, 4, "index", _index_dec, Sortable2, _index);
__decorateElement(_init, 4, "group", _group_dec, Sortable2, _group);
__decoratorMetadata(_init, Sortable2);
var SortableDraggable = class extends Draggable {
	constructor(input, manager, sortable) {
		super(input, manager);
		this.sortable = sortable;
	}
	get index() {
		return this.sortable.index;
	}
};
var SortableDroppable = class extends Droppable {
	constructor(input, manager, sortable) {
		super(input, manager);
		this.sortable = sortable;
	}
};
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@dnd-kit/react/sortable.js
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, {
	enumerable: true,
	configurable: true,
	writable: true,
	value
}) : obj[key] = value;
var __spreadValues = (a, b) => {
	for (var prop in b || (b = {})) if (__hasOwnProp.call(b, prop)) __defNormalProp(a, prop, b[prop]);
	if (__getOwnPropSymbols) {
		for (var prop of __getOwnPropSymbols(b)) if (__propIsEnum.call(b, prop)) __defNormalProp(a, prop, b[prop]);
	}
	return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
function useSortable(input) {
	const { accept, collisionDetector, collisionPriority, id, data, element, handle, index, group, disabled, feedback, modifiers, sensors, target, type } = input;
	const transition = __spreadValues(__spreadValues({}, defaultSortableTransition), input.transition);
	const sortable = useInstance((manager) => {
		return new Sortable2(__spreadProps(__spreadValues({}, input), {
			transition,
			register: false,
			handle: currentValue(handle),
			element: currentValue(element),
			target: currentValue(target),
			feedback
		}), manager);
	});
	const trackedSortable = useDeepSignal(sortable, shouldUpdateSynchronously);
	useOnValueChange(id, () => sortable.id = id);
	useIsomorphicLayoutEffect$1(() => {
		n$1(() => {
			sortable.group = group;
			sortable.index = index;
		});
	}, [
		sortable,
		group,
		index
	]);
	useOnValueChange(type, () => sortable.type = type);
	useOnValueChange(accept, () => sortable.accept = accept, void 0, deepEqual);
	useOnValueChange(data, () => data && (sortable.data = data));
	useOnValueChange(index, () => {
		var _a;
		if (((_a = sortable.manager) == null ? void 0 : _a.dragOperation.status.idle) && (transition == null ? void 0 : transition.idle)) sortable.refreshShape();
	}, useImmediateEffect);
	useOnElementChange(handle, (handle2) => sortable.handle = handle2);
	useOnElementChange(element, (element2) => sortable.element = element2);
	useOnElementChange(target, (target2) => sortable.target = target2);
	useOnValueChange(disabled, () => sortable.disabled = disabled === true);
	useOnValueChange(sensors, () => sortable.sensors = sensors);
	useOnValueChange(collisionDetector, () => sortable.collisionDetector = collisionDetector);
	useOnValueChange(collisionPriority, () => sortable.collisionPriority = collisionPriority);
	useOnValueChange(feedback, () => sortable.feedback = feedback != null ? feedback : "default");
	useOnValueChange(transition, () => sortable.transition = transition, void 0, deepEqual);
	useOnValueChange(modifiers, () => sortable.modifiers = modifiers, void 0, deepEqual);
	useOnValueChange(input.alignment, () => sortable.alignment = input.alignment);
	return {
		sortable: trackedSortable,
		get isDragging() {
			return trackedSortable.isDragging;
		},
		get isDropping() {
			return trackedSortable.isDropping;
		},
		get isDragSource() {
			return trackedSortable.isDragSource;
		},
		get isDropTarget() {
			return trackedSortable.isDropTarget;
		},
		handleRef: (0, import_react.useCallback)((element2) => {
			sortable.handle = element2 != null ? element2 : void 0;
		}, [sortable]),
		ref: (0, import_react.useCallback)((element2) => {
			var _a, _b;
			if (!element2 && ((_a = sortable.element) == null ? void 0 : _a.isConnected) && !((_b = sortable.manager) == null ? void 0 : _b.dragOperation.status.idle)) return;
			sortable.element = element2 != null ? element2 : void 0;
		}, [sortable]),
		sourceRef: (0, import_react.useCallback)((element2) => {
			var _a, _b;
			if (!element2 && ((_a = sortable.source) == null ? void 0 : _a.isConnected) && !((_b = sortable.manager) == null ? void 0 : _b.dragOperation.status.idle)) return;
			sortable.source = element2 != null ? element2 : void 0;
		}, [sortable]),
		targetRef: (0, import_react.useCallback)((element2) => {
			var _a, _b;
			if (!element2 && ((_a = sortable.target) == null ? void 0 : _a.isConnected) && !((_b = sortable.manager) == null ? void 0 : _b.dragOperation.status.idle)) return;
			sortable.target = element2 != null ? element2 : void 0;
		}, [sortable])
	};
}
function shouldUpdateSynchronously(key, oldValue, newValue) {
	if (key === "isDragSource" && !newValue && oldValue) return true;
	return false;
}
//#endregion
//#region node_modules/use-debounce/dist/index.module.js
function c(u, e, c) {
	var i = this, a = (0, import_react.useRef)(null), o = (0, import_react.useRef)(0), f = (0, import_react.useRef)(null), l = (0, import_react.useRef)([]), m = (0, import_react.useRef)(), v = (0, import_react.useRef)(), d = (0, import_react.useRef)(u), p = (0, import_react.useRef)(!0);
	(0, import_react.useEffect)(function() {
		d.current = u;
	}, [u]);
	var g = !e && 0 !== e && "undefined" != typeof window;
	if ("function" != typeof u) throw new TypeError("Expected a function");
	e = +e || 0;
	var w = !!(c = c || {}).leading, s = !("trailing" in c) || !!c.trailing, x = "maxWait" in c, y = x ? Math.max(+c.maxWait || 0, e) : null;
	(0, import_react.useEffect)(function() {
		return p.current = !0, function() {
			p.current = !1;
		};
	}, []);
	return (0, import_react.useMemo)(function() {
		var r = function(r) {
			var n = l.current, t = m.current;
			return l.current = m.current = null, o.current = r, v.current = d.current.apply(t, n);
		}, n = function(r, n) {
			g && cancelAnimationFrame(f.current), f.current = g ? requestAnimationFrame(r) : setTimeout(r, n);
		}, t = function(r) {
			if (!p.current) return !1;
			var n = r - a.current;
			return !a.current || n >= e || n < 0 || x && r - o.current >= y;
		}, u = function(n) {
			return f.current = null, s && l.current ? r(n) : (l.current = m.current = null, v.current);
		}, c = function r() {
			var c = Date.now();
			if (t(c)) return u(c);
			if (p.current) {
				var i = e - (c - a.current);
				n(r, x ? Math.min(i, y - (c - o.current)) : i);
			}
		}, h = function() {
			var u = Date.now(), d = t(u);
			if (l.current = [].slice.call(arguments), m.current = i, a.current = u, d) {
				if (!f.current && p.current) return o.current = a.current, n(c, e), w ? r(a.current) : v.current;
				if (x) return n(c, e), r(a.current);
			}
			return f.current || n(c, e), v.current;
		};
		return h.cancel = function() {
			f.current && (g ? cancelAnimationFrame(f.current) : clearTimeout(f.current)), o.current = 0, l.current = a.current = m.current = f.current = null;
		}, h.isPending = function() {
			return !!f.current;
		}, h.flush = function() {
			return f.current ? u(Date.now()) : v.current;
		}, h;
	}, [
		w,
		x,
		e,
		y,
		s,
		g
	]);
}
function i(r, n) {
	return r === n;
}
function a(r) {
	return "function" == typeof r ? function() {
		return r;
	} : r;
}
function o(n, t, o) {
	var f, l, m = o && o.equalityFn || i, v = (f = (0, import_react.useState)(a(n)), l = f[1], [f[0], (0, import_react.useCallback)(function(r) {
		return l(a(r));
	}, [])]), d = v[0], p = v[1], g = c((0, import_react.useCallback)(function(r) {
		return p(r);
	}, [p]), t, o), w = (0, import_react.useRef)(n);
	return m(w.current, n) || (g(n), w.current = n), [d, g];
}
//#endregion
//#region node_modules/@tanstack/virtual-core/dist/esm/lazy-measurements.js
function createLazyMeasurementsView(count, flat, getItemKey) {
	const cache = new Array(count);
	return new Proxy(cache, { get(target, prop, receiver) {
		if (typeof prop === "string") {
			const c = prop.charCodeAt(0);
			if (c >= 48 && c <= 57) {
				const i = +prop;
				if (Number.isInteger(i) && i >= 0 && i < count) {
					let v = target[i];
					if (!v) {
						const s = flat[i * 2];
						v = target[i] = {
							index: i,
							key: getItemKey(i),
							start: s,
							size: flat[i * 2 + 1],
							end: s + flat[i * 2 + 1],
							lane: 0
						};
					}
					return v;
				}
			}
			if (prop === "length") return count;
		}
		return Reflect.get(target, prop, receiver);
	} });
}
//#endregion
//#region node_modules/@tanstack/virtual-core/dist/esm/utils.js
function memo$1(getDeps, fn, opts) {
	let deps = opts.initialDeps ?? [];
	let result;
	let isInitial = true;
	function memoizedFunction() {
		const newDeps = getDeps();
		if (!(newDeps.length !== deps.length || newDeps.some((dep, index) => deps[index] !== dep))) return result;
		deps = newDeps;
		result = fn(...newDeps);
		if ((opts == null ? void 0 : opts.onChange) && !(isInitial && opts.skipInitialOnChange)) opts.onChange(result);
		isInitial = false;
		return result;
	}
	memoizedFunction.updateDeps = (newDeps) => {
		deps = newDeps;
	};
	return memoizedFunction;
}
function notUndefined(value, msg) {
	if (value === void 0) throw new Error(`Unexpected undefined${msg ? `: ${msg}` : ""}`);
	else return value;
}
var approxEqual = (a, b) => Math.abs(a - b) < 1.01;
var debounce = (targetWindow, fn, ms) => {
	let timeoutId;
	return function(...args) {
		targetWindow.clearTimeout(timeoutId);
		timeoutId = targetWindow.setTimeout(() => fn.apply(this, args), ms);
	};
};
//#endregion
//#region node_modules/@tanstack/virtual-core/dist/esm/index.js
var _isIOSResult;
var isIOSWebKit = () => {
	if (_isIOSResult !== void 0) return _isIOSResult;
	if (typeof navigator === "undefined") return _isIOSResult = false;
	if (/iP(hone|od|ad)/.test(navigator.userAgent)) return _isIOSResult = true;
	const mtp = navigator.maxTouchPoints;
	return _isIOSResult = navigator.platform === "MacIntel" && mtp !== void 0 && mtp > 0;
};
var getRect = (element) => {
	const { offsetWidth, offsetHeight } = element;
	return {
		width: offsetWidth,
		height: offsetHeight
	};
};
var defaultKeyExtractor = (index) => index;
var defaultRangeExtractor = (range) => {
	const start = Math.max(range.startIndex - range.overscan, 0);
	const len = Math.min(range.endIndex + range.overscan, range.count - 1) - start + 1;
	const arr = new Array(len);
	for (let i = 0; i < len; i++) arr[i] = start + i;
	return arr;
};
var observeElementRect = (instance, cb) => {
	const element = instance.scrollElement;
	if (!element) return;
	const targetWindow = instance.targetWindow;
	if (!targetWindow) return;
	const handler = (rect) => {
		const { width, height } = rect;
		cb({
			width: Math.round(width),
			height: Math.round(height)
		});
	};
	handler(getRect(element));
	if (!targetWindow.ResizeObserver) return () => {};
	const observer = new targetWindow.ResizeObserver((entries) => {
		const run = () => {
			const entry = entries[0];
			if (entry == null ? void 0 : entry.borderBoxSize) {
				const box = entry.borderBoxSize[0];
				if (box) {
					handler({
						width: box.inlineSize,
						height: box.blockSize
					});
					return;
				}
			}
			handler(getRect(element));
		};
		instance.options.useAnimationFrameWithResizeObserver ? requestAnimationFrame(run) : run();
	});
	observer.observe(element, { box: "border-box" });
	return () => {
		observer.unobserve(element);
	};
};
var addEventListenerOptions = { passive: true };
var observeWindowRect = (instance, cb) => {
	const element = instance.scrollElement;
	if (!element) return;
	const handler = () => {
		cb({
			width: element.innerWidth,
			height: element.innerHeight
		});
	};
	handler();
	element.addEventListener("resize", handler, addEventListenerOptions);
	return () => {
		element.removeEventListener("resize", handler);
	};
};
var supportsScrollend = typeof window == "undefined" ? true : "onscrollend" in window;
var observeOffset = (instance, cb, readOffset) => {
	const element = instance.scrollElement;
	if (!element) return;
	const targetWindow = instance.targetWindow;
	if (!targetWindow) return;
	const registerScrollendEvent = instance.options.useScrollendEvent && supportsScrollend;
	let offset = 0;
	const fallback = registerScrollendEvent ? null : debounce(targetWindow, () => cb(offset, false), instance.options.isScrollingResetDelay);
	const createHandler = (isScrolling) => () => {
		offset = readOffset(element);
		fallback?.();
		cb(offset, isScrolling);
	};
	const handler = createHandler(true);
	const endHandler = createHandler(false);
	element.addEventListener("scroll", handler, addEventListenerOptions);
	if (registerScrollendEvent) element.addEventListener("scrollend", endHandler, addEventListenerOptions);
	return () => {
		element.removeEventListener("scroll", handler);
		if (registerScrollendEvent) element.removeEventListener("scrollend", endHandler);
	};
};
var observeElementOffset = (instance, cb) => observeOffset(instance, cb, (el) => {
	const { horizontal, isRtl } = instance.options;
	return horizontal ? el.scrollLeft * (isRtl && -1 || 1) : el.scrollTop;
});
var observeWindowOffset = (instance, cb) => observeOffset(instance, cb, (win) => instance.options.horizontal ? win.scrollX : win.scrollY);
var measureElement = (element, entry, instance) => {
	if (instance.options.useCachedMeasurements) {
		const index = instance.indexFromElement(element);
		const key = instance.options.getItemKey(index);
		return instance.itemSizeCache.get(key) ?? instance.options.estimateSize(index);
	}
	if (entry == null ? void 0 : entry.borderBoxSize) {
		const box = entry.borderBoxSize[0];
		if (box) return Math.round(box[instance.options.horizontal ? "inlineSize" : "blockSize"]);
	}
	if (!entry) {
		const index = instance.indexFromElement(element);
		const key = instance.options.getItemKey(index);
		const cachedSize = instance.itemSizeCache.get(key);
		if (cachedSize !== void 0) return cachedSize;
	}
	return element[instance.options.horizontal ? "offsetWidth" : "offsetHeight"];
};
var scrollWithAdjustments = (offset, { adjustments = 0, behavior }, instance) => {
	var _a, _b;
	(_b = (_a = instance.scrollElement) == null ? void 0 : _a.scrollTo) == null || _b.call(_a, {
		[instance.options.horizontal ? "left" : "top"]: offset + adjustments,
		behavior
	});
};
var windowScroll = scrollWithAdjustments;
var elementScroll = scrollWithAdjustments;
var Virtualizer = class {
	constructor(opts) {
		this.unsubs = [];
		this.scrollElement = null;
		this.targetWindow = null;
		this.isScrolling = false;
		this.scrollState = null;
		this.measurementsCache = [];
		this._flatMeasurements = null;
		this.itemSizeCache = /* @__PURE__ */ new Map();
		this.itemSizeCacheVersion = 0;
		this.laneAssignments = /* @__PURE__ */ new Map();
		this.pendingMin = null;
		this.prevLanes = void 0;
		this.lanesChangedFlag = false;
		this.lanesSettling = false;
		this.pendingScrollAnchor = null;
		this.scrollRect = null;
		this.scrollOffset = null;
		this.scrollDirection = null;
		this.scrollAdjustments = 0;
		this._iosDeferredAdjustment = 0;
		this._iosTouching = false;
		this._iosJustTouchEnded = false;
		this._iosTouchEndTimerId = null;
		this._intendedScrollOffset = null;
		this.elementsCache = /* @__PURE__ */ new Map();
		this.now = () => {
			var _a, _b, _c;
			return ((_c = (_b = (_a = this.targetWindow) == null ? void 0 : _a.performance) == null ? void 0 : _b.now) == null ? void 0 : _c.call(_b)) ?? Date.now();
		};
		this.observer = /* @__PURE__ */ (() => {
			let _ro = null;
			const get = () => {
				if (_ro) return _ro;
				if (!this.targetWindow || !this.targetWindow.ResizeObserver) return null;
				return _ro = new this.targetWindow.ResizeObserver((entries) => {
					entries.forEach((entry) => {
						const run = () => {
							const node = entry.target;
							const index = this.indexFromElement(node);
							if (!node.isConnected) {
								this.observer.unobserve(node);
								for (const [cacheKey, cachedNode] of this.elementsCache) if (cachedNode === node) {
									this.elementsCache.delete(cacheKey);
									break;
								}
								return;
							}
							if (this.shouldMeasureDuringScroll(index)) this.resizeItem(index, this.options.measureElement(node, entry, this));
						};
						this.options.useAnimationFrameWithResizeObserver ? requestAnimationFrame(run) : run();
					});
				});
			};
			return {
				disconnect: () => {
					var _a;
					(_a = get()) == null || _a.disconnect();
					_ro = null;
				},
				observe: (target) => {
					var _a;
					return (_a = get()) == null ? void 0 : _a.observe(target, { box: "border-box" });
				},
				unobserve: (target) => {
					var _a;
					return (_a = get()) == null ? void 0 : _a.unobserve(target);
				}
			};
		})();
		this.range = null;
		this.setOptions = (opts2) => {
			var _a, _b;
			const merged = {
				debug: false,
				initialOffset: 0,
				overscan: 1,
				paddingStart: 0,
				paddingEnd: 0,
				scrollPaddingStart: 0,
				scrollPaddingEnd: 0,
				horizontal: false,
				getItemKey: defaultKeyExtractor,
				rangeExtractor: defaultRangeExtractor,
				onChange: () => {},
				measureElement,
				initialRect: {
					width: 0,
					height: 0
				},
				scrollMargin: 0,
				gap: 0,
				indexAttribute: "data-index",
				initialMeasurementsCache: [],
				lanes: 1,
				anchorTo: "start",
				followOnAppend: false,
				scrollEndThreshold: 1,
				isScrollingResetDelay: 150,
				enabled: true,
				isRtl: false,
				useScrollendEvent: false,
				useAnimationFrameWithResizeObserver: false,
				laneAssignmentMode: "estimate",
				useCachedMeasurements: false
			};
			for (const key in opts2) {
				const v = opts2[key];
				if (v !== void 0) merged[key] = v;
			}
			const prevOptions = this.options;
			let anchor = null;
			let followOnAppend = null;
			let edgeKeysChanged = false;
			if (prevOptions !== void 0 && prevOptions.enabled && merged.enabled && merged.anchorTo === "end" && this.scrollElement !== null) {
				const prevCount = prevOptions.count;
				const nextCount = merged.count;
				const measurements = this.getMeasurements();
				const prevFirstKey = prevCount > 0 ? ((_a = measurements[0]) == null ? void 0 : _a.key) ?? prevOptions.getItemKey(0) : null;
				const prevLastKey = prevCount > 0 ? ((_b = measurements[prevCount - 1]) == null ? void 0 : _b.key) ?? prevOptions.getItemKey(prevCount - 1) : null;
				if (nextCount !== prevCount || prevCount > 0 && nextCount > 0 && (merged.getItemKey(0) !== prevFirstKey || merged.getItemKey(nextCount - 1) !== prevLastKey)) {
					edgeKeysChanged = true;
					const item = prevCount > 0 ? this.getVirtualItemForOffset(this.getScrollOffset()) ?? measurements[0] : null;
					if (item) anchor = [item.key, this.getScrollOffset() - item.start];
					const behavior = merged.followOnAppend === true ? "auto" : merged.followOnAppend || null;
					if (behavior && nextCount > prevCount && this.isAtEnd(prevOptions.scrollEndThreshold) && (prevCount === 0 || merged.getItemKey(nextCount - 1) !== prevLastKey)) followOnAppend = behavior;
				}
			}
			this.options = merged;
			if (edgeKeysChanged) {
				this.pendingMin = 0;
				this.itemSizeCacheVersion++;
			}
			let anchorResolved = false;
			let anchorDelta = 0;
			if (anchor && this.scrollOffset !== null) {
				const [anchorKey, anchorOffset] = anchor;
				const newMeasurements = this.getMeasurements();
				const { count, getItemKey } = this.options;
				let idx = 0;
				while (idx < count && getItemKey(idx) !== anchorKey) idx++;
				if (idx < count) {
					const anchorItem = newMeasurements[idx];
					if (anchorItem) {
						const newOffset = Math.max(0, anchorItem.start + anchorOffset);
						if (newOffset !== this.scrollOffset) {
							anchorDelta = newOffset - this.scrollOffset;
							this.scrollOffset = newOffset;
							anchorResolved = true;
						}
					}
				}
			}
			if (anchorResolved || followOnAppend) this.pendingScrollAnchor = [
				anchorResolved ? anchor[0] : null,
				anchorResolved ? anchor[1] : 0,
				followOnAppend,
				anchorDelta
			];
		};
		this.notify = (sync) => {
			var _a, _b;
			(_b = (_a = this.options).onChange) == null || _b.call(_a, this, sync);
		};
		this.maybeNotify = memo$1(() => {
			this.calculateRange();
			return [
				this.isScrolling,
				this.range ? this.range.startIndex : null,
				this.range ? this.range.endIndex : null
			];
		}, (isScrolling) => {
			this.notify(isScrolling);
		}, {
			key: false,
			debug: () => this.options.debug,
			initialDeps: [
				this.isScrolling,
				this.range ? this.range.startIndex : null,
				this.range ? this.range.endIndex : null
			]
		});
		this.cleanup = () => {
			this.unsubs.filter(Boolean).forEach((d) => d());
			this.unsubs = [];
			this.observer.disconnect();
			if (this.rafId != null && this.targetWindow) {
				this.targetWindow.cancelAnimationFrame(this.rafId);
				this.rafId = null;
			}
			this.scrollState = null;
			this._iosDeferredAdjustment = 0;
			this._iosTouching = false;
			this._iosJustTouchEnded = false;
			this.scrollElement = null;
			this.targetWindow = null;
		};
		this._didMount = () => {
			return () => {
				this.cleanup();
			};
		};
		this._willUpdate = () => {
			var _a;
			const scrollElement = this.options.enabled ? this.options.getScrollElement() : null;
			if (this.scrollElement !== scrollElement) {
				this.cleanup();
				if (!scrollElement) {
					this.maybeNotify();
					return;
				}
				this.scrollElement = scrollElement;
				if (this.scrollElement && "ownerDocument" in this.scrollElement) this.targetWindow = this.scrollElement.ownerDocument.defaultView;
				else this.targetWindow = ((_a = this.scrollElement) == null ? void 0 : _a.window) ?? null;
				this.elementsCache.forEach((cached) => {
					this.observer.observe(cached);
				});
				this.unsubs.push(this.options.observeElementRect(this, (rect) => {
					this.scrollRect = rect;
					this.maybeNotify();
				}));
				this.unsubs.push(this.options.observeElementOffset(this, (offset, isScrolling) => {
					if (isScrolling && this._intendedScrollOffset === null && offset === this.scrollOffset) return;
					if (this._intendedScrollOffset !== null && Math.abs(offset - this._intendedScrollOffset) < 1.5) offset = this._intendedScrollOffset;
					this._intendedScrollOffset = null;
					this.scrollAdjustments = 0;
					const prevOffset = this.getScrollOffset();
					this.scrollDirection = isScrolling ? prevOffset === offset ? this.scrollDirection : prevOffset < offset ? "forward" : "backward" : null;
					this.scrollOffset = offset;
					this.isScrolling = isScrolling;
					this._flushIosDeferredIfReady();
					if (this.scrollState) this.scheduleScrollReconcile();
					this.maybeNotify();
				}));
				if ("addEventListener" in this.scrollElement) {
					const scrollEl = this.scrollElement;
					const onTouchStart = () => {
						this._iosTouching = true;
						this._iosJustTouchEnded = false;
						if (this._iosTouchEndTimerId !== null && this.targetWindow != null) {
							this.targetWindow.clearTimeout(this._iosTouchEndTimerId);
							this._iosTouchEndTimerId = null;
						}
					};
					const onTouchEnd = () => {
						this._iosTouching = false;
						if (!isIOSWebKit() || this.targetWindow == null) return;
						this._iosJustTouchEnded = true;
						this._iosTouchEndTimerId = this.targetWindow.setTimeout(() => {
							this._iosJustTouchEnded = false;
							this._iosTouchEndTimerId = null;
							this._flushIosDeferredIfReady();
						}, 150);
					};
					scrollEl.addEventListener("touchstart", onTouchStart, addEventListenerOptions);
					scrollEl.addEventListener("touchend", onTouchEnd, addEventListenerOptions);
					this.unsubs.push(() => {
						scrollEl.removeEventListener("touchstart", onTouchStart);
						scrollEl.removeEventListener("touchend", onTouchEnd);
						if (this._iosTouchEndTimerId !== null && this.targetWindow != null) {
							this.targetWindow.clearTimeout(this._iosTouchEndTimerId);
							this._iosTouchEndTimerId = null;
						}
					});
				}
				this._scrollToOffset(this.getScrollOffset(), {
					adjustments: void 0,
					behavior: void 0
				});
			}
			const anchor = this.pendingScrollAnchor;
			this.pendingScrollAnchor = null;
			if (anchor && this.scrollElement && this.options.enabled) {
				const [key, _offset, followOnAppend, anchorDelta] = anchor;
				if (key !== null && !followOnAppend) if (isIOSWebKit() && (this.isScrolling || this._iosTouching || this._iosJustTouchEnded)) {
					if (anchorDelta !== 0) this._iosDeferredAdjustment += anchorDelta;
				} else this._scrollToOffset(this.getScrollOffset(), {
					adjustments: void 0,
					behavior: void 0
				});
				if (followOnAppend) this.scrollToEnd({ behavior: followOnAppend });
			}
		};
		this._flushIosDeferredIfReady = () => {
			if (this._iosDeferredAdjustment === 0) return;
			if (this.isScrolling) return;
			if (this._iosTouching) return;
			if (this._iosJustTouchEnded) return;
			const cur = this.getScrollOffset();
			const max = this.getMaxScrollOffset();
			if (cur < 0 || cur > max) return;
			if (this._iosDeferredAdjustment < 0 && cur >= max - 1) {
				this._iosDeferredAdjustment = 0;
				return;
			}
			const delta = this._iosDeferredAdjustment;
			this._iosDeferredAdjustment = 0;
			this._scrollToOffset(cur, {
				adjustments: this.scrollAdjustments += delta,
				behavior: void 0
			});
		};
		this.rafId = null;
		this.getSize = () => {
			if (!this.options.enabled) {
				this.scrollRect = null;
				return 0;
			}
			this.scrollRect = this.scrollRect ?? this.options.initialRect;
			return this.scrollRect[this.options.horizontal ? "width" : "height"];
		};
		this.getScrollOffset = () => {
			if (!this.options.enabled) {
				this.scrollOffset = null;
				return 0;
			}
			this.scrollOffset = this.scrollOffset ?? (typeof this.options.initialOffset === "function" ? this.options.initialOffset() : this.options.initialOffset);
			return this.scrollOffset;
		};
		this.getMeasurementOptions = memo$1(() => [
			this.options.count,
			this.options.paddingStart,
			this.options.scrollMargin,
			this.options.getItemKey,
			this.options.enabled,
			this.options.lanes,
			this.options.laneAssignmentMode,
			this.options.gap
		], (count, paddingStart, scrollMargin, getItemKey, enabled, lanes, laneAssignmentMode, gap) => {
			if (this.prevLanes !== void 0 && this.prevLanes !== lanes) this.lanesChangedFlag = true;
			this.prevLanes = lanes;
			this.pendingMin = null;
			return {
				count,
				paddingStart,
				scrollMargin,
				getItemKey,
				enabled,
				lanes,
				laneAssignmentMode,
				gap
			};
		}, { key: false });
		this.getMeasurements = memo$1(() => [this.getMeasurementOptions(), this.itemSizeCacheVersion], ({ count, paddingStart, scrollMargin, getItemKey, enabled, lanes, laneAssignmentMode, gap }, _itemSizeCacheVersion) => {
			const itemSizeCache = this.itemSizeCache;
			if (!enabled) {
				this.measurementsCache = [];
				this.itemSizeCache.clear();
				this.laneAssignments.clear();
				return [];
			}
			if (this.laneAssignments.size > count) {
				for (const index of this.laneAssignments.keys()) if (index >= count) this.laneAssignments.delete(index);
			}
			if (this.lanesChangedFlag) {
				this.lanesChangedFlag = false;
				this.lanesSettling = true;
				this.measurementsCache = [];
				this.itemSizeCache.clear();
				this.laneAssignments.clear();
				this.pendingMin = null;
			}
			if (this.measurementsCache.length === 0 && !this.lanesSettling) {
				this.measurementsCache = this.options.initialMeasurementsCache;
				this.measurementsCache.forEach((item) => {
					this.itemSizeCache.set(item.key, item.size);
				});
			}
			const min = this.lanesSettling ? 0 : this.pendingMin ?? 0;
			this.pendingMin = null;
			if (this.lanesSettling && this.measurementsCache.length === count) this.lanesSettling = false;
			if (lanes === 1) {
				const need = count * 2;
				let flat = this._flatMeasurements;
				if (!flat || flat.length < need) {
					const next = new Float64Array(need);
					if (flat && min > 0) next.set(flat.subarray(0, min * 2));
					flat = next;
					this._flatMeasurements = flat;
				}
				let runningStart;
				if (min === 0) runningStart = paddingStart + scrollMargin;
				else {
					const prevIdx = min - 1;
					runningStart = flat[prevIdx * 2] + flat[prevIdx * 2 + 1] + gap;
				}
				for (let i = min; i < count; i++) {
					const key = getItemKey(i);
					const measuredSize = itemSizeCache.get(key);
					const size = typeof measuredSize === "number" ? measuredSize : this.options.estimateSize(i);
					flat[i * 2] = runningStart;
					flat[i * 2 + 1] = size;
					runningStart += size + gap;
				}
				const view = createLazyMeasurementsView(count, flat, getItemKey);
				this.measurementsCache = view;
				return view;
			}
			const measurements = this.measurementsCache.slice(0, min);
			const laneLastIndex = new Array(lanes).fill(void 0);
			const laneEnds = new Float64Array(lanes);
			let filledLanes = 0;
			for (let m = 0; m < min; m++) {
				const item = measurements[m];
				if (item) {
					if (laneLastIndex[item.lane] === void 0) filledLanes++;
					laneLastIndex[item.lane] = m;
					laneEnds[item.lane] = item.end;
				}
			}
			for (let i = min; i < count; i++) {
				const key = getItemKey(i);
				const cachedLane = this.laneAssignments.get(i);
				let lane;
				let start;
				const shouldCacheLane = laneAssignmentMode === "estimate" || itemSizeCache.has(key);
				if (cachedLane !== void 0 && this.options.lanes > 1) {
					lane = cachedLane;
					const prevIndex = laneLastIndex[lane];
					const prevInLane = prevIndex !== void 0 ? measurements[prevIndex] : void 0;
					start = prevInLane ? prevInLane.end + gap : paddingStart + scrollMargin;
				} else if (filledLanes === lanes) {
					let bestLane = 0;
					let bestEnd = laneEnds[0];
					let bestIdx = laneLastIndex[0];
					for (let l = 1; l < lanes; l++) {
						const e = laneEnds[l];
						if (e < bestEnd || e === bestEnd && laneLastIndex[l] < bestIdx) {
							bestLane = l;
							bestEnd = e;
							bestIdx = laneLastIndex[l];
						}
					}
					lane = bestLane;
					start = bestEnd + gap;
					if (shouldCacheLane) this.laneAssignments.set(i, lane);
				} else {
					lane = i % this.options.lanes;
					start = paddingStart + scrollMargin;
					if (shouldCacheLane) this.laneAssignments.set(i, lane);
				}
				const measuredSize = itemSizeCache.get(key);
				const size = typeof measuredSize === "number" ? measuredSize : this.options.estimateSize(i);
				const end = start + size;
				measurements[i] = {
					index: i,
					start,
					size,
					end,
					key,
					lane
				};
				if (laneLastIndex[lane] === void 0) filledLanes++;
				laneLastIndex[lane] = i;
				laneEnds[lane] = end;
			}
			this.measurementsCache = measurements;
			return measurements;
		}, {
			key: false,
			debug: () => this.options.debug
		});
		this.calculateRange = memo$1(() => [
			this.getMeasurements(),
			this.getSize(),
			this.getScrollOffset(),
			this.options.lanes
		], (measurements, outerSize, scrollOffset, lanes) => {
			if (measurements.length === 0 || outerSize === 0) {
				this.range = null;
				return null;
			}
			this.range = calculateRangeImpl(measurements, outerSize, scrollOffset, lanes, lanes === 1 && this._flatMeasurements != null ? this._flatMeasurements : null);
			return this.range;
		}, {
			key: false,
			debug: () => this.options.debug
		});
		this.getVirtualIndexes = memo$1(() => {
			let startIndex = null;
			let endIndex = null;
			const range = this.calculateRange();
			if (range) {
				startIndex = range.startIndex;
				endIndex = range.endIndex;
			}
			this.maybeNotify.updateDeps([
				this.isScrolling,
				startIndex,
				endIndex
			]);
			return [
				this.options.rangeExtractor,
				this.options.overscan,
				this.options.count,
				startIndex,
				endIndex
			];
		}, (rangeExtractor, overscan, count, startIndex, endIndex) => {
			return startIndex === null || endIndex === null ? [] : rangeExtractor({
				startIndex,
				endIndex,
				overscan,
				count
			});
		}, {
			key: false,
			debug: () => this.options.debug
		});
		this.indexFromElement = (node) => {
			const attributeName = this.options.indexAttribute;
			const indexStr = node.getAttribute(attributeName);
			if (!indexStr) {
				console.warn(`Missing attribute name '${attributeName}={index}' on measured element.`);
				return -1;
			}
			return parseInt(indexStr, 10);
		};
		this.shouldMeasureDuringScroll = (index) => {
			var _a;
			if (!this.scrollState || this.scrollState.behavior !== "smooth") return true;
			const scrollIndex = this.scrollState.index ?? ((_a = this.getVirtualItemForOffset(this.scrollState.lastTargetOffset)) == null ? void 0 : _a.index);
			if (scrollIndex !== void 0 && this.range) {
				const bufferSize = Math.max(this.options.overscan, Math.ceil((this.range.endIndex - this.range.startIndex) / 2));
				const minIndex = Math.max(0, scrollIndex - bufferSize);
				const maxIndex = Math.min(this.options.count - 1, scrollIndex + bufferSize);
				return index >= minIndex && index <= maxIndex;
			}
			return true;
		};
		this.measureElement = (node) => {
			if (!node) {
				this.elementsCache.forEach((cached, key2) => {
					if (!cached.isConnected) {
						this.observer.unobserve(cached);
						this.elementsCache.delete(key2);
					}
				});
				return;
			}
			const index = this.indexFromElement(node);
			const key = this.options.getItemKey(index);
			const prevNode = this.elementsCache.get(key);
			if (prevNode !== node) {
				if (prevNode) this.observer.unobserve(prevNode);
				this.observer.observe(node);
				this.elementsCache.set(key, node);
			}
			if ((!this.isScrolling || this.scrollState) && this.shouldMeasureDuringScroll(index)) this.resizeItem(index, this.options.measureElement(node, void 0, this));
		};
		this.resizeItem = (index, size) => {
			var _a, _b;
			if (index < 0 || index >= this.options.count) return;
			let cachedSize;
			let itemStart;
			let key;
			const flat = this._flatMeasurements;
			if (this.options.lanes === 1 && flat !== null) {
				key = this.options.getItemKey(index);
				itemStart = flat[index * 2];
				cachedSize = flat[index * 2 + 1];
			} else {
				const item = this.measurementsCache[index];
				if (!item) return;
				key = item.key;
				itemStart = item.start;
				cachedSize = item.size;
			}
			const itemSize = this.itemSizeCache.get(key) ?? cachedSize;
			const delta = size - itemSize;
			if (delta !== 0) {
				const wasAtEnd = this.options.anchorTo === "end" && ((_a = this.scrollState) == null ? void 0 : _a.behavior) !== "smooth" && this.getVirtualDistanceFromEnd() <= this.options.scrollEndThreshold;
				const prevTotalSize = wasAtEnd ? this.getTotalSize() : 0;
				const scrollOffsetWithAdj = this.getScrollOffset() + this.scrollAdjustments;
				const defaultShouldAdjust = !this.itemSizeCache.has(key) ? itemStart < scrollOffsetWithAdj : itemStart + itemSize <= scrollOffsetWithAdj && this.scrollDirection !== "backward";
				const shouldAdjustScroll = ((_b = this.scrollState) == null ? void 0 : _b.behavior) !== "smooth" && (this.shouldAdjustScrollPositionOnItemSizeChange !== void 0 ? this.shouldAdjustScrollPositionOnItemSizeChange(this.measurementsCache[index] ?? {
					index,
					key,
					start: itemStart,
					size: cachedSize,
					end: itemStart + cachedSize,
					lane: 0
				}, delta, this) : defaultShouldAdjust);
				if (this.pendingMin === null || index < this.pendingMin) this.pendingMin = index;
				this.itemSizeCache.set(key, size);
				this.itemSizeCacheVersion++;
				let adjustedSync = false;
				if (wasAtEnd) adjustedSync = this.applyScrollAdjustment(this.getTotalSize() - prevTotalSize);
				else if (shouldAdjustScroll) adjustedSync = this.applyScrollAdjustment(delta);
				this.notify(adjustedSync);
			}
		};
		this.getVirtualItems = memo$1(() => [this.getVirtualIndexes(), this.getMeasurements()], (indexes, measurements) => {
			const virtualItems = [];
			for (let k = 0, len = indexes.length; k < len; k++) {
				const measurement = measurements[indexes[k]];
				virtualItems.push(measurement);
			}
			return virtualItems;
		}, {
			key: false,
			debug: () => this.options.debug
		});
		this.getVirtualItemForOffset = (offset) => {
			const measurements = this.getMeasurements();
			if (measurements.length === 0) return;
			const flat = this._flatMeasurements;
			const useFlat = this.options.lanes === 1 && flat != null;
			return notUndefined(measurements[findNearestBinarySearch(0, measurements.length - 1, useFlat ? (i) => flat[i * 2] : (i) => notUndefined(measurements[i]).start, offset)]);
		};
		this.getMaxScrollOffset = () => {
			if (!this.scrollElement) return 0;
			if ("scrollHeight" in this.scrollElement) return this.options.horizontal ? this.scrollElement.scrollWidth - this.scrollElement.clientWidth : this.scrollElement.scrollHeight - this.scrollElement.clientHeight;
			else {
				const doc = this.scrollElement.document.documentElement;
				return this.options.horizontal ? doc.scrollWidth - this.scrollElement.innerWidth : doc.scrollHeight - this.scrollElement.innerHeight;
			}
		};
		this.getVirtualDistanceFromEnd = () => {
			return Math.max(this.getTotalSize() - this.getSize() - this.getScrollOffset(), 0);
		};
		this.getDistanceFromEnd = () => {
			return Math.max(this.getMaxScrollOffset() - this.getScrollOffset(), 0);
		};
		this.isAtEnd = (threshold = this.options.scrollEndThreshold) => {
			return this.getDistanceFromEnd() <= threshold;
		};
		this.getOffsetForAlignment = (toOffset, align, itemSize = 0) => {
			if (!this.scrollElement) return 0;
			const size = this.getSize();
			const scrollOffset = this.getScrollOffset();
			if (align === "auto") align = toOffset >= scrollOffset + size ? "end" : "start";
			if (align === "center") toOffset += (itemSize - size) / 2;
			else if (align === "end") toOffset -= size;
			const maxOffset = this.getMaxScrollOffset();
			return Math.max(Math.min(maxOffset, toOffset), 0);
		};
		this.getOffsetForIndex = (index, align = "auto") => {
			index = Math.max(0, Math.min(index, this.options.count - 1));
			const size = this.getSize();
			const scrollOffset = this.getScrollOffset();
			const item = this.measurementsCache[index];
			if (!item) return;
			if (align === "auto") if (item.end >= scrollOffset + size - this.options.scrollPaddingEnd) align = "end";
			else if (item.start <= scrollOffset + this.options.scrollPaddingStart) align = "start";
			else return [scrollOffset, align];
			if (align === "end" && index === this.options.count - 1) return [this.getMaxScrollOffset(), align];
			const toOffset = align === "end" ? item.end + this.options.scrollPaddingEnd : item.start - this.options.scrollPaddingStart;
			return [this.getOffsetForAlignment(toOffset, align, item.size), align];
		};
		this.scrollToOffset = (toOffset, { align = "start", behavior = "auto" } = {}) => {
			this._iosDeferredAdjustment = 0;
			const offset = this.getOffsetForAlignment(toOffset, align);
			const now = this.now();
			this.scrollState = {
				index: null,
				align,
				behavior,
				startedAt: now,
				lastTargetOffset: offset,
				stableFrames: 0
			};
			this._scrollToOffset(offset, {
				adjustments: void 0,
				behavior
			});
			this.scheduleScrollReconcile();
		};
		this.scrollToIndex = (index, { align: initialAlign = "auto", behavior = "auto" } = {}) => {
			this._iosDeferredAdjustment = 0;
			index = Math.max(0, Math.min(index, this.options.count - 1));
			const offsetInfo = this.getOffsetForIndex(index, initialAlign);
			if (!offsetInfo) return;
			const [offset, align] = offsetInfo;
			const now = this.now();
			this.scrollState = {
				index,
				align,
				behavior,
				startedAt: now,
				lastTargetOffset: offset,
				stableFrames: 0
			};
			this._scrollToOffset(offset, {
				adjustments: void 0,
				behavior
			});
			this.scheduleScrollReconcile();
		};
		this.scrollBy = (delta, { behavior = "auto" } = {}) => {
			const offset = this.getScrollOffset() + delta;
			const now = this.now();
			this.scrollState = {
				index: null,
				align: "start",
				behavior,
				startedAt: now,
				lastTargetOffset: offset,
				stableFrames: 0
			};
			this._scrollToOffset(offset, {
				adjustments: void 0,
				behavior
			});
			this.scheduleScrollReconcile();
		};
		this.scrollToEnd = ({ behavior = "auto" } = {}) => {
			if (this.options.count > 0) {
				this.scrollToIndex(this.options.count - 1, {
					align: "end",
					behavior
				});
				return;
			}
			this.scrollToOffset(Math.max(this.getTotalSize() - this.getSize(), 0), { behavior });
		};
		this.getTotalSize = () => {
			var _a;
			const measurements = this.getMeasurements();
			let end;
			if (measurements.length === 0) end = this.options.paddingStart;
			else if (this.options.lanes === 1) {
				const lastIdx = measurements.length - 1;
				const flat = this._flatMeasurements;
				if (flat != null) end = flat[lastIdx * 2] + flat[lastIdx * 2 + 1];
				else end = ((_a = measurements[lastIdx]) == null ? void 0 : _a.end) ?? 0;
			} else {
				const endByLane = Array(this.options.lanes).fill(null);
				let endIndex = measurements.length - 1;
				while (endIndex >= 0 && endByLane.some((val) => val === null)) {
					const item = measurements[endIndex];
					if (endByLane[item.lane] === null) endByLane[item.lane] = item.end;
					endIndex--;
				}
				end = Math.max(...endByLane.filter((val) => val !== null));
			}
			return Math.max(end - this.options.scrollMargin + this.options.paddingEnd, 0);
		};
		this.takeSnapshot = () => {
			const snapshot = [];
			if (this.itemSizeCache.size === 0) return snapshot;
			const m = this.getMeasurements();
			for (const item of m) if (item && this.itemSizeCache.has(item.key)) snapshot.push({
				index: item.index,
				key: item.key,
				start: item.start,
				size: item.size,
				end: item.end,
				lane: item.lane
			});
			return snapshot;
		};
		this._scrollToOffset = (offset, { adjustments, behavior }) => {
			this._intendedScrollOffset = offset + (adjustments ?? 0);
			this.options.scrollToFn(offset, {
				behavior,
				adjustments
			}, this);
		};
		this.measure = () => {
			this.pendingMin = null;
			this.itemSizeCache.clear();
			this.laneAssignments.clear();
			this.itemSizeCacheVersion++;
			this.notify(false);
		};
		this.setOptions(opts);
	}
	applyScrollAdjustment(delta, behavior) {
		if (delta === 0) return false;
		if (isIOSWebKit() && (this.isScrolling || this._iosTouching || this._iosJustTouchEnded)) {
			this._iosDeferredAdjustment += delta;
			return false;
		} else {
			this._scrollToOffset(this.getScrollOffset(), {
				adjustments: this.scrollAdjustments += delta,
				behavior
			});
			if (this.scrollOffset !== null) {
				this.scrollOffset += this.scrollAdjustments;
				if (this.scrollOffset < 0) this.scrollOffset = 0;
				this.scrollAdjustments = 0;
			}
			return true;
		}
	}
	scheduleScrollReconcile() {
		if (!this.targetWindow) {
			this.scrollState = null;
			return;
		}
		if (this.rafId != null) return;
		this.rafId = this.targetWindow.requestAnimationFrame(() => {
			this.rafId = null;
			this.reconcileScroll();
		});
	}
	reconcileScroll() {
		if (!this.scrollState) return;
		if (!this.scrollElement) return;
		if (this.now() - this.scrollState.startedAt > 5e3) {
			this.scrollState = null;
			return;
		}
		const offsetInfo = this.scrollState.index != null ? this.getOffsetForIndex(this.scrollState.index, this.scrollState.align) : void 0;
		const targetOffset = offsetInfo ? offsetInfo[0] : this.scrollState.lastTargetOffset;
		const STABLE_FRAMES = 1;
		const targetChanged = targetOffset !== this.scrollState.lastTargetOffset;
		if (!targetChanged && approxEqual(targetOffset, this.getScrollOffset())) {
			this.scrollState.stableFrames++;
			if (this.scrollState.stableFrames >= STABLE_FRAMES) {
				if (this.getScrollOffset() !== targetOffset) this._scrollToOffset(targetOffset, {
					adjustments: void 0,
					behavior: "auto"
				});
				this.scrollState = null;
				return;
			}
		} else {
			this.scrollState.stableFrames = 0;
			if (targetChanged) {
				const viewport = this.getSize() || 600;
				const distance = Math.abs(targetOffset - this.getScrollOffset());
				const keepSmooth = this.scrollState.behavior === "smooth" && distance > viewport;
				this.scrollState.lastTargetOffset = targetOffset;
				if (!keepSmooth) this.scrollState.behavior = "auto";
				this._scrollToOffset(targetOffset, {
					adjustments: void 0,
					behavior: keepSmooth ? "smooth" : "auto"
				});
			}
		}
		this.scheduleScrollReconcile();
	}
};
var findNearestBinarySearch = (low, high, getCurrentValue, value) => {
	while (low <= high) {
		const middle = (low + high) / 2 | 0;
		const currentValue = getCurrentValue(middle);
		if (currentValue < value) low = middle + 1;
		else if (currentValue > value) high = middle - 1;
		else return middle;
	}
	if (low > 0) return low - 1;
	else return 0;
};
function findNearestBinarySearchFlat(flat, high, value) {
	let low = 0;
	while (low <= high) {
		const middle = (low + high) / 2 | 0;
		const currentValue = flat[middle * 2];
		if (currentValue < value) low = middle + 1;
		else if (currentValue > value) high = middle - 1;
		else return middle;
	}
	return low > 0 ? low - 1 : 0;
}
function calculateRangeImpl(measurements, outerSize, scrollOffset, lanes, flat) {
	const lastIndex = measurements.length - 1;
	if (measurements.length <= lanes) return {
		startIndex: 0,
		endIndex: lastIndex
	};
	if (lanes === 1 && flat !== null) {
		const startIndex2 = findNearestBinarySearchFlat(flat, lastIndex, scrollOffset);
		let endIndex2 = startIndex2;
		const limit = scrollOffset + outerSize;
		while (endIndex2 < lastIndex && flat[endIndex2 * 2] + flat[endIndex2 * 2 + 1] < limit) endIndex2++;
		return {
			startIndex: startIndex2,
			endIndex: endIndex2
		};
	}
	const getStart = (index) => measurements[index].start;
	let startIndex = findNearestBinarySearch(0, lastIndex, getStart, scrollOffset);
	let endIndex = startIndex;
	if (lanes === 1) while (endIndex < lastIndex && measurements[endIndex].end < scrollOffset + outerSize) endIndex++;
	else if (lanes > 1) {
		const endPerLane = Array(lanes).fill(0);
		while (endIndex < lastIndex && endPerLane.some((pos) => pos < scrollOffset + outerSize)) {
			const item = measurements[endIndex];
			endPerLane[item.lane] = item.end;
			endIndex++;
		}
		const startPerLane = Array(lanes).fill(scrollOffset + outerSize);
		while (startIndex >= 0 && startPerLane.some((pos) => pos >= scrollOffset)) {
			const item = measurements[startIndex];
			startPerLane[item.lane] = item.start;
			startIndex--;
		}
		startIndex = Math.max(0, startIndex - startIndex % lanes);
		endIndex = Math.min(lastIndex, endIndex + (lanes - 1 - endIndex % lanes));
	}
	return {
		startIndex,
		endIndex
	};
}
//#endregion
//#region node_modules/@puckeditor/core/node_modules/@tanstack/react-virtual/dist/esm/index.js
var useIsomorphicLayoutEffect = typeof document !== "undefined" ? import_react.useLayoutEffect : import_react.useEffect;
function useVirtualizerBase({ useFlushSync = true, directDomUpdates = false, directDomUpdatesMode = "transform", ...options }) {
	const rerender = import_react.useReducer((x) => x + 1, 0)[1];
	const directRef = import_react.useRef({
		enabled: directDomUpdates,
		mode: directDomUpdatesMode,
		container: null,
		lastSize: null,
		lastPositions: /* @__PURE__ */ new WeakMap(),
		prevRange: null
	});
	directRef.current.enabled = directDomUpdates;
	directRef.current.mode = directDomUpdatesMode;
	const applyContainerSize = (instance2) => {
		const state = directRef.current;
		if (!state.enabled || !state.container) return;
		const totalSize = instance2.getTotalSize();
		if (totalSize !== state.lastSize) {
			state.lastSize = totalSize;
			const sizeAxis = instance2.options.horizontal ? "width" : "height";
			state.container.style[sizeAxis] = `${totalSize}px`;
		}
	};
	const applyDirectStyles = (instance2) => {
		const state = directRef.current;
		if (!state.enabled || !state.container) return;
		applyContainerSize(instance2);
		const horizontal = !!instance2.options.horizontal;
		const useTransform = state.mode === "transform";
		const posAxis = horizontal ? "left" : "top";
		const scrollMargin = instance2.options.scrollMargin;
		const items = instance2.getVirtualItems();
		for (const item of items) {
			const next = item.start - scrollMargin;
			const el = instance2.elementsCache.get(item.key);
			if (!el) continue;
			if (state.lastPositions.get(el) === next) continue;
			state.lastPositions.set(el, next);
			if (useTransform) el.style.transform = horizontal ? `translate3d(${next}px, 0, 0)` : `translate3d(0, ${next}px, 0)`;
			else el.style[posAxis] = `${next}px`;
		}
	};
	const resolvedOptions = {
		...options,
		onChange: (instance2, sync) => {
			var _a;
			const state = directRef.current;
			let shouldRerender = true;
			if (state.enabled) {
				applyDirectStyles(instance2);
				const range = instance2.range;
				const prev = state.prevRange;
				shouldRerender = !prev || prev.isScrolling !== instance2.isScrolling || prev.startIndex !== (range == null ? void 0 : range.startIndex) || prev.endIndex !== (range == null ? void 0 : range.endIndex);
				if (shouldRerender) state.prevRange = range ? {
					startIndex: range.startIndex,
					endIndex: range.endIndex,
					isScrolling: instance2.isScrolling
				} : null;
			}
			if (shouldRerender) if (useFlushSync && sync) (0, import_react_dom.flushSync)(rerender);
			else rerender();
			(_a = options.onChange) == null || _a.call(options, instance2, sync);
		}
	};
	const [instance] = import_react.useState(() => {
		const v = new Virtualizer(resolvedOptions);
		return Object.assign(v, { containerRef: (node) => {
			const state = directRef.current;
			state.container = node;
			state.lastSize = null;
			if (node && state.enabled) {
				const total = v.getTotalSize();
				state.lastSize = total;
				const axis = v.options.horizontal ? "width" : "height";
				node.style[axis] = `${total}px`;
			}
		} });
	});
	instance.setOptions(resolvedOptions);
	useIsomorphicLayoutEffect(() => {
		return instance._didMount();
	}, []);
	useIsomorphicLayoutEffect(() => {
		applyContainerSize(instance);
		return instance._willUpdate();
	});
	useIsomorphicLayoutEffect(() => {
		applyDirectStyles(instance);
	});
	return instance;
}
function useVirtualizer(options) {
	return useVirtualizerBase({
		observeElementRect,
		observeElementOffset,
		scrollToFn: elementScroll,
		...options
	});
}
//#endregion
//#region builtin:esm-external-require-crypto
var require_builtin_esm_external_require_crypto = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = m.default;
}));
//#endregion
//#region node_modules/@puckeditor/core/dist/chunk-YXFTA2VL.mjs
var import_object_hash = /* @__PURE__ */ __toESM$1((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	var crypto = require_builtin_esm_external_require_crypto();
	/**
	* Exported function
	*
	* Options:
	*
	*  - `algorithm` hash algo to be used by this instance: *'sha1', 'md5'
	*  - `excludeValues` {true|*false} hash object keys, values ignored
	*  - `encoding` hash encoding, supports 'buffer', '*hex', 'binary', 'base64'
	*  - `ignoreUnknown` {true|*false} ignore unknown object types
	*  - `replacer` optional function that replaces values before hashing
	*  - `respectFunctionProperties` {*true|false} consider function properties when hashing
	*  - `respectFunctionNames` {*true|false} consider 'name' property of functions for hashing
	*  - `respectType` {*true|false} Respect special properties (prototype, constructor)
	*    when hashing to distinguish between types
	*  - `unorderedArrays` {true|*false} Sort all arrays before hashing
	*  - `unorderedSets` {*true|false} Sort `Set` and `Map` instances before hashing
	*  * = default
	*
	* @param {object} object value to hash
	* @param {object} options hashing options
	* @return {string} hash value
	* @api public
	*/
	exports = module.exports = objectHash;
	function objectHash(object, options) {
		options = applyDefaults(object, options);
		return hash(object, options);
	}
	/**
	* Exported sugar methods
	*
	* @param {object} object value to hash
	* @return {string} hash value
	* @api public
	*/
	exports.sha1 = function(object) {
		return objectHash(object);
	};
	exports.keys = function(object) {
		return objectHash(object, {
			excludeValues: true,
			algorithm: "sha1",
			encoding: "hex"
		});
	};
	exports.MD5 = function(object) {
		return objectHash(object, {
			algorithm: "md5",
			encoding: "hex"
		});
	};
	exports.keysMD5 = function(object) {
		return objectHash(object, {
			algorithm: "md5",
			encoding: "hex",
			excludeValues: true
		});
	};
	var hashes = crypto.getHashes ? crypto.getHashes().slice() : ["sha1", "md5"];
	hashes.push("passthrough");
	var encodings = [
		"buffer",
		"hex",
		"binary",
		"base64"
	];
	function applyDefaults(object, sourceOptions) {
		sourceOptions = sourceOptions || {};
		var options = {};
		options.algorithm = sourceOptions.algorithm || "sha1";
		options.encoding = sourceOptions.encoding || "hex";
		options.excludeValues = sourceOptions.excludeValues ? true : false;
		options.algorithm = options.algorithm.toLowerCase();
		options.encoding = options.encoding.toLowerCase();
		options.ignoreUnknown = sourceOptions.ignoreUnknown !== true ? false : true;
		options.respectType = sourceOptions.respectType === false ? false : true;
		options.respectFunctionNames = sourceOptions.respectFunctionNames === false ? false : true;
		options.respectFunctionProperties = sourceOptions.respectFunctionProperties === false ? false : true;
		options.unorderedArrays = sourceOptions.unorderedArrays !== true ? false : true;
		options.unorderedSets = sourceOptions.unorderedSets === false ? false : true;
		options.unorderedObjects = sourceOptions.unorderedObjects === false ? false : true;
		options.replacer = sourceOptions.replacer || void 0;
		options.excludeKeys = sourceOptions.excludeKeys || void 0;
		if (typeof object === "undefined") throw new Error("Object argument required.");
		for (var i = 0; i < hashes.length; ++i) if (hashes[i].toLowerCase() === options.algorithm.toLowerCase()) options.algorithm = hashes[i];
		if (hashes.indexOf(options.algorithm) === -1) throw new Error("Algorithm \"" + options.algorithm + "\"  not supported. supported values: " + hashes.join(", "));
		if (encodings.indexOf(options.encoding) === -1 && options.algorithm !== "passthrough") throw new Error("Encoding \"" + options.encoding + "\"  not supported. supported values: " + encodings.join(", "));
		return options;
	}
	/** Check if the given function is a native function */
	function isNativeFunction(f) {
		if (typeof f !== "function") return false;
		return /^function\s+\w*\s*\(\s*\)\s*{\s+\[native code\]\s+}$/i.exec(Function.prototype.toString.call(f)) != null;
	}
	function hash(object, options) {
		var hashingStream;
		if (options.algorithm !== "passthrough") hashingStream = crypto.createHash(options.algorithm);
		else hashingStream = new PassThrough();
		if (typeof hashingStream.write === "undefined") {
			hashingStream.write = hashingStream.update;
			hashingStream.end = hashingStream.update;
		}
		typeHasher(options, hashingStream).dispatch(object);
		if (!hashingStream.update) hashingStream.end("");
		if (hashingStream.digest) return hashingStream.digest(options.encoding === "buffer" ? void 0 : options.encoding);
		var buf = hashingStream.read();
		if (options.encoding === "buffer") return buf;
		return buf.toString(options.encoding);
	}
	/**
	* Expose streaming API
	*
	* @param {object} object  Value to serialize
	* @param {object} options  Options, as for hash()
	* @param {object} stream  A stream to write the serializiation to
	* @api public
	*/
	exports.writeToStream = function(object, options, stream) {
		if (typeof stream === "undefined") {
			stream = options;
			options = {};
		}
		options = applyDefaults(object, options);
		return typeHasher(options, stream).dispatch(object);
	};
	function typeHasher(options, writeTo, context) {
		context = context || [];
		var write = function(str) {
			if (writeTo.update) return writeTo.update(str, "utf8");
			else return writeTo.write(str, "utf8");
		};
		return {
			dispatch: function(value) {
				if (options.replacer) value = options.replacer(value);
				var type = typeof value;
				if (value === null) type = "null";
				return this["_" + type](value);
			},
			_object: function(object) {
				var pattern = /\[object (.*)\]/i;
				var objString = Object.prototype.toString.call(object);
				var objType = pattern.exec(objString);
				if (!objType) objType = "unknown:[" + objString + "]";
				else objType = objType[1];
				objType = objType.toLowerCase();
				var objectNumber = null;
				if ((objectNumber = context.indexOf(object)) >= 0) return this.dispatch("[CIRCULAR:" + objectNumber + "]");
				else context.push(object);
				if (typeof Buffer !== "undefined" && Buffer.isBuffer && Buffer.isBuffer(object)) {
					write("buffer:");
					return write(object);
				}
				if (objType !== "object" && objType !== "function" && objType !== "asyncfunction") if (this["_" + objType]) this["_" + objType](object);
				else if (options.ignoreUnknown) return write("[" + objType + "]");
				else throw new Error("Unknown object type \"" + objType + "\"");
				else {
					var keys = Object.keys(object);
					if (options.unorderedObjects) keys = keys.sort();
					if (options.respectType !== false && !isNativeFunction(object)) keys.splice(0, 0, "prototype", "__proto__", "constructor");
					if (options.excludeKeys) keys = keys.filter(function(key) {
						return !options.excludeKeys(key);
					});
					write("object:" + keys.length + ":");
					var self = this;
					return keys.forEach(function(key) {
						self.dispatch(key);
						write(":");
						if (!options.excludeValues) self.dispatch(object[key]);
						write(",");
					});
				}
			},
			_array: function(arr, unordered) {
				unordered = typeof unordered !== "undefined" ? unordered : options.unorderedArrays !== false;
				var self = this;
				write("array:" + arr.length + ":");
				if (!unordered || arr.length <= 1) return arr.forEach(function(entry) {
					return self.dispatch(entry);
				});
				var contextAdditions = [];
				var entries = arr.map(function(entry) {
					var strm = new PassThrough();
					var localContext = context.slice();
					typeHasher(options, strm, localContext).dispatch(entry);
					contextAdditions = contextAdditions.concat(localContext.slice(context.length));
					return strm.read().toString();
				});
				context = context.concat(contextAdditions);
				entries.sort();
				return this._array(entries, false);
			},
			_date: function(date) {
				return write("date:" + date.toJSON());
			},
			_symbol: function(sym) {
				return write("symbol:" + sym.toString());
			},
			_error: function(err) {
				return write("error:" + err.toString());
			},
			_boolean: function(bool) {
				return write("bool:" + bool.toString());
			},
			_string: function(string) {
				write("string:" + string.length + ":");
				write(string.toString());
			},
			_function: function(fn) {
				write("fn:");
				if (isNativeFunction(fn)) this.dispatch("[native]");
				else this.dispatch(fn.toString());
				if (options.respectFunctionNames !== false) this.dispatch("function-name:" + String(fn.name));
				if (options.respectFunctionProperties) this._object(fn);
			},
			_number: function(number) {
				return write("number:" + number.toString());
			},
			_xml: function(xml) {
				return write("xml:" + xml.toString());
			},
			_null: function() {
				return write("Null");
			},
			_undefined: function() {
				return write("Undefined");
			},
			_regexp: function(regex) {
				return write("regex:" + regex.toString());
			},
			_uint8array: function(arr) {
				write("uint8array:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_uint8clampedarray: function(arr) {
				write("uint8clampedarray:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_int8array: function(arr) {
				write("int8array:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_uint16array: function(arr) {
				write("uint16array:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_int16array: function(arr) {
				write("int16array:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_uint32array: function(arr) {
				write("uint32array:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_int32array: function(arr) {
				write("int32array:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_float32array: function(arr) {
				write("float32array:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_float64array: function(arr) {
				write("float64array:");
				return this.dispatch(Array.prototype.slice.call(arr));
			},
			_arraybuffer: function(arr) {
				write("arraybuffer:");
				return this.dispatch(new Uint8Array(arr));
			},
			_url: function(url) {
				return write("url:" + url.toString(), "utf8");
			},
			_map: function(map) {
				write("map:");
				var arr = Array.from(map);
				return this._array(arr, options.unorderedSets !== false);
			},
			_set: function(set) {
				write("set:");
				var arr = Array.from(set);
				return this._array(arr, options.unorderedSets !== false);
			},
			_file: function(file) {
				write("file:");
				return this.dispatch([
					file.name,
					file.size,
					file.type,
					file.lastModfied
				]);
			},
			_blob: function() {
				if (options.ignoreUnknown) return write("[blob]");
				throw Error("Hashing Blob objects is currently not supported\n(see https://github.com/puleos/object-hash/issues/26)\nUse \"options.replacer\" or \"options.ignoreUnknown\"\n");
			},
			_domwindow: function() {
				return write("domwindow");
			},
			_bigint: function(number) {
				return write("bigint:" + number.toString());
			},
			_process: function() {
				return write("process");
			},
			_timer: function() {
				return write("timer");
			},
			_pipe: function() {
				return write("pipe");
			},
			_tcp: function() {
				return write("tcp");
			},
			_udp: function() {
				return write("udp");
			},
			_tty: function() {
				return write("tty");
			},
			_statwatcher: function() {
				return write("statwatcher");
			},
			_securecontext: function() {
				return write("securecontext");
			},
			_connection: function() {
				return write("connection");
			},
			_zlib: function() {
				return write("zlib");
			},
			_context: function() {
				return write("context");
			},
			_nodescript: function() {
				return write("nodescript");
			},
			_httpparser: function() {
				return write("httpparser");
			},
			_dataview: function() {
				return write("dataview");
			},
			_signal: function() {
				return write("signal");
			},
			_fsevent: function() {
				return write("fsevent");
			},
			_tlswrap: function() {
				return write("tlswrap");
			}
		};
	}
	function PassThrough() {
		return {
			buf: "",
			write: function(b) {
				this.buf += b;
			},
			end: function(b) {
				this.buf += b;
			},
			read: function() {
				return this.buf;
			}
		};
	}
})))(), 1);
init_react_import();
init_react_import();
function setDeep(node, path, newVal) {
	const parts = path.split(".");
	const newNode = __spreadValues$7({}, node);
	let cur = newNode;
	for (let i = 0; i < parts.length; i++) {
		const [prop, idxStr] = parts[i].replace("]", "").split("[");
		const isLast = i === parts.length - 1;
		if (idxStr !== void 0) {
			if (!Array.isArray(cur[prop])) cur[prop] = [];
			const idx = Number(idxStr);
			if (isLast) {
				cur[prop][idx] = newVal;
				continue;
			}
			if (cur[prop][idx] === void 0) cur[prop][idx] = {};
			cur = cur[prop][idx];
			continue;
		}
		if (isLast) {
			cur[prop] = newVal;
			continue;
		}
		if (cur[prop] === void 0) cur[prop] = {};
		cur = cur[prop];
	}
	return __spreadValues$7(__spreadValues$7({}, node), newNode);
}
init_react_import();
init_react_import();
var Button_module_default = {
	"Button": "_Button_10byl_1",
	"Button--medium": "_Button--medium_10byl_29",
	"Button--large": "_Button--large_10byl_37",
	"Button-icon": "_Button-icon_10byl_44",
	"Button--primary": "_Button--primary_10byl_48",
	"Button--secondary": "_Button--secondary_10byl_67",
	"Button--flush": "_Button--flush_10byl_84",
	"Button--disabled": "_Button--disabled_10byl_88",
	"Button--fullWidth": "_Button--fullWidth_10byl_95",
	"Button-spinner": "_Button-spinner_10byl_100"
};
init_react_import();
var dataAttrRe = /^(data-.*)$/;
var filterDataAttrs = (props) => {
	let filteredProps = {};
	for (const prop in props) if (Object.prototype.hasOwnProperty.call(props, prop) && dataAttrRe.test(prop)) filteredProps[prop] = props[prop];
	return filteredProps;
};
var getClassName = get_class_name_factory_default("Button", Button_module_default);
var Button$1 = (_a) => {
	var _b = _a, { children, href, onClick, variant = "primary", type, disabled, tabIndex, newTab, fullWidth, icon, size = "medium", loading: loadingProp = false } = _b, props = __objRest$4(_b, [
		"children",
		"href",
		"onClick",
		"variant",
		"type",
		"disabled",
		"tabIndex",
		"newTab",
		"fullWidth",
		"icon",
		"size",
		"loading"
	]);
	const [loading, setLoading] = (0, import_react.useState)(loadingProp);
	(0, import_react.useEffect)(() => setLoading(loadingProp), [loadingProp]);
	const ElementType = href ? "a" : type ? "button" : "span";
	const dataAttrs = filterDataAttrs(props);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ElementType, __spreadProps$6(__spreadValues$7({
		className: getClassName({
			primary: variant === "primary",
			secondary: variant === "secondary",
			disabled,
			fullWidth,
			[size]: true
		}),
		onClick: (e) => {
			if (!onClick) return;
			setLoading(true);
			Promise.resolve(onClick(e)).then(() => {
				setLoading(false);
			});
		},
		type,
		disabled: disabled || loading,
		tabIndex,
		target: newTab ? "_blank" : void 0,
		rel: newTab ? "noreferrer" : void 0,
		href
	}, dataAttrs), { children: [
		icon && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName("icon"),
			children: icon
		}),
		children,
		loading && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName("spinner"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Loader, { size: 14 })
		})
	] }));
};
init_react_import();
init_react_import();
var styles_module_default = {
	"InputWrapper": "_InputWrapper_bsxfo_1",
	"Input-label": "_Input-label_bsxfo_5",
	"Input-labelIcon": "_Input-labelIcon_bsxfo_14",
	"Input-disabledIcon": "_Input-disabledIcon_bsxfo_21",
	"Input-input": "_Input-input_bsxfo_26",
	"Input": "_Input_bsxfo_1",
	"Input--readOnly": "_Input--readOnly_bsxfo_82",
	"Input-radioGroupItems": "_Input-radioGroupItems_bsxfo_93",
	"Input-radio": "_Input-radio_bsxfo_93",
	"Input-radioInner": "_Input-radioInner_bsxfo_110",
	"Input-radioInput": "_Input-radioInput_bsxfo_155"
};
var getClassName2 = get_class_name_factory_default("Input", styles_module_default);
var FieldLabel = ({ children, icon, label, el = "label", readOnly, className }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(el, {
		className,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: getClassName2("label"),
			children: [
				icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: getClassName2("labelIcon"),
					children: icon
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {}),
				label,
				readOnly && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: getClassName2("disabledIcon"),
					title: "Read-only",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Lock, { size: "12" })
				})
			]
		}), children]
	});
};
var FieldLabelInternal = ({ children, icon, label, el = "label", readOnly }) => {
	const overrides = useAppStore((s) => s.overrides);
	const Wrapper = (0, import_react.useMemo)(() => overrides.fieldLabel || FieldLabel, [overrides]);
	if (!label) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Wrapper, {
		label,
		icon,
		className: getClassName2({ readOnly }),
		readOnly,
		el,
		children
	});
};
init_react_import();
init_react_import();
init_react_import();
init_react_import();
var styles_module_default2 = {
	"ArrayField": "_ArrayField_1vaho_5",
	"ArrayField--isDraggingFrom": "_ArrayField--isDraggingFrom_1vaho_13",
	"ArrayField-addButton": "_ArrayField-addButton_1vaho_18",
	"ArrayField--hasItems": "_ArrayField--hasItems_1vaho_33",
	"ArrayField-inner": "_ArrayField-inner_1vaho_59",
	"ArrayFieldItem": "_ArrayFieldItem_1vaho_67",
	"ArrayFieldItem--isDragging": "_ArrayFieldItem--isDragging_1vaho_78",
	"ArrayFieldItem--isExpanded": "_ArrayFieldItem--isExpanded_1vaho_82",
	"ArrayFieldItem-summary": "_ArrayFieldItem-summary_1vaho_97",
	"ArrayFieldItem--noFields": "_ArrayFieldItem--noFields_1vaho_122",
	"ArrayField--addDisabled": "_ArrayField--addDisabled_1vaho_131",
	"ArrayFieldItem-body": "_ArrayFieldItem-body_1vaho_170",
	"ArrayFieldItem-fieldset": "_ArrayFieldItem-fieldset_1vaho_179",
	"ArrayFieldItem-rhs": "_ArrayFieldItem-rhs_1vaho_187",
	"ArrayFieldItem-actions": "_ArrayFieldItem-actions_1vaho_193"
};
init_react_import();
init_react_import();
function useContextStore(context, selector) {
	const store = (0, import_react.useContext)(context);
	if (!store) throw new Error("useContextStore must be used inside context");
	return useStore(store, useShallow(selector));
}
function createStoreProvider(ContextComponent) {
	const StoreProvider = ({ children, value }) => {
		const [store] = (0, import_react.useState)(() => createStore(() => value));
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ContextComponent.Provider, {
			value: store,
			children
		});
	};
	return StoreProvider;
}
function createContextStore(defaultValue) {
	const ctx = (0, import_react.createContext)(createStore(subscribeWithSelector(() => defaultValue)));
	return {
		ctx,
		Provider: createStoreProvider(ctx)
	};
}
var fieldContextStore = createContextStore({});
var useFieldStoreApi = () => (0, import_react.useContext)(fieldContextStore.ctx);
function useFieldStore(selector) {
	const store = (0, import_react.useContext)(fieldContextStore.ctx);
	if (!store) throw new Error("useContextStore must be used inside context");
	return useStore(store, useShallow(selector));
}
init_react_import();
init_react_import();
var getClassName3 = get_class_name_factory_default("DragIcon", {
	"DragIcon": "_DragIcon_17p8x_1",
	"DragIcon--disabled": "_DragIcon--disabled_17p8x_8"
});
var DragIcon = ({ isDragDisabled }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
	className: getClassName3({ disabled: isDragDisabled }),
	children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("svg", {
		viewBox: "0 0 20 20",
		width: "12",
		fill: "currentColor",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("path", { d: "M7 2a2 2 0 1 0 .001 4.001A2 2 0 0 0 7 2zm0 6a2 2 0 1 0 .001 4.001A2 2 0 0 0 7 8zm0 6a2 2 0 1 0 .001 4.001A2 2 0 0 0 7 14zm6-8a2 2 0 1 0-.001-4.001A2 2 0 0 0 13 6zm0 2a2 2 0 1 0 .001 4.001A2 2 0 0 0 13 8zm0 6a2 2 0 1 0 .001 4.001A2 2 0 0 0 13 14z" })
	})
});
init_react_import();
init_react_import();
var touchDefault = { delay: {
	value: 200,
	tolerance: 10
} };
var otherDefault = {
	delay: {
		value: 200,
		tolerance: 10
	},
	distance: { value: 5 }
};
var useSensors = ({ other = otherDefault, mouse, touch = touchDefault } = {
	touch: touchDefault,
	other: otherDefault
}) => {
	const [sensors] = (0, import_react.useState)(() => [PointerSensor.configure({ activationConstraints(event, source) {
		var _a;
		const { pointerType, target } = event;
		if (pointerType === "mouse" && isElement(target) && (source.handle === target || ((_a = source.handle) == null ? void 0 : _a.contains(target)))) return mouse;
		if (pointerType === "touch") return touch;
		return other;
	} })]);
	return sensors;
};
init_react_import();
init_react_import();
init_react_import();
var DEBUG = false;
var debugElements = {};
var timeout;
var collisionDebug = (a, b, id, color, label) => {
	if (!DEBUG) return;
	const debugId = `${id}-debug`;
	clearTimeout(timeout);
	timeout = setTimeout(() => {
		Object.entries(debugElements).forEach(([id2, { svg }]) => {
			svg.remove();
			delete debugElements[id2];
		});
	}, 1e3);
	requestAnimationFrame(() => {
		var _a, _b;
		const existingEl = debugElements[debugId];
		let line = (_a = debugElements[debugId]) == null ? void 0 : _a.line;
		let text = (_b = debugElements[debugId]) == null ? void 0 : _b.text;
		if (!existingEl) {
			const svgNs = "http://www.w3.org/2000/svg";
			const svg = document.createElementNS(svgNs, "svg");
			line = document.createElementNS(svgNs, "line");
			text = document.createElementNS(svgNs, "text");
			svg.setAttribute("id", debugId);
			svg.setAttribute("style", "position: fixed; height: 100%; width: 100%; pointer-events: none; top: 0px; left: 0px;");
			svg.appendChild(line);
			svg.appendChild(text);
			text.setAttribute("fill", `black`);
			document.body.appendChild(svg);
			debugElements[debugId] = {
				svg,
				line,
				text
			};
		}
		line.setAttribute("x1", a.x.toString());
		line.setAttribute("x2", b.x.toString());
		line.setAttribute("y1", a.y.toString());
		line.setAttribute("y2", b.y.toString());
		line.setAttribute("style", `stroke:${color};stroke-width:2`);
		text.setAttribute("x", (a.x - (a.x - b.x) / 2).toString());
		text.setAttribute("y", (a.y - (a.y - b.y) / 2).toString());
		if (label) text.innerHTML = label;
	});
};
var distanceChange = "increasing";
var directionalCollision = (input, previous) => {
	var _a;
	const { dragOperation, droppable } = input;
	const { shape: dropShape } = droppable;
	const { position } = dragOperation;
	const dragShape = (_a = dragOperation.shape) == null ? void 0 : _a.current;
	if (!dragShape || !dropShape) return null;
	const dropCenter = dropShape.center;
	const distanceToPrevious = Math.sqrt(Math.pow(dropCenter.x - previous.x, 2) + Math.pow(dropCenter.y - previous.y, 2));
	const distanceToCurrent = Math.sqrt(Math.pow(dropCenter.x - position.current.x, 2) + Math.pow(dropCenter.y - position.current.y, 2));
	distanceChange = distanceToCurrent === distanceToPrevious ? distanceChange : distanceToCurrent < distanceToPrevious ? "decreasing" : "increasing";
	collisionDebug(dragShape.center, dropCenter, droppable.id.toString(), "rebeccapurple");
	if (distanceChange === "decreasing") return {
		id: droppable.id,
		value: 1,
		type: CollisionType.Collision
	};
	return null;
};
init_react_import();
var getDirection = (dragAxis, delta) => {
	if (dragAxis === "dynamic") if (Math.abs(delta.y) > Math.abs(delta.x)) return delta.y === 0 ? null : delta.y > 0 ? "down" : "up";
	else return delta.x === 0 ? null : delta.x > 0 ? "right" : "left";
	else if (dragAxis === "x") return delta.x === 0 ? null : delta.x > 0 ? "right" : "left";
	return delta.y === 0 ? null : delta.y > 0 ? "down" : "up";
};
init_react_import();
var getMidpointImpact = (dragShape, dropShape, direction, offsetMultiplier = 0) => {
	const dragRect = dragShape.boundingRectangle;
	const dropCenter = dropShape.center;
	if (direction === "down") {
		const offset2 = offsetMultiplier * dropShape.boundingRectangle.height;
		return dragRect.bottom >= dropCenter.y + offset2;
	} else if (direction === "up") {
		const offset2 = offsetMultiplier * dropShape.boundingRectangle.height;
		return dragRect.top < dropCenter.y - offset2;
	} else if (direction === "left") {
		const offset2 = offsetMultiplier * dropShape.boundingRectangle.width;
		return dropCenter.x - offset2 >= dragRect.left;
	}
	const offset = offsetMultiplier * dropShape.boundingRectangle.width;
	return dragRect.right - offset >= dropCenter.x;
};
init_react_import();
var INTERVAL_SENSITIVITY = 10;
var intervalCache = {
	current: {
		x: 0,
		y: 0
	},
	delta: {
		x: 0,
		y: 0
	},
	previous: {
		x: 0,
		y: 0
	},
	direction: null
};
var trackMovementInterval = (point, dragAxis = "dynamic") => {
	intervalCache.current = point;
	intervalCache.delta = {
		x: point.x - intervalCache.previous.x,
		y: point.y - intervalCache.previous.y
	};
	intervalCache.direction = getDirection(dragAxis, intervalCache.delta) || intervalCache.direction;
	if (Math.abs(intervalCache.delta.x) > INTERVAL_SENSITIVITY || Math.abs(intervalCache.delta.y) > INTERVAL_SENSITIVITY) intervalCache.previous = Point$1.from(point);
	return intervalCache;
};
init_react_import();
var pointerIntersection = ({ dragOperation, droppable }) => {
	const pointerCoordinates = dragOperation.position.current;
	if (!pointerCoordinates) return null;
	const { id } = droppable;
	if (!droppable.shape) return null;
	if (droppable.shape.containsPoint(pointerCoordinates)) return {
		id,
		value: 1 / Point$1.distance(droppable.shape.center, pointerCoordinates),
		type: CollisionType.PointerIntersection,
		priority: CollisionPriority.High
	};
	return null;
};
var closestCorners = (input) => {
	const { dragOperation, droppable } = input;
	const { shape, position } = dragOperation;
	if (!droppable.shape) return null;
	const shapeCorners = shape ? Rectangle.from(shape.current.boundingRectangle).corners : void 0;
	const value = Rectangle.from(droppable.shape.boundingRectangle).corners.reduce((acc, corner, index) => {
		var _a;
		return acc + Point$1.distance(Point$1.from(corner), (_a = shapeCorners == null ? void 0 : shapeCorners[index]) != null ? _a : position.current);
	}, 0) / 4;
	return {
		id: droppable.id,
		value: 1 / value,
		type: CollisionType.Collision,
		priority: CollisionPriority.Normal
	};
};
init_react_import();
var collisionStore = createStore(() => ({ fallbackEnabled: false }));
var flushNext = "";
var createDynamicCollisionDetector = (dragAxis, midpointOffset = .05) => ((input) => {
	var _a, _b, _c, _d, _e;
	const { dragOperation, droppable } = input;
	const { position } = dragOperation;
	const dragShape = (_a = dragOperation.shape) == null ? void 0 : _a.current;
	const { shape: dropShape } = droppable;
	if (!dragShape || !dropShape) return null;
	const { center: dragCenter } = dragShape;
	const { fallbackEnabled } = collisionStore.getState();
	const interval = trackMovementInterval(position.current, dragAxis);
	const data = { direction: interval.direction };
	const { center: dropCenter } = dropShape;
	const overMidpoint = getMidpointImpact(dragShape, dropShape, interval.direction, midpointOffset);
	if (((_b = dragOperation.source) == null ? void 0 : _b.id) === droppable.id) {
		const collision = directionalCollision(input, interval.previous);
		collisionDebug(dragCenter, dropCenter, droppable.id.toString(), "yellow");
		if (collision) return __spreadProps$6(__spreadValues$7({}, collision), {
			priority: CollisionPriority.Highest,
			data
		});
	}
	const intersectionArea = dragShape.intersectionArea(dropShape);
	const intersectionRatio = intersectionArea / dropShape.area;
	if (intersectionArea && overMidpoint) {
		collisionDebug(dragCenter, dropCenter, droppable.id.toString(), "green", interval.direction);
		const collision = {
			id: droppable.id,
			value: intersectionRatio,
			priority: CollisionPriority.High,
			type: CollisionType.Collision
		};
		const shouldFlushId = flushNext === droppable.id;
		flushNext = "";
		return __spreadProps$6(__spreadValues$7({}, collision), {
			id: shouldFlushId ? "flush" : collision.id,
			data
		});
	}
	if (fallbackEnabled && ((_c = dragOperation.source) == null ? void 0 : _c.id) !== droppable.id) {
		const xAxisIntersection = dropShape.boundingRectangle.right > dragShape.boundingRectangle.left && dropShape.boundingRectangle.left < dragShape.boundingRectangle.right;
		const yAxisIntersection = dropShape.boundingRectangle.bottom > dragShape.boundingRectangle.top && dropShape.boundingRectangle.top < dragShape.boundingRectangle.bottom;
		if (dragAxis === "y" && xAxisIntersection || yAxisIntersection) {
			const fallbackCollision = closestCorners(input);
			if (fallbackCollision) {
				const direction = getDirection(dragAxis, {
					x: dragShape.center.x - (((_d = droppable.shape) == null ? void 0 : _d.center.x) || 0),
					y: dragShape.center.y - (((_e = droppable.shape) == null ? void 0 : _e.center.y) || 0)
				});
				data.direction = direction;
				if (intersectionArea) {
					collisionDebug(dragCenter, dropCenter, droppable.id.toString(), "red", direction || "");
					flushNext = droppable.id;
					return __spreadProps$6(__spreadValues$7({}, fallbackCollision), {
						priority: CollisionPriority.Low,
						data
					});
				}
				collisionDebug(dragCenter, dropCenter, droppable.id.toString(), "orange", direction || "");
				return __spreadProps$6(__spreadValues$7({}, fallbackCollision), {
					priority: CollisionPriority.Lowest,
					data
				});
			}
		}
	}
	collisionDebug(dragCenter, dropCenter, droppable.id.toString(), "hotpink");
	return null;
});
var SortableProvider = ({ children, onDragStart, onDragEnd, onMove }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DragDropProvider, {
		sensors: useSensors({ mouse: { distance: { value: 5 } } }),
		onDragStart: (event) => {
			var _a, _b;
			return onDragStart((_b = (_a = event.operation.source) == null ? void 0 : _a.id.toString()) != null ? _b : "");
		},
		onDragOver: (event, manager) => {
			var _a;
			event.preventDefault();
			const { operation } = event;
			const { source, target } = operation;
			if (!source || !target) return;
			let sourceIndex = source.data.index;
			let targetIndex = target.data.index;
			const collisionData = (_a = manager.collisionObserver.collisions[0]) == null ? void 0 : _a.data;
			if (sourceIndex !== targetIndex && source.id !== target.id) {
				const collisionPosition = (collisionData == null ? void 0 : collisionData.direction) === "up" ? "before" : "after";
				if (targetIndex >= sourceIndex) targetIndex = targetIndex - 1;
				if (collisionPosition === "after") targetIndex = targetIndex + 1;
				onMove({
					source: sourceIndex,
					target: targetIndex
				});
			}
		},
		onDragEnd: () => {
			setTimeout(() => {
				onDragEnd();
			}, 250);
		},
		children
	});
};
var Sortable = ({ id, index, disabled, children, type = "item" }) => {
	const { ref: sortableRef, isDragging, isDropping, handleRef } = useSortable({
		id,
		type,
		index,
		disabled,
		data: { index },
		collisionDetector: createDynamicCollisionDetector("y")
	});
	return children({
		isDragging,
		isDropping,
		ref: sortableRef,
		handleRef
	});
};
init_react_import();
var NestedFieldContext = (0, import_react.createContext)({});
var useNestedFieldContext = () => {
	const context = (0, import_react.useContext)(NestedFieldContext);
	return __spreadProps$6(__spreadValues$7({}, context), { readOnlyFields: context.readOnlyFields || {} });
};
var NestedFieldProvider = ({ children, name, subName, wildcardName = name, readOnlyFields }) => {
	const subPath = `${name}.${subName}`;
	const wildcardSubPath = `${wildcardName}.${subName}`;
	const subReadOnlyFields = (0, import_react.useMemo)(() => Object.keys(readOnlyFields).reduce((acc, readOnlyKey) => {
		if (readOnlyKey.indexOf(subPath) > -1 || readOnlyKey.indexOf(wildcardSubPath) > -1) {
			const subPathPattern = new RegExp(`^(${name}|${wildcardName}).`.replace(/\[/g, "\\[").replace(/\]/g, "\\]").replace(/\./g, "\\.").replace(/\*/g, "\\*"));
			const localName = readOnlyKey.replace(subPathPattern, "");
			return __spreadProps$6(__spreadValues$7({}, acc), { [localName]: readOnlyFields[readOnlyKey] });
		}
		return acc;
	}, {}), [
		name,
		subName,
		wildcardName,
		readOnlyFields
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NestedFieldContext.Provider, {
		value: {
			readOnlyFields: subReadOnlyFields,
			localName: subName
		},
		children
	});
};
init_react_import();
var getDeep = (node, path) => {
	return path.split(".").reduce((acc, item) => {
		if (!acc) return;
		const [prop, indexStr] = item.replace("]", "").split("[");
		const val = acc[prop];
		if (indexStr && val) return val[parseInt(indexStr)];
		return val;
	}, node);
};
init_react_import();
var SubFieldInternal = ({ field, id, index, name, subName, localName, onChange, forceReadOnly }) => {
	const indexName = typeof index !== "undefined" ? `${name}[${index}]` : name;
	const subPath = name ? `${indexName}.${subName}` : subName;
	const localIndexName = typeof index !== "undefined" ? `${localName}[${index}]` : localName != null ? localName : subName;
	const localWildcardName = typeof index !== "undefined" ? `${localName}[*]` : localName;
	const localSubPath = `${localIndexName}.${subName}`;
	const localWildcardSubPath = `${localWildcardName}.${subName}`;
	const { readOnlyFields } = useNestedFieldContext();
	const subReadOnly = forceReadOnly ? forceReadOnly : typeof readOnlyFields[subPath] !== "undefined" ? readOnlyFields[localSubPath] : readOnlyFields[localWildcardSubPath];
	const label = field.label || subName;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NestedFieldProvider, {
		name: localIndexName,
		wildcardName: localWildcardName,
		subName,
		readOnlyFields,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AutoFieldPrivate, {
			name: subPath,
			label,
			id,
			readOnly: subReadOnly,
			field: __spreadProps$6(__spreadValues$7({}, field), { label }),
			onChange: (val, ui) => {
				onChange(val, ui, subName);
			}
		})
	});
};
var SubField = (0, import_react.memo)(SubFieldInternal);
var getClassName4 = get_class_name_factory_default("ArrayField", styles_module_default2);
var getClassNameItem = get_class_name_factory_default("ArrayFieldItem", styles_module_default2);
var ItemSummaryInner = ({ index, originalIndex, field, name }) => {
	const data = useFieldStore((s) => {
		return getDeep(s, `${[name]}[${index}]`);
	});
	return (0, import_react.useMemo)(() => {
		if (data && field.getItemSummary) return field.getItemSummary(data, index);
		return `Item #${originalIndex}`;
	}, [
		data,
		field,
		originalIndex,
		index
	]);
};
var ItemSummary = (0, import_react.memo)(ItemSummaryInner);
var ArrayFieldItemInternal = ({ id, arrayId, index, dragIndex, originalIndex, field, onChange, onToggleExpand, readOnly, actions, name, localName }) => {
	const isExpanded = useAppStore((s) => {
		var _a;
		return ((_a = s.state.ui.arrayState[arrayId]) == null ? void 0 : _a.openId) === id;
	});
	const canEdit = useAppStore((s) => s.permissions.getPermissions({ item: s.selectedItem }).edit);
	const hasVisibleFields = (0, import_react.useMemo)(() => {
		if (!field.arrayFields) return false;
		return Object.values(field.arrayFields).some((subField) => subField.type !== "slot" && subField.visible !== false);
	}, [field.arrayFields]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Sortable, {
		id,
		index: dragIndex,
		disabled: readOnly,
		children: ({ isDragging, ref, handleRef }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			ref,
			className: getClassNameItem({
				isExpanded: isExpanded && hasVisibleFields,
				isDragging,
				noFields: !hasVisibleFields
			}),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				ref: handleRef,
				onClick: (e) => {
					if (isDragging) return;
					e.preventDefault();
					e.stopPropagation();
					if (!hasVisibleFields) return;
					onToggleExpand(id, isExpanded);
				},
				className: getClassNameItem("summary"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ItemSummary, {
					index,
					originalIndex,
					field,
					name
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: getClassNameItem("rhs"),
					children: [!readOnly && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassNameItem("actions"),
						children: actions
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DragIcon, {}) })]
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassNameItem("body"),
				children: isExpanded && hasVisibleFields && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("fieldset", {
					className: getClassNameItem("fieldset"),
					children: Object.keys(field.arrayFields).map((subName) => {
						const subField = field.arrayFields[subName];
						return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SubField, {
							id: `${id}_${subName}`,
							name,
							index,
							subName,
							localName,
							field: subField,
							onChange,
							forceReadOnly: !canEdit
						}, `${id}_${subName}_${index}`);
					})
				})
			})]
		})
	});
};
var ArrayFieldItem = (0, import_react.memo)(ArrayFieldItemInternal);
var ArrayField = ({ field, onChange, id, name = id, label, labelIcon, readOnly, Label = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", __spreadValues$7({}, props)) }) => {
	const setUi = useAppStore((s) => s.setUi);
	const appStoreApi = useAppStoreApi();
	const fieldStore = useFieldStoreApi();
	const { localName = name } = useNestedFieldContext();
	const getValue = () => {
		var _a;
		return (_a = getDeep(fieldStore.getState(), name)) != null ? _a : [];
	};
	const getArrayState = (0, import_react.useCallback)(() => {
		var _a;
		const { state } = appStoreApi.getState();
		const thisState = state.ui.arrayState[id];
		if ((_a = thisState == null ? void 0 : thisState.items) == null ? void 0 : _a.length) return thisState;
		const value = getValue();
		return {
			items: Array.from(value || []).map((item, idx) => {
				return {
					_originalIndex: idx,
					_currentIndex: idx,
					_arrayId: `${id}-${idx}`
				};
			}),
			openId: ""
		};
	}, [
		appStoreApi,
		id,
		getValue,
		name
	]);
	const numItems = useFieldStore(() => {
		return getValue().length;
	});
	const defaultArrayState = (0, import_react.useMemo)(getArrayState, [getArrayState]);
	const mirror = useAppStore((s) => {
		const thisArrayState = s.state.ui.arrayState[id];
		return thisArrayState != null ? thisArrayState : defaultArrayState;
	});
	const appStore = useAppStoreApi();
	const mapArrayStateToUi = (0, import_react.useCallback)((partialArrayState) => {
		const state = appStore.getState().state;
		return { arrayState: __spreadProps$6(__spreadValues$7({}, state.ui.arrayState), { [id]: __spreadValues$7(__spreadValues$7({}, getArrayState()), partialArrayState) }) };
	}, [appStore]);
	const getHighestIndex = (0, import_react.useCallback)(() => {
		return getArrayState().items.reduce((acc, item) => item._originalIndex > acc ? item._originalIndex : acc, -1);
	}, []);
	const regenerateArrayState = (0, import_react.useCallback)((value) => {
		let highestIndex = getHighestIndex();
		const arrayState = getArrayState();
		const newItems = Array.from(value || []).map((item, idx) => {
			var _a, _b, _c;
			const arrayStateItem = arrayState.items[idx];
			const newItem = {
				_originalIndex: (_a = arrayStateItem == null ? void 0 : arrayStateItem._originalIndex) != null ? _a : highestIndex + 1,
				_currentIndex: (_b = arrayStateItem == null ? void 0 : arrayStateItem._currentIndex) != null ? _b : idx,
				_arrayId: ((_c = arrayState.items[idx]) == null ? void 0 : _c._arrayId) || `${id}-${highestIndex + 1}`
			};
			if (newItem._originalIndex > highestIndex) highestIndex = newItem._originalIndex;
			return newItem;
		});
		return __spreadProps$6(__spreadValues$7({}, arrayState), { items: newItems });
	}, []);
	const [draggedItem, setDraggedItem] = (0, import_react.useState)("");
	const isDraggingAny = !!draggedItem;
	const valueRef = (0, import_react.useRef)([]);
	(0, import_react.useEffect)(() => {
		valueRef.current = getValue();
	}, []);
	const uniqifyItem = (0, import_react.useCallback)((val) => {
		if (field.type !== "array" || !field.arrayFields) return;
		const config = appStore.getState().config;
		return walkField({
			value: val,
			fields: field.arrayFields,
			mappers: { slot: ({ value }) => {
				return value.map((item) => populateIds(item, config, true));
			} },
			config
		});
	}, [appStore, field]);
	const syncCurrentIndexes = (0, import_react.useCallback)(() => {
		const arrayState = getArrayState();
		const newArrayStateItems = arrayState.items.map((item, index) => __spreadProps$6(__spreadValues$7({}, item), { _currentIndex: index }));
		const state = appStore.getState().state;
		setUi({ arrayState: __spreadProps$6(__spreadValues$7({}, state.ui.arrayState), { [id]: __spreadProps$6(__spreadValues$7({}, arrayState), { items: newArrayStateItems }) }) }, false);
	}, []);
	const updateValue = (0, import_react.useCallback)((newValue) => {
		setUi(mapArrayStateToUi(regenerateArrayState(newValue)), false);
		onChange(newValue);
	}, [
		regenerateArrayState,
		setUi,
		mapArrayStateToUi,
		onChange
	]);
	(0, import_react.useEffect)(() => {
		setUi(mapArrayStateToUi(regenerateArrayState(getValue())), false);
	}, [numItems]);
	if (field.type !== "array" || !field.arrayFields) return null;
	const addDisabled = field.max !== void 0 && (mirror == null ? void 0 : mirror.items.length) >= field.max || readOnly;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
		label: label || name,
		icon: labelIcon || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(List, { size: 16 }),
		el: "div",
		readOnly,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SortableProvider, {
			onDragStart: (id2) => {
				valueRef.current = getValue();
				setDraggedItem(id2);
				syncCurrentIndexes();
			},
			onDragEnd: () => {
				setDraggedItem("");
				onChange(valueRef.current);
				const currentFieldVal = fieldStore.getState();
				fieldStore.setState(setDeep(currentFieldVal, name, valueRef.current));
				syncCurrentIndexes();
			},
			onMove: (move) => {
				const arrayState = getArrayState();
				if (arrayState.items[move.source]._arrayId !== draggedItem) return;
				const newValue = reorder$1(valueRef.current, move.source, move.target);
				const newArrayStateItems = reorder$1(arrayState.items, move.source, move.target);
				const state = appStore.getState().state;
				setUi({ arrayState: __spreadProps$6(__spreadValues$7({}, state.ui.arrayState), { [id]: __spreadProps$6(__spreadValues$7({}, arrayState), { items: newArrayStateItems }) }) }, false);
				valueRef.current = newValue;
			},
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: getClassName4({
					hasItems: numItems > 0,
					addDisabled
				}),
				children: [mirror.items.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: getClassName4("inner"),
					"data-dnd-container": true,
					children: mirror.items.map((item, index) => {
						const { _arrayId = `${id}-${index}`, _originalIndex = index, _currentIndex = index } = item;
						return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ArrayFieldItem, {
							index: _currentIndex,
							dragIndex: index,
							originalIndex: _originalIndex,
							arrayId: id,
							id: _arrayId,
							readOnly,
							field,
							name,
							localName,
							onChange: (val, ui, subName) => {
								const value = getValue();
								onChange(replace(value, index, __spreadProps$6(__spreadValues$7({}, Array.from(value || [])[index] || {}), { [subName]: val })), ui);
							},
							onToggleExpand: (id2, isExpanded) => {
								if (isExpanded) setUi(mapArrayStateToUi({ openId: "" }));
								else setUi(mapArrayStateToUi({ openId: id2 }));
							},
							actions: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: getClassNameItem("action"),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
									type: "button",
									disabled: !!addDisabled,
									onClick: (e) => {
										e.stopPropagation();
										const existingValue = [...getValue() || []];
										const newItem = uniqifyItem(existingValue[index]);
										existingValue.splice(index, 0, newItem);
										updateValue(existingValue);
									},
									title: "Duplicate",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, { size: 16 })
								})
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: getClassNameItem("action"),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
									type: "button",
									disabled: field.min !== void 0 && field.min >= mirror.items.length,
									onClick: (e) => {
										e.stopPropagation();
										const existingValue = [...getValue() || []];
										existingValue.splice(index, 1);
										updateValue(existingValue);
									},
									title: "Delete",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash, { size: 16 })
								})
							})] })
						}, _arrayId);
					})
				}), !addDisabled && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
					type: "button",
					className: getClassName4("addButton"),
					onClick: () => {
						var _a;
						if (isDraggingAny) return;
						const existingValue = getValue() || [];
						const newItem = defaultSlots(uniqifyItem(typeof field.defaultItemProps === "function" ? field.defaultItemProps(existingValue.length) : (_a = field.defaultItemProps) != null ? _a : {}), field.arrayFields);
						updateValue([...existingValue, newItem]);
					},
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Plus, { size: 21 })
				})]
			})
		})
	});
};
init_react_import();
init_react_import();
init_react_import();
var useDeepField = (path) => {
	return useFieldStore((s) => getDeep(s, path));
};
init_react_import();
var useIsFocused = (path) => {
	return useAppStore((s) => s.state.ui.field.focus === path);
};
var useLocalValue = (path, onChange) => {
	const value = useDeepField(path);
	const isFocused = useIsFocused(path);
	const [localValue, setLocalValue] = (0, import_react.useState)(value == null ? void 0 : value.toString());
	const onChangeLocal = (0, import_react.useCallback)((val) => {
		setLocalValue(val);
		onChange(val);
	}, [onChange]);
	(0, import_react.useEffect)(() => {
		if (!isFocused) setLocalValue(value);
	}, [isFocused, value]);
	return [localValue != null ? localValue : "", onChangeLocal];
};
var getClassName5 = get_class_name_factory_default("Input", styles_module_default);
var DefaultField = ({ field, onChange, readOnly, id, name = id, label, labelIcon, Label }) => {
	const [localValue, onChangeLocal] = useLocalValue(name, onChange);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
		label: label || name,
		icon: labelIcon || /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [field.type === "text" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Type, { size: 16 }), field.type === "number" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Hash, { size: 16 })] }),
		readOnly,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
			className: getClassName5("input"),
			autoComplete: "off",
			type: field.type,
			title: label || name,
			name,
			value: localValue,
			onChange: (e) => {
				if (field.type === "number") {
					const numberValue = Number(e.currentTarget.value);
					if (typeof field.min !== "undefined" && numberValue < field.min) return;
					if (typeof field.max !== "undefined" && numberValue > field.max) return;
					onChangeLocal(numberValue);
				} else onChangeLocal(e.currentTarget.value);
			},
			readOnly,
			tabIndex: readOnly ? -1 : void 0,
			id,
			min: field.type === "number" ? field.min : void 0,
			max: field.type === "number" ? field.max : void 0,
			placeholder: field.type === "text" || field.type === "number" ? field.placeholder : void 0,
			step: field.type === "number" ? field.step : void 0
		})
	});
};
init_react_import();
init_react_import();
init_react_import();
var styles_module_default4 = {
	"ExternalInput-actions": "_ExternalInput-actions_91ls0_1",
	"ExternalInput-button": "_ExternalInput-button_91ls0_5",
	"ExternalInput--dataSelected": "_ExternalInput--dataSelected_91ls0_24",
	"ExternalInput--readOnly": "_ExternalInput--readOnly_91ls0_31",
	"ExternalInput-detachButton": "_ExternalInput-detachButton_91ls0_35",
	"ExternalInput": "_ExternalInput_91ls0_1",
	"ExternalInputModal": "_ExternalInputModal_91ls0_79",
	"ExternalInputModal-grid": "_ExternalInputModal-grid_91ls0_89",
	"ExternalInputModal--filtersToggled": "_ExternalInputModal--filtersToggled_91ls0_100",
	"ExternalInputModal-filters": "_ExternalInputModal-filters_91ls0_105",
	"ExternalInputModal-masthead": "_ExternalInputModal-masthead_91ls0_124",
	"ExternalInputModal-tableWrapper": "_ExternalInputModal-tableWrapper_91ls0_133",
	"ExternalInputModal-table": "_ExternalInputModal-table_91ls0_133",
	"ExternalInputModal-thead": "_ExternalInputModal-thead_91ls0_149",
	"ExternalInputModal-th": "_ExternalInputModal-th_91ls0_149",
	"ExternalInputModal-td": "_ExternalInputModal-td_91ls0_164",
	"ExternalInputModal-tr": "_ExternalInputModal-tr_91ls0_169",
	"ExternalInputModal-tbody": "_ExternalInputModal-tbody_91ls0_176",
	"ExternalInputModal--hasData": "_ExternalInputModal--hasData_91ls0_202",
	"ExternalInputModal-loadingBanner": "_ExternalInputModal-loadingBanner_91ls0_206",
	"ExternalInputModal--isLoading": "_ExternalInputModal--isLoading_91ls0_223",
	"ExternalInputModal-searchForm": "_ExternalInputModal-searchForm_91ls0_227",
	"ExternalInputModal-search": "_ExternalInputModal-search_91ls0_227",
	"ExternalInputModal-searchIcon": "_ExternalInputModal-searchIcon_91ls0_264",
	"ExternalInputModal-searchIconText": "_ExternalInputModal-searchIconText_91ls0_289",
	"ExternalInputModal-searchInput": "_ExternalInputModal-searchInput_91ls0_299",
	"ExternalInputModal-searchActions": "_ExternalInputModal-searchActions_91ls0_313",
	"ExternalInputModal-searchActionIcon": "_ExternalInputModal-searchActionIcon_91ls0_326",
	"ExternalInputModal-footerContainer": "_ExternalInputModal-footerContainer_91ls0_330",
	"ExternalInputModal-footer": "_ExternalInputModal-footer_91ls0_330",
	"ExternalInputModal-field": "_ExternalInputModal-field_91ls0_343"
};
init_react_import();
init_react_import();
var getClassName6 = get_class_name_factory_default("Modal", {
	"Modal": "_Modal_ikbaj_1",
	"Modal--isOpen": "_Modal--isOpen_ikbaj_15",
	"Modal-inner": "_Modal-inner_ikbaj_19"
});
var Modal = ({ children, onClose, isOpen }) => {
	const [rootEl, setRootEl] = (0, import_react.useState)(null);
	(0, import_react.useEffect)(() => {
		setRootEl(document.getElementById("puck-portal-root"));
	}, []);
	if (!rootEl) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {});
	return (0, import_react_dom.createPortal)(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName6({ isOpen }),
		onClick: onClose,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName6("inner"),
			onClick: (e) => e.stopPropagation(),
			children
		})
	}), rootEl);
};
init_react_import();
init_react_import();
var getClassName7 = get_class_name_factory_default("Heading", {
	"Heading": "_Heading_qxrry_1",
	"Heading--xxxxl": "_Heading--xxxxl_qxrry_12",
	"Heading--xxxl": "_Heading--xxxl_qxrry_18",
	"Heading--xxl": "_Heading--xxl_qxrry_22",
	"Heading--xl": "_Heading--xl_qxrry_26",
	"Heading--l": "_Heading--l_qxrry_30",
	"Heading--m": "_Heading--m_qxrry_34",
	"Heading--s": "_Heading--s_qxrry_38",
	"Heading--xs": "_Heading--xs_qxrry_42"
});
var Heading = ({ children, rank, size = "m" }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(rank ? `h${rank}` : "span", {
		className: getClassName7({ [size]: true }),
		children
	});
};
init_react_import();
var getClassName8 = get_class_name_factory_default("ExternalInput", styles_module_default4);
var getClassNameModal = get_class_name_factory_default("ExternalInputModal", styles_module_default4);
var dataCache = {};
var ExternalInput = ({ field, onChange, value = null, name, id, readOnly }) => {
	var _a;
	const { mapProp = (val) => val, mapRow = (val) => val, filterFields } = field || {};
	const { enabled: shouldCacheData } = (_a = field.cache) != null ? _a : { enabled: true };
	const [data, setData] = (0, import_react.useState)([]);
	const [isOpen, setOpen] = (0, import_react.useState)(false);
	const [isLoading, setIsLoading] = (0, import_react.useState)(true);
	const hasFilterFields = !!filterFields;
	const [filters, setFilters] = (0, import_react.useState)(field.initialFilters || {});
	const [filtersToggled, setFiltersToggled] = (0, import_react.useState)(hasFilterFields);
	const mappedData = (0, import_react.useMemo)(() => {
		return data.map(mapRow);
	}, [data]);
	const keys = (0, import_react.useMemo)(() => {
		const validKeys = /* @__PURE__ */ new Set();
		for (const item of mappedData) for (const key of Object.keys(item)) if (typeof item[key] === "string" || typeof item[key] === "number" || (0, import_react.isValidElement)(item[key])) validKeys.add(key);
		return Array.from(validKeys);
	}, [mappedData]);
	const [searchQuery, setSearchQuery] = (0, import_react.useState)(field.initialQuery || "");
	const search = (0, import_react.useCallback)((query, filters2) => __async(null, null, function* () {
		setIsLoading(true);
		const cacheKey = `${id}-${query}-${JSON.stringify(filters2)}`;
		let listData;
		if (shouldCacheData && dataCache[cacheKey]) listData = dataCache[cacheKey];
		else listData = yield field.fetchList({
			query,
			filters: filters2
		});
		if (listData) {
			setData(listData);
			setIsLoading(false);
			if (shouldCacheData) dataCache[cacheKey] = listData;
		}
	}), [id, field]);
	const Footer = (0, import_react.useCallback)((props) => field.renderFooter ? field.renderFooter(props) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", {
		className: getClassNameModal("footer"),
		children: [
			props.items.length,
			" result",
			props.items.length === 1 ? "" : "s"
		]
	}), [field.renderFooter]);
	(0, import_react.useEffect)(() => {
		search(searchQuery, filters);
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName8({
			dataSelected: !!value,
			modalVisible: isOpen,
			readOnly
		}),
		id,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: getClassName8("actions"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				onClick: () => setOpen(true),
				className: getClassName8("button"),
				disabled: readOnly,
				children: value ? field.getItemSummary ? field.getItemSummary(value) : "External item" : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Link, { size: "16" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: field.placeholder })] })
			}), value && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				className: getClassName8("detachButton"),
				onClick: () => {
					onChange(null);
				},
				disabled: readOnly,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LockOpen, { size: 16 })
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Modal, {
			onClose: () => setOpen(false),
			isOpen,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("form", {
				className: getClassNameModal({
					isLoading,
					loaded: !isLoading,
					hasData: mappedData.length > 0,
					filtersToggled
				}),
				onSubmit: (e) => {
					e.preventDefault();
					e.stopPropagation();
					search(searchQuery, filters);
				},
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassNameModal("masthead"),
						children: field.showSearch ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: getClassNameModal("searchForm"),
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
								className: getClassNameModal("search"),
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
										className: getClassNameModal("searchIconText"),
										children: "Search"
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
										className: getClassNameModal("searchIcon"),
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Search, { size: "18" })
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
										className: getClassNameModal("searchInput"),
										name: "q",
										type: "search",
										placeholder: field.placeholder,
										onChange: (e) => {
											setSearchQuery(e.currentTarget.value);
										},
										autoComplete: "off",
										value: searchQuery
									})
								]
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: getClassNameModal("searchActions"),
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
									type: "submit",
									loading: isLoading,
									fullWidth: true,
									children: "Search"
								}), hasFilterFields && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: getClassNameModal("searchActionIcon"),
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
										type: "button",
										title: "Toggle filters",
										onClick: (e) => {
											e.preventDefault();
											e.stopPropagation();
											setFiltersToggled(!filtersToggled);
										},
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlidersHorizontal, { size: 20 })
									})
								})]
							})]
						}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
							rank: "2",
							size: "xs",
							children: field.placeholder || "Select data"
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: getClassNameModal("grid"),
						children: [hasFilterFields && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: getClassNameModal("filters"),
							children: hasFilterFields && Object.keys(filterFields).map((fieldName) => {
								const filterField = filterFields[fieldName];
								return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
									className: getClassNameModal("field"),
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldLabel, {
										label: filterField.label || fieldName,
										children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AutoField, {
											field: filterField,
											id: `external_field_${fieldName}_filter`,
											value: filters[fieldName],
											onChange: (value2) => {
												setFilters((filters2) => {
													const newFilters = __spreadProps$6(__spreadValues$7({}, filters2), { [fieldName]: value2 });
													search(searchQuery, newFilters);
													return newFilters;
												});
											}
										})
									})
								}, fieldName);
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: getClassNameModal("tableWrapper"),
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("table", {
								className: getClassNameModal("table"),
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("thead", {
									className: getClassNameModal("thead"),
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tr", {
										className: getClassNameModal("tr"),
										children: keys.map((key) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("th", {
											className: getClassNameModal("th"),
											style: { textAlign: "left" },
											children: key
										}, key))
									})
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tbody", {
									className: getClassNameModal("tbody"),
									children: mappedData.map((item, i) => {
										return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tr", {
											style: { whiteSpace: "nowrap" },
											className: getClassNameModal("tr"),
											onClick: () => {
												onChange(mapProp(data[i]));
												setOpen(false);
											},
											children: keys.map((key) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("td", {
												className: getClassNameModal("td"),
												children: item[key]
											}, key))
										}, i);
									})
								})]
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: getClassNameModal("loadingBanner"),
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Loader, { size: 24 })
							})]
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassNameModal("footerContainer"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Footer, { items: mappedData })
					})
				]
			})
		})]
	});
};
var ExternalField = ({ field, onChange, id, name = id, label, labelIcon, Label, readOnly }) => {
	var _a, _b, _c;
	const value = useDeepField(name);
	const validField = field;
	const deprecatedField = field;
	(0, import_react.useEffect)(() => {
		if (deprecatedField.adaptor) console.error("Warning: The `adaptor` API is deprecated. Please use updated APIs on the `external` field instead. This will be a breaking change in a future release.");
	}, []);
	if (field.type !== "external") return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
		label: label || name,
		icon: labelIcon || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Link, { size: 16 }),
		el: "div",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ExternalInput, {
			name,
			field: __spreadProps$6(__spreadValues$7({}, validField), {
				placeholder: ((_a = deprecatedField.adaptor) == null ? void 0 : _a.name) ? `Select from ${deprecatedField.adaptor.name}` : validField.placeholder || "Select data",
				mapProp: ((_b = deprecatedField.adaptor) == null ? void 0 : _b.mapProp) || validField.mapProp,
				mapRow: validField.mapRow,
				fetchList: ((_c = deprecatedField.adaptor) == null ? void 0 : _c.fetchList) ? () => __async(null, null, function* () {
					return yield deprecatedField.adaptor.fetchList(deprecatedField.adaptorParams);
				}) : validField.fetchList
			}),
			onChange,
			value,
			id,
			readOnly
		})
	});
};
init_react_import();
var getClassName9 = get_class_name_factory_default("Input", styles_module_default);
var RadioField = ({ field, onChange, readOnly, id, name = id, label, labelIcon, Label }) => {
	const value = useDeepField(name);
	if (field.type !== "radio" || !field.options) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
		icon: labelIcon || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CircleCheckBig, { size: 16 }),
		label: label || name,
		readOnly,
		el: "div",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName9("radioGroupItems"),
			id,
			children: field.options.map((option) => {
				var _a;
				return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", {
					className: getClassName9("radio"),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
						type: "radio",
						className: getClassName9("radioInput"),
						value: JSON.stringify({ value: option.value }),
						name,
						onChange: (e) => {
							onChange(JSON.parse(e.target.value).value);
						},
						disabled: readOnly,
						checked: value === option.value
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassName9("radioInner"),
						children: option.label || ((_a = option.value) == null ? void 0 : _a.toString())
					})]
				}, option.label + option.value);
			})
		})
	});
};
init_react_import();
var getClassName10 = get_class_name_factory_default("Input", styles_module_default);
var SelectField = ({ field, onChange, label, labelIcon, Label, id, name = id, readOnly }) => {
	const value = useDeepField(name);
	if (field.type !== "select" || !field.options) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
		label: label || name,
		icon: labelIcon || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown$1, { size: 16 }),
		readOnly,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("select", {
			id,
			title: label || name,
			className: getClassName10("input"),
			disabled: readOnly,
			onChange: (e) => {
				onChange(JSON.parse(e.target.value).value);
			},
			value: JSON.stringify({ value }),
			children: field.options.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", {
				label: option.label,
				value: JSON.stringify({ value: option.value })
			}, option.label + JSON.stringify(option.value)))
		})
	});
};
init_react_import();
var getClassName11 = get_class_name_factory_default("Input", styles_module_default);
var TextareaField = ({ field, onChange, readOnly, id, name = id, label, labelIcon, Label }) => {
	const [localValue, onChangeLocal] = useLocalValue(name, onChange);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
		label: label || name,
		icon: labelIcon || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Type, { size: 16 }),
		readOnly,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("textarea", {
			id,
			className: getClassName11("input"),
			autoComplete: "off",
			name,
			value: typeof localValue === "undefined" ? "" : localValue,
			onChange: (e) => onChangeLocal(e.currentTarget.value),
			readOnly,
			tabIndex: readOnly ? -1 : void 0,
			rows: 5,
			placeholder: field.type === "textarea" ? field.placeholder : void 0
		})
	});
};
init_react_import();
init_react_import();
var EditorFallback = (0, import_react.memo)((props) => {
	var _a;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EditorInner, __spreadProps$6(__spreadValues$7({}, props), {
		editor: null,
		menu: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadedRichTextMenuInner, {
			field: props.field,
			editor: null,
			editorState: null,
			readOnly: (_a = props.readOnly) != null ? _a : false
		}),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "rich-text",
			dangerouslySetInnerHTML: { __html: props.content },
			contentEditable: true
		})
	}));
});
EditorFallback.displayName = "EditorFallback";
var Editor = (0, import_react.lazy)(() => import("./Editor-MCVDFQH6-C5mVcivN.js").then((m) => ({ default: m.Editor })));
var RichtextField = ({ onChange, readOnly = false, id, name = id, label, labelIcon, Label, field }) => {
	const editorProps = {
		onChange,
		content: useDeepField(name),
		readOnly,
		field,
		id,
		name
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
		label: label || name,
		icon: labelIcon || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Type, { size: 16 }),
		readOnly,
		el: "div",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
			fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EditorFallback, __spreadValues$7({}, editorProps)),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editor, __spreadValues$7({}, editorProps))
		})
	}) });
};
init_react_import();
init_react_import();
var getClassName12 = get_class_name_factory_default("ObjectField", {
	"ObjectField": "_ObjectField_1ua3y_5",
	"ObjectField-fieldset": "_ObjectField-fieldset_1ua3y_13"
});
var ObjectField = ({ field, onChange, id, name = id, label, labelIcon, Label, readOnly }) => {
	const { localName = name } = useNestedFieldContext();
	const fieldStore = useFieldStoreApi();
	const canEdit = useAppStore((s) => s.permissions.getPermissions({ item: s.selectedItem }).edit);
	const getValue = () => {
		var _a;
		return (_a = getDeep(fieldStore.getState(), name)) != null ? _a : {};
	};
	if (field.type !== "object" || !field.objectFields) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Label, {
		label: label || name,
		icon: labelIcon || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EllipsisVertical, { size: 16 }),
		el: "div",
		readOnly,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName12(),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("fieldset", {
				className: getClassName12("fieldset"),
				children: Object.keys(field.objectFields).map((subName) => {
					const subField = field.objectFields[subName];
					const subPath = `${localName}.${subName}`;
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SubField, {
						id: `${id}_${subName}`,
						name,
						subName,
						localName,
						field: subField,
						forceReadOnly: !canEdit,
						onChange: (subValue, ui, subName2) => {
							const value = getValue();
							if (value[subName2] === subValue) return;
							onChange(__spreadProps$6(__spreadValues$7({}, value), { [subName2]: subValue }), ui);
						}
					}, subPath);
				})
			})
		})
	});
};
init_react_import();
var useSafeId = () => {
	if (typeof import_react.useId !== "undefined") return import_react.useId();
	const [id] = (0, import_react.useState)(generateId());
	return id;
};
var getClassName13 = get_class_name_factory_default("Input", styles_module_default);
var getClassNameWrapper = get_class_name_factory_default("InputWrapper", styles_module_default);
var defaultFields = {
	array: ArrayField,
	external: ExternalField,
	object: ObjectField,
	select: SelectField,
	textarea: TextareaField,
	radio: RadioField,
	text: DefaultField,
	number: DefaultField,
	richtext: RichtextField
};
function AutoFieldInternal(props) {
	var _a;
	const dispatch = useAppStore((s) => s.dispatch);
	const overrides = useAppStore((s) => s.overrides);
	const readOnly = useAppStore(useShallow((s) => {
		var _a2;
		return (_a2 = s.selectedItem) == null ? void 0 : _a2.readOnly;
	}));
	const nestedFieldContext = (0, import_react.useContext)(NestedFieldContext);
	const { id, Label = FieldLabelInternal } = props;
	const field = props.field;
	const label = field.label;
	const labelIcon = field.labelIcon;
	const defaultId = useSafeId();
	const resolvedId = id || defaultId;
	const render = (0, import_react.useMemo)(() => {
		var _a2, _b, _c, _d, _e, _f, _g, _h, _i, _j;
		return __spreadProps$6(__spreadValues$7({}, overrides.fieldTypes), {
			custom: (_a2 = overrides.fieldTypes) == null ? void 0 : _a2.custom,
			array: ((_b = overrides.fieldTypes) == null ? void 0 : _b.array) || defaultFields.array,
			external: ((_c = overrides.fieldTypes) == null ? void 0 : _c.external) || defaultFields.external,
			object: ((_d = overrides.fieldTypes) == null ? void 0 : _d.object) || defaultFields.object,
			select: ((_e = overrides.fieldTypes) == null ? void 0 : _e.select) || defaultFields.select,
			textarea: ((_f = overrides.fieldTypes) == null ? void 0 : _f.textarea) || defaultFields.textarea,
			radio: ((_g = overrides.fieldTypes) == null ? void 0 : _g.radio) || defaultFields.radio,
			text: ((_h = overrides.fieldTypes) == null ? void 0 : _h.text) || defaultFields.text,
			number: ((_i = overrides.fieldTypes) == null ? void 0 : _i.number) || defaultFields.number,
			richtext: ((_j = overrides.fieldTypes) == null ? void 0 : _j.richtext) || defaultFields.richtext
		});
	}, [overrides]);
	const fieldValue = useFieldStore((s) => {
		var _a2, _b;
		if (field.type === "custom" || ((_a2 = overrides.fieldTypes) == null ? void 0 : _a2[field.type])) return getDeep(s, (_b = props.name) != null ? _b : resolvedId);
	});
	const mergedProps = (0, import_react.useMemo)(() => __spreadProps$6(__spreadValues$7({}, props), {
		field,
		label,
		labelIcon,
		Label,
		id: resolvedId,
		value: fieldValue
	}), [
		props,
		field,
		label,
		labelIcon,
		Label,
		resolvedId,
		fieldValue
	]);
	const onFocus = (0, import_react.useCallback)((e) => {
		if (mergedProps.name && (e.target.nodeName === "INPUT" || e.target.nodeName === "TEXTAREA")) {
			e.stopPropagation();
			dispatch({
				type: "setUi",
				ui: { field: { focus: mergedProps.name } }
			});
		}
	}, [mergedProps.name]);
	const onBlur = (0, import_react.useCallback)((e) => {
		if ("name" in e.target) dispatch({
			type: "setUi",
			ui: { field: { focus: null } }
		});
	}, []);
	let Children = (0, import_react.useMemo)(() => {
		if (field.type !== "custom" && field.type !== "slot") return defaultFields[field.type];
		return (_props) => null;
	}, [field.type]);
	const fieldKey = field.type === "custom" ? field.key : void 0;
	let FieldComponent = (0, import_react.useMemo)(() => {
		if (field.type === "custom" && !render[field.type]) {
			if (!field.render) return null;
			return field.render;
		} else if (field.type !== "slot") return render[field.type];
	}, [
		field.type,
		fieldKey,
		render
	]);
	const { visible = true } = props.field;
	if (!visible) return null;
	if (field.type === "slot") return null;
	if (!FieldComponent) throw new Error(`Field type for ${field.type} did not exist.`);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NestedFieldContext.Provider, {
		value: {
			readOnlyFields: nestedFieldContext.readOnlyFields || readOnly || {},
			localName: (_a = nestedFieldContext.localName) != null ? _a : mergedProps.name
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassNameWrapper(),
			onFocus,
			onBlur,
			onClick: (e) => {
				e.stopPropagation();
			},
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldComponent, __spreadProps$6(__spreadValues$7({}, mergedProps), { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Children, __spreadValues$7({}, mergedProps)) }))
		})
	});
}
function AutoFieldPrivate(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AutoFieldInternal, __spreadValues$7({}, props));
}
function AutoFieldPublicInternal(_a) {
	var _b = _a, { value } = _b, props = __objRest$4(_b, ["value"]);
	const DefaultLabel = (0, import_react.useMemo)(() => {
		const DefaultLabel2 = (labelProps) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", __spreadProps$6(__spreadValues$7({}, labelProps), { className: getClassName13({ readOnly: props.readOnly }) }));
		return DefaultLabel2;
	}, [props.readOnly]);
	const fieldStore = useFieldStoreApi();
	const onChange = (0, import_react.useCallback)((value2) => {
		if (!props.id) return;
		fieldStore.setState({ [props.id]: value2 });
		props.onChange(value2);
	}, [
		fieldStore,
		props.onChange,
		props.id
	]);
	(0, import_react.useEffect)(() => {
		if (!props.id) return;
		fieldStore.setState({ [props.id]: value });
	}, [
		props.id,
		value,
		fieldStore
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AutoFieldInternal, __spreadProps$6(__spreadValues$7({}, props), {
		onChange,
		Label: DefaultLabel
	}));
}
function AutoField(props) {
	const id = useSafeId();
	if (props.field.type === "slot") return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(fieldContextStore.Provider, {
		value: { [id]: props.value },
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AutoFieldPublicInternal, __spreadProps$6(__spreadValues$7({}, props), { id }))
	});
}
init_react_import();
init_react_import();
init_react_import();
init_react_import();
var styles_module_default8 = {
	"DraggableComponent": "_DraggableComponent_1vaqy_1",
	"DraggableComponent-overlayWrapper": "_DraggableComponent-overlayWrapper_1vaqy_12",
	"DraggableComponent-overlay": "_DraggableComponent-overlay_1vaqy_12",
	"DraggableComponent-loadingOverlay": "_DraggableComponent-loadingOverlay_1vaqy_34",
	"DraggableComponent--hover": "_DraggableComponent--hover_1vaqy_50",
	"DraggableComponent--isSelected": "_DraggableComponent--isSelected_1vaqy_57",
	"DraggableComponent-actionsOverlay": "_DraggableComponent-actionsOverlay_1vaqy_71",
	"DraggableComponent-actions": "_DraggableComponent-actions_1vaqy_71"
};
init_react_import();
function getDeepScrollPosition(element) {
	let totalScroll = {
		x: 0,
		y: 0
	};
	let current = element;
	while (current && current !== document.documentElement) {
		const parent = current.parentElement;
		if (parent) {
			totalScroll.x += parent.scrollLeft;
			totalScroll.y += parent.scrollTop;
		}
		current = parent;
	}
	return totalScroll;
}
init_react_import();
var dropZoneContext = (0, import_react.createContext)(null);
var ZoneStoreContext = (0, import_react.createContext)(createStore(() => ({
	zoneDepthIndex: {},
	nextZoneDepthIndex: {},
	areaDepthIndex: {},
	nextAreaDepthIndex: {},
	draggedItem: null,
	previewIndex: {},
	enabledIndex: {},
	hoveringComponent: null,
	registerRootVirtualizer: () => {},
	unregisterRootVirtualizer: () => {},
	scrollToComponent: () => false
})));
var ZoneStoreProvider = ({ children, store }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ZoneStoreContext.Provider, {
		value: store,
		children
	});
};
var DropZoneProvider = ({ children, value }) => {
	const dispatch = useAppStore((s) => s.dispatch);
	const registerZone = (0, import_react.useCallback)((zoneCompound) => {
		dispatch({
			type: "registerZone",
			zone: zoneCompound
		});
	}, [dispatch]);
	const memoValue = (0, import_react.useMemo)(() => __spreadValues$7({ registerZone }, value), [value]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: memoValue && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(dropZoneContext.Provider, {
		value: memoValue,
		children
	}) });
};
init_react_import();
var useOnDragFinished = (cb, deps = []) => {
	const appStore = useAppStoreApi();
	return (0, import_react.useCallback)(() => {
		let dispose = () => {};
		const processDragging = (isDragging2) => {
			if (isDragging2) cb(false);
			else {
				setTimeout(() => {
					cb(true);
				}, 0);
				if (dispose) dispose();
			}
		};
		const isDragging = appStore.getState().state.ui.isDragging;
		processDragging(isDragging);
		if (isDragging) dispose = appStore.subscribe((s) => s.state.ui.isDragging, (isDragging2) => {
			processDragging(isDragging2);
		});
		return dispose;
	}, [appStore, ...deps]);
};
init_react_import();
function assignRef(ref, node) {
	if (typeof ref === "function") ref(node);
	else if (ref && typeof ref === "object" && "current" in ref) ref.current = node;
}
function assignRefs(refs, node) {
	refs.forEach((ref) => {
		assignRef(ref, node);
	});
}
var getClassName14 = get_class_name_factory_default("DraggableComponent", styles_module_default8);
var DEBUG2 = false;
var MEASURE_EVERY_MS = 100;
var space = 8;
var actionsOverlayTop = space * 6.5;
var actionsTop = -(actionsOverlayTop - 8);
var actionsSide = space;
var DefaultActionBar = ({ label, children, parentAction }) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ActionBar, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ActionBar.Group, { children: [parentAction, label && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionBar.Label, { label })] }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionBar.Group, { children })] });
var DefaultOverlay = ({ children }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children });
var DraggableComponent = ({ children, depth, componentType, id, index, zoneCompound, isLoading = false, isSelected = false, debug, label, autoDragAxis, userDragAxis, inDroppableZone = true, itemRef }) => {
	const zoom = useAppStore((s) => {
		var _a;
		return ((_a = s.selectedItem) == null ? void 0 : _a.props.id) === id ? s.zoomConfig.zoom : 1;
	});
	const _experimentalFullScreenCanvas = useAppStore((s) => s._experimentalFullScreenCanvas);
	const overrides = useAppStore((s) => s.overrides);
	const dispatch = useAppStore((s) => s.dispatch);
	const iframe = useAppStore((s) => s.iframe);
	const lastMeasureRef = (0, import_react.useRef)(0);
	const ctx = (0, import_react.useContext)(dropZoneContext);
	const [localZones, setLocalZones] = (0, import_react.useState)({});
	const registerLocalZone = (0, import_react.useCallback)((zoneCompound2, active) => {
		var _a;
		(_a = ctx == null ? void 0 : ctx.registerLocalZone) == null || _a.call(ctx, zoneCompound2, active);
		setLocalZones((obj) => __spreadProps$6(__spreadValues$7({}, obj), { [zoneCompound2]: active }));
	}, [setLocalZones]);
	const unregisterLocalZone = (0, import_react.useCallback)((zoneCompound2) => {
		var _a;
		(_a = ctx == null ? void 0 : ctx.unregisterLocalZone) == null || _a.call(ctx, zoneCompound2);
		setLocalZones((obj) => {
			const newLocalZones = __spreadValues$7({}, obj);
			delete newLocalZones[zoneCompound2];
			return newLocalZones;
		});
	}, [setLocalZones]);
	const containsActiveZone = Object.values(localZones).filter(Boolean).length > 0;
	const path = useAppStore(useShallow((s) => {
		var _a;
		return (_a = s.state.indexes.nodes[id]) == null ? void 0 : _a.path;
	}));
	const permissions = useAppStore(useShallow((s) => {
		const item = getItem({
			index,
			zone: zoneCompound
		}, s.state);
		return s.permissions.getPermissions({ item });
	}));
	const zoneStore = (0, import_react.useContext)(ZoneStoreContext);
	const [dragAxis, setDragAxis] = (0, import_react.useState)(userDragAxis || autoDragAxis);
	const dynamicCollisionDetector = (0, import_react.useMemo)(() => createDynamicCollisionDetector(dragAxis), [dragAxis]);
	const { ref: sortableRef, isDragging: thisIsDragging, sortable } = useSortable({
		id,
		index,
		group: zoneCompound,
		type: "component",
		data: {
			areaId: ctx == null ? void 0 : ctx.areaId,
			zone: zoneCompound,
			index,
			componentType,
			containsActiveZone,
			depth,
			path: path || [],
			inDroppableZone
		},
		collisionPriority: depth,
		collisionDetector: dynamicCollisionDetector,
		transition: {
			duration: 200,
			easing: "cubic-bezier(0.2, 0, 0, 1)"
		},
		feedback: "clone"
	});
	(0, import_react.useEffect)(() => {
		const isEnabled = zoneStore.getState().enabledIndex[zoneCompound];
		sortable.droppable.disabled = !isEnabled;
		sortable.draggable.disabled = !permissions.drag;
		const cleanup = zoneStore.subscribe((s) => {
			sortable.droppable.disabled = !s.enabledIndex[zoneCompound];
		});
		if (ref.current && !permissions.drag) {
			ref.current.setAttribute("data-puck-disabled", "");
			return () => {
				var _a;
				(_a = ref.current) == null || _a.removeAttribute("data-puck-disabled");
				cleanup();
			};
		}
		return cleanup;
	}, [permissions.drag, zoneCompound]);
	const [, setRerender] = (0, import_react.useState)(0);
	const ref = (0, import_react.useRef)(null);
	const refSetter = (0, import_react.useCallback)((el) => {
		sortableRef(el);
		if (ref.current !== el) {
			ref.current = el;
			setRerender((update) => update + 1);
			if (itemRef) assignRefs([itemRef], el);
		}
	}, [itemRef, sortableRef]);
	const [portalEl, setPortalEl] = (0, import_react.useState)();
	(0, import_react.useEffect)(() => {
		var _a, _b, _c;
		setPortalEl(iframe.enabled ? (_a = ref.current) == null ? void 0 : _a.ownerDocument.body : (_c = (_b = ref.current) == null ? void 0 : _b.closest("[data-puck-preview]")) != null ? _c : document.body);
	}, [iframe.enabled]);
	const getStyle = (0, import_react.useCallback)(() => {
		var _a, _b;
		if (!ref.current) return;
		const el = ref.current;
		const rect = el.getBoundingClientRect();
		const portalContainerEl = iframe.enabled ? null : el.closest("[data-puck-preview]");
		const targetIsFixed = (() => {
			let node = el;
			while (node && node !== document.documentElement) {
				if (getComputedStyle(node).position === "fixed") return true;
				node = node.parentElement;
			}
			return false;
		})();
		const portalContainerRect = portalContainerEl == null ? void 0 : portalContainerEl.getBoundingClientRect();
		const portalScroll = portalContainerEl ? getDeepScrollPosition(portalContainerEl) : {
			x: 0,
			y: 0
		};
		const deepScrollPosition = targetIsFixed ? {
			x: 0,
			y: 0
		} : getDeepScrollPosition(el);
		const scroll = targetIsFixed ? {
			x: 0,
			y: 0
		} : {
			x: deepScrollPosition.x - portalScroll.x - ((_a = portalContainerRect == null ? void 0 : portalContainerRect.left) != null ? _a : 0),
			y: deepScrollPosition.y - portalScroll.y - ((_b = portalContainerRect == null ? void 0 : portalContainerRect.top) != null ? _b : 0)
		};
		return {
			left: `${rect.left + scroll.x}px`,
			top: `${rect.top + scroll.y}px`,
			height: `${rect.height}px`,
			width: `${rect.width}px`,
			position: targetIsFixed ? "fixed" : void 0
		};
	}, [iframe.enabled]);
	const [style, setStyle] = (0, import_react.useState)();
	const lastRectRef = (0, import_react.useRef)(null);
	const syncRafRef = (0, import_react.useRef)(null);
	const sync = (0, import_react.useCallback)(() => {
		setStyle(getStyle());
		if (itemRef) assignRefs([itemRef], ref.current);
	}, [getStyle, itemRef]);
	const scheduleSync = (0, import_react.useCallback)(() => {
		if (syncRafRef.current != null) return;
		syncRafRef.current = requestAnimationFrame(() => {
			syncRafRef.current = null;
			sync();
		});
	}, [sync]);
	(0, import_react.useEffect)(() => {
		return () => {
			if (syncRafRef.current != null) {
				cancelAnimationFrame(syncRafRef.current);
				syncRafRef.current = null;
			}
		};
	}, []);
	(0, import_react.useEffect)(() => {
		if (ref.current) {
			const observer = new ResizeObserver(() => {
				scheduleSync();
			});
			observer.observe(ref.current);
			return () => {
				observer.disconnect();
			};
		}
	}, [scheduleSync, itemRef]);
	const registerNode = useAppStore((s) => s.nodes.registerNode);
	const unregisterNode = useAppStore((s) => s.nodes.unregisterNode);
	const hideOverlay = (0, import_react.useCallback)(() => {
		setIsVisible(false);
	}, []);
	const showOverlay = (0, import_react.useCallback)(() => {
		setIsVisible(true);
	}, []);
	const nodeHandleRef = (0, import_react.useRef)({
		sync: () => null,
		hideOverlay: () => null,
		showOverlay: () => null
	});
	(0, import_react.useLayoutEffect)(() => {
		nodeHandleRef.current.sync = sync;
		nodeHandleRef.current.hideOverlay = hideOverlay;
		nodeHandleRef.current.showOverlay = showOverlay;
	}, [
		hideOverlay,
		showOverlay,
		sync
	]);
	(0, import_react.useEffect)(() => {
		registerNode(id, nodeHandleRef.current);
		return () => {
			unregisterNode(id);
		};
	}, [
		id,
		registerNode,
		unregisterNode
	]);
	const CustomActionBar = (0, import_react.useMemo)(() => overrides.actionBar || DefaultActionBar, [overrides.actionBar]);
	const CustomOverlay = (0, import_react.useMemo)(() => overrides.componentOverlay || DefaultOverlay, [overrides.componentOverlay]);
	const onClick = (0, import_react.useCallback)((e) => {
		if (!!zoneStore.getState().draggedItem) return;
		if (!e.target.closest("[data-puck-overlay-portal]")) e.stopPropagation();
		if (_experimentalFullScreenCanvas) dispatch({
			type: "setUi",
			ui: { itemSelector: isSelected ? null : {
				index,
				zone: zoneCompound
			} }
		});
		else dispatch({
			type: "setUi",
			ui: { itemSelector: {
				index,
				zone: zoneCompound
			} }
		});
	}, [
		index,
		zoneCompound,
		id,
		isSelected,
		_experimentalFullScreenCanvas
	]);
	const appStore = useAppStoreApi();
	const onSelectParent = (0, import_react.useCallback)(() => {
		const { nodes, zones } = appStore.getState().state.indexes;
		const node = nodes[id];
		const parentNode = (node == null ? void 0 : node.parentId) ? nodes[node == null ? void 0 : node.parentId] : null;
		if (!parentNode || !node.parentId) return;
		const parentZoneCompound = `${parentNode.parentId}:${parentNode.zone}`;
		dispatch({
			type: "setUi",
			ui: { itemSelector: {
				zone: parentZoneCompound,
				index: zones[parentZoneCompound].contentIds.indexOf(node.parentId)
			} }
		});
	}, [ctx, path]);
	const onDuplicate = (0, import_react.useCallback)(() => {
		dispatch({
			type: "duplicate",
			sourceIndex: index,
			sourceZone: zoneCompound
		});
	}, [index, zoneCompound]);
	const onDelete = (0, import_react.useCallback)(() => {
		dispatch({
			type: "remove",
			index,
			zone: zoneCompound
		});
	}, [index, zoneCompound]);
	const [hover, setHover] = (0, import_react.useState)(false);
	const indicativeHover = useContextStore(ZoneStoreContext, (s) => s.hoveringComponent === id);
	(0, import_react.useEffect)(() => {
		if (!ref.current) return;
		const el = ref.current;
		const _onMouseOver = (e) => {
			if (!!zoneStore.getState().draggedItem) if (thisIsDragging) setHover(true);
			else setHover(false);
			else setHover(true);
			e.stopPropagation();
		};
		const _onMouseOut = (e) => {
			e.stopPropagation();
			setHover(false);
		};
		el.setAttribute("data-puck-component", id);
		el.setAttribute("data-puck-dnd", id);
		el.style.position = "relative";
		el.addEventListener("click", onClick);
		el.addEventListener("mouseover", _onMouseOver);
		el.addEventListener("mouseout", _onMouseOut);
		return () => {
			el.removeAttribute("data-puck-component");
			el.removeAttribute("data-puck-dnd");
			el.removeEventListener("click", onClick);
			el.removeEventListener("mouseover", _onMouseOver);
			el.removeEventListener("mouseout", _onMouseOut);
		};
	}, [
		ref.current,
		onClick,
		containsActiveZone,
		zoneCompound,
		id,
		thisIsDragging,
		inDroppableZone
	]);
	const [isVisible, setIsVisible] = (0, import_react.useState)(false);
	const [dragFinished, setDragFinished] = (0, import_react.useState)(true);
	const [_, startTransition] = (0, import_react.useTransition)();
	(0, import_react.useEffect)(() => {
		startTransition(() => {
			if (hover || indicativeHover || isSelected) {
				scheduleSync();
				setIsVisible(true);
				setThisWasDragging(false);
			} else setIsVisible(false);
		});
	}, [
		hover,
		indicativeHover,
		isSelected,
		iframe
	]);
	const [thisWasDragging, setThisWasDragging] = (0, import_react.useState)(false);
	const onDragFinished = useOnDragFinished((finished) => {
		if (finished) startTransition(() => {
			sync();
			setDragFinished(true);
		});
		else setDragFinished(false);
	});
	(0, import_react.useEffect)(() => {
		if (thisIsDragging) setThisWasDragging(true);
	}, [thisIsDragging]);
	(0, import_react.useEffect)(() => {
		if (thisWasDragging) return onDragFinished();
	}, [thisWasDragging, onDragFinished]);
	(0, import_react.useEffect)(() => {
		if (!dragFinished || !(isSelected || thisIsDragging)) return;
		const el = ref.current;
		if (!el) return;
		const doc = el.ownerDocument;
		const view = doc.defaultView;
		if (!view) return;
		lastMeasureRef.current = 0;
		scheduleSync();
		const onScroll = () => scheduleSync();
		const onResize = () => scheduleSync();
		doc.addEventListener("scroll", onScroll, true);
		view.addEventListener("resize", onResize);
		let frame = 0;
		const tick = (t) => {
			if (t - lastMeasureRef.current >= MEASURE_EVERY_MS) {
				lastMeasureRef.current = t;
				const node = ref.current;
				if (node) {
					const rect = node.getBoundingClientRect();
					const prev = lastRectRef.current;
					if (!prev || Math.abs(rect.x - prev.x) > .5 || Math.abs(rect.y - prev.y) > .5 || Math.abs(rect.width - prev.width) > .5 || Math.abs(rect.height - prev.height) > .5) {
						lastRectRef.current = rect;
						scheduleSync();
					}
				}
			}
			frame = requestAnimationFrame(tick);
		};
		frame = requestAnimationFrame(tick);
		return () => {
			doc.removeEventListener("scroll", onScroll, true);
			view.removeEventListener("resize", onResize);
			cancelAnimationFrame(frame);
		};
	}, [
		dragFinished,
		isSelected,
		thisIsDragging,
		scheduleSync
	]);
	const syncActionsPosition = (0, import_react.useCallback)((el) => {
		if (el) {
			if (el.ownerDocument.defaultView) {
				const rect = el.getBoundingClientRect();
				const exceedsBoundsLeft = rect.x < 0;
				const exceedsBoundsTop = rect.y < 0;
				if (exceedsBoundsLeft) {
					el.style.transformOrigin = "left top";
					el.style.left = "0px";
				}
				if (exceedsBoundsTop) {
					el.style.top = "12px";
					if (!exceedsBoundsLeft) el.style.transformOrigin = "right top";
				}
			}
		}
	}, [zoom]);
	const actionBarRef = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		syncActionsPosition(actionBarRef.current);
	}, [actionBarRef.current, syncActionsPosition]);
	(0, import_react.useEffect)(() => {
		if (userDragAxis) {
			setDragAxis(userDragAxis);
			return;
		}
		if (ref.current) {
			const computedStyle = window.getComputedStyle(ref.current);
			if (computedStyle.display === "inline" || computedStyle.display === "inline-block") {
				setDragAxis("x");
				return;
			}
		}
		setDragAxis(autoDragAxis);
	}, [
		ref,
		userDragAxis,
		autoDragAxis
	]);
	const parentAction = (0, import_react.useMemo)(() => (ctx == null ? void 0 : ctx.areaId) && (ctx == null ? void 0 : ctx.areaId) !== "root" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionBar.Action, {
		onClick: onSelectParent,
		label: "Select parent",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CornerLeftUp, { size: 16 })
	}), [ctx == null ? void 0 : ctx.areaId]);
	const nextContextValue = (0, import_react.useMemo)(() => __spreadProps$6(__spreadValues$7({}, ctx), {
		areaId: id,
		zoneCompound,
		index,
		depth: depth + 1,
		registerLocalZone,
		unregisterLocalZone
	}), [
		ctx,
		id,
		zoneCompound,
		index,
		depth,
		registerLocalZone,
		unregisterLocalZone
	]);
	const richText = useAppStore((s) => {
		var _a;
		return ((_a = s.currentRichText) == null ? void 0 : _a.inlineComponentId) === id ? s.currentRichText : null;
	});
	const hasNormalActions = permissions.duplicate || permissions.delete;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(DropZoneProvider, {
		value: nextContextValue,
		children: [dragFinished && isVisible && (0, import_react_dom.createPortal)(/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: getClassName14({
				isSelected,
				isDragging: thisIsDragging,
				hover: hover || indicativeHover
			}),
			style: __spreadValues$7({}, style),
			"data-puck-overlay": true,
			children: [
				debug,
				isLoading && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: getClassName14("loadingOverlay"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Loader, {})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: getClassName14("actionsOverlay"),
					style: { top: actionsOverlayTop / zoom },
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassName14("actions"),
						style: {
							transform: `scale(${1 / zoom}`,
							top: actionsTop / zoom,
							right: 0,
							paddingLeft: actionsSide,
							paddingRight: actionsSide
						},
						ref: actionBarRef,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(CustomActionBar, {
							parentAction,
							label: DEBUG2 ? id : label,
							children: [
								richText && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(LoadedRichTextMenu, {
									editor: richText.editor,
									field: richText.field,
									inline: true,
									readOnly: false
								}), hasNormalActions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionBar.Separator, {})] }),
								permissions.duplicate && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionBar.Action, {
									onClick: onDuplicate,
									label: "Duplicate",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Copy, { size: 16 })
								}),
								permissions.delete && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionBar.Action, {
									onClick: onDelete,
									label: "Delete",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Trash, { size: 16 })
								})
							]
						})
					})
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: getClassName14("overlayWrapper"),
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomOverlay, {
						componentId: id,
						componentType,
						hover,
						isSelected,
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: getClassName14("overlay") })
					})
				})
			]
		}), portalEl || document.body), children(refSetter)]
	});
};
init_react_import();
var styles_module_default9 = {
	"DropZone": "_DropZone_1i2sv_1",
	"DropZone--hasChildren": "_DropZone--hasChildren_1i2sv_11",
	"DropZone--isAreaSelected": "_DropZone--isAreaSelected_1i2sv_24",
	"DropZone--hoveringOverArea": "_DropZone--hoveringOverArea_1i2sv_25",
	"DropZone--isRootZone": "_DropZone--isRootZone_1i2sv_25",
	"DropZone--isDestination": "_DropZone--isDestination_1i2sv_35",
	"DropZone-item": "_DropZone-item_1i2sv_47",
	"DropZone-hitbox": "_DropZone-hitbox_1i2sv_51",
	"DropZone--isEnabled": "_DropZone--isEnabled_1i2sv_59",
	"DropZone--isAnimating": "_DropZone--isAnimating_1i2sv_68"
};
init_react_import();
init_react_import();
var styles_module_default10 = {
	"Drawer": "_Drawer_pl7z0_1",
	"Drawer-draggable": "_Drawer-draggable_pl7z0_8",
	"Drawer-draggableBg": "_Drawer-draggableBg_pl7z0_12",
	"DrawerItem-draggable": "_DrawerItem-draggable_pl7z0_22",
	"DrawerItem--disabled": "_DrawerItem--disabled_pl7z0_35",
	"DrawerItem": "_DrawerItem_pl7z0_22",
	"Drawer--isDraggingFrom": "_Drawer--isDraggingFrom_pl7z0_45",
	"DrawerItem-name": "_DrawerItem-name_pl7z0_63"
};
init_react_import();
init_react_import();
init_react_import();
function timeout2(callback, duration) {
	const id = setTimeout(callback, duration);
	return () => clearTimeout(id);
}
function throttle(func, limit) {
	const time = () => performance.now();
	let cancel;
	let lastRan = 0;
	return function(...args) {
		const now = time();
		const context = this;
		if (now - lastRan >= limit) {
			func.apply(context, args);
			lastRan = now;
		} else {
			cancel?.();
			cancel = timeout2(() => {
				func.apply(context, args);
				lastRan = time();
			}, limit - (now - lastRan));
		}
	};
}
init_react_import();
var getFrame = () => {
	if (typeof window === "undefined") return;
	let frameEl = document.querySelector("#preview-frame");
	if ((frameEl == null ? void 0 : frameEl.tagName) === "IFRAME") return frameEl.contentDocument || document;
	return (frameEl == null ? void 0 : frameEl.ownerDocument) || document;
};
init_react_import();
var GlobalPosition = class {
	constructor(target, original) {
		this.scaleFactor = 1;
		this.frameEl = null;
		this.frameRect = null;
		var _a;
		this.target = target;
		this.original = original;
		this.frameEl = document.querySelector("iframe#preview-frame");
		if (this.frameEl) {
			this.frameRect = this.frameEl.getBoundingClientRect();
			this.scaleFactor = this.frameRect.width / (((_a = this.frameEl.contentWindow) == null ? void 0 : _a.innerWidth) || 1);
		}
	}
	get x() {
		return this.original.x;
	}
	get y() {
		return this.original.y;
	}
	get global() {
		if (document !== this.target.ownerDocument && this.frameRect) return {
			x: this.x * this.scaleFactor + this.frameRect.left,
			y: this.y * this.scaleFactor + this.frameRect.top
		};
		return this.original;
	}
	get frame() {
		if (document === this.target.ownerDocument && this.frameRect) return {
			x: (this.x - this.frameRect.left) / this.scaleFactor,
			y: (this.y - this.frameRect.top) / this.scaleFactor
		};
		return this.original;
	}
};
init_react_import();
var BaseEvent = typeof PointerEvent !== "undefined" ? PointerEvent : Event;
var BubbledPointerEvent = class extends BaseEvent {
	constructor(type, data) {
		super(type, data);
		this._originalTarget = null;
		this.originalTarget = data.originalTarget;
	}
	set originalTarget(target) {
		this._originalTarget = target;
	}
	get originalTarget() {
		return this._originalTarget;
	}
};
var depthSort = (candidates) => {
	return candidates.sort((a, b) => {
		const aData = a.data;
		const bData = b.data;
		if (aData.depth > bData.depth) return 1;
		if (bData.depth > aData.depth) return -1;
		return 0;
	});
};
var getZoneId = (candidate) => {
	let id = candidate == null ? void 0 : candidate.id;
	if (!candidate) return null;
	if (candidate.type === "component") {
		const data = candidate.data;
		if (data.containsActiveZone) id = null;
		else id = data.zone;
	} else if (candidate.type === "void") return "void";
	return id;
};
var BUFFER = 6;
var getPointerCollisions = (position, manager) => {
	const candidates = [];
	let elements = position.target.ownerDocument.elementsFromPoint(position.x, position.y);
	const previewFrame = elements.find((el) => el.getAttribute("data-puck-preview"));
	const drawer = elements.find((el) => el.getAttribute("data-puck-drawer"));
	if (drawer) elements = [drawer];
	if (previewFrame) {
		const frame = getFrame();
		if (frame) elements = frame.elementsFromPoint(position.frame.x, position.frame.y);
	}
	if (elements) for (let i = 0; i < elements.length; i++) {
		const element = elements[i];
		const dropzoneId = element.getAttribute("data-puck-dropzone");
		const id = element.getAttribute("data-puck-dnd");
		const isVoid = element.hasAttribute("data-puck-dnd-void");
		if (BUFFER && (dropzoneId || id) && !isVoid) {
			const box = element.getBoundingClientRect();
			const contractedBox = {
				left: box.left + BUFFER,
				right: box.right - BUFFER,
				top: box.top + BUFFER,
				bottom: box.bottom - BUFFER
			};
			if (position.frame.x < contractedBox.left || position.frame.x > contractedBox.right || position.frame.y > contractedBox.bottom || position.frame.y < contractedBox.top) continue;
		}
		if (dropzoneId) {
			const droppable = manager.registry.droppables.get(dropzoneId);
			if (droppable) candidates.push(droppable);
		}
		if (id) {
			const droppable = manager.registry.droppables.get(id);
			if (droppable) candidates.push(droppable);
		}
	}
	return candidates;
};
var findDeepestCandidate = (position, manager) => {
	var _a;
	const candidates = getPointerCollisions(position, manager);
	if (candidates.length > 0) {
		const sortedCandidates = depthSort(candidates);
		const draggable = manager.dragOperation.source;
		const draggedCandidateIndex = sortedCandidates.findIndex((candidate) => candidate.id === (draggable == null ? void 0 : draggable.id));
		const draggedCandidateId = draggable == null ? void 0 : draggable.id;
		let filteredCandidates = [...sortedCandidates];
		if (draggedCandidateId && draggedCandidateIndex > -1) filteredCandidates.splice(draggedCandidateIndex, 1);
		filteredCandidates = filteredCandidates.filter((candidate) => {
			const candidateData = candidate.data;
			if (draggedCandidateId && draggedCandidateIndex > -1) {
				if (candidateData.path.indexOf(draggedCandidateId) > -1) return false;
			}
			if (candidate.type === "dropzone") {
				const candidateData2 = candidate.data;
				if (!candidateData2.isDroppableTarget) return false;
				if (candidateData2.areaId === draggedCandidateId) return false;
			} else if (candidate.type === "component") {
				if (!candidate.data.inDroppableZone) return false;
			}
			return true;
		});
		filteredCandidates.reverse();
		const primaryCandidate = filteredCandidates[0];
		if (!primaryCandidate) return {
			zone: null,
			area: null
		};
		const primaryCandidateData = primaryCandidate.data;
		const primaryCandidateIsComponent = "containsActiveZone" in primaryCandidateData;
		return {
			zone: getZoneId(primaryCandidate),
			area: primaryCandidateIsComponent && primaryCandidateData.containsActiveZone ? filteredCandidates[0].id : (_a = filteredCandidates[0]) == null ? void 0 : _a.data.areaId
		};
	}
	return {
		zone: rootDroppableId,
		area: rootAreaId
	};
};
var createNestedDroppablePlugin = ({ onChange }, id) => class NestedDroppablePlugin extends Plugin {
	constructor(manager, options) {
		super(manager);
		if (typeof window === "undefined") return;
		this.registerEffect(() => {
			const handleMove = (event) => {
				const position = new GlobalPosition(event instanceof BubbledPointerEvent ? event.originalTarget || event.target : event.target, {
					x: event.clientX,
					y: event.clientY
				});
				if (document.elementsFromPoint(position.global.x, position.global.y).some((el) => el.id === id)) onChange(findDeepestCandidate(position, manager), manager);
			};
			const handleMoveThrottled = throttle(handleMove, 50);
			const handlePointerMove = (event) => {
				handleMoveThrottled(event);
			};
			document.body.addEventListener("pointermove", handlePointerMove, { capture: true });
			const cleanup = () => {
				document.body.removeEventListener("pointermove", handlePointerMove, { capture: true });
			};
			return cleanup;
		});
	}
};
init_react_import();
init_react_import();
var getSelectorForId = (state, id) => {
	const node = state.indexes.nodes[id];
	if (!node) return;
	const zoneCompound = `${node.parentId}:${node.zone}`;
	return {
		zone: zoneCompound,
		index: state.indexes.zones[zoneCompound].contentIds.indexOf(id)
	};
};
var insertComponent = (componentType, zone, index, appStore) => __async(null, null, function* () {
	const { getState } = appStore;
	const id = generateId(componentType);
	const insertActionData = {
		type: "insert",
		componentType,
		destinationIndex: index,
		destinationZone: zone,
		id
	};
	const stateBefore = getState().state;
	const insertedState = insertAction(stateBefore, insertActionData, getState());
	const dispatch = getState().dispatch;
	dispatch(__spreadProps$6(__spreadValues$7({}, insertActionData), { recordHistory: true }));
	const itemSelector = {
		index,
		zone
	};
	dispatch({
		type: "setUi",
		ui: { itemSelector }
	});
	const itemData = getItem(itemSelector, insertedState);
	if (!itemData) return;
	const resolveComponentData = getState().resolveComponentData;
	const resolved = yield resolveComponentData(itemData, "insert");
	if (!resolved.didChange) return;
	const latestItemSelector = getSelectorForId(getState().state, id);
	if (!latestItemSelector) return;
	dispatch({
		type: "replace",
		destinationZone: latestItemSelector.zone,
		destinationIndex: latestItemSelector.index,
		data: resolved.node
	});
});
init_react_import();
var moveComponent = (id, sourceSelector, destinationSelector, appStore) => __async(null, null, function* () {
	var _a, _b, _c, _d;
	const dispatch = appStore.getState().dispatch;
	dispatch({
		type: "move",
		sourceIndex: sourceSelector.index,
		sourceZone: (_a = sourceSelector.zone) != null ? _a : rootDroppableId,
		destinationIndex: destinationSelector.index,
		destinationZone: (_b = destinationSelector.zone) != null ? _b : rootDroppableId,
		recordHistory: false
	});
	const componentData = (_c = appStore.getState().state.indexes.nodes[id]) == null ? void 0 : _c.data;
	if (!componentData) return;
	const resolveComponentData = appStore.getState().resolveComponentData;
	const resolvedData = yield resolveComponentData(componentData, "move");
	const latestItemSelector = getSelectorForId(appStore.getState().state, componentData.props.id);
	if (!latestItemSelector) return;
	if (resolvedData.didChange) dispatch({
		type: "replace",
		data: resolvedData.node,
		destinationIndex: latestItemSelector.index,
		destinationZone: (_d = latestItemSelector.zone) != null ? _d : rootDroppableId
	});
});
init_react_import();
function getDeepDir(el) {
	function findDir(node) {
		if (!node) return "ltr";
		return node.getAttribute("dir") || findDir(node.parentElement);
	}
	return el ? findDir(el) : "ltr";
}
var DEBUG3 = false;
var dragListenerContext = (0, import_react.createContext)({ dragListeners: {} });
function useDragListener(type, fn, deps = []) {
	const { setDragListeners } = (0, import_react.useContext)(dragListenerContext);
	(0, import_react.useEffect)(() => {
		if (setDragListeners) setDragListeners((old) => __spreadProps$6(__spreadValues$7({}, old), { [type]: [...old[type] || [], fn] }));
	}, deps);
}
var AREA_CHANGE_DEBOUNCE_MS = 100;
var useTempDisableFallback = (timeout3) => {
	const lastFallbackDisable = (0, import_react.useRef)(null);
	return (0, import_react.useCallback)((manager) => {
		collisionStore.setState({ fallbackEnabled: false });
		const fallbackId = generateId();
		lastFallbackDisable.current = fallbackId;
		setTimeout(() => {
			if (lastFallbackDisable.current === fallbackId) {
				collisionStore.setState({ fallbackEnabled: true });
				manager.collisionObserver.forceUpdate(true);
			}
		}, timeout3);
	}, []);
};
var DragDropContextClient = ({ children, disableAutoScroll }) => {
	const dispatch = useAppStore((s) => s.dispatch);
	const instanceId = useAppStore((s) => s.instanceId);
	const appStore = useAppStoreApi();
	const debouncedParamsRef = (0, import_react.useRef)(null);
	const tempDisableFallback = useTempDisableFallback(100);
	const [zoneStore] = (0, import_react.useState)(() => {
		const rootVirtualizers = /* @__PURE__ */ new Map();
		return createStore(() => ({
			zoneDepthIndex: {},
			nextZoneDepthIndex: {},
			areaDepthIndex: {},
			nextAreaDepthIndex: {},
			draggedItem: null,
			previewIndex: {},
			enabledIndex: {},
			hoveringComponent: null,
			registerRootVirtualizer: (zoneCompound, handle) => {
				rootVirtualizers.set(zoneCompound, handle);
			},
			unregisterRootVirtualizer: (zoneCompound) => {
				rootVirtualizers.delete(zoneCompound);
			},
			scrollToComponent: (id) => {
				const virtualizers = Array.from(rootVirtualizers.values());
				if (virtualizers.length > 0) for (const handle of virtualizers) {
					const index = handle.resolveIndex(id);
					if (index < 0) continue;
					handle.virtualizer.scrollToIndex(index, {
						behavior: "auto",
						align: "auto"
					});
				}
				else {
					const frame = getFrame();
					(frame == null ? void 0 : frame.querySelector(`[data-puck-component="${id}"]`))?.scrollIntoView({ behavior: "smooth" });
				}
			}
		}));
	});
	const getChanged = (0, import_react.useCallback)((params) => {
		const { zoneDepthIndex = {}, areaDepthIndex = {} } = zoneStore.getState() || {};
		const stateHasZone = Object.keys(zoneDepthIndex).length > 0;
		const stateHasArea = Object.keys(areaDepthIndex).length > 0;
		let zoneChanged = false;
		let areaChanged = false;
		if (params.zone && !zoneDepthIndex[params.zone]) zoneChanged = true;
		else if (!params.zone && stateHasZone) zoneChanged = true;
		if (params.area && !areaDepthIndex[params.area]) areaChanged = true;
		else if (!params.area && stateHasArea) areaChanged = true;
		return {
			zoneChanged,
			areaChanged
		};
	}, [zoneStore]);
	const setDeepestAndCollide = (0, import_react.useCallback)((params, manager) => {
		const { zoneChanged, areaChanged } = getChanged(params);
		if (!zoneChanged && !areaChanged) return;
		zoneStore.setState({
			zoneDepthIndex: params.zone ? { [params.zone]: true } : {},
			areaDepthIndex: params.area ? { [params.area]: true } : {}
		});
		tempDisableFallback(manager);
		setTimeout(() => {
			manager.collisionObserver.forceUpdate(true);
		}, 50);
		debouncedParamsRef.current = null;
	}, [zoneStore]);
	const setDeepestDb = c(setDeepestAndCollide, AREA_CHANGE_DEBOUNCE_MS);
	const cancelDb = () => {
		setDeepestDb.cancel();
		debouncedParamsRef.current = null;
	};
	(0, import_react.useEffect)(() => {
		if (DEBUG3) zoneStore.subscribe((s) => {
			var _a, _b;
			return console.log(s.previewIndex, (_a = Object.entries(s.zoneDepthIndex || {})[0]) == null ? void 0 : _a[0], (_b = Object.entries(s.areaDepthIndex || {})[0]) == null ? void 0 : _b[0]);
		});
	}, []);
	const [plugins] = (0, import_react.useState)(() => [...disableAutoScroll ? defaultPreset.plugins.filter((plugin) => plugin !== AutoScroller) : defaultPreset.plugins, createNestedDroppablePlugin({ onChange: (params, manager) => {
		const state = zoneStore.getState();
		const { zoneChanged, areaChanged } = getChanged(params);
		const isDragging = manager.dragOperation.status.dragging;
		if (areaChanged || zoneChanged) {
			let nextZoneDepthIndex = {};
			let nextAreaDepthIndex = {};
			if (params.zone) nextZoneDepthIndex = { [params.zone]: true };
			if (params.area) nextAreaDepthIndex = { [params.area]: true };
			zoneStore.setState({
				nextZoneDepthIndex,
				nextAreaDepthIndex
			});
		}
		if (params.zone !== "void" && (state == null ? void 0 : state.zoneDepthIndex["void"])) {
			setDeepestAndCollide(params, manager);
			return;
		}
		if (areaChanged) {
			if (isDragging) {
				const debouncedParams = debouncedParamsRef.current;
				if (!(debouncedParams && debouncedParams.area === params.area && debouncedParams.zone === params.zone)) {
					cancelDb();
					setDeepestDb(params, manager);
					debouncedParamsRef.current = params;
				}
			} else {
				cancelDb();
				setDeepestAndCollide(params, manager);
			}
			return;
		}
		if (zoneChanged) setDeepestAndCollide(params, manager);
		cancelDb();
	} }, instanceId)]);
	const sensors = useSensors();
	const [dragListeners, setDragListeners] = (0, import_react.useState)({});
	const dragMode = (0, import_react.useRef)(null);
	const initialSelector = (0, import_react.useRef)(void 0);
	const nextContextValue = (0, import_react.useMemo)(() => ({
		mode: "edit",
		areaId: "root",
		depth: 0
	}), []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(dragListenerContext.Provider, {
		value: {
			dragListeners,
			setDragListeners
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DragDropProvider, {
			plugins,
			sensors,
			onDragEnd: (event, manager) => {
				var _a, _b;
				((_a = getFrame()) == null ? void 0 : _a.querySelector("[data-puck-entry]"))?.removeAttribute("data-puck-dragging");
				const { source, target } = event.operation;
				if (!source) {
					zoneStore.setState({ draggedItem: null });
					return;
				}
				const { zone, index } = source.data;
				const { previewIndex = {} } = zoneStore.getState() || {};
				const thisPreview = ((_b = previewIndex[zone]) == null ? void 0 : _b.props.id) === source.id ? previewIndex[zone] : null;
				const onAnimationEnd = () => {
					var _a2, _b2, _c, _d;
					zoneStore.setState({ draggedItem: null });
					if (event.canceled || (target == null ? void 0 : target.type) === "void") {
						zoneStore.setState({ previewIndex: {} });
						(_a2 = dragListeners.dragend) == null || _a2.forEach((fn) => {
							fn(event, manager);
						});
						dispatch({
							type: "setUi",
							ui: {
								itemSelector: null,
								isDragging: false
							}
						});
						return;
					}
					if (thisPreview) {
						zoneStore.setState({ previewIndex: {} });
						if (thisPreview.type === "insert") insertComponent(thisPreview.componentType, thisPreview.zone, thisPreview.index, appStore);
						else if (initialSelector.current) moveComponent(thisPreview.props.id, initialSelector.current, thisPreview, appStore);
					}
					const movedToNewPosition = ((_b2 = initialSelector.current) == null ? void 0 : _b2.zone) !== (thisPreview == null ? void 0 : thisPreview.zone) || ((_c = initialSelector.current) == null ? void 0 : _c.index) !== (thisPreview == null ? void 0 : thisPreview.index);
					dispatch({
						type: "setUi",
						ui: {
							itemSelector: {
								index,
								zone
							},
							isDragging: false
						},
						recordHistory: movedToNewPosition
					});
					(_d = dragListeners.dragend) == null || _d.forEach((fn) => {
						fn(event, manager);
					});
				};
				let dispose;
				dispose = j(() => {
					if (source.status === "idle") {
						onAnimationEnd();
						dispose?.();
					}
				});
			},
			onDragOver: (event, manager) => {
				var _a, _b, _c, _d;
				event.preventDefault();
				if (!((_a = zoneStore.getState()) == null ? void 0 : _a.draggedItem)) return;
				cancelDb();
				const { source, target } = event.operation;
				if (!target || !source || target.type === "void") return;
				const [sourceId] = source.id.split(":");
				const [targetId] = target.id.split(":");
				const sourceData = source.data;
				let sourceZone = sourceData.zone;
				let sourceIndex = sourceData.index;
				let targetZone = "";
				let targetIndex = 0;
				if (target.type === "component") {
					const targetData = target.data;
					targetZone = targetData.zone;
					targetIndex = targetData.index;
					const collisionData = (_b = manager.collisionObserver.collisions[0]) == null ? void 0 : _b.data;
					const dir = getDeepDir(target.element);
					const collisionPosition = (collisionData == null ? void 0 : collisionData.direction) === "up" || dir === "ltr" && (collisionData == null ? void 0 : collisionData.direction) === "left" || dir === "rtl" && (collisionData == null ? void 0 : collisionData.direction) === "right" ? "before" : "after";
					if (targetIndex >= sourceIndex && sourceZone === targetZone) targetIndex = targetIndex - 1;
					if (collisionPosition === "after") targetIndex = targetIndex + 1;
				} else {
					targetZone = target.id.toString();
					targetIndex = 0;
				}
				const path = ((_c = appStore.getState().state.indexes.nodes[target.id]) == null ? void 0 : _c.path) || [];
				if (targetId === sourceId || path.find((path2) => {
					const [pathId] = path2.split(":");
					return pathId === sourceId;
				})) return;
				if (dragMode.current === "new") zoneStore.setState({ previewIndex: { [targetZone]: {
					componentType: sourceData.componentType,
					type: "insert",
					index: targetIndex,
					zone: targetZone,
					element: source.element,
					props: { id: source.id.toString() }
				} } });
				else {
					if (!initialSelector.current) initialSelector.current = {
						zone: sourceData.zone,
						index: sourceData.index
					};
					const item = getItem(initialSelector.current, appStore.getState().state);
					if (item) zoneStore.setState({ previewIndex: { [targetZone]: {
						componentType: sourceData.componentType,
						type: "move",
						index: targetIndex,
						zone: targetZone,
						props: item.props,
						element: source.element
					} } });
				}
				(_d = dragListeners.dragover) == null || _d.forEach((fn) => {
					fn(event, manager);
				});
			},
			onDragStart: (event, manager) => {
				var _a;
				const { source } = event.operation;
				if (source && source.type !== "void") {
					const sourceData = source.data;
					const item = getItem({
						zone: sourceData.zone,
						index: sourceData.index
					}, appStore.getState().state);
					if (item) zoneStore.setState({ previewIndex: { [sourceData.zone]: {
						componentType: sourceData.componentType,
						type: "move",
						index: sourceData.index,
						zone: sourceData.zone,
						props: item.props,
						element: source.element
					} } });
				}
				(_a = dragListeners.dragstart) == null || _a.forEach((fn) => {
					fn(event, manager);
				});
			},
			onBeforeDragStart: (event) => {
				var _a, _b, _c, _d;
				dragMode.current = ((_a = event.operation.source) == null ? void 0 : _a.type) === "drawer" ? "new" : "existing";
				initialSelector.current = void 0;
				zoneStore.setState({ draggedItem: event.operation.source });
				if (((_b = appStore.getState().selectedItem) == null ? void 0 : _b.props.id) !== ((_c = event.operation.source) == null ? void 0 : _c.id)) dispatch({
					type: "setUi",
					ui: {
						itemSelector: null,
						isDragging: true
					},
					recordHistory: false
				});
				else dispatch({
					type: "setUi",
					ui: { isDragging: true },
					recordHistory: false
				});
				((_d = getFrame()) == null ? void 0 : _d.querySelector("[data-puck-entry]"))?.setAttribute("data-puck-dragging", "true");
			},
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ZoneStoreProvider, {
				store: zoneStore,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneProvider, {
					value: nextContextValue,
					children
				})
			})
		})
	});
};
var DragDropContext = ({ children, disableAutoScroll }) => {
	if (useAppStore((s) => s.status) === "LOADING") return children;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DragDropContextClient, {
		disableAutoScroll,
		children
	});
};
var getClassName15 = get_class_name_factory_default("Drawer", styles_module_default10);
var getClassNameItem2 = get_class_name_factory_default("DrawerItem", styles_module_default10);
var DrawerItemInner = ({ children, name, label, dragRef, isDragDisabled }) => {
	const CustomInner = (0, import_react.useMemo)(() => children || (({ children: children2 }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassNameItem2("default"),
		children: children2
	})), [children]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassNameItem2({ disabled: isDragDisabled }),
		ref: dragRef,
		onMouseDown: (e) => e.preventDefault(),
		"data-testid": dragRef ? `drawer-item:${name}` : "",
		"data-puck-drawer-item": true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomInner, {
			name,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassNameItem2("draggableWrapper"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: getClassNameItem2("draggable"),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassNameItem2("name"),
						children: label != null ? label : name
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassNameItem2("icon"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DragIcon, {})
					})]
				})
			})
		})
	});
};
var DrawerItemDraggable = ({ children, name, label, id, isDragDisabled }) => {
	const { ref } = useDraggable({
		id,
		data: { componentType: name },
		disabled: isDragDisabled,
		type: "drawer"
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName15("draggable"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName15("draggableBg"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerItemInner, {
				name,
				label,
				children
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName15("draggableFg"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerItemInner, {
				name,
				label,
				dragRef: ref,
				isDragDisabled,
				children
			})
		})]
	});
};
var DrawerItem = ({ name, children, id, label, index, isDragDisabled }) => {
	const resolvedId = id || name;
	const [dynamicId, setDynamicId] = (0, import_react.useState)(generateId(resolvedId));
	if (typeof index !== "undefined") console.error("Warning: The `index` prop on Drawer.Item is deprecated and no longer required.");
	useDragListener("dragend", () => {
		setDynamicId(generateId(resolvedId));
	}, [resolvedId]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerItemDraggable, {
		name,
		label,
		id: dynamicId,
		isDragDisabled,
		children
	}) }, dynamicId);
};
var Drawer = ({ children, droppableId, direction }) => {
	if (droppableId) console.error("Warning: The `droppableId` prop on Drawer is deprecated and no longer required.");
	if (direction) console.error("Warning: The `direction` prop on Drawer is deprecated and no longer required to achieve multi-directional dragging.");
	const id = useSafeId();
	const { ref } = useDroppable({
		id,
		type: "void",
		collisionPriority: 0
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName15(),
		ref,
		"data-puck-dnd": id,
		"data-puck-drawer": true,
		"data-puck-dnd-void": true,
		children
	});
};
Drawer.Item = DrawerItem;
init_react_import();
var getNumItems = (appStore, zoneCompound) => appStore.getState().state.indexes.zones[zoneCompound].contentIds.length;
var useMinEmptyHeight = ({ zoneCompound, userMinEmptyHeight, ref }) => {
	const appStore = useAppStoreApi();
	const [prevHeight, setPrevHeight] = (0, import_react.useState)(0);
	const [isAnimating, setIsAnimating] = (0, import_react.useState)(false);
	const { draggedItem, isZone } = useContextStore(ZoneStoreContext, (s) => {
		var _a, _b;
		return {
			draggedItem: ((_a = s.draggedItem) == null ? void 0 : _a.data.zone) === zoneCompound ? s.draggedItem : null,
			isZone: ((_b = s.draggedItem) == null ? void 0 : _b.data.zone) === zoneCompound
		};
	});
	const numItems = (0, import_react.useRef)(0);
	const onDragFinished = useOnDragFinished((finished) => {
		if (finished) {
			const newNumItems = getNumItems(appStore, zoneCompound);
			setPrevHeight(0);
			if (newNumItems || numItems.current === 0) {
				setIsAnimating(false);
				return;
			}
			const selectedItem = appStore.getState().selectedItem;
			const zones = appStore.getState().state.indexes.zones;
			const nodes = appStore.getState().nodes;
			nodes.setOverlayVisible(selectedItem == null ? void 0 : selectedItem.props.id, false);
			setTimeout(() => {
				var _a;
				const contentIds = ((_a = zones[zoneCompound]) == null ? void 0 : _a.contentIds) || [];
				nodes.syncNodes(contentIds);
				if (selectedItem) setTimeout(() => {
					nodes.syncNode(selectedItem.props.id);
					nodes.setOverlayVisible(selectedItem.props.id, true);
				}, 200);
				setIsAnimating(false);
			}, 100);
		}
	}, [
		appStore,
		prevHeight,
		zoneCompound
	]);
	(0, import_react.useEffect)(() => {
		if (draggedItem && ref.current) {
			if (isZone) {
				const rect = ref.current.getBoundingClientRect();
				numItems.current = getNumItems(appStore, zoneCompound);
				setPrevHeight(rect.height);
				setIsAnimating(true);
				return onDragFinished();
			}
		}
	}, [
		ref.current,
		draggedItem,
		onDragFinished
	]);
	const returnedMinHeight = isNaN(Number(userMinEmptyHeight)) ? userMinEmptyHeight : `${userMinEmptyHeight}px`;
	return [prevHeight ? `${prevHeight}px` : returnedMinHeight, isAnimating];
};
init_react_import();
init_react_import();
function useRenderedCallback(callback, deps) {
	const manager = useDragDropManager();
	return (0, import_react.useCallback)((...args) => __async(null, null, function* () {
		yield manager == null ? void 0 : manager.renderer.rendering;
		return callback(...args);
	}), [...deps, manager]);
}
var useContentIdsWithPreview = (contentIds, zoneCompound) => {
	const zoneStore = (0, import_react.useContext)(ZoneStoreContext);
	const preview = useContextStore(ZoneStoreContext, (s) => s.previewIndex[zoneCompound]);
	const isDragging = useAppStore((s) => s.state.ui.isDragging);
	const [contentIdsWithPreview, setContentIdsWithPreview] = (0, import_react.useState)(contentIds);
	const [localPreview, setLocalPreview] = (0, import_react.useState)(preview);
	const updateContent = useRenderedCallback((contentIds2, preview2, isDragging2, draggedItemId, previewExists) => {
		if (isDragging2 && !previewExists) return;
		if (preview2) if (preview2.type === "insert") setContentIdsWithPreview(insert(contentIds2.filter((id) => id !== preview2.props.id), preview2.index, preview2.props.id));
		else setContentIdsWithPreview(insert(contentIds2.filter((id) => id !== preview2.props.id), preview2.index, preview2.props.id));
		else setContentIdsWithPreview(previewExists ? contentIds2.filter((id) => id !== draggedItemId) : contentIds2);
		setLocalPreview(preview2);
	}, []);
	(0, import_react.useEffect)(() => {
		var _a;
		const s = zoneStore.getState();
		updateContent(contentIds, preview, isDragging, (_a = s.draggedItem) == null ? void 0 : _a.id, Object.keys(s.previewIndex || {}).length > 0);
	}, [
		contentIds,
		preview,
		isDragging
	]);
	return [contentIdsWithPreview, localPreview];
};
init_react_import();
var GRID_DRAG_AXIS = "dynamic";
var FLEX_ROW_DRAG_AXIS = "x";
var DEFAULT_DRAG_AXIS = "y";
var useDragAxis = (ref, collisionAxis) => {
	const status = useAppStore((s) => s.status);
	const [dragAxis, setDragAxis] = (0, import_react.useState)(collisionAxis || DEFAULT_DRAG_AXIS);
	const calculateDragAxis = (0, import_react.useCallback)(() => {
		if (ref.current) {
			const computedStyle = window.getComputedStyle(ref.current);
			if (computedStyle.display === "grid") setDragAxis(GRID_DRAG_AXIS);
			else if (computedStyle.display === "flex" && computedStyle.flexDirection === "row") setDragAxis(FLEX_ROW_DRAG_AXIS);
			else setDragAxis(DEFAULT_DRAG_AXIS);
		}
	}, [ref.current]);
	(0, import_react.useEffect)(() => {
		const onViewportChange = () => {
			calculateDragAxis();
		};
		window.addEventListener("viewportchange", onViewportChange);
		return () => {
			window.removeEventListener("viewportchange", onViewportChange);
		};
	}, []);
	(0, import_react.useEffect)(calculateDragAxis, [status, collisionAxis]);
	return [dragAxis, calculateDragAxis];
};
init_react_import();
var ContextSlotRender = ({ componentId, zone }) => {
	const config = useAppStore((s) => s.config);
	const metadata = useAppStore((s) => s.metadata);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotRenderPure, {
		content: useAppStore(useShallow((s) => {
			var _a, _b;
			const indexes = s.state.indexes;
			return ((_b = (_a = indexes.zones[`${componentId}:${zone}`]) == null ? void 0 : _a.contentIds) != null ? _b : []).map((contentId) => indexes.nodes[contentId].flatData);
		})),
		zone,
		config,
		metadata
	});
};
init_react_import();
function useFieldTransformsTracked(config, item, transforms, readOnly, forceReadOnly) {
	const prevProps = (0, import_react.useRef)(null);
	const prevResult = (0, import_react.useRef)(item.props);
	const mappers = (0, import_react.useMemo)(() => buildMappers(transforms, readOnly, forceReadOnly), [
		transforms,
		readOnly,
		forceReadOnly
	]);
	const transformedProps = (0, import_react.useMemo)(() => {
		var _a, _b, _c;
		const changedProps = {};
		const componentConfig = item.type === "root" ? config.root : (_a = config.components) == null ? void 0 : _a[item.type];
		let changeIncludesSlot = false;
		for (const fieldName in item.props) {
			const fieldType = (_c = (_b = componentConfig == null ? void 0 : componentConfig.fields) == null ? void 0 : _b[fieldName]) == null ? void 0 : _c.type;
			if (!prevProps.current || item.props[fieldName] !== prevProps.current[fieldName]) {
				changedProps[fieldName] = item.props[fieldName];
				if (fieldType === "slot") changeIncludesSlot = true;
			}
		}
		changedProps.id = item.props.id;
		prevProps.current = item.props;
		const mapped = mapFields(__spreadProps$6(__spreadValues$7({}, item), { props: changedProps }), mappers, config, false, changeIncludesSlot).props;
		prevResult.current = __spreadValues$7(__spreadValues$7({}, prevResult.current), mapped);
		return prevResult.current;
	}, [
		config,
		item,
		mappers
	]);
	return (0, import_react.useMemo)(() => __spreadValues$7(__spreadValues$7({}, item.props), transformedProps), [item.props, transformedProps]);
}
init_react_import();
init_react_import();
init_react_import();
var registerOverlayPortal = (el, opts = {}) => {
	if (!el) return;
	const { disableDrag = false, disableDragOnFocus = true } = opts;
	const stopPropagation = (e) => {
		e.stopPropagation();
	};
	el.addEventListener("mouseover", stopPropagation, { capture: true });
	const onFocus = () => {
		setTimeout(() => {
			el.addEventListener("pointerdown", stopPropagation, { capture: true });
		}, 200);
	};
	const onBlur = () => {
		el.removeEventListener("pointerdown", stopPropagation, { capture: true });
	};
	if (disableDrag) el.addEventListener("pointerdown", stopPropagation, { capture: true });
	else if (disableDragOnFocus) {
		el.addEventListener("focus", onFocus, { capture: true });
		el.addEventListener("blur", onBlur, { capture: true });
	}
	el.setAttribute("data-puck-overlay-portal", "true");
	return () => {
		el.removeEventListener("mouseover", stopPropagation, { capture: true });
		if (disableDrag) el.removeEventListener("pointerdown", stopPropagation, { capture: true });
		else if (disableDragOnFocus) {
			el.removeEventListener("focus", onFocus, { capture: true });
			el.removeEventListener("blur", onBlur, { capture: true });
		}
		el.removeAttribute("data-puck-overlay-portal");
	};
};
init_react_import();
var getClassName16 = get_class_name_factory_default("InlineTextField", { "InlineTextField": "_InlineTextField_104qp_1" });
var InlineTextFieldInternal = ({ propPath, componentId, value, isReadOnly, opts = {} }) => {
	var _a;
	const ref = (0, import_react.useRef)(null);
	const appStoreApi = useAppStoreApi();
	const disableLineBreaks = (_a = opts.disableLineBreaks) != null ? _a : false;
	(0, import_react.useEffect)(() => {
		const appStore = appStoreApi.getState();
		const data = appStore.state.indexes.nodes[componentId].data;
		if (!appStore.getComponentConfig(data.type)) throw new Error(`InlineTextField Error: No config defined for ${data.type}`);
		if (ref.current) {
			const safeValue = value != null ? value : "";
			if (safeValue !== ref.current.innerText) ref.current.replaceChildren(safeValue);
			const cleanupPortal = registerOverlayPortal(ref.current);
			const handleInput = (e) => __async(null, null, function* () {
				var _a2;
				const appStore2 = appStoreApi.getState();
				const node = appStore2.state.indexes.nodes[componentId];
				const zoneCompound = `${node.parentId}:${node.zone}`;
				const index = (_a2 = appStore2.state.indexes.zones[zoneCompound]) == null ? void 0 : _a2.contentIds.indexOf(componentId);
				let value2 = e.target.innerText;
				if (disableLineBreaks) value2 = value2.replaceAll(/\n/gm, "");
				const newProps = setDeep(node.data.props, propPath, value2);
				const resolvedData = yield appStore2.resolveComponentData(__spreadProps$6(__spreadValues$7({}, node.data), { props: newProps }), "replace");
				appStore2.dispatch({
					type: "replace",
					data: resolvedData.node,
					destinationIndex: index,
					destinationZone: zoneCompound
				});
			});
			ref.current.addEventListener("input", handleInput);
			return () => {
				var _a2;
				(_a2 = ref.current) == null || _a2.removeEventListener("input", handleInput);
				cleanupPortal?.();
			};
		}
	}, [
		appStoreApi,
		ref.current,
		value,
		disableLineBreaks
	]);
	const [isHovering, setIsHovering] = (0, import_react.useState)(false);
	const [isFocused, setIsFocused] = (0, import_react.useState)(false);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		className: getClassName16(),
		ref,
		contentEditable: isHovering || isFocused ? "plaintext-only" : "false",
		onClick: (e) => {
			e.preventDefault();
			e.stopPropagation();
		},
		onClickCapture: (e) => {
			e.preventDefault();
			e.stopPropagation();
			const itemSelector = getSelectorForId(appStoreApi.getState().state, componentId);
			appStoreApi.getState().setUi({ itemSelector });
		},
		onKeyDown: (e) => {
			e.stopPropagation();
			if (disableLineBreaks && e.key === "Enter" || isReadOnly) e.preventDefault();
		},
		onKeyUp: (e) => {
			e.stopPropagation();
			e.preventDefault();
		},
		onMouseOverCapture: () => setIsHovering(true),
		onMouseOutCapture: () => setIsHovering(false),
		onFocus: () => setIsFocused(true),
		onBlur: () => setIsFocused(false)
	});
};
var InlineTextField = (0, import_react.memo)(InlineTextFieldInternal);
var getInlineTextTransform = () => ({
	text: ({ value, componentId, field, propPath, isReadOnly }) => {
		if (field.contentEditable) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineTextField, {
			propPath,
			componentId,
			value,
			opts: { disableLineBreaks: true },
			isReadOnly
		});
		return value;
	},
	textarea: ({ value, componentId, field, propPath, isReadOnly }) => {
		if (field.contentEditable) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineTextField, {
			propPath,
			componentId,
			value,
			isReadOnly
		});
		return value;
	},
	custom: ({ value, componentId, field, propPath, isReadOnly }) => {
		if (field.contentEditable && typeof value === "string") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineTextField, {
			propPath,
			componentId,
			value,
			isReadOnly
		});
		return value;
	}
});
init_react_import();
var Editor2 = (0, import_react.lazy)(() => import("./Editor-MCVDFQH6-C5mVcivN.js").then((m) => ({ default: m.Editor })));
var RichTextRender = (0, import_react.lazy)(() => import("./Render-MJ563JES-BvL0nwRl.js").then((m) => ({ default: m.RichTextRender })));
var InlineEditorWrapper = (0, import_react.memo)(({ value, componentId, propPath, field, id }) => {
	const portalRef = (0, import_react.useRef)(null);
	const appStoreApi = useAppStoreApi();
	const onClickHandler = (e) => {
		e.preventDefault();
		e.stopPropagation();
	};
	const onClickCaptureHandler = (e) => {
		e.preventDefault();
		e.stopPropagation();
		const itemSelector = getSelectorForId(appStoreApi.getState().state, componentId);
		appStoreApi.getState().setUi({ itemSelector });
	};
	(0, import_react.useEffect)(() => {
		if (!portalRef.current) return;
		const cleanup = registerOverlayPortal(portalRef.current, { disableDragOnFocus: true });
		return () => cleanup == null ? void 0 : cleanup();
	}, [portalRef.current]);
	const handleChange = (0, import_react.useCallback)((content, ui) => __async(null, null, function* () {
		var _a;
		const appStore = appStoreApi.getState();
		const node = appStore.state.indexes.nodes[componentId];
		const zoneCompound = `${node.parentId}:${node.zone}`;
		const index = (_a = appStore.state.indexes.zones[zoneCompound]) == null ? void 0 : _a.contentIds.indexOf(componentId);
		const newProps = setDeep(node.data.props, propPath, content);
		const resolvedData = yield appStore.resolveComponentData(__spreadProps$6(__spreadValues$7({}, node.data), { props: newProps }), "replace");
		appStore.dispatch({
			type: "replace",
			data: resolvedData.node,
			destinationIndex: index,
			destinationZone: zoneCompound,
			ui
		});
	}), [
		appStoreApi,
		componentId,
		propPath
	]);
	const handleFocus = (0, import_react.useCallback)((editor) => {
		appStoreApi.setState({ currentRichText: {
			inlineComponentId: componentId,
			inline: true,
			field,
			editor,
			id
		} });
	}, [field, componentId]);
	if (!field.contentEditable) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
		fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RichTextRenderFallback, { content: value }),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RichTextRender, {
			content: value,
			field
		})
	});
	const editorProps = {
		content: value,
		onChange: handleChange,
		field,
		inline: true,
		onFocus: handleFocus,
		id,
		name: propPath
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref: portalRef,
		onClick: onClickHandler,
		onClickCapture: onClickCaptureHandler,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Suspense, {
			fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(EditorFallback, __spreadValues$7({}, editorProps)),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editor2, __spreadValues$7({}, editorProps))
		})
	});
});
InlineEditorWrapper.displayName = "InlineEditorWrapper";
var getRichTextTransform = () => ({ richtext: ({ value, componentId, field, propPath, isReadOnly }) => {
	const { contentEditable = true, tiptap } = field;
	if (contentEditable === false || isReadOnly) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RichTextRender, {
		content: value,
		field
	});
	const id = `${componentId}_${field.type}_${propPath}`;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InlineEditorWrapper, {
		value,
		componentId,
		propPath,
		field,
		id
	}, id);
} });
init_react_import();
init_react_import();
function shallowEqual(obj1, obj2, keysToIgnore = []) {
	if (Object.is(obj1, obj2)) return true;
	if (typeof obj1 !== "object" || obj1 === null || typeof obj2 !== "object" || obj2 === null) return false;
	if (Object.getPrototypeOf(obj1) !== Object.getPrototypeOf(obj2)) return false;
	const ignore = new Set(keysToIgnore);
	const keys1 = Object.keys(obj1).filter((k) => !ignore.has(k));
	const keys2 = Object.keys(obj2).filter((k) => !ignore.has(k));
	if (keys1.length !== keys2.length) return false;
	for (let i = 0; i < keys1.length; i++) {
		const currKey = keys1[i];
		if (!Object.prototype.hasOwnProperty.call(obj2, currKey)) return false;
		const val1 = obj1[currKey];
		const val2 = obj2[currKey];
		if (!Object.is(val1, val2)) return false;
	}
	return true;
}
var RenderComponent = ({ Component, componentProps: renderProps }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component, __spreadValues$7({}, renderProps));
};
var MemoizeComponent = (0, import_react.memo)(RenderComponent, (prev, next) => {
	let puckEquals = true;
	if ("puck" in prev.componentProps && "puck" in next.componentProps) puckEquals = deepEqual$2(prev.componentProps.puck, next.componentProps.puck);
	return prev.Component === next.Component && shallowEqual(prev.componentProps, next.componentProps, ["puck"]) && puckEquals;
});
init_react_import();
var ROOT_ZONE_VIRTUALIZATION_OVERSCAN = 5;
var DEFAULT_VIRTUALIZED_ITEM_HEIGHT = 320;
var measuredItemHeights = /* @__PURE__ */ new Map();
var getEstimatedItemHeight = (componentId) => {
	var _a;
	return (_a = measuredItemHeights.get(componentId)) != null ? _a : DEFAULT_VIRTUALIZED_ITEM_HEIGHT;
};
var cacheMeasuredItemHeight = (componentId, height) => {
	if (height <= 0) return;
	measuredItemHeights.set(componentId, height);
};
var VirtualizedDropZone = ({ contentIds, zoneCompound, renderItem }) => {
	const selectedId = useAppStore((s) => {
		var _a, _b;
		return (_b = (_a = s.selectedItem) == null ? void 0 : _a.props.id) != null ? _b : null;
	});
	const frame = getFrame();
	const zoneStore = (0, import_react.useContext)(ZoneStoreContext);
	const draggedItemId = useContextStore(ZoneStoreContext, (s) => {
		var _a;
		const draggedId = (_a = s.draggedItem) == null ? void 0 : _a.id;
		return draggedId ? String(draggedId) : null;
	});
	const dragTargetParentId = useContextStore(ZoneStoreContext, (s) => {
		var _a, _b;
		if ((_a = s.draggedItem) == null ? void 0 : _a.id) {
			const parentZone = Object.keys((_b = s.previewIndex) != null ? _b : {})[0];
			return parentZone == null ? void 0 : parentZone.split(":")[0];
		}
		return null;
	});
	const iframeWindow = frame == null ? void 0 : frame.defaultView;
	const measureRefsRef = (0, import_react.useRef)(/* @__PURE__ */ new Map());
	const appStoreApi = useAppStoreApi();
	const resolveIndex = (0, import_react.useCallback)((targetId) => {
		var _a, _b, _c, _d;
		if (!targetId || targetId === "root") return -1;
		const directIndex = contentIds.indexOf(targetId);
		if (directIndex > -1) return directIndex;
		const path = (_c = (_b = (_a = appStoreApi.getState().state.indexes.nodes) == null ? void 0 : _a[targetId]) == null ? void 0 : _b.path) != null ? _c : [];
		for (let i = path.length - 1; i >= 0; i -= 1) {
			const candidateId = (_d = path[i]) == null ? void 0 : _d.split(":")[0];
			if (!candidateId || candidateId === "root") continue;
			const index = contentIds.indexOf(candidateId);
			if (index > -1) return index;
		}
		return -1;
	}, [appStoreApi, contentIds]);
	const pinnedIndexes = (0, import_react.useMemo)(() => {
		const nextPinnedIndexes = /* @__PURE__ */ new Set();
		[
			selectedId,
			draggedItemId,
			dragTargetParentId
		].forEach((targetId) => {
			const currentIndex = resolveIndex(targetId);
			if (currentIndex > -1) nextPinnedIndexes.add(currentIndex);
		});
		return Array.from(nextPinnedIndexes).sort((a, b) => a - b);
	}, [
		dragTargetParentId,
		draggedItemId,
		resolveIndex,
		selectedId
	]);
	const rangeExtractor = (0, import_react.useCallback)((range) => {
		const indexes = defaultRangeExtractor(range);
		pinnedIndexes.forEach((index) => {
			if (!indexes.includes(index)) indexes.push(index);
		});
		indexes.sort((a, b) => a - b);
		return indexes;
	}, [pinnedIndexes]);
	const virtualizer = useVirtualizer({
		count: contentIds.length,
		getItemKey: (index) => contentIds[index],
		estimateSize: (index) => getEstimatedItemHeight(contentIds[index]),
		getScrollElement: () => iframeWindow != null ? iframeWindow : null,
		overscan: ROOT_ZONE_VIRTUALIZATION_OVERSCAN,
		observeElementRect: (instance, cb) => iframeWindow ? observeWindowRect(instance, cb) : observeElementRect(instance, cb),
		observeElementOffset: (instance, cb) => iframeWindow ? observeWindowOffset(instance, cb) : observeElementOffset(instance, cb),
		scrollToFn: (offset, options, instance) => iframeWindow ? windowScroll(offset, options, instance) : elementScroll(offset, options, instance),
		rangeExtractor,
		initialOffset: () => iframeWindow ? iframeWindow.scrollY : 0
	});
	(0, import_react.useEffect)(() => {
		zoneStore.getState().registerRootVirtualizer(zoneCompound, {
			resolveIndex: (targetId) => resolveIndex(targetId),
			virtualizer
		});
		return () => {
			zoneStore.getState().unregisterRootVirtualizer(zoneCompound);
		};
	}, [
		resolveIndex,
		virtualizer,
		zoneCompound,
		zoneStore
	]);
	const getMeasureRef = (0, import_react.useCallback)((componentId) => {
		const cachedRef = measureRefsRef.current.get(componentId);
		if (cachedRef) return cachedRef;
		const measureRef = (element) => {
			if (!element) return;
			const height = Math.ceil(element.getBoundingClientRect().height) || DEFAULT_VIRTUALIZED_ITEM_HEIGHT;
			if (typeof height === "number" && height > 0) cacheMeasuredItemHeight(componentId, height);
		};
		measureRefsRef.current.set(componentId, measureRef);
		return measureRef;
	}, []);
	(0, import_react.useEffect)(() => {
		const validIds = new Set(contentIds);
		Array.from(measureRefsRef.current.keys()).forEach((componentId) => {
			if (!validIds.has(componentId)) measureRefsRef.current.delete(componentId);
		});
	}, [contentIds]);
	const virtualItems = virtualizer.getVirtualItems();
	const totalSize = virtualizer.getTotalSize();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: (0, import_react.useMemo)(() => {
		const items = [];
		let previousEnd = 0;
		let previousIndex = -1;
		virtualItems.forEach((virtualItem) => {
			if (!virtualItem) return;
			const componentId = contentIds[virtualItem.index];
			const gapSize = Math.max(virtualItem.start - previousEnd, 0);
			if (gapSize > 0) items.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: `${gapSize}px` } }, `gap:${previousIndex}:${virtualItem.index}`));
			items.push(renderItem({
				componentId,
				index: virtualItem.index,
				measureRef: getMeasureRef(componentId)
			}));
			previousEnd = virtualItem.end;
			previousIndex = virtualItem.index;
		});
		const trailingGap = Math.max(totalSize - previousEnd, 0);
		if (trailingGap > 0) items.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { style: { height: `${trailingGap}px` } }, `gap:${previousIndex}:end`));
		return items;
	}, [
		totalSize,
		virtualItems,
		getMeasureRef
	]) });
};
var getClassName17 = get_class_name_factory_default("DropZone", styles_module_default9);
var getRandomColor = () => `#${Math.floor(Math.random() * 16777215).toString(16)}`;
var RENDER_DEBUG = false;
var InsertPreview = ({ element, label, override }) => {
	if (element) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { dangerouslySetInnerHTML: { __html: element.outerHTML } });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerItemInner, {
		name: label,
		children: override
	});
};
var DropZoneEditPure = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneEdit, __spreadValues$7({}, props));
var DropZoneChild = ({ zoneCompound, componentId, index, dragAxis, collisionAxis, inDroppableZone, itemRef }) => {
	var _a, _b;
	const metadata = useAppStore((s) => s.metadata);
	const ctx = (0, import_react.useContext)(dropZoneContext);
	const { depth = 1 } = ctx != null ? ctx : {};
	const zoneStore = (0, import_react.useContext)(ZoneStoreContext);
	const nodeProps = useAppStore(useShallow((s) => {
		var _a2;
		return (_a2 = s.state.indexes.nodes[componentId]) == null ? void 0 : _a2.flatData.props;
	}));
	const nodeType = useAppStore((s) => {
		var _a2;
		return (_a2 = s.state.indexes.nodes[componentId]) == null ? void 0 : _a2.data.type;
	});
	const nodeReadOnly = useAppStore(useShallow((s) => {
		var _a2;
		return (_a2 = s.state.indexes.nodes[componentId]) == null ? void 0 : _a2.data.readOnly;
	}));
	const item = (0, import_react.useMemo)(() => {
		if (nodeProps) return expandNode({
			type: nodeType,
			props: nodeProps
		});
		const preview = zoneStore.getState().previewIndex[zoneCompound];
		if (componentId === (preview == null ? void 0 : preview.props.id)) return {
			type: preview.componentType,
			props: preview.props,
			previewType: preview.type,
			element: preview.element
		};
		return null;
	}, [
		useAppStoreApi(),
		componentId,
		zoneCompound,
		nodeType,
		nodeProps
	]);
	const componentConfig = useAppStore((s) => (item == null ? void 0 : item.type) ? s.config.components[item.type] : null);
	const puckProps = (0, import_react.useMemo)(() => ({
		renderDropZone: DropZoneEditPure,
		isEditing: true,
		dragRef: null,
		metadata: __spreadValues$7(__spreadValues$7({}, metadata), componentConfig == null ? void 0 : componentConfig.metadata)
	}), [metadata, componentConfig == null ? void 0 : componentConfig.metadata]);
	const overrides = useAppStore((s) => s.overrides);
	const isLoading = useAppStore((s) => {
		var _a2;
		return ((_a2 = s.componentState[componentId]) == null ? void 0 : _a2.loadingCount) > 0;
	});
	const isSelected = useAppStore((s) => {
		var _a2;
		return ((_a2 = s.selectedItem) == null ? void 0 : _a2.props.id) === componentId || false;
	});
	let label = (_b = (_a = componentConfig == null ? void 0 : componentConfig.label) != null ? _a : item == null ? void 0 : item.type.toString()) != null ? _b : "Component";
	const defaultsProps = (0, import_react.useMemo)(() => __spreadProps$6(__spreadValues$7(__spreadValues$7({}, componentConfig == null ? void 0 : componentConfig.defaultProps), item == null ? void 0 : item.props), {
		puck: puckProps,
		editMode: true
	}), [
		componentConfig == null ? void 0 : componentConfig.defaultProps,
		item == null ? void 0 : item.props,
		puckProps
	]);
	const defaultedNode = (0, import_react.useMemo)(() => {
		var _a2;
		return {
			type: (_a2 = item == null ? void 0 : item.type) != null ? _a2 : nodeType,
			props: defaultsProps
		};
	}, [
		item == null ? void 0 : item.type,
		nodeType,
		defaultsProps
	]);
	const config = useAppStore((s) => s.config);
	const plugins = useAppStore((s) => s.plugins);
	const userFieldTransforms = useAppStore((s) => s.fieldTransforms);
	const transformedProps = useFieldTransformsTracked(config, defaultedNode, (0, import_react.useMemo)(() => __spreadValues$7(__spreadValues$7(__spreadValues$7(__spreadValues$7(__spreadValues$7({}, getSlotTransform(DropZoneEditPure, (slotProps) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ContextSlotRender, {
		componentId,
		zone: slotProps.zone
	}))), getInlineTextTransform()), getRichTextTransform()), plugins.reduce((acc, plugin) => __spreadValues$7(__spreadValues$7({}, acc), plugin.fieldTransforms), {})), userFieldTransforms), [plugins, userFieldTransforms]), nodeReadOnly, isLoading);
	if (!item) return;
	const Render2 = componentConfig ? componentConfig.render : () => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		style: {
			padding: 48,
			textAlign: "center"
		},
		children: ["No configuration for ", item.type]
	});
	let componentType = item.type;
	const isInserting = "previewType" in item ? item.previewType === "insert" : false;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DraggableComponent, {
		id: componentId,
		componentType,
		zoneCompound,
		depth: depth + 1,
		index,
		isLoading,
		isSelected,
		label,
		autoDragAxis: dragAxis,
		userDragAxis: collisionAxis,
		inDroppableZone,
		itemRef,
		children: (dragRef) => {
			var _a2;
			if ((componentConfig == null ? void 0 : componentConfig.inline) && !isInserting) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MemoizeComponent, {
				Component: Render2,
				componentProps: __spreadProps$6(__spreadValues$7({}, transformedProps), { puck: __spreadProps$6(__spreadValues$7({}, transformedProps.puck), { dragRef }) })
			});
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				ref: dragRef,
				children: isInserting ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InsertPreview, {
					label,
					override: (_a2 = overrides.componentItem) != null ? _a2 : overrides.drawerItem,
					element: "element" in item && item.element ? item.element : void 0
				}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MemoizeComponent, {
					Component: Render2,
					componentProps: transformedProps
				})
			});
		}
	});
};
var DropZoneChildMemo = (0, import_react.memo)(DropZoneChild);
var DropZoneEdit = (0, import_react.forwardRef)(function DropZoneEditInternal({ zone, allow, disallow, style, className, minEmptyHeight: userMinEmptyHeight = "128px", collisionAxis, as }, userRef) {
	const ctx = (0, import_react.useContext)(dropZoneContext);
	const appStoreApi = useAppStoreApi();
	const { areaId, depth = 0, registerLocalZone, unregisterLocalZone } = ctx != null ? ctx : {};
	const path = useAppStore(useShallow((s) => {
		var _a;
		return areaId ? (_a = s.state.indexes.nodes[areaId]) == null ? void 0 : _a.path : null;
	}));
	let zoneCompound = rootDroppableId;
	if (areaId) {
		if (zone !== rootDroppableId) zoneCompound = `${areaId}:${zone}`;
	}
	const isRootZone = zoneCompound === rootDroppableId || zone === rootDroppableId || areaId === "root";
	const inNextDeepestArea = useContextStore(ZoneStoreContext, (s) => s.nextAreaDepthIndex[areaId || ""]);
	const zoneContentIds = useAppStore(useShallow((s) => {
		var _a;
		return (_a = s.state.indexes.zones[zoneCompound]) == null ? void 0 : _a.contentIds;
	}));
	const zoneType = useAppStore(useShallow((s) => {
		var _a;
		return (_a = s.state.indexes.zones[zoneCompound]) == null ? void 0 : _a.type;
	}));
	(0, import_react.useEffect)(() => {
		if (!zoneType || zoneType === "dropzone") {
			if (ctx == null ? void 0 : ctx.registerZone) ctx?.registerZone(zoneCompound);
		}
	}, [zoneType, appStoreApi]);
	(0, import_react.useEffect)(() => {
		if (zoneType === "dropzone") {
			if (zoneCompound !== rootDroppableId) console.warn("DropZones have been deprecated in favor of slot fields and will be removed in a future version of Puck. Please see the migration guide: https://www.puckeditor.com/docs/guides/migrations/dropzones-to-slots");
		}
	}, [zoneType]);
	const contentIds = (0, import_react.useMemo)(() => {
		return zoneContentIds || [];
	}, [zoneContentIds]);
	const ref = (0, import_react.useRef)(null);
	const acceptsTarget = (0, import_react.useCallback)((componentType) => {
		if (!componentType) return true;
		if (disallow) {
			const defaultedAllow = allow || [];
			if ((disallow || []).filter((item) => defaultedAllow.indexOf(item) === -1).indexOf(componentType) !== -1) return false;
		} else if (allow) {
			if (allow.indexOf(componentType) === -1) return false;
		}
		return true;
	}, [allow, disallow]);
	const targetAccepted = useContextStore(ZoneStoreContext, (s) => {
		var _a;
		return acceptsTarget((_a = s.draggedItem) == null ? void 0 : _a.data.componentType);
	});
	const hoveringOverArea = inNextDeepestArea || isRootZone;
	const isEnabled = useContextStore(ZoneStoreContext, (s) => {
		var _a;
		let _isEnabled = true;
		_isEnabled = (_a = s.zoneDepthIndex[zoneCompound]) != null ? _a : false;
		if (_isEnabled) _isEnabled = targetAccepted;
		return _isEnabled;
	});
	(0, import_react.useEffect)(() => {
		if (registerLocalZone) registerLocalZone(zoneCompound, targetAccepted || isEnabled);
		return () => {
			if (unregisterLocalZone) unregisterLocalZone(zoneCompound);
		};
	}, [
		targetAccepted,
		isEnabled,
		zoneCompound
	]);
	const [contentIdsWithPreview, preview] = useContentIdsWithPreview(contentIds, zoneCompound);
	const isDropEnabled = isEnabled && (preview ? contentIdsWithPreview.length === 1 : contentIdsWithPreview.length === 0);
	const zoneStore = (0, import_react.useContext)(ZoneStoreContext);
	(0, import_react.useEffect)(() => {
		const { enabledIndex } = zoneStore.getState();
		zoneStore.setState({ enabledIndex: __spreadProps$6(__spreadValues$7({}, enabledIndex), { [zoneCompound]: isEnabled }) });
	}, [
		isEnabled,
		zoneStore,
		zoneCompound
	]);
	const { ref: dropRef } = useDroppable({
		id: zoneCompound,
		collisionPriority: isEnabled ? depth : 0,
		disabled: !isDropEnabled,
		collisionDetector: pointerIntersection,
		type: "dropzone",
		data: {
			areaId,
			depth,
			isDroppableTarget: targetAccepted,
			path: path || []
		}
	});
	const isAreaSelected = useAppStore((s) => (s == null ? void 0 : s.selectedItem) && areaId === (s == null ? void 0 : s.selectedItem.props.id));
	const [dragAxis] = useDragAxis(ref, collisionAxis);
	const [minEmptyHeight, isAnimating] = useMinEmptyHeight({
		zoneCompound,
		userMinEmptyHeight,
		ref
	});
	const setRefs = (0, import_react.useCallback)((node) => {
		assignRefs([
			ref,
			dropRef,
			userRef
		], node);
	}, [dropRef]);
	const _experimentalVirtualization = useAppStore((s) => s._experimentalVirtualization);
	const El = as != null ? as : "div";
	const shouldVirtualizeItems = _experimentalVirtualization && (areaId != null ? areaId : "root") === "root" && depth === 0;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(El, {
		className: `${getClassName17({
			isRootZone,
			hoveringOverArea,
			isEnabled,
			isAreaSelected,
			hasChildren: contentIds.length > 0,
			isAnimating
		})}${className ? ` ${className}` : ""}`,
		ref: setRefs,
		"data-testid": `dropzone:${zoneCompound}`,
		"data-puck-dropzone": zoneCompound,
		style: __spreadProps$6(__spreadValues$7({}, style), {
			"--min-empty-height": minEmptyHeight,
			backgroundColor: RENDER_DEBUG ? getRandomColor() : style == null ? void 0 : style.backgroundColor
		}),
		children: shouldVirtualizeItems ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VirtualizedDropZone, {
			contentIds: contentIdsWithPreview,
			zoneCompound,
			renderItem: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneChildMemo, {
				zoneCompound,
				componentId: props.componentId,
				dragAxis,
				index: props.index,
				collisionAxis,
				inDroppableZone: targetAccepted,
				itemRef: props.measureRef
			}, props.componentId)
		}) : contentIdsWithPreview.map((componentId, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneChildMemo, {
			zoneCompound,
			componentId,
			dragAxis,
			index: i,
			collisionAxis,
			inDroppableZone: targetAccepted
		}, componentId))
	});
});
var DropZoneRenderItem = ({ config, item, metadata }) => {
	const Component = config.components[item.type];
	const props = useSlots(config, item, (slotProps) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotRenderPure, __spreadProps$6(__spreadValues$7({}, slotProps), {
		config,
		metadata
	})));
	const nextContextValue = (0, import_react.useMemo)(() => ({
		areaId: props.id,
		depth: 1
	}), [props]);
	const richtextProps = useRichtextProps(Component.fields, props);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneProvider, {
		value: nextContextValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Component.render, __spreadProps$6(__spreadValues$7(__spreadValues$7({}, props), richtextProps), { puck: __spreadProps$6(__spreadValues$7({}, props.puck), {
			renderDropZone: DropZoneRenderPure,
			metadata: __spreadValues$7(__spreadValues$7({}, metadata), Component.metadata)
		}) }))
	}, props.id);
};
var DropZoneRenderPure = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneRender, __spreadValues$7({}, props));
var DropZoneRender = (0, import_react.forwardRef)(function DropZoneRenderInternal({ className, style, zone, as }, ref) {
	const ctx = (0, import_react.useContext)(dropZoneContext);
	const { areaId = "root" } = ctx || {};
	const { config, data, metadata } = (0, import_react.useContext)(renderContext);
	let zoneCompound = `${areaId}:${zone}`;
	let content = (data == null ? void 0 : data.content) || [];
	(0, import_react.useEffect)(() => {
		if (!content) {
			if (ctx == null ? void 0 : ctx.registerZone) ctx?.registerZone(zoneCompound);
		}
	}, [content]);
	const El = as != null ? as : "div";
	if (!data || !config) return null;
	if (zoneCompound !== rootDroppableId) content = setupZone(data, zoneCompound).zones[zoneCompound];
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(El, {
		className,
		style,
		ref,
		children: content.map((item) => {
			if (config.components[item.type]) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneRenderItem, {
				config,
				item,
				metadata
			}, item.props.id);
			return null;
		})
	});
});
var DropZonePure = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZone, __spreadValues$7({}, props));
var DropZone = (0, import_react.forwardRef)(function DropZone2(props, ref) {
	const ctx = (0, import_react.useContext)(dropZoneContext);
	if ((ctx == null ? void 0 : ctx.mode) === "edit") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneEdit, __spreadProps$6(__spreadValues$7({}, props), { ref })) });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneRender, __spreadProps$6(__spreadValues$7({}, props), { ref })) });
});
var renderContext = import_react.createContext({
	config: { components: {} },
	data: {
		root: {},
		content: []
	},
	metadata: {}
});
function Render({ config, data, metadata = {} }) {
	var _a, _b;
	const defaultedData = __spreadProps$6(__spreadValues$7({}, data), {
		root: data.root || {},
		content: data.content || []
	});
	const rootProps = "props" in defaultedData.root ? defaultedData.root.props : defaultedData.root;
	const title = (rootProps == null ? void 0 : rootProps.title) || "";
	const pageProps = __spreadProps$6(__spreadValues$7({}, rootProps), {
		puck: {
			renderDropZone: DropZonePure,
			isEditing: false,
			dragRef: null,
			metadata
		},
		title,
		editMode: false,
		id: "puck-root"
	});
	const propsWithSlots = useSlots(config, {
		type: "root",
		props: pageProps
	}, (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotRender, __spreadProps$6(__spreadValues$7({}, props), {
		config,
		metadata
	})));
	const richtextProps = useRichtextProps((_a = config.root) == null ? void 0 : _a.fields, pageProps);
	const nextContextValue = (0, import_react.useMemo)(() => ({
		mode: "render",
		depth: 0
	}), []);
	if ((_b = config.root) == null ? void 0 : _b.render) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(renderContext.Provider, {
		value: {
			config,
			data: defaultedData,
			metadata
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneProvider, {
			value: nextContextValue,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(config.root.render, __spreadProps$6(__spreadValues$7(__spreadValues$7({}, propsWithSlots), richtextProps), { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneRenderPure, { zone: rootZone }) }))
		})
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(renderContext.Provider, {
		value: {
			config,
			data: defaultedData,
			metadata
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneProvider, {
			value: nextContextValue,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZoneRenderPure, { zone: rootZone })
		})
	});
}
init_react_import();
init_react_import();
init_react_import();
function resolveAndReplaceData(currentData, getState, trigger = "force") {
	return __async(this, null, function* () {
		const resolvedResult = yield getState().resolveComponentData(currentData, trigger);
		if (!resolvedResult.didChange) return;
		const itemSelector = getSelectorForId(getState().state, resolvedResult.node.props.id);
		if (!itemSelector) {
			console.warn(`Warning: Could not find component with id "${currentData.props.id}" to resolve its data. Component may have been removed or the id is invalid.`);
			return;
		}
		getState().dispatch({
			type: "replace",
			data: toComponent(resolvedResult.node),
			destinationIndex: itemSelector.index,
			destinationZone: itemSelector.zone
		});
	});
}
function resolveDataById(id, getState, trigger) {
	return __async(this, null, function* () {
		const node = getState().state.indexes.nodes[id];
		if (!node) {
			console.warn(`Warning: Could not find component with id "${id}" to resolve its data. Component may have been removed or the id is invalid.`);
			return;
		}
		yield resolveAndReplaceData(node.data, getState, trigger);
	});
}
init_react_import();
function resolveDataBySelector(selector, getState, trigger) {
	return __async(this, null, function* () {
		const item = getItem(selector, getState().state);
		if (!item) {
			console.warn(`Warning: Could not find component for selector "${JSON.stringify(selector)}" to resolve its data. Component may have been removed or the selector is invalid.`);
			return;
		}
		yield resolveAndReplaceData(toComponent(item), getState, trigger);
	});
}
var generateUsePuck = (store, getState) => {
	const history = {
		back: store.history.back,
		forward: store.history.forward,
		setHistories: store.history.setHistories,
		setHistoryIndex: store.history.setHistoryIndex,
		hasPast: store.history.hasPast(),
		hasFuture: store.history.hasFuture(),
		histories: store.history.histories,
		index: store.history.index
	};
	const storeData = {
		appState: makeStatePublic(store.state),
		config: store.config,
		dispatch: store.dispatch,
		getPermissions: store.permissions.getPermissions,
		refreshPermissions: store.permissions.refreshPermissions,
		resolveDataById: (id, trigger) => resolveDataById(id, getState, trigger),
		resolveDataBySelector: (selector, trigger) => resolveDataBySelector(selector, getState, trigger),
		history,
		selectedItem: store.selectedItem || null,
		getItemBySelector: (selector) => getItem(selector, store.state),
		getItemById: (id) => store.state.indexes.nodes[id].data,
		getSelectorForId: (id) => getSelectorForId(store.state, id),
		getParentById: (id) => {
			const parentId = store.state.indexes.nodes[id].parentId;
			if (parentId === null) return;
			const parentNode = store.state.indexes.nodes[parentId];
			if (!parentNode) return;
			return parentNode.data;
		}
	};
	storeData.__private = { appState: store.state };
	return storeData;
};
var UsePuckStoreContext = (0, import_react.createContext)(null);
var convertToPickedStore = (store) => {
	return {
		state: store.state,
		config: store.config,
		dispatch: store.dispatch,
		permissions: store.permissions,
		history: store.history,
		selectedItem: store.selectedItem
	};
};
var useRegisterUsePuckStore = (appStore) => {
	const [usePuckStore] = (0, import_react.useState)(() => createStore(() => generateUsePuck(convertToPickedStore(appStore.getState()), appStore.getState)));
	(0, import_react.useEffect)(() => {
		return appStore.subscribe((store) => convertToPickedStore(store), (pickedStore) => {
			usePuckStore.setState(generateUsePuck(pickedStore, appStore.getState));
		});
	}, []);
	return usePuckStore;
};
function createUsePuck() {
	return function usePuck2(selector) {
		const usePuckApi = (0, import_react.useContext)(UsePuckStoreContext);
		if (!usePuckApi) throw new Error("usePuck must be used inside <Puck>.");
		return useStore(usePuckApi, selector != null ? selector : ((s) => s));
	};
}
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
var getClassName18 = get_class_name_factory_default("ComponentList", {
	"ComponentList": "_ComponentList_1rrlt_1",
	"ComponentList--isExpanded": "_ComponentList--isExpanded_1rrlt_5",
	"ComponentList-content": "_ComponentList-content_1rrlt_9",
	"ComponentList-title": "_ComponentList-title_1rrlt_17",
	"ComponentList-titleIcon": "_ComponentList-titleIcon_1rrlt_53"
});
var ComponentListItem = ({ name, label }) => {
	var _a;
	const overrides = useAppStore((s) => s.overrides);
	const canInsert = useAppStore((s) => s.permissions.getPermissions({ type: name }).insert);
	(0, import_react.useEffect)(() => {
		if (overrides.componentItem) console.warn("The `componentItem` override has been deprecated and renamed to `drawerItem`");
	}, [overrides]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Drawer.Item, {
		label,
		name,
		isDragDisabled: !canInsert,
		children: (_a = overrides.componentItem) != null ? _a : overrides.drawerItem
	});
};
var ComponentList = ({ children, title, id }) => {
	const config = useAppStore((s) => s.config);
	const setUi = useAppStore((s) => s.setUi);
	const componentList = useAppStore((s) => s.state.ui.componentList);
	const { expanded = true } = componentList[id] || {};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName18({ isExpanded: expanded }),
		children: [title && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
			type: "button",
			className: getClassName18("title"),
			onClick: () => setUi({ componentList: __spreadProps$6(__spreadValues$7({}, componentList), { [id]: __spreadProps$6(__spreadValues$7({}, componentList[id]), { expanded: !expanded }) }) }),
			title: expanded ? `Collapse${title ? ` ${title}` : ""}` : `Expand${title ? ` ${title}` : ""}`,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: title }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassName18("titleIcon"),
				children: expanded ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronUp, { size: 12 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown$1, { size: 12 })
			})]
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName18("content"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Drawer, { children: children || Object.keys(config.components).map((componentKey) => {
				var _a;
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComponentListItem, {
					label: (_a = config.components[componentKey]["label"]) != null ? _a : componentKey,
					name: componentKey
				}, componentKey);
			}) })
		})]
	});
};
ComponentList.Item = ComponentListItem;
var useComponentList = () => {
	const [componentList, setComponentList] = (0, import_react.useState)();
	const config = useAppStore((s) => s.config);
	const uiComponentList = useAppStore((s) => s.state.ui.componentList);
	(0, import_react.useEffect)(() => {
		var _a, _b, _c;
		if (Object.keys(uiComponentList).length > 0) {
			const matchedComponents = [];
			let _componentList;
			_componentList = Object.entries(uiComponentList).map(([categoryKey, category]) => {
				if (!category.components) return null;
				category.components.forEach((componentName) => {
					matchedComponents.push(componentName);
				});
				if (category.visible === false) return null;
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComponentList, {
					id: categoryKey,
					title: category.title || categoryKey,
					children: category.components.map((componentName, i) => {
						var _a2;
						const componentConf = config.components[componentName] || {};
						return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComponentList.Item, {
							label: (_a2 = componentConf["label"]) != null ? _a2 : componentName,
							name: componentName,
							index: i
						}, componentName);
					})
				}, categoryKey);
			});
			const remainingComponents = Object.keys(config.components).filter((component) => matchedComponents.indexOf(component) === -1);
			if (remainingComponents.length > 0 && !((_a = uiComponentList.other) == null ? void 0 : _a.components) && ((_b = uiComponentList.other) == null ? void 0 : _b.visible) !== false) _componentList.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComponentList, {
				id: "other",
				title: ((_c = uiComponentList.other) == null ? void 0 : _c.title) || "Other",
				children: remainingComponents.map((componentName, i) => {
					var _a2;
					const componentConf = config.components[componentName] || {};
					return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComponentList.Item, {
						name: componentName,
						label: (_a2 = componentConf["label"]) != null ? _a2 : componentName,
						index: i
					}, componentName);
				})
			}, "other"));
			setComponentList(_componentList);
		}
	}, [
		config.categories,
		config.components,
		uiComponentList
	]);
	return componentList;
};
var Components = () => {
	const overrides = useAppStore((s) => s.overrides);
	const componentList = useComponentList();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)((0, import_react.useMemo)(() => {
		if (overrides.components) console.warn("The `components` override has been deprecated and renamed to `drawer`");
		return overrides.components || overrides.drawer || "div";
	}, [overrides]), { children: componentList ? componentList : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComponentList, { id: "all" }) });
};
init_react_import();
var getClassName19 = get_class_name_factory_default("BlocksPlugin", { "BlocksPlugin": "_BlocksPlugin_1ey1i_1" });
var blocksPlugin = () => ({
	name: "blocks",
	label: "Blocks",
	render: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName19(),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Components, {})
	}),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Hammer, {})
});
init_react_import();
init_react_import();
init_react_import();
init_react_import();
var styles_module_default14 = {
	"LayerTree": "_LayerTree_7rx04_1",
	"LayerTree-zoneTitle": "_LayerTree-zoneTitle_7rx04_11",
	"LayerTree-helper": "_LayerTree-helper_7rx04_17",
	"Layer": "_Layer_7rx04_1",
	"Layer-inner": "_Layer-inner_7rx04_29",
	"Layer--containsZone": "_Layer--containsZone_7rx04_35",
	"Layer-clickable": "_Layer-clickable_7rx04_39",
	"Layer--isSelected": "_Layer--isSelected_7rx04_61",
	"Layer-chevron": "_Layer-chevron_7rx04_77",
	"Layer--childIsSelected": "_Layer--childIsSelected_7rx04_78",
	"Layer-zones": "_Layer-zones_7rx04_82",
	"Layer-title": "_Layer-title_7rx04_96",
	"Layer-name": "_Layer-name_7rx04_105",
	"Layer-icon": "_Layer-icon_7rx04_111",
	"Layer-zoneIcon": "_Layer-zoneIcon_7rx04_116"
};
var getClassName20 = get_class_name_factory_default("LayerTree", styles_module_default14);
var getClassNameLayer = get_class_name_factory_default("Layer", styles_module_default14);
var DEFAULT_LAYER_ROW_HEIGHT = 32;
var LAYER_TREE_VIRTUALIZATION_OVERSCAN = 8;
var MIN_VIRTUALIZED_LAYER_COUNT = 25;
var measuredRowHeights = /* @__PURE__ */ new Map();
var getZonesByParent = (zones) => {
	return Object.keys(zones).reduce((acc, zone) => {
		const [parentId] = zone.split(":");
		acc[parentId] = [...acc[parentId] || [], zone];
		return acc;
	}, {});
};
var getZoneLabel = (zoneCompound, nodes, config, label) => {
	var _a, _b, _c, _d;
	if (label !== void 0) return label;
	const [componentId, slotId] = zoneCompound.split(":");
	if (!slotId) return;
	const componentType = (_a = nodes[componentId]) == null ? void 0 : _a.data.type;
	const configForComponent = componentType && componentType !== "root" ? config.components[componentType] : config.root;
	return (_d = (_c = (_b = configForComponent == null ? void 0 : configForComponent.fields) == null ? void 0 : _b[slotId]) == null ? void 0 : _c.label) != null ? _d : slotId;
};
var buildLayerNode = ({ config, itemId, index, nodes, zoneCompound, zones, zonesByParent }) => {
	var _a, _b, _c;
	const nodeData = nodes[itemId];
	const componentType = ((_a = nodeData == null ? void 0 : nodeData.data.type) == null ? void 0 : _a.toString()) || "Component";
	const label = (_c = (_b = config.components[componentType]) == null ? void 0 : _b.label) != null ? _c : componentType;
	return {
		childZones: (zonesByParent[itemId] || []).map((childZoneCompound) => buildLayerTree({
			config,
			nodes,
			zoneCompound: childZoneCompound,
			zones,
			zonesByParent
		})),
		componentType,
		index,
		itemId,
		label,
		zoneCompound
	};
};
var buildLayerTree = ({ config, label, nodes, zoneCompound, zones, zonesByParent = getZonesByParent(zones) }) => {
	var _a, _b;
	return {
		items: ((_b = (_a = zones[zoneCompound]) == null ? void 0 : _a.contentIds) != null ? _b : []).map((itemId, index) => buildLayerNode({
			config,
			itemId,
			index,
			nodes,
			zoneCompound,
			zones,
			zonesByParent
		})),
		label: getZoneLabel(zoneCompound, nodes, config, label),
		zoneCompound
	};
};
var getEstimatedRowHeight = (itemId) => {
	var _a;
	return (_a = measuredRowHeights.get(itemId)) != null ? _a : DEFAULT_LAYER_ROW_HEIGHT;
};
var cacheMeasuredRowHeight = (itemId, height) => {
	if (height <= 0) return;
	measuredRowHeights.set(itemId, height);
};
var getScrollParent = (el) => {
	var _a;
	let current = (_a = el == null ? void 0 : el.parentElement) != null ? _a : null;
	while (current) {
		const { overflow, overflowY } = getComputedStyle(current);
		if ([overflow, overflowY].some((value) => /auto|scroll/.test(value))) return current;
		current = current.parentElement;
	}
	return null;
};
var Layer = (0, import_react.forwardRef)(function Layer2({ childIsSelected, dataIndex, depth, isSelected, node, selectedId, selectedPathIds }, ref) {
	const dispatch = useAppStore((s) => s.dispatch);
	const zoneStore = (0, import_react.useContext)(ZoneStoreContext);
	const isHovering = useContextStore(ZoneStoreContext, (s) => s.hoveringComponent === node.itemId);
	const containsZone = node.childZones.length > 0;
	const setItemSelector = (0, import_react.useCallback)((itemSelector) => {
		dispatch({
			type: "setUi",
			ui: { itemSelector }
		});
	}, [dispatch]);
	const shouldRenderChildren = isSelected || childIsSelected;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", {
		ref,
		className: getClassNameLayer({
			childIsSelected,
			containsZone,
			isHovering,
			isSelected
		}),
		"data-index": dataIndex,
		"data-puck-layer-tree-id": node.itemId,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassNameLayer("inner"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("button", {
				type: "button",
				className: getClassNameLayer("clickable"),
				onClick: () => {
					if (isSelected) {
						setItemSelector(null);
						return;
					}
					setItemSelector({
						index: node.index,
						zone: node.zoneCompound
					});
					zoneStore.getState().scrollToComponent(node.itemId);
				},
				onMouseEnter: (e) => {
					e.stopPropagation();
					zoneStore.setState({ hoveringComponent: node.itemId });
				},
				onMouseLeave: (e) => {
					e.stopPropagation();
					zoneStore.setState({ hoveringComponent: null });
				},
				children: [containsZone && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: getClassNameLayer("chevron"),
					title: isSelected ? "Collapse" : "Expand",
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown$1, { size: "12" })
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: getClassNameLayer("title"),
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassNameLayer("icon"),
						children: node.componentType === "Text" || node.componentType === "Heading" ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Type, { size: "16" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayoutGrid, { size: "16" })
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassNameLayer("name"),
						children: node.label
					})]
				})]
			})
		}), containsZone && shouldRenderChildren && node.childZones.map((childZone) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassNameLayer("zones"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayerTreeZone, {
				depth: depth + 1,
				selectedId,
				selectedPathIds,
				tree: childZone
			})
		}, childZone.zoneCompound))]
	});
});
var LayerTreeZone = ({ depth, selectedId, selectedPathIds, tree }) => {
	const shouldVirtualize = depth === 0 && tree.items.length >= MIN_VIRTUALIZED_LAYER_COUNT;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [tree.label && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName20("zoneTitle"),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName20("zoneIcon"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layers, { size: "16" })
		}), tree.label]
	}), shouldVirtualize ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(VirtualizedLayerTreeItems, {
		depth,
		selectedId,
		selectedPathIds,
		tree
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(StaticLayerTreeItems, {
		depth,
		selectedId,
		selectedPathIds,
		tree
	})] });
};
var StaticLayerTreeItems = ({ depth, selectedId, selectedPathIds, tree }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("ul", {
		className: getClassName20(),
		children: [tree.items.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName20("helper"),
			children: "No items"
		}), tree.items.map((node) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layer, {
			childIsSelected: selectedPathIds.has(node.itemId),
			depth,
			isSelected: selectedId === node.itemId,
			node,
			selectedId,
			selectedPathIds
		}, node.itemId))]
	});
};
var VirtualizedLayerTreeItems = ({ depth, selectedId, selectedPathIds, tree }) => {
	const listRef = (0, import_react.useRef)(null);
	const virtualizer = useVirtualizer({
		count: tree.items.length,
		estimateSize: (index) => getEstimatedRowHeight(tree.items[index].itemId),
		getItemKey: (index) => tree.items[index].itemId,
		getScrollElement: () => getScrollParent(listRef.current),
		overscan: LAYER_TREE_VIRTUALIZATION_OVERSCAN,
		measureElement: (element) => {
			const height = Math.ceil(element.getBoundingClientRect().height);
			const itemId = element.dataset.puckLayerTreeId;
			if (itemId) cacheMeasuredRowHeight(itemId, height);
			return height || DEFAULT_LAYER_ROW_HEIGHT;
		}
	});
	const virtualItems = virtualizer.getVirtualItems();
	const totalSize = virtualizer.getTotalSize();
	const renderedItems = [];
	let previousEnd = 0;
	let previousIndex = -1;
	virtualItems.forEach((virtualItem) => {
		const node = tree.items[virtualItem.index];
		const gapSize = Math.max(virtualItem.start - previousEnd, 0);
		if (gapSize > 0) renderedItems.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", {
			"aria-hidden": "true",
			style: { height: `${gapSize}px` }
		}, `gap:${tree.zoneCompound}:${previousIndex}:${virtualItem.index}`));
		renderedItems.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layer, {
			childIsSelected: selectedPathIds.has(node.itemId),
			dataIndex: virtualItem.index,
			depth,
			isSelected: selectedId === node.itemId,
			node,
			ref: virtualizer.measureElement,
			selectedId,
			selectedPathIds
		}, node.itemId));
		previousEnd = virtualItem.end;
		previousIndex = virtualItem.index;
	});
	const trailingGap = Math.max(totalSize - previousEnd, 0);
	if (trailingGap > 0) renderedItems.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", {
		"aria-hidden": "true",
		style: { height: `${trailingGap}px` }
	}, `gap:${tree.zoneCompound}:${previousIndex}:end`));
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("ul", {
		className: getClassName20(),
		ref: listRef,
		children: [tree.items.length === 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName20("helper"),
			children: "No items"
		}), renderedItems]
	});
};
var LayerTree = ({ selectedId, selectedPathIds, trees }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: trees.map((tree) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayerTreeZone, {
		depth: 0,
		selectedId,
		selectedPathIds,
		tree
	}, tree.zoneCompound)) });
};
init_react_import();
var findZonesForArea = (state, area) => {
	return Object.keys(state.indexes.zones).filter((zone) => zone.split(":")[0] === area);
};
var Outline = () => {
	const outlineOverride = useAppStore((s) => s.overrides.outline);
	const config = useAppStore((s) => s.config);
	const nodes = useAppStore((s) => s.state.indexes.nodes);
	const zones = useAppStore((s) => s.state.indexes.zones);
	const selectedId = useAppStore((s) => {
		var _a;
		return ((_a = s.selectedItem) == null ? void 0 : _a.props.id) || null;
	});
	const rootZones = useAppStore(useShallow((s) => findZonesForArea(s.state, "root")));
	const selectedPathIds = (0, import_react.useMemo)(() => {
		var _a;
		const selectedPath = selectedId ? (_a = nodes[selectedId]) == null ? void 0 : _a.path : null;
		return new Set((selectedPath == null ? void 0 : selectedPath.map((candidate) => candidate.split(":")[0]).filter(Boolean)) || []);
	}, [nodes, selectedId]);
	const trees = (0, import_react.useMemo)(() => rootZones.map((zoneCompound) => buildLayerTree({
		config,
		label: rootZones.length === 1 ? "" : zoneCompound.split(":")[1],
		nodes,
		zoneCompound,
		zones
	})), [
		config,
		nodes,
		rootZones,
		zones
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)((0, import_react.useMemo)(() => outlineOverride || "div", [outlineOverride]), { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LayerTree, {
		selectedId,
		selectedPathIds,
		trees
	}) });
};
init_react_import();
var getClassName21 = get_class_name_factory_default("OutlinePlugin", { "OutlinePlugin": "_OutlinePlugin_q92j6_1" });
var outlinePlugin = () => ({
	name: "outline",
	label: "Outline",
	render: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName21(),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Outline, {})
	}),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layers, {})
});
init_react_import();
init_react_import();
init_react_import();
var styles_module_default16 = {
	"Breadcrumbs": "_Breadcrumbs_1c9yh_1",
	"Breadcrumbs-breadcrumbLabel": "_Breadcrumbs-breadcrumbLabel_1c9yh_7",
	"Breadcrumbs-breadcrumb": "_Breadcrumbs-breadcrumb_1c9yh_7"
};
init_react_import();
var useBreadcrumbs = (renderCount) => {
	const selectedId = useAppStore((s) => {
		var _a;
		return (_a = s.selectedItem) == null ? void 0 : _a.props.id;
	});
	const config = useAppStore((s) => s.config);
	const path = useAppStore((s) => {
		var _a;
		return (_a = s.state.indexes.nodes[selectedId]) == null ? void 0 : _a.path;
	});
	const appStore = useAppStoreApi();
	return (0, import_react.useMemo)(() => {
		const breadcrumbs = (path == null ? void 0 : path.map((zoneCompound) => {
			var _a, _b, _c, _d;
			const [componentId] = zoneCompound.split(":");
			if (componentId === "root") return {
				label: ((_a = config == null ? void 0 : config.root) == null ? void 0 : _a.label) || "Page",
				selector: null
			};
			const node = appStore.getState().state.indexes.nodes[componentId];
			const parentId = node.path[node.path.length - 1];
			const index = (((_b = appStore.getState().state.indexes.zones[parentId]) == null ? void 0 : _b.contentIds) || []).indexOf(componentId);
			return {
				label: node ? (_d = (_c = config.components[node.data.type]) == null ? void 0 : _c.label) != null ? _d : node.data.type : "Component",
				selector: node ? {
					index,
					zone: node.path[node.path.length - 1]
				} : null
			};
		})) || [];
		if (renderCount) return breadcrumbs.slice(breadcrumbs.length - renderCount);
		return breadcrumbs;
	}, [path, renderCount]);
};
var getClassName22 = get_class_name_factory_default("Breadcrumbs", styles_module_default16);
var Breadcrumbs = ({ children, numParents = 1 }) => {
	const setUi = useAppStore((s) => s.setUi);
	const breadcrumbs = useBreadcrumbs(numParents);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName22(),
		children: [breadcrumbs.map((breadcrumb, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: getClassName22("breadcrumb"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				type: "button",
				className: getClassName22("breadcrumbLabel"),
				onClick: () => setUi({ itemSelector: breadcrumb.selector }),
				children: breadcrumb.label
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight, { size: 16 })]
		}, i)), children]
	});
};
init_react_import();
init_react_import();
var getClassName23 = get_class_name_factory_default("PuckFields", {
	"PuckFields": "_PuckFields_10bh7_1",
	"PuckFields--isLoading": "_PuckFields--isLoading_10bh7_6",
	"PuckFields-loadingOverlay": "_PuckFields-loadingOverlay_10bh7_10",
	"PuckFields-loadingOverlayInner": "_PuckFields-loadingOverlayInner_10bh7_25",
	"PuckFields-field": "_PuckFields-field_10bh7_32",
	"PuckFields--wrapFields": "_PuckFields--wrapFields_10bh7_36"
});
var DefaultFields = ({ children }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children });
};
var createOnChange = (fieldName, appStore) => (value, updatedUi) => __async(null, null, function* () {
	const { dispatch, state, selectedItem, resolveComponentData } = appStore.getState();
	const { data, ui } = state;
	const { itemSelector } = ui;
	const rootProps = data.root.props || data.root;
	const newProps = __spreadProps$6(__spreadValues$7({}, selectedItem ? selectedItem.props : rootProps), { [fieldName]: value });
	if (selectedItem && itemSelector) {
		const resolved = yield resolveComponentData(__spreadProps$6(__spreadValues$7({}, selectedItem), { props: newProps }), "replace");
		const latestSelector = getSelectorForId(appStore.getState().state, selectedItem.props.id);
		if (!latestSelector) return;
		dispatch({
			type: "replace",
			destinationIndex: latestSelector.index,
			destinationZone: latestSelector.zone || rootDroppableId,
			data: resolved.node,
			ui: updatedUi
		});
		return;
	}
	if (data.root.props) {
		dispatch({
			type: "replaceRoot",
			root: (yield resolveComponentData(__spreadProps$6(__spreadValues$7({}, data.root), { props: newProps }), "replace")).node,
			ui: __spreadValues$7(__spreadValues$7({}, ui), updatedUi),
			recordHistory: true
		});
		return;
	}
	dispatch({
		type: "setData",
		data: { root: newProps }
	});
});
var FieldsChildInner = ({ fieldName }) => {
	const field = useAppStore((s) => s.fields.fields[fieldName]);
	const isReadOnly = useAppStore((s) => ((s.selectedItem ? s.selectedItem.readOnly : s.state.data.root.readOnly) || {})[fieldName]);
	const id = useAppStore((s) => {
		if (!field) return null;
		return s.selectedItem ? `${s.selectedItem.props.id}_${field.type}_${fieldName}` : `root_${field.type}_${fieldName}`;
	});
	const permissions = useAppStore(useShallow((s) => {
		const { selectedItem, permissions: permissions2 } = s;
		return selectedItem ? permissions2.getPermissions({ item: selectedItem }) : permissions2.getPermissions({ root: true });
	}));
	const appStore = useAppStoreApi();
	const onChange = (0, import_react.useCallback)(createOnChange(fieldName, appStore), [fieldName]);
	const { visible = true } = field != null ? field : {};
	const fieldStore = (0, import_react.useContext)(fieldContextStore.ctx);
	(0, import_react.useEffect)(() => {
		return appStore.subscribe((s) => {
			var _a;
			return (_a = s.getCurrentData().props) == null ? void 0 : _a[fieldName];
		}, (value) => {
			fieldStore.setState({ [fieldName]: value });
		});
	}, [appStore, fieldStore]);
	if (!field || !id || !visible) return null;
	if (field.type === "slot") return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName23("field"),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AutoFieldPrivate, {
			field,
			name: fieldName,
			id,
			readOnly: !permissions.edit || isReadOnly,
			onChange
		})
	}, id);
};
var FieldsChild = ({ fieldName }) => {
	const appStore = useAppStoreApi();
	const initialValue = (0, import_react.useMemo)(() => {
		var _a;
		const value = (_a = appStore.getState().getCurrentData().props) == null ? void 0 : _a[fieldName];
		return { [fieldName]: value };
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(fieldContextStore.Provider, {
		value: initialValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldsChildInner, { fieldName })
	});
};
var FieldsChildMemo = (0, import_react.memo)(FieldsChild);
var FieldsInternal = ({ wrapFields = true }) => {
	const overrides = useAppStore((s) => s.overrides);
	const componentResolving = useAppStore((s) => {
		var _a, _b;
		const loadingCount = s.selectedItem ? (_a = s.componentState[s.selectedItem.props.id]) == null ? void 0 : _a.loadingCount : (_b = s.componentState["root"]) == null ? void 0 : _b.loadingCount;
		return (loadingCount != null ? loadingCount : 0) > 0;
	});
	const itemSelector = useAppStore(useShallow((s) => s.state.ui.itemSelector));
	const id = useAppStore((s) => {
		var _a;
		return (_a = s.selectedItem) == null ? void 0 : _a.props.id;
	});
	useRegisterFieldsSlice(useAppStoreApi(), id);
	const fieldsLoading = useAppStore((s) => s.fields.loading);
	const fieldNames = useAppStore(useShallow((s) => {
		if (s.fields.id === id) return Object.keys(s.fields.fields);
		return [];
	}));
	const isLoading = fieldsLoading || componentResolving;
	const Wrapper = (0, import_react.useMemo)(() => overrides.fields || DefaultFields, [overrides]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("form", {
		className: getClassName23({ wrapFields }),
		onSubmit: (e) => {
			e.preventDefault();
		},
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Wrapper, {
			isLoading,
			itemSelector,
			children: fieldNames.map((fieldName) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldsChildMemo, { fieldName }, fieldName))
		}), isLoading && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName23("loadingOverlay"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassName23("loadingOverlayInner"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Loader, { size: 16 })
			})
		})]
	});
};
var Fields = (0, import_react.memo)(FieldsInternal);
init_react_import();
var getClassName24 = get_class_name_factory_default("FieldsPlugin", {
	"FieldsPlugin": "_FieldsPlugin_nd930_1",
	"FieldsPlugin-header": "_FieldsPlugin-header_nd930_7"
});
var CurrentTitle = () => {
	return useAppStore((s) => {
		var _a, _b;
		const selectedItem = s.selectedItem;
		return selectedItem ? (_b = (_a = s.config.components[selectedItem.type]) == null ? void 0 : _a.label) != null ? _b : selectedItem.type : "Page";
	});
};
var fieldsPlugin = ({ desktopSideBar = "right" } = {}) => ({
	name: "fields",
	label: "Fields",
	render: () => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName24(),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName24("header"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Breadcrumbs, {
				numParents: 2,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CurrentTitle, {})
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Fields, {})]
	}),
	icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RectangleEllipsis, {}),
	mobileOnly: desktopSideBar === "right"
});
init_react_import();
init_react_import();
init_react_import();
var styleSelector = "style, link[rel=\"stylesheet\"]";
var collectStyles = (doc) => {
	const collected = [];
	doc.querySelectorAll(styleSelector).forEach((style) => {
		if (style.tagName === "STYLE") {
			if (!!style.innerHTML.trim()) collected.push(style);
		} else collected.push(style);
	});
	return collected;
};
var getStyleSheet = (el) => {
	return Array.from(document.styleSheets).find((ss) => {
		return ss.ownerNode.href === el.href;
	});
};
var getStyles = (styleSheet) => {
	if (styleSheet) try {
		return Array.from(styleSheet.cssRules).map((rule) => rule.cssText).join("");
	} catch (e) {
		console.warn("Access to stylesheet %s is denied. Ignoring…", styleSheet.href);
	}
	return "";
};
var syncAttributes = (sourceElement, targetElement) => {
	const attributes = sourceElement.attributes;
	if ((attributes == null ? void 0 : attributes.length) > 0) Array.from(attributes).forEach((attribute) => {
		targetElement.setAttribute(attribute.name, attribute.value);
	});
};
var defer = (fn) => setTimeout(fn, 0);
var CopyHostStyles = ({ children, debug = false, onStylesLoaded = () => null }) => {
	const { document: doc, window: win } = useFrame();
	(0, import_react.useEffect)(() => {
		if (!win || !doc) return () => {};
		let elements = [];
		const hashes = {};
		const lookupEl = (el) => elements.findIndex((elementMap) => elementMap.original === el);
		const mirrorEl = (el, inlineStyles = false) => __async(null, null, function* () {
			let mirror;
			if (el.nodeName === "LINK" && inlineStyles) {
				mirror = document.createElement("style");
				mirror.type = "text/css";
				let styleSheet = getStyleSheet(el);
				if (!styleSheet) {
					yield new Promise((resolve) => {
						const fn = () => {
							resolve();
							el.removeEventListener("load", fn);
						};
						el.addEventListener("load", fn);
					});
					styleSheet = getStyleSheet(el);
				}
				const styles2 = getStyles(styleSheet);
				if (!styles2) {
					if (debug) console.warn(`Tried to load styles for link element, but couldn't find them. Skipping...`);
					return;
				}
				mirror.innerHTML = styles2;
				mirror.setAttribute("data-href", el.getAttribute("href"));
			} else mirror = el.cloneNode(true);
			return mirror;
		});
		const addEl = (el) => __async(null, null, function* () {
			const index = lookupEl(el);
			if (index > -1) {
				if (debug) console.log(`Tried to add an element that was already mirrored. Updating instead...`);
				elements[index].mirror.innerText = el.innerText;
				return;
			}
			const mirror = yield mirrorEl(el);
			if (!mirror) return;
			const elHash = (0, import_object_hash.default)(mirror.outerHTML);
			if (hashes[elHash]) {
				if (debug) console.log(`iframe already contains element that is being mirrored. Skipping...`);
				return;
			}
			hashes[elHash] = true;
			doc.head.append(mirror);
			elements.push({
				original: el,
				mirror
			});
			if (debug) console.log(`Added style node ${el.outerHTML}`);
		});
		const removeEl = (el) => {
			var _a, _b;
			const index = lookupEl(el);
			if (index === -1) {
				if (debug) console.log(`Tried to remove an element that did not exist. Skipping...`);
				return;
			}
			const elHash = (0, import_object_hash.default)(el.outerHTML);
			(_b = (_a = elements[index]) == null ? void 0 : _a.mirror) == null || _b.remove();
			delete hashes[elHash];
			if (debug) console.log(`Removed style node ${el.outerHTML}`);
		};
		const observer = new MutationObserver((mutations) => {
			mutations.forEach((mutation) => {
				if (mutation.type === "childList") {
					mutation.addedNodes.forEach((node) => {
						if (node.nodeType === Node.TEXT_NODE || node.nodeType === Node.ELEMENT_NODE) {
							const el = node.nodeType === Node.TEXT_NODE ? node.parentElement : node;
							if (el && el.matches(styleSelector)) defer(() => addEl(el));
						}
					});
					mutation.removedNodes.forEach((node) => {
						if (node.nodeType === Node.TEXT_NODE || node.nodeType === Node.ELEMENT_NODE) {
							const el = node.nodeType === Node.TEXT_NODE ? node.parentElement : node;
							if (el && el.matches(styleSelector)) defer(() => removeEl(el));
						}
					});
				}
			});
		});
		const parentDocument = win.parent.document;
		const collectedStyles = collectStyles(parentDocument);
		const hrefs = [];
		let stylesLoaded = 0;
		const parentHtml = parentDocument.getElementsByTagName("html")[0];
		syncAttributes(parentHtml, doc.documentElement);
		const parentBody = parentDocument.getElementsByTagName("body")[0];
		syncAttributes(parentBody, doc.body);
		Promise.all(collectedStyles.map((styleNode, i) => __async(null, null, function* () {
			if (styleNode.nodeName === "LINK") {
				const linkHref = styleNode.href;
				if (hrefs.indexOf(linkHref) > -1) return;
				hrefs.push(linkHref);
			}
			const mirror = yield mirrorEl(styleNode);
			if (!mirror) return;
			elements.push({
				original: styleNode,
				mirror
			});
			return mirror;
		}))).then((mirrorStyles) => {
			const filtered = mirrorStyles.filter((el) => typeof el !== "undefined");
			filtered.forEach((mirror) => {
				mirror.onload = () => {
					stylesLoaded = stylesLoaded + 1;
					if (stylesLoaded >= filtered.length) onStylesLoaded();
				};
				mirror.onerror = () => {
					console.warn(`AutoFrame couldn't load a stylesheet`);
					stylesLoaded = stylesLoaded + 1;
					if (stylesLoaded >= filtered.length) onStylesLoaded();
				};
			});
			doc.head.innerHTML = "";
			doc.head.append(...filtered);
			filtered.forEach((mirror) => {
				if (mirror.nodeName === "STYLE") stylesLoaded = stylesLoaded + 1;
			});
			if (stylesLoaded >= filtered.length) onStylesLoaded();
			observer.observe(parentDocument.head, {
				childList: true,
				subtree: true
			});
			filtered.forEach((el) => {
				const elHash = (0, import_object_hash.default)(el.outerHTML);
				hashes[elHash] = true;
			});
		});
		return () => {
			observer.disconnect();
		};
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children });
};
var autoFrameContext = (0, import_react.createContext)({});
var useFrame = () => (0, import_react.useContext)(autoFrameContext);
function AutoFrame(_a) {
	var _b = _a, { children, className, debug, id, onReady = () => {}, onNotReady = () => {}, frameRef } = _b, props = __objRest$4(_b, [
		"children",
		"className",
		"debug",
		"id",
		"onReady",
		"onNotReady",
		"frameRef"
	]);
	const [loaded, setLoaded] = (0, import_react.useState)(false);
	const [ctx, setCtx] = (0, import_react.useState)({});
	const [mountTarget, setMountTarget] = (0, import_react.useState)();
	const [stylesLoaded, setStylesLoaded] = (0, import_react.useState)(false);
	(0, import_react.useEffect)(() => {
		var _a2;
		if (frameRef.current) {
			const doc = frameRef.current.contentDocument;
			const win = frameRef.current.contentWindow;
			setCtx({
				document: doc || void 0,
				window: win || void 0
			});
			setMountTarget((_a2 = frameRef.current.contentDocument) == null ? void 0 : _a2.getElementById("frame-root"));
			if (doc && win && stylesLoaded) onReady();
			else onNotReady();
		}
	}, [
		frameRef,
		loaded,
		stylesLoaded
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("iframe", __spreadProps$6(__spreadValues$7({}, props), {
		className,
		id,
		srcDoc: "<!DOCTYPE html><html><head></head><body><div id=\"frame-root\" data-puck-entry></div></body></html>",
		ref: frameRef,
		onLoad: () => {
			setLoaded(true);
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(autoFrameContext.Provider, {
			value: ctx,
			children: loaded && mountTarget && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CopyHostStyles, {
				debug,
				onStylesLoaded: () => setStylesLoaded(true),
				children: (0, import_react_dom.createPortal)(children, mountTarget)
			})
		})
	}));
}
AutoFrame.displayName = "AutoFrame";
var AutoFrame_default = AutoFrame;
init_react_import();
var getClassName25 = get_class_name_factory_default("PuckPreview", {
	"PuckPreview": "_PuckPreview_z2rgu_1",
	"PuckPreview-frame": "_PuckPreview-frame_z2rgu_6"
});
var useBubbleIframeEvents = (ref) => {
	const status = useAppStore((s) => s.status);
	(0, import_react.useEffect)(() => {
		if (ref.current && status === "READY") {
			const iframe = ref.current;
			const handlePointerMove = (event) => {
				const evt = new BubbledPointerEvent("pointermove", __spreadProps$6(__spreadValues$7({}, event), {
					bubbles: true,
					cancelable: false,
					clientX: event.clientX,
					clientY: event.clientY,
					originalTarget: event.target
				}));
				iframe.dispatchEvent(evt);
			};
			const register = () => {
				var _a;
				unregister();
				(_a = iframe.contentDocument) == null || _a.addEventListener("pointermove", handlePointerMove, { capture: true });
			};
			const unregister = () => {
				var _a;
				(_a = iframe.contentDocument) == null || _a.removeEventListener("pointermove", handlePointerMove);
			};
			register();
			return () => {
				unregister();
			};
		}
	}, [status]);
};
var Preview2 = ({ id = "puck-preview" }) => {
	const dispatch = useAppStore((s) => s.dispatch);
	const root = useAppStore((s) => s.state.data.root);
	const config = useAppStore((s) => s.config);
	const setStatus = useAppStore((s) => s.setStatus);
	const iframe = useAppStore((s) => s.iframe);
	const overrides = useAppStore((s) => s.overrides);
	const metadata = useAppStore((s) => s.metadata);
	const renderData = useAppStore((s) => s.state.ui.previewMode === "edit" ? null : s.state.data);
	const Page = (0, import_react.useCallback)((pageProps) => {
		var _a, _b, _c, _d;
		const propsWithSlots = useSlots(config, {
			type: "root",
			props: pageProps
		}, DropZoneEditPure);
		const richtextProps = useRichtextProps((_b = (_a = config.root) == null ? void 0 : _a.fields) != null ? _b : {}, pageProps);
		return ((_c = config.root) == null ? void 0 : _c.render) ? (_d = config.root) == null ? void 0 : _d.render(__spreadValues$7(__spreadValues$7({ id: "puck-root" }, propsWithSlots), richtextProps)) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: propsWithSlots.children });
	}, [config]);
	const Frame = (0, import_react.useMemo)(() => overrides.iframe, [overrides]);
	const rootProps = root.props || root;
	const ref = (0, import_react.useRef)(null);
	useBubbleIframeEvents(ref);
	const inner = !renderData ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Page, __spreadProps$6(__spreadValues$7({}, rootProps), {
		puck: {
			renderDropZone: DropZonePure,
			isEditing: true,
			dragRef: null,
			metadata
		},
		editMode: true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DropZonePure, { zone: rootDroppableId })
	})) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Render, {
		data: renderData,
		config,
		metadata
	});
	(0, import_react.useEffect)(() => {
		if (!iframe.enabled) setStatus("READY");
	}, [iframe.enabled]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName25(),
		id,
		"data-puck-preview": true,
		onClick: (e) => {
			const el = e.target;
			if (!el.hasAttribute("data-puck-component") && !el.hasAttribute("data-puck-dropzone")) dispatch({
				type: "setUi",
				ui: { itemSelector: null }
			});
		},
		children: iframe.enabled ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AutoFrame_default, {
			id: "preview-frame",
			className: getClassName25("frame"),
			"data-rfd-iframe": true,
			onReady: () => {
				setStatus("READY");
			},
			onNotReady: () => {
				setStatus("MOUNTED");
			},
			frameRef: ref,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(autoFrameContext.Consumer, { children: ({ document: document2 }) => {
				if (Frame) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Frame, {
					document: document2,
					children: inner
				});
				return inner;
			} })
		}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			id: "preview-frame",
			className: getClassName25("frame"),
			ref,
			"data-puck-entry": true,
			children: inner
		})
	});
};
init_react_import();
init_react_import();
var loadOverrides = ({ overrides, plugins }) => {
	const collected = __spreadValues$7({}, overrides);
	plugins?.forEach((plugin) => {
		if (!plugin.overrides) return;
		Object.keys(plugin.overrides).forEach((_overridesType) => {
			var _a;
			const overridesType = _overridesType;
			if (!((_a = plugin.overrides) == null ? void 0 : _a[overridesType])) return;
			if (overridesType === "fieldTypes") {
				const fieldTypes = plugin.overrides.fieldTypes;
				Object.keys(fieldTypes).forEach((fieldType) => {
					collected.fieldTypes = collected.fieldTypes || {};
					const childNode2 = collected.fieldTypes[fieldType];
					const Comp2 = (props) => fieldTypes[fieldType](__spreadProps$6(__spreadValues$7({}, props), { children: childNode2 ? childNode2(props) : props.children }));
					collected.fieldTypes[fieldType] = Comp2;
				});
				return;
			}
			const childNode = collected[overridesType];
			const Comp = (props) => plugin.overrides[overridesType](__spreadProps$6(__spreadValues$7({}, props), { children: childNode ? childNode(props) : props.children }));
			collected[overridesType] = Comp;
		});
	});
	return collected;
};
var useLoadedOverrides = ({ overrides, plugins }) => {
	return (0, import_react.useMemo)(() => {
		return loadOverrides({
			overrides,
			plugins
		});
	}, [plugins, overrides]);
};
init_react_import();
init_react_import();
var styles_module_default20 = {
	"Puck": "_Puck_1dd16_19",
	"Puck-portal": "_Puck-portal_1dd16_31",
	"PuckLayout": "_PuckLayout_1dd16_36",
	"PuckLayout-inner": "_PuckLayout-inner_1dd16_40",
	"Puck--hidePlugins": "_Puck--hidePlugins_1dd16_72",
	"PuckLayout--mounted": "_PuckLayout--mounted_1dd16_77",
	"PuckLayout--mobilePanelHeightToggle": "_PuckLayout--mobilePanelHeightToggle_1dd16_81",
	"PuckLayout--leftSideBarVisible": "_PuckLayout--leftSideBarVisible_1dd16_81",
	"PuckLayout--isExpanded": "_PuckLayout--isExpanded_1dd16_87",
	"PuckLayout--mobilePanelHeightMinContent": "_PuckLayout--mobilePanelHeightMinContent_1dd16_105",
	"PuckLayout--rightSideBarVisible": "_PuckLayout--rightSideBarVisible_1dd16_132",
	"PuckLayout-mounted": "_PuckLayout-mounted_1dd16_151",
	"PuckLayout-nav": "_PuckLayout-nav_1dd16_192",
	"PuckLayout-header": "_PuckLayout-header_1dd16_208",
	"PuckPluginTab": "_PuckPluginTab_1dd16_222",
	"PuckPluginTab--visible": "_PuckPluginTab--visible_1dd16_228",
	"PuckPluginTab-body": "_PuckPluginTab-body_1dd16_233"
};
init_react_import();
var styles = ``;
var useInjectStyleSheet = (initialStyles, iframeEnabled) => {
	const [el, setEl] = (0, import_react.useState)();
	(0, import_react.useEffect)(() => {
		setEl(document.createElement("style"));
	}, []);
	(0, import_react.useEffect)(() => {
		var _a;
		if (!el || typeof window === "undefined") return;
		el.innerHTML = initialStyles;
		if (iframeEnabled) {
			const frame = getFrame();
			(_a = frame == null ? void 0 : frame.head) == null || _a.appendChild(el);
		}
		document.head.appendChild(el);
	}, [iframeEnabled, el]);
	return el;
};
var useInjectGlobalCss = (iframeEnabled) => {
	return useInjectStyleSheet(styles, iframeEnabled);
};
init_react_import();
var DefaultOverride = ({ children }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children });
init_react_import();
var usePreviewModeHotkeys = () => {
	const appStore = useAppStoreApi();
	const toggleInteractive = (0, import_react.useCallback)(() => {
		const dispatch = appStore.getState().dispatch;
		dispatch({
			type: "setUi",
			ui: (ui) => ({ previewMode: ui.previewMode === "edit" ? "interactive" : "edit" })
		});
	}, [appStore]);
	useHotkey({
		meta: true,
		i: true
	}, toggleInteractive);
	useHotkey({
		ctrl: true,
		i: true
	}, toggleInteractive);
};
init_react_import();
init_react_import();
init_react_import();
var getClassName26 = get_class_name_factory_default("MenuBar", {
	"MenuBar": "_MenuBar_8pf8c_1",
	"MenuBar--menuOpen": "_MenuBar--menuOpen_8pf8c_14",
	"MenuBar-inner": "_MenuBar-inner_8pf8c_29",
	"MenuBar-history": "_MenuBar-history_8pf8c_45"
});
function MenuBar({ menuOpen = false, renderHeaderActions, setMenuOpen }) {
	const back = useAppStore((s) => s.history.back);
	const forward = useAppStore((s) => s.history.forward);
	const hasFuture = useAppStore((s) => s.history.hasFuture());
	const hasPast = useAppStore((s) => s.history.hasPast());
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getClassName26({ menuOpen }),
		onClick: (event) => {
			var _a;
			const element = event.target;
			if (window.matchMedia("(min-width: 638px)").matches) return;
			if (element.tagName === "A" && ((_a = element.getAttribute("href")) == null ? void 0 : _a.startsWith("#"))) setMenuOpen(false);
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: getClassName26("inner"),
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: getClassName26("history"),
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
					type: "button",
					title: "undo",
					disabled: !hasPast,
					onClick: back,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Undo2, { size: 21 })
				}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
					type: "button",
					title: "redo",
					disabled: !hasFuture,
					onClick: forward,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Redo2, { size: 21 })
				})]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: renderHeaderActions && renderHeaderActions() })]
		})
	});
}
init_react_import();
var getClassName27 = get_class_name_factory_default("PuckHeader", {
	"PuckHeader": "_PuckHeader_63pti_1",
	"PuckHeader--hidePlugins": "_PuckHeader--hidePlugins_63pti_15",
	"PuckHeader-inner": "_PuckHeader-inner_63pti_20",
	"PuckHeader-toggle": "_PuckHeader-toggle_63pti_40",
	"PuckHeader--rightSideBarVisible": "_PuckHeader--rightSideBarVisible_63pti_47",
	"PuckHeader-rightSideBarToggle": "_PuckHeader-rightSideBarToggle_63pti_47",
	"PuckHeader--leftSideBarVisible": "_PuckHeader--leftSideBarVisible_63pti_48",
	"PuckHeader-leftSideBarToggle": "_PuckHeader-leftSideBarToggle_63pti_48",
	"PuckHeader-title": "_PuckHeader-title_63pti_64",
	"PuckHeader-path": "_PuckHeader-path_63pti_68",
	"PuckHeader-tools": "_PuckHeader-tools_63pti_75",
	"PuckHeader-menuButton": "_PuckHeader-menuButton_63pti_81",
	"PuckHeader--menuOpen": "_PuckHeader--menuOpen_63pti_86"
});
var HeaderInner = ({ hidePlugins }) => {
	const { onPublish, renderHeader, renderHeaderActions, headerTitle, headerPath, iframe: _iframe } = usePropsContext();
	const dispatch = useAppStore((s) => s.dispatch);
	const appStore = useAppStoreApi();
	const defaultHeaderRender = (0, import_react.useMemo)(() => {
		if (renderHeader) {
			console.warn("`renderHeader` is deprecated. Please use `overrides.header` and the `usePuck` hook instead");
			const RenderHeader = (_a) => {
				var _b = _a, { actions } = _b, props = __objRest$4(_b, ["actions"]);
				const Comp = renderHeader;
				const appState = useAppStore((s) => s.state);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Comp, __spreadProps$6(__spreadValues$7({}, props), {
					dispatch,
					state: appState,
					children: actions
				}));
			};
			return RenderHeader;
		}
		return DefaultOverride;
	}, [renderHeader]);
	const defaultHeaderActionsRender = (0, import_react.useMemo)(() => {
		if (renderHeaderActions) {
			console.warn("`renderHeaderActions` is deprecated. Please use `overrides.headerActions` and the `usePuck` hook instead.");
			const RenderHeader = (props) => {
				const Comp = renderHeaderActions;
				const appState = useAppStore((s) => s.state);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Comp, __spreadProps$6(__spreadValues$7({}, props), {
					dispatch,
					state: appState
				}));
			};
			return RenderHeader;
		}
		return DefaultOverride;
	}, [renderHeaderActions]);
	const CustomHeader = useAppStore((s) => s.overrides.header || defaultHeaderRender);
	const CustomHeaderActions = useAppStore((s) => s.overrides.headerActions || defaultHeaderActionsRender);
	const [menuOpen, setMenuOpen] = (0, import_react.useState)(false);
	const rootTitle = useAppStore((s) => {
		var _a, _b;
		return (_b = ((_a = s.state.indexes.nodes["root"]) == null ? void 0 : _a.data).props.title) != null ? _b : "";
	});
	const leftSideBarVisible = useAppStore((s) => s.state.ui.leftSideBarVisible);
	const rightSideBarVisible = useAppStore((s) => s.state.ui.rightSideBarVisible);
	const toggleSidebars = (0, import_react.useCallback)((sidebar) => {
		const widerViewport = window.matchMedia("(min-width: 638px)").matches;
		const sideBarVisible = sidebar === "left" ? leftSideBarVisible : rightSideBarVisible;
		const oppositeSideBar = sidebar === "left" ? "rightSideBarVisible" : "leftSideBarVisible";
		dispatch({
			type: "setUi",
			ui: __spreadValues$7({ [`${sidebar}SideBarVisible`]: !sideBarVisible }, !widerViewport ? { [oppositeSideBar]: false } : {})
		});
	}, [
		dispatch,
		leftSideBarVisible,
		rightSideBarVisible
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomHeader, {
		actions: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomHeaderActions, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
			onClick: () => {
				const data = appStore.getState().state.data;
				onPublish && onPublish(data);
			},
			icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Globe, { size: "14px" }),
			children: "Publish"
		}) }) }),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("header", {
			className: getClassName27({
				leftSideBarVisible,
				rightSideBarVisible,
				hidePlugins
			}),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: getClassName27("inner"),
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: getClassName27("toggle"),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: getClassName27("leftSideBarToggle"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
								type: "button",
								onClick: () => {
									toggleSidebars("left");
								},
								title: "Toggle left sidebar",
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PanelLeft, { focusable: "false" })
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: getClassName27("rightSideBarToggle"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
								type: "button",
								onClick: () => {
									toggleSidebars("right");
								},
								title: "Toggle right sidebar",
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PanelRight, { focusable: "false" })
							})
						})]
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassName27("title"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(Heading, {
							rank: "2",
							size: "xs",
							children: [headerTitle || rootTitle || "Page", headerPath && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [" ", /* @__PURE__ */ (0, import_jsx_runtime.jsx)("code", {
								className: getClassName27("path"),
								children: headerPath
							})] })]
						})
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: getClassName27("tools"),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: getClassName27("menuButton"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
								type: "button",
								onClick: () => {
									return setMenuOpen(!menuOpen);
								},
								title: "Toggle menu bar",
								children: menuOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronUp, { focusable: "false" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown$1, { focusable: "false" })
							})
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuBar, {
							dispatch,
							onPublish,
							menuOpen,
							renderHeaderActions: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomHeaderActions, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button$1, {
								onClick: () => {
									const data = appStore.getState().state.data;
									onPublish && onPublish(data);
								},
								icon: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Globe, { size: "14px" }),
								children: "Publish"
							}) }),
							setMenuOpen
						})]
					})
				]
			})
		})
	});
};
var Header = (0, import_react.memo)(HeaderInner);
init_react_import();
init_react_import();
var getClassName28 = get_class_name_factory_default("SidebarSection", {
	"SidebarSection": "_SidebarSection_5otpt_1",
	"SidebarSection-title": "_SidebarSection-title_5otpt_12",
	"SidebarSection--noBorderTop": "_SidebarSection--noBorderTop_5otpt_20",
	"SidebarSection-content": "_SidebarSection-content_5otpt_24",
	"SidebarSection-breadcrumbLabel": "_SidebarSection-breadcrumbLabel_5otpt_33",
	"SidebarSection-breadcrumbs": "_SidebarSection-breadcrumbs_5otpt_62",
	"SidebarSection-breadcrumb": "_SidebarSection-breadcrumb_5otpt_33",
	"SidebarSection-heading": "_SidebarSection-heading_5otpt_74",
	"SidebarSection-loadingOverlay": "_SidebarSection-loadingOverlay_5otpt_78"
});
var SidebarSection = ({ children, title, background, showBreadcrumbs, noBorderTop, isLoading }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName28({ noBorderTop }),
		style: { background },
		children: [
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassName28("title"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: getClassName28("breadcrumbs"),
					children: [showBreadcrumbs && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Breadcrumbs, {}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: getClassName28("heading"),
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Heading, {
							rank: "2",
							size: "xs",
							children: title
						})
					})]
				})
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassName28("content"),
				children
			}),
			isLoading && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassName28("loadingOverlay"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Loader, { size: 32 })
			})
		]
	});
};
init_react_import();
init_react_import();
init_react_import();
var styles_module_default24 = {
	"ViewportControls": "_ViewportControls_e3unb_1",
	"ViewportControls--fullScreen": "_ViewportControls--fullScreen_e3unb_5",
	"ViewportControls-toggleButton": "_ViewportControls-toggleButton_e3unb_14",
	"ViewportControls--isExpanded": "_ViewportControls--isExpanded_e3unb_38",
	"ViewportControls-actions": "_ViewportControls-actions_e3unb_42",
	"ViewportControls-actionsInner": "_ViewportControls-actionsInner_e3unb_46",
	"ViewportControls-divider": "_ViewportControls-divider_e3unb_75",
	"ViewportControls-zoomSelect": "_ViewportControls-zoomSelect_e3unb_81",
	"ViewportControls-zoom": "_ViewportControls-zoom_e3unb_81",
	"ViewportButton-inner": "_ViewportButton-inner_e3unb_111",
	"ViewportButton--isActive": "_ViewportButton--isActive_e3unb_119"
};
var icons = {
	Smartphone: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Smartphone, { size: 16 }),
	Tablet: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tablet, { size: 16 }),
	Monitor: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Monitor$1, { size: 16 }),
	FullWidth: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Expand, { size: 16 })
};
var getClassName29 = get_class_name_factory_default("ViewportControls", styles_module_default24);
var getClassNameButton = get_class_name_factory_default("ViewportButton", styles_module_default24);
var ActionButton = ({ children, title, onClick, isActive, disabled }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		className: getClassNameButton({ isActive }),
		suppressHydrationWarning: true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
			type: "button",
			title,
			disabled: disabled || isActive,
			onClick,
			suppressHydrationWarning: true,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: getClassNameButton("inner"),
				children
			})
		})
	});
};
var defaultZoomOptions = [
	{
		label: "25%",
		value: .25
	},
	{
		label: "50%",
		value: .5
	},
	{
		label: "75%",
		value: .75
	},
	{
		label: "100%",
		value: 1
	},
	{
		label: "125%",
		value: 1.25
	},
	{
		label: "150%",
		value: 1.5
	},
	{
		label: "200%",
		value: 2
	}
];
var ViewportControls = ({ autoZoom, zoom, onViewportChange, onZoom, fullScreen }) => {
	var _a, _b;
	const viewports = useAppStore((s) => s.viewports);
	const uiViewports = useAppStore((s) => s.state.ui.viewports);
	const defaultsContainAutoZoom = defaultZoomOptions.find((option) => option.value === autoZoom);
	const zoomOptions = (0, import_react.useMemo)(() => [...defaultZoomOptions, ...defaultsContainAutoZoom ? [] : [{
		value: autoZoom,
		label: `${(autoZoom * 100).toFixed(0)}% (Auto)`
	}]].filter((a) => a.value <= autoZoom).sort((a, b) => a.value > b.value ? 1 : -1), [autoZoom]);
	const [activeViewport, setActiveViewport] = (0, import_react.useState)(uiViewports.current.width);
	(0, import_react.useEffect)(() => {
		setActiveViewport(uiViewports.current.width);
	}, [uiViewports.current]);
	const [isExpanded, setIsExpanded] = (0, import_react.useState)(false);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName29({
			isExpanded,
			fullScreen
		}),
		suppressHydrationWarning: true,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName29("actions"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: getClassName29("actionsInner"),
				children: [
					viewports.map((viewport, i) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionButton, {
						title: viewport.label ? `Switch to ${viewport.label} viewport` : "Switch viewport",
						onClick: () => {
							setActiveViewport(viewport.width);
							onViewportChange(viewport);
						},
						isActive: activeViewport === viewport.width,
						children: typeof viewport.icon === "string" ? icons[viewport.icon] || viewport.icon : viewport.icon || icons.Smartphone
					}, i)),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: getClassName29("divider") }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionButton, {
						title: "Zoom viewport out",
						disabled: zoom <= ((_a = zoomOptions[0]) == null ? void 0 : _a.value),
						onClick: (e) => {
							e.stopPropagation();
							onZoom(zoomOptions[Math.max(zoomOptions.findIndex((option) => option.value === zoom) - 1, 0)].value);
						},
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ZoomOut, { size: 16 })
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(ActionButton, {
						title: "Zoom viewport in",
						disabled: zoom >= ((_b = zoomOptions[zoomOptions.length - 1]) == null ? void 0 : _b.value),
						onClick: (e) => {
							e.stopPropagation();
							onZoom(zoomOptions[Math.min(zoomOptions.findIndex((option) => option.value === zoom) + 1, zoomOptions.length - 1)].value);
						},
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ZoomIn, { size: 16 })
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: getClassName29("zoom"),
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: getClassName29("divider") }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("select", {
							className: getClassName29("zoomSelect"),
							value: zoom.toString(),
							onClick: (e) => {
								e.stopPropagation();
							},
							onChange: (e) => {
								onZoom(parseFloat(e.currentTarget.value));
							},
							children: zoomOptions.map((option) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("option", {
								value: option.value,
								label: option.label
							}, option.label))
						})]
					})
				]
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
			className: getClassName29("toggleButton"),
			title: "Toggle viewport menu",
			onClick: () => setIsExpanded((s) => !s),
			children: isExpanded ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(X, { size: 16 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Monitor$1, { size: 16 })
		})]
	});
};
init_react_import();
var styles_module_default25 = {
	"PuckCanvas": "_PuckCanvas_ur8dl_1",
	"PuckCanvas-controls": "_PuckCanvas-controls_ur8dl_17",
	"PuckCanvas--fullScreen": "_PuckCanvas--fullScreen_ur8dl_22",
	"PuckCanvas-inner": "_PuckCanvas-inner_ur8dl_33",
	"PuckCanvas-root": "_PuckCanvas-root_ur8dl_42",
	"PuckCanvas--ready": "_PuckCanvas--ready_ur8dl_67",
	"PuckCanvas-loader": "_PuckCanvas-loader_ur8dl_72",
	"PuckCanvas--showLoader": "_PuckCanvas--showLoader_ur8dl_83"
};
init_react_import();
var FrameContext = (0, import_react.createContext)(null);
var FrameProvider = ({ children }) => {
	const frameRef = (0, import_react.useRef)(null);
	const value = (0, import_react.useMemo)(() => ({ frameRef }), []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FrameContext.Provider, {
		value,
		children
	});
};
var useCanvasFrame = () => {
	const context = (0, import_react.useContext)(FrameContext);
	if (context === null) throw new Error("useCanvasFrame must be used within a FrameProvider");
	return context;
};
var getClassName30 = get_class_name_factory_default("PuckCanvas", styles_module_default25);
var ZOOM_ON_CHANGE = true;
var TRANSITION_DURATION = 150;
var Canvas = () => {
	var _a;
	const { frameRef } = useCanvasFrame();
	const resetAutoZoom = useResetAutoZoom(frameRef);
	const { viewports: viewportOptions = defaultViewports, ui: uiProp } = usePropsContext();
	const { dispatch, overrides, setUi, zoomConfig, setZoomConfig, status, iframe, _experimentalFullScreenCanvas } = useAppStore(useShallow((s) => ({
		dispatch: s.dispatch,
		overrides: s.overrides,
		setUi: s.setUi,
		zoomConfig: s.zoomConfig,
		setZoomConfig: s.setZoomConfig,
		status: s.status,
		iframe: s.iframe,
		_experimentalFullScreenCanvas: s._experimentalFullScreenCanvas
	})));
	const { leftSideBarVisible, rightSideBarVisible, leftSideBarWidth, rightSideBarWidth, viewports } = useAppStore(useShallow((s) => ({
		leftSideBarVisible: s.state.ui.leftSideBarVisible,
		rightSideBarVisible: s.state.ui.rightSideBarVisible,
		leftSideBarWidth: s.state.ui.leftSideBarWidth,
		rightSideBarWidth: s.state.ui.rightSideBarWidth,
		viewports: s.state.ui.viewports
	})));
	const [showTransition, setShowTransition] = (0, import_react.useState)(false);
	const isResizingRef = (0, import_react.useRef)(false);
	const defaultRender = (0, import_react.useMemo)(() => {
		const PuckDefault = ({ children }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children });
		return PuckDefault;
	}, []);
	const CustomPreview = (0, import_react.useMemo)(() => overrides.preview || defaultRender, [overrides]);
	const getFrameDimensions = (0, import_react.useCallback)(() => {
		if (frameRef.current) {
			const frame = frameRef.current;
			const box = getBox(frame);
			return {
				width: box.contentBox.width,
				height: box.contentBox.height
			};
		}
		return {
			width: 0,
			height: 0
		};
	}, [frameRef]);
	(0, import_react.useEffect)(() => {
		resetAutoZoom();
	}, [
		frameRef,
		leftSideBarVisible,
		rightSideBarVisible,
		leftSideBarWidth,
		rightSideBarWidth,
		viewports
	]);
	(0, import_react.useEffect)(() => {
		const { height: frameHeight } = getFrameDimensions();
		if (viewports.current.height === "auto") setZoomConfig(__spreadProps$6(__spreadValues$7({}, zoomConfig), { rootHeight: frameHeight / zoomConfig.zoom }));
	}, [
		zoomConfig.zoom,
		getFrameDimensions,
		setZoomConfig
	]);
	(0, import_react.useEffect)(() => {
		if (ZOOM_ON_CHANGE) resetAutoZoom();
	}, [viewports.current.width, viewports]);
	(0, import_react.useEffect)(() => {
		if (!frameRef.current) return;
		const resizeObserver = new ResizeObserver(() => {
			if (!isResizingRef.current) resetAutoZoom();
		});
		resizeObserver.observe(frameRef.current);
		return () => {
			resizeObserver.disconnect();
		};
	}, [frameRef.current]);
	const [showLoader, setShowLoader] = (0, import_react.useState)(false);
	(0, import_react.useEffect)(() => {
		setTimeout(() => {
			setShowLoader(true);
		}, 500);
	}, []);
	const appStoreApi = useAppStoreApi();
	(0, import_react.useEffect)(() => {
		var _a2, _b;
		if (typeof window === "undefined") return;
		if ((_a2 = uiProp == null ? void 0 : uiProp.viewports) == null ? void 0 : _a2.current) return;
		const viewportWidth = window.innerWidth;
		const frameWidth = (_b = frameRef.current) == null ? void 0 : _b.getBoundingClientRect().width;
		if (!viewportWidth) return;
		if (!frameWidth) return;
		if (viewportOptions.length === 0) return;
		const fullWidthViewport = Object.values(viewportOptions).find((v) => v.width === "100%");
		const containsFullWidthViewport = !!fullWidthViewport;
		let closestViewport = Object.entries(viewportOptions).filter(([_, value]) => value.width !== "100%").map(([key, value]) => ({
			key,
			diff: Math.abs(viewportWidth - (typeof value.width === "string" ? viewportWidth : value.width)),
			value
		})).sort((a, b) => a.diff > b.diff ? 1 : -1)[0].value;
		if (closestViewport.width < frameWidth && containsFullWidthViewport) closestViewport = fullWidthViewport;
		if (iframe.enabled) {
			const s = appStoreApi.getState();
			const appState = { state: __spreadProps$6(__spreadValues$7({}, s.state), { ui: __spreadProps$6(__spreadValues$7({}, s.state.ui), { viewports: __spreadProps$6(__spreadValues$7({}, s.state.ui.viewports), { current: __spreadProps$6(__spreadValues$7({}, s.state.ui.viewports.current), {
				height: (closestViewport == null ? void 0 : closestViewport.height) || "auto",
				width: closestViewport == null ? void 0 : closestViewport.width
			}) }) }) }) };
			let history = s.history;
			if (s.history.histories.length === 1) history = __spreadProps$6(__spreadValues$7({}, history), { histories: [appState] });
			appStoreApi.setState(__spreadProps$6(__spreadValues$7({}, appState), { history }));
		}
	}, [
		viewportOptions,
		frameRef.current,
		iframe,
		appStoreApi,
		(_a = uiProp == null ? void 0 : uiProp.viewports) == null ? void 0 : _a.current
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: getClassName30({
			ready: status === "READY" || !iframe.enabled || !iframe.waitForStyles,
			showLoader,
			fullScreen: _experimentalFullScreenCanvas
		}),
		onClick: (e) => {
			const el = e.target;
			if (!el.hasAttribute("data-puck-component") && !el.hasAttribute("data-puck-dropzone")) dispatch({
				type: "setUi",
				ui: { itemSelector: null },
				recordHistory: false
			});
		},
		children: [viewports.controlsVisible && iframe.enabled && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName30("controls"),
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ViewportControls, {
				fullScreen: _experimentalFullScreenCanvas,
				autoZoom: zoomConfig.autoZoom,
				zoom: zoomConfig.zoom,
				onViewportChange: (viewport) => {
					setShowTransition(true);
					isResizingRef.current = true;
					const uiViewport = __spreadProps$6(__spreadValues$7({}, viewport), {
						height: viewport.height || "auto",
						zoom: zoomConfig.zoom
					});
					setUi({ viewports: __spreadProps$6(__spreadValues$7({}, viewports), { current: uiViewport }) });
					if (ZOOM_ON_CHANGE) resetAutoZoom({ viewports: __spreadProps$6(__spreadValues$7({}, viewports), { current: uiViewport }) });
				},
				onZoom: (zoom) => {
					setShowTransition(true);
					isResizingRef.current = true;
					setZoomConfig(__spreadProps$6(__spreadValues$7({}, zoomConfig), { zoom }));
				}
			})
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: getClassName30("inner"),
			ref: frameRef,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassName30("root"),
				style: {
					width: iframe.enabled ? viewports.current.width : "100%",
					height: zoomConfig.rootHeight,
					transform: iframe.enabled ? `scale(${zoomConfig.zoom})` : void 0,
					transition: showTransition ? `width ${TRANSITION_DURATION}ms ease-out, height ${TRANSITION_DURATION}ms ease-out, transform ${TRANSITION_DURATION}ms ease-out` : "",
					overflow: iframe.enabled ? void 0 : "auto"
				},
				suppressHydrationWarning: true,
				id: "puck-canvas-root",
				onTransitionEnd: () => {
					setShowTransition(false);
					isResizingRef.current = false;
				},
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomPreview, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Preview2, {}) })
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getClassName30("loader"),
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Loader, { size: 24 })
			})]
		})]
	});
};
init_react_import();
function useSidebarResize(position, dispatch) {
	const [width, setWidth] = (0, import_react.useState)(null);
	const sidebarRef = (0, import_react.useRef)(null);
	const storeWidth = useAppStore((s) => position === "left" ? s.state.ui.leftSideBarWidth : s.state.ui.rightSideBarWidth);
	(0, import_react.useEffect)(() => {
		if (typeof window !== "undefined" && !storeWidth) try {
			const savedWidths = localStorage.getItem("puck-sidebar-widths");
			if (savedWidths) {
				const savedWidth = JSON.parse(savedWidths)[position];
				const key = position === "left" ? "leftSideBarWidth" : "rightSideBarWidth";
				if (savedWidth) dispatch({
					type: "setUi",
					ui: { [key]: savedWidth }
				});
			}
		} catch (error) {
			console.error(`Failed to load ${position} sidebar width from localStorage`, error);
		}
	}, [
		dispatch,
		position,
		storeWidth
	]);
	(0, import_react.useEffect)(() => {
		if (storeWidth !== void 0) setWidth(storeWidth);
	}, [storeWidth]);
	return {
		width,
		setWidth,
		sidebarRef,
		handleResizeEnd: (0, import_react.useCallback)((width2) => {
			dispatch({
				type: "setUi",
				ui: { [position === "left" ? "leftSideBarWidth" : "rightSideBarWidth"]: width2 }
			});
			let widths = {};
			try {
				const savedWidths = localStorage.getItem("puck-sidebar-widths");
				widths = savedWidths ? JSON.parse(savedWidths) : {};
			} catch (error) {
				console.error(`Failed to save ${position} sidebar width to localStorage`, error);
			} finally {
				localStorage.setItem("puck-sidebar-widths", JSON.stringify(__spreadProps$6(__spreadValues$7({}, widths), { [position]: width2 })));
			}
			window.dispatchEvent(new CustomEvent("viewportchange", {
				bubbles: true,
				cancelable: false
			}));
		}, [dispatch, position])
	};
}
init_react_import();
init_react_import();
init_react_import();
var getClassName31 = get_class_name_factory_default("ResizeHandle", {
	"ResizeHandle": "_ResizeHandle_144bf_2",
	"ResizeHandle--left": "_ResizeHandle--left_144bf_16",
	"ResizeHandle--right": "_ResizeHandle--right_144bf_20"
});
var ResizeHandle = ({ position, sidebarRef, onResize, onResizeEnd }) => {
	const { frameRef } = useCanvasFrame();
	const resetAutoZoom = useResetAutoZoom(frameRef);
	const handleRef = (0, import_react.useRef)(null);
	const isDragging = (0, import_react.useRef)(false);
	const startX = (0, import_react.useRef)(0);
	const startWidth = (0, import_react.useRef)(0);
	const handleMouseMove = (0, import_react.useCallback)((e) => {
		if (!isDragging.current) return;
		const delta = e.clientX - startX.current;
		const newWidth = position === "left" ? startWidth.current + delta : startWidth.current - delta;
		onResize(Math.max(192, newWidth));
		e.preventDefault();
	}, [onResize, position]);
	const handleMouseUp = (0, import_react.useCallback)(() => {
		var _a;
		if (!isDragging.current) return;
		isDragging.current = false;
		document.body.style.cursor = "";
		document.body.style.userSelect = "";
		const overlay = document.getElementById("resize-overlay");
		if (overlay) document.body.removeChild(overlay);
		document.removeEventListener("mousemove", handleMouseMove);
		document.removeEventListener("mouseup", handleMouseUp);
		onResizeEnd(((_a = sidebarRef.current) == null ? void 0 : _a.getBoundingClientRect().width) || 0);
		resetAutoZoom();
	}, [onResizeEnd]);
	const handleMouseDown = (0, import_react.useCallback)((e) => {
		var _a;
		isDragging.current = true;
		startX.current = e.clientX;
		startWidth.current = ((_a = sidebarRef.current) == null ? void 0 : _a.getBoundingClientRect().width) || 0;
		document.body.style.cursor = "col-resize";
		document.body.style.userSelect = "none";
		const overlay = document.createElement("div");
		overlay.id = "resize-overlay";
		overlay.setAttribute("data-resize-overlay", "");
		document.body.appendChild(overlay);
		document.addEventListener("mousemove", handleMouseMove);
		document.addEventListener("mouseup", handleMouseUp);
		e.preventDefault();
	}, [
		position,
		handleMouseMove,
		handleMouseUp
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref: handleRef,
		className: getClassName31({ [position]: true }),
		onMouseDown: handleMouseDown
	});
};
init_react_import();
var getClassName32 = get_class_name_factory_default("Sidebar", {
	"Sidebar": "_Sidebar_o396p_1",
	"Sidebar--isVisible": "_Sidebar--isVisible_o396p_9",
	"Sidebar--left": "_Sidebar--left_o396p_13",
	"Sidebar--right": "_Sidebar--right_o396p_25",
	"Sidebar-resizeHandle": "_Sidebar-resizeHandle_o396p_37"
});
var Sidebar = ({ position, sidebarRef, isVisible, onResize, onResizeEnd, children }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		ref: sidebarRef,
		className: getClassName32({
			[position]: true,
			isVisible
		}),
		children
	}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: `${getClassName32("resizeHandle")}`,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ResizeHandle, {
			position,
			sidebarRef,
			onResize,
			onResizeEnd
		})
	})] });
};
init_react_import();
var isElementVisible = (element) => {
	let current = element;
	while (current && current !== document.body) {
		const style = window.getComputedStyle(current);
		if (style.display === "none" || style.visibility === "hidden" || style.opacity === "0" || current.getAttribute("aria-hidden") === "true" || current.hasAttribute("hidden")) return false;
		current = current.parentElement;
	}
	return true;
};
var shouldBlockDeleteHotkey = (e) => {
	var _a;
	if (e == null ? void 0 : e.defaultPrevented) return true;
	const origin = ((_a = e == null ? void 0 : e.composedPath) == null ? void 0 : _a.call(e)[0]) || (e == null ? void 0 : e.target) || document.activeElement;
	if (origin instanceof HTMLElement) {
		const tag = origin.tagName.toLowerCase();
		if (tag === "input" || tag === "textarea" || tag === "select") return true;
		if (origin.isContentEditable) return true;
		const role = origin.getAttribute("role");
		if (role === "textbox" || role === "combobox" || role === "searchbox" || role === "listbox" || role === "grid") return true;
	}
	const modal = document.querySelector("dialog[open], [aria-modal=\"true\"], [role=\"dialog\"], [role=\"alertdialog\"]");
	if (modal && isElementVisible(modal)) return true;
	return false;
};
var useDeleteHotkeys = () => {
	const appStore = useAppStoreApi();
	const deleteSelectedComponent = (0, import_react.useCallback)((e) => {
		var _a;
		if (shouldBlockDeleteHotkey(e)) return false;
		const { state, dispatch, permissions, selectedItem } = appStore.getState();
		const sel = (_a = state.ui) == null ? void 0 : _a.itemSelector;
		if (!(sel == null ? void 0 : sel.zone) || !selectedItem) return true;
		if (!permissions.getPermissions({ item: selectedItem }).delete) return true;
		dispatch({
			type: "remove",
			index: sel.index,
			zone: sel.zone
		});
		return true;
	}, [appStore]);
	useHotkey({ delete: true }, deleteSelectedComponent);
	useHotkey({ backspace: true }, deleteSelectedComponent);
};
init_react_import();
init_react_import();
var styles_module_default28 = {
	"Nav": "_Nav_1tvxq_1",
	"Nav-list": "_Nav-list_1tvxq_5",
	"Nav-mobileActions": "_Nav-mobileActions_1tvxq_23",
	"NavItem-link": "_NavItem-link_1tvxq_38",
	"NavItem": "_NavItem_1tvxq_38",
	"NavItem-linkIcon": "_NavItem-linkIcon_1tvxq_89",
	"NavItem--active": "_NavItem--active_1tvxq_94",
	"NavItem--mobileOnly": "_NavItem--mobileOnly_1tvxq_121",
	"NavItem--desktopOnly": "_NavItem--desktopOnly_1tvxq_126"
};
var getClassName33 = get_class_name_factory_default("Nav", styles_module_default28);
var getClassNameItem3 = get_class_name_factory_default("NavItem", styles_module_default28);
var MenuItem = ({ label, icon, onClick, isActive, mobileOnly, desktopOnly }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", {
		className: getClassNameItem3({
			active: isActive,
			mobileOnly,
			desktopOnly
		}),
		children: onClick && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: getClassNameItem3("link"),
			onClick,
			children: [icon && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: getClassNameItem3("linkIcon"),
				children: icon
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				className: getClassNameItem3("linkLabel"),
				children: label
			})]
		})
	});
};
var Nav = ({ items, mobileActions }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("nav", {
		className: getClassName33(),
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("ul", {
			className: getClassName33("list"),
			children: Object.entries(items).map(([key, item]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenuItem, __spreadValues$7({}, item), key))
		}), mobileActions && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getClassName33("mobileActions"),
			children: mobileActions
		})]
	});
};
var getClassName34 = get_class_name_factory_default("Puck", styles_module_default20);
var getLayoutClassName = get_class_name_factory_default("PuckLayout", styles_module_default20);
var getPluginTabClassName = get_class_name_factory_default("PuckPluginTab", styles_module_default20);
var FieldSideBar = () => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SidebarSection, {
		noBorderTop: true,
		showBreadcrumbs: true,
		title: useAppStore((s) => {
			var _a, _b, _c;
			return s.selectedItem ? (_b = (_a = s.config.components[s.selectedItem.type]) == null ? void 0 : _a["label"]) != null ? _b : s.selectedItem.type.toString() : ((_c = s.config.root) == null ? void 0 : _c.label) || "Page";
		}),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Fields, {})
	});
};
var PluginTab = ({ children, visible, mobileOnly }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: getPluginTabClassName({
			visible,
			mobileOnly
		}),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: getPluginTabClassName("body"),
			children
		})
	});
};
var Layout = ({ children }) => {
	const { iframe: _iframe, dnd, initialHistory: _initialHistory, plugins, height } = usePropsContext();
	const iframe = (0, import_react.useMemo)(() => __spreadValues$7({
		enabled: true,
		waitForStyles: true
	}, _iframe), [_iframe]);
	useInjectGlobalCss(iframe.enabled);
	const dispatch = useAppStore((s) => s.dispatch);
	const leftSideBarVisible = useAppStore((s) => s.state.ui.leftSideBarVisible);
	const rightSideBarVisible = useAppStore((s) => s.state.ui.rightSideBarVisible);
	const instanceId = useAppStore((s) => s.instanceId);
	const { width: leftWidth, setWidth: setLeftWidth, sidebarRef: leftSidebarRef, handleResizeEnd: handleLeftSidebarResizeEnd } = useSidebarResize("left", dispatch);
	const { width: rightWidth, setWidth: setRightWidth, sidebarRef: rightSidebarRef, handleResizeEnd: handleRightSidebarResizeEnd } = useSidebarResize("right", dispatch);
	(0, import_react.useEffect)(() => {
		if (!window.matchMedia("(min-width: 638px)").matches) dispatch({
			type: "setUi",
			ui: {
				leftSideBarVisible: false,
				rightSideBarVisible: false
			}
		});
		const handleResize = () => {
			if (!window.matchMedia("(min-width: 638px)").matches) dispatch({
				type: "setUi",
				ui: (ui) => __spreadValues$7(__spreadValues$7({}, ui), ui.rightSideBarVisible ? { leftSideBarVisible: false } : {})
			});
		};
		window.addEventListener("resize", handleResize);
		return () => {
			window.removeEventListener("resize", handleResize);
		};
	}, []);
	const overrides = useAppStore((s) => s.overrides);
	const CustomPuck = (0, import_react.useMemo)(() => overrides.puck || DefaultOverride, [overrides]);
	const [mounted, setMounted] = (0, import_react.useState)(false);
	(0, import_react.useEffect)(() => {
		setMounted(true);
	}, []);
	const ready = useAppStore((s) => s.status === "READY");
	useMonitorHotkeys();
	(0, import_react.useEffect)(() => {
		if (ready && iframe.enabled) {
			const frameDoc = getFrame();
			if (frameDoc) return monitorHotkeys(frameDoc);
		}
	}, [ready, iframe.enabled]);
	usePreviewModeHotkeys();
	useDeleteHotkeys();
	const layoutOptions = {};
	if (leftWidth) layoutOptions["--puck-user-left-side-bar-width"] = `${leftWidth}px`;
	if (rightWidth) layoutOptions["--puck-user-right-side-bar-width"] = `${rightWidth}px`;
	const setUi = useAppStore((s) => s.setUi);
	const currentPlugin = useAppStore((s) => {
		var _a;
		return (_a = s.state.ui.plugin) == null ? void 0 : _a.current;
	});
	const appStoreApi = useAppStoreApi();
	const [mobilePanelHeightMode, setMobilePanelHeightMode] = (0, import_react.useState)("toggle");
	const hasLegacySideBarPlugin = (0, import_react.useMemo)(() => !!(plugins == null ? void 0 : plugins.find((p) => p.name === "legacy-side-bar")), [plugins]);
	const pluginItems = (0, import_react.useMemo)(() => {
		const details = {};
		const defaultPlugins = [blocksPlugin(), outlinePlugin()];
		const isLegacy = (plugin) => plugin.name === "legacy-side-bar" ? -1 : 0;
		const combinedPlugins = [...defaultPlugins, ...plugins != null ? plugins : []].sort((a, b) => isLegacy(a) - isLegacy(b));
		if (!(plugins == null ? void 0 : plugins.some((p) => p.name === "fields"))) combinedPlugins.push(fieldsPlugin());
		combinedPlugins?.forEach((plugin) => {
			var _a, _b;
			if (plugin.name && plugin.render) {
				if (details[plugin.name]) delete details[plugin.name];
				details[plugin.name] = {
					label: (_a = plugin.label) != null ? _a : plugin.name,
					icon: (_b = plugin.icon) != null ? _b : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToyBrick, {}),
					onClick: () => {
						var _a2;
						setMobilePanelHeightMode((_a2 = plugin.mobilePanelHeight) != null ? _a2 : "toggle");
						if (plugin.name === currentPlugin) if (leftSideBarVisible) setUi({ leftSideBarVisible: false });
						else setUi({ leftSideBarVisible: true });
						else if (plugin.name) setUi({
							plugin: { current: plugin.name },
							leftSideBarVisible: true
						});
					},
					isActive: leftSideBarVisible && currentPlugin === plugin.name,
					render: plugin.render,
					mobileOnly: hasLegacySideBarPlugin || plugin.mobileOnly,
					desktopOnly: plugin.name === "legacy-side-bar" || plugin.desktopOnly
				};
			}
		});
		return details;
	}, [
		plugins,
		currentPlugin,
		appStoreApi,
		leftSideBarVisible
	]);
	(0, import_react.useEffect)(() => {
		if (!currentPlugin) setUi({ plugin: { current: Object.keys(pluginItems)[0] } });
	}, [pluginItems, currentPlugin]);
	const hasDesktopFieldsPlugin = pluginItems["fields"] && pluginItems["fields"].mobileOnly === false;
	const mobilePanelExpanded = useAppStore((s) => {
		var _a;
		return (_a = s.state.ui.mobilePanelExpanded) != null ? _a : false;
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: `Puck ${getClassName34({ hidePlugins: hasLegacySideBarPlugin })}`,
		id: instanceId,
		style: { height },
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(DragDropContext, {
			disableAutoScroll: dnd == null ? void 0 : dnd.disableAutoScroll,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CustomPuck, { children: children || /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FrameProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: getLayoutClassName({
					leftSideBarVisible,
					mounted,
					rightSideBarVisible: !hasDesktopFieldsPlugin && rightSideBarVisible,
					isExpanded: mobilePanelExpanded,
					mobilePanelHeightToggle: mobilePanelHeightMode === "toggle",
					mobilePanelHeightMinContent: mobilePanelHeightMode === "min-content"
				}),
				style: { height },
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: getLayoutClassName("inner"),
					style: layoutOptions,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: getLayoutClassName("header"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Header, { hidePlugins: hasLegacySideBarPlugin })
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: getLayoutClassName("nav"),
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Nav, {
								items: pluginItems,
								mobileActions: leftSideBarVisible && mobilePanelHeightMode === "toggle" && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IconButton, {
									type: "button",
									title: "maximize",
									onClick: () => {
										setUi({ mobilePanelExpanded: !mobilePanelExpanded });
									},
									children: mobilePanelExpanded ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Minimize2, { size: 21 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Maximize2, { size: 21 })
								})
							})
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Sidebar, {
							position: "left",
							sidebarRef: leftSidebarRef,
							isVisible: leftSideBarVisible,
							onResize: setLeftWidth,
							onResizeEnd: handleLeftSidebarResizeEnd,
							children: Object.entries(pluginItems).map(([id, { mobileOnly, render: Render2, label }]) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PluginTab, {
								visible: currentPlugin === id,
								mobileOnly,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Render2, {})
							}, id))
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Canvas, {}),
						!hasDesktopFieldsPlugin && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Sidebar, {
							position: "right",
							sidebarRef: rightSidebarRef,
							isVisible: rightSideBarVisible,
							onResize: setRightWidth,
							onResizeEnd: handleRightSidebarResizeEnd,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldSideBar, {})
						})
					]
				})
			}) }) })
		}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			id: "puck-portal-root",
			className: getClassName34("portal")
		})]
	});
};
var propsContext = (0, import_react.createContext)({});
function PropsProvider(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(propsContext.Provider, {
		value: props,
		children: props.children
	});
}
var usePropsContext = () => (0, import_react.useContext)(propsContext);
function PuckProvider({ children }) {
	const { config, data: initialData, ui: initialUi, onChange, permissions = {}, plugins, overrides, viewports = defaultViewports, iframe: _iframe, initialHistory: _initialHistory, metadata, onAction, fieldTransforms, _experimentalFullScreenCanvas, _experimentalVirtualization } = usePropsContext();
	const iframe = (0, import_react.useMemo)(() => __spreadValues$7({
		enabled: true,
		waitForStyles: true
	}, _iframe), [_iframe]);
	const [generatedAppState] = (0, import_react.useState)(() => {
		var _a, _b, _c;
		const initial = __spreadValues$7(__spreadValues$7({}, defaultAppState.ui), initialUi);
		let clientUiState = {};
		if (Object.keys((initialData == null ? void 0 : initialData.root) || {}).length > 0 && !((_a = initialData == null ? void 0 : initialData.root) == null ? void 0 : _a.props)) console.warn("Warning: Defining props on `root` is deprecated. Please use `root.props`, or republish this page to migrate automatically.");
		const rootProps = ((_b = initialData == null ? void 0 : initialData.root) == null ? void 0 : _b.props) || (initialData == null ? void 0 : initialData.root) || {};
		const defaultedRootProps = __spreadValues$7(__spreadValues$7({}, (_c = config.root) == null ? void 0 : _c.defaultProps), rootProps);
		const root = populateIds(toComponent(__spreadProps$6(__spreadValues$7({}, initialData == null ? void 0 : initialData.root), { props: defaultedRootProps })), config);
		return walkAppState(__spreadProps$6(__spreadValues$7({}, defaultAppState), {
			data: __spreadProps$6(__spreadValues$7({}, initialData), {
				root: __spreadProps$6(__spreadValues$7({}, initialData == null ? void 0 : initialData.root), { props: root.props }),
				content: initialData.content || []
			}),
			ui: __spreadProps$6(__spreadValues$7(__spreadValues$7({}, initial), clientUiState), { componentList: config.categories ? Object.entries(config.categories).reduce((acc, [categoryName, category]) => {
				return __spreadProps$6(__spreadValues$7({}, acc), { [categoryName]: {
					title: category.title,
					components: category.components,
					expanded: category.defaultExpanded,
					visible: category.visible
				} });
			}, {}) : {} })
		}), config);
	});
	const { appendData = true } = _initialHistory || {};
	const [blendedHistories] = (0, import_react.useState)([...(_initialHistory == null ? void 0 : _initialHistory.histories) || [], ...appendData ? [{ state: generatedAppState }] : []].map((history) => {
		let newState = __spreadValues$7(__spreadValues$7({}, generatedAppState), history.state);
		if (!history.state.indexes) newState = walkAppState(newState, config);
		return __spreadProps$6(__spreadValues$7({}, history), { state: newState });
	}));
	const initialHistoryIndex = (0, import_react.useMemo)(() => {
		if ((_initialHistory == null ? void 0 : _initialHistory.index) !== void 0 && (_initialHistory == null ? void 0 : _initialHistory.index) >= 0 && (_initialHistory == null ? void 0 : _initialHistory.index) < blendedHistories.length) return _initialHistory == null ? void 0 : _initialHistory.index;
		return blendedHistories.length - 1;
	}, []);
	const initialAppState = blendedHistories[initialHistoryIndex].state;
	const loadedOverrides = useLoadedOverrides({
		overrides,
		plugins
	});
	const loadedFieldTransforms = (0, import_react.useMemo)(() => {
		return __spreadValues$7(__spreadValues$7({}, (plugins || []).reduce((acc, plugin) => __spreadValues$7(__spreadValues$7({}, acc), plugin.fieldTransforms), {})), fieldTransforms);
	}, [fieldTransforms, plugins]);
	const instanceId = useSafeId();
	const generateAppStore = (0, import_react.useCallback)((state) => {
		return {
			instanceId,
			state,
			config,
			plugins: plugins || [],
			overrides: loadedOverrides,
			viewports,
			iframe,
			_experimentalFullScreenCanvas: !!_experimentalFullScreenCanvas,
			_experimentalVirtualization: !!_experimentalVirtualization,
			onAction,
			metadata,
			fieldTransforms: loadedFieldTransforms
		};
	}, [
		instanceId,
		initialAppState,
		config,
		plugins,
		loadedOverrides,
		viewports,
		iframe,
		_experimentalFullScreenCanvas,
		_experimentalVirtualization,
		onAction,
		metadata,
		loadedFieldTransforms
	]);
	const [appStore] = (0, import_react.useState)(() => createAppStore(generateAppStore(initialAppState)));
	(0, import_react.useEffect)(() => {}, [appStore]);
	(0, import_react.useEffect)(() => {
		const state = appStore.getState().state;
		appStore.setState(__spreadValues$7({}, generateAppStore(state)));
	}, [generateAppStore]);
	useRegisterHistorySlice(appStore, {
		histories: blendedHistories,
		index: initialHistoryIndex,
		initialAppState
	});
	const previousData = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		return appStore.subscribe((s) => s.state.data, (data) => {
			if (onChange) {
				if (deepEqual$2(data, previousData.current)) return;
				onChange(data);
				previousData.current = data;
			}
		});
	}, [onChange]);
	useRegisterPermissionsSlice(appStore, permissions);
	const uPuckStore = useRegisterUsePuckStore(appStore);
	(0, import_react.useEffect)(() => {
		const { resolveAndCommitData } = appStore.getState();
		setTimeout(() => {
			resolveAndCommitData();
		}, 0);
	}, []);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(appStoreContext.Provider, {
		value: appStore,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(UsePuckStoreContext.Provider, {
			value: uPuckStore,
			children
		})
	});
}
function Puck(props) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PropsProvider, __spreadProps$6(__spreadValues$7({}, props), { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PuckProvider, __spreadProps$6(__spreadValues$7({}, props), { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Layout, { children: props.children }) })) }));
}
Puck.Components = Components;
Puck.Fields = Fields;
Puck.Layout = Layout;
Puck.Outline = Outline;
Puck.Preview = Preview2;
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
init_react_import();
//#endregion
//#region node_modules/@puckeditor/core/dist/index.mjs
init_react_import();
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/blockquote.js
var BlockquoteJSXConverter = { quote: ({ node, nodesToJSX }) => {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("blockquote", { children: nodesToJSX({ nodes: node.children }) });
} };
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/heading.js
var HeadingJSXConverter = { heading: ({ node, nodesToJSX }) => {
	const children = nodesToJSX({ nodes: node.children });
	const NodeTag = node.tag;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NodeTag, { children });
} };
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/horizontalRule.js
var HorizontalRuleJSXConverter = { horizontalrule: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("hr", {}) };
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/linebreak.js
var LinebreakJSXConverter = { linebreak: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}) };
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/link.js
var LinkJSXConverter = ({ internalDocToHref }) => ({
	autolink: ({ node, nodesToJSX }) => {
		const children = nodesToJSX({ nodes: node.children });
		const rel = node.fields.newTab ? "noopener noreferrer" : void 0;
		const target = node.fields.newTab ? "_blank" : void 0;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
			href: node.fields.url,
			rel,
			target,
			children
		});
	},
	link: ({ node, nodesToJSX }) => {
		const children = nodesToJSX({ nodes: node.children });
		const rel = node.fields.newTab ? "noopener noreferrer" : void 0;
		const target = node.fields.newTab ? "_blank" : void 0;
		let href = node.fields.url ?? "";
		if (node.fields.linkType === "internal") if (internalDocToHref) href = internalDocToHref({ linkNode: node });
		else {
			console.error("Lexical => JSX converter: Link converter: found internal link, but internalDocToHref is not provided");
			href = "#";
		}
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
			href,
			rel,
			target,
			children
		});
	}
});
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/list.js
var ListJSXConverter = {
	list: ({ node, nodesToJSX }) => {
		const children = nodesToJSX({ nodes: node.children });
		const NodeTag = node.tag;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NodeTag, {
			className: `list-${node?.listType}`,
			children
		});
	},
	listitem: ({ node, nodesToJSX, parent }) => {
		const hasSubLists = node.children.some((child) => child.type === "list");
		const children = nodesToJSX({ nodes: node.children });
		if ("listType" in parent && parent?.listType === "check") {
			const uuid = v4$1();
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", {
				"aria-checked": node.checked ? "true" : "false",
				className: `list-item-checkbox${node.checked ? " list-item-checkbox-checked" : " list-item-checkbox-unchecked"}${hasSubLists ? " nestedListItem" : ""}`,
				role: "checkbox",
				style: { listStyleType: "none" },
				tabIndex: -1,
				value: node?.value,
				children: hasSubLists ? children : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
						checked: node.checked,
						id: uuid,
						readOnly: true,
						type: "checkbox"
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("label", {
						htmlFor: uuid,
						children
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {})
				] })
			});
		} else return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", {
			className: `${hasSubLists ? "nestedListItem" : ""}`,
			style: hasSubLists ? { listStyleType: "none" } : void 0,
			value: node?.value,
			children
		});
	}
};
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/paragraph.js
var ParagraphJSXConverter = { paragraph: ({ node, nodesToJSX }) => {
	const children = nodesToJSX({ nodes: node.children });
	if (!children?.length) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("br", {}) });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children });
} };
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/tab.js
var TabJSXConverter = { tab: "	" };
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/table.js
var TableJSXConverter = {
	table: ({ node, nodesToJSX }) => {
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: "lexical-table-container",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("table", {
				className: "lexical-table",
				style: { borderCollapse: "collapse" },
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tbody", { children: nodesToJSX({ nodes: node.children }) })
			})
		});
	},
	tablecell: ({ node, nodesToJSX }) => {
		const children = nodesToJSX({ nodes: node.children });
		const TagName = node.headerState > 0 ? "th" : "td";
		const headerStateClass = `lexical-table-cell-header-${node.headerState}`;
		const style = {
			backgroundColor: node.backgroundColor || void 0,
			border: "1px solid #ccc",
			padding: "8px"
		};
		const colSpan = node.colSpan && node.colSpan > 1 ? node.colSpan : void 0;
		const rowSpan = node.rowSpan && node.rowSpan > 1 ? node.rowSpan : void 0;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TagName, {
			className: `lexical-table-cell ${headerStateClass}`,
			colSpan,
			rowSpan,
			style,
			children
		});
	},
	tablerow: ({ node, nodesToJSX }) => {
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("tr", {
			className: "lexical-table-row",
			children: nodesToJSX({ nodes: node.children })
		});
	}
};
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/lexical/utils/nodeFormat.js
var NodeFormat = {
	DOM_ELEMENT_TYPE: 1,
	DOM_TEXT_TYPE: 3,
	NO_DIRTY_NODES: 0,
	HAS_DIRTY_NODES: 1,
	FULL_RECONCILE: 2,
	IS_NORMAL: 0,
	IS_TOKEN: 1,
	IS_SEGMENTED: 2,
	IS_INERT: 3,
	IS_BOLD: 1,
	IS_ITALIC: 2,
	IS_STRIKETHROUGH: 4,
	IS_UNDERLINE: 8,
	IS_CODE: 16,
	IS_SUBSCRIPT: 32,
	IS_SUPERSCRIPT: 64,
	IS_HIGHLIGHT: 128,
	IS_DIRECTIONLESS: 1,
	IS_UNMERGEABLE: 2,
	IS_ALIGN_LEFT: 1,
	IS_ALIGN_CENTER: 2,
	IS_ALIGN_RIGHT: 3,
	IS_ALIGN_JUSTIFY: 4,
	IS_ALIGN_START: 5,
	IS_ALIGN_END: 6
};
NodeFormat.IS_BOLD | NodeFormat.IS_ITALIC | NodeFormat.IS_STRIKETHROUGH | NodeFormat.IS_UNDERLINE | NodeFormat.IS_CODE | NodeFormat.IS_SUBSCRIPT | NodeFormat.IS_SUPERSCRIPT | NodeFormat.IS_HIGHLIGHT;
var RTL = "֑-߿יִ-﷽ﹰ-ﻼ";
new RegExp("^[^A-Za-zÀ-ÖØ-öø-ʸ̀-֐ࠀ-῿‎Ⰰ-﬜︀-﹯﻽-￿]*[֑-߿יִ-﷽ﹰ-ﻼ]");
new RegExp("^[^" + RTL + "]*[A-Za-zÀ-ÖØ-öø-ʸ̀-֐ࠀ-῿‎Ⰰ-﬜︀-﹯﻽-￿]");
NodeFormat.IS_BOLD, NodeFormat.IS_CODE, NodeFormat.IS_HIGHLIGHT, NodeFormat.IS_ITALIC, NodeFormat.IS_STRIKETHROUGH, NodeFormat.IS_SUBSCRIPT, NodeFormat.IS_SUPERSCRIPT, NodeFormat.IS_UNDERLINE;
NodeFormat.IS_DIRECTIONLESS, NodeFormat.IS_UNMERGEABLE;
NodeFormat.IS_ALIGN_CENTER, NodeFormat.IS_ALIGN_END, NodeFormat.IS_ALIGN_JUSTIFY, NodeFormat.IS_ALIGN_LEFT, NodeFormat.IS_ALIGN_RIGHT, NodeFormat.IS_ALIGN_START;
NodeFormat.IS_ALIGN_CENTER, NodeFormat.IS_ALIGN_END, NodeFormat.IS_ALIGN_JUSTIFY, NodeFormat.IS_ALIGN_LEFT, NodeFormat.IS_ALIGN_RIGHT, NodeFormat.IS_ALIGN_START;
NodeFormat.IS_NORMAL, NodeFormat.IS_SEGMENTED, NodeFormat.IS_TOKEN;
NodeFormat.IS_NORMAL, NodeFormat.IS_SEGMENTED, NodeFormat.IS_TOKEN;
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/text.js
var TextJSXConverter = { text: ({ node }) => {
	let text = node.text;
	if (node.format & NodeFormat.IS_BOLD) text = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: text });
	if (node.format & NodeFormat.IS_ITALIC) text = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("em", { children: text });
	if (node.format & NodeFormat.IS_STRIKETHROUGH) text = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		style: { textDecoration: "line-through" },
		children: text
	});
	if (node.format & NodeFormat.IS_UNDERLINE) text = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		style: { textDecoration: "underline" },
		children: text
	});
	if (node.format & NodeFormat.IS_CODE) text = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("code", { children: text });
	if (node.format & NodeFormat.IS_SUBSCRIPT) text = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("sub", { children: text });
	if (node.format & NodeFormat.IS_SUPERSCRIPT) text = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("sup", { children: text });
	return text;
} };
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/converters/upload.js
var UploadJSXConverter = { upload: ({ node }) => {
	const uploadNode = node;
	if (typeof uploadNode.value !== "object") return null;
	const uploadDoc = uploadNode.value;
	const alt = uploadNode.fields?.alt || uploadDoc?.alt || "";
	const url = uploadDoc.url;
	/**
	* If the upload is not an image, return a link to the upload
	*/
	if (!uploadDoc.mimeType.startsWith("image")) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
		href: url,
		rel: "noopener noreferrer",
		children: uploadDoc.filename
	});
	/**
	* If the upload is a simple image with no different sizes, return a simple img tag
	*/
	if (!uploadDoc.sizes || !Object.keys(uploadDoc.sizes).length) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
		alt,
		height: uploadDoc.height,
		src: url,
		width: uploadDoc.width
	});
	/**
	* If the upload is an image with different sizes, return a picture element
	*/
	const pictureJSX = [];
	for (const size in uploadDoc.sizes) {
		const imageSize = uploadDoc.sizes[size];
		if (!imageSize || !imageSize.width || !imageSize.height || !imageSize.mimeType || !imageSize.filesize || !imageSize.filename || !imageSize.url) continue;
		const imageSizeURL = imageSize?.url;
		pictureJSX.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("source", {
			media: `(max-width: ${imageSize.width}px)`,
			srcSet: imageSizeURL,
			type: imageSize.mimeType
		}, size));
	}
	pictureJSX.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
		alt,
		height: uploadDoc?.height,
		src: url,
		width: uploadDoc?.width
	}, "image"));
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("picture", { children: pictureJSX });
} };
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/defaultConverters.js
var defaultJSXConverters = {
	...ParagraphJSXConverter,
	...TextJSXConverter,
	...LinebreakJSXConverter,
	...BlockquoteJSXConverter,
	...TableJSXConverter,
	...HeadingJSXConverter,
	...HorizontalRuleJSXConverter,
	...ListJSXConverter,
	...LinkJSXConverter({}),
	...UploadJSXConverter,
	...TabJSXConverter
};
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/validate/hasText.js
/**
* This function checks if the editor state is empty (has any text). If the editor state has no nodes,
* or only an empty paragraph node (no TextNode with length > 0), it returns false.
* Otherwise, it returns true.
*/ function hasText(value) {
	if (!!!value?.root?.children?.length) return false;
	let hasOnlyEmptyParagraph = false;
	if (value?.root?.children?.length === 1) {
		if (value?.root?.children[0]?.type === "paragraph") {
			const paragraphNode = value?.root?.children[0];
			if (!paragraphNode?.children || paragraphNode?.children?.length === 0) hasOnlyEmptyParagraph = true;
			else if (paragraphNode?.children?.length === 1) {
				const paragraphNodeChild = paragraphNode?.children[0];
				if (paragraphNodeChild?.type === "text") {
					if (!paragraphNodeChild?.["text"]?.length) hasOnlyEmptyParagraph = true;
				}
			}
		}
	}
	if (hasOnlyEmptyParagraph) return false;
	return true;
}
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/converter/index.js
/**
* Creates a JSX converter from a NodeMapValue
*/
function createConverterFromNodeMapValue(viewDef) {
	return (args) => {
		const converterArgs = {
			...args,
			isEditor: false,
			isJSXConverter: true
		};
		if (viewDef.Component) return viewDef.Component(converterArgs);
		if (viewDef.html) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { dangerouslySetInnerHTML: { __html: typeof viewDef.html === "function" ? viewDef.html(converterArgs) : viewDef.html } });
		return null;
	};
}
/**
* Creates a JSX converter from a NodeMapBlockValue (for blocks/inlineBlocks)
* This also checks for Block FC as a fallback when Component is not defined.
*/
function createConverterFromBlockValue(viewDef) {
	return (args) => {
		if (viewDef.Component) {
			const converterArgs = {
				...args,
				isEditor: false,
				isJSXConverter: true
			};
			return viewDef.Component(converterArgs);
		}
		if (viewDef.Block) {
			const blockNode = args.node;
			const blockProps = {
				childIndex: args.childIndex,
				converters: args.converters,
				formData: blockNode.fields ?? {},
				isEditor: false,
				isJSXConverter: true,
				node: blockNode,
				nodesToJSX: args.nodesToJSX,
				parent: args.parent
			};
			return /* @__PURE__ */ import_react.createElement(viewDef.Block, blockProps);
		}
		if (viewDef.html) {
			const converterArgs = {
				...args,
				isEditor: false,
				isJSXConverter: true
			};
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { dangerouslySetInnerHTML: { __html: typeof viewDef.html === "function" ? viewDef.html(converterArgs) : viewDef.html } });
		}
		return null;
	};
}
/**
* Converts a LexicalEditorNodeMap into JSXConverters
*/
function nodeMapToConverters(nodeMap) {
	const converters = {};
	for (const [nodeType, value] of Object.entries(nodeMap)) {
		if (!value || typeof value !== "object") continue;
		if (nodeType === "blocks") {
			converters.blocks = {};
			for (const [blockType, _viewDef] of Object.entries(value)) {
				const viewDef = _viewDef;
				if (viewDef.Component || viewDef.Block || viewDef.html) converters.blocks[blockType] = createConverterFromBlockValue(viewDef);
			}
			continue;
		}
		if (nodeType === "inlineBlocks") {
			converters.inlineBlocks = {};
			for (const [blockType, _viewDef] of Object.entries(value)) {
				const viewDef = _viewDef;
				if (viewDef.Component || viewDef.Block || viewDef.html) converters.inlineBlocks[blockType] = createConverterFromBlockValue(viewDef);
			}
			continue;
		}
		const viewDef = value;
		if (viewDef.Component || viewDef.html) converters[nodeType] = createConverterFromNodeMapValue(viewDef);
	}
	return converters;
}
function convertLexicalToJSX({ converters, data, disableIndent, disableTextAlign, nodeMap }) {
	if (hasText(data)) {
		const nodeMapConverters = nodeMap ? nodeMapToConverters(nodeMap) : void 0;
		return convertLexicalNodesToJSX({
			converters: nodeMapConverters ? {
				...converters,
				...nodeMapConverters,
				blocks: {
					...converters.blocks,
					...nodeMapConverters.blocks
				},
				inlineBlocks: {
					...converters.inlineBlocks,
					...nodeMapConverters.inlineBlocks
				}
			} : converters,
			disableIndent,
			disableTextAlign,
			nodes: data.root.children,
			parent: data.root
		});
	}
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {});
}
function convertLexicalNodesToJSX({ converters, disableIndent, disableTextAlign, nodes, parent }) {
	const unknownConverter = converters.unknown;
	return nodes.map((node, i) => {
		let converterForNode;
		if (node.type === "block") {
			converterForNode = converters?.blocks?.[node?.fields?.blockType];
			if (!converterForNode && !unknownConverter) console.error(`Lexical => JSX converter: Blocks converter: found ${node?.fields?.blockType} block, but no converter is provided`);
		} else if (node.type === "inlineBlock") {
			converterForNode = converters?.inlineBlocks?.[node?.fields?.blockType];
			if (!converterForNode && !unknownConverter) console.error(`Lexical => JSX converter: Inline Blocks converter: found ${node?.fields?.blockType} inline block, but no converter is provided`);
		} else converterForNode = converters[node.type];
		try {
			if (!converterForNode && unknownConverter) converterForNode = unknownConverter;
			let reactNode;
			if (converterForNode) reactNode = typeof converterForNode === "function" ? converterForNode({
				childIndex: i,
				converters,
				node,
				nodesToJSX: (args) => {
					return convertLexicalNodesToJSX({
						converters: args.converters ?? converters,
						disableIndent: args.disableIndent ?? disableIndent,
						disableTextAlign: args.disableTextAlign ?? disableTextAlign,
						nodes: args.nodes,
						parent: args.parent ?? {
							...node,
							parent
						}
					});
				},
				parent
			}) : converterForNode;
			else reactNode = /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "unknown node" }, i);
			const style = {};
			if (!disableTextAlign && (!Array.isArray(disableTextAlign) || !disableTextAlign?.includes(node.type))) {
				if ("format" in node && node.format) switch (node.format) {
					case "center":
						style.textAlign = "center";
						break;
					case "end":
						style.textAlign = "right";
						break;
					case "justify":
						style.textAlign = "justify";
						break;
					case "left": break;
					case "right":
						style.textAlign = "right";
						break;
					case "start":
						style.textAlign = "left";
						break;
				}
			}
			if (!disableIndent && (!Array.isArray(disableIndent) || !disableIndent?.includes(node.type))) {
				if ("indent" in node && node.indent && node.type !== "listitem") style.paddingInlineStart = `${Number(node.indent) * 40}px`;
			}
			if (/* @__PURE__ */ import_react.isValidElement(reactNode)) {
				if (style.textAlign || style.paddingInlineStart) {
					const newStyle = {
						...style,
						...reactNode?.props?.style ?? {}
					};
					return /* @__PURE__ */ import_react.cloneElement(reactNode, {
						key: i,
						style: newStyle
					});
				}
				return /* @__PURE__ */ import_react.cloneElement(reactNode, { key: i });
			}
			return reactNode;
		} catch (error) {
			console.error("Error converting lexical node to JSX:", error, "node:", node);
			return null;
		}
	}).filter(Boolean);
}
//#endregion
//#region node_modules/@payloadcms/richtext-lexical/dist/features/converters/lexicalToJSX/Component/index.js
function RichText({ className, converters, data: editorState, disableContainer, disableIndent, disableTextAlign, nodeMap }) {
	if (!editorState) return null;
	let finalConverters = {};
	if (converters) if (typeof converters === "function") finalConverters = converters({ defaultConverters: defaultJSXConverters });
	else finalConverters = converters;
	else finalConverters = defaultJSXConverters;
	const content = editorState && !Array.isArray(editorState) && typeof editorState === "object" && "root" in editorState && convertLexicalToJSX({
		converters: finalConverters,
		data: editorState,
		disableIndent,
		disableTextAlign,
		nodeMap
	});
	if (disableContainer) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: content });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: className ?? "payload-richtext",
		children: content
	});
}
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/badge-dollar-sign.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var BadgeDollarSign = createLucideIcon$1("BadgeDollarSign", [
	["path", {
		d: "M3.85 8.62a4 4 0 0 1 4.78-4.77 4 4 0 0 1 6.74 0 4 4 0 0 1 4.78 4.78 4 4 0 0 1 0 6.74 4 4 0 0 1-4.77 4.78 4 4 0 0 1-6.75 0 4 4 0 0 1-4.78-4.77 4 4 0 0 1 0-6.76Z",
		key: "3c2336"
	}],
	["path", {
		d: "M16 8h-6a2 2 0 1 0 0 4h4a2 2 0 1 1 0 4H8",
		key: "1h4pet"
	}],
	["path", {
		d: "M12 18V6",
		key: "zqpxq5"
	}]
]);
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/briefcase-business.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var BriefcaseBusiness = createLucideIcon$1("BriefcaseBusiness", [
	["path", {
		d: "M12 12h.01",
		key: "1mp3jc"
	}],
	["path", {
		d: "M16 6V4a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2",
		key: "1ksdt3"
	}],
	["path", {
		d: "M22 13a18.15 18.15 0 0 1-20 0",
		key: "12hx5q"
	}],
	["rect", {
		width: "20",
		height: "14",
		x: "2",
		y: "6",
		rx: "2",
		key: "i6l2r4"
	}]
]);
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/chevron-down.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var ChevronDown = createLucideIcon$1("ChevronDown", [["path", {
	d: "m6 9 6 6 6-6",
	key: "qrunsl"
}]]);
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/heart-handshake.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var HeartHandshake = createLucideIcon$1("HeartHandshake", [
	["path", {
		d: "M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z",
		key: "c3ymky"
	}],
	["path", {
		d: "M12 5 9.04 7.96a2.17 2.17 0 0 0 0 3.08c.82.82 2.13.85 3 .07l2.07-1.9a2.82 2.82 0 0 1 3.79 0l2.96 2.66",
		key: "4oyue0"
	}],
	["path", {
		d: "m18 15-2-2",
		key: "60u0ii"
	}],
	["path", {
		d: "m15 18-2-2",
		key: "6p76be"
	}]
]);
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/landmark.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var Landmark = createLucideIcon$1("Landmark", [
	["line", {
		x1: "3",
		x2: "21",
		y1: "22",
		y2: "22",
		key: "j8o0r"
	}],
	["line", {
		x1: "6",
		x2: "6",
		y1: "18",
		y2: "11",
		key: "10tf0k"
	}],
	["line", {
		x1: "10",
		x2: "10",
		y1: "18",
		y2: "11",
		key: "54lgf6"
	}],
	["line", {
		x1: "14",
		x2: "14",
		y1: "18",
		y2: "11",
		key: "380y"
	}],
	["line", {
		x1: "18",
		x2: "18",
		y1: "18",
		y2: "11",
		key: "1kevvc"
	}],
	["polygon", {
		points: "12 2 20 7 4 7",
		key: "jkujk7"
	}]
]);
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/megaphone.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var Megaphone = createLucideIcon$1("Megaphone", [["path", {
	d: "m3 11 18-5v12L3 14v-3z",
	key: "n962bs"
}], ["path", {
	d: "M11.6 16.8a3 3 0 1 1-5.8-1.6",
	key: "1yl0tm"
}]]);
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/shield-check.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var ShieldCheck = createLucideIcon$1("ShieldCheck", [["path", {
	d: "M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z",
	key: "oel41y"
}], ["path", {
	d: "m9 12 2 2 4-4",
	key: "dzmm74"
}]]);
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/users.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var Users = createLucideIcon$1("Users", [
	["path", {
		d: "M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2",
		key: "1yyitq"
	}],
	["circle", {
		cx: "9",
		cy: "7",
		r: "4",
		key: "nufk8"
	}],
	["path", {
		d: "M22 21v-2a4 4 0 0 0-3-3.87",
		key: "kshegd"
	}],
	["path", {
		d: "M16 3.13a4 4 0 0 1 0 7.75",
		key: "1da9ce"
	}]
]);
//#endregion
//#region node_modules/lucide-react/dist/esm/icons/vote.js
/**
* @license lucide-react v0.468.0 - ISC
*
* This source code is licensed under the ISC license.
* See the LICENSE file in the root directory of this source tree.
*/
var Vote = createLucideIcon$1("Vote", [
	["path", {
		d: "m9 12 2 2 4-4",
		key: "dzmm74"
	}],
	["path", {
		d: "M5 7c0-1.1.9-2 2-2h10a2 2 0 0 1 2 2v12H5V7Z",
		key: "1ezoue"
	}],
	["path", {
		d: "M22 19H2",
		key: "nuriw5"
	}]
]);
//#endregion
//#region components/site/Countdown.tsx
function remaining(target) {
	const ms = Math.max(0, new Date(target).getTime() - Date.now());
	return {
		days: Math.floor(ms / 864e5),
		hours: Math.floor(ms / 36e5 % 24),
		minutes: Math.floor(ms / 6e4 % 60),
		seconds: Math.floor(ms / 1e3 % 60)
	};
}
var initialTime = {
	days: 0,
	hours: 0,
	minutes: 0,
	seconds: 0
};
function Countdown({ target }) {
	const [time, setTime] = (0, import_react.useState)(initialTime);
	(0, import_react.useEffect)(() => {
		setTime(remaining(target));
		const timer = window.setInterval(() => setTime(remaining(target)), 1e3);
		return () => window.clearInterval(timer);
	}, [target]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
		className: "puck-countdown",
		"aria-label": `${time.days} days, ${time.hours} hours, ${time.minutes} minutes until the convention`,
		children: Object.entries(time).map(([label, value]) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: String(value).padStart(2, "0") }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: label })] }, label))
	});
}
//#endregion
//#region puck/lexical-value.ts
function emptyLexicalValue(text = "") {
	return { root: {
		type: "root",
		direction: "ltr",
		format: "",
		indent: 0,
		version: 1,
		children: [{
			type: "paragraph",
			direction: "ltr",
			format: "",
			indent: 0,
			version: 1,
			textFormat: 0,
			textStyle: "",
			children: text ? [{
				type: "text",
				detail: 0,
				format: 0,
				mode: "normal",
				style: "",
				text,
				version: 1
			}] : []
		}]
	} };
}
function normalizeLexicalValue(value) {
	if (typeof value === "string") return emptyLexicalValue(value);
	if (value && typeof value === "object" && !Array.isArray(value) && "root" in value && value.root && typeof value.root === "object") return value;
	return emptyLexicalValue();
}
//#endregion
//#region puck/layout-utils.mjs
function layoutColumnCount(layout) {
	return layout === "one" ? 1 : layout === "three" ? 3 : layout === "four" ? 4 : 2;
}
//#endregion
//#region puck/list-values.js
function lines(value) {
	if (typeof value === "string") return value.split(/\r?\n/).map((item) => item.trim()).filter(Boolean);
	if (Array.isArray(value)) return value;
	return [];
}
function pair(value) {
	if (typeof value !== "string") return ["", ""];
	const separator = value.includes("—") ? "—" : value.includes(" – ") ? " – " : " - ";
	const index = value.indexOf(separator);
	if (index < 0) return [value.trim(), ""];
	return [value.slice(0, index).trim(), value.slice(index + separator.length).trim()];
}
function text$1(record, keys) {
	for (const key of keys) {
		const value = record[key];
		if (typeof value === "string" && value.trim()) return value.trim();
	}
	return "";
}
/** @param {unknown} value */
function normalizeImportantDates(value) {
	return lines(value).map((item) => {
		if (typeof item === "string") {
			const [date, label] = pair(item);
			return {
				date,
				label
			};
		}
		if (!item || typeof item !== "object" || Array.isArray(item)) return null;
		return {
			date: text$1(item, ["date", "when"]),
			label: text$1(item, [
				"label",
				"title",
				"name",
				"details",
				"value"
			])
		};
	}).filter((item) => item && (item.date || item.label));
}
/** @param {unknown} value */
function normalizePastEvents(value) {
	return lines(value).map((item) => {
		if (typeof item === "string") {
			const [title, date] = pair(item);
			return {
				title,
				date
			};
		}
		if (!item || typeof item !== "object" || Array.isArray(item)) return null;
		return {
			title: text$1(item, [
				"title",
				"name",
				"label",
				"value"
			]),
			date: text$1(item, ["date", "when"]),
			image: item.image && typeof item.image === "object" && !Array.isArray(item.image) ? item.image : null
		};
	}).filter((item) => item && (item.title || item.date));
}
/** @param {unknown} value */
function normalizeMeetings(value) {
	return lines(value).map((item) => {
		if (typeof item === "string") {
			const [name, location] = pair(item);
			return {
				name,
				location
			};
		}
		if (!item || typeof item !== "object" || Array.isArray(item)) return null;
		const meeting = {
			name: text$1(item, [
				"name",
				"title",
				"label",
				"value"
			]),
			location: text$1(item, [
				"location",
				"place",
				"state",
				"region"
			])
		};
		const date = text$1(item, ["date", "when"]);
		const url = text$1(item, [
			"url",
			"href",
			"link"
		]);
		return {
			...meeting,
			...date ? { date } : {},
			...url ? { url } : {}
		};
	}).filter((item) => item && (item.name || item.location));
}
/** @param {unknown} value */
function normalizeUpcomingEvents(value) {
	return lines(value).map((item) => {
		if (typeof item === "string") {
			const [title, date] = pair(item);
			return {
				title,
				date
			};
		}
		if (!item || typeof item !== "object" || Array.isArray(item)) return null;
		return {
			title: text$1(item, [
				"title",
				"name",
				"label",
				"value"
			]),
			date: text$1(item, ["date", "when"])
		};
	}).filter((item) => item && (item.title || item.date));
}
/** @param {unknown} value */
function normalizeScheduleMeetings(value) {
	return lines(value).map((item) => {
		if (!item || typeof item !== "object" || Array.isArray(item)) return null;
		return {
			day: text$1(item, ["day", "date"]),
			time: text$1(item, ["time"]),
			name: text$1(item, [
				"name",
				"title",
				"label"
			]),
			url: text$1(item, [
				"url",
				"href",
				"link"
			]),
			location: text$1(item, [
				"location",
				"venue",
				"place"
			]),
			city: text$1(item, ["city", "town"]),
			attendance: text$1(item, ["attendance", "format"]),
			address: text$1(item, ["address"]),
			types: text$1(item, ["types", "meetingTypes"])
		};
	}).filter((item) => item && (item.name || item.location));
}
//#endregion
//#region puck/ct-meeting-schedule-data.ts
var ctMeetingSchedule = [
	{
		day: "Sunday",
		time: "7:30 PM",
		name: "A Vision For Us",
		url: "https://ct-aa.org/meetings/do-it-sober-young-peoples-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Sacred Heart Church School",
		city: "Danbury",
		attendance: "In-person and Online",
		address: "12 Cottage St",
		types: "Discussion, Online Meeting, Open, Wheelchair Access, Wheelchair-Accessible Bathroom, Young People"
	},
	{
		day: "Sunday",
		time: "7:30 PM",
		name: "UConn Young Peoples",
		url: "https://ct-aa.org/meetings/sunday-nite-live-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Storrs Congregational Church",
		city: "Storrs",
		attendance: "In-person",
		address: "2 N Eagleville Rd",
		types: "Discussion, Open, Speaker, Wheelchair Access, Young People"
	},
	{
		day: "Monday",
		time: "6:00 PM",
		name: "Y.A.N.A. Young Peoples Group",
		url: "https://ct-aa.org/meetings/y-a-n-a-young-peoples/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Wolcott Activ & Learning Center",
		city: "Wolcott",
		attendance: "In-person",
		address: "48 Todd Rd",
		types: "Discussion, Open, Wheelchair Access, Wheelchair-Accessible Bathroom, Young People"
	},
	{
		day: "Monday",
		time: "7:00 PM",
		name: "Coventry Young Peoples Group",
		url: "https://ct-aa.org/meetings/coventry-young-peoples-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Second Congregational Church",
		city: "Coventry",
		attendance: "In-person",
		address: "1746 Boston Turnpike",
		types: "Discussion, Open, Young People"
	},
	{
		day: "Monday",
		time: "8:00 PM",
		name: "Beyond Your Wildest Dreams Group",
		url: "https://ct-aa.org/meetings/beyond-your-wildest-dreams-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "First Baptist Church",
		city: "New Haven",
		attendance: "In-person",
		address: "205 Edwards St",
		types: "Closed, Step Meeting, Young People"
	},
	{
		day: "Tuesday",
		time: "7:00 PM",
		name: "Forever Young Group",
		url: "https://ct-aa.org/meetings/forever-young-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Bethany Lutheran Church",
		city: "West Hartford",
		attendance: "In-person",
		address: "1655 Boulevard",
		types: "Discussion, Open, Wheelchair Access, Young People"
	},
	{
		day: "Tuesday",
		time: "7:00 PM",
		name: "Choices Young Peoples Group",
		url: "https://ct-aa.org/meetings/choices-young-peoples-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Walnut Hill Community Church",
		city: "Waterbury",
		attendance: "In-person",
		address: "274 Bunker Hill Ave",
		types: "Discussion, Open, Young People"
	},
	{
		day: "Tuesday",
		time: "7:30 PM",
		name: "Canton Young People's-Open Topic Discussion",
		url: "https://ct-aa.org/meetings/canton-young-peoples-open-topic-discussion/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "First Congregational Church",
		city: "Canton",
		attendance: "In-person",
		address: "184 Cherry Brook Rd",
		types: "Discussion, Open, Young People"
	},
	{
		day: "Tuesday",
		time: "7:30 PM",
		name: "Your Not Alone",
		url: "https://ct-aa.org/meetings/tuesday-night-young-peoples-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "ZOOM ID: 463 705 209, Password: 123795",
		city: "Online",
		attendance: "Online",
		address: "",
		types: "Discussion, Online Meeting, Open, Young People"
	},
	{
		day: "Tuesday",
		time: "7:30 PM",
		name: "The Steps to Serenity",
		url: "https://ct-aa.org/meetings/tuesday-step-meeting-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Grace Lutheran Church",
		city: "Middletown",
		attendance: "In-person",
		address: "1055 Randolph Rd",
		types: "Closed, Discussion, Step Meeting, Wheelchair Access, Wheelchair-Accessible Bathroom, Young People"
	},
	{
		day: "Tuesday",
		time: "8:00 PM",
		name: "Canaan Young Peoples Group",
		url: "https://ct-aa.org/meetings/canaan-young-peoples-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Canaan United Methodist Church",
		city: "Canaan",
		attendance: "In-person",
		address: "4 Church St",
		types: "Open, Speaker, Young People"
	},
	{
		day: "Wednesday",
		time: "7:00 PM",
		name: "Young People's Greenwich",
		url: "https://ct-aa.org/meetings/young-peoples-greenwich/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "YWCA",
		city: "Greenwich",
		attendance: "In-person",
		address: "259 E Putnam Ave",
		types: "Wheelchair Access, Young People"
	},
	{
		day: "Thursday",
		time: "7:00 PM",
		name: "Deep River Young People's Meeting",
		url: "https://ct-aa.org/meetings/deep-river-beginners-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "First Congregational Church",
		city: "Deep River",
		attendance: "In-person",
		address: "1 Church St",
		types: "Discussion, Newcomer, Open, Young People"
	},
	{
		day: "Thursday",
		time: "7:30 PM",
		name: "Young and the Restless Group",
		url: "https://ct-aa.org/meetings/young-restless/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Long Hill United Methodist Church",
		city: "Trumbull",
		attendance: "In-person and Online",
		address: "6358 Main St",
		types: "Discussion, Online Meeting, Open, Wheelchair Access, Young People"
	},
	{
		day: "Friday",
		time: "7:00 PM",
		name: "Guilford Young People's",
		url: "https://ct-aa.org/meetings/guilford-young-peoples/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Guilford Athletic Center",
		city: "Guilford",
		attendance: "In-person",
		address: "391 Soundview Rd",
		types: "Discussion, Open, Young People"
	},
	{
		day: "Friday",
		time: "7:00 PM",
		name: "Enfield Young People's Group",
		url: "https://ct-aa.org/meetings/enfield-young-peoples/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "United Methodist Church",
		city: "Enfield",
		attendance: "In-person",
		address: "330 Hazard Ave",
		types: "Discussion, Open, Speaker, Young People"
	},
	{
		day: "Friday",
		time: "7:30 PM",
		name: "Young Grenadiers",
		url: "https://ct-aa.org/meetings/steppin-free-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "St James Episcopal Church",
		city: "Farmington",
		attendance: "In-person",
		address: "3 Mountain Rd",
		types: "Open, Step Meeting, Young People"
	},
	{
		day: "Friday",
		time: "8:00 PM",
		name: "The Joy of Living Group",
		url: "https://ct-aa.org/meetings/the-joy-of-living/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Bible Church of Waterbury",
		city: "Waterbury",
		attendance: "In-person",
		address: "240 Dwight St",
		types: "Discussion, Open, Speaker, Step Meeting, Wheelchair Access, Wheelchair-Accessible Bathroom, Young People"
	},
	{
		day: "Saturday",
		time: "9:30 AM",
		name: "Saturday Morning BYOC Group",
		url: "https://ct-aa.org/meetings/saturday-morning-byoc-group/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Colchester Federated Church",
		city: "Colchester",
		attendance: "In-person and Online",
		address: "60 Main St",
		types: "Discussion, Online Meeting, Open, Step Meeting, Young People"
	},
	{
		day: "Saturday",
		time: "5:30 PM",
		name: "Never Too Young",
		url: "https://ct-aa.org/meetings/never-too-young-2/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "Greens Farms Church",
		city: "Westport",
		attendance: "In-person",
		address: "71 Hillandale Rd",
		types: "Discussion, Newcomer, Open, Young People"
	},
	{
		day: "Saturday",
		time: "7:00 PM",
		name: "Young and The Restless SHU",
		url: "https://ct-aa.org/meetings/young-the-restless-shu/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "SHU Campus",
		city: "Fairfield",
		attendance: "In-person",
		address: "5401 Park Ave",
		types: "Discussion, English, Literature, Open, Speaker, Wheelchair Access, Wheelchair-Accessible Bathroom, Young People"
	},
	{
		day: "Saturday",
		time: "7:00 PM",
		name: "Never Too Young Group",
		url: "https://ct-aa.org/meetings/never-too-young/?tsml-day=any&tsml-distance=100&tsml-mode=location&tsml-query=06106&tsml-type=Y",
		location: "South Congregational Church",
		city: "South Glastonbury",
		attendance: "In-person",
		address: "949 Main St",
		types: "Discussion, Open, Wheelchair Access, Wheelchair-Accessible Bathroom, Young People"
	}
];
//#endregion
//#region puck/campaign-alt-definitions.ts
var standardPresentations = [
	"contained",
	"wide",
	"fullBleed"
];
var campaignAltDefinitions = [
	{
		type: "HeroAlt",
		sourceSlug: "heroFamily",
		label: "Hero Alt",
		kind: "hero",
		palette: "sections",
		variants: [
			"splitSpotlight",
			"centeredStatement",
			"ribbonOverlay",
			"mediaFocus",
			"campaignPoster",
			"photoOverlay",
			"splitCandidate",
			"officialMasthead",
			"grassrootsAction",
			"civicOutdoors",
			"civicOutdoorsPanel"
		],
		presentations: standardPresentations
	},
	{
		type: "AboutAlt",
		sourceSlug: "aboutFamily",
		label: "Bio Section",
		kind: "profile",
		palette: "sections",
		variants: [
			"splitProfile",
			"storyStack",
			"checklist"
		]
	},
	{
		type: "PalmCardMastheadAlt",
		sourceSlug: "palmCardMasthead",
		label: "Palm Card Masthead",
		kind: "hero",
		palette: "sections",
		variants: [
			"poster",
			"namePlate",
			"photoSplit"
		],
		presentations: standardPresentations
	},
	{
		type: "PalmCardPointsAlt",
		sourceSlug: "palmCardPoints",
		label: "Palm Card Points",
		kind: "cards",
		palette: "sections",
		variants: [
			"checklist",
			"iconCards",
			"twoColumn"
		],
		presentations: standardPresentations
	},
	{
		type: "PalmCardBioAlt",
		sourceSlug: "palmCardBio",
		label: "Palm Card Bio",
		kind: "profile",
		palette: "sections",
		variants: [
			"shortBio",
			"profileSplit",
			"quoteBio"
		],
		presentations: standardPresentations
	},
	{
		type: "PalmCardGalleryAlt",
		sourceSlug: "palmCardGallery",
		label: "Palm Card Gallery",
		kind: "media",
		palette: "sections",
		variants: [
			"photoStrip",
			"featureCollage",
			"storyPanel"
		],
		presentations: standardPresentations
	},
	{
		type: "PalmCardContactAlt",
		sourceSlug: "palmCardContact",
		label: "Palm Card Contact",
		kind: "cta",
		palette: "sections",
		variants: [
			"contactPanel",
			"footerBand",
			"splitAction"
		],
		presentations: standardPresentations
	},
	{
		type: "PalmCardAlt",
		sourceSlug: "palmCardBlock",
		label: "Palm Card Content",
		kind: "cards",
		palette: "sections",
		variants: [
			"compactList",
			"splitProfile",
			"issueBriefs"
		],
		presentations: standardPresentations
	},
	{
		type: "FeatureFamilyAlt",
		sourceSlug: "featureFamily",
		label: "Feature Family Alt",
		kind: "cards",
		palette: "sections",
		variants: [
			"cards",
			"editorial",
			"tabs",
			"stacked",
			"alternatingBand"
		],
		presentations: standardPresentations
	},
	{
		type: "CardsGridAlt",
		sourceSlug: "cardsGrid",
		label: "Issues Cards",
		kind: "cards",
		palette: "sections",
		variants: [
			"imageOverlay",
			"infoCards",
			"iconCards",
			"editorialGrid"
		],
		presentations: standardPresentations
	},
	{
		type: "ComposableCardsAlt",
		sourceSlug: "composableCards",
		label: "Composable Cards Alt",
		kind: "cards",
		palette: "sections",
		variants: [
			"imageOverlay",
			"infoCards",
			"iconCards",
			"editorialGrid"
		],
		presentations: standardPresentations,
		nestedCollection: "cards"
	},
	{
		type: "TestimonialAlt",
		sourceSlug: "testimonialBlock",
		label: "Quote / Testimonial",
		kind: "proof",
		palette: "sections",
		variants: [
			"contained",
			"editorial",
			"spotlight"
		],
		presentations: standardPresentations
	},
	{
		type: "StatsAlt",
		sourceSlug: "statsBlock",
		label: "Stats Alt",
		kind: "proof",
		palette: "sections",
		variants: [
			"compact",
			"panel",
			"ranked"
		],
		presentations: standardPresentations
	},
	{
		type: "LogoCloudAlt",
		sourceSlug: "logoCloudBlock",
		label: "Logo Cloud Alt",
		kind: "proof",
		palette: "sections",
		variants: [
			"grid",
			"officialEndorsement",
			"band"
		],
		presentations: standardPresentations
	},
	{
		type: "ProofFamilyAlt",
		sourceSlug: "proofFamily",
		label: "Proof Family Alt",
		kind: "proof",
		palette: "sections",
		variants: [
			"testimonialCards",
			"statsStrip",
			"logoCloud"
		]
	},
	{
		type: "CTAFamilyAlt",
		sourceSlug: "ctaFamily",
		label: "CTA Family Alt",
		kind: "cta",
		palette: "sections",
		variants: [
			"boxed",
			"banner",
			"split"
		],
		presentations: standardPresentations
	},
	{
		type: "ColumnsAlt",
		sourceSlug: "columnsBlock",
		label: "Columns Alt",
		kind: "columns",
		palette: "rows",
		variants: [
			"oneColumn",
			"twoEqual",
			"oneThirdTwoThirds",
			"twoThirdsOneThird",
			"threeEqual",
			"fourEqual"
		],
		presentations: standardPresentations,
		nestedCollection: "columns"
	},
	{
		type: "TabbedContentAlt",
		sourceSlug: "tabbedContent",
		label: "Tabbed Content Alt",
		kind: "tabs",
		palette: "sections",
		variants: [
			"horizontal",
			"sideTabs",
			"accordion"
		],
		presentations: standardPresentations,
		nestedCollection: "tabs"
	},
	{
		type: "MediaBlockAlt",
		sourceSlug: "mediaBlock",
		label: "Media Block Alt",
		kind: "media",
		palette: "elements",
		variants: ["default"]
	},
	{
		type: "MediaGalleryAlt",
		sourceSlug: "mediaGallery",
		label: "Media Gallery Alt",
		kind: "media",
		palette: "sections",
		variants: [
			"auto",
			"small",
			"medium",
			"large"
		]
	},
	{
		type: "FormBlockAlt",
		sourceSlug: "formBlock",
		label: "Form Block Alt",
		kind: "form",
		palette: "sections",
		variants: [
			"standard",
			"campaign",
			"split"
		]
	},
	{
		type: "MenuBlockAlt",
		sourceSlug: "menuBlock",
		label: "Menu Block Alt",
		kind: "menu",
		palette: "sections",
		variants: ["default"]
	},
	{
		type: "MeetingScheduleAlt",
		sourceSlug: "meetingSchedule",
		label: "Meeting Schedule Alt",
		kind: "schedule",
		palette: "sections",
		variants: ["default"]
	},
	{
		type: "ArchiveAlt",
		sourceSlug: "archive",
		label: "Archive Alt",
		kind: "archive",
		palette: "sections",
		variants: ["default"]
	},
	{
		type: "FacebookFeedAlt",
		sourceSlug: "facebookFeed",
		label: "Facebook Feed Alt",
		kind: "feed",
		palette: "sections",
		variants: [
			"cards",
			"mosaic",
			"staggered",
			"carousel",
			"flyerLightbox"
		]
	},
	{
		type: "BannerAlt",
		sourceSlug: "banner",
		label: "Banner Alt",
		kind: "banner",
		palette: "sections",
		variants: [
			"info",
			"warning",
			"error",
			"success"
		]
	},
	{
		type: "ButtonRowAlt",
		sourceSlug: "buttonRow",
		label: "Button Row Alt",
		kind: "cta",
		palette: "elements",
		variants: ["default"]
	},
	{
		type: "InlineRichTextAlt",
		sourceSlug: "inlineRichText",
		label: "Inline Rich Text Alt",
		kind: "content",
		palette: "elements",
		variants: ["default"],
		presentations: standardPresentations
	},
	{
		type: "SimpleFormAlt",
		sourceSlug: "simpleFormBlock",
		label: "Simple Form Alt",
		kind: "form",
		palette: "sections",
		variants: ["default"]
	},
	{
		type: "TextElementAlt",
		sourceSlug: "textElement",
		label: "Text Element Alt",
		kind: "content",
		palette: "elements",
		variants: ["default"]
	}
];
var campaignAltTypes = campaignAltDefinitions.map((definition) => definition.type);
var campaignAltTypesByPalette = {
	sections: campaignAltDefinitions.filter((definition) => definition.palette === "sections").map((definition) => definition.type),
	rows: campaignAltDefinitions.filter((definition) => definition.palette === "rows").map((definition) => definition.type),
	elements: campaignAltDefinitions.filter((definition) => definition.palette === "elements").map((definition) => definition.type)
};
var puck_module_default = {
	canvas: "_canvas_fm1hj_1",
	shell: "_shell_fm1hj_85",
	eyebrow: "_eyebrow_fm1hj_137",
	eyebrowDark: "_eyebrowDark_fm1hj_139",
	eventLabel: "_eventLabel_fm1hj_141",
	lede: "_lede_fm1hj_161",
	body: "_body_fm1hj_163",
	button: "_button_fm1hj_175",
	actions: "_actions_fm1hj_215",
	hero: "_hero_fm1hj_221",
	heroBackground: "_heroBackground_fm1hj_279",
	heroBackgroundPoster: "_heroBackgroundPoster_fm1hj_297",
	heroBackgroundVideo: "_heroBackgroundVideo_fm1hj_299",
	heroCopy: "_heroCopy_fm1hj_315",
	meta: "_meta_fm1hj_337",
	heroArt: "_heroArt_fm1hj_371",
	heroSurf: "_heroSurf_fm1hj_1",
	dark: "_dark_fm1hj_528",
	twoCol: "_twoCol_fm1hj_554",
	artPlaceholder: "_artPlaceholder_fm1hj_558",
	advisory: "_advisory_fm1hj_638",
	light: "_light_fm1hj_664",
	meeting: "_meeting_fm1hj_690",
	dates: "_dates_fm1hj_702",
	events: "_events_fm1hj_724",
	eventBlend: "_eventBlend_fm1hj_740",
	upcoming: "_upcoming_fm1hj_744",
	flyer: "_flyer_fm1hj_746",
	futureEvents: "_futureEvents_fm1hj_802",
	archive: "_archive_fm1hj_808",
	directory: "_directory_fm1hj_837",
	schedule: "_schedule_fm1hj_892",
	scheduleIntroduction: "_scheduleIntroduction_fm1hj_894",
	scheduleTable: "_scheduleTable_fm1hj_895",
	scheduleHeader: "_scheduleHeader_fm1hj_896",
	scheduleRow: "_scheduleRow_fm1hj_897",
	scheduleName: "_scheduleName_fm1hj_904",
	scheduleDetailsToggle: "_scheduleDetailsToggle_fm1hj_906",
	scheduleDetails: "_scheduleDetails_fm1hj_906",
	cta: "_cta_fm1hj_919",
	ctaImage: "_ctaImage_fm1hj_959",
	freeText: "_freeText_fm1hj_989",
	richText: "_richText_fm1hj_993",
	imageBlock: "_imageBlock_fm1hj_1007",
	imageEmpty: "_imageEmpty_fm1hj_1017",
	campaignAlt: "_campaignAlt_fm1hj_1192",
	campaignAltBackground: "_campaignAltBackground_fm1hj_1195",
	campaignAltCopy: "_campaignAltCopy_fm1hj_1197",
	campaignAltItems: "_campaignAltItems_fm1hj_1203",
	campaignAltGrid: "_campaignAltGrid_fm1hj_1204",
	campaignAltHeader: "_campaignAltHeader_fm1hj_1216",
	campaignAltLongCopy: "_campaignAltLongCopy_fm1hj_1220",
	campaignAltProfileMedia: "_campaignAltProfileMedia_fm1hj_1221",
	campaignAltIcon: "_campaignAltIcon_fm1hj_1223",
	campaignAltQuote: "_campaignAltQuote_fm1hj_1225",
	heroAlt: "_heroAlt_fm1hj_1230",
	heroAltMedia: "_heroAltMedia_fm1hj_1240",
	heroAltLogo: "_heroAltLogo_fm1hj_1242",
	heroAltHighlight: "_heroAltHighlight_fm1hj_1243",
	bioSectionAlt: "_bioSectionAlt_fm1hj_1293",
	bioHighlights: "_bioHighlights_fm1hj_1294",
	bioSectionCopy: "_bioSectionCopy_fm1hj_1299",
	issuesCardsAltGrid: "_issuesCardsAltGrid_fm1hj_1303",
	issuesCardsAlt: "_issuesCardsAlt_fm1hj_1303",
	palmPointsGrid: "_palmPointsGrid_fm1hj_1322",
	palmPointsAlt: "_palmPointsAlt_fm1hj_1325",
	palmBioAlt: "_palmBioAlt_fm1hj_1329",
	testimonialsAlt: "_testimonialsAlt_fm1hj_1337",
	testimonialsAltGrid: "_testimonialsAltGrid_fm1hj_1339",
	palmContentAlt: "_palmContentAlt_fm1hj_1353",
	palmContentGrid: "_palmContentGrid_fm1hj_1354",
	palmContactAlt: "_palmContactAlt_fm1hj_1365",
	palmContactDetails: "_palmContactDetails_fm1hj_1367",
	palmContactLinks: "_palmContactLinks_fm1hj_1370",
	palmContactQr: "_palmContactQr_fm1hj_1373",
	palmContactDisclaimer: "_palmContactDisclaimer_fm1hj_1376",
	campaignAltTabs: "_campaignAltTabs_fm1hj_1421",
	nestedDropZone: "_nestedDropZone_fm1hj_1463",
	nestedSlot: "_nestedSlot_fm1hj_1464",
	issuesSection: "_issuesSection_fm1hj_1468",
	issueCards: "_issueCards_fm1hj_1469",
	resultsStats: "_resultsStats_fm1hj_1470",
	supporterLogos: "_supporterLogos_fm1hj_1471",
	actionTabs: "_actionTabs_fm1hj_1472",
	mediaGallery: "_mediaGallery_fm1hj_1473",
	contentRow: "_contentRow_fm1hj_1474",
	issuesGrid: "_issuesGrid_fm1hj_1477",
	issueCardGrid: "_issueCardGrid_fm1hj_1494",
	cardLink: "_cardLink_fm1hj_1501",
	quote: "_quote_fm1hj_1508",
	quoteMark: "_quoteMark_fm1hj_1512",
	tabList: "_tabList_fm1hj_1534",
	tabPanels: "_tabPanels_fm1hj_1537",
	tabPanel: "_tabPanel_fm1hj_1537",
	buttonRow: "_buttonRow_fm1hj_1560",
	navigation: "_navigation_fm1hj_1563",
	followLinks: "_followLinks_fm1hj_1564",
	headline: "_headline_fm1hj_1566",
	divider: "_divider_fm1hj_1567",
	bulletedList: "_bulletedList_fm1hj_1569",
	inlineForm: "_inlineForm_fm1hj_1570",
	imageCaption: "_imageCaption_fm1hj_1574",
	video: "_video_fm1hj_1574",
	embed: "_embed_fm1hj_1578",
	payPal: "_payPal_fm1hj_1579",
	standaloneCountdown: "_standaloneCountdown_fm1hj_1581",
	builderSection: "_builderSection_fm1hj_1583",
	builderColumn: "_builderColumn_fm1hj_1587"
};
var editableFieldsByType = {
	HeroCountdown: [
		"eyebrow",
		"heading",
		"body",
		"eventDate",
		"eventLocation",
		"registerLabel",
		"hotelLabel"
	],
	About: [
		"eyebrow",
		"heading",
		"body",
		"advisoryHeading",
		"advisoryBody"
	],
	MeetingInfo: [
		"eyebrow",
		"heading",
		"body",
		"date",
		"time",
		"location",
		"actionLabel"
	],
	Events: [
		"eyebrow",
		"heading",
		"upcomingLabel",
		"upcomingTitle",
		"upcomingBody",
		"upcomingDate",
		"upcomingLocation"
	],
	MeetingDirectory: [
		"eyebrow",
		"heading",
		"body"
	],
	CTMeetingSchedule: ["heading", "introduction"],
	CallToAction: [
		"eyebrow",
		"heading",
		"body",
		"primaryLabel",
		"secondaryLabel"
	],
	Image: ["caption"],
	RichText: ["content"],
	FreeText: ["text"],
	Text: ["text"],
	Button: ["label"],
	Countdown: ["label"],
	Section: ["heading"],
	Column: ["label"],
	ProgramSchedule: ["heading", "introduction"],
	ButtonRow: ["primaryLabel", "secondaryLabel"],
	IssuesSection: [
		"eyebrow",
		"heading",
		"body"
	],
	IssueCards: ["heading", "intro"],
	QuoteBlock: [
		"heading",
		"quote",
		"attribution",
		"role"
	],
	ResultsStats: ["heading", "intro"],
	SupporterLogos: ["heading", "intro"],
	ActionTabs: ["heading", "intro"],
	MediaGallery: ["heading", "intro"],
	Navigation: ["brand"],
	Headline: ["text"],
	Divider: [],
	FollowLinks: ["heading"],
	BulletedList: [],
	InlineForm: [
		"heading",
		"intro",
		"submitLabel"
	],
	ImageCaption: ["caption"],
	Video: ["caption"],
	Embed: ["title"],
	PayPal: ["label", "amount"],
	ContentRow: [],
	Row: [],
	RowOneColumn: [],
	RowTwoColumns: [],
	RowLeftWide: [],
	RowRightWide: [],
	RowThreeColumns: [],
	RowFourColumns: [],
	...Object.fromEntries(campaignAltDefinitions.map((definition) => {
		const fields = [
			"eyebrow",
			"heading",
			"body",
			"primaryLabel",
			"secondaryLabel"
		];
		if ([
			"AboutAlt",
			"CardsGridAlt",
			"PalmCardPointsAlt",
			"TestimonialAlt",
			"PalmCardAlt",
			"PalmCardContactAlt"
		].includes(definition.type)) fields.push("intro");
		if (["PalmCardBioAlt", "PalmCardAlt"].includes(definition.type)) fields.push("quote", "quoteAttribution");
		if (definition.type === "HeroAlt") fields.push("highlightTitle", "highlightText");
		if (definition.type === "PalmCardContactAlt") fields.push("disclaimer");
		return [definition.type, fields];
	}))
};
var text = (label) => ({
	type: "text",
	label,
	contentEditable: true
});
var area = (label) => ({
	type: "textarea",
	label,
	contentEditable: true
});
var plainText = (label) => ({
	type: "text",
	label
});
var backgroundDarknessField = {
	type: "custom",
	label: "Background darkness",
	render: ({ value, onChange, readOnly }) => {
		const darkness = Number.isFinite(value) ? Math.min(100, Math.max(0, value)) : 45;
		return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			style: {
				alignItems: "center",
				display: "grid",
				gap: 10,
				gridTemplateColumns: "1fr 52px"
			},
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				"aria-label": "Background darkness",
				disabled: readOnly,
				max: 100,
				min: 0,
				onChange: (event) => onChange(Number(event.currentTarget.value)),
				step: 1,
				type: "range",
				value: darkness
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("output", {
				style: {
					fontVariantNumeric: "tabular-nums",
					textAlign: "right"
				},
				children: [darkness, "%"]
			})]
		});
	}
};
function normalizeMedia(value) {
	if (typeof value === "string" && value.trim()) return {
		url: value.trim(),
		alt: ""
	};
	if (!value || typeof value !== "object" || Array.isArray(value)) return null;
	const record = value;
	if (typeof record.url !== "string" || !record.url.trim()) return null;
	return {
		id: typeof record.id === "string" || typeof record.id === "number" ? record.id : void 0,
		url: record.url,
		alt: typeof record.alt === "string" ? record.alt : "",
		filename: typeof record.filename === "string" ? record.filename : void 0,
		mimeType: typeof record.mimeType === "string" ? record.mimeType : void 0,
		width: typeof record.width === "number" ? record.width : void 0,
		height: typeof record.height === "number" ? record.height : void 0
	};
}
var mediaField = (label, allowVideo = false) => ({
	type: "external",
	label,
	placeholder: "Choose from Payload Media",
	showSearch: true,
	cache: { enabled: false },
	fetchList: async ({ query }) => {
		const response = await fetch("/api/media?limit=100&sort=-createdAt&depth=0", { credentials: "same-origin" });
		if (!response.ok) return [];
		const result = await response.json();
		const docs = Array.isArray(result.docs) ? result.docs : [];
		const compatibleDocs = allowVideo ? docs : docs.filter((value) => {
			if (!value || typeof value !== "object") return false;
			const mimeType = value.mimeType;
			return typeof mimeType !== "string" || mimeType.startsWith("image/");
		});
		const needle = query.trim().toLowerCase();
		if (!needle) return compatibleDocs;
		return compatibleDocs.filter((value) => {
			if (!value || typeof value !== "object") return false;
			const record = value;
			return [record.alt, record.filename].some((item) => typeof item === "string" && item.toLowerCase().includes(needle));
		});
	},
	mapProp: (value) => normalizeMedia(value) || {
		url: "",
		alt: ""
	},
	mapRow: (value) => {
		const media = normalizeMedia(value);
		const isVideo = media?.mimeType?.startsWith("video/") || /\.(?:mp4|webm)(?:\?.*)?$/i.test(media?.url || "");
		return {
			Preview: media?.url ? isVideo ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("video", {
				"aria-label": "Video preview",
				muted: true,
				preload: "metadata",
				src: media.url,
				style: {
					aspectRatio: "4 / 3",
					height: 58,
					objectFit: "cover",
					width: 76
				}
			}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
				alt: "",
				src: media.url,
				style: {
					aspectRatio: "4 / 3",
					height: 58,
					objectFit: "cover",
					width: 76
				}
			}) : "No preview",
			Name: media?.alt || media?.filename || "Untitled media",
			Size: media?.width && media?.height ? `${media.width} × ${media.height}` : "—"
		};
	},
	getItemSummary: (value) => value?.alt || value?.filename || "Selected image",
	renderFooter: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
		href: "/admin/collections/media/create",
		rel: "noreferrer",
		target: "_blank",
		children: "Upload a new image in Media"
	})
});
var richTextPlaceholder = {
	type: "custom",
	label: "Rich text",
	render: () => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, {})
};
var importantDatesField = {
	type: "array",
	label: "Important dates",
	defaultItemProps: {
		date: "",
		label: ""
	},
	arrayFields: {
		date: plainText("Date"),
		label: plainText("Details")
	},
	getItemSummary: (item) => item.label || item.date || "Important date"
};
var pastEventsField = {
	type: "array",
	label: "Past events",
	defaultItemProps: {
		title: "",
		date: "",
		image: null
	},
	arrayFields: {
		title: plainText("Title"),
		date: plainText("Date"),
		image: mediaField("Event flyer")
	},
	getItemSummary: (item) => item.title || item.date || "Past event"
};
var meetingsField = {
	type: "array",
	label: "Meetings",
	defaultItemProps: {
		name: "",
		location: "",
		date: "",
		url: ""
	},
	arrayFields: {
		name: plainText("Name"),
		location: plainText("Location"),
		date: plainText("Date"),
		url: plainText("Name URL")
	},
	getItemSummary: (item) => item.name || item.location || "Meeting"
};
var upcomingEventsField = {
	type: "array",
	label: "More upcoming events",
	defaultItemProps: {
		title: "",
		date: ""
	},
	arrayFields: {
		title: plainText("Event name"),
		date: plainText("Date")
	},
	getItemSummary: (item) => item.title || item.date || "Upcoming event"
};
var scheduleMeetingsField = {
	type: "array",
	label: "Meetings",
	defaultItemProps: {
		day: "",
		time: "",
		name: "",
		url: "",
		location: "",
		city: "",
		attendance: "",
		address: "",
		types: ""
	},
	arrayFields: {
		day: plainText("Day"),
		time: plainText("Time"),
		name: plainText("Meeting name"),
		url: plainText("Meeting URL"),
		location: plainText("Location"),
		city: plainText("City"),
		attendance: plainText("Attendance"),
		address: plainText("Address"),
		types: plainText("Meeting types")
	},
	getItemSummary: (item) => item.name || item.day || "Meeting"
};
var issueField = {
	type: "array",
	label: "Features",
	defaultItemProps: {
		title: "",
		body: "",
		icon: ""
	},
	arrayFields: {
		title: plainText("Title"),
		body: area("Description"),
		icon: plainText("Icon or short label")
	},
	getItemSummary: (item) => item.title || "Issue"
};
var nestedElementTypes = [
	"Image",
	"RichText",
	"FreeText",
	"Text",
	"Button",
	"Countdown",
	"ButtonRow",
	"Headline",
	"Divider",
	"BulletedList",
	"ImageCaption",
	"Video",
	"Embed",
	"FollowLinks",
	"InlineForm",
	"PayPal",
	"Navigation",
	...campaignAltTypesByPalette.elements
];
var issueCardsField = {
	type: "array",
	label: "Feature cards",
	defaultItemProps: {
		label: "",
		heading: "",
		body: "",
		image: null,
		linkLabel: "",
		linkUrl: ""
	},
	arrayFields: {
		label: plainText("Editor label"),
		heading: plainText("Heading"),
		body: area("Description"),
		image: mediaField("Card image"),
		linkLabel: plainText("Link label"),
		linkUrl: plainText("Link URL"),
		blocks: {
			type: "slot",
			allow: nestedElementTypes
		}
	},
	getItemSummary: (item) => item.heading || item.label || "Issue card"
};
var statsField = {
	type: "array",
	label: "Statistics",
	defaultItemProps: {
		value: "",
		label: "",
		detail: ""
	},
	arrayFields: {
		value: plainText("Value"),
		label: plainText("Label"),
		detail: area("Detail")
	},
	getItemSummary: (item) => item.label || item.value || "Result"
};
var logosField = {
	type: "array",
	label: "Partners",
	defaultItemProps: {
		name: "",
		image: null
	},
	arrayFields: {
		name: plainText("Name"),
		image: mediaField("Logo")
	},
	getItemSummary: (item) => item.name || "Supporter"
};
var tabsField = {
	type: "array",
	label: "Content tabs",
	defaultItemProps: {
		label: "",
		description: ""
	},
	arrayFields: {
		label: plainText("Tab label"),
		description: area("Description"),
		blocks: {
			type: "slot",
			allow: nestedElementTypes
		}
	},
	getItemSummary: (item) => item.label || "Action tab"
};
var galleryItemsField = {
	type: "array",
	label: "Gallery items",
	defaultItemProps: {
		image: null,
		caption: "",
		size: "medium"
	},
	arrayFields: {
		image: mediaField("Image"),
		caption: plainText("Caption"),
		size: {
			type: "select",
			label: "Tile size",
			options: [
				{
					label: "Small",
					value: "small"
				},
				{
					label: "Medium",
					value: "medium"
				},
				{
					label: "Large",
					value: "large"
				}
			]
		}
	},
	getItemSummary: (item) => item.caption || "Gallery item"
};
var contentRowFields = {
	layout: {
		type: "select",
		label: "Columns",
		options: [
			{
				label: "One column",
				value: "one"
			},
			{
				label: "Two equal",
				value: "two"
			},
			{
				label: "Left wide",
				value: "leftWide"
			},
			{
				label: "Right wide",
				value: "rightWide"
			},
			{
				label: "Three equal",
				value: "three"
			},
			{
				label: "Four equal",
				value: "four"
			}
		]
	},
	columns: {
		type: "array",
		label: "Columns",
		defaultItemProps: { label: "" },
		arrayFields: {
			label: plainText("Editor label"),
			blocks: {
				type: "slot",
				allow: nestedElementTypes
			}
		},
		getItemSummary: (item) => item.label || "Column"
	}
};
function visibleColumnCount(layout) {
	return layoutColumnCount(layout);
}
function NestedDropZone({ children, label }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: puck_module_default.nestedDropZone,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: label }), children]
	});
}
function renderZone(props, zone, label) {
	const content = props.puck?.renderDropZone ? props.puck.renderDropZone({
		zone,
		allow: nestedElementTypes,
		minEmptyHeight: 96
	}) : null;
	return props.puck?.isEditing ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NestedDropZone, {
		label,
		children: content
	}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: content });
}
function SlotContent({ content, label, fallback }) {
	if (typeof content === "function") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(content, {
		className: puck_module_default.nestedSlot,
		minEmptyHeight: 96,
		allow: nestedElementTypes
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: fallback });
}
function contentRowRender(props) {
	const columns = (props.columns || []).slice(0, visibleColumnCount(props.layout));
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: puck_module_default.contentRow,
		"data-layout": props.layout,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			className: puck_module_default.shell,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: columns.map((column, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [props.puck?.isEditing ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: column.label || `Column ${index + 1}` }) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotContent, {
				content: column.blocks,
				label: column.label || `column ${index + 1}`,
				fallback: renderZone(props, `columns.${index}.blocks`, `Drop elements in ${column.label || `column ${index + 1}`}`)
			})] }, `${column.label}-${index}`)) })
		})
	});
}
function ActionTabsRender(props) {
	const [active, setActive] = (0, import_react.useState)(0);
	const tabs = props.tabs?.length ? props.tabs : [];
	const editing = Boolean(props.puck?.isEditing);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: puck_module_default.actionTabs,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "h2",
					field: "heading",
					props,
					children: props.heading
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "p",
					className: puck_module_default.body,
					field: "intro",
					props,
					children: props.intro
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: puck_module_default.tabList,
					role: "tablist",
					children: tabs.map((tab, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						"aria-selected": active === index,
						onClick: () => setActive(index),
						role: "tab",
						type: "button",
						children: tab.label || `Tab ${index + 1}`
					}, `${tab.label}-${index}`))
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: puck_module_default.tabPanels,
					children: tabs.map((tab, index) => editing || active === index ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
						"aria-label": tab.label || `Tab ${index + 1}`,
						className: puck_module_default.tabPanel,
						hidden: !editing && active !== index,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							field: "intro",
							props,
							children: tab.description
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotContent, {
							content: tab.blocks,
							label: tab.label || `tab ${index + 1}`,
							fallback: renderZone(props, `tabs.${index}.blocks`, `Drop elements in ${tab.label || `tab ${index + 1}`}`)
						})]
					}, `${tab.label}-${index}`) : null)
				})
			]
		})
	});
}
function styleFor(props, field) {
	const values = props;
	const cap = `${field.charAt(0).toUpperCase()}${field.slice(1)}`;
	const fontSize = values[`${field}FontSize`];
	const color = values[`${field}Color`];
	const fontWeight = values[`${field}FontWeight`];
	const textAlign = values[`${field}TextAlign`];
	return {
		fontSize: typeof fontSize === "string" ? fontSize : void 0,
		color: typeof color === "string" ? color : void 0,
		fontWeight: typeof fontWeight === "string" ? fontWeight : void 0,
		textAlign: textAlign === "center" || textAlign === "right" ? textAlign : void 0,
		["--field"]: cap
	};
}
function Editable({ as: Tag = "span", props, field, children, className }) {
	const focus = () => {
		if (typeof props.id !== "string") return;
		window.parent.postMessage({
			type: "necypaa:puck-text-focus",
			componentId: props.id,
			field
		}, window.location.origin);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tag, {
		className,
		"data-puck-text-field": field,
		onFocus: focus,
		onPointerDown: focus,
		style: styleFor(props, field),
		children
	});
}
function Button({ href, children, outline = false }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
		className: puck_module_default.button,
		"data-outline": outline,
		href: href || "#",
		children
	});
}
function CTMeetingScheduleBlock(props) {
	const meetings = normalizeScheduleMeetings(props.meetings);
	const [openIndex, setOpenIndex] = (0, import_react.useState)(null);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: puck_module_default.schedule,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "h2",
					field: "heading",
					props,
					children: props.heading
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "p",
					className: puck_module_default.scheduleIntroduction,
					field: "introduction",
					props,
					children: props.introduction
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					"aria-label": "CT meeting schedule",
					className: puck_module_default.scheduleTable,
					role: "table",
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: puck_module_default.scheduleHeader,
						role: "row",
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								role: "columnheader",
								children: "Day"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								role: "columnheader",
								children: "Time"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								role: "columnheader",
								children: "Meeting"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								role: "columnheader",
								children: "Location"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								role: "columnheader",
								children: "City"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								role: "columnheader",
								children: "Attendance"
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
								"aria-label": "Details",
								role: "columnheader"
							}),
							" "
						]
					}), meetings.map((item, index) => {
						const isOpen = openIndex === index;
						return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: puck_module_default.scheduleRow,
							role: "row",
							children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									"data-label": "Day",
									role: "cell",
									children: item.day
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									"data-label": "Time",
									role: "cell",
									children: item.time
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: puck_module_default.scheduleName,
									"data-label": "Meeting",
									role: "cell",
									children: item.url ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
										href: item.url,
										rel: "noreferrer",
										target: "_blank",
										children: item.name
									}) : item.name
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									"data-label": "Location",
									role: "cell",
									children: item.location
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									"data-label": "City",
									role: "cell",
									children: item.city
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									"data-label": "Attendance",
									role: "cell",
									children: item.attendance
								}),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
									className: puck_module_default.scheduleDetailsToggle,
									role: "cell",
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
										"aria-expanded": isOpen,
										"aria-label": `${isOpen ? "Collapse" : "Expand"} details for ${item.name}`,
										onClick: () => setOpenIndex(isOpen ? null : index),
										type: "button",
										children: isOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronDown, { "aria-hidden": "true" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ChevronRight$1, { "aria-hidden": "true" })
									})
								})
							]
						}), isOpen ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: puck_module_default.scheduleDetails,
							role: "row",
							children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								role: "cell",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: "Address" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: item.address || "Not listed" })]
							}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								role: "cell",
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: "Meeting types" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: item.types || "Not listed" })]
							})]
						}) : null] }, `${item.day}-${item.time}-${item.name}-${index}`);
					})]
				})
			]
		})
	});
}
var campaignItemsField = {
	type: "array",
	label: "Items",
	defaultItemProps: {
		label: "",
		heading: "New item",
		text: "Add details",
		value: "",
		url: "",
		icon: "check",
		attribution: "",
		role: "",
		image: null
	},
	arrayFields: {
		label: plainText("Label"),
		heading: plainText("Heading"),
		text: area("Text"),
		value: plainText("Value"),
		url: plainText("URL"),
		icon: plainText("Icon"),
		attribution: plainText("Attribution"),
		role: plainText("Role"),
		image: mediaField("Image")
	},
	getItemSummary: (item) => item.heading || item.label || item.value || "Item"
};
var campaignIconOptions = [
	"none",
	"check",
	"people",
	"family",
	"mapPin",
	"dollar",
	"shieldCheck",
	"landmark",
	"briefcase",
	"vote",
	"handshake",
	"megaphone",
	"calendar"
].map((value) => ({
	label: campaignOptionLabel(value),
	value
}));
function campaignItems(label, fields, defaults) {
	return {
		type: "array",
		label,
		defaultItemProps: defaults,
		arrayFields: fields,
		getItemSummary: (item) => item.heading || item.attribution || item.label || item.text || label.replace(/s$/, "")
	};
}
var bioHighlightsField = campaignItems("Highlights", { text: area("Highlight") }, { text: "A concise proof point" });
var issueCardsAltField = campaignItems("Issue cards", {
	heading: plainText("Heading"),
	icon: {
		type: "select",
		label: "Icon",
		options: campaignIconOptions
	},
	text: area("Description"),
	image: mediaField("Background image"),
	url: plainText("Link URL")
}, {
	heading: "Community priority",
	icon: "check",
	text: "Explain the practical outcome.",
	image: null,
	url: ""
});
var palmPointsField = campaignItems("Palm card points", {
	icon: {
		type: "select",
		label: "Icon",
		options: campaignIconOptions
	},
	text: area("Point")
}, {
	icon: "check",
	text: "Palm card point"
});
var palmContentField = campaignItems("Palm card points", {
	icon: {
		type: "select",
		label: "Icon",
		options: campaignIconOptions
	},
	heading: plainText("Heading"),
	text: area("Text")
}, {
	icon: "check",
	heading: "Palm card point",
	text: ""
});
var testimonialsField = campaignItems("Testimonials", {
	text: area("Quote"),
	attribution: plainText("Attribution"),
	role: plainText("Role"),
	image: mediaField("Photo")
}, {
	text: "This work is rooted in what our community needs.",
	attribution: "Community supporter",
	role: "Resident",
	image: null
});
var contactLinksField = campaignItems("Contact links", {
	label: plainText("Label"),
	value: plainText("Display value"),
	url: plainText("URL")
}, {
	label: "Website",
	value: "example.org",
	url: "https://example.org"
});
var campaignNestedField = {
	type: "array",
	label: "Nested content",
	defaultItemProps: {
		label: "",
		heading: "New item",
		text: "Add details",
		image: null
	},
	arrayFields: {
		label: plainText("Editor label"),
		heading: plainText("Heading"),
		text: area("Text"),
		image: mediaField("Image"),
		blocks: {
			type: "slot",
			allow: nestedElementTypes
		}
	},
	getItemSummary: (item) => item.heading || item.label || "Item"
};
function campaignOptionLabel(value) {
	return value.replace(/([a-z])([A-Z])/g, "$1 $2").replace(/^./, (letter) => letter.toUpperCase());
}
function CampaignIcon({ name }) {
	const Icon = name && name !== "none" ? {
		briefcase: BriefcaseBusiness,
		calendar: CalendarDays,
		check: Check,
		dollar: BadgeDollarSign,
		family: HeartHandshake,
		handshake: HeartHandshake,
		landmark: Landmark,
		mapPin: MapPin,
		megaphone: Megaphone,
		people: Users,
		shieldCheck: ShieldCheck,
		vote: Vote
	}[name] || Check : null;
	return Icon ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		className: puck_module_default.campaignAltIcon,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Icon, { "aria-hidden": "true" })
	}) : null;
}
function CampaignSectionHeader({ props, centered = false }) {
	const intro = props.intro || props.body;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("header", {
		className: puck_module_default.campaignAltHeader,
		"data-centered": centered,
		children: [
			props.eyebrow ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
				as: "p",
				className: puck_module_default.eyebrowDark,
				field: "eyebrow",
				props,
				children: props.eyebrow
			}) : null,
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
				as: "h2",
				field: "heading",
				props,
				children: props.heading
			}),
			intro ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
				as: "p",
				field: props.intro ? "intro" : "body",
				props,
				children: intro
			}) : null
		]
	});
}
function CampaignActions({ props }) {
	if (!props.primaryLabel && !props.secondaryLabel) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: puck_module_default.actions,
		children: [props.primaryLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			href: props.primaryUrl || "#",
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
				field: "primaryLabel",
				props,
				children: props.primaryLabel
			})
		}) : null, props.secondaryLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
			href: props.secondaryUrl || "#",
			outline: true,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
				field: "secondaryLabel",
				props,
				children: props.secondaryLabel
			})
		}) : null]
	});
}
function CampaignImage({ media, className }) {
	const image = normalizeMedia(media);
	return image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("figure", {
		className,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
			alt: image.alt || "",
			src: image.url
		})
	}) : null;
}
function HeroAltRender({ props }) {
	const background = normalizeMedia(props.backgroundMedia);
	const headingLogo = normalizeMedia(props.headingLogo);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
		className: `${puck_module_default.campaignAlt} ${puck_module_default.heroAlt}`,
		"data-overlay": props.backgroundOverlay || "standard",
		"data-panel-color": props.textPanelColor || "primary",
		"data-panel-opacity": props.textPanelOpacity || "translucent",
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: [background ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
			"aria-hidden": "true",
			alt: "",
			className: puck_module_default.campaignAltBackground,
			src: background.url
		}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: puck_module_default.campaignAltCopy,
				children: [
					props.eyebrow ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
						as: "p",
						className: puck_module_default.eyebrowDark,
						field: "eyebrow",
						props,
						children: props.eyebrow
					}) : null,
					headingLogo ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
						alt: props.heading || headingLogo.alt || "Heading",
						className: puck_module_default.heroAltLogo,
						src: headingLogo.url
					}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
						as: "h1",
						field: "heading",
						props,
						children: props.heading
					}),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
						as: "p",
						field: "body",
						props,
						children: props.body
					}),
					props.highlightTitle || props.highlightText ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("aside", {
						className: puck_module_default.heroAltHighlight,
						children: [props.highlightTitle ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "strong",
							field: "highlightTitle",
							props,
							children: props.highlightTitle
						}) : null, props.highlightText ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "span",
							field: "highlightText",
							props,
							children: props.highlightText
						}) : null]
					}) : null,
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignActions, { props })
				]
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignImage, {
				className: puck_module_default.heroAltMedia,
				media: props.media
			})]
		})]
	});
}
function BioSectionRender({ props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: `${puck_module_default.campaignAlt} ${puck_module_default.bioSectionAlt}`,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignImage, {
				className: puck_module_default.campaignAltProfileMedia,
				media: props.media
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: puck_module_default.bioSectionCopy,
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignSectionHeader, { props }),
					props.body && props.intro ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
						as: "p",
						className: puck_module_default.campaignAltLongCopy,
						field: "body",
						props,
						children: props.body
					}) : null,
					props.items.length ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: puck_module_default.bioHighlights,
						children: props.items.map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignIcon, { name: item.icon }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: item.text || item.heading })] }, `${item.text}-${index}`))
					}) : null
				]
			})]
		})
	});
}
function IssuesCardsRender({ props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: `${puck_module_default.campaignAlt} ${puck_module_default.issuesCardsAlt}`,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignSectionHeader, {
				centered: props.variant !== "editorialGrid",
				props
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: puck_module_default.issuesCardsAltGrid,
				children: props.items.map((item, index) => {
					const image = normalizeMedia(item.image);
					return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", {
						"data-featured": index === 0,
						children: [image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
							alt: image.alt || "",
							src: image.url
						}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignIcon, { name: item.icon }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", { children: item.heading }),
							item.text ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: item.text }) : null,
							item.url ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
								href: item.url,
								children: "Learn more"
							}) : null
						] })]
					}, `${item.heading}-${index}`);
				})
			})]
		})
	});
}
function PalmCardPointsRender({ props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: `${puck_module_default.campaignAlt} ${puck_module_default.palmPointsAlt}`,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignSectionHeader, {
				centered: true,
				props
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: puck_module_default.palmPointsGrid,
				children: props.items.map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignIcon, { name: item.icon }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: item.text || item.heading })] }, `${item.text}-${index}`))
			})]
		})
	});
}
function PalmCardBioRender({ props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: `${puck_module_default.campaignAlt} ${puck_module_default.palmBioAlt}`,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignImage, {
				className: puck_module_default.campaignAltProfileMedia,
				media: props.media
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignSectionHeader, { props }), props.quote ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figure", {
				className: puck_module_default.campaignAltQuote,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "blockquote",
					field: "quote",
					props,
					children: props.quote
				}), props.quoteAttribution ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "figcaption",
					field: "quoteAttribution",
					props,
					children: props.quoteAttribution
				}) : null]
			}) : null] })]
		})
	});
}
function TestimonialAltRender({ props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: `${puck_module_default.campaignAlt} ${puck_module_default.testimonialsAlt}`,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignSectionHeader, {
				centered: props.variant === "contained",
				props
			}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: puck_module_default.testimonialsAltGrid,
				children: props.items.map((item, index) => {
					const image = normalizeMedia(item.image);
					return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figure", {
						"data-lead": index === 0,
						children: [
							image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
								alt: image.alt || "",
								src: image.url
							}) : null,
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("blockquote", { children: item.text }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figcaption", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: item.attribution || item.heading }), item.role || item.label ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: item.role || item.label }) : null] })
						]
					}, `${item.attribution}-${index}`);
				})
			})]
		})
	});
}
function PalmCardContentRender({ props }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: `${puck_module_default.campaignAlt} ${puck_module_default.palmContentAlt}`,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: puck_module_default.palmContentIntro,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignSectionHeader, { props }), props.quote ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figure", {
						className: puck_module_default.campaignAltQuote,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "blockquote",
							field: "quote",
							props,
							children: props.quote
						}), props.quoteAttribution ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "figcaption",
							field: "quoteAttribution",
							props,
							children: props.quoteAttribution
						}) : null]
					}) : null]
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignImage, {
					className: puck_module_default.campaignAltProfileMedia,
					media: props.media
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: puck_module_default.palmContentGrid,
					children: props.items.map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignIcon, { name: item.icon }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", { children: item.heading }), item.text ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: item.text }) : null] })] }, `${item.heading}-${index}`))
				})
			]
		})
	});
}
function PalmCardContactRender({ props }) {
	const qr = normalizeMedia(props.qrImage);
	const details = [
		{
			label: "Election day",
			value: props.electionDay
		},
		{
			label: "Early vote",
			value: props.earlyVote
		},
		{
			label: "Phone",
			value: props.phone
		},
		{
			label: "Email",
			value: props.email
		},
		{
			label: "Website",
			value: props.website
		}
	].filter((item) => item.value);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: `${puck_module_default.campaignAlt} ${puck_module_default.palmContactAlt}`,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignSectionHeader, { props }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: puck_module_default.palmContactDetails,
						children: details.map((item) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: item.label }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: item.value })] }, item.label))
					}),
					props.items.length ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("nav", {
						className: puck_module_default.palmContactLinks,
						children: props.items.map((item, index) => item.url ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
							href: item.url,
							children: item.label || item.value
						}, `${item.label}-${index}`) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: item.label || item.value }, `${item.label}-${index}`))
					}) : null
				] }),
				qr ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figure", {
					className: puck_module_default.palmContactQr,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
						alt: qr.alt || props.qrCaption || "QR code",
						src: qr.url
					}), props.qrCaption ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("figcaption", { children: props.qrCaption }) : null]
				}) : null,
				props.disclaimer ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "small",
					className: puck_module_default.palmContactDisclaimer,
					field: "disclaimer",
					props,
					children: props.disclaimer
				}) : null
			]
		})
	});
}
function CampaignAltTabs({ props }) {
	const [active, setActive] = (0, import_react.useState)(0);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
		className: puck_module_default.campaignAltTabs,
		"data-variant": props.variant,
		children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			role: "tablist",
			children: props.tabs.map((tab, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
				"aria-selected": active === index,
				onClick: () => setActive(index),
				role: "tab",
				type: "button",
				children: tab.label || tab.heading || `Tab ${index + 1}`
			}, `${tab.label}-${index}`))
		}), props.tabs.map((tab, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
			hidden: !props.puck?.isEditing && active !== index,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", { children: tab.heading }),
				tab.text ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: tab.text }) : null,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotContent, {
					content: tab.blocks,
					label: tab.label || `tab ${index + 1}`,
					fallback: renderZone(props, `tabs.${index}.blocks`, `Drop elements in ${tab.label || `tab ${index + 1}`}`)
				})
			]
		}, `${tab.heading}-${index}`))]
	});
}
function CampaignAltRender({ definition, props }) {
	if (definition.type === "HeroAlt") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(HeroAltRender, { props });
	if (definition.type === "AboutAlt") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(BioSectionRender, { props });
	if (definition.type === "CardsGridAlt") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(IssuesCardsRender, { props });
	if (definition.type === "PalmCardPointsAlt") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PalmCardPointsRender, { props });
	if (definition.type === "PalmCardBioAlt") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PalmCardBioRender, { props });
	if (definition.type === "TestimonialAlt") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TestimonialAltRender, { props });
	if (definition.type === "PalmCardAlt") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PalmCardContentRender, { props });
	if (definition.type === "PalmCardContactAlt") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(PalmCardContactRender, { props });
	const media = normalizeMedia(props.media);
	const background = normalizeMedia(props.backgroundMedia);
	const nestedItems = definition.nestedCollection === "cards" ? props.cards : definition.nestedCollection === "columns" ? props.columns : [];
	if (definition.kind === "tabs") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: puck_module_default.campaignAlt,
		"data-kind": "tabs",
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "h2",
					field: "heading",
					props,
					children: props.heading
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "p",
					field: "body",
					props,
					children: props.body
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignAltTabs, { props })
			]
		})
	});
	if (definition.kind === "columns" || definition.nestedCollection === "cards") return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
		className: puck_module_default.campaignAlt,
		"data-kind": definition.kind,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "h2",
					field: "heading",
					props,
					children: props.heading
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "p",
					field: "body",
					props,
					children: props.body
				}),
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: puck_module_default.campaignAltGrid,
					children: nestedItems.map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: item.label || `${definition.nestedCollection === "columns" ? "Column" : "Card"} ${index + 1}` }),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", { children: item.heading }),
						item.text ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: item.text }) : null,
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotContent, {
							content: item.blocks,
							label: item.label || `item ${index + 1}`,
							fallback: renderZone(props, `${definition.nestedCollection}.${index}.blocks`, `Drop elements in ${item.label || `item ${index + 1}`}`)
						})
					] }, `${item.heading}-${index}`))
				})
			]
		})
	});
	const isHero = definition.kind === "hero";
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
		className: puck_module_default.campaignAlt,
		"data-kind": definition.kind,
		"data-presentation": props.presentation,
		"data-variant": props.variant,
		id: props.id,
		children: [background ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
			"aria-hidden": "true",
			alt: "",
			className: puck_module_default.campaignAltBackground,
			src: background.url
		}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
			className: puck_module_default.shell,
			children: [
				/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: puck_module_default.campaignAltCopy,
					children: [
						props.eyebrow ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.eyebrowDark,
							field: "eyebrow",
							props,
							children: props.eyebrow
						}) : null,
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: isHero ? "h1" : "h2",
							field: "heading",
							props,
							children: props.heading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							field: "body",
							props,
							children: props.body
						}),
						props.primaryLabel || props.secondaryLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
							className: puck_module_default.actions,
							children: [props.primaryLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
								href: props.primaryUrl || "#",
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
									field: "primaryLabel",
									props,
									children: props.primaryLabel
								})
							}) : null, props.secondaryLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
								href: props.secondaryUrl || "#",
								outline: true,
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
									field: "secondaryLabel",
									props,
									children: props.secondaryLabel
								})
							}) : null]
						}) : null
					]
				}),
				media ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("figure", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
					alt: media.alt || "",
					src: media.url
				}) }) : null,
				props.items.length ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: puck_module_default.campaignAltItems,
					children: props.items.map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [
						item.image && normalizeMedia(item.image) ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
							alt: normalizeMedia(item.image)?.alt || "",
							src: normalizeMedia(item.image)?.url
						}) : null,
						item.label ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: item.label }) : null,
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", { children: item.heading || item.value }),
						item.text ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: item.text }) : null,
						item.url ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
							href: item.url,
							children: "Learn more"
						}) : null
					] }, `${item.heading}-${item.label}-${index}`))
				}) : null
			]
		})]
	});
}
function campaignAltDefaults(definition) {
	const base = {
		variant: definition.variants[0],
		presentation: definition.presentations?.[0] || "contained",
		eyebrow: "",
		heading: definition.label,
		intro: "",
		body: "Add campaign-facing content here.",
		media: null,
		backgroundMedia: null,
		headingLogo: null,
		qrImage: null,
		highlightTitle: "",
		highlightText: "",
		backgroundOverlay: "standard",
		textPanelColor: "primary",
		textPanelOpacity: "translucent",
		primaryLabel: "Learn more",
		primaryUrl: "#",
		secondaryLabel: "",
		secondaryUrl: "#",
		quote: "",
		quoteAttribution: "",
		electionDay: "",
		earlyVote: "",
		phone: "",
		email: "",
		website: "",
		qrCaption: "",
		disclaimer: "",
		items: [{
			label: "",
			heading: "First item",
			text: "Add details",
			value: "",
			url: "",
			icon: "check",
			attribution: "",
			role: "",
			image: null
		}],
		cards: [],
		columns: [],
		tabs: []
	};
	if (definition.type === "HeroAlt") return {
		...base,
		variant: "civicOutdoors",
		eyebrow: "NECYPAA XXXVI",
		heading: "A bold alternate opening",
		body: "Use the hero variant that best fits the message, media, and call to action.",
		highlightTitle: "Hartford, Connecticut",
		highlightText: "Connection, service, and recovery.",
		primaryLabel: "Register",
		secondaryLabel: "Learn more"
	};
	if (definition.type === "AboutAlt") return {
		...base,
		heading: "Our story",
		intro: "A clear introduction to the people and purpose behind the work.",
		body: "Use this space for the fuller biography or organizational story.",
		items: [
			{
				text: "Built through service",
				icon: "check"
			},
			{
				text: "Rooted in community",
				icon: "check"
			},
			{
				text: "Focused on the next person",
				icon: "check"
			}
		]
	};
	if (definition.type === "CardsGridAlt") return {
		...base,
		variant: "iconCards",
		presentation: "wide",
		heading: "Priorities",
		intro: "Use these cards for issues, services, promises, or voter actions.",
		body: "",
		items: [
			{
				heading: "Public safety",
				icon: "shieldCheck",
				text: "A short, specific priority with a credible outcome.",
				image: null
			},
			{
				heading: "Local opportunity",
				icon: "handshake",
				text: "Explain how the work improves life in the community.",
				image: null
			},
			{
				heading: "Responsive service",
				icon: "mapPin",
				text: "Keep each card focused and easy to scan.",
				image: null
			}
		]
	};
	if (definition.type === "PalmCardPointsAlt") return {
		...base,
		presentation: "wide",
		heading: "Working for you",
		intro: "",
		body: "",
		items: [
			{
				icon: "check",
				text: "Palm card point"
			},
			{
				icon: "check",
				text: "Palm card point"
			},
			{
				icon: "check",
				text: "Palm card point"
			}
		]
	};
	if (definition.type === "PalmCardBioAlt") return {
		...base,
		presentation: "wide",
		eyebrow: "About",
		heading: "Candidate name",
		body: "Use this short biography to introduce experience, values, and community ties.",
		quote: "Leadership starts by listening.",
		quoteAttribution: "Candidate name",
		items: []
	};
	if (definition.type === "TestimonialAlt") return {
		...base,
		heading: "What people are saying",
		intro: "Use quotes for social proof, endorsements, or community voices.",
		body: "",
		items: [{
			text: "This work is focused on what our community needs right now.",
			attribution: "Community supporter",
			role: "Resident",
			image: null
		}, {
			text: "The message is clear, practical, and rooted in local priorities.",
			attribution: "Local leader",
			role: "Endorser",
			image: null
		}]
	};
	if (definition.type === "PalmCardAlt") return {
		...base,
		presentation: "wide",
		heading: "Palm card content",
		intro: "",
		body: "",
		items: [
			{
				icon: "check",
				heading: "Palm card point",
				text: ""
			},
			{
				icon: "check",
				heading: "Palm card point",
				text: ""
			},
			{
				icon: "check",
				heading: "Palm card point",
				text: ""
			}
		]
	};
	if (definition.type === "PalmCardContactAlt") return {
		...base,
		presentation: "wide",
		eyebrow: "Voting information",
		heading: "Everything you need to take action",
		intro: "Keep essential dates and contact details together.",
		body: "",
		electionDay: "Election Day: Nov. 3",
		earlyVote: "Vote early: Oct. 19 - Nov. 1",
		website: "example.org",
		disclaimer: "Paid for by the campaign. Approved by the candidate.",
		items: [{
			label: "Website",
			value: "example.org",
			url: "https://example.org"
		}, {
			label: "Facebook",
			value: "Follow the campaign",
			url: "#"
		}]
	};
	return base;
}
function campaignAltFields(definition) {
	const common = {
		variant: {
			type: "select",
			label: "Variant",
			options: definition.variants.map((value) => ({
				label: campaignOptionLabel(value),
				value
			}))
		},
		presentation: {
			type: "select",
			label: "Presentation",
			options: (definition.presentations || ["contained"]).map((value) => ({
				label: campaignOptionLabel(value),
				value
			}))
		}
	};
	const header = {
		eyebrow: text("Eyebrow"),
		heading: text("Heading"),
		intro: area("Introduction")
	};
	if (definition.type === "HeroAlt") return {
		...common,
		eyebrow: text("Eyebrow"),
		heading: text("Heading"),
		headingLogo: mediaField("Heading logo"),
		body: area("Body"),
		highlightTitle: text("Highlight title"),
		highlightText: area("Highlight text"),
		media: mediaField("Primary media"),
		backgroundMedia: mediaField("Background media"),
		backgroundOverlay: {
			type: "select",
			label: "Background overlay",
			options: [
				"none",
				"off",
				"subtle",
				"standard",
				"strong"
			].map((value) => ({
				label: campaignOptionLabel(value),
				value
			}))
		},
		textPanelColor: {
			type: "select",
			label: "Panel color",
			options: [
				"primary",
				"accent",
				"foreground",
				"background",
				"white"
			].map((value) => ({
				label: campaignOptionLabel(value),
				value
			}))
		},
		textPanelOpacity: {
			type: "select",
			label: "Panel opacity",
			options: ["translucent", "solid"].map((value) => ({
				label: campaignOptionLabel(value),
				value
			}))
		},
		primaryLabel: text("Primary action label"),
		primaryUrl: plainText("Primary action URL"),
		secondaryLabel: text("Secondary action label"),
		secondaryUrl: plainText("Secondary action URL")
	};
	if (definition.type === "AboutAlt") return {
		...common,
		...header,
		body: area("Body"),
		media: mediaField("Portrait or feature image"),
		items: bioHighlightsField
	};
	if (definition.type === "CardsGridAlt") return {
		...common,
		heading: text("Heading"),
		intro: area("Introduction"),
		items: issueCardsAltField
	};
	if (definition.type === "PalmCardPointsAlt") return {
		...common,
		...header,
		items: palmPointsField
	};
	if (definition.type === "PalmCardBioAlt") return {
		...common,
		eyebrow: text("Eyebrow"),
		heading: text("Heading"),
		body: area("Biography"),
		quote: area("Quote"),
		quoteAttribution: text("Quote attribution"),
		media: mediaField("Photo")
	};
	if (definition.type === "TestimonialAlt") return {
		...common,
		heading: text("Heading"),
		intro: area("Introduction"),
		items: testimonialsField
	};
	if (definition.type === "PalmCardAlt") return {
		...common,
		...header,
		media: mediaField("Photo"),
		items: palmContentField,
		quote: area("Quote"),
		quoteAttribution: text("Quote attribution")
	};
	if (definition.type === "PalmCardContactAlt") return {
		...common,
		...header,
		electionDay: text("Election day"),
		earlyVote: text("Early vote"),
		phone: text("Phone"),
		email: text("Email"),
		website: text("Website"),
		items: contactLinksField,
		qrImage: mediaField("QR image"),
		qrCaption: text("QR caption"),
		disclaimer: text("Paid for / disclaimer")
	};
	return {
		...common,
		eyebrow: text("Eyebrow"),
		heading: text("Heading"),
		body: area("Body"),
		media: mediaField("Primary media"),
		backgroundMedia: mediaField("Background media"),
		primaryLabel: text("Primary action label"),
		primaryUrl: plainText("Primary action URL"),
		secondaryLabel: text("Secondary action label"),
		secondaryUrl: plainText("Secondary action URL"),
		items: campaignItemsField
	};
}
var campaignAltComponents = Object.fromEntries(campaignAltDefinitions.map((definition) => {
	const nestedDefaults = definition.nestedCollection === "columns" ? [{
		label: "Column 1",
		heading: "",
		text: ""
	}, {
		label: "Column 2",
		heading: "",
		text: ""
	}] : definition.nestedCollection === "tabs" ? [{
		label: "First tab",
		heading: "First tab",
		text: "Add tab content"
	}, {
		label: "Second tab",
		heading: "Second tab",
		text: "Add tab content"
	}] : definition.nestedCollection === "cards" ? [{
		label: "Card 1",
		heading: "First card",
		text: "Add card content"
	}, {
		label: "Card 2",
		heading: "Second card",
		text: "Add card content"
	}] : [];
	const nestedFields = definition.nestedCollection ? { [definition.nestedCollection]: campaignNestedField } : {};
	return [definition.type, {
		label: definition.label,
		defaultProps: {
			...campaignAltDefaults(definition),
			cards: definition.nestedCollection === "cards" ? nestedDefaults : [],
			columns: definition.nestedCollection === "columns" ? nestedDefaults : [],
			tabs: definition.nestedCollection === "tabs" ? nestedDefaults : []
		},
		fields: {
			...campaignAltFields(definition),
			...nestedFields
		},
		render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CampaignAltRender, {
			definition,
			props
		})
	}];
}));
var puckConfig = {
	categories: {
		"Home page": { components: [
			"HeroCountdown",
			"About",
			"MeetingInfo",
			"Events",
			"MeetingDirectory",
			"CTMeetingSchedule",
			"ProgramSchedule",
			"CallToAction"
		] },
		"Features & content": { components: ["IssuesSection", "IssueCards"] },
		"Quotes & highlights": { components: [
			"QuoteBlock",
			"ResultsStats",
			"SupporterLogos"
		] },
		"Actions & tabs": { components: ["ActionTabs", "ButtonRow"] },
		"Media & layout": { components: [
			"MediaGallery",
			"ContentRow",
			"Row",
			"RowOneColumn",
			"RowTwoColumns",
			"RowLeftWide",
			"RowRightWide",
			"RowThreeColumns",
			"RowFourColumns",
			"Section",
			"Column"
		] },
		"Elements for rows": { components: [
			"Headline",
			"Text",
			"Button",
			"Countdown",
			"Image",
			"RichText",
			"FreeText",
			"Divider",
			"BulletedList",
			"FollowLinks",
			"InlineForm",
			"PayPal",
			"Navigation",
			"ImageCaption",
			"Video",
			"Embed"
		] },
		"Campaign alternatives": { components: campaignAltTypes }
	},
	root: {
		fields: {
			title: plainText("Page title"),
			slug: plainText("Slug"),
			metaTitle: plainText("SEO title"),
			metaDescription: {
				type: "textarea",
				label: "SEO description"
			}
		},
		render: ({ children }) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("main", {
			className: puck_module_default.canvas,
			children
		})
	},
	components: {
		HeroCountdown: {
			label: "Hero + countdown",
			defaultProps: {
				eyebrow: "Escaping the Mad Realm",
				heading: "NECYPAA XXXVI",
				body: "Connection, service, and recovery.",
				eventDate: "December 31, 2026 – January 3, 2027",
				eventLocation: "Hartford, Connecticut",
				countdownTarget: "2026-12-31T17:00:00-05:00",
				registerLabel: "Register",
				registerUrl: "#",
				hotelLabel: "Book a hotel room",
				hotelUrl: "#",
				foregroundImage: null,
				backgroundImage: null,
				backgroundPosterImage: null,
				backgroundDarkness: 45
			},
			fields: {
				eyebrow: text("Eyebrow"),
				heading: text("Heading"),
				body: area("Introduction"),
				eventDate: text("Dates"),
				eventLocation: text("Location"),
				countdownTarget: plainText("Countdown ISO date"),
				registerLabel: text("Register label"),
				registerUrl: plainText("Register URL"),
				hotelLabel: text("Hotel label"),
				hotelUrl: plainText("Hotel URL"),
				foregroundImage: mediaField("Foreground image"),
				backgroundImage: mediaField("Background image or video (optional)", true),
				backgroundPosterImage: mediaField("Video poster image (optional)"),
				backgroundDarkness: backgroundDarknessField
			},
			render: (props) => {
				const foregroundImage = normalizeMedia(props.foregroundImage) || normalizeMedia(props.image);
				const selectedBackground = normalizeMedia(props.backgroundImage);
				const selectedPoster = normalizeMedia(props.backgroundPosterImage);
				const backgroundSource = selectedBackground?.url || "/media/necypaa-portal-background.mp4";
				const backgroundPoster = selectedPoster?.url || (selectedBackground ? void 0 : "/images/necypaa-portal-background.webp");
				const backgroundIsVideo = selectedBackground ? selectedBackground.mimeType?.startsWith("video/") || /\.(?:mp4|webm)(?:\?.*)?$/i.test(selectedBackground.url) : true;
				const foregroundSource = foregroundImage?.url || "/images/necypaa-floating-hotel-hero.webp";
				const foregroundAlt = foregroundImage?.alt || "Hartford Marriott Downtown flying above a floating island with two illustrated mascots";
				const darknessScale = (Number.isFinite(props.backgroundDarkness) ? Math.min(100, Math.max(0, props.backgroundDarkness)) : 45) / 45;
				const overlayStyle = {
					"--hero-overlay-left": Math.min(.96, .7 * darknessScale).toFixed(3),
					"--hero-overlay-middle": Math.min(.96, .46 * darknessScale).toFixed(3),
					"--hero-overlay-right": Math.min(.75, .14 * darknessScale).toFixed(3),
					"--hero-overlay-bottom": Math.min(.9, .28 * darknessScale).toFixed(3)
				};
				return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
					className: puck_module_default.hero,
					"data-has-background": "true",
					id: props.id,
					style: overlayStyle,
					children: [backgroundIsVideo ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [backgroundPoster ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
						"aria-hidden": "true",
						alt: "",
						className: `${puck_module_default.heroBackground} ${puck_module_default.heroBackgroundPoster}`,
						src: backgroundPoster
					}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("video", {
						"aria-hidden": "true",
						autoPlay: true,
						className: `${puck_module_default.heroBackground} ${puck_module_default.heroBackgroundVideo}`,
						loop: true,
						muted: true,
						playsInline: true,
						poster: backgroundPoster,
						preload: "metadata",
						children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("source", {
							src: backgroundSource,
							type: selectedBackground?.mimeType || "video/mp4"
						})
					})] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
						"aria-hidden": "true",
						alt: "",
						className: puck_module_default.heroBackground,
						src: backgroundSource
					}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: puck_module_default.shell,
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: puck_module_default.heroCopy,
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
										as: "p",
										className: puck_module_default.eyebrow,
										field: "eyebrow",
										props,
										children: props.eyebrow
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
										as: "h1",
										field: "heading",
										props,
										children: props.heading
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
										as: "p",
										className: puck_module_default.lede,
										field: "body",
										props,
										children: props.body
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: puck_module_default.meta,
										children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
											field: "eventDate",
											props,
											children: props.eventDate
										}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
											field: "eventLocation",
											props,
											children: props.eventLocation
										})]
									}),
									/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: puck_module_default.actions,
										children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
											href: props.registerUrl,
											children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
												field: "registerLabel",
												props,
												children: props.registerLabel
											})
										}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
											href: props.hotelUrl,
											outline: true,
											children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
												field: "hotelLabel",
												props,
												children: props.hotelLabel
											})
										})]
									})
								]
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
								className: puck_module_default.heroArt,
								"data-has-image": "true",
								children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
									alt: foregroundAlt,
									src: foregroundSource
								})
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Countdown, { target: props.countdownTarget })
						]
					})]
				});
			}
		},
		About: {
			label: "About + advisory",
			defaultProps: {
				eyebrow: "About NECYPAA",
				heading: "A weekend built around connection",
				body: "Add the convention story here.",
				advisoryHeading: "Anonymity matters",
				advisoryBody: "Please protect personal anonymity.",
				image: null
			},
			fields: {
				eyebrow: text("Eyebrow"),
				heading: text("Heading"),
				body: area("Body"),
				advisoryHeading: text("Advisory heading"),
				advisoryBody: area("Advisory body"),
				image: mediaField("About artwork")
			},
			render: (props) => {
				const image = normalizeMedia(props.image);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
					className: puck_module_default.dark,
					id: props.id,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: `${puck_module_default.shell} ${puck_module_default.twoCol}`,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: puck_module_default.artPlaceholder,
							"data-has-image": Boolean(image),
							children: image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
								alt: image.alt || "",
								src: image.url
							}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "Mad Realm imagery" })
						}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "p",
								className: puck_module_default.eyebrow,
								field: "eyebrow",
								props,
								children: props.eyebrow
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "h2",
								field: "heading",
								props,
								children: props.heading
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "p",
								className: puck_module_default.body,
								field: "body",
								props,
								children: props.body
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("aside", {
								className: puck_module_default.advisory,
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
									as: "h3",
									field: "advisoryHeading",
									props,
									children: props.advisoryHeading
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
									as: "p",
									field: "advisoryBody",
									props,
									children: props.advisoryBody
								})]
							})
						] })]
					})
				});
			}
		},
		MeetingInfo: {
			label: "Business meeting",
			defaultProps: {
				eyebrow: "Host committee",
				heading: "Business meeting",
				body: "Join the host committee.",
				date: "Date",
				time: "Time",
				location: "Zoom",
				actionLabel: "Join on Zoom",
				actionUrl: "#",
				importantDates: [{
					date: "Date",
					label: "Details"
				}]
			},
			fields: {
				eyebrow: text("Eyebrow"),
				heading: text("Heading"),
				body: area("Body"),
				date: text("Date"),
				time: text("Time"),
				location: text("Location"),
				actionLabel: text("Action label"),
				actionUrl: plainText("Action URL"),
				importantDates: importantDatesField
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.light,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: `${puck_module_default.shell} ${puck_module_default.meeting}`,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.eyebrowDark,
							field: "eyebrow",
							props,
							children: props.eyebrow
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "h2",
							field: "heading",
							props,
							children: props.heading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.body,
							field: "body",
							props,
							children: props.body
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("dl", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("dt", { children: "Date" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("dd", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								field: "date",
								props,
								children: props.date
							}) })] }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("dt", { children: "Time" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("dd", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								field: "time",
								props,
								children: props.time
							}) })] }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("dt", { children: "Where" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("dd", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								field: "location",
								props,
								children: props.location
							}) })] })
						] }),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
							href: props.actionUrl,
							children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								field: "actionLabel",
								props,
								children: props.actionLabel
							})
						})
					] }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("aside", {
						className: puck_module_default.dates,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: "Important dates" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("ul", { children: normalizeImportantDates(props.importantDates).map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: item.date }), item.label ? ` — ${item.label}` : ""] }, `${item.date}-${item.label}-${index}`)) })]
					})]
				})
			})
		},
		Events: {
			label: "Upcoming + past events",
			defaultProps: {
				eyebrow: "Gather with us",
				heading: "Upcoming and past events",
				upcomingLabel: "Next up",
				upcomingTitle: "Upcoming event",
				upcomingBody: "Event details",
				upcomingDate: "Date",
				upcomingLocation: "Location",
				upcomingImage: null,
				upcomingEvents: [],
				pastEvents: [{
					title: "Past event",
					date: "Date",
					image: null
				}]
			},
			fields: {
				eyebrow: text("Eyebrow"),
				heading: text("Heading"),
				upcomingLabel: text("Upcoming label"),
				upcomingTitle: text("Upcoming title"),
				upcomingBody: area("Upcoming description"),
				upcomingDate: text("Upcoming date"),
				upcomingLocation: text("Upcoming location"),
				upcomingImage: mediaField("Upcoming flyer"),
				upcomingEvents: upcomingEventsField,
				pastEvents: pastEventsField
			},
			render: (props) => {
				const upcomingImage = normalizeMedia(props.upcomingImage);
				const upcomingEvents = normalizeUpcomingEvents(props.upcomingEvents);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
					className: puck_module_default.events,
					id: props.id,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: puck_module_default.shell,
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "p",
								className: puck_module_default.eyebrow,
								field: "eyebrow",
								props,
								children: props.eyebrow
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "h2",
								field: "heading",
								props,
								children: props.heading
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: puck_module_default.eventBlend,
								children: [
									/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", {
										className: puck_module_default.upcoming,
										children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
											className: puck_module_default.flyer,
											"data-has-image": Boolean(upcomingImage),
											children: upcomingImage ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
												alt: upcomingImage.alt || "",
												src: upcomingImage.url
											}) : "Upcoming flyer"
										}), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
											/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
												as: "p",
												className: puck_module_default.eventLabel,
												field: "upcomingLabel",
												props,
												children: props.upcomingLabel
											}),
											/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
												as: "h3",
												field: "upcomingTitle",
												props,
												children: props.upcomingTitle
											}),
											/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
												as: "p",
												field: "upcomingBody",
												props,
												children: props.upcomingBody
											}),
											/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
												as: "strong",
												field: "upcomingDate",
												props,
												children: props.upcomingDate
											}),
											/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
												as: "span",
												field: "upcomingLocation",
												props,
												children: props.upcomingLocation
											})
										] })]
									}),
									upcomingEvents.length ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: puck_module_default.futureEvents,
										children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "More upcoming events" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("ul", { children: upcomingEvents.map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("li", { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: item.title }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("time", { children: item.date })] }, `${item.title}-${item.date}-${index}`)) })]
									}) : null,
									/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
										className: puck_module_default.archive,
										children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: "From the archive" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: normalizePastEvents(props.pastEvents).map((item, index) => {
											const image = normalizeMedia(item.image);
											return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [
												/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
													"data-has-image": Boolean(image),
													children: image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
														alt: image.alt || "",
														src: image.url
													}) : "Event flyer"
												}),
												/* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: item.date }),
												/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: item.title })
											] }, `${item.title}-${item.date}-${index}`);
										}) })]
									})
								]
							})
						]
					})
				});
			}
		},
		MeetingDirectory: {
			label: "YPAA directory",
			defaultProps: {
				eyebrow: "Across the Northeast",
				heading: "YPAA meetings near you",
				body: "Find fellowship near you.",
				meetings: [{
					name: "Connecticut YPAA",
					location: "Connecticut"
				}]
			},
			fields: {
				eyebrow: text("Eyebrow"),
				heading: text("Heading"),
				body: area("Body"),
				meetings: meetingsField
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.directory,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: `${puck_module_default.shell} ${puck_module_default.twoCol}`,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.eyebrowDark,
							field: "eyebrow",
							props,
							children: props.eyebrow
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "h2",
							field: "heading",
							props,
							children: props.heading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							field: "body",
							props,
							children: props.body
						})
					] }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("ul", { children: normalizeMeetings(props.meetings).map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
						item.url ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
							href: item.url,
							rel: "noreferrer",
							target: "_blank",
							children: item.name
						}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: item.name }),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: item.location }),
						item.date ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: item.date }) : null
					] }) }, `${item.name}-${item.location}-${index}`)) })]
				})
			})
		},
		CTMeetingSchedule: {
			label: "CT Meeting schedule",
			defaultProps: {
				heading: "Young People's Meetings in Connecticut",
				introduction: "Click any meeting name for the CT-AA details. Use the arrow to expand the address and meeting types.",
				meetings: ctMeetingSchedule
			},
			fields: {
				heading: text("Heading"),
				introduction: area("Introduction"),
				meetings: scheduleMeetingsField
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CTMeetingScheduleBlock, { ...props })
		},
		CallToAction: {
			label: "Call to action",
			defaultProps: {
				eyebrow: "See you there",
				heading: "Ready for NECYPAA?",
				body: "Register and reserve your room.",
				primaryLabel: "Register",
				primaryUrl: "#",
				secondaryLabel: "Book a hotel room",
				secondaryUrl: "#",
				image: null
			},
			fields: {
				eyebrow: text("Eyebrow"),
				heading: text("Heading"),
				body: area("Body"),
				primaryLabel: text("Primary label"),
				primaryUrl: plainText("Primary URL"),
				secondaryLabel: text("Secondary label"),
				secondaryUrl: plainText("Secondary URL"),
				image: mediaField("Closing artwork")
			},
			render: (props) => {
				const image = normalizeMedia(props.image);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
					className: puck_module_default.cta,
					"data-has-image": Boolean(image),
					id: props.id,
					children: [image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
						className: puck_module_default.ctaImage,
						alt: image.alt || "",
						src: image.url
					}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: puck_module_default.shell,
						children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "p",
								className: puck_module_default.eyebrow,
								field: "eyebrow",
								props,
								children: props.eyebrow
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "h2",
								field: "heading",
								props,
								children: props.heading
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "p",
								className: puck_module_default.lede,
								field: "body",
								props,
								children: props.body
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
								className: puck_module_default.actions,
								children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
									href: props.primaryUrl,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
										field: "primaryLabel",
										props,
										children: props.primaryLabel
									})
								}), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
									href: props.secondaryUrl,
									outline: true,
									children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
										field: "secondaryLabel",
										props,
										children: props.secondaryLabel
									})
								})]
							})
						]
					})]
				});
			}
		},
		Image: {
			label: "Image",
			defaultProps: {
				image: null,
				caption: "",
				aspectRatio: "natural",
				width: "wide"
			},
			fields: {
				image: mediaField("Image"),
				caption: text("Caption"),
				aspectRatio: {
					type: "select",
					label: "Crop",
					options: [
						{
							label: "Natural",
							value: "natural"
						},
						{
							label: "Landscape",
							value: "landscape"
						},
						{
							label: "Portrait",
							value: "portrait"
						},
						{
							label: "Square",
							value: "square"
						}
					]
				},
				width: {
					type: "select",
					label: "Width",
					options: [
						{
							label: "Full width",
							value: "full"
						},
						{
							label: "Wide",
							value: "wide"
						},
						{
							label: "Content",
							value: "content"
						}
					]
				}
			},
			render: (props) => {
				const image = normalizeMedia(props.image);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figure", {
					className: puck_module_default.imageBlock,
					"data-aspect": props.aspectRatio,
					"data-width": props.width,
					id: props.id,
					children: [image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
						alt: image.alt || "",
						src: image.url
					}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
						className: puck_module_default.imageEmpty,
						children: "Choose an image from Payload Media"
					}), props.caption ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
						as: "figcaption",
						field: "caption",
						props,
						children: props.caption
					}) : null]
				});
			}
		},
		RichText: {
			label: "Rich text",
			defaultProps: {
				content: normalizeLexicalValue("Write rich text here."),
				fontSize: "1rem",
				color: "#171b20",
				fontWeight: "400",
				alignment: "left"
			},
			fields: {
				content: richTextPlaceholder,
				fontSize: plainText("Font size"),
				color: plainText("Color"),
				fontWeight: {
					type: "select",
					label: "Weight",
					options: [{
						label: "Regular",
						value: "400"
					}, {
						label: "Bold",
						value: "700"
					}]
				},
				alignment: {
					type: "select",
					label: "Alignment",
					options: [
						{
							label: "Left",
							value: "left"
						},
						{
							label: "Center",
							value: "center"
						},
						{
							label: "Right",
							value: "right"
						}
					]
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.richText,
				id: props.id,
				onPointerDown: () => {
					if (typeof props.id === "string") window.parent.postMessage({
						type: "necypaa:puck-text-focus",
						componentId: props.id,
						field: "content"
					}, window.location.origin);
				},
				style: {
					color: props.color,
					fontSize: props.fontSize,
					fontWeight: props.fontWeight,
					textAlign: props.alignment
				},
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
					className: puck_module_default.shell,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RichText, { data: normalizeLexicalValue(props.content) })
				})
			})
		},
		FreeText: {
			label: "Free text",
			defaultProps: {
				text: "Click to edit this text.",
				fontSize: "1rem",
				color: "#171b20",
				fontWeight: "400",
				alignment: "left"
			},
			fields: {
				text: area("Text"),
				fontSize: plainText("Font size"),
				color: plainText("Color"),
				fontWeight: plainText("Weight"),
				alignment: {
					type: "select",
					label: "Alignment",
					options: [
						{
							label: "Left",
							value: "left"
						},
						{
							label: "Center",
							value: "center"
						},
						{
							label: "Right",
							value: "right"
						}
					]
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.freeText,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
					as: "p",
					field: "text",
					props: {
						...props,
						textFontSize: props.fontSize,
						textColor: props.color,
						textFontWeight: props.fontWeight,
						textTextAlign: props.alignment
					},
					children: props.text
				})
			})
		},
		ProgramSchedule: {
			label: "Live program",
			defaultProps: {
				heading: "Your weekend, mapped out",
				introduction: "Search the live convention schedule by day, room, or session type."
			},
			fields: {
				heading: text("Heading"),
				introduction: area("Introduction")
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProgramExplorer, {
					embedded: true,
					heading: props.heading,
					introduction: props.introduction
				})
			})
		},
		ButtonRow: {
			label: "Button row",
			defaultProps: {
				primaryLabel: "Learn more",
				primaryUrl: "#",
				secondaryLabel: "Get involved",
				secondaryUrl: "#",
				alignment: "left"
			},
			fields: {
				primaryLabel: text("Primary label"),
				primaryUrl: plainText("Primary URL"),
				secondaryLabel: text("Secondary label"),
				secondaryUrl: plainText("Secondary URL"),
				alignment: {
					type: "select",
					label: "Alignment",
					options: [
						{
							label: "Left",
							value: "left"
						},
						{
							label: "Center",
							value: "center"
						},
						{
							label: "Right",
							value: "right"
						}
					]
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: puck_module_default.buttonRow,
				"data-align": props.alignment,
				id: props.id,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					href: props.primaryUrl,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
						field: "primaryLabel",
						props,
						children: props.primaryLabel
					})
				}), props.secondaryLabel ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, {
					href: props.secondaryUrl,
					outline: true,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
						field: "secondaryLabel",
						props,
						children: props.secondaryLabel
					})
				}) : null]
			})
		},
		IssuesSection: {
			label: "Feature section",
			defaultProps: {
				eyebrow: "What matters",
				heading: "Priorities for a stronger weekend",
				body: "Give visitors a clear view of the issues and ideas guiding this work.",
				issues: [
					{
						title: "Connection",
						body: "Make meaningful recovery connection easier to find.",
						icon: "01"
					},
					{
						title: "Service",
						body: "Create practical ways to be part of the convention.",
						icon: "02"
					},
					{
						title: "Accessibility",
						body: "Build a welcoming weekend for every attendee.",
						icon: "03"
					}
				]
			},
			fields: {
				eyebrow: text("Eyebrow"),
				heading: text("Heading"),
				body: area("Introduction"),
				issues: issueField
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.issuesSection,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: puck_module_default.shell,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.eyebrowDark,
							field: "eyebrow",
							props,
							children: props.eyebrow
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "h2",
							field: "heading",
							props,
							children: props.heading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.body,
							field: "body",
							props,
							children: props.body
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: puck_module_default.issuesGrid,
							children: props.issues.map((issue, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: issue.icon || String(index + 1).padStart(2, "0") }),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", { children: issue.title }),
								/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: issue.body })
							] }, `${issue.title}-${index}`))
						})
					]
				})
			})
		},
		IssueCards: {
			label: "Feature cards",
			defaultProps: {
				heading: "The work in focus",
				intro: "Use cards for detailed priorities, workshops, or ways to help.",
				variant: "cards",
				cards: [
					{
						label: "Recovery",
						heading: "Bring the next person in",
						body: "Create a convention experience that makes newcomers feel at home.",
						image: null
					},
					{
						label: "Fellowship",
						heading: "Make room for connection",
						body: "Build spaces where people can meet, laugh, and stay involved.",
						image: null
					},
					{
						label: "Service",
						heading: "Put gratitude into action",
						body: "Invite people into the work that makes the weekend possible.",
						image: null
					}
				]
			},
			fields: {
				heading: text("Heading"),
				intro: area("Introduction"),
				variant: {
					type: "select",
					label: "Card style",
					options: [
						{
							label: "Cards",
							value: "cards"
						},
						{
							label: "Editorial",
							value: "editorial"
						},
						{
							label: "Image overlay",
							value: "image"
						}
					]
				},
				cards: issueCardsField
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.issueCards,
				"data-variant": props.variant,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: puck_module_default.shell,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "h2",
							field: "heading",
							props,
							children: props.heading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.body,
							field: "intro",
							props,
							children: props.intro
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
							className: puck_module_default.issueCardGrid,
							children: props.cards.map((card, index) => {
								const image = normalizeMedia(card.image);
								return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", {
									"data-has-image": Boolean(image),
									children: [image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
										alt: image.alt || "",
										src: image.url
									}) : null, /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)("small", { children: card.label }),
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h3", { children: card.heading }),
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: card.body }),
										card.linkLabel && card.linkUrl ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
											className: puck_module_default.cardLink,
											href: card.linkUrl,
											children: card.linkLabel
										}) : null,
										/* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotContent, {
											content: card.blocks,
											label: card.label || `card ${index + 1}`,
											fallback: renderZone(props, `cards.${index}.blocks`, `Drop elements in ${card.label || `card ${index + 1}`}`)
										})
									] })]
								}, `${card.heading}-${index}`);
							})
						})
					]
				})
			})
		},
		QuoteBlock: {
			label: "Quote / testimonial",
			defaultProps: {
				heading: "In their own words",
				quote: "Recovery is not something we do alone. It is something we learn to live together.",
				attribution: "NECYPAA member",
				role: "Host committee",
				image: null
			},
			fields: {
				heading: text("Heading"),
				quote: area("Quote"),
				attribution: text("Attribution"),
				role: text("Role"),
				image: mediaField("Portrait")
			},
			render: (props) => {
				const image = normalizeMedia(props.image);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
					className: puck_module_default.quote,
					id: props.id,
					children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
						className: puck_module_default.shell,
						children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
							alt: image.alt || "",
							src: image.url
						}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
							className: puck_module_default.quoteMark,
							children: "“"
						}) }), /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "p",
								className: puck_module_default.eyebrow,
								field: "heading",
								props,
								children: props.heading
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "blockquote",
								field: "quote",
								props,
								children: props.quote
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "strong",
								field: "attribution",
								props,
								children: props.attribution
							}),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
								as: "span",
								field: "role",
								props,
								children: props.role
							})
						] })]
					})
				});
			}
		},
		ResultsStats: {
			label: "Statistics",
			defaultProps: {
				heading: "What we are building",
				intro: "A few concrete markers of a weekend built by and for the fellowship.",
				stats: [
					{
						value: "4",
						label: "days together",
						detail: "Speakers, workshops, dancing, service, and fellowship."
					},
					{
						value: "9",
						label: "states represented",
						detail: "Young people and young-at-heart members across the Northeast."
					},
					{
						value: "1",
						label: "shared purpose",
						detail: "Carry the message and make space for recovery."
					}
				]
			},
			fields: {
				heading: text("Heading"),
				intro: area("Introduction"),
				stats: statsField
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.resultsStats,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: puck_module_default.shell,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "h2",
							field: "heading",
							props,
							children: props.heading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.body,
							field: "intro",
							props,
							children: props.intro
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: props.stats.map((stat, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: stat.value }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: stat.label }),
							/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: stat.detail })
						] }, `${stat.label}-${index}`)) })
					]
				})
			})
		},
		SupporterLogos: {
			label: "Partner logos",
			defaultProps: {
				heading: "With support from",
				intro: "Recognize the groups, committees, and partners helping make the weekend possible.",
				logos: [
					{
						name: "Connecticut YPAA",
						image: null
					},
					{
						name: "New England Area",
						image: null
					},
					{
						name: "Host Committee",
						image: null
					},
					{
						name: "Recovery Community",
						image: null
					}
				]
			},
			fields: {
				heading: text("Heading"),
				intro: area("Introduction"),
				logos: logosField
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.supporterLogos,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: puck_module_default.shell,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "h2",
							field: "heading",
							props,
							children: props.heading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.body,
							field: "intro",
							props,
							children: props.intro
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: props.logos.map((logo, index) => {
							const image = normalizeMedia(logo.image);
							return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("article", { children: [image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
								alt: image.alt || logo.name,
								src: image.url
							}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: logo.name.slice(0, 2).toUpperCase() }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: logo.name })] }, `${logo.name}-${index}`);
						}) })
					]
				})
			})
		},
		ActionTabs: {
			label: "Content tabs",
			defaultProps: {
				heading: "Choose how to join in",
				intro: "Give visitors a clear next step and add richer content inside each action tab.",
				tabs: [
					{
						label: "Register",
						description: "Save your place for NECYPAA XXXVI."
					},
					{
						label: "Volunteer",
						description: "Join the committee work that brings the convention to life."
					},
					{
						label: "Stay connected",
						description: "Follow announcements, meetings, and upcoming events."
					}
				]
			},
			fields: {
				heading: text("Heading"),
				intro: area("Introduction"),
				tabs: tabsField
			},
			render: ActionTabsRender
		},
		Navigation: {
			label: "Navigation",
			defaultProps: {
				brand: "NECYPAA XXXVI",
				links: [{
					label: "About",
					url: "#about"
				}, {
					label: "Register",
					url: "#register"
				}]
			},
			fields: {
				brand: text("Brand"),
				links: {
					type: "array",
					label: "Links",
					arrayFields: {
						label: plainText("Label"),
						url: plainText("URL")
					}
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("nav", {
				className: puck_module_default.navigation,
				id: props.id,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: props.brand }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: props.links.map((link, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
					href: link.url || "#",
					children: link.label
				}, `${link.label}-${index}`)) })]
			})
		},
		Text: {
			label: "Text",
			defaultProps: {
				text: "Add text",
				fontSize: "1rem",
				color: "#171b20",
				alignment: "left"
			},
			fields: {
				text: area("Text"),
				fontSize: plainText("Font size"),
				color: plainText("Color"),
				alignment: {
					type: "select",
					label: "Alignment",
					options: [
						{
							label: "Left",
							value: "left"
						},
						{
							label: "Center",
							value: "center"
						},
						{
							label: "Right",
							value: "right"
						}
					]
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", {
				className: puck_module_default.freeText,
				id: props.id,
				style: {
					color: props.color,
					fontSize: props.fontSize,
					textAlign: props.alignment
				},
				children: props.text
			})
		},
		Button: {
			label: "Button",
			defaultProps: {
				label: "Learn more",
				url: "#",
				style: "solid"
			},
			fields: {
				label: text("Label"),
				url: plainText("URL"),
				style: {
					type: "select",
					label: "Style",
					options: [{
						label: "Solid",
						value: "solid"
					}, {
						label: "Outline",
						value: "outline"
					}]
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
				className: puck_module_default.button,
				"data-outline": props.style === "outline",
				href: props.url || "#",
				id: props.id,
				children: props.label
			})
		},
		Countdown: {
			label: "Countdown",
			defaultProps: {
				target: "2026-12-31T17:00:00-05:00",
				label: "Time remaining"
			},
			fields: {
				target: plainText("Target ISO date"),
				label: text("Label")
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: puck_module_default.standaloneCountdown,
				id: props.id,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: props.label }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Countdown, { target: props.target })]
			})
		},
		Section: {
			label: "Section",
			defaultProps: {
				heading: "Section heading",
				background: "light"
			},
			fields: {
				heading: text("Heading"),
				background: {
					type: "select",
					label: "Background",
					options: [
						{
							label: "Light",
							value: "light"
						},
						{
							label: "Dark",
							value: "dark"
						},
						{
							label: "Muted",
							value: "muted"
						}
					]
				},
				blocks: {
					type: "slot",
					allow: nestedElementTypes
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.builderSection,
				"data-background": props.background,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: puck_module_default.shell,
					children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", { children: props.heading }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotContent, {
						content: props.blocks,
						label: "section",
						fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {})
					})]
				})
			})
		},
		Column: {
			label: "Column",
			defaultProps: { label: "Column" },
			fields: {
				label: plainText("Editor label"),
				blocks: {
					type: "slot",
					allow: nestedElementTypes
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				className: puck_module_default.builderColumn,
				"data-label": props.label,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SlotContent, {
					content: props.blocks,
					label: props.label,
					fallback: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {})
				})
			})
		},
		Headline: {
			label: "Headline",
			defaultProps: {
				text: "Add a headline",
				level: "h2",
				alignment: "left"
			},
			fields: {
				text: text("Headline"),
				level: {
					type: "select",
					label: "Level",
					options: [
						{
							label: "Heading 1",
							value: "h1"
						},
						{
							label: "Heading 2",
							value: "h2"
						},
						{
							label: "Heading 3",
							value: "h3"
						}
					]
				},
				alignment: {
					type: "select",
					label: "Alignment",
					options: [
						{
							label: "Left",
							value: "left"
						},
						{
							label: "Center",
							value: "center"
						},
						{
							label: "Right",
							value: "right"
						}
					]
				}
			},
			render: (props) => {
				const Tag = props.level;
				return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Tag, {
					className: puck_module_default.headline,
					id: props.id,
					style: { textAlign: props.alignment },
					children: props.text
				});
			}
		},
		Divider: {
			label: "Divider",
			defaultProps: {
				style: "solid",
				color: "#d8d0c4"
			},
			fields: {
				style: {
					type: "select",
					label: "Style",
					options: [
						{
							label: "Solid",
							value: "solid"
						},
						{
							label: "Dashed",
							value: "dashed"
						},
						{
							label: "Dotted",
							value: "dotted"
						}
					]
				},
				color: {
					type: "text",
					label: "Color"
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("hr", {
				className: puck_module_default.divider,
				id: props.id,
				style: {
					borderTopStyle: props.style,
					borderTopColor: props.color
				}
			})
		},
		FollowLinks: {
			label: "Follow links",
			defaultProps: {
				heading: "Follow along",
				links: [{
					label: "Instagram",
					url: "#"
				}, {
					label: "Facebook",
					url: "#"
				}]
			},
			fields: {
				heading: text("Heading"),
				links: {
					type: "array",
					label: "Links",
					arrayFields: {
						label: plainText("Label"),
						url: plainText("URL")
					}
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", {
				className: puck_module_default.followLinks,
				id: props.id,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("strong", { children: props.heading }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: props.links.map((link, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
					href: link.url || "#",
					children: link.label
				}, `${link.label}-${index}`)) })]
			})
		},
		BulletedList: {
			label: "Bulleted list",
			defaultProps: { items: [{ text: "Add a list item" }] },
			fields: { items: {
				type: "array",
				label: "Items",
				arrayFields: { text: plainText("Item") }
			} },
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("ul", {
				className: puck_module_default.bulletedList,
				id: props.id,
				children: props.items.map((item, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", { children: item.text }, `${item.text}-${index}`))
			})
		},
		InlineForm: {
			label: "Inline form",
			defaultProps: {
				heading: "Stay in the loop",
				intro: "Get updates from the campaign.",
				submitLabel: "Submit",
				actionUrl: "#",
				fields: [{
					label: "Email",
					name: "email",
					type: "email"
				}]
			},
			fields: {
				heading: text("Heading"),
				intro: area("Introduction"),
				submitLabel: text("Submit label"),
				actionUrl: plainText("Action URL"),
				fields: {
					type: "array",
					label: "Fields",
					arrayFields: {
						label: plainText("Label"),
						name: plainText("Name"),
						type: {
							type: "select",
							label: "Type",
							options: [{
								label: "Text",
								value: "text"
							}, {
								label: "Email",
								value: "email"
							}]
						}
					}
				}
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("form", {
				action: props.actionUrl || "#",
				className: puck_module_default.inlineForm,
				id: props.id,
				method: "post",
				children: [
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("h2", { children: props.heading }),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { children: props.intro }),
					props.fields.map((field, index) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("label", { children: [field.label, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
						name: field.name || `field-${index}`,
						type: field.type
					})] }, `${field.name}-${index}`)),
					/* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
						className: puck_module_default.button,
						type: "submit",
						children: props.submitLabel
					})
				]
			})
		},
		ImageCaption: {
			label: "Image + caption",
			defaultProps: {
				image: null,
				caption: "Add an image caption"
			},
			fields: {
				image: mediaField("Image"),
				caption: text("Caption")
			},
			render: (props) => {
				const image = normalizeMedia(props.image);
				return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figure", {
					className: puck_module_default.imageCaption,
					id: props.id,
					children: [image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
						alt: image.alt || props.caption,
						src: image.url
					}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "Choose media" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("figcaption", { children: props.caption })]
				});
			}
		},
		Video: {
			label: "Video",
			defaultProps: {
				video: null,
				url: "",
				caption: ""
			},
			fields: {
				video: mediaField("Video", true),
				url: plainText("Video URL"),
				caption: text("Caption")
			},
			render: (props) => {
				const source = normalizeMedia(props.video)?.url || props.url;
				return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figure", {
					className: puck_module_default.video,
					id: props.id,
					children: [source ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("video", {
						controls: true,
						src: source
					}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "Choose a video" }), props.caption ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("figcaption", { children: props.caption }) : null]
				});
			}
		},
		Embed: {
			label: "Embed",
			defaultProps: {
				url: "https://example.com",
				title: "Embedded content"
			},
			fields: {
				url: plainText("Embed URL"),
				title: text("Title")
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("iframe", {
				className: puck_module_default.embed,
				id: props.id,
				src: props.url,
				title: props.title
			})
		},
		PayPal: {
			label: "PayPal button",
			defaultProps: {
				label: "Donate with PayPal",
				url: "#",
				amount: ""
			},
			fields: {
				label: text("Button label"),
				url: plainText("PayPal URL"),
				amount: plainText("Amount")
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				className: puck_module_default.payPal,
				id: props.id,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
					className: puck_module_default.button,
					href: props.url || "#",
					children: props.label
				}), props.amount ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: props.amount }) : null]
			})
		},
		MediaGallery: {
			label: "Media gallery",
			defaultProps: {
				heading: "From the fellowship",
				intro: "Add flyers, event moments, and artwork from Payload Media.",
				items: [
					{
						image: null,
						caption: "Add a gallery image",
						size: "large"
					},
					{
						image: null,
						caption: "Add a gallery image",
						size: "medium"
					},
					{
						image: null,
						caption: "Add a gallery image",
						size: "small"
					}
				]
			},
			fields: {
				heading: text("Heading"),
				intro: area("Introduction"),
				items: galleryItemsField
			},
			render: (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("section", {
				className: puck_module_default.mediaGallery,
				id: props.id,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
					className: puck_module_default.shell,
					children: [
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "h2",
							field: "heading",
							props,
							children: props.heading
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
							as: "p",
							className: puck_module_default.body,
							field: "intro",
							props,
							children: props.intro
						}),
						/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: props.items.map((item, index) => {
							const image = normalizeMedia(item.image);
							return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("figure", {
								"data-size": item.size,
								children: [image ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("img", {
									alt: image.alt || item.caption,
									src: image.url
								}) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: "Choose media" }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Editable, {
									as: "figcaption",
									field: "intro",
									props,
									children: item.caption
								})]
							}, `${item.caption}-${index}`);
						}) })
					]
				})
			})
		},
		ContentRow: {
			label: "Content row",
			defaultProps: {
				layout: "two",
				columns: [{ label: "Column 1" }, { label: "Column 2" }]
			},
			fields: contentRowFields,
			render: contentRowRender
		},
		Row: {
			label: "Row",
			defaultProps: {
				layout: "two",
				columns: [{ label: "Column 1" }, { label: "Column 2" }]
			},
			fields: contentRowFields,
			render: contentRowRender
		},
		RowOneColumn: {
			label: "1 column row",
			defaultProps: {
				layout: "one",
				columns: [{ label: "Column 1" }]
			},
			fields: contentRowFields,
			render: contentRowRender
		},
		RowTwoColumns: {
			label: "2 column row",
			defaultProps: {
				layout: "two",
				columns: [{ label: "Column 1" }, { label: "Column 2" }]
			},
			fields: contentRowFields,
			render: contentRowRender
		},
		RowLeftWide: {
			label: "left wide row",
			defaultProps: {
				layout: "leftWide",
				columns: [{ label: "Main column" }, { label: "Side column" }]
			},
			fields: contentRowFields,
			render: contentRowRender
		},
		RowRightWide: {
			label: "right wide row",
			defaultProps: {
				layout: "rightWide",
				columns: [{ label: "Side column" }, { label: "Main column" }]
			},
			fields: contentRowFields,
			render: contentRowRender
		},
		RowThreeColumns: {
			label: "3 column row",
			defaultProps: {
				layout: "three",
				columns: [
					{ label: "Column 1" },
					{ label: "Column 2" },
					{ label: "Column 3" }
				]
			},
			fields: contentRowFields,
			render: contentRowRender
		},
		RowFourColumns: {
			label: "4 column row",
			defaultProps: {
				layout: "four",
				columns: [
					{ label: "Column 1" },
					{ label: "Column 2" },
					{ label: "Column 3" },
					{ label: "Column 4" }
				]
			},
			fields: contentRowFields,
			render: contentRowRender
		},
		...campaignAltComponents
	}
};
function PublicPuckRender({ data }) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Render, {
		config: puckConfig,
		data
	});
}
//#endregion
export { styles_module_default$3 as C, init_react_import as E, PublicPuckRender, get_class_name_factory_default as S, __spreadValues$7 as T, List as _, Puck as a, useAppStoreApi as b, o as c, LoadedRichTextMenuInner as d, editableFieldsByType, useListOptions as f, Heading$1 as g, AlignLeft as h, Drawer as i, EditorInner as l, useAlignOptions as m, campaignAltTypesByPalette as n, nestedElementTypes, createUsePuck as o, useHeadingOptions as p, puckConfig, normalizeLexicalValue as r, fieldsPlugin as s, campaignAltDefinitions as t, LoadedRichTextMenu as u, SelectControl as v, __spreadProps$6 as w, useControlContext as x, useAppStore as y };
